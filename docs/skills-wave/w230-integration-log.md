# W230 集成记账:W228 一键成片整趟空跑时顶层把子步那句实话提上来

新基线 `cursor/w229-integration-7b3d`,tip **`497b09d`**(`git fetch` 后 `git rev-parse` 现取)。
**交接自称的 `73aec65` 是那条合入链的 merge 提交、不是分支 tip**——其后还有一个把份数与两格下限
校到 live 的 `docs` 提交 `497b09d`(它把索引份数校到 243、`FLOOR` 抬到 243、README 那格抬到 649);
按自称那个 SHA 起手会把 W229 自己那份记账件与两格 `FLOOR` 一起丢掉。
这已经是**连续第四槽**遇到同一形状(W222 遇 `91d3768`、W227 遇 `fa6ac3a`、W229 遇 `caccbda`、本槽遇 `73aec65`),
口径照旧:命令行里一个 SHA 都不从交接文里抄,先 `fetch` 再 `rev-parse`,自称与现取不符时以现取为准。
被合入支 `cursor/w228-compose-nested-note-4d1a` 现取 head **`f129b82`** 与自称同值,但同样是量出来的不是采信的。

本槽合**一条**已完成支,一次 `--no-ff` 且两个 parent 齐全:

| 序 | 被合入支 | 自称 head | 现取 head | merge commit | parents(现取) | 共同祖先(现取) |
|---|---|---|---|---|---|---|
| 1 | `cursor/w228-compose-nested-note-4d1a` | `f129b82` | `f129b82` | `6fa2c09` | `497b09d` + `f129b82` | `9b81c52`(W227 tip) |

`git cat-file -p 6fa2c09` 数出 **2 行 `parent`**,是真 `--no-ff` 不是快进;全程没用过 `--ours`,
没用过 `git checkout <old> -- .`,三处冲突没有一处是靠机械丢掉一侧收的场。

## 〇、叉点这一格与前几槽不同:**共同祖先恰是 W227 tip**

`git merge-base` 现取 **`9b81c52`**,与交接说的「W228 从 `9b81c52` 起手」逐字对上,
而 `9b81c52` 又恰是 `cursor/w227-integration-3c8f` 的现取 tip。即 **W228 是从我方这条集成线上一个节点直接叉出去的**,
不是像 W226/W223 那样从更早的某个波次叉出后与基线互不相识。

这带来一格与前几槽不同的成色:**W228 一侧相对叉点的每一处,基线都没有对着改过第二遍**——
W229 自 `9b81c52` 起只合了 W226(`cli.js`),而 W228 只动 `js/commands.js`,两侧产品面**零文件重叠**。
故它们自称的 **651 / 242**(W228)与 **649 / 243**(W229)在合完这棵树上**一个都不是答案**,
合完 live 是 **652 / 245**。

跳过的三件按口径一条没碰:`master` 没合、没有开第三条功能支、
交接明令不代修的三格(存量重复 id / `state-put` 逃生舱 / 选人双扣费)一处没动(§八、§九)。

合完整棵产品树相对叉点 `9b81c52` 只此两个文件,恰是两侧各自那份的并集:

```
git diff --numstat 9b81c52 HEAD -- js/ server.js cli.js mcp.js billing.js index.html css/
15	1	cli.js             ← 基线侧(W226,经 W229 合入)
8	1	js/commands.js     ← W228
```

`js/commands.js` **+8 −1** 与 W228 单独对着叉点的 numstat 逐格相同(该支产品面只此一个文件);
`js/`(除 `commands.js` 外)/`mcp.js`/`server.js`/`billing.js`/`index.html`/`css/` 零 diff。

## 一、四棵树机检:**`js/commands.js` 是 `P1 == B`**

四份 blob 逐文件现取(`B` = 叉点 `9b81c52`,`P1` = 我方 `497b09d`,`P2` = 对侧 `f129b82`):

| 文件 | B | P1 | P2 | tip | 成色 |
|---|---|---|---|---|---|
| `js/commands.js` | `31c689e5` | `31c689e5` | `ebcada01` | `ebcada01` | **`P1 == B`,git 整份取对侧** |
| `cli.js` | `d55233f0` | `7dccfca8` | `d55233f0` | `7dccfca8` | `P2 == B`,整份取我方 |
| `js/domain.js` | `8af2a73f` | `8af2a73f` | `8af2a73f` | `8af2a73f` | 三树全同,两侧都没碰 |
| `js/produce.js` | `b943a175` | `b943a175` | `b943a175` | `b943a175` | 三树全同,两侧都没碰 |
| `js/plans.js` | `bebe8606` | `bebe8606` | `bebe8606` | `bebe8606` | 三树全同,两侧都没碰 |
| `js/skills.js` | `f7c0baf0` | `f7c0baf0` | `f7c0baf0` | `f7c0baf0` | 三树全同,两侧都没碰 |
| `README.md` | `199514cf` | `18020a7e` | `6a4f712f` | `c429d40a` | 真并集(四份互不同) |
| `docs/skills-wave/README.md` | `32f7dec3` | `33189595` | `156422a3` | `157c9865` | 真并集(四份互不同) |
| `tests/unit.js` | `1f232b3e` | `d82f072e` | `45b82461` | `befe0007` | 真并集(四份互不同) |

**产品面唯一那个 `P1 == B` 是 `js/commands.js`**:我方自 `9b81c52` 起只动过 `cli.js`(W226),
`js/commands.js` 一个字没碰,git **整份取对侧、不做任何 hunk 级取舍**。
合得"干净"不等于合得对——这个文件一个冲突都没报,恰恰说明对侧在它里面带来的**每一处**都得逐条现取(§二)。

**`js/domain.js` / `js/produce.js` / `js/plans.js` / `js/skills.js` 那四行是 `P1 == B` 的退化形态,不是同一件事**:
它们三树 blob 全同,两侧谁都没碰,"整份取对侧"取回来的就是叉点原样。
只按"`P1` 与 `B` 的 hash 相等"筛会把这四个也筛进来,量的时候要连 `P2` 一起看——
真正需要逐条现取的 `P1 == B` 只有 `js/commands.js` 一个。

反过来 `cli.js` 是 `P2 == B`(整份取我方),故 W226 那道闸不会被对侧顶掉,但同样逐条现取过(§三)。

## 二、`js/commands.js` 整份取对侧后逐条现取

交接点名「`commands.js` 若整份取对侧,核 produce 提 note **且** 不要丢掉 W224 `smartReview` note」。
两格分开量。

### 2.1 后半格先机检:对侧那份里 W224 还在不在

`js/commands.js` 的 `P2` blob 相对 `B` 只多一处 hunk(`+8 −1`,`episode.produce` 的 `return` 那一句),
W224 那两档"一镜也没审"文案在叉点上就已经在(它随 W227 那条链进来),对侧没有对着改第二遍。
tip 上现取:

| 点名 | tip 上现取到的 | 在不在 |
|---|---|---|
| W224 `smartReview` note(点名档) | `js/commands.js:182` `'点名的 ' + picked.length + ' 镜一镜也没审:可审的镜需已出片、非终稿且在本集'` | ✅ |
| W224 `smartReview` note(整集档) | `js/commands.js:183` `'本集没有可审的镜头,一镜也没审:可审的镜需已出片、非终稿'` | ✅ |
| W228 produce 提 note | `js/commands.js` `episode.produce` 成功出口新增 `idle` 判据与 `out.result.note = …` | ✅ |

`git diff 9b81c52 HEAD -- js/commands.js` 输出**恰是 W228 那一处 hunk、一行不多**,
即整份取对侧没有顺带顶掉基线侧的任何东西。这是量出来的结论,不是"看起来没冲突"。

### 2.2 提 note 那一处的判据逐条现取

```
const out = ok({ steps, url: c.result.url });
const idle = c.result.fresh && steps.every(x => x.step === 'compose' || (x.result && x.result.note));
if (idle) out.result.note = steps.find(x => x.result && x.result.note).result.note;
```

| 点名 | tip 上现取到的 | 在不在 |
|---|---|---|
| 只在 `fresh` 时提 | `c.result.fresh &&`——合成那一步真跑了就有引擎提示可依赖,顶层不搭第二句 | ✅ |
| 每步都是 compose 或带 note | `steps.every(x => x.step === 'compose' \|\| (x.result && x.result.note))` | ✅ |
| 提的是子步已有的那句 | `steps.find(…).result.note`,不另拼第二句文案 | ✅ |
| 只在成功出口提 | 失败出口那一句 `return Object.assign({ ok: false, … })` 一个字没动 | ✅ |
| 计费面没动 | `cost()` / `nextOf(p, ep)` 原样,提 note 不经 `Tasks.run` | ✅ |

### 2.3 合完的树上真跑一遍(不是读源码猜)

`P1 == B` 不许只靠源级比对交差。在**合完的树上**加载真 `js/commands.js` + 真 `js/produce.js`
(`vm` 沙箱注入浏览器全局,被测的就是仓库里那两份源码),两档各跑一遍:

```
[档一 整趟空跑] ok=true 引擎实收=[] 后台面板=0
  steps      = generateVideos / smartReview / compose
  子步 note  = 本集没有待生成的镜头,一镜也没跑:2 镜已定稿 || 本集没有可审的镜头,一镜也没审:可审的镜需已出片、非终稿 || —
  顶层 note  = 本集没有待生成的镜头,一镜也没跑:2 镜已定稿
  digest 播 = 1 条:本集没有待生成的镜头,一镜也没跑:2 镜已定稿

[档二 真跑到活] ok=true 引擎实收=["batchGenVideos","autoSmartReview","composeVideo"]
  顶层 note  = undefined
  digest 播 = 0 条
```

三格读数都是在合完的树上现取的,不是从 W228 记账件里抄的:

- 档一引擎实收 **0**、后台面板 **0**——这一趟一次都没起来,零计费;基线上这一按用户读到 **0 句**,tip 上读到 **1 句**;
- 档一子步那两句里,第二句正是 **W224 的 `smartReview` note**——它在同一次真跑里露了头,
  即"produce 提 note"与"W224 note 没丢"这两格**是同一次现取同时证到的**,不是各读一遍源码拼出来的;
- 档二真跑到活时顶层 `note` 是 `undefined`、`digest` 一句不播,即提 note 没有变成恒播。

这段验证跑在本进程内(`node` + `vm` 沙箱),**没有起 `server.js`、没有往 shell 里 `export` 任何 `HUJING_*`/`MV_*`**,
故 §五之二 的 `cli.smoke` 不存在被劫端口的可能(W229 §五之二 那格教训按口径避开了,另加 `env -u` 兜底)。

## 三、`P2 == B` 那个文件:W226 那道闸逐条复核没被顶掉

`cli.js` 是整份取我方。先机检后半格:`git diff --numstat 9b81c52 f129b82 -- cli.js` 输出 **0 行**,
`cli.js` 的 `P2` blob 与 `B` 逐字节相同(`d55233f0`),W228 一侧在这个文件上一个字都没改,
故整份取我方不存在顶掉任何东西的可能。tip 上的 `cli.js` blob `7dccfca8` 与 `P1` 逐字节相同。

交接点名的保留项逐条现取:

| 点名 | tip 上现取到的 | 在不在 |
|---|---|---|
| W226 `shots-import` 唯一闸 | `cli.js:734–742` `renamed` 计数 + 撞 id 重发 `sh_` 前缀新 id | ✅ |
| 回报 `renamedIds` | `cli.js:747` 回执 `{ …, renamedIds: renamed }` | ✅ |
| W210 `force` | `episode.compose` 参数面仍 `pid,epid,force,ui`(`js/cmd-registry.js`) | ✅ |
| W219 生成侧闸 | `js/wf-core.js` `manualCmd` 单点判据仍在;`js/plans.js` 里 `expert.evolve` 字面 **0** 处;全仓 `manual: true` 仍 **1** 处 | ✅ |
| W223 镜头侧 `gone` 按 id | `js/domain.js` `const gone = hits.filter(h => !h.length).length;` 不是减法 | ✅ |
| W217 主体侧 `gone` 按 id | `ids.filter(id => !subs.some(…))` | ✅ |
| W221 按钮测 | `pipeline` 套件那条"整集全是过期终稿时按钮照旧挂着"在 tip 上现取到 | ✅ |
| W224 `targets` 出口 | `js/produce.js` 空审档 `{ pass: 0, retry: 0, manual: 0, targets: 0 }` | ✅ |

`js/domain.js` / `js/produce.js` / `js/plans.js` 三份 blob 四棵树全同,即这几格根本没有被顶掉的机会;
列在这里是因为交接点名了它们,机检 0 diff 也是一种现取。

## 四、冲突逐处怎么收的

三处冲突,一处没机械取侧。

### 4.1 `README.md` 第 611 行长行:**两侧都只改了数字**

那行 16939 字符。四份(`B`/`P1`/`P2`/tip)字节长度**逐份相同**,`sed 's/[0-9]\+/#/g'` 掩数字后:

```
掩数字后 B == P1 == P2 == tip :True
```

即两侧都没动散文,并集就是"同一份散文 + 数字按 live 定"。逐个数字对下来 **82 个数只有第 0 个动过**
(`B` 648 / `P1` 649 / `P2` 651),收成 live **652**。这与 W229 那槽同形(那槽 34196 字符、82 个数、也只动第 0 个),
与 W222/W225/W227 三槽的"一侧改数字另一侧插散文"不同,故没有用上求 opcode 分并集那一手。

收完做双向回验,两条都过:整份 `README.md` 掩数字后与 `P1`、与 `P2` **各自逐字节相同**——
即两侧的散文一个字都没丢也没多。

### 4.2 目录索引表:取并集后按波次号归位

我方带 1 行(w229),对侧带 1 行(w228)。取并集后按波次号排,
**w228 排在 w227 与 w229 之间**(追加表尾会当场红在"索引行序"那条)。两行逐行与各自源树 `cmp` 回验:
w229 一行与 `P1` 逐字节相同,w228 一行与 `P2` 逐字节相同。
份数字面两侧给的 243 / 242 都不是答案,merge 提交上按 live 收成 **244**,写完本文再抬到 **245**。

### 4.3 `tests/unit.js`:两处纯数字冲突

两处都是掩数字后两侧逐字节相同的纯数字位,按 live 收:
`['单元测试', 649 / 651 → **652**` 与 `const FLOOR = 243 / 242 → **245**`(merge 提交上是 244,本文落地后抬到 245)。
W228 那三条新用例(104 行)落在 `commands` 与 `release` 两个套件里,与我方 W226 那条的插入点不在同一处,
git 逐 hunk 合上,不是"两侧在同一插入点各追加一条、共用块尾"那个形状,故没有用上 W220 那三步手法。

**并集用 numstat 对了一遍账**:`B→P1` 是 `34 / 2`、`B→P2` 是 `104 / 2`、`B→tip` 是 `136 / 2`。
`34 + 104 = 138`,减去两侧都改过的那 2 行(4.3 那两个数字位各算了两遍)恰是 **136**,
即 tip 上一行不多一行不少,是真并集不是取侧。

## 五、数字:全部合完 live 重跑

合完先整跑一遍,让对账用例自己报差额——它当场报「实测 652,文档 649」与「期望 243,实际 244」,
按它报的订正;写完本文再抬份数即绿。一个数都不是从两侧记账件里抄的:

| 格 | W228 自称 | 基线(W229)自称 | **合完 live** |
|---|---|---|---|
| `unit` 总数 | 651 | 649 | **652** |
| `commands` | — | — | 47 → **50**(W226 那条 +1、W228 两条 +2) |
| `release` | — | — | 48 → **49**(W228 那条) |
| `domain` | — | — | **39**(两侧都没加) |
| `contract` | — | — | **140**(两侧都没加,复核 live 恰是它) |
| `plans` / `skills` | — | — | **17 / 95**(两侧都没加) |
| 记账件份数 | 242 | 243 | **245**(含本文) |
| `integration` | — | — | **147/147 全绿** |
| `cli.smoke` | — | — | **107/109**(见 §五之二) |

`648 + 1 + 3 = 652` 自洽:叉点 648,`P1` 相对叉点 +1,`P2` 相对叉点 +3,两侧各自的增量相加恰等于合完 live。

两格 `FLOOR` 按 live 抬:`tests/unit.js` 的 `FLOOR = 245`、`docs/skills-wave/README.md` 明写「共 245 份」,
与目录实况 245 份三方对齐;单元那格 `['单元测试', 652` 与 README 明写的 652 对齐。

**交接点名的两格套件增量都落在 tip 上**,按名点住不是按数点住:

- `commands` **+2**:`produce:整趟一步都没跑起来时顶层把子步那句实话提上来(digest 只认顶层 note,子步那句本来没有出口)`
  与 `produce:只要有一步真跑到活,顶层就不提子步那句(引擎自己会播,命令层不重复)`;
- `release` **+1**:`G1 一键处置:整趟一步都没跑起来时按钮按下去有回音(编排回执只在顶层播,子步那句本来没有出口)`。

### 五之二、`cli.smoke` 单独整跑

`tests/cli.smoke.js` 非并行安全,单独整跑(并按 W229 §五之二 那格教训带 `env -u HUJING_SERVER HUJING_CONFIG_DIR MV_*` 兜底):
**107/109**,两条失败:

| 失败条目 | 表现 | master 现跑对照 |
|---|---|---|
| `未登录 whoami → exit 3` | `exit=1` | 同名同表现 |
| `llm --json mock 链路` | `undefined` | 同名同表现 |

`master`(`9adcf0ff`)现跑 **51/53**,失败的就是这两条、名字与表现逐字相同,故是 master 同名失败不是本槽引入。
本槽的手工 live 验证跑在进程内沙箱、没起服务也没 `export` 任何 env(§2.3),
故这一遍不像 W229 那槽先跑出一片 `ECONNREFUSED` 再排查——**污染源这次是从一开始就没造出来的**。

## 六、名集按 `|` 切做多重集

四棵源树与 tip 的快照都在**全绿** commit 上取(648 / 649 / 651 / 652,名字后不会缀失败原因):

| 方向 | 新增 | 去掉 |
|---|---|---|
| `P1`(w229)相对 `B` | 1 | **0** |
| `P2`(w228)相对 `B` | 3 | **0** |

`P1` 那 1 条:`commands` 那条 `CLI shots-import:同 id 两行进不了分镜表`。
`P2` 那 3 条:`commands` 两条(顶层提子步那句 / 真跑到活就不提)、`release` 一条(G1 一键处置按下去有回音)。

tip 上**基线独有 0 条、两支新增 1+3 条全在、零多出**,`648+1+3=652` 自洽;
多重集直接对等式 `B + ΔP1 + ΔP2 == tip` 也成立(含计数,不只是集合),tip 上重名 **0** 条。
**本槽两侧「去掉」都是 0**,即没有 W222/W225 那种"等量替换掉一条断言方向相反的用例"的形状;
这一格是量出来是 0,不是没量。

## 七、治理面:一格未动

- `js/skills.js` **四棵树 blob 全同**(`f7c0baf0`),故 `gaps()` **20 键**一个没剥、
  **SK-04 / G-11 / G-13 原样开着没装清**(`G-11` 仍挂 `review.memoryFeedback`,`G-13` 仍挂 6 条),
  `skills` 套件 95/95 全绿。
- `GUARD_TOPICS` **19** 条 / `TOPIC_FLOOR` **19** / 花名册 **19**,`GUARD_TOPICS_CLOSED` **0** 条,一条没销号。
- `expert.evolve` 仍不进 playbook `steps`(`js/plans.js` 里该字面 0 处),全仓 `manual: true` 仍 1 处。
- `episode.compose` 参数面仍 `pid,epid,force,ui`。
- 本文零相对路径互引(记账件自足,不借道 markdown 相对链接指向同目录别的记账件)。

## 八、本槽收掉的一格与没做的三件

**收掉一格**:W229 §9.4 记的残留「一键成片子步 note 嵌套不播」——那格写的是
"`episode.produce` 顶层仍回 `{ steps, url }`,子步的 `note` 嵌在 `steps[].result.note` 里,
而 `digest` 只认顶层那一位",并点名"在飞的 W228 正是冲这一格去的"。
本槽合入 W228 后**这一格已收**:§2.3 那次 live 现取里,顶层 `note` 与 `digest` 播的那一句逐字相同。
W229 记账把它当残留是当时的实况,不是记错。

没做的三件,交接明令不代修,在 tip 上原样留着(逐条现取见 §九),两个方向的偷懒都没走——
既没顺手把它们捎带修了,也没把残留抹掉不写:

1. 存量重复 id 迁移;
2. `state-put` 逃生舱;
3. 选人双扣费。

另外两件按口径没碰:没合 `master`、没开第三条功能支。

## 九、交接:三格残留原样留着

### 9.1 未收(交接明令不代修):存量重复 id 迁移

W226 的闸只管**新进来的**导入。全仓现取**没有任何存量迁移实现**
(`dedupeShotIds` / `migrateShotIds` 之类一处都没有),故闸上线之前就已经躺在库里的同 id 多行不会被清。
形状与修好之后的对照:

- 闸后新导:同 id 两行 → `renamedIds: 1`,落库两个不同 id;
- 存量老数据:同 id 两行照旧躺着,点名该 id 仍是引擎实收两行、两笔 `video.gen`。

要收的话得单开一槽:落点是"读到分镜表时发现重复即改发新 id 并回报",而这会**改动已落库数据的寻址键**,
用户手上按老 id 记的脚本会失配,故不该顺手塞进集成槽里。

### 9.2 未收(交接明令不代修):`state-put` 逃生舱

`cli.js` 的 `CMD['state-put']` 是全量覆盖(`--force` 才放行),直接 `PUT /api/state`,
**中途不过 `shots-import` 那道闸、也不做任何镜头 id 唯一性校验**。故拿一份含同 id 两行的 `state.json`
灌进去,重复 id 照样落库。它是有意留的危险逃生舱(帮助文本明写"危险操作"),
但它意味着 §9.1 的存量重复**还能被再造出来**——收 9.1 那一槽得连它一起判,不然修完还会长回来。

### 9.3 未收(交接明令不代修):同 id 两镜时选人双扣费

两端选人闸仍按**行**筛:`cli.js` 与 `js/commands.js` 都是
`(ep.shots || []).filter(s => !s.final && ids.has(s.id) …)`。同 id 两行在库时点名一次两行都跑,
一个 id 收两笔 `video.gen`,且两笔写回的都是首行(`findShot` 取首行,第二行永远寻不着)。
本槽 `js/commands.js` 虽是"整份取对侧",但对侧那份在这一处与叉点逐字节相同——
**W228 没有顺手改它,本槽也没有**(§2.1 那处 hunk 只有 `episode.produce` 的成功出口)。

W226 修完之后这一格的成因已经变窄:新导入这条路进不来重复 id,
故 tip 上还能撞见这个形状的,**主要就是 §9.1 的存量数据与 §9.2 那条逃生舱灌进去的**。
这一格不许改成"选人按 id 去重只跑一行"来假过——那会把用户点名两行的正常子集也一并砍掉。

### 9.4 给下一个人的三格

- **基线 tip 连续四槽都比交接自称多一个 `docs` 提交**(W222 遇 `91d3768`、W227 遇 `fa6ac3a`、
  W229 遇 `caccbda`、本槽遇 `73aec65`)。这已经是稳定形状不是偶发:合入链最后那个校数字的 `docs` 提交,
  交接文往往写的是它前面那个 merge 提交。照旧 `fetch` + `rev-parse`,别从交接文抄 SHA。
- **叉点恰是我方上一个集成 tip 时,`P1 == B` 那个文件的风险反而最低但仍要量**(§〇、§一):
  被合入支从我方线上直接叉出去,两侧不打照面,git 整份取对侧取回来的就是"叉点 + 对侧那一处";
  但"风险低"是量出来的(`git diff B P2` 只此一处 hunk),不是从拓扑推出来就可以不看的。
- **`P1 == B` 的两格点名要在同一次真跑里同时证**(§2.3):交接点的是"提 note **且** 不丢 W224 note",
  分两次读源码各证一格,证不到"它们在同一条执行路径上共存";
  一次空跑里子步那句正是 W224 的、顶层那句是 W228 提上来的,两格一次证完。
