# W234 集成记账:W232 除 `shots-import` 外的镜头 id 写入口现盘(停工位成立,只钉一句注释)

一条槽并进同一条集成线。被合入支 `origin/cursor/w232-other-import-shot-ids-4b7d`,产品面**行为零 diff**:
只在 `js/wf-core.js` 的 `W.blankShot` 上方加一段注释,把「镜头 id 是唯一寻址键、只认注入的 uid、
外来 id 只有 `shots-import` 一条透传路径且它自带闸」三句写成源级锚点。

- merge 提交:`3a35165`,真 `--no-ff`,两个 parent 都是 `fetch` 后 `rev-parse` **现取**
  - `P1` = `bb0653f2` —— `origin/cursor/w233-integration-3d7a` 现取 tip
  - `P2` = `0238268` —— `origin/cursor/w232-other-import-shot-ids-4b7d` 现取 head(自称同值)
- 叉点 `B` = `merge-base` 现取 `964da7bd`,恰是 W230 集成 tip(被合入支从我方线上直接叉出去)
- 全程没用 `--ours`、没用 `checkout <old> -- .`;`master` 没合、没开第三条功能支

---

## 〇、交接自称的 tip 又是合入链中间那个 merge —— 连续第六槽

交接文给的 W233 merge 自称 `322cc2b`。现取 tip 是 `bb0653f2`,`322cc2b..bb0653f2` 之间还有一个
`docs` 提交,把记账件份数与 `FLOOR` 从 246 校到 247。按自称那个 SHA 起手,会把 W233 自己那份记账件
连同两处下限一起丢掉。

这已是**连续第六槽**遇到同一形状(W222 遇 `91d3768`、W227 遇 `fa6ac3a`、W229 遇 `caccbda`、
W230 遇 `73aec65`、W233 遇 `6fa2c09`、本槽遇 `322cc2b`)。规律是稳定的:**连续多槽自称的都是链中间的
merge 提交**,因为交接文是在 merge 落地那一刻写的,而校数字的 `docs` 提交总在其后。口径照旧——
先 `fetch` 再 `rev-parse`,一个 SHA 都不从交接文抄。被合入支那一侧 `0238268` 现取与自称同值。

---

## 一、四棵树机检:`js/wf-core.js` 是**唯一**一个真形态的 `P1 == B`

按 blob hash 逐文件比 `B` / `P1` / `P2` / tip 四棵树:

| 文件 | `B` | `P1` | `P2` | tip | 判定 |
|---|---|---|---|---|---|
| `js/wf-core.js` | `0e678ef2` | `0e678ef2` | `6170c19f` | `6170c19f` | **`P1 == B` 真形态**,git 整份取对侧 |
| `cli.js` | `7dccfca8` | `5949595f` | `7dccfca8` | `5949595f` | `P2 == B`,整份取我方 |
| `tests/integration.js` | `22c3954d` | `1afb4600` | `22c3954d` | `1afb4600` | `P2 == B`,整份取我方 |
| `tests/cli.smoke.js` | `c34457a2` | `fcbbc8c3` | `c34457a2` | `fcbbc8c3` | `P2 == B`,整份取我方 |
| `README.md`(根) | `c429d40a` | `43b0d8eb` | `c429d40a` | `43b0d8eb` | `P2 == B`,零冲突 |
| `docs/AI助手接入指南.md` | `f1c82dad` | `9084447a` | `f1c82dad` | `9084447a` | `P2 == B`,零冲突 |
| `js/skills.js` / `js/commands.js` / `js/produce.js` / `js/domain.js` / `js/plans.js` / `js/cmd-registry.js` / `server.js` / `js/store.js` | 同 | 同 | 同 | 同 | **四树全同(退化形态)** |
| `tests/unit.js` | `9f2d6bb2` | `b706f1f3` | `4b72b8b3` | `99d8d60c` | 两侧改同一行,tip 既非 `P1` 也非 `P2` |
| `docs/skills-wave/README.md` | — | — | — | — | 两侧都改,取并集 |

要点是**真形态与退化形态得分开数**:按 hash 筛「`P1 == B`」会一次筛出九个文件,但其中八个是四棵树全同、
对侧什么都没带来,真正需要逐条现取的只有 `js/wf-core.js` 一个。量的时候必须连 `P2` 一起看,
只看 `P1 == B` 会把八个空壳当成八处风险。

`git diff --numstat B P2` 全量只有四个文件:`docs/skills-wave/README.md` **+2 −1**、
`docs/skills-wave/w232-other-import-shot-ids.md` **+282 −0**(新记账件)、`js/wf-core.js` **+6 −1**、
`tests/unit.js` **+1 −1**(只有 `FLOOR` 那个字面)。产品面就 `js/wf-core.js` 一个文件。

---

## 二、「行为零 diff」不是抄交接文,是量出来的

被合入支自称「整段是注释、零行为改动」。这句话按两条独立判据现取,都在**合完的树上**对 `B` 做:

**判据一 —— 行级 opcode 里不许有可执行语句。** 求 `B` 与 tip 的 `js/wf-core.js` 行级最长公共子序列,
非 `equal` 的段共 **7 行**(1 `del` + 6 `ins`),逐行打印后**每一行都以 `/*` 或 `*` 起头**,
差异段里的可执行语句条数 **0**。删掉的那一行是原来的单行注释 `/* 新建分镜空白结构… */`,
新增六行是同一段注释展开成多行块注释。

**判据二 —— 剥掉全部注释后逐字节比。** 把两份源码的块注释与行注释整体剥掉、压掉空行后,
`B` 与 tip **逐字节相同**(43008 vs 43008 字符)。

两条判据是独立的:判据一证「改动落在注释行上」,判据二证「注释之外一个字节都没动」——
只有判据一时,一行里注释后面跟着代码这种形状漏得掉。

落地的注释本身逐句现取在场(`js/wf-core.js` `W.blankShot` 上方):唯一寻址键那句点名了
`findShot / shot-set / 审片按 shotId 回写 / 点名子集`,只认注入 uid 那句点名了浏览器十一个入口与两端 LLM
拆镜,外来 id 那句点名了 `cli.js shots-import` 是唯一透传路径且**自带落库前的唯一闸**。

---

## 三、`P1 == B` 时整份取对侧:注释在,而它声称的事也真成立

git 把 `js/wf-core.js` 整份取了对侧,合得"干净"不等于合得对。这一槽的特殊之处在于,
对侧带来的是一段**声称**——注释说「id 只认注入的 uid」「其它入口都不接外来 id」。
源码里读到这句话,和这句话是真的,是两件事。故在合完的树上真跑一遍:

```
[5] blankShot 铸造(W232 注释所声称的那件事)
  连铸 5000 次去重后                             5000
  id 只认注入的 uid(外来字段进不来)                    true
```

连铸 5000 次去重后仍 5000(铸造层不撞);注入 `() => 'sh_FIXED'` 即得 `sh_FIXED`、
返回结构里没有任何外来 id 字段的落脚点(通道层没有第二条道)。注释说的两层就是这两句。

---

## 四、`P2 == B` 那几个文件:闸与声明一处没被顶掉

`cli.js` 反向是 `P2 == B`(机检对侧 0 行 diff),git 整份取我方。交接点名要核的两格逐条现取:

- **W226 `shots-import` 唯一闸**在 `cli.js` 733–747:`taken` 在 `ep.shots =` 赋值**之前**建集、
  撞 id 走 `do…while` 重发 `sh_` 新 id、`taken.add` 收口、回执带 `renamedIds`。
- **W231 `state-put` 三处声明**全在,同一句锚点「镜头 id 唯一性由调用方自己保证」:
  `cli.js` help 串、根 `README.md` API 表与命令总览、`docs/AI助手接入指南.md`,各在近旁点名 `shots-import`。
- **`state-put` 没被顺手补闸**:落库路径上无 `taken` / 无 `renamedIds` / 无按 id 建集的动作,
  仍是 `--force` 才放行。交接明令的「不要给 state-put 补闸」结构性成立。
- **选人那一侧 `ids.has(s.id)` 按行筛一个字没动**。

`tests/integration.js` 与 `tests/cli.smoke.js` 同为 `P2 == B`,W231 那 5 条测原样在(名集一节量了)。
`server.js` 与 `js/store.js` 四树全同,即 `PUT /api/state` 那个端点也没被顺手设闸。

---

## 五、冲突逐处怎么收的(两个文件三处,一处没机械取侧)

### 5.1 `docs/skills-wave/README.md` 份数行:两侧都只改了数字

`B` 245 / `P1` 247 / `P2` 246。掩掉数字后三份逐字节相同——是"两侧都只改数字"的形状,
不是"一侧数字一侧散文"。按合完 live 收:merge 提交那一格目录实况 248,故收 **248**;本文落地后 **249**。

### 5.2 `docs/skills-wave/README.md` 索引表:并集后按波次号归位

我方那侧带 w231、w233 两行,对侧带 w232 一行。取并集后按波次号排成 231 / 232 / 233——
**顺序不是随手排的**:`contract` 里「索引行序」那条钉住波次号沿表往下不许倒退,
直接把对侧那行接在表尾会当场红。

并集用行文本多重集对账,不靠肉眼:

```
index rows  B/P1/P2/tip: 245 247 246 248
rows missing from tip: 0 | rows in tip from nowhere: 0
row delta  P1 +2 -0 | P2 +1 -0
accounting 245+2+1 = 248 vs tip 248
```

零缺失、零多出、两侧「去掉」各 **0**,`245 + 2 + 1 = 248` 与 tip 相等。

### 5.3 `tests/unit.js` `FLOOR`:**对侧下限比我方低**,这一槽的新形状

`B` 245 / `P1` 247 / `P2` **246**。与 W233 那槽正好**反向**:那次是对侧抬高了分母、我方那侧给的是旧分母,
照抄我方即把新测变成删了也不红;这次是**对侧那个数比我方还低**,照抄对侧等于把棘轮往回拧一格。
两个方向的坑不是同一个,但都通向"下限低于实况"。

危险到什么程度是量出来的,不是推出来的。在合完的树(目录实况 248)上跑四档反事实:

| 明写份数 | `FLOOR` | `contract` 结果 |
|---|---|---|
| 246(连带取对侧) | 246 | **红 1**:三方对齐那句,期望 246 实际 248 |
| 247(连带取我方) | 247 | **红 1**:同上,期望 247 实际 248 |
| 248(live 正确) | 246(只 `FLOOR` 抄对侧) | **140/140 全绿** |
| 248(live 正确) | 247(只 `FLOOR` 抄我方) | **140/140 全绿** |

**最要紧的是后两行**:只要明写份数收了 live,`FLOOR` 照抄哪一侧都一片绿。因为棘轮判的是 `>=`,
而「差额有上限」那条的 `SLACK` 是 3,落后 1–2 格还在预算之内。也就是说这处冲突**没有任何判据拦得住抄**,
抄完的后果是棘轮静默停在旧水位,那 1–2 格差额就是往后能被静默删掉的记账件条数。故按 live 收 **248**,
本文落地后同轮抬到 **249**。

`tests/unit.js` 的 numstat 对账:`B → P1` **+61 −4**、`B → P2` **+1 −1**、`B → tip` **+61 −4**——
对侧那一处改动整个落在我方也改过的同一行上,并集在行级上不产生额外增删,数字自洽。

---

## 六、数字:全部合完 live 现跑

| 套件 | live | 备注 |
|---|---|---|
| `unit` | **653 / 653** | 全绿 |
| `integration` | **148 / 148** | 全绿 |
| `cli.smoke` | **111 / 113** | 单独整跑 `env -u HUJING_SERVER` |
| `contract`(unit 子集) | **140 / 140** | 全绿 |
| 记账件 | **249** | merge 提交那一格 248,本文落地后 249 |

`cli.smoke` 失败两条,与 `master`(`9adcf0ff`,现跑 51/53)**同名同表现**:

- `未登录 whoami → exit 3 | exit=1`
- `llm --json mock 链路 | undefined`

冒烟一律单独跑并带 `env -u HUJING_SERVER`:手工 live 验证会往 env 里塞服务地址,与冒烟同壳跑会把它劫到错端口。

**交接自称的数字要分开看。** 被合入支自称 unit 652 未涨、记账 246,**两个都不是答案**。
W233 自称 unit 653、`integration` 148、`cli.smoke` 111/113、记账 247——**前三个与 live 相等,
但那是被合入支零新测撞出来的巧合,不是可以照抄的理由**;唯一不等的正是记账件(自称 247,live 249),
而它恰好也是唯一一个抄了会错的格。三个数对、一个数错的自称,比全错的自称更容易骗过复核。

---

## 七、名集按 `|` 切做多重集:被合入支净增 0 是**量出来的**

四棵源树与 tip 快照都在全绿状态上取名集,按 `|` 切出用例名,排序后保留重复(多重集,不退化成 Set):

| 套件 | `B` | `P1` | `P2` | tip | `P1` 增删 | `P2` 增删 | 对账 |
|---|---|---|---|---|---|---|---|
| `unit` | 652 | 653 | 652 | 653 | **+1 −0** | **+0 −0** | `652+1+0 = 653` 自洽 |
| `integration` | 147 | 148 | 147 | 148 | **+1 −0** | **+0 −0** | `147+1+0 = 148` 自洽 |
| `cli.smoke` | 109 | 113 | 109 | 113 | **+4 −0** | **+0 −0** | `109+4+0 = 113` 自洽 |

三个套件都是 tip **零缺失、零多出**,两侧「去掉」各 **0**。

**「被合入支零新测」这句话是量出来的,不是从『它只改了注释』推出来的。** 推理会漏掉两种形状:
改注释的同轮顺手删一条测(`−1` 而 `+1`,计数不动)、或改一条既有用例的名字(等量替换,计数也不动)。
多重集的 `+0 −0` 两个方向同时钉住,才排得掉这两种。名集净增 **0**,与计数不变互为独立证据。

我方那侧带来的 6 条(W231 的)按名点住:

- `unit` +1:`commands · CLI state-put 逃生舱:整树原样落库(同 id 两镜照落两行),三处契约明写唯一性归调用方`
- `integration` +1:`state 写入端点不做领域校验:同 id 两镜原样落库(唯一闸只在 shots-import 那条导入路上)`
- `cli.smoke` +4:`shots-import 唯一闸在真链路上也在:撞 id 改发新 id 并回报 renamedIds` /
  `state-put 整树原样落库:同 id 两镜照落两行(闸修好的表被逃生舱一把打回)` /
  `state-put 缺 --force → exit 2(整树覆盖唯一的那道闸)` /
  `重复 id 落库后只有首行寻得着:shot-set 改的永远是第一行(第二行没有入口够得着)`

交接文说我方相对 W230 是「+5 条测」,现取是 **6 条**(1 + 1 + 4),按名逐条列在上面。

---

## 八、合完的树上真跑一遍:闸、逃生舱、铸造三格同证

起真 `server.js` + 真 `cli.js`,数据目录与配置目录都在临时目录(`MV_DATA_DIR` / `HUJING_CONFIG_DIR`),
不碰仓库 `data/`,不往 env 里留污染源:

```
[1] shots-import 同 id 两行 —— 闸应改发新 id 并回报 renamedIds
  回执      {"episode":"ep_…","imported":3,"total":3,"replaced":true,"renamedIds":1}
  落库 id    ["sh_dup","sh_mtdc76pc_7771549e","sh_solo"]  唯一数 3/3
  首行仍是用户给的 sh_dup                          true

[2] state-put 缺 --force —— 唯一那道闸应 exit 2
  缺 --force exit                           2

[3] state-put --force —— 整树原样落库,同 id 两行照落(不设闸)
  带 --force exit                           0
  落库 id    ["sh_dup","sh_dup"]  唯一数 1/2

[4] 整表重导 —— 闸把逃生舱灌进去的重复又收回唯一
  回执      {"episode":"ep_…","imported":3,"total":3,"replaced":true,"renamedIds":1}
  落库 id    ["sh_dup","sh_mtdc76z6_452f07a3","sh_solo"]  唯一数 3/3
```

一次跑同时证四格:闸在(`[1]`)、逃生舱仍不设闸且行为一格没动(`[2]` `[3]`)、
**闸能把逃生舱灌进去的重复收回来**(`[4]`)、注释所声称的铸造层唯一性成立(第三节 `[5]`)。
分几次读源码各证一格,证不到这些事在同一条执行路径上共存。

`[3]` → `[4]` 也是「因果没解除」那格的现跑复现:闸刚把表修成唯一数 3,逃生舱一把打回唯一数 1,
再重导又回到 3。**存量重复 id 与逃生舱这两格互为因果,得同槽收**——本槽按交接口径一格都不代修。

三处声明现取:`cli.js help` 命中 1、根 `README.md` 命中 1、`docs/AI助手接入指南.md` 命中 1。
**这里记一手**:`cli.js help` 打在 **stderr** 上,只读 `spawnSync` 的 `stdout` 会读成 0 命中,
误判成"声明丢了"。核文档声明这类判据,取输出时得把两个流都收上。

---

## 九、治理面:一格未动

- `Skills.gaps()` **20 键**一个没剥:`G-08 G-15 G-09 G-01 G-02 G-12 G-13 G-04 G-10 S-01 S-03 G-07 G-14 G-05 S-04 S-05 G-03 G-11 S-06 S-07`
- **SK-04 / G-11 / G-13 原样开着**,`CLOSED` 仍 0 条
- `GUARD_TOPICS` 花名册与 `TOPIC_FLOOR` 仍 19
- `episode.compose` 参数面仍 `pid,epid,force,ui`(`js/cmd-registry.js` 四树全同)
- `js/skills.js` / `js/commands.js` / `js/produce.js` / `js/domain.js` / `js/plans.js` 四树 blob 全同,
  故 W228 顶层提 note、W224 `smartReview` 那句、`emptyBatchNote` 四堆按 id、plans 过滤、按钮测**不可能被剥**;
  仍逐条现取核过一遍,没拿"四树全同"顶替核对

---

## 十、交接:一格已收,三格原样留着

**本槽收掉的一格。** W233 记的残留「在飞的 **W232**(其它导入 id 闸,从 W230 出)」本槽已收:
判定是**停工位成立**——全仓 shot-id 铸造点只 5 个、外部 id 只有 `shots-import` 一条透传路径且它自带闸,
故**不设第二道闸**,只把这个结论钉成 `W.blankShot` 上的源级注释。行为一格没动。

**未收(交接明令不代修),三格:**

1. **存量重复 id 迁移** —— 全仓零迁移实现。收它要改已落库数据的寻址键,不该塞进集成槽。
2. **`state-put` / `PUT /api/state` 不校验** —— W231 已判定为**契约而非漏洞**(整树原样落库、
   唯一性归调用方,三处声明在册),本槽不翻案、不补闸。但**因果没解除**:逃生舱还能把重复再造出来
   (第八节 `[3]` 现跑),故它与第 1 格得同槽收。
3. **同 id 两镜时选人双扣费** —— `ids.has(s.id)` 按行筛一个字没动,成因仍是存量与逃生舱灌进去的那批。

**按口径跳过的:** `master` 没合、没开第三条功能支、没给 `state-put` 补闸、没做存量静默迁移、
没改选人双扣费。两个方向的偷懒(该核的不核 / 不该改的顺手改)都没走。
