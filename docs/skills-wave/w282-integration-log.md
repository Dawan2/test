# W282 把 W281 并进 W280 集成线:一次 `--no-ff`,集成线上第十一条「快进形」

> 我方尖(现取):`origin/cursor/w280-integration-9c17` = `d6eabbcb95d6dd6c1b884160892367f2d9d56ed6`
> 对侧(现取):`origin/cursor/w281-landed-seat-8f31` = `4d35e8e262a00a5646d846aac31e54e63db05e4a`
> 本支:`cursor/w282-integration-896d`,merge commit `e7c3fda9ecf015ea353ca81e04f47cd8728ed6cc`
> merge parents:`d6eabbcb95d6dd6c1b884160892367f2d9d56ed6` + `4d35e8e262a00a5646d846aac31e54e63db05e4a`

## 一、形状:快进形第十一次,而这一次两个结构性空集又叠了一层

`git merge-base` 现取 `d6eabbcb95d6dd6c1b884160892367f2d9d56ed6`,**恰等于本支起点**,即对侧确是从我方现取尖上叉出去的、
其间我方一条提交没走(`P2..P1` 空)。对侧自称「从 `d6eabbc` 起支、HEAD `4d35e8e`」两处现取核过、逐字节相符,
自称尖是链尾(`P1..P2` 两条提交:`eaa1523` 加测、`4d35e8e` 记账)。

仍走 `--no-ff` 留下合点。于是树面照旧**恒真**:`tree(M)` 与 `tree(P2)` 同为 `cd1b0ef1395b94ea72173ad8384b7630e3e73695`,
`git diff M P2` 空集、零冲突。这两格**一个结论都不承载**——快进形下它们不可能不成立。

本槽在此之上还叠了第二个空集:**核产品面两侧双向零 diff**。`git diff M P2 -- js/ cli.js` 与
`git diff M P1 -- js/ cli.js` **两条都空**,即「核产品面相对对侧零改写」这句话在本槽同样是恒真的——
对侧本就只加测。合起来看,树面、产品面两格都说不出话,**取证全部压在变异与 live 上**。
数数那一面这次倒是说得出话(对侧净加 2 条,不同于 W280 那次的 `+0`),见 §四。

## 二、并进来的是什么(逐处现取核过)

对侧改的五个文件:`tests/unit.js`(+167/−2)、`README.md`(断言条数 696→698)、
`docs/skills-wave/README.md`(索引补一行、份数 295→296)、`w178-topic-floor-unlist.md`(花名册补一行)、
新增 `w281-landed-seat-success-gate.md`。**`js/` 与 `cli.js` 一个字节没动。**

加的两条判据(都在 `commands` 套件):

1. **行为面六档**——夹具的形状是「取行/取位**成功**,而本轮引擎才失败」:先钉住这个前提
   (失败话术必须是引擎那一句;报「镜头不存在」/「主体不存在」说明夹具压根没跑到引擎),
   再钉 `landed` 恒等于磁盘上真到手的行数/位数、且永不超过 `ok`。六档覆盖
   CLI `gen-episode`(视频与补底图两台引擎各一档)、`exec episode.generateVideos`、
   `exec subject.generateImage`、浏览器两条批量。落库面一律读 `cliDisk` 那份 clone 语义夹具。
2. **源级锚**——四处循环各判「`seats.add(` 只此一处 + 引擎在前座位在后」,`gen-episode` 另钉
   「排在 `if (r.ret) throw r.ret;` 之后」,浏览器批量视频那处钉两句派生关系
   (`landedRows` 按 `Store.shotVideoReady` 筛、`seats` 由 `landedRows` 派生)。

同轮登记护栏主题 `landed-seat-order`(锚点 `landedRows` + `seats.add(`)、花名册补行、`TOPIC_FLOOR` 19→**20**。

## 三、变异:快进形的空 diff 到底有没有带东西进来

树面与产品面两格都恒真,所以「有没有真收到一道保证」只能拿变异量。**五手全部本槽现跑**,
每手改产品面、跑整份 `unit` 后 `git checkout --` 复原(复原后 worktree 现取干净):

| 手 | 做的什么 | 现取红数 | 报错句(原样抄出,取行为面那一句) |
|---|---|---|---|
| M1 | `cli.js` `gen-episode`:`seats.add(seat)` 提到取行之后、开跑之前 | **红 2** | `本轮引擎失败那一行不许占座位(提前登记会报 3,而磁盘上只有两行有片):首行:/uploads/gen/v1.mp4 \| 第二行:无片 \| 第三行:/uploads/gen/v3.mp4:期望 2,实际 3` |
| M2 | `cli.js` `exec episode.generateVideos`:座位提到开跑之前 | **红 2** | `exec 那条批量同形:失败那一行不占座位:…:期望 "2/2",实际 "2/3"` |
| M3 | `cli.js` `exec subject.generateImage`:座位提到开跑之前 | **红 2** | `本轮生图失败那一位不许占座位:A-首位:/uploads/img/g1.png \| B-第二位:无图 \| C-第三位:/uploads/img/g3.png:期望 "2/2",实际 "2/3"` |
| M4 | `js/commands.js` `subject.generateImage`:座位提到 `await` 之前 | **红 2** | `浏览器那一端同形:没出图那一位不占座位:…:期望 "2/2",实际 "2/3"` |
| M5 | `js/commands.js` `episode.generateVideos`:`seats` 改按 `pend` 算 | **红 2** | `浏览器批量视频那一端:失败行不占座位:剧情1:/uploads/gen/sh0.mp4 \| 剧情2:无片 \| 剧情3:/uploads/gen/sh2.mp4:期望 "2/2",实际 "2/3"` |

五手的第二句红一律来自源级锚,各自点名犯规那一处
(`cli.js gen-episode` / `cli.js exec episode.generateVideos` / `cli.js exec subject.generateImage` /
`js/commands.js subject.generateImage` / 浏览器批量视频那句派生),**没有一手落到同一句话上**;
整份 `unit` 五手均为 `696/698 PASS, 2 FAIL`,报错句与对侧记账件逐字节相符,一手没盲抄。

### 本槽真正承载结论的那一格:同一手在起点上现跑

M1 就是 W280 §四 M6 那一手。为把「快进形 diff 空集」和「什么都没收到」分开,**在起点 `d6eabbc` 上另开
干净 worktree 把同一手原样再跑一遍**:现取 **`696/696 PASS, 0 FAIL`**。

于是这一格有了两侧读数:**同一手变异,起点红 0 / 合完红 2**。W279/W280 连续两槽记的那句残留
(「座位只在本轮真成功后才登记」提前登记红 0)按这个读数收掉——**不靠任何一侧的文档口径**,
而这也正是本槽唯一说得出「合进来一道真保证」的证据:树面和产品面都是空集,只有它不是。

## 四、反事实:这道保证能不能被静默删掉

| 手 | 做的什么 | 现取红数 | 红在哪 |
|---|---|---|---|
| CF1 | 只删两条新用例,文档与四处下限一个字不改 | **红 4** | README 三方对账(实测 696 / 文档 698)+ 三套件下限 + 护栏主题失联(点名 `landed-seat-order`)+ 护栏抽样前提 |
| CF3 | 只删源级锚那一条,行为面留着 | **红 4** | 同 CF1,数字层两条按 697 报,护栏两条照旧 |
| CF2 | 删两条 + `README.md` 改 696 + 单元 `FLOOR` 改 696 + `TOPIC_FLOOR` 改 19 + 花名册那行删掉 | **红 3** | 护栏主题失联 + 抽样前提 + 销号判词(「撤掉最后一条并把下限一并改小,判词须点名 `landed-seat-order`」) |

**CF2 是本槽最实在的一格。** W280 §五那次同形改法(删掉判据 + 文档与 `FLOOR` 齐改小)现取**红 0**——
`landed` 这一族当时只有数数那一层网,W278 §四 M8 量到的「全族零源级判据」就是这个意思。
本槽把源级锚登记进护栏主题之后,同形的「四处齐改小」仍**红 3**:要真拆掉它,
只剩「在源码里把主题搬进 `GUARD_TOPICS_CLOSED` 并写下闭合理由」这一条路,那是留痕动作,不是静默删除。
CF4(显式销号 → 红 0)本槽照对侧记账原样收下、未另跑一遍,如实记在 §七。

## 五、live 数字:八格逐个现跑,两侧自称一个没盲抄

明令点名「W280 自称 unit 696、W281 自称 698,禁止抄 696」。两个自称数都各自现跑核过:
**起点干净 worktree 现取 696/696、合完现取 698/698**,自称与实况相符,但记的是实况。

| 面 | 起点 `d6eabbc` 现取 | 本槽 `e7c3fda` 现取 | 差 |
|---|---|---|---|
| `unit` | 696/696 | **698/698** | +2 |
| `commands` | 59 | **61** | +2 |
| `contract` | 153 | **153** | 0 |
| `domain` | 44 | **44** | 0 |
| `skills` | 104 | **104** | 0 |
| `issues` | 22 | **22** | 0 |
| `integration` | 152/152 | **152/152** | 0 |
| `cli.smoke` | 115/117 | **115/117** | 0 |

24 个套件逐格点数求和 `= 698`,与总行一致。

**名集多重集对账**(两侧都在干净整树上取——W280 §六记过那道值,基线用「合完的 README + 旧的 unit.js」
这种混树会当场把对账判成红,故两侧各自 `git checkout -- .` 后现跑):**`+2 −0`**,`696 + 2 = 698` 自洽。
进来的正是那两条:

- `commands · 座位只在本轮引擎真成功之后才登记:取行/取位成功而本轮生成失败的那一轮,ok 不计、landed 也不占这个座位`
- `commands · landed 那一族的源级锚:四处循环里的座位登记都排在本轮引擎之后,浏览器批量视频那一处仍由「真就绪的行」派生`

一条没出。与 W280 那槽(对侧原地重写、`+1 −1`、数数面整格空掉)不同,本槽这格是**说得出话的**:
「旧数 + 新增 = live」这句对账此刻不退化成恒真,因为对侧真加了条数。

`cli.smoke` **单独整跑**,`env -u HUJING_SERVER` 加 `MV_*` 隔离;两条失败:
`未登录 whoami → exit 3 | exit=1` 与 `llm --json mock 链路 | undefined`;
同 env 下 `master` `9adcf0f` 独立 worktree 现跑 **51/53**、失败两条**同名同表现**,按纪律允许。
`e2e` 未跑(明令)。`node --check` 过:`tests/unit.js`、`cli.js` 与 `js/` 全部文件。

## 六、棘轮、护栏主题与花名册

**四者对齐到 20**(明令点名不许仍写 19,现取逐个数出):

| 面 | 现取 |
|---|---|
| `GUARD_TOPICS` 在册 | **20**(末条 `landed-seat-order`) |
| `GUARD_TOPICS_CLOSED` | **0** |
| `TOPIC_FLOOR` | **20** |
| `w178-topic-floor-unlist.md` 花名册行数 | **20** |

在册集合与花名册集合**逐号相等**(双向差集都空)。

五格棘轮:单元 `FLOOR` **698**(对侧抬到 live)、集成 **152**、冒烟 **117**、
记账件 `FLOOR` 296→**297**(本文抬到 live,三处同轮校齐:目录实况 297 份、
`docs/skills-wave/README.md` 明写份数 297、`tests/unit.js` 的 `const FLOOR` 字面)、护栏主题 **20**。
五格差额 **0/0/0/0/0**,`SLACK` **3** 一格没用掉。
`Skills.gaps()` **20** 键一个没剥。

残留锚点现取复核:`js/batchops.js` 的 `.order + 1` **17** 行、`js/produce.js` 与 `js/issues.js` 各 **0** 处;
`cli.js` 里 `shots-dedupe` **5** 处、主体侧同类命令 **0** 处(残留 9 的实况没变)。

## 七、残留(明令:本文不代修,原话逐条保留)

1. **座位键形状仅行为面。** 座位键的形状(`id + '#' + 同 id 里第几位`)此刻仍只有行为面判据,
   源级面一句没写——本槽合进来的源级锚只覆盖「登记时序」这一面。
2. **失败轮退费未验。** 判据只钉「失败轮不占座位」,没钉「那一笔真退了」;退费口在 `Tasks`/服务端,
   `cli.js` 那一侧的夹具把服务端往返整段换成了桩,现有形状量不到。
3. **两条新判据落在 `commands` 而非 `contract`。** 源级锚按形态更像 `contract` 套件的东西(它读的是源码而不是跑产品),
   放在 `commands` 是为了让它与行为面那条挨着、变异时两句红一起出;代价是「按套件分工」这条隐规矩上多了一处例外,
   往后真要搬,得同轮改护栏主题的落点计数。
4. **单发七条无 `landed`。** 未动:`shot.generateVideo` 等单发出口零 diff,没给它们硬加这个字段。
5. **`batchDone` ok。** 未动。
6. **共位不退费。** 未触及:`metered`/`Tasks.run` 与 `ok` 的口径一个字没动,共位覆盖照旧不退费。
7. **`result.shots` 按 id。** 未动(同 id 几行只剩一个键,故落库数只看 `landed`)。
8. **`state-put` 不设闸。** 逃生舱有意不设闸,整树原样落库,零 diff。
9. **主体侧无 `shots-dedupe`。** 主体侧没有镜头侧那条同类去重命令,现取仍是 0 处;
   `dupSubjectRowsNote` 末句给的仍是「到主体库里删掉或改 id」这条现有修法。
10. **CF4 本槽没另跑。** §四那张表里 CF4(把主题显式销号 → 红 0)照对侧记账收下,本槽只现跑了 CF1/CF2/CF3 三手;
    CF2 红 3 已经把「静默删除这条路走不通」这句话钉住,CF4 承载的是「留痕那条路走得通」,不影响本槽结论。
11. **W280 §四 M9 那把跨支量尺本槽没量。** 本槽 `js/` 双向零 diff,照 W280 的读法结论必然是「分家」,
    量它不新增信息,故明写没量而不是抄一个数;同理 W280 §四 M4「摘 `nthShot` 两处兜底红 3」那把量尺也未重量,
    往后要拿它当「两支有没有接触」的尺子时,基准数仍按 **3** 读。
12. W277 起历次残留(写回路径、`ok` 口径与计费)按明令一条没代修,原话逐条保留。
