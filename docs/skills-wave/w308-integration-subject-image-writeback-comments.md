# W308 把 W307(主体生图写回注释改准)合入 W306 尖:第六条快进形槽

> 本文所有 SHA 与数字一律**现取**,两侧自称一个没抄。
> 引用别的记账件时只写文件名(如 `w307-subject-image-writeback-comments.md`),不挂相对链接。

## 一、三个 SHA 与并入点

| 位置 | 现取 SHA | 说明 |
|---|---|---|
| P1(我方尖,w306) | `053a2e3` | `origin/cursor/w306-integration-df41` |
| P2(对侧尖,w307) | `1785a70` | `origin/cursor/w307-subject-imgcomment-7b12` |
| merge-base | `053a2e3` | **恰等于 P1** |
| M(merge commit) | `04b7d50` | parents 依次 `053a2e3` + `1785a70` |
| `tree(M)` | `170eea9` | 与 `tree(P2)` 逐字节相同;`tree(P1)` 是 `b3f266a` |
| 对照 `origin/master` | `9adcf0f` | 本槽一步没合、一步没推 master |

对侧自称「从 w306 尖 `053a2e3` 起支、HEAD `1785a70`」,现取 `rev-parse` 两个都对得上。
`git merge-base --is-ancestor P1 P2` 回 0 ——**快进形**,集成线上连着第六条。
`P1..P2` 两条提交(`667ea5d` 产品 + `1785a70` 记账),现取核过对侧自称的尖就是链尾,
不存在「自称尖其实是链中间那个 merge」这一格。

仍按明令 `--no-ff`,留独立 merge 节点;零冲突,**未用过** `--ours`,也未用过 `git checkout <old> -- .`。

### 这一格作证到什么程度

`git diff M P2` 空集、`tree(M) == tree(P2)`、`P1→M` 与 `merge-base→P2` 的 numstat 逐行相同——
这几句在快进形下**恒真**,整份覆盖丢东西时同样成立,树比对分不开。
连着第六条,这一格已彻底不承载结论。故本槽的判据全压在下面三张台上:**锚点台 · 变异台 · live 探针台**。

## 二、对侧改了什么(先把面摸清)

`git diff P1 P2 --stat` 现取五个文件:`README.md` 1/1、`cli.js` 9/4、`docs/skills-wave/README.md` 2/1、
`tests/unit.js` 1/1、新增记账件 `w307-subject-image-writeback-comments.md` 257 行。
**零运行时改动**:`js/`、`server.js`、`mcp.js`、`index.html`、`tests/` 目录下除 `unit.js` 那个 `FLOOR` 字面外一处未动。

收的是「主体侧那句『后面几位**结构性**够不着』自 W246 起已不准」——批量补图早已按 `nthSubject` 逐位写回,
几位各得一张图;落首位的只剩按 id 点名那一类。改准三处:`cli.js` `subjects-dedupe` 头顶那句加限定并补一段括注、
`cli.js` 逃生舱那段两侧同形的两句、根 `README.md` 命令总览同一句的第三份。

## 三、可执行面与真并集

**可执行面相对对侧零 diff**(现取逐文件 numstat):`cli.js` 0/0、`server.js` 0/0、`mcp.js` 0/0、`index.html` 0/0,
`js/` 0 个文件、`tests/` 0 个文件、`docs/` 0 个文件;`git diff P2 M` 全树 **0** 个文件。

**真并集逐组现取核过(快进形也照核,空 diff 不作证)**——P1 有的每一项 M 里都还在:

| 组 | P1 → M | 丢 | 增 |
|---|---|---|---|
| 记账件索引行(wNN 文件名) | 321 → 322 | 0 | 1(`w307-…md`) |
| 记账件目录实况 | 321 → 322 | 0 | 1(同上) |
| `GUARD_TOPICS` 编号 | 30 → 30 | 0 | 0 |
| `GUARD_TOPICS_CLOSED` 编号 | 0 → 0 | 0 | 0 |
| 花名册(`w178` §花名册) | 30 → 30 | 0 | 0 |
| 根 README 二级标题 | 36 → 36 | 0 | 0 |
| 根 README API 表行 | 4 → 4 | 0 | 0 |
| CLI 命令注册名 | 50 → 50 | 0 | 0 |
| EXEC 领域命令名 | 13 → 13 | 0 | 0 |
| 单元套件名 `SUITES` | 24 → 24 | 0 | 0 |
| **单元用例名(全量)** | **718 → 718** | **0** | **0** |

末行那格是**名集多重集对账**:两侧都自称 718,而这里量的是「718 条用例名逐条相同、一条没被换掉」——
两个 718 相等不等于同一批 718,这一格才把它钉住。

## 四、锚点台(22 → 现取 31 条,分两组成对立)

产出面在 P1 必须失配、合完必须齐;残留面**两侧都必须命中**——合入槽不许顺手代修对侧明令留着的东西,
少了这后半组就说不出「该带的带了、不该动的没动」。

| | P1 `053a2e3` | M `04b7d50` |
|---|---|---|
| 产出面 OUT(21 条) | **0/21** | **21/21** |
| 残留面 RES(10 条) | **10/10** | **10/10** |

产出面 21 条:主体闸注释五条(新限定词在 / 老措辞已摘 / 三句括注)、逃生舱镜头侧三条、逃生舱主体侧三条、
根 README 四条(两侧新句 + 老措辞已摘)、记账面六条(明写份数 322 / 索引行 / `FLOOR` 322 / 记账件落盘 /
目录实况 322 / 索引表行数 322)。
残留面 10 条:`cli.js:828`、`js/storyboard.js:73`、`README.md:300`、`tests/unit.js:3595` 四处旧措辞,
外加 `DUP_UNITS`、`nthSubject` 与 `nthShot` 两处立据句、`js/roles.js` 运行时文案、`dupSubjectRowsNote`、`dupIdScan`。

## 五、变异台 18 手:红(unit 接住)7 / 绿·树上漏网 11

**判红只认树上自己的判据面(`unit`)**;锚点台是本槽搭的脚手架,它接住不算树上看守得住。

| 手 | 内容 | `unit` | 判 |
|---|---|---|---|
| M01 | 主体闸注释退回「结构性够不着」 | 718/718 | 绿·漏网 |
| M02 | 摘掉「那几笔各落各的位」整段括注 | 718/718 | 绿·漏网 |
| M03 | 逃生舱主体侧退回「只有首位寻得着」 | 718/718 | 绿·漏网 |
| M04 | 逃生舱镜头侧退回「只有首行寻得着」 | 718/718 | 绿·漏网 |
| M05 | README 总览两句一并退回老措辞 | 718/718 | 绿·漏网 |
| M06 | README 总览主体侧摘掉括注 | 718/718 | 绿·漏网 |
| **M07** | **W307 整槽退回**(两侧注释 + README + 记账件 + 索引行 + 明写份数 + `FLOOR` 全回 321) | **718/718** | **绿·漏网** |
| M08 | 只把 `FLOOR` 改小 322→321,文件与索引行原样 | 718/718 | 绿·漏网 |
| M09 | 删记账件 + 删索引行 + 份数改小(三方仍自洽) | 718/718 | 绿·漏网 |
| M10 | 只删记账件、索引行与份数不动(三方失衡) | 714/718 | **红 4** |
| M11 | 只把明写份数改小 322→321 | 717/718 | **红 1** |
| M12 | 顺手代修残留:`cli.js:828` 也改成「按 id」 | 718/718 | 绿·漏网(锚点台 RES 9/10 接住) |
| M13 | 顺手代修残留:`tests/unit.js` 段内注释也改成「按 id」 | 718/718 | 绿·漏网(RES 9/10 接住) |
| M14 | 对照:批量写回改回 `findSubject`(首位语义) | 714/718 | **红 4** |
| M15 | 对照:`nthSubject` 定义体改回取首位 | 714/718 | **红 4** |
| M16 | 对照:单摘 `nthSubject` 越界兜底 | 716/718 | **红 2** |
| M17 | 对照:`nthShot` 定义体改回取首行 | 712/718 | **红 6** |
| M18 | 对照:`landed` 改成数引擎调用次数(`seats.size → okCnt`) | 717/718 | **红 1** |

**M07 是这张台上最直白的一格**:把 W307 整槽退回去,`unit` 仍 **718/718 全绿**。
注释那半边 `blankNonCode(...)` 把注释与字面量一并抹掉、根本不看,记账那半边是 W304 记过的明面口子
(三方自洽地一起改小,`live >= FLOOR` 仍成立),两件事叠起来正是**「快进形空 diff 不能当没丢东西」**的读数——
合入时把对侧整槽丢掉,树上没有一条判据会响。

**M14–M18 五手对照方向红 4/4/2/6/1**:同一棵树上,把**行为**改回首位/首行语义当场就红。
一句话:**注释无人看守 ≠ 行为无人看守**。M12/M13 两手代修残留同样全绿,证明「不代修」这条纪律在树上也无人看守,
只能靠锚点台的残留面钉住。

> 变异台的复原得连 git index 一起(`reset --hard` 而不是 `checkout -- .`):M07 那手用 `git checkout <sha> --`
> 会把旧版塞进 index,只 `checkout -- .` 会把污染带进下一手——第一遍就是这么把 M08 之后整批读数带偏的。

## 六、live 探针台 13 格:P1 **10/13** → M **13/13**,翻绿恰 3 格

真跑 `cli.js` 本体,打一台**独立进程**的桩服务端(与探针共用事件循环时 `spawnSync` 会把桩自己饿死),
state 落磁盘夹具、每次 `GET` 现读现深拷贝(clone 语义),生图/生视频桩**按次序发各不相同的产物**。
夹具:主体库同 id 三位(`dup`:甲/乙/丙)+ 一位 `solo`;分镜表同 id 三行(`dsh`)+ 一行 `ssh`。

| 格 | 读数(两侧逐字相同) |
|---|---|
| L1 `exec subject.generateImage` 点名 `['dup']` | `total/ok/landed = 3/3/3`,三位得 `/img/i1,i2,i3` 各不相同,`solo` 无图 |
| L2 同上整库那一档 | `4/4/4`,四位各得一张各不相同 |
| L3 `subject-image --gen` 按 id 点名 | 只写第 1 位,后两位无图 |
| L4 `subject-image --url` 按 id 点名 | 只写第 1 位,后两位无图 |
| L5 `subject-copy` 按 id 取源 | 取到的是第 1 位(甲) |
| L6 定位口本身 | `findSubject` 取首位;`nthSubject 0/1/2` 恰是甲/乙/丙 |
| L7 `exec episode.generateVideos` 点名 `['dsh']` | `3/3/3`,三行各得一段而 `ssh` 无片 |
| L8 `gen-episode` 整集 | 四行各得 `/vid/v1…v4` 各不相同 |
| L9 `gen-shot-video` 按 id 点名 | 只写第 1 行,后两行无片 |
| L10 `exec shot.generateVideo` 按 id 点名 | 只写第 1 行,后两行无片 |
| L11–L13 同屏对照:树上那三句注释原文 | **P1 全 FAIL,M 全 PASS** |

**行为面 10 格两侧读数逐字相同**——零运行时改动这件事是量出来的,不是从 diff 空集推出来的。
翻绿的恰是 L11–L13 那三格注释面。

**P1 那一侧的失败句正是残留现场**:同一屏上面读着
`total/ok/landed=3/3/3 · dup三位图=[/img/i1,/img/i2,/img/i3] 各不相同=3/3`,
下面紧跟着树上那句原话「全按 id 取首位,后面几位**结构性**够不着」与
「点名一次几位都跑**而只有首位寻得着**」。合完之后两句分别读作
「后面几位**按 id** 够不着」与「批量补图按 `nthSubject` 逐位写回、**几位各得一张图**,而按 id 点名单位的只落首位」。

## 七、live 数字(全部现跑,两侧自称一个没抄)

| 项 | P1 `053a2e3` | M `04b7d50` |
|---|---|---|
| `unit` | **718/718** | **718/718** |
| `integration` | **152/152** | **152/152** |
| `commands` / `domain` / `contract` / `sb-views` / `skills` | — | **75 / 49 / 153 / 11 / 104**(均未动) |
| `cli.smoke`(单独现跑) | — | **115/117** |
| `TOPIC_FLOOR` / 在册 / 销号 / 花名册 | 30 / 30 / **0** / 30 | 30 / 30 / **0** / 30 |
| `Skills.gaps()` 键数 | **20** | **20**(一个没剥) |
| `SLACK` | 3 | 3(**一格没用掉**) |
| 五格棘轮差额 | 全 **0** | 全 **0** |
| 记账件三方对齐(明写/索引行/目录) | 321/321/321 | **322/322/322** |
| 记账件 `FLOOR` | 321 | **322** |
| ±300 字窗口契约 | **4/4 处 true** | **4/4 处 true** |

**两侧 `unit` 都自称 718,本槽仍两边各现跑一遍**,并另加 §三 那格名集多重集对账(`+0 −0`)——
两个 718 相等不等于同一批 718。

**销号现取是 0**:`GUARD_TOPICS_CLOSED` 是空表。W306 记过对侧自称销号 1 与实况不符,本槽照 live 写、不盲抄。

**±300 字窗口契约按树上那条判据自己的取法量**(`tests/unit.js` 那条:锚点是
「镜头 id 唯一性…由调用方自己保证」,窗口内须点名 `shots-import`),现取两侧各 4 处全 `true`
——`cli.js` 1 处、`README.md` 2 处、`docs/AI助手接入指南.md` 1 处。

`node --check` 现取:改动面 `cli.js` / `tests/unit.js` 过,**全树 `git ls-files '*.js'` 逐个 `--check` 全过**。
本槽**未跑** `e2e`(明令)。

## 八、`cli.smoke` 单独现跑

按明令单独整跑,`env -u HUJING_SERVER`(连 `HUJING_TOKEN` 一并摘)加 `MV_DATA_DIR` / `MV_UPLOADS_DIR` / `MV_CONFIG`
三个 env 重定向到临时目录隔离:

- M `04b7d50`:**115/117**
- `origin/master` `9adcf0f` 同 env 下:**51/53**

两条失败**同名同表现**,逐字如下,按明令允许:

1. `FAIL | 未登录 whoami → exit 3 | exit=1`
2. `FAIL | llm --json mock 链路 | undefined`

## 九、本槽现取新记的一条:索引表那一行被空行拆离了表体

W307 那条索引行落在 `docs/skills-wave/README.md` 里时,**上面多了一个空行**(现取 332 行为空、
w307 那一行在 333、`## 一分钟摘要` 紧跟在 334 且中间没有空行),于是它在 markdown 里不接在索引表体上,
渲染时是从表格里掉出来的孤行,而不是索引表的第 322 行。

**树上没人看守这件事**:`contract` 那条数的是正则 `^\|\s*\[wNN-….md\]` 匹配到几行,孤行照样匹配得到。
现取变异证过——把 `w303` / `w304` / `w305` 三行也各用空行拆离表体,`contract` 仍 **153/153 全绿**、
三方对齐仍报 322,一条判据都没响。

**本槽的处置**:只删掉那个空行让索引表重新连成一体,并在末尾补回 `## 一分钟摘要` 前的空行;
**W307 那一行的正文一个字节没动**(§四 O17 与 §三 真并集那格现取仍齐),这是**结构**修复不是内容代修。
「这件事无人看守」如实进 §十 残留,本槽不给它立判据(明令不动判据面)。

## 十、残留(本槽**一条没代修**,原话逐条保留)

`w307-subject-image-writeback-comments.md` §九 六条原话:

> 1. **`tests/unit.js` 里那一条用例名与它段内那段块注释仍写着旧措辞**(§四 末尾那格)。
>    用例名是「`CLI subjects-dedupe:…--apply 只改够不着的后续位`」,段内 3594–3595 又重述了一遍
>    「全按 id 取首位、后面几位结构性够不着」。名与注释是一句话的两半,只改一半会让同一条用例自相矛盾;
>    而改名会牵动护栏主题的锚点面,本槽明令不动判据面,故整条原样留下。
>    镜头侧 3483 那条同形(「只改够不着的后续行」),要收就两条一起。
> 2. **注释准不准仍然零判据**(W303 残留第 2 条、W305/W306 原话)。§五 现取再证一遍:
>    老措辞与新措辞两边 `unit` 718/718、`integration` 152/152 全绿。本槽与前三槽同判:不立字面判据。
> 3. **W306 残留第 4 条原样在**:`unit` 那条批量写回判据只钉住 `exec episode.generateVideos` 两档,
>    `CMD['gen-episode']` 那一路没有自己的「同 id 几行各得一段片」判据(§2.2 ⑦ 现取仍只在本槽一次性脚本里量过,
>    树上没有承载)。本槽明令不代补。
> 4. **主体侧那一路同形的空缺**:§2.1 ⑤ 那格(`subject-copy` 按 id 取源落首位)现取同样只在一次性脚本里量过,
>    树上没有它的判据。与第 3 条同形,一并留给判据面那一槽。
> 5. W302/W303 起仍活着的那几条,本槽一条没代修、原话逐条保留:
>    - **发现存量重复仍得先走到主体库那一页或分集工作区顶栏**;整树导入/恢复与 `state-put` 照旧能写进重复 id
>      (明令不给入口加闸),这仍是**存量**出口。
>    - **跳转仍只有「去第 1 行/第 1 位」一个方向**;第 1 行那一枚一个跳转都不挂,要指向后续那几行是一对多的另一种交互。
>    - **`board` / `bb` 两档仍只看得见顶栏那个总数**,一行分镜都不渲,故那两档上既没有小标也没有落点。
>    - **主体侧那条「定位动作只许有一处」的判据仍绑在接收者变量名上**(`tests/unit.js:4037` 现取仍是
>      `card\.scrollIntoView\(`,镜头侧 `:4417` 是 `\.scrollIntoView\(`)——明令不改主体侧判据绑变量名。
>    - **切过去之后没有回程**(高亮褪掉就没了,只能自己往回滚)。
>    - **主体侧与镜头侧的去重说明仍各写一份**;本槽 3.1 与 3.2 又让两侧措辞分开了一点(主体侧那段多出括注那一句),
>      合并成一份模板的代价比 W306 记的时候再高了一点点。

W306 §六 里那几条更早的原话(W302 起)一并原样带下,含「项目详情面与项目列表那几枚角标点过来落的仍是**整页**,
不是撞车那张卡」。

**逐条现取核过,一条没代修(12/12 仍在):**

- 第 1 条:主体侧用例名现取在 `tests/unit.js:3592`、段内注释在 `:3595`、镜头侧同形用例名在 `:3483`,三处一字未动。
- 第 2 条:现取 `tests/unit.js` 全文搜本槽两句新措辞(「后面几位按 id 够不着」/「那几笔各落各的位」)**零处**
  ——散文仍无字面判据;§五 M01–M09 九手退回/退槽全绿是同一件事的现场读数。
- 第 3 条:`unit` 里点名 `gen-episode` 的用例现取 1 条,无一条判「同 id 几行各得一段片」
  (§六 L8 那格仍只在本槽一次性探针里量过,树上没有承载)。本槽不代补。
- 第 4 条:`unit` 里点名 `subject-copy` 的用例现取 **0** 条——§六 L5 那格同样只在探针里量过,树上无判据。
- 第 5 条各分条:`dupjump` 全树现取 **6** 处(`js/roles.js` 3 + `js/sb-views.js` 3,`js/storyboard.js` **0**),
  反方向一条路都没有;`js/storyboard.js` 里 `dupRowTag` / `data-dupjump` **零处**,`board`/`bb` 两档仍一行分镜不渲;
  `tests/unit.js:4037` 现取仍是 `card\.scrollIntoView\(`、镜头侧 `:4417` 是 `\.scrollIntoView\(`,两条判据形状一字未变;
  回程路径全树 **0** 处;两侧去重说明现取仍各一份。
- W302 角标那条:`js/episodes.js` / `js/projects.js` 现取 `dupjump|dupRowTag` **各 0 处**,那几枚角标落点一行没改。
- 对侧明令留着的三处旧措辞现取一字未动:`cli.js:828`、`js/storyboard.js:73`、`README.md:300`。

**顺带把 W306 §六 那格 `dupRowTag` 的数再订准一次**:现取是 **1 处定义**(`js/sb-views.js:66`)
+ **2 处消费**(`js/sb-views.js:116` 与 `js/shotgroups.js:217`),另有 1 处 UMD 导出登记(`js/sb-views.js:1407`)
——导出登记不是消费点。W306 订正的「1 定义 + 2 消费」现取成立,结论不变。

**本槽新记残留一条**:§九 那件事——**索引表的结构完整性无人看守**。
`contract` 那条只数正则匹配到几行,行接不接在表体上、渲不渲得出来一概不判;
现取变异(再拆离三行)仍 153/153 全绿。本槽只做结构修复,不给它立判据。

## 十一、这一槽可复用的两件事

**一、连到第六条的快进形,树面证据已经该整段跳过了。**
`diff` 空集 / `tree` 相等 / numstat 逐行相同,这三句在拓扑上恒真,写进记账只是占字数。
值得写的是三张台各自**翻了几格、哪几格没翻**:锚点台产出面 0→21 而残留面两侧恒 10、
live 探针行为面 10 格逐字不动而注释面 3 格翻绿、变异台 11 手绿 7 手红。
「哪几格**没**翻」和「翻了哪几格」一样是证据——残留面两侧都命中,说的是「不该动的没动」。

**二、给「整槽退回」留一手变异,它全绿这件事本身就是锚点台为什么非有不可的读数。**
M07 把对侧整槽退回去,`unit` 718/718 一条不红。这说明合入槽若只靠跑套件确认「合进来了」,
在纯注释 + 记账类的槽上等于什么都没确认。锚点台不是锦上添花的脚手架,它是这类槽**唯一**的判据面;
而它必须**成对**立(产出面在 P1 失配 + 残留面两侧都命中),否则「顺手代修」这一类失败(M12/M13,`unit` 全绿)
照样漏得干干净净。
