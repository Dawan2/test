# W185 集成收敛记录:W180 G1 处置口按实况派 + W181 空 catch 的覆盖面

> 基线 `cursor/w182-integration-646e@5f98d0b`;本槽分支 `cursor/w185-integration-9b22`,tip 是本文这一提交。
> 两次 `--no-ff` 合并,取两支的远端实际 HEAD。

## 1. 结果一句话

两支并进同一条集成线:**W180** 让发布门 G1 未过门时那颗「一键处置」按首个受阻集的实况派
(`Domain.epFixOf` 单源:未拆镜派智能分镜、补正文/重新拆镜/处理失败镜出该集工作区的导航口、其余仍一键成片),
门槛/severity/info 一字未动;**W181** 判定 `js/release.js` 底部那处空 `catch` 的**停工位不成立**——
产品代码零改动,只把它的覆盖面按源级 + 行为两面钉住(填掉它反而会在同一形态下卸掉整支 Release)。

本槽最要紧的一格是 **W180 从旧叉点带回来的那条护栏登记**:W180 从 `b1f5fd6`(W174 那一线)叉出,
它看得见的登记面是 12 条、`TOPIC_FLOOR` 12,自己补了 `epfix-produce-gate` 后自称 13;
而集成线上 W182 已把登记面推到 18 条、下限 18。**两侧自称的 13 与 18 都不是答案**:
登记表与花名册都按并集合、`TOPIC_FLOOR` 按合完后 live 登记条数重测为 **19**。
**并集仍然不是形式**:反事实实测若不把 `epfix-produce-gate` 补进花名册,
再做一次「撤掉该条登记 + 把下限一并改小」的完整销号动作,**红 0**(见 5.3 的 M4)。

W182 残留 7 点名的那条(`js/release.js` 底部空 `catch` 一直开着,W151/W157/W161/W166/W170/W174/W179 一路原样)
**本槽换了状态而不是被填掉**:W181 拿源级 + 行为两面证出它兜的只有「注册订阅」这一下,
摘掉或填掉它的同一形态下 `window.Release` 当场没有、十项门一门不剩(5.3 的 M5 红 2)。

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w180-g1-extract-split-54d7@fecf06a` | `974705e` | G1 未过门时回执 info 逐集点名「生成分镜」,处置却恒挂 `episode.produce`,而它自己的就绪闸在四态上当场退回(零调用零计费、该集 counts 一字不变),回执说的和按钮做的对不上;按 `episodeState.action.key` 分档收进 `Domain.epFixOf` 一份,门的 pass 条件与 info 文案一字未动 | `js/domain.js` +20、`js/release.js` +12 −3 | +3 `unit`(1 `commands`、1 `domain`、1 `release`),另收严既有 `contract` 那条 |
| `w181-release-bus-catch-cc61@1be298a` | `81ab416` | 那处空 `catch` 的覆盖面此前只靠 W149 留下的三条散文理由,而此后 `episodes`/`issues-ui`/`workbench` 各自开了通配订阅,「下游是空的」那条已只对一半;本槽把它按源级(恰一处空 `catch`、`try` 块顶层只有注册那一句)+ 行为(降级形态下整支照常导出、十门与四个汇总数逐字不变、回调异常归总线隔离)钉死 | **产品 js/ 零 diff** | +5 `unit`(全在 `release`),`loadRelease` 参数化出 `real`/`no-on` 两种总线形态 |

合并后回归 **`unit 569/569`**、**`integration 143/143`**、**`cli.smoke 105/107`**(2 项失败与基线同名同表现,见 5.2),
`contract` 子套件 **128/128**;`GUARD_TOPICS` **19 条**、`TOPIC_FLOOR` **19**、花名册 **19 行**、
`GUARD_TOPICS_CLOSED` **0 条**;记账件 **198 份**(含本文)。

治理面:`gaps()` **20 键**(`G-03` 仍是两条值、`G-11` 仍在案)、提示词注册表 **41**、能力短名单 **30**、
`Skills.CHECKS` **17**、`preflightStages()` **七面**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 八个数一个没动。
本槽**注册表五个文件相对基线零 diff**(`git diff --stat 5f98d0b HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js` 空输出),
故这八个数是结构性成立,与 W182 / W179 / W174 / W170 / W166 / W161 / W157 同形。

**不在本槽射程内**(仍在飞,一行没碰):W183、W184。`master` 也没合。

## 2. 两次合并各自的形态

两次都是真 `--no-ff`,两个 parent 逐个记在这里:

| # | 合并提交 | parent 1(集成线) | parent 2(被合入支) | 叉点 | 冲突文件 |
|---|---|---|---|---|---|
| 1 | `974705e` | `5f98d0b` | `fecf06a` | `b1f5fd6` | `README.md`、`docs/skills-wave/README.md`、`tests/unit.js` |
| 2 | `81ab416` | `974705e` | `1be298a` | `b1f5fd6` | 同上三份 |

两支同叉 `b1f5fd6`。三份冲突文件在两次合并里**都是真三方**——`M` 与 `P1` / `P2` / `B` 四棵树的 blob 两两不等,
逐份机检过(W161 那条教训:判 `--ours`/`--theirs` 要看另一侧等不等于叉点,而不是看 `M` 等于谁)。

**本槽出了一格「零冲突块而两侧都改过同一个文件」的正例**:第一次合并里 `js/domain.js` 与 `js/release.js`
两侧都改过、`git` 一个冲突块都没给,机检确认它们在合并 1 里同样是真三方(四棵树两两不等),
两侧改的是同一文件的不同段——`js/domain.js` 我方 W177 的 `staleShotSplit` 落在 `shotVideoStale` 附近、
对侧 `epFixOf` 落在 `episodeState` 之后;`js/release.js` 我方 W177 的分报在 G4 段、对侧的 `first`/`epFixOf` 在 G1 段。
**合完两面都在**,任务口径预告的「冲突时两面都留」这一格 `git` 自己办到了,但**它值多少另量了一次**:
反事实机械取 W180 那一侧的 `js/release.js`(即 W177 的分报被冲掉)**红 4**(5.3 的 M11),
反过来机械取我方(即 `epFixOf` 没接上)**红 1**(M1)——两个方向都不是零,取任一侧都会静默吃掉一支。

### 2.1 第一次合并(W180)怎么并的

| 位置 | 两侧各是什么 | 怎么并 |
|---|---|---|
| `tests/unit.js` `domain` 套件尾 | 我方 W177 `staleShotSplit` 一条 / 对侧 W180 `epFixOf` 一条 | **并集**,两侧在同一锚点各插一段且**共用块尾那个 `} },`**,给我方那半补一个 |
| `tests/unit.js` `GUARD_TOPICS` 表尾 | 我方七条(`review-min-literal`、`review-gate-single`、`project-script-single`、`ep-blocker-fanout`、`zip-placeholder-ban`、`topic-unlist-ledger`,以及表中段那几条)/ 对侧一条 `epfix-produce-gate` | **并集**,合后在册 **19 条** |
| `tests/unit.js` 下限那句判据 | 我方 W178 的「在册 + 销号 >= `TOPIC_FLOOR`」+ 模块作用域的 `TOPIC_FLOOR` / 对侧写在用例体内的 `const TOPIC_FLOOR = 13` | 取我方(它是对侧那句的超集,且留在模块作用域才让 `floor-slack` 第五格的 `^const TOPIC_FLOOR = (\d+);$` 取得到);下限按合完 live 登记条数取 **19**,两侧自称的 13 与 18 都不是答案 |
| 花名册(`w178-topic-floor-unlist.md`「花名册」那一节) | 我方十八行 / 对侧**没有这张表**(W180 叉在 W178 之前) | **并集**:把 `epfix-produce-gate` 按登记来路补成第十九行,首次登记波次记 **W180** |
| 花名册取数口自检 | 我方 `assert(roster.length >= 18, …)` | 按 live 抬到 **19**(留在 18 的话,悄悄从花名册里删掉一行仍全绿) |
| `tests/unit.js` 三套件下限 | 561/143/107 / 550/143/107 | 单元现取 live **564**;集成与冒烟两侧同值 |
| `tests/unit.js` 记账件 `FLOOR` | 195 / 188 | 现取 live **196** |
| `README.md`「单元测试(N 项断言」整句 | **只差那个数**——对侧那一句相对我方**少了** W177 的 G4 分报整段(它叉在 W177 之前),没有任何对侧独有的内容 | 取我方整句,数字换 live **564** |
| `README.md` 契约段「登记面现有十八条主题」 | 我方一句、对侧没碰 | 按 live 改成**十九条**并把受阻集处置口那条一并点名 |
| `README.md` 交付段 G1 那一句 | 零冲突块自动合上(我方没碰它) | 对侧整句留着,`Domain.epFixOf` 那段原样 |
| `docs/skills-wave/README.md` | 明写份数 195 / 188;索引表我方六行 / 对侧 `w180` 一行 | **196**;**并集**,七行按波次号排(`w180` 归到 `w179` 与 `w182` 之间) |

第一次合完先跑 `unit`:**564 / 564**、`contract` **128 / 128**。

### 2.2 第二次合并(W181)怎么并的

| 位置 | 两侧各是什么 | 怎么并 |
|---|---|---|
| `tests/unit.js` `release` 套件 + `loadRelease` | 对侧把 `loadRelease()` 参数化成 `loadRelease(bus)`(`real` 装真 `bus.js`、`no-on` 给半截 Bus)并加五条用例 | **零冲突块自动合上**;W180 第一次合进来的 G1 处置口那条与它同居一室,`release` 套件合后 **43 / 43** |
| `tests/unit.js` 单元下限 | 564 / **552** | 现取 live **569** |
| `tests/unit.js` 记账件 `FLOOR` | 196 / **188** | 现取 live **197**(本文落地后 **198**) |
| `README.md` 交付段 `release.js` 那一整句 | 我方带 W177 的 G4 分报整段(对侧没有)/ 对侧带空 `catch` 覆盖面那一整段(我方没有) | **并集**:对侧那段按锚点接回——插在「**Bus 通配订阅**:一条主线事件只转一次 `release.dirty`…」之后、「**交付 ZIP 落地面**」之前,我方那段原样留着 |
| `README.md`「单元测试(N 项断言」整句 | 两侧只差那个数 | 取我方整句,数字换 live **569** |
| `docs/skills-wave/README.md` | 明写份数 196 / 188;索引表我方七行 / 对侧 `w181` 一行 | **197**;**并集**,八行按波次号排(`w181` 归到 `w180` 与 `w182` 之间) |

## 3. 数字一律合完后 live 重跑

**没有一个数是抄两侧父值来的。** 逐格实测读数:

| 数 | 基线 `5f98d0b` | W180 侧自称 | W181 侧自称 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|
| 单元用例数 | 561 | **550** | **552** | **569** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 128 | 122 | 128 | **128** | 主 `README.md` 契约段 |
| `release` 套件 | 37 | 38 | 42 | **43** | 无明写落点,本文登记 |
| `integration` | 143 | 143 | 143 | **143** | 主 `README.md` + 下限表 |
| `cli.smoke` | 107 | 107 | 107 | **107**(实跑 105/107) | 主 `README.md` + 下限表 |
| 记账件份数 | 195 | **188** | **188** | **198**(含本文) | `docs/skills-wave/README.md` 明写 + `FLOOR` |
| `GUARD_TOPICS` 在册 | 18 | **13** | 12 | **19** | `tests/unit.js` 那张表 |
| `TOPIC_FLOOR` | 18 | **13** | 12 | **19** | `tests/unit.js` 模块作用域 |
| 花名册行数 | 18 | —(不存在这张表) | —(同左) | **19** | 本目录 `w178-topic-floor-unlist.md`「花名册」那一节 |

**两支自称的记账件份数逐字相同的 188**——两支同叉 `b1f5fd6`、各加一份记账件,又是那个静默窗口。
与 W182(两支)、W179(两对)、W174、W170、W166 各槽同形,**连着第六槽出现**。若照抄它,与 live 198 差 **10 格**。

逐次合完的读数:

| | 合入 W180 后 | 合入 W181 后(本槽 tip) |
|---|---|---|
| `unit` | 564 / 564 | **569 / 569** |
| `contract` | 128 / 128 | **128 / 128** |
| `release` | 38 | **43** |
| `integration` | — | **143 / 143** |
| `cli.smoke` | — | **105 / 107** |
| `GUARD_TOPICS` / `TOPIC_FLOOR` / 花名册 | 19 / 19 / 19 | **19 / 19 / 19** |
| 记账件份数 | 196 | **197**(本文后 198) |

**两侧写同一个「份数」时 `git` 静默留一边这件事本槽照旧核过**:两次合并的份数格都给了冲突块,
但仍按目录实数重数(`ls docs/skills-wave/*.md` 去掉 `README.md`)而不是取任一侧,读数 196 / 197 与实况逐格相符。

## 4. 产品门槛一个字没动

- **G1 仍逐集判 `episodeState().status !== 'done'`**:blockers 的收集条件、`severity: 'high'`、
  `info` 那句「· 集名(状态:推荐动作)」的拼法一字未改,`epFixOf` 只换 `fix` 挂什么。
  把未拆镜集从 blockers 里摘掉(真放宽门槛)**红 2**,见 5.3 的 M8。
- **G4 仍 fail 且仍含定稿过期镜**:`counts.stale` 的判旧函数 `shotVideoStale` 一字未改,
  `fix.shotIds` 仍是全部过期镜(**没有减员**),`staleSplit` 与 `lockedShotIds` 照旧另外多带一份。
- **W176 那个显式子集位仍放过期镜过**:`js/commands.js` 里 `shotVideoStale` 现取 **2** 处、`cli.js` **1** 处,终稿锁仍拦着。
- **空 `catch` 没有被「填掉」**:全仓现取 `js/release.js` 空 `catch` **1** 处、`js/bus.js` **0** 处、
  `js/release-core.js` **0** 处,与 W181 的判据实况一致。
- **`expert.evolve` 仍不在 playbook `steps` 里**(现取 `playbooks()` 5 条,逐条 `steps` 零命中)、`gaps()` 仍是 **20 键没拆**、
  `review.userSystem` 仍只一处、`FORGE_SYS` 仍是 getter、`Issues` 仍是 UMD、`project.release` 仍在注册表、
  `reviseRetryLimit` 消费点齐、`reviewGate` 仍只一份(`js/domain.js` 里 `const reviewGate = ` 现取 1 处)、
  `memWrite` 驱逐仍在 `wf-core` 一份(`js/agent.js` 零定义)、`Domain.projectScript` 与 `Domain.extractSourceText` 两份派生都在、
  W179 合并后的 `guardSpread` 四种判词(`lost` / `short` / `broad` / `extra`)与可注入用例集原样。

## 5. 取证

### 5.1 零 diff 与改动面

产品面只有 W180 那两个文件(`js/domain.js` +20、`js/release.js` +12 −3);**W181 产品 `js/` 零 diff**
(`git diff --stat 974705e 81ab416 -- js/` 空输出)。测试面 `tests/unit.js` +249 −8(含两次解冲突与本文这一提交抬 `FLOOR`)。
注册表五文件零 diff(见 §1)。`server.js` / `cli.js` / `mcp.js` / `billing.js` / `index.html` 本槽一行没碰。

### 5.2 三套件实跑

| 套件 | 基线 `5f98d0b` | 本槽 tip |
|---|---|---|
| `node tests/unit.js` | 561 / 561(`contract` 128 / 128) | **569 / 569**(`contract` **128 / 128**) |
| `node tests/integration.js` | 143 / 143 | **143 / 143** |
| `node tests/cli.smoke.js` | 105 / 107 | **105 / 107** |

冒烟那 2 项失败与基线**同名同表现**,逐字如下,两条都是 `master` 基线上就有的:

- `未登录 whoami → exit 3 | exit=1`
- `llm --json mock 链路 | undefined`

### 5.3 变异抽查

| # | 变异 | 合并后 HEAD |
|---|---|---|
| **M1** | `js/release.js` G1 的 `fix` 退回硬编码 `{ cmd: 'episode.produce', epid: blockers[0].epid }` | **红 1**(`release` 那条,报「处置应与 info 点名的那件事一致」) |
| **M2** | `Domain.epFixOf` 摘掉未拆镜那一档 | **红 3**:`commands` / `domain` / `release` 各一条,三面分工可辨(命令层那条报「一键成片在这一态跑不动」、`domain` 那条报分档写错、`release` 那条报回执与按钮对不上) |
| **M3** | 撤掉 `epfix-produce-gate` 登记**并把 `TOPIC_FLOOR` 一并改小**(完整的销号动作) | **红 1** 并**点名那个编号**:「`epfix-produce-gate`:花名册上有,清单与销号台账里都没有——撤一道护栏须把编号搬进销号台账并写下闭合理由,把 `TOPIC_FLOOR` 一起改小不作数 / 下限 18 低于花名册的 19 条」 |
| **M4** | 反事实:花名册**不并集**(不补 `epfix-produce-gate` 那行、`TOPIC_FLOOR` 与取数口自检一并停在 18),再做一次 M3 | **红 0**。这一格量的正是"并集"本身值多少:不并集的话,W180 补登记的那条护栏撤起来一个字都不留 |
| **M5** | 把 `js/release.js` 底部那处空 `catch` 「填掉」(改成 `catch (e) { throw e; }`) | **红 2**:源级那条报「恰有底部订阅注册那一处空 `catch`」失配 + 行为那条当场 `Bus.on is not a function`——W181 的结论(填掉它会在同一形态下卸掉整支 Release)在集成线上复现 |
| **M6** | `TOPIC_FLOOR` 19 → 18 / 16,**主题一条不删** | **各红 1**(销号留痕那条,报「下限 N 低于花名册的 19 条」) |
| **M7** | `js/release.js` 把 `fix.shotIds` 减员成 `rerunShotIds`(把定稿镜从处置子集里摘掉) | **红 3**:W176 终稿那条 + W177 分报两条 |
| **M8** | G1 门槛真放宽:未拆镜集不再计入 blockers | **红 2**:`release` G1 处置口那条(报「两集都还没拆镜:期望 fail,实际 pass」)+ `release-core` 两端回执同形那条 |
| **M9** | `Domain.staleShotSplit` 把 `final` 从 stale 里摘掉(G4 门槛被放宽) | **红 5**:`domain` 分堆那条 + `release` 三条 + `release-core` 两端那条 |
| **M10** | 把记账件里「花名册」那一节的标题改掉(取数口失效) | **红 2**:销号留痕那条(报「花名册只取到 0 个编号」)+ 判词自立那条 |
| **M11** | 反事实:`js/release.js` 机械取 W180 那一侧(W177 的 G4 分报被冲掉) | **红 4**:`release` 分报两条 + `release-core` 两端那条 + `contract` 取数口那条 |

M4 与 M11 是本槽的两格要紧读数:前者证并集不是形式(与 W182 的 M3 同形,连着第二槽成立),
后者证"零冲突块自动合上"这一格并不是白捡的——同一个文件反方向机械取一侧,两边都会静默吃掉一支。

### 5.4 名集证明(按 `|` 切,多重集,不 unique-sort)

抽名口径:`node tests/unit.js` 的 `PASS|FAIL` 行按 `|` 切取第 2 段,**多重集比较**(不去重、不排序去重),
`Counter` 相减。五棵树各跑一次:叉点 `b1f5fd6`(547)、基线 `5f98d0b`(561)、`w180` tip(550)、`w181` tip(552)、本槽 tip(569)。

- **基线独有(`base − tip`):0 条。** 本槽零删测、零改名。
- 只在 tip 里有的:**8 条**,逐条点名(W180 三条、W181 五条):
  - `commands · produce 的就绪闸与受阻集处置口同一份实况:当场退回的四态,epFixOf 一律不再挂一键成片`(W180)
  - `domain · epFixOf:受阻集的处置口按推荐动作分档(未拆镜派智能分镜/人工决策三态出导航/其余仍一键成片)`(W180)
  - `release · G1 处置口按首个受阻集的状态派:未拆镜集挂智能分镜,不挂按下去只回一句拦截语的一键成片`(W180)
  - `release · Bus 通配订阅 · 那处 catch 只兜注册这一下(源级:try 块顶层一句;行为:兜住的是整支模块导出)`(W181)
  - `release · Bus 通配订阅 · 那处 catch 兜不兜住,G1–G10 结论逐字不变(它改不了任何一门,也不吞回调异常)`(W181)
  - `release · Bus 通配订阅 · 走得到那处 catch 的形态在成品页不存在(Bus 只此一处整体赋值,且比 release 先装)`(W181)
  - `release · Bus 通配订阅 · headless 侧没有同一张网(release-core.js 零空 catch、零 Bus,降级路径各自点名)`(W181)
  - `release · Bus 通配订阅 · 转发件的全部代价:一条主线事件占 2 格留痕,50 格里真事件只剩一半`(W181)

561 − 0 + 8 = 569,与实跑总数对得上。按套件看:`commands` 32 → 33、`domain` 31 → 32、`release` 37 → 43,其余不动。

**被合入支 ↔ 合并后**逐支再比一轮(各自与叉点 `b1f5fd6` 取差,再看那些名在不在 tip 上):
W180 相对叉点新增 **3** 条、丢失 **0** 条,三条**全在 tip 上**;
W181 相对叉点新增 **5** 条、丢失 **0** 条,五条**全在 tip 上**。零吃测。

W180 那一侧另有一处**改断言不改名**的动作,名集口径看不见,在此登记:既有那条 `contract` 用例
(「脏项目每个 fail 门都得挂出处置」)把「至少 5 个命令类处置」改成 4 个,但**同轮补了两条更硬的**——
每个 `fail` 门都得有 `fix`、导航类 `fix.hash` 须命中 `app.js` 路由表。这是收严不是放宽:
G1 在那份夹具上出的是导航口(该集有失败镜),原判据的"5 个命令类"此刻本就不成立。

### 5.5 本槽自己改了什么

除两个合并提交解冲突外,本槽只多做一件:**本文这一提交**——新增本文、`docs/skills-wave/README.md` 补索引行
并把明写份数 197 → 198、`tests/unit.js` 的记账件 `FLOOR` 197 → 198。

单元用例数与 `contract` 条数**本文一条不加**,故 569 / 128 不动。
两个合并提交里**没有删过任何一条用例、没有放宽过任何一条断言、没有翻过面**;
唯一被改的既有断言是 5.4 末尾登记的那条,而那是 W180 自己在它那一支上做的收严,不是解冲突时动的手。

## 6. 残留与欠账

1. **`GUARD_TOPICS` 与花名册取并集这件事本身仍然没有任何判据管。** W178 收掉的是"撤登记要留痕"这一路,
   但"合并时机械取任一侧"仍是无声的:取我方丢 W180 那条而花名册没有它的号——**一条不红**(5.3 的 M4 就是这一路的实测)。
   花名册只覆盖**写进它的号**,而写进去这件事是人工的。W174 残留 2、W179 残留 1、W182 残留 1 记的是同一件事,
   本槽是它**连着第二次**在实测里被量出红 0。
2. **本槽把 `epfix-produce-gate` 补进花名册,靠的是 `git log -S` 追首次登记提交,不是任何一条判据。**
   花名册的「首次登记」那一列写错波次号一条不红(判据只读首列编号),写漏一整行同样只在那个号被撤时才红。
3. **`TOPIC_FLOOR` 那一格的棘轮上限仍是 `SLACK` 3。** 本槽把它抬到 live 19 后差额 0,但判据允许它落后 3 格:
   M6 里 19 → 16 时销号留痕那条报的是"下限低于花名册"、`floor-slack` 那条**不报**(16 与 live 19 差 3,压在上沿)。
4. **W182 残留 7(底部空 `catch` 一直开着)本槽换了状态,但换来的不是"填掉"。**
   W181 判定停工位不成立并给了五条判据,故这一条不再是欠账;要留意的是**判据本身的边界**——
   它钉的是「那处 `catch` 只兜注册这一下」与「唯一走得到的形态在成品页不存在」,
   哪天有人给 `release.dirty` 开出**命名订阅者**,「下游是空的」那半理由就要重新掂量
   (W181 那条 `assertEq(subs.join(','), '', …)` 会当场红,这一层接得住)。
5. **W181 新带进来的四条源级护栏一条都没进 `GUARD_TOPICS`**:`js/release.js` 恰一处空 `catch`、
   `window.Bus` 全树只由 `bus.js` 一处整体赋值、`index.html` 里 `bus.js` 排在 `release.js` 之前、
   `js/release-core.js` 零空 `catch` 零 `Bus`。补登记要各自量锚点与落点数,不在本槽射程内。
   同一路上,W176 那条 `contract`(两端子集位)与 W177 那条 `contract`(过期镜分报的取数口)**仍未登记**
   (W182 残留 8 原样)。
6. **CLI 那一端的子集位仍只有源级一条接得住,没有行为面**(W179 残留 3 原样未收);
   W180 / W181 本槽新增的八条全在浏览器与 headless 两端,**冒烟套件本槽一条没加**。
7. **W177 的 `staleSplitNote` 那句话仍是拼出来的中文字面,没有第二处判据钉它的措辞**(W182 残留 5 原样)。
8. **G4 那个按钮在"全是定稿过期镜"时仍原样挂着**(W182 残留 6 原样)——摘不摘它是产品口径。
   与它同形的另一件本槽有意没碰:**G1 派出导航口的那三态,`fix` 上没有任何"到了那一页该做什么"的提示**,
   用户到该集工作区后仍要自己判断是补正文还是挑失败镜;W180 明写这属人工决策、回执不代授权。
9. **`js/pipeline.js` 断点条与 `js/issues.js` 的 `stale-shots` 仍是老形状**(W177 有意没碰,本槽同样没碰)——
   「回执说的和按钮做的对不对得上」这一面目前只有发布门 G1 / G4 两处收过。
10. **W170 残留 7、W161 残留 4(弱断言普查)仍是零**,本槽两支都没碰交付链、也没碰 `assert(A || B)` 那一类。
11. **`G-03` 的形态面、`G-11` 的自动进化面均未动**(W151 残留 1、3 原样);现取核实 `G-03` 仍是两条值。
