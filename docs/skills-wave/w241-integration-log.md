# W241 集成记账:两条槽并进同一条集成线(W239 生成那一拍说出「同 id 多行」 + W240 助手接入面两个个数按 live 收)

两条已完成功能支按次序 `--no-ff` 并进同一条集成线,两次 merge 的三个 SHA 全部 `fetch` 后 `rev-parse` **现取**:

- **merge 一** `8b350803`:`P1` = `bba2b4ca`(`origin/cursor/w238-integration-7e3d` 现取 tip)、
  `P2` = `332178d3`(`origin/cursor/w239-dup-rows-gen-note-4a1c` 现取 head,自称同值);叉点 `B` = `bba2b4ca`
- **merge 二** `3cecbc45`:`P1` = `8b350803`(上面那条 merge)、
  `P2` = `50e2601e`(`origin/cursor/w240-mcp-tools-count-a1f2` 现取 head,自称同值);叉点 `B` = `bba2b4ca`
- 全程没用 `--ours`、没用 `checkout <old> -- .`;`master` 没合、没开第三条功能支、没跑 `e2e.js`

---

## 〇、交接自称的 tip 与现取:W238 那格又是链中间的 merge,而交接文这次先说了

交接给的三个参考点逐个现取核对:

| 交接自称 | 现取 | 判 |
|---|---|---|
| W238 merge `0df77da` | `0df77da5` 是 W238 合入链中间那个 merge,其后压着 `bba2b4ca`(把份数与下限校到 253 的 docs 提交)才是 tip | **不是 tip**,按交接文自己写的「现取 tip」取 `bba2b4ca` |
| W240 从 `bba2b4c` 起手 | `merge-base(bba2b4ca, 50e2601e)` = `bba2b4ca` | 相等 |
| W239 head `332178d` / W240 head `50e2601` | 现取相等 | 相等(相等是核出来的,不是省掉这一步的理由) |

这已是第八次遇到「自称 tip 是链中间那个 merge」,与上一槽相同的是交接文本身已把这条规律写进口径。
两条被合入支的 `merge-base` **都是 `bba2b4ca`**——它们是从同一个点平行叉出去的两条槽,
这个拓扑直接决定了下面第二节两次 merge 的形状完全不同。

---

## 一、两侧相对叉点各带来什么

`git diff --numstat B P2` 两支全量:

| 文件 | W239 | W240 | 面 |
|---|---|---|---|
| `js/domain.js` | **+17 −0** | — | 新开双端派生 `D.dupRowsNote(picked, rows)` |
| `js/commands.js` | **+4 −0** | — | 回执组装后挂 `result.note` |
| `cli.js` | **+5 −0** | — | 跑之前 `log` 一句 + 回执上同挂一份 |
| `tests/unit.js` | **+136 −2** | **+32 −0** | W239 加 3 条(`domain` / `commands` / `contract`)+ 抬两个 `FLOOR`;W240 加 1 条 `contract`,**一个 `FLOOR` 都没碰** |
| `README.md`(根) | +2 −2 | +3 −3 | 见第四节 |
| `docs/AI助手接入指南.md` | — | +1 −1 | 工具数 37 → 39 |
| `docs/skills-wave/README.md` | +2 −1 | +2 −1 | 份数行 + 索引行 |
| 各自记账件 | +180 | +108 | 新文件 |
| `mcp.js` | — | **零改** | W240 只改文档与判据,一个工具没加 |

**两侧产品面零文件重叠**(W239 三个 `js`/`cli` 文件,W240 一个产品文件都没碰),
**而判据面与文档面三个文件全重叠**——`tests/unit.js`、根 `README.md`、`docs/skills-wave/README.md`。

---

## 二、四棵树机检:两次 merge 一次是退化形态、一次是极端偏斜

### 2.1 merge 一:整棵树层面 `P1 == B` **成立**

被合入支是从我方 tip 直接叉出去的,`merge-base` 就是 `P1` 本身,于是 534 个文件**全部** `P1 == B`,
其中「`P2` 真有改动」的 **7** 个(`README.md` / `cli.js` / `docs/skills-wave/README.md` /
`docs/skills-wave/w239-dup-rows-gen-note.md` / `js/commands.js` / `js/domain.js` / `tests/unit.js`)。
git 一处都不用合:**合完的树与 `P2` 的树逐字节相同**(`git write-tree` 与 `332178d3^{tree}` 同为 `7b161ac3`),
零冲突。`--no-ff` 在这里买到的只有一件事——**历史上留下一个能指名道姓的合入点**,树上一个字都不是它决定的。

这一格值得单记:**「零冲突」在这次 merge 里不是运气也不是判据保住的,是拓扑退化**。
它同时意味着 merge 一之后的 live(unit 658 / `contract` 142 / 记账件 254)**恰好等于 W239 自称的三个数**——
而这三个数在合完两支的 tip 上一个都不对,见第六节。

### 2.2 merge 二:按 hash 筛 528 个,真形态 **2** 个

| 文件 | `B` | `P1` | `P2` | tip | 判定 |
|---|---|---|---|---|---|
| `docs/AI助手接入指南.md` | `9084447a` | `9084447a` | `8a020d0f` | `8a020d0f` | **`P1 == B` 真形态**,整份取对侧 |
| `docs/skills-wave/w240-mcp-tools-count.md` | — | — | `54d3b2d6` | `54d3b2d6` | **`P1 == B` 真形态**(新文件) |
| `cli.js` | `52ccfa6d` | `ec911d76` | `52ccfa6d` | `ec911d76` | `P2 == B`,整份取我方 |
| `js/commands.js` | `ebcada01` | `fe7493e0` | `ebcada01` | `fe7493e0` | `P2 == B`,整份取我方 |
| `js/domain.js` | `8af2a73f` | `d21ce53a` | `8af2a73f` | `d21ce53a` | `P2 == B`,整份取我方 |
| `docs/skills-wave/w239-dup-rows-gen-note.md` | — | `b2ab38c9` | — | `b2ab38c9` | `P2 == B`(新文件) |
| `README.md`(根) | `72bf8377` | `eb0b9fcf` | `64be322f` | `18941ab7` | 两侧都改,取并集 |
| `docs/skills-wave/README.md` | `db2e0159` | `aacca551` | `eb8412f3` | `c27d985f` | 两侧都改,取并集 |
| `tests/unit.js` | `3dca88cc` | `bc923d19` | `9924e161` | `080cfdb4` | 两侧都改,取并集 |
| 其余 526 个 | 同 | 同 | 同 | 同 | 四树全同(退化形态) |

四槽连看真形态 / 筛出:W234 **1/9**、W237 **4/277**、W238 **0/523**、本槽 merge 二 **2/528**。
与上一槽的读法一致:**这个数量的是"两侧改动面重不重叠",不是趋势也不是风险读数**。
本槽两支产品面零重叠而判据/文档面全重叠,于是真形态只落在 W240 独占的那两个文档文件上,
反向 `P2 == B` 出了四个,全在 W239 那条派生的落点上。

---

## 三、`FLOOR` 静默落后的第四种路径:落后不是抄来的,是**另一侧没抬**直接决定的

W237 记过两种(自动取对侧靠另一侧没动过 / 报冲突靠两侧都动过),W238 记了第三种(两侧动成同一个值,git 不报冲突)。
本槽出的是第四种,而且交接文事先点了名:**W240 有意不抬自己那两格 `FLOOR`**(加了 1 条用例,`FLOOR` 字面一个没碰),
于是那两格在合入时**连"两侧不一致"这个共谋都不需要**——git 只看到一侧改过,自动取我方,
而我方那个数是按**只含 W239 的 live** 算出来的,合完必然低正好 W240 新增的那么多格。

五个棘轮格逐格现取四棵树(`B` / `P1`=merge 一 / `P2`=W240 / 合完 live):

| 格 | `B` | `P1` | `P2` | 合完 live | git 怎么处置 | 收成 |
|---|---|---|---|---|---|---|
| 单元测试 `FLOOR` | 655 | **658** | 655 | **659** | 只我方改过,自动取我方 658 | 659 |
| 集成测试 `FLOOR` | 148 | 148 | 148 | 148 | 四树全同 | 148 ✔ |
| CLI 冒烟 `FLOOR` | 117 | 117 | 117 | 117 | 四树全同 | 117 ✔ |
| 记账件份数 `FLOOR` | 253 | **254** | 253 | **255** | 只我方改过,自动取我方 254 | 255 |
| 护栏主题 `TOPIC_FLOOR` | 19 | 19 | 19 | 19 | 四树全同 | 19 ✔ |
| README 单元断言数 | 655 | **658** | **656** | **659** | 两侧异值,**报冲突** | 659 |
| README `contract` 条数 | 141 | **142** | **142** | **143** | 两侧同值,那一行另因散文报冲突 | 143 |
| 声明记账件份数 | 253 | **254** | **254** | **255** | **两侧同值,不报冲突,静默合成 254** | 255 |

**两格 `FLOOR` 合完都恰好落后 live 1 格,而 `SLACK` 是 3,五层判据一条不红**(第七节反事实第 3、5 档实测全绿)。
接住这两格的判据一条都没有——`live >= floor` 成立、差额 1 在预算内、`floorLagVerdict` 回空。
唯一会响的报警器仍是它们在文档里那两个走**相等**判据的孪生数字,而那两个数走的是另一条路:
`README` 单元断言那行**两侧写了不同的数**(658 vs 656)故报了冲突,声明份数那行**两侧写成同一个 254** 故一声不吭。

**故五格一律合完现跑重取**,收成 **659 / 148 / 117 / 255 / 19**,五格差额全 **0**。
交接明令的「不要把落后抄进 tip」是这样落地的:不是"不抄 W240 那两个数"(它根本没写),
而是**不留我方那两个数**——它们在自己那棵树上是对的,合完就不是了。

---

## 四、冲突逐处怎么收:两条长行各由**另一侧**插了散文,数字都不是重点

merge 二报了三处冲突,分在两个文件。

### 4.1 根 `README.md` 两条长行:两侧的角色正好互换

`B` / `P1` / `P2` 三份逐行 opcode:`P1` 改了 2 行(363 / 612),`P2` 改了 3 行(188 / 363 / 612),
重叠的恰是 363 与 612 两行。这两行三份逐字节量:

```
l363 契约段     长度 B 4948 | P1 4948 | P2 5132
                掩数字后与 B 逐字节相同   P1 true | P2 false
                行内数字只有第 0 个不同    B 141 | P1 142 | P2 142
                P2 多插 184 字:MCP 工具数取运行期 tools/list、CLI 命令数取 Object.keys(CMD)、命令总览改逐条点名

l612 单元段     长度 B 17189 | P1 17435 | P2 17189
                掩数字后与 B 逐字节相同   P1 false | P2 true
                行内数字只有第 0 个不同    B 655 | P1 658 | P2 656
                P1 多插 246 字:点名 id 占多行时两端回执各带一句 note、双端同读 Domain.dupRowsNote、选人闸仍按行筛
```

**两行的角色正好互换**:363 行上 `P2` 是"改了数还插了散文"的那一侧、`P1` 只动了那一个数;
612 行上反过来。故 363 取 `P2` 整行、612 取 `P1` 整行,两行的数字再各按 live 校到 **143** 与 **659**。
**这不是"两行各取一侧",是先量出另一侧在那一行上的全部贡献就是那个数、而那个数按 live 收了之后的并集。**

同一个文件里第 188 行(MCP 工具数 38 → 39)只有 `P2` 改过,git 自动取对侧、一个字没进冲突面,而 39 恰是合完 live——
这一格与上一槽 `contract` 条数那格同形,靠的都是"另一侧当轮没碰这个数"。

### 4.2 `docs/skills-wave/README.md`:份数行没报冲突,索引表报了

份数行 `B` 253 / `P1` 254 / `P2` 254,把数字整体掩成 `#` 后三份逐字节相同——**两侧同值,git 静默合成 254**,
而合完目录实况是 255。按 live 收:merge 提交那一格 **255**,本文落地后 **256**。

索引表两侧各带一行(w239 / w240),取并集后按波次号归位成 **239 / 240**(接在 238 之后)。
用行文本多重集对账:

```
index rows  B/P1/P2/tip: 253 254 254 255
rows missing from tip: 0 | rows in tip from nowhere: 0
row delta  P1 +1 -0 | P2 +1 -0
accounting 253+1+1 = 255 vs tip 255
wave-number order regressions: 0
```

### 4.3 `tests/unit.js` 一处冲突都没报

两侧新增的用例落在文件的不同段落(W239 在 `commands` / `domain` / `contract` 三处,W240 在 `contract` 尾部另一处),
两个 `FLOOR` 字面只有我方改过。git 全自动合完、一处都没问,**而合出来的树上两格 `FLOOR` 都比 live 低 1 格**——
第三节讲的就是这个。

### 4.4 三个文件的 numstat 三向对账

```
README.md                    P1 +2 −2 | P2 +3 −3 | tip +3 −3   → 2+3−2 = 3 | 2+3−2 = 3
docs/skills-wave/README.md   P1 +2 −1 | P2 +2 −1 | tip +3 −1   → 2+2−1 = 3 | 1+1−1 = 1
tests/unit.js                P1 +136 −2 | P2 +32 −0 | tip +168 −2 → 136+32−0 = 168 | 2+0−0 = 2
```

减数先点名再相减:根 README 减的 2 行恰是 363 与 612(两侧都动过),
目录 README 减的 1 行恰是份数行,`tests/unit.js` 两侧零重叠故一行都不减。

---

## 五、名集按 `|` 切做多重集:两侧 `+3` 与 `+1`,去掉各 `0`

四棵源树与 tip 快照都在全绿状态上取名集(`B` 655/655、W239 658/658、W240 656/656、tip 659/659 全 PASS),
按 `|` 切出 `套件 · 用例名`,排序后保留重复(多重集,不退化成 Set):

| 套件 | `B` | W239 | W240 | tip | W239 增删 | W240 增删 | 对账 |
|---|---|---|---|---|---|---|---|
| `unit` | 655 | 658 | 656 | 659 | **+3 −0** | **+1 −0** | `655+3+1 = 659` 自洽 |
| `contract`(子集) | 141 | 142 | 142 | **143** | **+1 −0** | **+1 −0** | `141+1+1 = 143` 自洽 |
| `integration` | 148 | 148 | 148 | 148 | **+0 −0** | **+0 −0** | 源文件四树 blob 全同 |
| `cli.smoke` | 117 | 117 | 117 | 117 | **+0 −0** | **+0 −0** | 源文件四树 blob 全同 |

tip **零缺失、零多出**,两侧「去掉」各 **0**;名集大小恰等于登记条数(655/658/656/659),无重名。

两侧新增逐条按名点住:

- W239 `unit` +3:
  - `commands · generateVideos 点名重复 id 真跑那一趟:两端回执都说出行数并点名 shots-dedupe(选人闸仍按行筛,一行都不许少跑)`
  - `domain · dupRowsNote:点名的 id 在表里占着多行时把行数与出口说出来(没点名/不重复一律一句不说)`
  - `contract · 点名的 id 占多行那句实话双端单源:两端真跑回执都现取 Domain.dupRowsNote,选人闸仍按行筛没被改成按 id 去重`
- W240 `unit` +1(同时也是 `contract` +1):
  - `contract · README 数字对账:MCP 工具数按运行期 tools/list 现取、CLI 命令数按 Object.keys(CMD) 现取`

**交接明令的「W239 unit +3、W240 unit +1 应都在」在这张表上是量出来的**:
两侧的增量同时在 tip 里,`−0` 两个方向都钉住,`655 + 3 + 1 = 659` 与实测相等。

---

## 六、`contract` 那格:两侧都自称 142,合完 live 是 143

交接文点名的那格现取核出来正是它警告的形状:

```
套件         P1 净增   P2 净增   两侧自称       live    谁对
unit          +3       +1      658 / 656      659     都不对
contract      +1       +1      142 / 142      143     都不对
integration    0        0      148 / —        148     相等
cli.smoke      0        0      115/117 / —    115/117 相等
记账件         +1       +1      254 / 254      255     都不对
```

上一槽写下的那条规律在本槽照旧成立:**一侧的自称恰等于 live,当且仅当另一侧在那个套件上净增 0。**
本槽 `unit` / `contract` / 记账件三个套件两侧各有净增,故**六个自称数一个都不对**;
而 `integration` 与 `cli.smoke` 两侧净增都是 0,故两个"相等"是拓扑给的,不是核过的结果。

`contract` 那格另有一层值得单记:**两侧写的是同一个 142**。
若那一行上没有别的差异,git 会像声明份数那行一样静默合成 142、一声不吭——
本槽它之所以进了冲突面,靠的是 `P2` 在同一行上还插了 184 字散文这件**与数字无关**的事。
换句话说,**这一格的报警器是散文,不是数字**。第七节第 2 档量出:真把 142 留在那儿,`contract` 当场红 1 条。

---

## 七、反事实:十三档,每档改完先回读再下结论

在合完的树(目录实况 255、live 659/148/117/255/19)上跑,每档改完把被改的字面回读一遍确认变异真落进去了才看结果:

| 档 | 回读 | `contract` 结果 |
|---|---|---|
| 1 全按 live(基线) | — | **143/143 全绿** |
| 2 `contract` 条数写 142(两侧自称的那个数) | 142 | **红 1**:`contract 套件断言条数:README.md 与实测不符(实测 143,文档 142)` |
| 3 单元 `FLOOR` 留 658(git 自动取我方那个数) | 658 | **143/143 全绿** |
| 4 单元 `FLOOR` 655(抄 W240 那侧,落后 4 格) | 655 | **红 1**:`单元测试:下限 655 落后实测 659 共 4 格(上限 3 格)` |
| 5 记账件 `FLOOR` 留 254(自动取我方) | 254 | **143/143 全绿** |
| 6 记账件 `FLOOR` 251(落后 4 格) | 251 | **红 1**:`记账件份数:下限 251 落后实测 255 共 4 格` |
| 7 README 单元断言写 658(抄 W239) | 658 | **红 2**:相等那层 + 「README 写的 658 低于下限 659」 |
| 8 README 单元断言写 656(抄 W240) | 656 | **红 2**:同上,数字换成 656 |
| 9 声明份数 254 + `FLOOR` 254(连带取两侧同值) | 254 / 254 | **红 1**:三方对齐那句(期望 254 实际 255) |
| 10 根 README 契约段**整行取 `P1`**(丢掉 W240 那 184 字散文,数字仍 143) | 散文没了、143 在 | **143/143 全绿** |
| 11 根 README 单元段**整行取 `P2`**(丢掉 W239 那 246 字散文,数字仍 659) | 散文没了、659 在 | **143/143 全绿** |
| 12 `cli.js` 那端自己拼一句(不读 `Domain.dupRowsNote`) | 字面已换 | **红 1**:`cli.js 的真跑回执须现取 Domain.dupRowsNote(不许就地拼第二句)` |
| 13 `js/commands.js` 那端不挂 note | 字面已换 | **红 1**:`js/commands.js 的真跑回执须现取 Domain.dupRowsNote` |

第 4、6 档落后 4 格红了,这一对照同时证明**变异确实落进了判据**——第 3、5 档的全绿不是"根本没改成"。

**本槽最值得抄走的一格是第 10、11 档。** 两条长行的冲突里,数字那一面有相等判据钉着(第 2、7、8 档三条都红),
而**散文那一面一条判据都没有**:整行取任一侧、把数字校到 live,`contract` 143/143 全绿,
丢掉的是对侧那一整段说明(MCP/CLI 取数口径,或者双端 note 那件事)。
也就是说——**长行冲突里"取哪一侧"这个决定,机器只替你看住那一个数,散文全靠人工逐段量。**
本槽的做法(逐行 opcode + 掩数字逐字节量出"另一侧只动了那个数",再取带散文那一侧的整行)不是讲究,是这一格唯一的守卫。

判词与产品面另外五手变异,各红各的、报错句互不同形:

| 变异 | 结果 |
|---|---|
| 选人闸改成按 id 去重(`arr.findIndex` 收窄待跑清单) | **红 1**:`js/commands.js 的待跑清单被按 id 收窄了——第二行会永远跑不到,这句 note 不是拿来给它开路的` |
| `dupRowsNote` 不再点名 `shots-dedupe` | **红 1**:`这句话须点名 shots-dedupe 那条出口(只报警不给活路等于没说)` |
| README MCP 工具数改回 38 | **红 1**:`MCP 工具数:README.md 与实测不符(实测 39,文档 38)` |
| 接入指南工具数改回 37 | **红 1**:`MCP 工具数:docs/AI助手接入指南.md 与实测不符(实测 39,文档 37)` |
| `mcp.js` 工具表插一条探针(live 40 而两处文档 39) | **红 1**:`MCP 工具数:README.md 与实测不符(实测 40,文档 39)` |

最后一条同时证了 W240 那条用例**取的是运行期 `tools/list` 而不是源码字面**:探针是加在源码表里的,而报错报的是运行期读回的 40。

---

## 八、数字:全部合完 live 现跑

| 套件 | live | 备注 |
|---|---|---|
| `unit` | **659 / 659** | 全绿 |
| `contract`(unit 子集) | **143 / 143** | 全绿;`141 + 1 + 1 = 143`,两侧都自称 142 |
| `integration` | **148 / 148** | 全绿 |
| `cli.smoke` | **115 / 117** | 单独整跑 `env -u HUJING_SERVER` |
| 记账件 | **256** | merge 提交那一格 255,本文落地后 256 |

`cli.smoke` 失败两条,与 `master`(`9adcf0ff`,现跑 **51/53**)**同名同表现**:

- `未登录 whoami → exit 3 | exit=1`
- `llm --json mock 链路 | undefined`

冒烟一律单独跑并带 `env -u HUJING_SERVER`:手工 live 验证会往 env 里塞服务地址,同壳跑会被劫到错端口。

助手接入面两个数现取(W240 那条用例的四层各取一遍):

```
运行期 tools/list 工具总数     39   (源级 TOOLS 表逐名相等)
README「N 个 hujing_* 工具」   39
接入指南「N 个工具」            39
CLI 命令数  CMD['x'] 25 + CMD.x 24 = 49 == Object.keys(CMD).length
mcp.js 相对叉点               零改(blob 四树全同)
```

---

## 九、合完的树上真跑:W239 那句话在真 `server.js` + 真 `cli.js` 上走一遍

临时服务 + 临时账号 + 临时配置目录,逃生舱灌一张存量重复表(`dup` / `solo` / `dup` / `dup`,全部确认且已有图),
点名一个 `dup` 真发生成(上游未配置故三行各自如实报错并退费——不拿占位冒充成功):

```
[1] state-put 灌表            ok,读回 ["dup","solo","dup","dup"]
[2] 跑之前 stderr 先说一句     批量生成:3 镜待处理(串行,服务端限流)…
                              点名的 1 镜在分镜表里占着多行同 id(dup 3 行):这一趟按 3 行逐行跑、
                              逐行计费,比点名数多花了 2 行的钱。要一个 id 只跑一行,先用 CLI shots-dedupe 收拾存量重复。
[3] 回执                      total:3,note 与 stderr 那句逐字相同
[4] 真下发的行                failed 三条 shotId 全是 dup,order 1 / 3 / 4(选人闸按行筛,一行都没少跑)
[5] 余额                      before 100 → after 100,净额 0(三笔都失败、都退了)
[6] 表                        ["dup","solo","dup","dup"] —— 生成这一趟一行都没被顺手去重
[7] 点名 dup+solo             「点名的 2 镜…dup 3 行…按 4 行逐行跑…多花了 2 行的钱」(多花的按各自之和算)
[8] 整集那一路(没点名)        note undefined,total 4
[9] shots-dedupe --apply      applied true,renamedIds 2
[10] 收拾完再点名 dup          total 1,note undefined —— 这句话不是常驻噪音,表干净就不再露面
```

`[9]`/`[10]` 两步把这句话与它指向的那条出口串成闭环:**它点名的收拾办法真收拾得掉,收拾完这句话自己消失**。

W240 那一侧**没有产品面可证**——`mcp.js` 零改、只动文档与判据。故它的"真跑"是两件事:
运行期 stdio JSON-RPC `tools/list` 读回 **39** 条(含 `hujing_shots_dedupe`),
以及第七节那三手针对它的变异(README 38 / 接入指南 37 / 源码插探针)各红各的。
**判据的真跑是变异,不是把它再读一遍。**

---

## 十、治理面:一格未动

- `Skills.gaps()` **20 键**一个没剥(运行期读回):
  `G-08 G-15 G-09 G-01 G-02 G-12 G-13 G-04 G-10 S-01 S-03 G-07 G-14 G-05 S-04 S-05 G-03 G-11 S-06 S-07`
- **SK-04 / G-11 / G-13 原样开着**,`GUARD_TOPICS_CLOSED` 仍 **0 条**(空表)
- `GUARD_TOPICS` 花名册 **19 条**、`TOPIC_FLOOR` 仍 **19**、`w178` 花名册 **19 号**——两侧都没新登记主题
- `SLACK` 四棵树逐树现取都是 **3**,**一字未改**(交接明令)
- `floorLagVerdict` 在 tip 上 **7 处**、`shots-dedupe` 在 `cli.js` **5 处** / `tests/unit.js` **17 处** / `mcp.js` **2 处**,W238 与 W235 两槽的东西一处没掉
- `episode.compose` 参数面仍 `pid,epid,force,ui`(`js/cmd-registry.js` 四树全同)
- `js/skills.js` / `js/produce.js` / `js/plans.js` / `js/issues.js` / `js/wf-core.js` / `js/cmd-registry.js` /
  `server.js` / `js/store.js` / `mcp.js` 四树 blob 全同,故 W228 顶层提 note、W224 `smartReview` 那句、
  W219 `plans` 过滤、W221 按钮那条(`pipeline · nextForEp`:按钮上的数就是真下发的镜数)、
  W232 钉在 `W.blankShot` 上那句都不可能被剥;名集 `−0` 两个方向再钉一次
- `cli.js` 相对叉点只有 **5 行**改动、全落在 `EXEC['episode.generateVideos']` 段内(差异段 1119–1151),
  与 W226 那道 `shots-import` 唯一闸(721–760,`taken` 建在 `ep.shots =` 之前、撞 id 走 `do…while`、回执带 `renamedIds`)
  **区间不相交**;闸逐条现取都在
- **`state-put` 没被顺手补闸**:落库路径上无 `taken`、无按 id 建集,仍是 `need(f.force, …)` 一道闸。
  交接明令的「不要给 state-put 设闸」结构性成立
- **选人那一侧 `ids.has(s.id)` 按行筛一个字没动**(`js/commands.js` 108 与 `cli.js` 1107)——
  W239 补的是话不是少跑,`contract` 那条源级判据(待跑清单不许被 `new Set`/`findIndex`/`indexOf` 收窄)现在替它守着
- `shots-dedupe` 字面在 `js/` 与 `server.js` 下从 0 处变 **1 处**:那一处是 `js/domain.js` 里 `dupRowsNote` **说出来的那条出口名**,
  不是挂到保存路径上的调用——「不静默迁移」仍结构性成立

另记一手 W236 那条口子的 live 形状:除自己那行索引外**零 markdown 相对链接**的记账件,
W238 当轮是 252 份里 131 份,本槽现取是 255 份里 **134** 份。**这条口子的口径随每槽记账件线性变宽**,它不会自己收敛。

---

## 十一、交接:两格原样留着,一格新料

**未收(交接明令不代修),两格:**

1. **`state-put` / `PUT /api/state` 不校验** —— W231 已判定为**契约而非漏洞**,本槽不翻案、不补闸。
   因果没解除(第九节 `[1]` 现跑把重复灌回去),但 `shots-dedupe` 收拾得掉,而**这一槽起它还会在生成那一拍说出来**。
2. **同 id 两镜时选人双扣费** —— `ids.has(s.id)` 按行筛一个字没动;W239 收的是"闷声"那一面,不是双扣费本身。

**给下一槽留一条现取的料。** W236 → W238 已经把 `FLOOR` 静默落后的三种机械形状写进明面
(抄偏低的一侧 / 自动取对侧 / 两侧同值静默合)。本槽量到第四种,而且它是**被交接文事先声明的**:
**一侧有意不抬自己那格 `FLOOR`,合入时那一格连"两侧不一致"都不会出现**——
git 只看到一侧改过,自动取我方,而我方那个数按只含自己那支的 live 算,合完必然落后正好对侧新增的格数。
四种形状的共同点已经很清楚:**`FLOOR` 那一格永远量不出"合完是多少",它只记得某一棵父树上是多少**。
唯一的工序仍是合完把五格全部现跑重取,而不是在冲突面上挑。

另留一条本槽第七节量出来的新形状(它比 `FLOOR` 那件事更难看见):
**长行冲突里,数字有相等判据钉着,散文一条判据都没有。** 整行取任一侧、把数字校到 live,整套 143/143 全绿,
而对侧那一整段说明就此消失,`git log` 上看不出来、判据上也看不出来。
多槽并进且各槽都往同一段长散文里补话时,这个形状每一次合入都会出现一遍。

**按口径跳过的:** `master` 没合、没开第三条功能支、没给 `state-put` 设闸、没改 `SLACK`、没改选人闸、
没剥 `gaps()`、没跑 `e2e.js`。两个方向的偷懒(该核的不核 / 不该改的顺手改)都没走。
