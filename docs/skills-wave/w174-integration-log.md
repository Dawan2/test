# W174 · 三条槽并入集成线的收敛记录(集成分支)

> 集成分支:`cursor/w174-integration-7f3d`,基线 `cursor/w170-integration-98b2 @ c2acb4d`。
> 本文只记**收敛过程**:基线怎么定的、合了哪三条、冲突面长什么样、合并后的实测数字、没删测没摘 gaps 的取证。
> 被合入三槽自己的内容分别在 `w168-unrvep-gate.md`、`w169-extract-no-script.md`、`w171-tautology-assert.md`,本文不代述、不复写、不改它们正文里的任何时点读数。
> 三次合并都 `--no-ff`、各一个合并提交、可 revert;全程只解冲突与收敛数字,**不重做已落地的功能**(唯一的例外是把本槽合进来的两道源级护栏登记进 `GUARD_TOPICS`,那是任务口径点名要补的一格,见 5.7)。

## 1. 结果一句话

`--no-ff` 合入三条,按序、**各取被合入支的实际 HEAD**:

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w168-unrvep-gate-813a@264bfe3` | `93acbbe` | 流程条「审片」的挑集导航 `Pipeline.hashOf('review')` 改读 `Domain.episodeState().reviewGate` 那道门槛:缺正文 / 未拆镜 / 分镜表判旧的集当下审不了(`unready`),不再因为"没有达标分"被挑成落点,无可审集时退回首集 | `js/domain.js`(门槛表达式与基线上 W165 那份逐字节相同,只有注释首行不同)、`js/pipeline.js` 一行 | +2 `unit`(1 `pipeline`、1 `domain`) |
| `w169-extract-no-script-d1c8@de73480` | `2711d6c` | 提取主体读入的那份文本收成 `Domain.extractSourceText`(整本原文优先、没有整本退回各集正文拼接、提取结论一概不算),三端命令入口(浏览器 `Commands` / CLI `EXEC` / 服务端 `wf` 端点)与计划层同读;计划层的提取步据此分两态——读得到原文出可执行命令步,整本与各集正文都没有时出**补原文的导航步** | `js/domain.js` +14、`js/plans.js` +11 −3、`js/commands.js` / `cli.js` / `server.js` 各 1 行换取数口 | +3 `unit`(1 `commands`、1 `plans`、1 `contract`)、+2 `integration` |
| `w171-tautology-assert-c918@6e451ee` | `8d7dad2` | 发布门 G4–G6 那句**恒真**源级断言收紧:原写法 `/aggFail: 0/.test(rel) \|\| /counts/.test(rel)` 前一半在 `js/release.js` 里零命中、后一半只要整份文件任一处出现 `counts` 就成立(段头注释里就写着「统一从 Domain.counts 聚合」)。改钉那段的**可执行行**——注释按行首形态整段排除、三门共用的那次遍历必须现取 `Domain.episodeState(...)`、进 `agg` 的加数得是它回的 `counts` | 产品代码零改动 | `unit` 条数不变(在既有那条 `release` 用例里就地收紧) |

合并后回归 **`unit 547/547`**、**`integration 143/143`**、**`cli.smoke 105/107`**(2 项失败与基线同名同表现,见 5.2),`contract` 子套件 **122/122**。

治理面:`gaps()` **20 键**(`G-03` 仍是两条值、`G-11` 仍在案)、提示词注册表 **41**、能力短名单 **30**、`Skills.CHECKS` **17**、`preflightStages()` **七面**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 八个数一个没动。
本槽**注册表五个文件相对基线零 diff**(`git diff --stat c2acb4d HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js` 空输出),故这八个数是结构性成立,与 W170 / W166 / W161 / W157 同形。

**本槽最值得记下的五件事:**

1. **任务口径预告的两处"自称与基线逐字节相同"实测都成立,而两处的冲突形态相反。**
   `w168` 的 `reviewGate` 与基线上 W165 那份:表达式那四行**逐字节相同**,`git` 只把注释首行划进冲突块(现取核对整段 15 行,差的只有那一行)。
   `w169` 的 `projectScript` 与基线上 W167 那份:整段 5 行(4 行注释 + 1 行赋值)**逐字节相同**,`git` 因此连块都不给,冲突块是**对侧多出来的那段 `extractSourceText`** 被划进了一个"我方为空"的纯插入块。
   即同样是"两侧写下同一份东西",一处给块(注释差一行)、一处不给块(整段全等),而两处的处置都是**门槛/派生只留一份**:合并后 `js/domain.js` 里 `reviewGate` 恰一处、`projectScript` 恰一处、`extractSourceText` 恰一处,`extractSourceText` 内部现取 `projectScript`,不写第三份(3.1、3.2)。

2. **`js/plans.js` 那两个取材器真的撞上了,而 `git` 一个块都没给——撞的是同一条既有用例的期望串。**
   基线上 W167 把 `project.splitEpisodes` 那个取材器改成两态,`w169` 把紧挨着的 `project.extractSubjects` 改成两态。两侧改的行不相干,`git` 零冲突块把两半都搬了进去,合并后两个取材器各占各的。
   真撞的是 `tests/unit.js` 里 W147 那条既有用例:它对"提取过主体、没留原文、连分集都没建"的老项目断言整串投影步,**两侧各把它改了一半**——我方是 `'project.extractSubjects,goto:split'`,对侧是 `'goto:extract,project.splitEpisodes'`,**两侧给的都不是答案**,合并后的实况是 `'goto:extract,goto:split'`(两步都成了导航步)。这一格是在冲突块里判的,而判它靠的是把两个取材器的分流条件各读一遍,不是二选一(3.2、5.5 的 M4 反向印证)。

3. **一道门槛三个消费面,而"三侧都还在跑"是靠一条变异同时红三处证出来的。**
   基线上 W165 给 `reviewGate` 接了两个消费面(`workflow` 的 `review` 步三档集数、问题中心的分集审片条目),`w168` 接的是第三个(流程条挑集导航)。M2 把 `reviewGate` 的 `unready` 那一档去掉,实测**红 4**:`pipeline` 那条(W168)、`domain` 的 `workflow` 那条与 `domain` 的门槛那条、`issues` 双向相等那条(W165)——三个消费面一个不落。
   README 那句"两处不各设一道门"因此在合并后**当场失真**,`js/domain.js` 的注释首行也一样:两处都按实况改成点名消费方而不是数消费方(3.1)。

4. **W170 残留 1 那一格本槽补上了,而补完当轮就被一条"数字层完全看不见"的变异实测到。**
   把 `w171` 收紧的那句断言**退回原来的恒真写法**(M7):用例数仍是 547、README 仍对、`FLOOR` 仍对、行为面一条不红——在**登记进 `GUARD_TOPICS` 之前**这一手在 tip 上跑出来是 **547/547 全绿**(在 `8d7dad2` 的独立 worktree 上现跑复核过)。登记之后同一手**红 1** 并把主题编号 `g456-count-source`、它的两个锚点与 `why` 原样印出来。
   **这正是 W170 残留 1 说的那种形态**:数字层拦得住"删一条用例",拦不住"把判据放宽回恒真"——后者一条用例都没少。本槽合进来的两道源级护栏(`extract-source-single`、`g456-count-source`)因此都登记了,`TOPIC_FLOOR` 按 live 从 10 抬到 12;没登记的那几道逐条点名在 6.1。

5. **`w171` 收紧的那句断言是真的有新覆盖,而"新在哪"要挑对变异才量得出来。**
   把 G4–G6 改成就地数镜头(M9)在**基线上已经红 1**——不是那句恒真断言接住的,是 W145 那条"遍历抛错记 warn"的行为面顺手接住的(那一改把 `Domain.episodeState` 的调用一并去掉了)。
   挑对的那一手是 M10:**`Domain.episodeState(p, ep, online)` 照旧调用**(抛错那一路照旧成立),只把进 `agg` 的加数换成就地数出来的另一份——两份值一模一样。**基线上 542/542 全绿**,tip 上**红 1**,报的正是 `w171` 新加的第二句「进 `agg` 的加数仍得是 `episodeState` 回的 `counts`(换个来源即红)」。
   即"值一样也是第二份投影"这一课本槽第三次被实测(W164 的 M5、W170 的 M3 各一次),而这次接住它的仍然只有源级那一条(5.5)。

## 2. 基线与这三个槽

### 2.1 基线

任务口径给定 `origin/cursor/w170-integration-98b2 @ c2acb4d`。现取核实,并逐支核对任务给的参考 sha:

```
$ git rev-parse origin/cursor/w170-integration-98b2
c2acb4de5c91352b76241956ebe38061ff0f962a
$ git rev-parse --short origin/cursor/w168-unrvep-gate-813a      → 264bfe3
$ git rev-parse --short origin/cursor/w169-extract-no-script-d1c8 → de73480
$ git rev-parse --short origin/cursor/w171-tautology-assert-c918  → 6e451ee
```

四个都与任务给的参考值一致(三支都取的现取 HEAD)。

基线上的三套件数字(开工时现跑,不引用 `w170-integration-log.md` 里的数):

| 套件 | 基线 `c2acb4d` |
|---|---|
| `node tests/unit.js` | **542 / 542**(`contract` **121 / 121**) |
| `node tests/integration.js` | **141 / 141** |
| `node tests/cli.smoke.js` | **105 / 107** |

与任务给的参考数字逐个相符,`cli.smoke` 那 2 项失败也与任务口径说的一致(见 5.2)。

### 2.2 三支的叉点:三条都是真三方

| 支 | 与基线的 `merge-base` | 那是什么 | 基线在其之上还有 | 该支自己有 |
|---|---|---|---|---|
| `w168` | `c5de21e` | W161 集成记账件 | **23 个提交** | 3 个 |
| `w169` | `c5de21e` | 同上 | **23 个提交** | 2 个 |
| `w171` | `4eaea86` | W166 集成记账件 | **10 个提交** | 2 个 |

三次都 `merge-base != HEAD`,**都是真三方合并**。

基线在这两个叉点之上的提交里含 W162 / W163 / W164 / W165 / W166 / W167 / W170 等槽,故:

- W165 的 `reviewGate` 已在基线的 `js/domain.js` 里,而 `w168` 叉在它**落地之前**——两支各自写下了一份门槛,而表达式逐字节相同(1.1、3.1);
- W167 的 `projectScript` 同理已在基线里,`w169` 那份与它逐字节相同,冲突块因此只包住对侧多出来的 `extractSourceText`(3.2);
- W167 改过的 `js/plans.js` 拆集取材器与 `w169` 改的提取取材器是相邻两段,`git` 零冲突块合上,真撞的是测试里那条既有用例的期望串(1.2、3.2);
- `w171` 叉在 W166 之后,故它看得见 W162 那几条源级判据,`tests/unit.js` 那段改写与基线自 `4eaea86` 之后的改动不在同一段——零冲突块(3.3)。

### 2.3 不合的

任务口径明确不合在飞的 **W172** 与 **W173**,本槽一条都没碰,也未做叉点核实。
除此之外远端未合入本线的 `cursor/*` 支,按任务口径不在本槽范围内。

## 3. 冲突面

三次合并共 **12 个文件次、21 个冲突块**,分布:

| 合并 | `README.md` | `docs/skills-wave/README.md` | `tests/unit.js` | 产品文件 | 小计 |
|---|---|---|---|---|---|
| `93acbbe`(W168) | 2 | 2 | 2 | **1**(`js/domain.js`) | 7 |
| `2711d6c`(W169) | 2 | 2 | 5 | **2**(`js/domain.js` 1、`js/commands.js` 1) | 11 |
| `8d7dad2`(W171) | — | 2 | 1 | **0** | 3 |

**产品文件冲突块 3 个**,分在前两次合并里。

### 3.1 第一次合并(W168 `264bfe3` → `93acbbe`,7 块,含 1 个产品块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| **`js/domain.js`** | `reviewGate` 那段的**注释首行** | 表达式四行两侧逐字节相同,只有注释首行不同:我方(W165)「…主线 review 步与问题中心的分集审片条目同读这一份,**两处**不各设一道门」/ 对侧「…凡是"这一集该不该去审"的判断都读这一份,**消费方**不各设一道门」 | **取对侧那一行**:合并后消费方从两个变成三个,我方那句「两处」会当场失真,而对侧那句不数消费方、加一个也仍然成立。门槛本体一份不动 |
| `tests/unit.js` | 三套件下限 | 542/141/107 / 530/141/107 | 单元现取 live **544**;集成与冒烟两侧同值,取我方 |
| `tests/unit.js` | 记账件 `FLOOR` | 183 / 175 | 现取 live **184**(对侧新增 `w168-unrvep-gate.md` 一份) |
| `README.md` | 「🧐 审片主线步骤」整句 | 我方带 W165 的门槛单源整段 / 对侧只在 `Pipeline.hashOf('review')` 那一小节后面接了一句 | **并集**:取我方整句 + 对侧那句按锚点接回,并把「两处不各设一道门」改成点名三个消费面 |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带全部数段 / 对侧另在 `domain.js` 与 `pipeline.js` 两节里各插一段 | **并集**,两段按各自锚点插回;数字换 live **544** |
| `docs/skills-wave/README.md` | 明写份数 | 183 / 175 | 现取 live **184** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方 `w162`…`w170` 七行 / 对侧 `w168` 一行 | **并集**,八行按波次号排(`w168` 归到 `w167` 与 `w170` 之间) |

**`js/pipeline.js` 零冲突块,而这不是取 `--theirs`**:我方相对叉点零 diff(`P1 == B`,基线自 `c5de21e` 之后一行都没动过它),`git` 只能搬对侧那份。分开靠 `P1 == B` 这一格,不是靠哈希对上(5.1)。

**门槛注释那一格值得单记。** 两侧的差只有一行注释,机械取我方 `git` 不报任何东西、单测全绿、行为一字不差——**而那句话在合并后是假的**(它写着两个消费方,实况三个)。这与 W170 记的 `js/issues.js` 那一格同形而方向相反:那次是"取对侧会把 `EPB` 悄悄退回字面",这次是"取我方会把一句已经不成立的注释留下来"。两次都没有任何判据管,都靠逐行读块内容认出来(6.2)。

### 3.2 第二次合并(W169 `de73480` → `2711d6c`,11 块,含 2 个产品块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| **`js/domain.js`** | `projectScript` 之后那一段 | 我方为空、对侧是 `extractSourceText` 那 7 行(两侧的 `projectScript` 整段 5 行逐字节相同,故它自动合上、不进块) | **取对侧那段**:两问两份派生都留,`extractSourceText` 内部现取 `projectScript`,不写第三份 |
| **`js/commands.js`** | `project.extractSubjects` 的取数口 | 我方 `Domain.projectScript(p) \|\| (p.episodes \|\| []).map(...)` / 对侧 `Domain.extractSourceText(p)` | **取对侧**:两者取值恒等,而对侧那份把回退也收进了派生,内联那半正是本槽要收的第二处 |
| `tests/unit.js` | W147 那条既有用例的期望串 | 我方 `'project.extractSubjects,goto:split'` / 对侧 `'goto:extract,project.splitEpisodes'` | **两侧都不是答案**,按合并后实况取 `'goto:extract,goto:split'`,注释合成一句(1.2) |
| `tests/unit.js` | `plans` 套件尾部 | 两侧在同一锚点各插一条新用例 | **并集**,我方在前(块尾 `  } },` 两侧共用,给我方那半补一个) |
| `tests/unit.js` | `contract` 套件尾部 | 同上 | **并集**,同一手 |
| `tests/unit.js` | 三套件下限 | 544/141/107 / 531/143/107 | 单元现取 live **547**;集成取 live **143**(对侧那 2 条真的合进来了);冒烟两侧同值 |
| `tests/unit.js` | 记账件 `FLOOR` | 184 / 175 | 现取 live **185** |
| `README.md` | 「文档数字对账契约」整句 | **两侧只差 `contract` 那个数**(我方 121 / 对侧 117) | 取我方整句,数字换 live **122** |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带全部数段 / 对侧在 `plans.js` 那一节里插了一段 | **并集**,按对侧锚点插回;数字换 live **547** |
| `docs/skills-wave/README.md` | 明写份数 | 184 / 175 | 现取 live **185** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方八行 / 对侧 `w169` 一行 | **并集**,九行按波次号排 |

**`js/plans.js` 零冲突块而两侧都改了它**,合并后现取两个取材器各占各的:

```
js/plans.js:56   'project.extractSubjects': ({ p, gates }) => {   ← W169 那半
js/plans.js:58     return Domain.extractSourceText(p)              ← W169 那半
js/plans.js:70   'project.splitEpisodes': ({ p, gates }) => {      ← W167 那半(基线)
js/plans.js:72     return Domain.projectScript(p)                  ← W167 那半(基线)
```

**`tests/integration.js` / `cli.js` / `server.js` 三个文件合并后与对侧逐字节相同(`M == P2`),而这不是取 `--theirs`**:这三个文件我方相对叉点零 diff(`P1 == B`),`git` 只能搬对侧那份(5.1)。

### 3.3 第三次合并(W171 `6e451ee` → `8d7dad2`,3 块,产品面零改动)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `tests/unit.js` | 记账件 `FLOOR` | 185 / 180 | 现取 live **186** |
| `docs/skills-wave/README.md` | 明写份数 | 185 / 180 | 现取 live **186** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方九行 / 对侧 `w171` 一行 | **并集**,十行按波次号排 |

**`tests/unit.js` 里那段断言改写零冲突块自动合上**:对侧改的是 `release` 套件里那一条的中段,我方自 `4eaea86` 之后在那一段一行都没动过。`README.md` 本次一个块都没有(`M == P1` 且 `P2 == B`:对侧没动过它)。

### 3.4 三次都没有摘 gaps、没有删测

三条槽各自加自己的用例(`w171` 一条不加,只在既有那条里收紧);既有用例**删了 0 条、翻面 0 条、改名 0 条**,**改了 1 条断言**——W147 那条的期望串,而改它是因为两侧各改了一半且两侧都不成立,合并后按实况取值(3.2),不是放宽。
`GUARD_TOPICS` 原有十条**一条没动**,本槽在其后**新登记两条**并把 `TOPIC_FLOOR` 按 live 从 10 抬到 12(5.7、6.1)。
三次合并全程未用 `checkout --ours/--theirs`,也未用 `git checkout <old> -- .`。

## 4. 数字怎么定的

### 4.1 一律现取合并后 live,不抄任何一个父值

| 数 | 基线 | W168 侧 | W169 侧 | W171 侧 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|---|
| 单元用例数 | 542 | **530** | **531** | **534** | **547** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 121 | **116** | **117** | **119** | **122** | 主 `README.md` 契约段 |
| `integration` | 141 | 141 | **143** | 141 | **143** | 主 `README.md` + 下限表 |
| `cli.smoke` | 107 | 107 | 107 | 107 | **107**(实跑 105/107) | 主 `README.md` + 下限表 |
| 记账件份数 | 183 | **175** | **175** | **180** | **186**(含本文后 187) | `docs/skills-wave/README.md` 明写 |
| 记账件 `FLOOR` | 183 | 175 | 175 | 180 | **187**(本文落地后) | `tests/unit.js` |
| `GUARD_TOPICS` 条数 / `TOPIC_FLOOR` | 10 / 10 | 10 / 10 | 10 / 10 | 10 / 10 | **12 / 12** | `tests/unit.js` |

逐次落定的读数(每次合完当场跑,不等三次合完再算):

| | 合入 W168 后 | 合入 W169 后 | 合入 W171 后 |
|---|---|---|---|
| `unit` | 544 / 544 | 547 / 547 | **547 / 547** |
| `contract` | 121 / 121 | 122 / 122 | **122 / 122** |
| `integration` | — | 143 / 143 | **143 / 143** |
| `cli.smoke` | — | — | **105 / 107** |
| 记账件份数 | 184 | 185 | **186** |

**六个自称数里只有一个是答案**,而那一格值得单记:

- **`w169` 自称的 `integration` 143 与合并后 live 逐字相等**,成因是这个套件本槽只有它一支动过、且它的叉点在这个数上够新(基线自 `c5de21e` 之后一条集成用例都没加)。W170 那槽是三支的 `integration`/`cli.smoke` 四格全对而**一支都没碰过**那两个套件,本槽这一格是**碰过而且仍然对**——两种成因不同,都是结构事实不是运气。
- **`w168` 与 `w169` 自称的记账件份数逐字相同的 175**(两支同叉 `c5de21e`、各加一份记账件)。W161(三支全 163)、W166(两对各自相同)、W170(一对相同 + 一支落单)之后,**同一个静默窗口连着第四槽出现**,本槽的形态与 W170 逐字同形。
- **`w168` 自称的 `contract` 条数 116 是三支里最低的,而它一条 `contract` 用例都没加**——那是它叉点上的旧值。若照抄它,与 live 差 **6 格**。

### 4.2 `FLOOR` 抄父值与 README 抄父值:逐格实测

在**本文落地后的 tip** 上逐格实测,结果见 5.6。

## 5. 取证

### 5.1 合并纪律:按四棵树的文件哈希机检

对每次合并,取 `M`(合并结果)、`P1`(我方)、`P2`(对侧)、`B`(叉点)四棵树里同一文件的 blob 哈希做对照。
前两次的 `B` 是 `c5de21e`,第三次是 `4eaea86`。只列三者不全等的文件次。

| 合并 | 文件 | `M` | `P1` | `P2` | `B` | 结论 |
|---|---|---|---|---|---|---|
| `93acbbe` | `README.md` | `2e0117a` | `6ce143d` | `340dd93` | `7094da8` | 真融合 |
| `93acbbe` | `docs/skills-wave/README.md` | `798166c` | `a2ff53d` | `e5bb98b` | `6f387a6` | 真融合 |
| `93acbbe` | `tests/unit.js` | `77eb0bd` | `1338d97` | `61c2e90` | `a3cc666` | 真融合 |
| `93acbbe` | **`js/domain.js`** | `80b90a4` | `dd6c57f` | `dbde1c7` | `f4d427e` | **真融合**(W165/W167 那几半与本槽的注释首行都在) |
| `93acbbe` | `js/pipeline.js` | `aeff30d` | `2248e8c` | `aeff30d` | `2248e8c` | `M == P2` **且 `P1 == B`**:我方没动过它 |
| `2711d6c` | `README.md` | `b26f5d8` | `2e0117a` | `ff45564` | `7094da8` | 真融合 |
| `2711d6c` | `docs/skills-wave/README.md` | `e29e459` | `798166c` | `a97085f` | `6f387a6` | 真融合 |
| `2711d6c` | `tests/unit.js` | `c52f307` | `77eb0bd` | `20b08f9` | `a3cc666` | 真融合 |
| `2711d6c` | **`js/domain.js`** | `eeb642f` | `80b90a4` | `afd9977` | `f4d427e` | **真融合**(`reviewGate` / `projectScript` / `extractSourceText` 三段都在) |
| `2711d6c` | **`js/commands.js`** | `ff62950` | `9c976e3` | `6c5fffc` | `555685b` | **真融合**(拆集口是我方 W167 那半、提取口是对侧那半) |
| `2711d6c` | **`js/plans.js`** | `e7fff96` | `8a77052` | `88586cb` | `c2b7f93` | **真融合(零冲突块,两个取材器各占各的)** |
| `2711d6c` | `tests/integration.js` | `e8f60a2` | `196bd53` | `e8f60a2` | `196bd53` | `M == P2` 且 `P1 == B`:我方没动过它 |
| `2711d6c` | `cli.js` | `94c37a1` | `1640603` | `94c37a1` | `1640603` | 同上 |
| `2711d6c` | `server.js` | `77b9fb7` | `bc27a5a` | `77b9fb7` | `bc27a5a` | 同上 |
| `8d7dad2` | `docs/skills-wave/README.md` | `6d12e5b` | `e29e459` | `20c9c10` | `ebc3e7f` | 真融合 |
| `8d7dad2` | `tests/unit.js` | `bcd77c5` | `c52f307` | `ce54573` | `00b3bec` | 真融合 |
| `8d7dad2` | `README.md` | `b26f5d8` | `b26f5d8` | `2911253` | `2911253` | `M == P1` **且 `P2 == B`**:对侧没动过它 |

**整份取 `--ours` 零命中、整份取 `--theirs` 零命中、回退到叉点内容零命中。**
上表里一个 `M == P1` 与四个 `M == P2` 的格子**全部伴随另一侧 `== B`**——即那一侧根本没改过这个文件,`git` 只能搬另一份。判"是不是 `--ours`/`--theirs`"要看的是另一侧等不等于叉点,不是看 `M` 等于谁(W161 那条教训,W170 落成机检的写法本槽照用)。
三份新记账件是纯新增文件(`P1` / `B` 里都不存在),不参与上表。

### 5.2 三套件实跑

| 套件 | 基线 `c2acb4d` | 合入 W168 后 | 合入 W169 后 | 合入 W171 后(本槽 tip) |
|---|---|---|---|---|
| `node tests/unit.js` | 542 / 542 | 544 / 544 | 547 / 547 | **547 / 547** |
| └ `contract` 子套件 | 121 / 121 | 121 / 121 | 122 / 122 | **122 / 122** |
| `node tests/integration.js` | 141 / 141 | — | 143 / 143 | **143 / 143** |
| `node tests/cli.smoke.js` | 105 / 107 | — | — | **105 / 107** |

`cli.smoke` 那 2 项失败逐条现取,基线与 tip 上**同名同表现**:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

本槽三支一条冒烟用例都没加,故分母仍是 107、失败仍是这两条。

单元用例的**不同名字数**现取 547,与用例数 547 相等——**连续第十九槽全部相等**。

### 5.3 没删测:按名成集**三轮**比对

按 W157 立的口径做两轮,再按 W161 残留 3 补一轮。抽名脚本在**各 tip 的独立 `git archive` 解包树**上跑,不在工作区跑;抽名与 `comm` 两侧一律 `LC_ALL=C`(W170 残留 3 点名的那个洞,本槽从第一轮起就两处都写上了,三轮零假报)。

**第一轮:基线 `c2acb4d` ↔ tip `8d7dad2`**

- **只在基线里有的:0 条**;
- 只在 tip 里有的:**5 条**,逐条点名:
  - `pipeline · hashOf:审片步骤跳过当下审不了的集(门槛读 episodeState.reviewGate,不再只看 reviewAvg)`(W168)
  - `domain · episodeState:reviewGate 单源门槛——缺正文/未拆镜/分镜判旧一律 unready,过关后按未审/判旧/低分/达标归码`(W168)
  - `commands · extractSubjects:提取输入现取 Domain.extractSourceText(extractDone 老项目无原文无分集仍 blocked no-script)`(W169)
  - `plans · fromWorkflow:提取主体步按可读原文分两态(整本/各集正文都没有时出补原文导航步,不推注定 blocked 的命令步)`(W169)
  - `contract · 提取主体的输入文本单源:Domain.extractSourceText 一处,三端命令入口与计划层同读`(W169)

542 − 0 + 5 = 547,与实跑总数对得上。

**第二轮:逐次比对**

| | 上一步独有 | 新增 |
|---|---|---|
| `c2acb4d` → `93acbbe` | **0 条** | 2 条(W168 那两条) |
| `93acbbe` → `2711d6c` | **0 条** | 3 条(W169 那三条) |
| `2711d6c` → `8d7dad2` | **0 条** | **0 条**(`w171` 不加用例,只在既有那条里收紧断言) |

第三行那个 `0 / 0` 正是**按名比对这件工具看不见的那一路**:`w171` 那一槽的全部产出——把一句恒真断言换成三句钉可执行行的——在名集口径上一个字都不显示。接住它的是 `GUARD_TOPICS`(本槽刚补上,1.4)与人读 diff,不是名集(6.3)。

**第三轮:被合入支的用例名集合 ↔ 合并后**

| 支 | 自己的用例数 | 名字在 tip 上找不到的 |
|---|---|---|
| `w168@264bfe3` | 530 | **0 条** |
| `w169@de73480` | 531 | **0 条** |
| `w171@6e451ee` | 534 | **0 条** |

**三支的用例名在合并后一条不缺**:本槽没有翻面、没有改名、没有接位,故第三轮与前两轮结论一致。这个盲区(W161 残留 3)**本槽照旧没被触发,也没有被收掉**,第三轮仍是人工做的。

### 5.4 没摘 gaps;注册表零 diff,故八个数结构性成立

```
$ git diff --stat c2acb4d HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js
(无输出)
```

本槽**五个注册表文件相对基线一个字节都没动**,故八个数结构性成立;仍在合并后 HEAD 上逐个现跑确认:
提示词注册表 **41**、能力短名单 **30**、`CHECKS` **17**、`preflightStages()` **7**、`gaps()` **20 键**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 一个没动。

`gaps()` 现取 20 键逐字:`G-08,G-15,G-09,G-01,G-02,G-12,G-13,G-04,G-10,S-01,S-03,G-07,G-14,G-05,S-04,S-05,G-03,G-11,S-06,S-07`。
两条相关的键**都没摘**:`G-03` 仍是 `review.stage` / `review.reviseLoop` 两条值,`G-11` 仍是 `review.memoryFeedback`。
**`expert.evolve` 仍不在任何 playbook 的 `steps` 里**(现取 `playbooks()` 5 条、`steps` 共 25 步,含 `evolve` 的**零处**)。

产品面进 diff 的文件与行数(本槽自己那一层):

```
$ git diff --stat c2acb4d HEAD -- js/ server.js cli.js mcp.js billing.js
 cli.js         |  2 +-
 js/commands.js |  2 +-
 js/domain.js   | 10 +++++++++-
 js/pipeline.js |  5 +++--
 js/plans.js    | 14 +++++++++++---
 server.js      |  2 +-
```

**两条槽动了产品文件、一条零改动**,合计六个文件 26 加 9 删。

### 5.5 抽查变异

全部在**合并后 HEAD** 上跑,一次只改一处,跑完即还原(还原后复跑 547/547 全绿、工作区 clean)。
按 W161 残留 13 / W166 立的规矩,**每条变异先现取一次被改对象的实际产出再看颜色**——本槽每条都做了,记在下表「现取」列。

**A. 本槽新合进来的判据,各证是新覆盖**

| # | 变异 | 现取 | 合并后 HEAD |
|---|---|---|---|
| M1 | `js/pipeline.js` 把 `unRvEp()` 换回只看 `reviewAvg` 的老写法 | `js/pipeline.js` 里 `reviewGate` 命中从 2 处降到 1 处(只剩注释) | **红 1**:W168 的 `pipeline` 那条报「应越过三种审不了的集直达首个真待审集:期望 `…/ep5`,实际 `…/ep2`」 |
| **M2** | `js/domain.js` 去掉 `reviewGate` 的 `unready` 那一档 | `'unready'` 在 `domain.js` 里从 2 处降到 1 处(只剩注释) | **红 4**,三个消费面一个不落:`pipeline`(W168)、`domain` 的门槛那条(W168)、`domain` 的 `workflow` 那条与 `issues` 双向相等那条(W165)。**这是"一道门槛三侧都在跑"最有力的实测**(1.3) |
| M3 | `js/commands.js` 把提取取数口换回内联 `String(p.script \|\| '').trim() \|\| …` | `commands.js` 里 `extractSourceText` 命中 0 处 | **红 2**:W169 的 `commands` 行为面 + `contract` 报「`js/commands.js` 的提取入口不得再自己读 `p.script`:期望 0,实际 1」 |
| M4 | `js/plans.js` 把提取那两态改回单态命令步 | `plans.js` 里 `extractSourceText` 从 2 处降到 1 处 | **红 3**:W169 的 `plans` 那条、W169 的 `contract` 那条,**外加 W147 那条既有用例**——它报的正是合并后那个并集期望(「期望 `goto:extract,goto:split`,实际 `project.extractSubjects,goto:split`」,1.2) |
| **M5** | `Domain.extractSourceText` 改成**把 `extractDone` 当原文**(前两路都空时回一句「(已提取主体,原文从略)」) | `extractSourceText({extractDone:true})` 现取 `"(已提取主体,原文从略)"` | **红 4**:`commands` 报提取从 `blocked` 变成了 `done`、`plans` 两条(前提夹具与两态分流)、`contract` 报「提取结论(主体库/卖点梗概)都不是原文:它们是产物,提不出逐字证据」。**这是任务口径点名的那条纪律的正面实测,与 W170 的 M7 在拆集那一侧同形** |
| **M9** | `js/release.js` 把 G4–G6 改成**就地数镜头**(连 `Domain.episodeState` 的调用一并去掉) | 那段可执行行里 `Domain.episodeState(` 命中 0 处 | **红 2**:W145 那条「遍历抛错记 warn」的行为面 + `w171` 新加的第一句。**同一手在基线上已经红 1**(行为面那条),故这一手量不出 `w171` 新在哪 |
| **M10** | 同上但**保留 `Domain.episodeState(p, ep, online)` 的调用**,只把进 `agg` 的加数换成就地数出来的另一份(**两份值一模一样**) | 那段可执行行里 `Domain.episodeState(` 仍 1 处、`agg[k] += … counts` 命中 0 处 | **红 1**,只红 `w171` 新加的第二句「进 `agg` 的加数仍得是 `episodeState` 回的 `counts`(换个来源即红)」;**同一手在基线独立 worktree 上现跑 542/542 全绿**。这是 `w171` 新覆盖的干净证据,也是"值一样也是第二份投影"第三次被实测(1.5) |

**B. 任务点名要保的,逐样反向变异一次**

| 要保的 | 反向变异 | 现取 | 实测 |
|---|---|---|---|
| **门槛只留一份**(任务点名"不要两份 reviewGate") | 现取核实而非变异:`js/domain.js` 里 `const reviewGate =` **恰 1 处**、`D.projectScript =` **恰 1 处**、`D.extractSourceText =` **恰 1 处**;`extractSourceText` 的第一路读的就是 `projectScript(p)` | — | 三份派生各一处,零重复定义 |
| **W165 的两个消费面** | 见 M2 | — | **红 4** 里有它们两条 |
| **W167 的拆集那一态** | 见 M4 | — | W147 那条既有用例把两个取材器的合并期望一起钉住了 |
| **`GUARD_TOPICS` 护栏主题清单** | 把 `extract-source-single` 的**唯一承载用例整条删掉**(无继任者) | 该用例名在文件里剩 0 处 | **红 4**:数字对账 2 条、三套件下限 1 条、**失联那条 1 条**并把主题编号 `extract-source-single`、两个锚点与 `why` 原样印出来 |
| **`expert.evolve` 不进 `steps`** | 现取核实:`playbooks()` 5 条、`steps` 共 **25** 步、含 `evolve` **0** 处 | — | 原样未动 |

**C. 两条对照变异:登记过与没登记的差**

| # | 变异 | 实测 |
|---|---|---|
| **M7** | 把 `w171` 收紧的那句断言**退回原来的恒真写法**(用例一条不少、行为一字不改) | **红 1**,只红 `GUARD_TOPICS` 失联那条并点名 `g456-count-source`。**同一手在登记之前的 tip(`8d7dad2` 独立 worktree)上是 547/547 全绿**(1.4) |
| M8 | 把 **W168 那条 `domain` 门槛用例**整条删掉(它是行为面,本槽没给它登记主题) | **红 2**:数字对账 1 条 + 三套件下限 1 条,**失联那条不报**——没登记的只剩数字层兜着 |

M7 与 M8 合起来说的是同一句话的两面:**登记过的护栏被放宽或删掉都会被点名并印出主题编号,没登记的只有"用例数少了"这一层**,而那一层拦不住"把判据放宽回恒真"这种用例数一条不少的改法。这正是 W170 残留 1 的形态,本槽把它在自己带进来的两道护栏上收掉了;仍没登记的逐条点名在 6.1。

### 5.6 棘轮:四格 `FLOOR` 与 README 数字逐格实测

在**本文落地后的 tip** 上把四个 `FLOOR` 逐格改小重跑全套(`SLACK` 现取仍是 3):

| 格 | 值 | 与 live 的差 | 全套结果 |
|---|---|---|---|
| 单元测试(live 547) | 546 / 545 / 544 | 1 / 2 / 3 | 全绿 |
| 单元测试 | **542**(基线父值) | **5** | **红 1 条** |
| 单元测试 | **534**(`w171` 父值) | **13** | **红 1 条** |
| 单元测试 | **531**(`w169` 父值) | **16** | **红 1 条** |
| 单元测试 | **530**(`w168` 父值) | **17** | **红 1 条** |
| 记账件份数(live 187) | 186 / 185 / 184 | 1 / 2 / 3 | 全绿 |
| 记账件份数 | **183**(基线父值) | **4** | **红 1 条** |
| 记账件份数 | **180**(`w171` 父值) | **7** | **红 1 条** |
| 记账件份数 | **175**(`w168` 与 `w169` 父值同为 175) | **12** | **红 1 条** |
| `integration`(live 143) | 142 / 141 / 140 | 1 / 2 / 3 | 全绿 |
| `integration` | **141**(基线父值 / `w168`、`w171` 侧) | **2** | **全绿——本槽唯一一格被差额上限蒙过去的父值** |
| `integration` | 139 | 4 | **红 1 条** |
| `cli.smoke`(live 107) | 106 / 105 / 104 | 1 / 2 / 3 | 全绿 |
| `cli.smoke` | 103 | 4 | **红 1 条** |

**单元与记账件两格全部逮住全部父值,而 `integration` 那一格第一次在本线上放过了基线自己那个父值。** 成因是本槽 `integration` 只加了 2 条(全是 `w169` 带的),差 2 压在 `SLACK` 3 之内。这与 W162 / W148 / W170 三次"记账件那格压线"同形,而**换到 `integration` 这一格是第一次**:那两格此前一直因为"三支都没碰这个套件"而与 live 相等,从没进过这个测试。如实登记(6.5)。
记账件那格差 4——与 W170 那槽逐字同形(本槽三支各带一份记账件 + 本文 = 四份),**刚好越过 `SLACK` 3 一格**;若本槽只合两支,基线那个 183 同样会被差 3 蒙过去。
十二槽连读:W133 差 1 全放过、W134 放过一逮住一、W139 全放过含一次压线、W142 单元三个父值全逮住、W146 差 3 压线放过 + 差 5–11 全逮住、W148 单元全逮住 + 记账件差 3 压线放过、W151 四格差 4–22 全逮住、W157 四格差 4–14 全逮住、W161 四格差 4–18 全逮住、W166 四格差 5–14 全逮住、W170 四格差 4–16 全逮住、**W174 单元与记账件两格差 4–17 全逮住,`integration` 那格差 2 放过**。

README 那三处抄父值同样逐处实测:

- 主 `README.md` 单元数改成 542 / 534 / 531 / 530 → **各红 2 条**(精确对账那条报「实测 547,文档 N」+ 三套件下限那条报「README 写的 N 低于下限」);
- 主 `README.md` `contract` 条数改成 121 / 119 / 117 / 116 → **各红 1 条**(契约段自报条数那条);
- `docs/skills-wave/README.md` 明写份数改成 183 / 180 / 175 → **各红 1 条**(三方对齐那条)。

### 5.7 本槽自己改了什么

除三个合并提交解冲突外,本槽多做两件事:

1. **`GUARD_TOPICS` 补两条主题**(单独一个提交):`extract-source-single`(锚点 `Domain.extractSourceText` + `/api/wf/extract-subjects`,落点实测 1 处)与 `g456-count-source`(锚点 `aggCode` + `G4–G6 计数仍取`,落点实测 1 处),`TOPIC_FLOOR` 按 live 10 → 12。这是任务口径点名的 W170 残留 1,补的是**本槽自己合进来的那两道源级护栏**;两条锚点都先用探针量过落点数再登记(锚点太泛会当场超出、太窄会当场归零,那条判据自己会报)。
2. **本文这一提交**:新增本文、`docs/skills-wave/README.md` 补索引行并把明写份数 186 → 187、把"最新一份收敛记录"的指针从 `w170-integration-log.md` 挪到本文(旧指针进"更早的分叉"那一串)、`tests/unit.js` 的记账件 `FLOOR` 186 → 187。

单元用例数与 `contract` 条数**本文一条不加**,故 547 / 122 不动。
三个合并提交里**没有删过任何一条用例、没有放宽过任何一条断言、没有翻过面、没有改过名**;唯一改过的那条断言是 W147 那条的期望串,改的方向是"按合并后实况取值",两侧给的都不是它(3.2)。

## 6. 残留与欠账

1. **`GUARD_TOPICS` 本槽补了两条,而清单外仍有一批源级护栏没登记,逐条点名如下**(全部是本线既有的,本槽只合入不重做,故只登记不动它们):
   - W162 的两条(`js/release.js` 可执行行零 `ZipUtil.download` / 零 `PLACEHOLDER` 的源级那条、运行期打桩那条);
   - W163 的一条(`domain · REVIEW_MIN 消费点零分叉(源级)`);
   - W165 的一条(`contract · 分集级审片门槛单源`);
   - W167 的一条(`contract · 拆集的切分输入单源`);
   - W168 本槽带进来的两条都是**行为面**(`pipeline · hashOf` 与 `domain · episodeState:reviewGate`),按 `GUARD_TOPICS` 那张表的收录口径(记账/欠账类与单源类)本就不必进,故未登记——M8 实测它被删只红 2 条(数字层),这一格如实记下而不是当成漏登记。
   补登记这几条要各自量锚点与落点数,属"重做已落地功能"的一侧,不在本槽射程内。
2. **`js/domain.js` 那个门槛注释块里"取我方就留下一句已经不成立的话"这一格,没有任何判据管。** 两侧的差只有一行注释,机械取我方 `git` 不报、单测全绿、行为一字不差,而那句话写着两个消费方、实况三个(3.1)。这与 W170 残留 2 记的 `js/issues.js` 那一格是同一个洞的另一面:那次是"取对侧会把登记表退回字面",这次是"取我方会把过期的注释留下来",**两次都只能靠逐行读块内容认出来**。
3. **按名成集比对本槽第一次撞上"整槽零名集增量"的形态,而这一面此前只在理论上说过。** `w171` 那一槽的全部产出在名集口径上是 `0 / 0`(5.3 第二轮第三行):它不加用例、不改名、不翻面,只把一句恒真断言换成三句真判据。**名集这件工具对"判据被放宽"和"判据被收紧"一样看不见**,接住它的是 `GUARD_TOPICS`(本槽刚补)与人读 diff。W161 残留 3 那条"第三轮仍是人工的"原样未收。
4. **`comm` 的排序口径本槽零发作,而这是因为提前写上了不是因为洞被堵了。** W170 残留 3 记的是"脚本内外口径不一致",本槽从第一轮起抽名与 `comm` 两处都写了 `LC_ALL=C`,三轮全部干净。**零吃测这件取证工具的三个口径(去重 / 排序 / 按 `|` 切回执载荷)仍然一个都没有判据**,下一个人不写照旧会踩。
5. **棘轮那四格本槽第一次在 `integration` 上放过了基线父值。** 差 2、压在 `SLACK` 3 之内(5.6)。此前十一槽里这一格从没被测过——因为被合入支都没碰过那个套件、自称值与 live 恰好相等。**"抄父值响不响只由本槽在那个套件上加了几条决定"这句话至此在第三个套件上得到同形的实测**。
6. **`js/release.js` 底部那个包着 `Bus.on('*')` 注册的 `catch (_) {}` 仍开着**(W151 残留 2、W157 残留 5、W161 残留 5、W166 残留 7、W170 残留 6 原样未动)。全仓现取:`js/release.js` 空 `catch` **1 处**、`js/bus.js` **0 处**。
7. **W170 残留 7 仍成立且本槽没有实测机会**:`downloadReleaseZip` 那三条钉的是"这一段 / 这个文件 / 这三个入口不许再落第二个包",不是"不许再落第二个包"这个语义;本槽三支一条都没碰交付链。
8. **W166 残留 1、2(`GUARD_TOPICS` 的 `hosts` 是手填数、「锚点太泛」与「真有两个承载」报错句同形)本槽第一次从登记侧碰到它**:新登记两条时的落点数是用探针现取出来的(各 1 处),**而"登记的那个数对不对"仍然只能靠先量一遍**,判据只在写错时才报,不会替你算。这两条原样未收。
9. **W161 残留 4 那条弱断言(`assert(/aggFail: 0/.test(rel) || /counts/.test(rel), …)` 实际恒真)由 `w171` 收掉了**——这正是本槽合入的第三条槽做的事。**同类形态还有没有,本槽没有普查**:`w171` 自己的记账件里量的是它那一处,全仓其余 `assert(A || B)` 形态的断言有多少条 `B` 是恒真的,至今零判据、零清点。
10. **W151 残留 4 的门槛派生那条本槽被 W169 从侧面再碰一次,仍没收。** W167 立的是"'这一步走过了'是进度,'原文读不读得到'是输入",W169 把同一句话套到了提取那一步(输入是"整本原文或各集正文");而那句话的核心照旧成立——**"某个码该不该投、投成什么危险级/什么文案"仍是各消费方各自决定**。
11. **`G-03` 的形态面、`G-11` 的自动进化面均未动**(W151 残留 1、3 原样);现取核实 `G-03` 仍是两条值。
12. **W142 残留 1(W135 那句 `assertEq` 与 W137 共享 `fails` 计数器)本槽未动。**
13. **W144 的行序判据仍不管同号内的先后**(连续第九槽没有实测机会:本槽三行的波次号 168/169/171 互不相同)。
14. **散文那半仍没有判据。** 本槽三处 README 长句取并集靠的是"把对侧相对它自己叉点的增量按字级 `word-diff` 切出来、按锚点插回我方",这一步是人工判的;切得对不对没有判据管。W168 那次的门槛整句尤其如此——**改「两处」为点名三个消费面这一手,是人读出来的,不是任何一条断言要求的**。
15. **`w160` 那份 783 行核验件的结论至今未在集成线上逐条重跑**(W166 残留 17、W170 残留 16 原样);本槽任务口径同样只要求合入,故未做。
16. **在飞的 W172 / W173 本槽一条没碰**(任务口径明确不合),叉点与冲突面均未核实。
