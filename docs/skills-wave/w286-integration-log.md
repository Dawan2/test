# W286 集成记录:W285(主体库 `subjects-dedupe` 页面入口)并入 W284 尖

一条槽。W285 给主体库页面补上 `subjects-dedupe` 那条出口的浏览器入口(页头按钮 → `U.openModal` 预览 → 确认才写库),
本槽把它并进同一条集成线。一次 `--no-ff`,零冲突。

本文只记合入这一趟的取证，产品语义一格未改动。

---

## 一、支尖与形状(全部现取 `rev-parse`,不采信任何一侧的自称)

| 位置 | 现取 SHA |
|---|---|
| 我方尖 P1(`origin/cursor/w284-integration-7c31`) | `0a4fa43` |
| 对侧尖 P2(`origin/cursor/w285-subjects-dedupe-ui-3f26`) | `225ca28` |
| `merge-base(P1, P2)` | `0a4fa43` |
| merge parents | `0a4fa43` + `225ca28` |
| merge commit M | `85681f1` |
| `tree(M)` = `tree(P2)` | `41018d4`(逐字节相同) |

`merge-base` 现取 `0a4fa43` **恰等于本支起点**,`git merge-base --is-ancestor P1 P2` 成立、`P2..P1` 空——
**集成线上第十三条「快进形」**。仍走 `--no-ff`:两个 parent 都留在图上,以后要看这一槽是从哪并进来的还查得到。

`P1..P2` 三条提交,自称尖(`225ca28`)与自称叉点(`0a4fa43`)两处都现取核过、都与自称逐字节相同,自称尖是链尾:

| | 提交 | 干了什么 |
|---|---|---|
| 1 | `5284c6d` | 主体库页面补入口:预览(dry-run)+ 确认才写库,规则同读 `Domain.dupIdScan` |
| 2 | `3eefb76` | 记账件 + 索引一行,记账件下限抬到 live 300 |
| 3 | `225ca28` | 判据加固:弹窗桩按 `U.openModal` 真实契约建(开那一下就 `onMount`) |

### 快进形的空集哪一格不承载结论

`git diff M P2` 整树空(树面恒真,第十三次)——**这一格不说明任何事**,它是快进形的算术后果,不是「我方没丢东西」的证据。

产品面**不空**(与 W282 那种「对侧只加测」不同):`git diff M P1 -- js/ cli.js mcp.js server.js index.html` 现取
`js/domain.js`(+7/−4 那一段是 `dupSubjectRowsNote` 末句改指)+ `js/roles.js`(+71,`dedupeScan`/`openDedupe` 与两处入口),
`cli.js` / `mcp.js` / `server.js` / `index.html` **四个文件 diff 全空**——即 `shots-dedupe` 与 `subjects-dedupe` 两条 CLI 命令的外部行为一格没动,
MCP 也没多一条工具(页面入口不是 headless 出口)。但产品面不空只证明「对侧改了东西」,**仍不证明「我方没丢东西」**。

所以本槽说得出话的只有三面:**变异面**、**live 数字面**、**live 探针面**。

---

## 二、本槽真正承载结论的那一格:同一手变异,起点红几条 / 合完红几条

M3 = 在 `Domain.dupIdScan` 的派生里把**首位也发一个新 id**(违掉「首次出现留原 id」那半句规则)。
两侧改法逐字同形,两次都在干净 worktree 上现跑:

| 跑在哪 | 现跑 | 红几条 | 红在哪几处 |
|---|---|---|---|
| 起点 `0a4fa43`(W284 尖) | `697/701 PASS, 4 FAIL` | **4** | 镜头侧 CLI、主体侧 CLI、源级单源那条、派生纯函数那条 |
| 合完 `85681f1` | `698/703 PASS, 5 FAIL` | **5** | 上面四条 + **主体库页面那条入口**(`入口上得报出要改几位(三位同 id → 留首位、改后两位),实际:🧹 主体 id 去重(4)`) |

同一手概念,**起点红 4 / 合完红 5**。第五条红来自新接上的第三个消费点——
即「这份规则此刻真的只有一份、而且页面这一端真的读的是它」是**现跑出来的**,不是读两侧文档读来的。
三处消费点(`cli.js` 两条命令 + `js/roles.js` 页面入口)在同一手变异下**同时红**。

W284 那槽量到的是红 4(那时只有两个消费点),本槽红 5,差的那一条正是这一槽进来的东西。

### 其余变异逐手现跑(全部本槽现跑,报错句与对侧记账逐字节相符)

| 手 | 改法 | 现跑 | 接住它的判词 |
|---|---|---|---|
| M3 | 派生里首位也发新 id | **红 5** | 见上表 |
| M4 | 把整套规则抄回 `js/roles.js` 的 `dedupeScan`(自己攒 `first`/`taken`/`dups`,**行为完全等价**) | **红 1** | `页面这一端的扫描须委托 Domain.dupIdScan(两处各写一份规则迟早对同一份脏数据给出两种计划)` |
| M5 | 摘掉 embedded 那一处 `${dedupeBtn}`(只留独立页那一处) | **红 2** | 行为面 `嵌在项目页里的那一版(embedded)同样得有入口:两处头部按钮行只挂一处,从项目详情进来就没这条路` + 源级 `入口须在两处头部按钮行都插上:期望 2,实际 1` |
| M2 | 摘掉确认闸:把落库那一段提到 `onMount` 里(开弹窗即写库) | **红 2** | 行为面 `预览这一屏不许写库(这条入口的"可撤销"就落在这一格上):期望 0,实际 1` + 源级 `写库那一句须落在确认按钮的 handler 里` |
| M6 | `dupSubjectRowsNote` 末句退回「先用 CLI subjects-dedupe」(把页面按钮那半句摘掉) | **红 0** | 一条都没有——见 §六残留 |

M4 那一格是本槽第二个说得出话的地方:**行为完全等价却照旧红**,接住它的只有源级那一条。
两份计算一旦漂,用户照预览点的那个头就不作数了,而行为面永远量不出这个风险。

### M1:测试桩与真实契约差一格,会少守哪一格

对侧第三条提交把弹窗桩从「延后 `onMount`」改成「开那一下就 `onMount`(节点, close)」。本槽把两种桩下的同一手 M2 各跑一遍:

| 桩 | 同一手 M2 现跑 | 行为面那条红成什么 |
|---|---|---|
| 延后 `onMount`(对侧改之前) | 红 2 | `dry.m.querySelector(...).onclick is not a function`——**红在桩的管线上,一句不相干的话** |
| 开那一下就 `onMount`(现状) | 红 2 | `预览这一屏不许写库(这条入口的"可撤销"就落在这一格上):期望 0,实际 1` |

红数一样(都是 2),但**前者那条红什么都没说**:它报的是桩自己崩了,不是「确认闸被吃掉了」。
桩不按真实契约建时,判据看着在、说的话已经不作数——这一格照抄进量尺清单。

### 反事实:这套网从「删测」到「连花名册一起删」逐层还剩几条红

| | 改法 | 现跑 |
|---|---|---|
| CF1 | 只删对侧那两条新用例 | **红 4**(README 数字对账 / 三套件下限 / 护栏主题失联 / 护栏主题真删得出红) |
| CF-floor | 只把 `TOPIC_FLOOR` 由 22 改回 21(一条主题都不删) | **红 1**(`下限 21 低于花名册的 22 条:下限只增不减,改小它就是给"往后白删几条"腾地方`)——即明令「禁止仍写 21」有判据接着 |
| CF2 | 删那两条用例 **+** README 改 701 **+** 单元 `FLOOR` 改 701 **+** `TOPIC_FLOOR` 改 21 **+** `GUARD_TOPICS` 撤登记 **+** 花名册删行 | **红 0** |

CF2 红 0,与 W283 §六 CF2c 同形,如实记下:**最后一层靠的是记账件与花名册这两份文档,不是判据。**
把这套网整个拆掉仍是「五处一起改」这一手,判据拦得住其中任意四处、拦不住第五处齐改。

---

## 三、live 数字(逐格现跑,两侧自称一律不抄)

明令「W284 自称 unit 701;W285 自称 703;禁止抄 701」。本槽逐格现跑:

| 套件 | 起点 `0a4fa43` | 合完 live | 动没动 |
|---|---|---|---|
| `unit`(整跑) | 701 | **703** | +2 |
| `commands` | 63 | **65** | +2 |
| `contract` | 153 | **153** | — |
| `domain` | 45 | **45** | — |
| `skills` | 104 | **104** | — |
| `issues` | 22 | **22** | — |
| `integration`(`tests/integration.js`) | 152 | **152** | — |
| `cli.smoke` | 115/117 | **115/117** | — |

24 套件逐格求和 **= 703**(套件数现取 24,`contract` 153 / `skills` 104 / `commands` 65 / `agent-ops` 60 / `release` 51 /
`domain` 45 / `memory` 35 / `experts` 29 / `produce` 23 / `issues` 22 / `plans` 17 / `store` 16 / `pipeline` 13 /
`sb-views` 10 / `billing` 10 / `api` 10 / `split` 9 / `flow` 7 / `sb-gen` 5 / `bus` 5 / `sb-io` 4 / `continuity` 4 /
`understanding` 3 / `tasks` 3)。**两侧自称的 701 与 703 都没盲抄**:701 是起点数、703 是 live。

**名集多重集 `+2 −0`,`701 + 2 = 703` 自洽**(两侧各自在干净整树上取,不混树)。进来的正是这两条:

- `commands · 主体库页面那条去重入口:库里真有同 id 多位才露出来,点开只预览、确认那一下才改 id(一位不删、零计费、引用落到同一位)`
- `commands · 主体库那条去重入口不许另抄一份规则:页面这一端只注入自己的发号器与单位词,规则仍现取 Domain.dupIdScan(纯改表零计费,删除语义未动)`

一条没出。

`cli.smoke` **单独整跑**(不与别的套件同轮):`env -u HUJING_SERVER -u HUJING_TOKEN` 加 `MV_DATA_DIR` / `MV_UPLOADS_DIR` / `MV_CONFIG`
三个临时目录隔离。两条失败:`未登录 whoami → exit 3 | exit=1` 与 `llm --json mock 链路 | undefined`——
`master` `9adcf0f` 在**同 env** 下现取 **51/53**,同名同表现两条,故按明令记作 master 同名失败,不计本槽账上。

**MCP 与 CLI 那两个键数照旧分列现取,不混成一个**:MCP `tools/list` 现取 **40**、CLI `Object.keys(CMD)` 现取 **50**
(bracket **26** + dotted **24**)。两个数本槽都没动——页面入口不是 headless 出口,不该在这两处冒出来。

---

## 四、`TOPIC_FLOOR`、花名册与棘轮(对齐到 live)

明令「对侧自称 22,禁止仍写 21」。现取:

| 格 | 现取 |
|---|---|
| `GUARD_TOPICS` 在册 | **22**(末三条 `landed-seat-order` / `dedupe-rule-single` / `dedupe-ui-rule-single`,三条都在,不是二选一) |
| `GUARD_TOPICS_CLOSED` | **0** |
| `TOPIC_FLOOR` | **22** |
| 花名册(`w178-topic-floor-unlist.md`)行数 | **22** |

在册集合与花名册集合**双向差集都空**。改回 21 现跑红 1(见 §二 CF-floor)。

棘轮五格差额逐个现跑:

| 格 | `FLOOR` | live | 差额 |
|---|---|---|---|
| 单元测试 | 703 | 703 | **0** |
| 集成测试 | 152 | 152 | **0** |
| CLI 冒烟 | 117 | 117 | **0** |
| 记账件份数 | 300 → **301** | 301 | **0** |
| 护栏主题 | 22 | 22 | **0** |

记账件那格本槽抬到 live **301**(目录实况 / 索引表行数 / README 明写份数三处同轮校齐),另四格未动。
五格差额 **0/0/0/0/0**,`SLACK` **3** 一格没用掉。`Skills.gaps()` **20** 键一个没剥。

---

## 五、核过的四格(明令逐条)

| 要核的 | 现取 |
|---|---|
| **两处入口** | `${dedupeBtn}` 在 `js/roles.js` 现取 **2** 处(非嵌入页头部行 + 项目详情嵌入版头部行);摘掉一处红 2(M5) |
| **确认闸** | `openDedupe` 里 `Store.save()` 恰 **1** 处,且排在 `[data-x=apply]` 的 handler 之内;预览那一屏零写库;取消 / ✕ / 遮罩只关窗。提到 `onMount` 红 2(M2) |
| **三处同读 `Domain.dupIdScan`** | `js/roles.js:284`(页面入口)、`cli.js:727`(`subjects-dedupe`)、`cli.js:814`(`shots-dedupe`)——恰三处,派生里改一手三处同时红(M3) |
| **`shots-dedupe` 仍无浏览器按钮** | 全树 `js/` 里 `shots-dedupe` 只在 `js/domain.js:328` 那句 note 文案里出现(`要一个 id 只跑一行,先用 CLI shots-dedupe 收拾存量重复。`),零按钮零入口;live 探针真开分镜路由再核一遍 |

另核:`shots-dedupe` 单位词仍是 `rows`(`cli.js:819`),没被主体侧的 `seats` 串味;
`js/roles.js` 的删除语义那一句(`p.subjects = p.subjects.filter(x => x.id !== s.id)`,行 560)一字未动。

### live 探针 26 格(真起服务 + 无头 Chrome + 真 `Store` / 真 `U.openModal` / 真 DOM)

**26/26 通过**。灌的脏树:`sj_dup` 三位同 id(名字与设定各不同)+ 一位干净主体 `sj_ok`。要点:

- 独立页(`#/project/<pid>/roles`)与项目详情「主体」tab 两条路**都**露出 `🧹 主体 id 去重(2)`,数字与计划一致;
- 卡片按 kind tab 分渲,角色 tab **3 张**(同 id 三位各渲一张,页面上与三个不同主体长得一样)、场景 1 张;
- 点开是弹窗,预览报出 `sj_dup / 3 位 / 留哪一位` 与逐位改名计划,末句说清「这一屏还没写库、新 id 在确认那一下现发」;
- 预览这一屏 id 与名字逐条未动;取消一下仍未动;
- 确认那一下才落库:仍 **4 位一位不删**,首位留 `sj_dup`、干净那位不动、撞车两位各发一个新 `sj_` id 互不相同也不与在库任何 id 撞,四位的名字与设定逐条一字不动;
- **积分 100 → 100 一分没动**,任务台零登记(不经 `Tasks.run`);
- 落库真落到服务端(重 `pullState` 后新 id 还在);收拾干净后入口自己消失;干净库上压根不露出;
- 真开分镜路由:**分镜面零 `shots-dedupe` 按钮**;整趟零未捕获异常。

`node --check` 过:`js/roles.js`、`js/domain.js`、`tests/unit.js`。按明令本槽**不跑** `tests/e2e.js`。

---

## 六、残留(按明令一条没代修,原话逐条保留)

以下八条本槽**一条都没动**,原话照搬:

1. **分镜面仍无 shots-dedupe 入口**——本槽核过:`js/` 里零按钮,live 探针真开路由再核一遍。W285 只收了主体那一半。
2. **卡片无重复标记**——同 id 那几位在卡片上与几个不同主体逐字一样;live 探针现取:三张卡里带「同 id / 重复 / dup」字样的 **0 张**。页面上发现它只靠页头那个数。
3. **dupSubjectRowsNote 页面按钮半句无判据**——本槽量过:M6 把末句退回「先用 CLI subjects-dedupe」现跑 **红 0**,那半句可以静默退回去。
4. **unique 扫描前口径**——`js/domain.js:415` 的 `unique` 数的是**进来那张表**上的相异 id 数,不是去重之后的位数;调用方拿它当「收拾完剩几位」会读错。
5. **按 id 删仍清每一位**——`js/roles.js:560` 一字未动。那是删除语义不是去重语义,本槽不碰它。
6. **落库无 rev 闸**——确认那一下直接 `Store.save()`,不比 rev、不检冲突;别处同时改了同一棵树时以后写的那一份为准。
7. **state-put 不设闸**——逃生舱 `state-put --force` 整树原样落库、一处领域校验都不做,主体 id 唯一性仍由调用方自己负责。
8. **单发 `landed` / 共位 / 座位键等 W282 起未收项**——原样挂着。残留锚点本槽现取复核:`js/batchops.js` 的 `.order + 1` **17** 行、`js/produce.js` 与 `js/issues.js` 各 **0** 处。

另新记两格,如实登记本槽没做的量:

- CF2 那一手(五处齐改)红 0 是第二次量到同一形状(W283 CF2c 已记),本槽没有为它补任何判据。
- M4 在 CLI 那两侧的对称改法(把规则抄回 `cli.js`)本槽没重跑,那一格照 W284 记的红 1 用。

---

## 七、一句话

快进形第十三条,整树 diff 恒空这一格不承载结论;承载的是**同一手 M3 在起点红 4、合完红 5**——
第三个消费点接上之后,「这份规则只有一份」多了一层现跑出来的红。
数字八格逐格现跑、两侧自称的 701 与 703 都没盲抄;`TOPIC_FLOOR` 与花名册对齐到 **22**;
`SLACK` 3 一格没用掉、`gaps()` 20 键一个没剥;八条残留一条没代修。
