# W302 集成:把 W301(分镜卡片小标跳到留原 id 的第 1 行)合入 W300 尖

## 一、这一槽的形状:又一条快进形,而"diff 空集"照旧一个字都不作证

现取(全部以 `rev-parse` 为准,一侧的自称都不抄):

| 位 | SHA | 说明 |
| --- | --- | --- |
| P1(我方尖) | `d1933301662499e61baf3bef71b8a30c636c62c7` | `origin/cursor/w300-integration-06c6` |
| P2(对侧) | `03ce67304bf051a36c420f7ff2b139c219000e13` | `origin/cursor/w301-shot-dupjump-70c4` |
| merge-base | `d1933301662499e61baf3bef71b8a30c636c62c7` | **恰等于 P1** |
| merge commit | `38022ab9e0257cef555dc3767032f840d18b34ce` | 本槽支尖 `cursor/w302-integration-b299` |

W301 自称"从 w300 尖 `d193330` 起支、HEAD `03ce673`",两个数**现取都对得上**;
`git merge-base --is-ancestor P1 P2` 成立,即 P1 是 P2 的祖先,故这是集成线上连着第三条**快进形**槽。
仍按纪律走 `--no-ff` 留住并入点(零冲突,`ort` 策略一次过),P2 上那三条提交原样挂在第二父上:
`1458322`(落地)、`5599944`(记账件 + 索引 + 花名册)、`03ce673`(变异手数按现跑改正)。

快进形的代价照旧写在这里:`git diff M P2` **空集**、`tree(M) == tree(P2)`(两侧同为
`73274dbec7c759241e943d0f426a125935a027c6`)。这两格**都不承载结论**——它们在"对侧的东西一件没丢"
与"对侧的东西压根没进来过"两种世界里长得一模一样,一条 `--ours` 或 `git checkout <old> -- .` 的误合
同样能让 `tree(M)` 自洽。故本槽的正面证据全压在另外两格上:

1. **变异台**——19 手打在合完的 `38022ab` 上要红,同一批锚点打在 P1 上要**全部失配**;
2. **live 探针**——同一份探针在 P1 与合完两处各跑一遍,看翻绿几格。

`git diff P1 M` 是 8 个文件 398 增 16 删,与 W301 自报的落地面逐文件相符
(`README.md` / `docs/skills-wave/README.md` / `docs/skills-wave/w178-topic-floor-unlist.md` /
新增 `docs/skills-wave/w301-shot-card-dup-jump.md` / `js/sb-views.js` / `js/shotgroups.js` /
`js/storyboard.js` / `tests/unit.js`)。
`js/domain.js`、`js/roles.js`、`js/episodes.js`、`js/projects.js`、`cli.js`、`mcp.js`、`server.js`、
`index.html` 逐字节未动。

## 二、变异台(19 手 + 1 手对照 + 2 手残留取证,全打在 merge tip `38022ab` 上)

**先在 P1 上做锚点匹配**:19 手 + 1 手对照那批锚点逐条拿到 `d193330` 上试,**命中 0 处、全部失配**——
这就是快进形槽里"对侧的东西真进来了"唯一说得出话的那一格(合完那一侧逐条命中,M3 命中 3 处、M15 命中 2 处)。

| 手 | 改哪 | 打哪个套件 | 结果 | 报的是哪句 |
| --- | --- | --- | --- | --- |
| M1 | `const jump = keep >= 0`(第 1 行自己那一枚也挂上跳自己) | `commands` | 红 1 | 落点串成 `1/2→0`、`2/2→0`、`1/2→3` … |
| M2 | 落点写成被点那一行自己(`data-dupjump="${row}"`) | `commands` | 红 1 | 落点串成 `2/2→2`、`2/2→4` |
| M3 | **行为完全等价**:派生回键 `data-dupjump` → `data-dupgoto`(渲与绑同轮改) | `commands` | 红 1 | 落点串取空 |
| M4 | 摘掉分镜视频/剪辑台那一处绑定(`js/storyboard.js`) | `commands` | 红 1 | `chip(...).onclick is not a function` |
| M5 | 摘掉镜头组时间线那一处绑定(`js/shotgroups.js`) | `commands` | 红 1 | `chip(...).onclick is not a function` |
| M6 | **行为完全等价**:镜头组时间线 `data-row` 改用渲染下标 `i` | `commands` | 红 1 | 源级"这一档自己绑一处":期望 2 实际 1 |
| M7 | **行为完全等价**:定位动作抄第二份、接收者改名 `el` | `commands` | 红 1 | 定位那一下只许有一处:期望 1 实际 2 |
| M7b | (同形对照)第二份接收者仍叫 `card` | `commands` | 红 1 | 同上 |
| M8 | **行为完全等价**:不调 `Domain.dupIdKeepOrder`,页面自己翻一遍首行 | `commands` | 红 1 | 落点序号须从同一份位次表上现取 |
| M9 | 跳转那一下不 `stopPropagation` | `commands` | 红 1 | `stopped`:期望 6 实际 0 |
| M10 | 跳转那一路顺手 `Store.save()`(纯导航破功) | `commands` | 红 1 | 点几下都不许写库:期望 0 实际 6 |
| M11 | 摘掉 `cursor:pointer` 与那句"点得动"的说明 | `commands` | 红 1 | 点得动这件事得让人看得出来 |
| M12 | 缩略图卡 `data-row` 用视图局部下标 `i` | `commands` | 红 1 | 缩略图卡按全表实位认:期望 1 实际 0 |
| M13 | `TOPIC_FLOOR` 退回 29 | `contract` | 红 1 | 花名册/在册/销号三者对不上 |
| M14 | 撤掉 `dedupe-shot-card-jump` 那条在册登记(花名册留着) | `contract` | 红 2 | 登记退化 + 花名册对不上 |
| M15 | README 单测数抄 W300 自称的 717 | `contract` | 红 1 | 实测 718 文档 717 |
| M16 | 单测棘轮 `FLOOR` 退到 714(**越过 `SLACK` 3 格缓冲**) | `contract` | 红 1 | 下限 714 落后实测 718 |
| M16b | (对照)单测棘轮 `FLOOR` 退到 715(恰在缓冲内) | `contract` | 绿 —— **设计内**,不拿减压阀冒充漏网 | —— |
| M17 | 花名册里改掉 `dedupe-shot-card-jump` 那一行(下限仍 30) | `contract` | 红 1 | 花名册/在册对不上 |
| M18 | 撤掉 `dedupe-card-jump-keep` 的 `hosts: 2` | `contract` | 红 2 | 落点多于登记且锚点条条都窄 |

计:**19 手当场红、1 手对照绿(设计内)、漏网 0**。W301 自报"15 手全红、漏网 0",
本槽加打的四手(M7b / M11 / M12 / M18)同样红,没量出它漏掉的格子。

### 2.1 M4/M5 那两格:源级数"绑了几处"与行为面点"哪一档没反应"是两句话

W301 §五 把这一格写在明处,本槽现跑逐字复核:摘掉任一处绑定时,报出来的**不是**源级那句
"绑定处数不对",而是那一档真点下去的 `chip(...).onclick is not a function`——小标照旧渲得出来、
`data-dupjump` 照旧在,只是没人给它挂 handler。M6 那一手正相反(行为等价、只有源级红)。
两句话分得开,这一格才既拦得住"少绑一档",也拦得住"绑对了但认错落点"。

### 2.2 R7/R7b:主体侧那条判据仍绑在接收者变量名上(残留取证,**明令不代修**)

W300 残留第 7 条说的是主体侧那条「定位动作只许有一处」数的是 `card\.scrollIntoView\(` 这个
**连着接收者变量名**的字面。W301 给镜头侧写的那条从一开始就收在 `\.scrollIntoView\(` 上,
故 M7(改名 `el`)与 M7b(仍叫 `card`)两手同红。主体侧那条本槽照旧不碰,现跑两手取证:

| 手 | 在 `js/roles.js` 里把定位动作抄第二份,接收者叫什么 | `commands` |
| --- | --- | --- |
| R7 | `el` | 75/75 **绿 —— 残留现场** |
| R7b | `card`(照旧) | 74/75 **红(咬住)** |

即:那条判据此刻仍只在"抄的那一份恰好也把变量叫 `card`"时成立。两手的锚点在 P1 上**命中**
(它是 W299 带进来的旧代码,本槽一行没改),与上表那 19 手"全部失配"正好互为对照——
失配的那批是这次真合进来的东西,命中的这一手是原样留着的残留。

## 三、live 探针(真起服务 + 真渲页面 + 真点)

单测那一层的 DOM 是桩(`dataset` 是照标记上写的值填的,`style.outline` 是往普通对象上写的,
`scrollIntoView` 是个空函数),故"三档卡片小标真点得动"这一跳在真 DOM 上另量一遍。

探针形状:真起 `server.js` 子进程(`MV_DATA_DIR` / `MV_UPLOADS_DIR` / `MV_CONFIG` 三个 env 重定向到
`/tmp`,`env -u HUJING_SERVER`)+ 真注册登录 + 真 `PUT /api/state` 灌一棵脏树 + 无头 Chrome + CDP +
页面里真 `Store.pullState()` + 真进分集工作区 + 五档逐档真渲 + 真 `click()` 那几枚小标。
脏树按"落点与被点那一行隔着别的行"造:五行分镜里 `dup` 撞第 1 与第 3 行、`two` 撞第 4 与第 5 行。

同一份探针两处各跑一遍:

| 格 | P1(`d193330`) | 合完(`38022ab`) |
| --- | --- | --- |
| 真登录 + 真 `pullState` + 脏树到位(5 行分镜) | PASS | PASS |
| 视图 `shots`:`data-row` 全表实位、后续那几行各挂落点(第 1 行不挂) | **FAIL** | PASS |
| 视图 `shots`:真点 → 本组第 1 行那张卡被高亮,视图/选中镜不换、零 toast | **FAIL** | PASS |
| 视图 `cut`:同上两格 | **FAIL** / **FAIL** | PASS / PASS |
| 视图 `groups`:同上两格 | **FAIL** / **FAIL** | PASS / PASS |
| 视图 `board`:一枚可点的小标都没有,顶栏那条入口照旧在 | PASS | PASS |
| 视图 `bb`:同上 | PASS | PASS |
| 纯导航:点击那几下 `rev` 一格没动、5 行 id 一个没改 | PASS | PASS |
| 页面真加载了这三档的渲染文件 | PASS | PASS |
| **合计** | **5/11** | **11/11** |

翻绿的恰 **6 格**。P1 那一侧的失败句本身就是 W299 残留第 1 条的现场:
四枚小标**渲得出来**(`第 1/2 行`、`第 2/2 行`、`第 1/2 行`、`第 2/2 行` 逐枚在场),
而 `data-row` 一个都没有、`data-dupjump` 一枚都没有,真点时报的是
`NO-CHIP(这一档没绑上跳转,点了没有任何反应)`——"标出来了,但点不动、翻不到第 1 行"就是这个形状。

合完那一侧几个数原样抄在这里(探针现取,不是重述用例;三档逐字相同):

- `data-row` 序列 `0,1,2,3,4`(全表实位,不是视图里那张表的局部下标);
- 小标与落点:`第 1/2 行→(无跳转) | 第 2/2 行→0 | 第 1/2 行→(无跳转) | 第 2/2 行→3`;
- 真点两下:`{"outline3":"2px solid var(--accent)","outline0":"2px solid var(--accent)","vmSame":true,"selSame":true,"toast":0}`;
- `board` / `bb` 两档:`{"jumps":0,"rows":0,"entry":true}`;
- 纯导航:`rev 0(起) → 1(渲完) → 1(点完)`,`dup,solo,dup,two,two`(那一格 `rev` 是进工作区渲那一下动的,
  点击那几下之后一格没再动——探针把"渲一遍"与"点几下"分成两次取数,才说得出这句话)。

## 四、四格核对(明令逐条)

**① jump 挂在 shots / cut / groups,board / bb 仍无。** 真 DOM 上现取:三档各渲出 4 枚小标、其中 2 枚挂跳转;
`board` 与 `bb` 两档 `data-dupjump` 与 `data-row` **各 0 个**,顶栏那条 `data-x=shotdedupe` 入口照旧在。
源码侧同口径:`js/sb-board.js` 与 `js/beatboard.js` 对 `dupRowTag` / `data-row` / `data-dupjump` / `bindDupJump`
**零命中**。判据侧 M4/M5 各红 1(报的是那一档的 `chip(...).onclick is not a function`)。

**② 第 1 行那一枚不挂跳转。** 两组的 `第 1/2 行` 各现取 `dataset.dupjump === undefined`;
`js/sb-views.js` 里是 `const jump = keep >= 0 && keep !== row;`,落点等于自己那一行时整个属性都不渲。
M1 把它放宽成 `keep >= 0` 时当场红。

**③ 主体侧 jump 未改。** `git diff P1 M -- js/roles.js` **整块空集**——渲一处(313 行)、绑一处(474 行)、
取一处(476 行)三处 `data-dupjump` 与 W299 落地时逐字节相同,那条"先切分类 tab 再定位"的写法一个字没被
镜头侧这一槽带偏。`unit · commands` 里主体侧那条用例现跑照旧绿(R7b 那一手才让它红)。

**④ `dupCopy` 未改。** `git diff P1 M -- js/domain.js` **整块空集**,`dupCopy` / `dupGateNote` / `DUP_UNITS`
三个名字零命中。那句"点这枚小标定位到第 1 行那张卡"是在 `js/sb-views.js` 展示层用 `c.title + '…'` 缀上去的,
模板本身一个字没进(这一条同时是残留第 6 条的现场:两侧的这句话仍各写一份)。

另**产品语义未动**:`git diff P2 M` 整树空集,W301 那一侧的行为一个字节都没被本槽改写。

## 五、live 数字(全部现跑,一个数不抄)

| 项 | 数 | 取法 |
| --- | --- | --- |
| `unit` | **718/718** | `env -u HUJING_SERVER node tests/unit.js`(起点在 P1 上现跑 **717**,+1 落在 `commands`) |
| `unit · commands` | **75** | 74 → 75 |
| `unit · domain` | **49** | 未动 |
| `unit · contract` | **153** | 未动 |
| `unit · sb-views` | **11** | 未动 |
| `unit · skills` | **104** | 未动 |
| `integration` | **152/152** | `MV_DATA_DIR`/`MV_UPLOADS_DIR`/`MV_CONFIG` 隔离到 `/tmp`,`env -u HUJING_SERVER` |
| `cli.smoke` | **115/117** | **单独现跑**,同上隔离 + `HUJING_CONFIG_DIR` |
| `e2e` | 明令不跑 | —— |

两个自称都没盲抄:W300 自称 `unit` 717、W301 自称 718,**live 是 718**,故 README 那句写 718
(M15 把它改回 717 时 `contract` 当场红)。

`cli.smoke` 那两条失败与 `origin/master`(`9adcf0ff964891dc17c352f6ae06db6ee7a9383b`)在同一套 env 下
**同名同表现**,那边现跑 51/53:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

`node --check` 逐个过:`js/sb-views.js` / `js/shotgroups.js` / `js/storyboard.js` / `tests/unit.js`。

## 六、并集与棘轮

`TOPIC_FLOOR` 与花名册**对齐到 live 的 30**(对侧自称 30,现取 `GUARD_TOPICS` 恰 30 条、花名册正文恰 30 行,
末两条是 W299 的 `dedupe-card-jump-keep`(本槽起 `hosts: 2`)与 W301 新登的 `dedupe-shot-card-jump`)——
没有仍写 29,M13 把它退回 29 时当场红。在册 / 销号 / 花名册 / 下限四数:**30 / 0 / 30 / 30**。

记账件三方对齐 **317**(目录实况 / `docs/skills-wave/README.md` 明写 / 索引表行数),`FLOOR` 316 → 317。
索引新增本文一行,插在 w301 那一行之后(索引按波次号递增那条判据接得住整行挪位)。

五格棘轮差额:

| 格 | live | `FLOOR` | 差额 |
| --- | --- | --- | --- |
| `unit` | 718 | 718 | 0 |
| `integration` | 152 | 152 | 0 |
| `cli.smoke` | 117 | 117 | 0 |
| 记账件份数 | 317 | 317 | 0 |
| 护栏主题 | 30 | 30 | 0 |

**`SLACK` 3 一格没用掉。** M16 有意选"越过缓冲"那一档(718 退到 714 才红),
M16b 对照那一手退到 715 落在缓冲格内、绿是设计内的。

`gaps()` 仍 **20 键**,一键没剥(现取 `G-08,G-15,G-09,G-01,G-02,G-12,G-13,G-04,G-10,S-01,S-03,G-07,G-14,G-05,S-04,S-05,G-03,G-11,S-06,S-07`)。
`cli.js` / `mcp.js` / `server.js` / `index.html` 逐字节未动。
既有清单原样保留:`Domain.dupIdScan` / `dupIdMarks` / `dupIdKeepOrder` / `dupCopy` / `dupGateNote` 一字未改,
新并入的是分镜卡片那条 `data-dupjump` 与 `SBViews.bindDupJump`(三档同读一处)。
`Domain.dupIdKeepOrder` 的同读处由 1 处(`js/roles.js`)变成 2 处(另一处 `js/sb-views.js`)。

## 七、残留(本槽**不代修**,W301 原话逐条保留)

W301 §八七条原话:

> 1. **`cli.js` 的 `shots-dedupe` 注释里点名 `Domain.reviseTargets` 的那半句仍不准**(W287 残留第 1 条原样在)。
>    明令不碰,继续记账点名。
> 2. **发现存量重复仍得先走到主体库那一页或分集工作区顶栏。** 项目详情面与项目列表那几枚角标点过来落的仍是整页,
>    不是撞车那张卡;整树导入/恢复与 `state-put` 照旧能写进重复 id(明令不给入口加闸),这仍是**存量**出口。
> 3. **跳转仍只有"去第 1 行/第 1 位"一个方向。** 站在第 1 行那张卡上,看不到"还有哪几行撞了我这个 id"——
>    它那一枚一个跳转都不挂(挂上就是跳自己)。要收得让第 1 行那一枚指向后续那几行(不止一行时还要挑一个或列出来),
>    那是一对多的另一种交互(W299 残留第 4 条同形,本槽没动)。
> 4. **`board` / `bb` 两档仍只看得见顶栏那个总数。** 分镜脚本渲场次与节拍、节拍板渲五段节拍,一行分镜都不渲,
>    故那两档上既没有小标也没有落点;"这一集哪几行撞了"要看得见,仍得先切到另外三档之一
>    (§二那张表把这一格量在明处,明令不给这两档做逐行落点)。
> 5. **主体侧那条「定位动作只许有一处」的判据仍绑在接收者变量名上**(W300 残留第 7 条原样在)。
>    镜头侧这一条从一开始就写成不带接收者的形状(§六 M4/M4b 两手同红),而 `js/roles.js` 那一条本槽明令没代修。
> 6. **主体侧与镜头侧的展示文案仍各写一份**(W287 残留第 5 条、W299 残留第 6 条原样在)。本槽又添了一句
>    (小标那句"点得动",在 `js/sb-views.js` 展示那一层,与主体侧那句只差单位词与"要不要切 tab"半句);
>    要合并成一份模板得先把"点了做什么"这一段与单位词一起参数化。
> 7. **切过去之后没有回程。** 跳到第 1 行那张卡之后,"回到刚才那一行"没有路(高亮 2.2s 褪掉就没了),
>    只能自己往回滚——这一格比主体侧轻(没换视图、整屏没变),但仍是一条单向路。

七条一条没代修,逐条现取核过:

- 第 1 条:`cli.js` 本槽零 diff,那半句仍在 825 行与 1283 行两处点名 `Domain.reviseTargets`;
- 第 2 条:`js/episodes.js` / `js/projects.js` 本槽逐字节未动,那几枚角标的落点一行没改;
- 第 3 条:§三那份真 DOM 探针现取,两组的 `第 1/2 行` 各 `(无跳转)`,反方向一条路都没有;
- 第 4 条:§四① 现取,`board` / `bb` 两档 `data-dupjump` 与 `data-row` 各 0 个;
- 第 5 条:§2.2 两手现跑取证(`el` 绿、`card` 红),`js/roles.js` 那条判据原样绑在变量名上;
- 第 6 条:两句文案现取仍各一份——`js/sb-views.js:75`(「…第 1 行那张卡(就在这一档里,不换视图也不换选中镜)…」)
  与 `js/roles.js:312`(「…第 1 位那张卡(在别的分类下就先切过去)…」);
- 第 7 条:探针里三档各点两下,点完只有落点那张卡的 `outline`,没有任何"回到刚才那一行"的落点。

**本槽不新记残留。** §2.1 与 §2.2 量到的两件事都已由 W301 第 5 条与本文 §二写在明处,
不另起一条(把同一件事记两遍会让下一槽以为多欠了一笔)。

## 八、这一槽可复用的两件事

- **连着第三条快进形槽,`tree(M) == tree(P2)` 仍旧一个字都不作证。** 三槽下来这一格的读法已经稳定:
  它只说明"合完与对侧长得一样",而误合出来的树同样长这样。真正说得出话的是"同一批锚点在 P1 上全部失配"——
  本槽 19 手 + 1 手对照的锚点逐条拿到 `d193330` 上试,命中 0 处。
- **合入槽里,"残留还在不在"可以拿变异手当量具,而且它与"合进来了什么"的方向正好相反。**
  合进来的东西:锚点在 P1 上**失配**、变异在合完那侧**红**;原样留着的残留:锚点在 P1 上**命中**、
  同一手变异在合完那侧**绿**(R7)。两组一起摆出来,"哪些是这次真进来的、哪些是从上一槽原样带过来的"
  一眼分得开,比逐条读 diff 快得多,也比"我核过了"这句话立得住。
