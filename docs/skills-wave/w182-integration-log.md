# W182 集成收敛记录:W177 G4 分报 + W178 护栏主题销号留痕

> 基线 `cursor/w179-integration-3a6e@7cae7c8`;本槽分支 `cursor/w182-integration-646e`,tip 是本文这一提交。
> 两次 `--no-ff` 合并,取两支的远端实际 HEAD。

## 1. 结果一句话

两支并进同一条集成线:**W177** 让发布门 G4 的回执把「可重跑的过期镜」与「已定稿、批量重生成碰不到的过期镜」
分开报(门槛一个字没动:`counts.stale` 照旧数定稿过期镜、G4 照旧 fail、`fix.shotIds` 照旧是全部过期镜);
**W178** 让护栏主题的**销号非留痕不可**——花名册逐号点名 + 销号台账 `GUARD_TOPICS_CLOSED` + 下限只增不减。

本槽最要紧的一格是 **W178 的花名册与集成线的登记表撞在了一起**:W178 从 `c2acb4d`(W170 那一线)叉出,
它看得见的登记面是 10 条、`TOPIC_FLOOR` 是 10;而集成线上 W174 补了 2 条、W175 补了 5 条,基线已是 17 条、下限 17。
两侧的花名册与登记表**都按并集合**,`TOPIC_FLOOR` 按合完后 live 登记条数重测为 **18**——
两侧自称的 11 与 17 都不是答案。**并集不是形式**:实测若照抄 W178 那份 11 行花名册,
悄悄撤掉集成线上补登记的 `ep-blocker-fanout` 并把下限一并改小,**红 0**(见 5.3 的 M3)。

同时收掉 W179 记的那条残留:**「只把 `TOPIC_FLOOR` 改小、一条主题都不删」在基线上曾全绿**
(W179 §5.6 实测 17 → 16 / 15 / 14 / 12 / 10 五格全绿),合完后 **18 → 17 / 16 / 15 各红 1** 并点名。

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w177-g4-final-stale-a1c7@8b09da4` | `585def8` | G4 印着「N 镜素材与当前剧本不一致」,而这 N 镜里定稿的那几镜两端批量生成都锁着 `!s.final` 碰不到,用户按下处置回来门禁照旧 fail;分堆判据与那句话收进 `Domain.staleShotSplit` / `staleSplitNote` 一份,浏览器十门与 headless 七门同读 | `js/domain.js` +19、`js/release.js` +15 −3、`js/release-core.js` +13 −1 | +5 `unit`(1 `domain`、3 `release`、1 `contract`) |
| `w178-topic-floor-unlist-3a7f@3a2adc6` | `ae1b821` | `GUARD_TOPICS` 的下限此前只是个可改的字面:改小它而一条主题都不删、或连同下限一起把某条主题整段删掉,两种改法都一条不红;换成逐号点名的花名册(写在记账件里,与源码分开)+ 只能显式落笔的销号台账,并把护栏主题收进既有 `SLACK` 棘轮成第五格 | 产品代码零改动 | +2 `unit`(`contract`),另重命名 1 条(四条 FLOOR → 五条 FLOOR) |

合并后回归 **`unit 561/561`**、**`integration 143/143`**、**`cli.smoke 105/107`**(2 项失败与基线同名同表现,见 5.2),
`contract` 子套件 **128/128**;`GUARD_TOPICS` **18 条**、`TOPIC_FLOOR` **18**、`GUARD_TOPICS_CLOSED` **0 条**;记账件 **195 份**(含本文)。

治理面:`gaps()` **20 键**(`G-03` 仍是两条值、`G-11` 仍在案)、提示词注册表 **41**、能力短名单 **30**、
`Skills.CHECKS` **17**、`preflightStages()` **七面**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 八个数一个没动。
本槽**注册表五个文件相对基线零 diff**(`git diff --stat 7cae7c8 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js` 空输出),
故这八个数是结构性成立,与 W179 / W174 / W170 / W166 / W161 / W157 同形。

**不在本槽射程内**(仍在飞,一行没碰):W180 的 G1 分集口径、W181 的 `Bus.on` 空 `catch`。

## 2. 两次合并各自的形态

两次都是真 `--no-ff`,两个 parent 逐个记在这里:

| # | 合并提交 | parent 1(集成线) | parent 2(被合入支) | 叉点 | 冲突文件 |
|---|---|---|---|---|---|
| 1 | `585def8` | `7cae7c8` | `8b09da4` | `c2acb4d` | `README.md`、`docs/skills-wave/README.md`、`tests/unit.js` |
| 2 | `ae1b821` | `585def8` | `3a2adc6` | `c2acb4d` | 同上三份 |

两支同叉 `c2acb4d`。**三份冲突文件在两次合并里都是真三方**——`M` 与 `P1` / `P2` / `B` 四棵树的 blob 两两不等,
逐份机检过(W161 那条教训:判 `--ours`/`--theirs` 要看另一侧等不等于叉点,而不是看 `M` 等于谁)。
产品面三个文件(`js/domain.js` / `js/release.js` / `js/release-core.js`)**零冲突块自动合上**,
本槽没有"零冲突块但两侧都改过同一段"的形态可量。

### 2.1 第一次合并(W177)怎么并的

| 位置 | 两侧各是什么 | 怎么并 |
|---|---|---|
| `tests/unit.js` 夹具区 | 我方 `loadReleaseFix` / `staleFixProject`(W176 带进来的)/ 对侧 `releaseStaleEp` / `releaseStaleP` | **并集**,两侧在同一锚点各插一段且**共用块尾那个 `}`**,给我方那半补一个 |
| `tests/unit.js` `release` 套件尾 | 我方 W173 一条 + W176 两条 / 对侧 W177 两条 | **并集**,我方在前(块尾 `  } },` 两侧共用,给我方那半补一个) |
| `tests/unit.js` 三套件下限 | 554/143/107 / 547/141/107 | 单元现取 live **559**;集成取我方 **143**(对侧那个 141 是它叉点上的旧值);冒烟两侧同值 |
| `tests/unit.js` 记账件 `FLOOR` | 192 / 184 | 现取 live **193** |
| `README.md` 契约段 | 两侧只差自报条数 | 取我方整句,`contract` 条数换 live **126** |
| `README.md` 「单元测试(N 项断言」整句 | **不只差那个数**——对侧还在「G4/G5/G6 计数判不出来时如实回执」那段之后插了 G4 分报一整段 | **并集**,对侧那段按锚点接回,数字换 live **559** |
| `docs/skills-wave/README.md` | 明写份数 192 / 184;索引表我方八行 / 对侧 `w177` 一行 | **193**;**并集**,九行按波次号排(`w177` 归到 `w176` 与 `w179` 之间) |

第一次合完先跑 `unit`:**559 / 559**、`contract` **126 / 126**。

### 2.2 第二次合并(W178)怎么并的 —— 花名册那一格

这是本槽唯一一处"两侧说的是同一件事而基数不同"的冲突。

| 位置 | 两侧各是什么 | 怎么并 |
|---|---|---|
| `GUARD_TOPICS` 表尾 | 我方七条(`extract-source-single`、`g456-count-source`、`review-min-literal`、`review-gate-single`、`project-script-single`、`ep-blocker-fanout`、`zip-placeholder-ban`)/ 对侧一条 `topic-unlist-ledger` | **并集**,合后在册 **18 条** |
| 花名册(`w178-topic-floor-unlist.md`「花名册」那一节) | 对侧十一行(基线 `c2acb4d` 上的十条 + 它自己那条)/ 我方**没有这张表**(本槽之前不存在花名册) | **并集**:把我方那七个号按登记来路(`extract-source-single` / `g456-count-source` 记 W174,其余五条记 W175)补进表里,共 **18 行**,并在表下写明后七条是集成线上补登记、W178 叉点看不见 |
| `TOPIC_FLOOR` 字面 | 对侧 **11**(模块作用域,`floor-slack` 的第五格靠 `^const TOPIC_FLOOR = (\d+);$` 现取它)/ 我方 **17**(写在那条用例体内,缩进四格) | **两侧都不是答案**:按合完后 live 登记条数取 **18**,并留在模块作用域(留在用例体内的话第五格棘轮取不到那个字面,会红在它自己的自检句上) |
| 下限那句判据 | 我方 `GUARD_TOPICS.length >= TOPIC_FLOOR` / 对侧 `在册 + 销号 >= TOPIC_FLOOR` | 取对侧(它是我方那句的超集:销号把编号搬进台账时总数不掉,故销号不必回头改下限) |
| 花名册取数口自检 | 对侧 `assert(roster.length >= 11, …)` | 按 live 抬到 **18**(留在 11 的话,悄悄从花名册里删掉七行仍全绿) |
| `contract` 套件尾 | 我方 W172 四判词那条 + W175 抽样那条 / 对侧销号两条 | **并集**,我方在前(块尾 `  } },` 两侧共用,给我方那半补一个) |
| 「四条 FLOOR」那条用例 | 我方叫「四条 FLOOR」/ 对侧改名成「五条 FLOOR」并把护栏主题收进 `live` 表 | 取对侧整条(**这是改名不是删测**,见 5.4 的名集口径) |
| 三套件下限 | 559/143/107 / 544/141/107 | 单元现取 live **561**;集成取我方 143;冒烟两侧同值 |
| 记账件 `FLOOR` | 193 / 184 | 现取 live **194**(本文落地后 **195**) |
| `README.md` 契约段整句 | 我方带 W172 / W175 / W177 三段 / 对侧带销号那段与五格棘轮那两处 | **并集**,各按锚点接回;「登记面现有十七条主题」按 live 改成**十八条**并把销号留痕这条一并点名;`contract` 条数换 live **128** |
| `README.md` 「单元测试(N 项断言」整句 | 两侧只差那个数 | 取我方整句,数字换 live **561** |
| `docs/skills-wave/README.md` | 明写份数 + 索引表尾 | **194**;**并集**,十行按波次号排(`w178` 归到 `w177` 与 `w179` 之间) |

## 3. 数字一律合完后 live 重跑

**没有一个数是抄两侧父值来的。** 逐格实测读数:

| 数 | 基线 `7cae7c8` | W177 侧自称 | W178 侧自称 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|
| 单元用例数 | 554 | **547** | **544** | **561** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 125 | **122** | **123** | **128** | 主 `README.md` 契约段 |
| `integration` | 143 | **141** | **141** | **143** | 主 `README.md` + 下限表 |
| `cli.smoke` | 107 | 107 | 107 | **107**(实跑 105/107) | 主 `README.md` + 下限表 |
| 记账件份数 | 192 | **184** | **184** | **195**(含本文) | `docs/skills-wave/README.md` 明写 + `FLOOR` |
| `GUARD_TOPICS` 在册 | 17 | 17 | **11** | **18** | `tests/unit.js` 那张表 |
| `TOPIC_FLOOR` | 17 | — | **11** | **18** | `tests/unit.js` 模块作用域 |
| 花名册行数 | —(不存在) | — | **11** | **18** | 本目录 `w178-topic-floor-unlist.md`「花名册」那一节 |

**两支自称的记账件份数逐字相同的 184**——两支同叉 `c2acb4d`、各加一份记账件,又是那个静默窗口。
与 W179(两对)、W174、W170、W166 各槽同形,**连着第五槽出现**。若照抄它,与 live 195 差 **11 格**。

逐次合完的读数:

| | 合入 W177 后 | 合入 W178 后(本槽 tip) |
|---|---|---|
| `unit` | 559 / 559 | **561 / 561** |
| `contract` | 126 / 126 | **128 / 128** |
| `integration` | — | **143 / 143** |
| `cli.smoke` | — | **105 / 107** |
| `GUARD_TOPICS` / `TOPIC_FLOOR` | 17 / 17 | **18 / 18** |
| 记账件份数 | 193 | **194**(本文后 195) |

**两侧写同一个「份数」时 `git` 静默留一边这件事本槽照旧核过**:两次合并的份数格都给了冲突块,
但仍按目录实数重数(`ls docs/skills-wave/*.md` 去掉 `README.md`)而不是取任一侧,读数 193 / 194 与实况逐格相符。

## 4. 产品门槛一个字没动

- **G1 仍看整本 `script`**:`Domain.projectScript` 那份派生本槽一行没碰,命令层与计划层同读照旧(`project-script-single` 那条登记的两处承载都在)。
- **G4 仍 fail 且仍含定稿过期镜**:`counts.stale` 的判旧函数 `shotVideoStale` 一字未改,`staleShotSplit` 只是拿它筛完再分两堆(`all` = `rerun` + `locked`,`all.length` 恒等于 `counts.stale`);
  `fix.shotIds` 仍是全部过期镜(**没有减员**),定稿的那几镜只是**另外**多带一份 `lockedShotIds`。
- **W176 那个显式子集位仍放过期镜过**:`js/commands.js` 里 `shotVideoStale` 现取 **2** 处、`cli.js` **1** 处,终稿锁仍拦着。
- **`expert.evolve` 仍不在 playbook `steps` 里**(现取 `playbooks()` 5 条,逐条 `steps` 零命中)、`gaps()` 仍是 **20 键没拆**、
  `review.userSystem` 仍只一处、`FORGE_SYS` 仍是 getter、`Issues` 仍是 UMD、`project.release` 仍在注册表、
  `reviseRetryLimit` 五处消费点齐、`reviewGate` 仍只一份、`memWrite` 驱逐仍在 `wf-core` 一份、
  W179 合并后的 `guardSpread` 四种判词(`lost` / `short` / `broad` / `extra`)与可注入 `hosts` 原样。

## 5. 取证

### 5.1 零 diff 与改动面

产品面只有 W177 那三个文件(`js/domain.js` +19、`js/release.js` +15 −3、`js/release-core.js` +13 −1);
W178 产品代码零改动。测试面 `tests/unit.js` +298 −11(含两次解冲突与本文这一提交抬 `FLOOR` 那一行)。注册表五文件零 diff(见 §1)。

### 5.2 三套件实跑

| 套件 | 基线 `7cae7c8` | 本槽 tip |
|---|---|---|
| `node tests/unit.js` | 554 / 554(`contract` 125 / 125) | **561 / 561**(`contract` **128 / 128**) |
| `node tests/integration.js` | 143 / 143 | **143 / 143** |
| `node tests/cli.smoke.js` | 105 / 107 | **105 / 107** |

冒烟那 2 项失败与基线**同名同表现**,逐字如下,两条都是 `master` 基线上就有的:

- `未登录 whoami → exit 3 | exit=1`
- `llm --json mock 链路 | undefined`

### 5.3 变异抽查

| # | 变异 | 合并后 HEAD |
|---|---|---|
| **M1** | `TOPIC_FLOOR` 18 → 17 / 16 / 15,**主题一条不删** | **各红 1**(销号留痕那条,报「下限 N 低于花名册的 18 条」)。**基线上同一手是全绿**——同一变异在 `7cae7c8` 上把 17 改成 16,实测 554/554 全绿,W179 残留 1 就此闭环 |
| **M2** | 删掉 `ep-blocker-fanout` 那条登记**并把下限一并改小**(完整的销号动作) | **红 1** 并**点名那个编号**:「`ep-blocker-fanout`:花名册上有,清单与销号台账里都没有——撤一道护栏须把编号搬进销号台账并写下闭合理由,把 `TOPIC_FLOOR` 一起改小不作数 / 下限 17 低于花名册的 18 条」 |
| **M3** | 反事实:花名册退回 W178 那份 **11 行**(不并集),再做一次 M2 | **红 0**。这一格量的正是"并集"本身值多少:不并集的话,集成线上 W174/W175 补登记的那七条撤起来一个字都不留 |
| **M4** | `js/release.js` 把 `fix.shotIds` 减员成 `rerunShotIds`(把定稿镜从处置子集里摘掉) | **红 3**:W176 终稿那条(「子集位如实带上终稿过期镜」)+ W177 分报两条 |
| **M5** | `Domain.staleShotSplit` 把 `final` 从 stale 里摘掉(门槛被放宽) | **红 5**:`domain` 分堆那条 + `release` 三条 + `release-core` 两端那条 |
| **M6** | 只让 **headless** 那一端不接分堆(`note` 恒空),浏览器端原样 | **红 2**:`release-core` 两端同一句那条 + `contract` 取数口那条。一端改了另一端没改接得住 |
| **M7** | 把记账件里「花名册」那一节的标题改掉(取数口失效) | **红 2**:销号留痕那条(报「花名册只取到 0 个编号」)+ 判词自立那条 |

M1 与 M3 是本槽的两格要紧读数:前者证 W179 残留 1 真收掉了,后者证并集不是形式。

### 5.4 名集证明(按 `|` 切,多重集,不 unique-sort)

抽名口径:`node tests/unit.js` 的 `PASS|FAIL` 行按 `|` 切取第 2 段,**多重集比较**(不去重、不排序去重),
`Counter` 相减。四棵树各跑一次:叉点 `c2acb4d`(544)、基线 `7cae7c8`(554)、`w177` tip(547)、`w178` tip(544)、本槽 tip(561)。

- **基线独有(`base − tip`):1 条**,逐条点名:
  - `contract · 棘轮下限不得静默落后实况:四条 FLOOR 与 live 的差额有上限(加测不抬下限、把下限改小都红在这里)`

  **这是改名不是删测**:W178 把这条从「四条 FLOOR」改成「五条 FLOOR」并把护栏主题收进它的 `live` 表,
  tip 上「五条 FLOOR」那条在场且跑绿。判这一种与"真删掉"的差别正是 `GUARD_TOPICS` 存在的理由——
  `floor-slack` 那条主题的锚点(`SLACK`、`记账件份数`)在 tip 上照旧点得到承载用例,故名集这 1 条按"改名"记,不按丢失记。
  **除它之外基线独有 0 条。**
- 只在 tip 里有的:**8 条**,逐条点名(W177 五条、W178 三条):
  - `domain · staleShotSplit:过期镜按终稿锁分两堆(判旧仍只此一份;两堆之和 = counts.stale)`(W177)
  - `release · G4 回执分报:过期镜里定稿的那几镜单列报出来(counts.stale 与 fix.shotIds 一个数没动)`(W177)
  - `release · G4 回执分报:过期镜全是定稿镜时如实说一镜也重跑不到(不报「可重跑 0 镜」糊过去)`(W177)
  - `release · release-core · G4 分报两端同一句:headless 与浏览器同读 Domain 的分堆,窄 Domain 退回只报总数`(W177)
  - `contract · 过期镜分报的取数口:两端 G4 都现取 Domain 的分堆与那句话,谁也不自写第二份终稿判据`(W177)
  - `contract · 棘轮下限不得静默落后实况:五条 FLOOR 与 live 的差额有上限(加测不抬下限、把下限改小都红在这里)`(W178,改名后那条)
  - `contract · 护栏主题销号须留痕:花名册逐号点名 + 下限只增不减(改小下限、连同下限一起撤登记都红在这一条)`(W178)
  - `contract · 护栏主题销号判词自身立得住:撤登记、连同下限一起改小、空理由销号各自点名(拿造出来的清单喂判词)`(W178)

554 − 1 + 8 = 561,与实跑总数对得上。

**被合入支 ↔ 合并后**逐支再比一轮(各自与叉点 `c2acb4d` 取差,再看那些名在不在 tip 上):
W177 相对叉点新增 **5** 条、丢失 **0** 条,五条**全在 tip 上**;
W178 相对叉点新增 **3** 条、丢失 **1** 条(即上面那次改名),三条**全在 tip 上**。零吃测。

### 5.5 本槽自己改了什么

除两个合并提交解冲突外,本槽只多做一件:**本文这一提交**——新增本文、`docs/skills-wave/README.md` 补索引行
并把明写份数 194 → 195、把"最新一份收敛记录"的指针从 `w179-integration-log.md` 挪到本文(旧指针进"更早的分叉"那一串)、
`tests/unit.js` 的记账件 `FLOOR` 194 → 195。

单元用例数与 `contract` 条数**本文一条不加**,故 561 / 128 不动。
两个合并提交里**没有删过任何一条用例、没有放宽过任何一条断言、没有翻过面、没有改过任何一条既有断言**;
唯一被改名的是「四条 FLOOR」那条,而那是 W178 自己在它那一支上做的收严(护栏主题并进第五格),不是解冲突时动的手。

## 6. 残留与欠账

1. **`GUARD_TOPICS` 取并集这件事本身仍然没有任何判据管。** W178 收掉的是"撤登记要留痕"这一路(花名册接住了),
   但"合并时机械取任一侧"仍是无声的:取我方丢 W178 那条而花名册没有它的号——**一条不红**;
   取对侧丢我方七条,那七条的号也不在 W178 那份 11 行花名册里——**同样一条不红**(5.3 的 M3 就是这一路的实测)。
   花名册只覆盖**写进它的号**,而写进去这件事是人工的。W174 残留 2、W179 残留 1 记的是同一件事的两个面:
   冲突块里"两侧各有一半"的东西,至今没有一层判据算得出并集。
2. **本槽把七个号补进花名册,靠的是逐条查 `git log -S` 追首次登记提交,不是任何一条判据。**
   花名册的「首次登记」那一列写错波次号一条不红(判据只读首列编号),写漏一整行同样只在那个号被撤时才红。
3. **`TOPIC_FLOOR` 那一格的棘轮上限仍是 `SLACK` 3。** 本槽把它抬到 live 18 后差额 0,但判据允许它落后 3 格:
   实测 18 → 15 时销号留痕那条报的是"下限低于花名册"、`floor-slack` 那条**不报**(15 与 live 18 差 3,压在上沿)。
   花名册这一层顶上来之后 `SLACK` 那一格对护栏主题基本是冗余的,如实登记不动它。
4. **CLI 那一端的子集位仍只有源级一条接得住,没有行为面**(W179 残留 3 原样未收);
   W177 本槽新增的四条行为面全在浏览器与 headless 两端,**冒烟套件本槽一条没加**。
5. **W177 的 `staleSplitNote` 那句话是拼出来的中文字面,没有第二处判据钉它的措辞**——
   两端同读一份(`contract` 那条接住了"谁也不自写第二份"),但把那句话整体改写成别的说法时,
   只有 `release` 那两条用例里的 `/可重跑 1 镜/` 正则接得住,而它们判的是**这一份**措辞。
6. **G4 那个按钮在"全是定稿过期镜"时仍原样挂着**(W177 用例里有意留了一句 `assertEq(g.fix.cmd, 'episode.generateVideos')` 钉住现状):
   按下去一镜也跑不到。摘不摘这个按钮、还是改挂"去解锁终稿"的导航,是产品口径,W177 明写不在它射程内,本槽也没动。
7. **`js/release.js` 底部那个包着 `Bus.on('*')` 注册的 `catch (_) {}` 仍开着**
   (W151 残留 2、W157 残留 5、W161 残留 5、W166 残留 7、W170 残留 6、W174 残留 6、W179 残留 6 原样未动)。
   全仓现取:`js/release.js` 空 `catch` **1** 处、`js/bus.js` **0** 处。**W181 正在收这一条,本槽按纪律没合它。**
8. **W179 残留 7 点名的那批没登记的源级护栏仍有剩**:W162 的两条(占位空壳那对已由 `zip-placeholder-ban` 收编)、
   W163 那条与 `review-min-literal` 是否同指一处仍未核;W176 带进来的那条 `contract`(两端子集位)**仍未登记**;
   W177 本槽新带进来的那条 `contract`(过期镜分报的取数口)**同样未登记**——补登记要各自量锚点与落点数,不在本槽射程内。
9. **W170 残留 7、W161 残留 4(弱断言普查)仍是零**,本槽两支都没碰交付链、也没碰 `assert(A || B)` 那一类。
10. **`G-03` 的形态面、`G-11` 的自动进化面均未动**(W151 残留 1、3 原样);现取核实 `G-03` 仍是两条值。
