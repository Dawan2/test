# W225 集成记账:W223 镜头空跑回执四堆按 id + W221 全终稿过期按钮停工位

新基线 `cursor/w222-integration-b7e1`,tip **`3e8893f`**(`git fetch` 后 `git rev-parse` 现取,与交接自称同值)。
**自称里点名的 `7724e5b` 是那条合入链的 merge 提交、不是分支 tip**——其后还有一个把份数与下限校到 live 的
`docs` 提交 `3e8893f`;命令行里一个 SHA 都不从交接文里抄,先 `fetch` 再 `rev-parse`,
自称与现取不符时以现取为准。本槽两支的自称 head 现取后也都与自称同值,但同样是量出来的不是采信的。

本槽合**两条**已完成支,按交接给的次序、每次 `--no-ff` 且两个 parent 齐全:

| 序 | 被合入支 | 自称 head | 现取 head | merge commit | parents | 共同祖先(现取) |
|---|---|---|---|---|---|---|
| 1 | `cursor/w223-shot-dup-gone-9e41` | `c8049d1` | `c8049d1` | `b5a2e90` | `3e8893f` + `c8049d1` | `16baeb9`(W220) |
| 2 | `cursor/w221-stale-final-pipeline-btn-9c4e` | `7220289` | `7220289` | `fa6ac3a` | `b5a2e90` + `7220289` | `5f76836`(W218) |

两次都是真 `--no-ff`(不是快进);全程没用过 `--ours`,没用过 `git checkout <old> -- .`,
没有一处冲突是靠机械丢掉一侧收的场。两条支的叉点与交接给的基线逐个对上:W223 从 W220 出、W221 从 W218 出,
故它们自称的 **644 / 235** 与 **641 / 232**、以及基线自称的 **643 / 236**,在合完这棵树上一个都不是答案。

在飞的 **W224**(成功回执 note,从 W220 出)按任务口径**一条没碰**,`master` 没合,没有开第三条功能支,
没有 cherry-pick 任何已在基线里的东西。

合完整棵产品树相对基线只此一个文件:

```
git diff --stat 3e8893f HEAD -- js/ server.js cli.js mcp.js billing.js index.html css/
 js/domain.js | 14 ++++++++++----
 1 file changed, 10 insertions(+), 4 deletions(-)
```

与 W223 自称的 `js/domain.js` **+10 −4** 逐格相同;**W221 产品面零 diff**(第二次合并后相对第一次的
merge commit,上面那七个产品路径一个字节没变),与它自称的"停工位成立、只把现况钉成测试"对得上。

## 一、四棵树机检:头一次是 `P1 == B`,第二次两个方向都不是

### 1.1 第一次合并(W223):`js/domain.js` 是 `P1 == B`

逐文件比对 `B`(`16baeb9`)/ `P1`(`3e8893f`)/ `P2`(`c8049d1`)/ `M`(`b5a2e90`):

| 文件 | 成色 | 后果 |
|---|---|---|
| `js/domain.js` | **`P1 == B`**(blob 两侧同为 `056d178`,W222 那一槽没碰它) | git **整份取对侧、不是并集**,对侧带来的东西须逐条现取,见 §二 |
| `README.md` | 真并集(四份互不相同) | 逐处人工收场,见 §三 |
| `docs/skills-wave/README.md` | 真并集 | 同上 |
| `tests/unit.js` | 真并集 | 同上 |
| `docs/skills-wave/w223-shot-dup-gone.md` | `M == P2` | 对侧新开的文件,不存在并集问题 |
| `js/plans.js`、`server.js`、`cli.js`、`mcp.js`、`billing.js`、`index.html`、`css/`、`js/` 其余全树 | 三方全同 | — |

**上一波交接点名要防的就是这一格**:git 不做任何 hunk 级取舍,直接把对侧那份 blob 整个搬过来。
合得"干净"不等于合得对——我方在这个文件上确实没有要保的东西(`B` 与 `P1` 的 blob hash 逐字节相同,
这是量出来的),但对侧到底带来了什么、以及同一文件里**没**被对侧碰过的那几处派生还在不在,
都不能靠 git 没报冲突来担保。合完的 `js/domain.js` blob 是 `8af2a73`,与 `P2` 逐字节相同,故 §二逐条现取。

### 1.2 第二次合并(W221):三个冲突文件全是真并集,产品面 `P2 == B`

`B`(`5f76836`)/ `P1`(`b5a2e90`)/ `P2`(`7220289`)/ `M`(`fa6ac3a`):

| 文件 | 成色 | 后果 |
|---|---|---|
| `README.md` | 真并集(`49c1fa9` / `ae68acc` / `3575995` 互不相同) | 逐处人工收场 |
| `docs/skills-wave/README.md` | 真并集(`e4394b5` / `43904f2` / `cd489a7`) | 同上 |
| `tests/unit.js` | 真并集(`ffaef8f` / `4298cc9` / `f28d366`) | 同上 |
| `docs/skills-wave/w221-stale-final-pipeline-btn.md` | `M == P2` | 对侧新开的文件 |
| `js/` 全树与 `server.js`/`cli.js`/`mcp.js`/`billing.js`/`index.html`/`css/` | **`P2 == B`**(对侧一个产品文件没碰) | git 整份取我方,故第一次合进来的 `js/domain.js` 不可能被顶掉——合完 blob 仍是 `8af2a73`,现取回验过 |

**这一次没有 `P1 == B` 那一格**:对侧改过的三个文件我方都改过,四份 blob 互不相同,git 是逐 hunk 合的。
上一波是 `P1 == B` 不等于这一波也是,成色按当轮重判,不照抄上一槽的结论。

## 二、`js/domain.js` 整份取对侧后逐条现取

交接点名的四堆与三道既有闸,逐条在合完的树上现取(不因为 git 没报冲突就跳过):

| 点名项 | 现取结果 |
|---|---|
| 四堆按点名 id 数 | `js/domain.js:291-295`:`hits = ids.map(id => shots.filter(s => s.id === id))`,`locked`/`fresh` 各是 `hits.filter(h => all(h, …))`、`gone = hits.filter(h => !h.length).length`——**四堆一律数点名 id,一堆都不从命中的镜条数里数** |
| 安全阀按 id | `:299` 仍是 `ids.length - locked - fresh - gone`,各堆之和恒等于点名数 |
| W216 镜头侧选人闸同形 | `:284` `Array.isArray(picked) && picked.length` 原样在 |
| W214 主体侧选人闸同形 | `:325` 同形,那一行没被顶掉 |
| W217 主体侧 `gone` | `:330` `ids.filter(id => !subs.some(s => s.id === id)).length`,仍是「找不到的 id 数」而不是「点名数 − 命中条数」 |

行为面在合完的树上现跑一遍(`vm` 直加载 `js/domain.js`,不读历史结论):

| 形状 | 合完 live 读出来的整句 |
|---|---|
| 同 id 两镜(都定稿)、点名 `['dup']` | `点名的 1 镜一镜也没跑:1 镜已定稿(批量重生成不覆盖定稿产物,需先解锁终稿)` |
| 同 id 两镜 + 真幽灵,点名 `['dup','ghost']` | `点名的 2 镜一镜也没跑:1 镜已定稿(…)、1 镜不在本集` |
| 同 id 两镜口径不一(一定稿一鲜)、点名 `['dup']` | `点名的 1 镜一镜也没跑:1 镜没能说清原因` |
| 非数组 `shotIds`(W216 那道闸) | `本集没有待生成的镜头,一镜也没跑:2 镜已定稿`(整集那一路,没被拆成字符) |
| 主体侧同 id 两位 + 真幽灵(W217) | `点名的 2 位主体一位也没跑:1 位不在主体库、1 位没能说清原因` |

三件事一起读出来了:**负数没了**(头一格从 `-1 镜不在本集` 变成如实的 `1 镜已定稿`)、
**真幽灵不再被抵消**(第二格 `ghost` 自己落进"不在本集",不是一声不吭进末堆)、
**口径不一时不硬派**(第三格落安全阀而不是硬算成定稿或鲜镜)。四格里各堆之和都恰等于点名数。

其余上游几槽的保留面同样逐条现取,一件没被顶掉:

| 来自 | 点名项 | 现取 |
|---|---|---|
| W219 | `plans` 生成侧闸 | `js/plans.js:184` 白名单 `Commands.list().filter(c => !manualCmd(c.name))`;`manualCmd` 仍是同一个函数一处;`js/plans.js` 里 `expert.evolve` 字面 **0** 处;全仓 `manual: true` 仍 **1** 处,即 **evolve 不进 `steps`** |
| W210 | 合成自动步 skip + `force` 授权位 | `episode.compose` 参数面仍 `pid,epid,force,ui`,`contract` 那条全绿 |
| W211 | 生成三步成功档零 `memWrite` | `memory` 三十五条全绿 |
| W213 | `agentAuto` 档下发点仍须过确认闸 | `agent-ops` 五十九条全绿 |
| SK-04 / G-11 / G-13 | `gaps()` 一个键没剥 | `js/skills.js` 与基线 blob **逐字节相同**(`f7c0baf`),故 `gaps()` 原样开着 |
| 护栏花名册 | `GUARD_TOPICS` / `TOPIC_FLOOR` | 仍 **19 / 19**,一条未登记也未销号 |

## 三、冲突逐处怎么收的

两次合并、七处冲突,一处都没机械取侧。

### 3.1 `README.md` 那条长行:两次都是「我方散文 + 对侧数字」

两次冲突都落在第 611 行(`node tests/unit.js` 那条 16000 余字符的长行)。
**先把数字整体掩成 `#` 再逐字符比对**,两次读出来的都是两件互不相干的事:

| 次 | 我方 | 对侧 | 收成 |
|---|---|---|---|
| 1 | 掩数字后与基线**不同**:`difflib` 求 opcode 全落在 `equal` + **单个 `insert`**(位置 2923、160 字符,W222 带进来的「人手命令两道闸」那段散文);82 个数一个没动 | 掩数字后与基线**逐字节相同**:82 个数里只有第 0 个 `643 → 644` | 取「我方散文 + 对侧数字」,数字再按 live 定 |
| 2 | 同上(那段散文仍在,第 0 个数已是 644) | 掩数字后仍**逐字节相同**:第 0 个数 `639 → 641` | 同上,数字按 live 定成 **646** |

**这两次方向与上一槽是反的**:上一槽是"我方只改数字、对侧插散文",这一槽是"我方带着散文、对侧只改数字"。
掩码比对的价值正在这里——它不预设哪一侧是散文侧,而是当轮量出来。
两次都**没有走"整句取一侧"**:整句取对侧会把那 160 字散文丢掉(README 里唯一写着"两道闸"的地方),
整句取我方则会把对侧那次抬数丢掉、让长行与实况脱节。

### 3.2 目录索引表:两次都取并集后按波次号归位

第一次冲突块里我方是 w222 一行、对侧是 w223 一行;第二次我方是 w219/w220/w222/w223 四行、对侧是 w221 一行。
两次都取并集,并**把新行插回它那个波次的位置**(w223 排在 w222 之后、w221 插回 w220 与 w222 之间)
而不是追加到表尾——`索引行序` 那条用例判的是波次号沿表往下不许倒退,追加到表尾当场红。
插进去的行与源树里那一行 `cmp` 逐字节相同(逐字节回验过,不是照着敲的)。

顺带收了一格**上一槽掉下来的空行**:表尾与 `## 一分钟摘要` 之间那个空行在基线与 W223 侧都在、
只有我方那一侧没了(W222 收索引冲突时连表尾空行一起吃掉了)。并集按"两侧行都要 + 结构按基线"收,
故空行补回,标题不再紧贴表尾。

### 3.3 数字:五处字面,两侧给的都不是答案

| 处 | 我方 | 对侧 | 收成 | 依据 |
|---|---|---|---|---|
| `README.md` 长行单元数(第 1 次) | 643 | 644 | **644** | 合完 live |
| `README.md` 长行单元数(第 2 次) | 644 | 641 | **646** | 合完 live |
| `docs/skills-wave/README.md` 明写份数 | 236 → 237 | 235 / 232 | **239**(含本文) | 合完 live |
| `tests/unit.js` 记账件 `const FLOOR` | 236 → 237 | 235 / 232 | **239** | 同上 |
| `tests/unit.js` 套件下限 `['单元测试', N, …]` | 644 | 641 | **646** | 同上 |

### 3.4 另记一格:零冲突的那个数才是本槽最险的一处

第二次合并里 `README.md` 第 362 行(契约段自报的断言条数)**两侧都写着 139、逐字相同,git 一个冲突块都没给**。
两侧那个 139 却是两笔不同的账:我方那一侧是 W216 当年抬上去的,对侧那一侧是 W221 自己新加的一条 `contract`
把 138 抬成 139。合完两条加测都在,live 实跑是 **140**。

处置没有靠人眼看出来,而是**合完先整跑一遍让对账用例自己报差额**:头一遍 `645/646`,
唯一那条红的是 `contract · README 数字对账:契约段自报的断言条数由 SUITES.contract 实计`,
报「实测 140,文档 139」,按它订正后 `646/646`。
这与 W212 记的那一课是同一格(两侧改同一行、改成同一个值,零冲突自动合上而 live 是第三个数),
在本槽再次现形——**零冲突不等于这个数就对**,每合完一次都得整跑。

## 四、数字:全部合完 live 重跑

| 项 | 基线自称 | W223 自称 | W221 自称 | 合完 live |
|---|---|---|---|---|
| 单元测试 | 643 | 644 | 641 | **646 / 646 全绿** |
| 集成测试 | 147 | 147 | 147 | **147 / 147 全绿** |
| CLI 冒烟 | 107/109 | 107/109 | 107/109 | **107 / 109** |
| 记账件份数 | 236 | 235 | 232 | **239**(含本文) |

分套件 live:`domain` 38 → **39**(W223 那一条)、`pipeline` 12 → **13**、`contract` 139 → **140**
(后两条是 W221 那两条)、`agent-ops` **59**、`commands` **47**、`plans` **17**、`memory` **35**、
`skills` **95**、`release` **48**,其余未动。
两格 `FLOOR`(单元 646、记账件 239)按 live 抬到实况,与 `SLACK = 3` 那条棘轮对得上。

**名集按 `|` 切做多重集比对**(六棵源树的快照都在**全绿 commit** 上取:
`16baeb9` 643、`3e8893f` 643、`c8049d1` 644、`5f76836` 639、`b5a2e90` 644、`7220289` 641,
一条红都没有,故名字后面不会缀失败原因):

- 第一次:`P1` 相对 `B` 净增 0(去掉 1 条、新增 1 条,是 W219 那条断言方向相反的用例改写);
  `P2` 相对 `B` **净增 1**(`domain · emptyBatchNote:分镜表里同 id 存着两镜时四堆都按点名 id 数…`)。
  合完 tip 零缺失、零多出(不属于 `B ∪ P1 ∪ P2` 的为 0),`643 + 0 + 1 = 644` 自洽。
- 第二次:`P1` 相对 `B`(W218)净增 5(W216 三条、W217 一条、W219 净 0、W223 一条);
  `P2` 相对 `B` **净增 2**——`pipeline · nextForEp:整集全是过期终稿时按钮照旧挂着,文案与实收的 0 镜对得上(点完还给一句为什么没跑)`
  与 `contract · 断点条不藏按钮:工作区「下一步」无条件渲染 Pipeline 给的文案,带 run 的动作照旧派到 nx.run`。
  合完 tip 零缺失、零多出,`639 + 5 + 2 = 646` 自洽。
- **W221 的 pipeline / contract 两条加测与 W223 的 domain 那一条,三条都在 tip 上现取到**,一条不缺。

CLI 冒烟**单独整跑**(它非并行安全,不与别的套件同跑),两条失败与 `master` 同名同表现
(在 `origin/master` 的独立 worktree 上现跑对照,那边是 51/53,同样红这两条):

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
==== CLI 冒烟:107/109 通过 ====
```

`node --check` 解过本槽解出来的 `.js`:`js/domain.js`、`tests/unit.js`,全过。

## 五、W221 那一格钉住的到底是什么

W221 产品零 diff,交付的是把「整集全是过期终稿」那一档的现况钉成用例。合完在 tip 上现取那条 `pipeline`
用例的四项读数,与交接点名的四件逐个对上:

| 点名 | 用例里现取的那一行 |
|---|---|
| 按钮仍显示 | `st.action.key` / `nx.key` 都是 `regen-stale`,`typeof nx.run === 'function'` |
| 文案说明 | `nx.txt` 含「一镜也重跑不到」与「解锁终稿」,且**不含**「可重跑」与「(3)」——不许把 3 镜过期印成"这一按能跑 3 镜" |
| dispatch all | `sb.__sentArgs` 的 `shotIds` 是 `sh0,sh1,sh2`(分堆的 `all`;收窄成 `rerun` 在这一档是空数组,而空数组在子集位上等于整集重跑) |
| 引擎 0 | `sb.__genShots` 为空、`sb.__charges` 为空、`r.result.total === 0`、`counts.stale` 仍 3、三镜定稿产物一个字节没被覆盖 |

另一条 `contract` 在源级钉住工作区「下一步」按钮无条件渲染、click 照旧派 `nx.run`——
即"读到按钮还在就把它藏掉"这条最省事的改法,加测后当场红。

## 六、本槽没做的三件事,与为什么

1. **W224 成功回执 note 没合**——它从 W220 出、仍在飞(`origin` 上还没有这条支),任务口径明令跳过。本槽一条没碰,
   `digest` / `commands` 一个字未动。
2. **没有开第三条功能支,`master` 没合**,也没有 cherry-pick 任何已在基线里的东西。
3. **W223 交接留下的那条选人面残留没有代修**——见 §七。

## 七、交接:收掉一格,留下一格

### 7.1 已收:W222 残留①(镜头侧 `gone` 为负)

W222 的记账件 §八 把「镜头侧 `Domain.emptyBatchNote` 的 `gone` 仍是减法、同 id 时为负、
另一形状还会把真不在本集那镜抵消掉」如实留成残留,并写明那一槽有意不代修。
**本槽合进 W223 之后这一格已收**:四堆一律改数点名 id,§二那张 live 表逐格印出来了——
负数没了、真幽灵不再被抵消、口径不一时落安全阀而不硬派。W222 那份记账件的 §八 已加一行「已收(W225)」的标注,
原文照留(它记的是那一槽当时的实况),现取实况以本文为准。

至此镜头侧与主体侧两句 `gone` 同形:一侧是 `hits.filter(h => !h.length).length`、另一侧是
`ids.filter(id => !subs.some(s => s.id === id)).length`,两句都是「点名的 id 里找不到的个数」,
只是镜头侧另有 `locked`/`fresh` 两堆要一起按 id 数,故写法不是逐字照抄那一份 `filter`。

### 7.2 未收(本槽有意不代修):W223 残留——同 id 两镜时点名一次真起两次引擎、两笔钱

W223 的记账件自己留的那一格:分镜表里同 id 存着两镜时,**点名该 id 一次,选人那一侧会把两条镜都收进 `todo`**,
引擎真起两次、钱真扣两笔。它与本槽收掉的那一格是同一个数据形状的两侧——

| 侧 | 本槽状态 |
|---|---|
| **回执怎么分档**(`Domain.emptyBatchNote` 四堆) | **已收**:一律按点名 id 数 |
| **选人跑几镜**(命令层与 CLI 的 `todo` 筛法) | **未收**:同 id 两镜时一次点名跑两镜、扣两笔 |

**本槽有意不代修**:交接明令这一格本槽不代修,且它落在**选人与计费**那一侧,
不是一句回执的事——要收它得同轮定「同 id 两镜到底算一镜还是两镜」、重跑口径、终稿锁与扣费四件事,
那是另一个槽的分量。两个方向的偷懒都没走:既没顺手去动选人闸(那是替别人的槽交货,且会把计费面一起带上),
也没把这条残留从记账里抹掉。

在飞的 **W224** 一条没碰。
