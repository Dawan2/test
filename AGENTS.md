# AGENTS.md — 虎鲸漫剧开发约定

## 产品方向(2026-08 起)

- 允许在用户提出时**对照竞品做参考分析**;分析结论经用户筛选后再决定是否落地,不自动跟进堆功能。
- 文档与注释中**不写功能溯源**(不出现"借鉴/对标/复刻某竞品"之类表述);功能是本平台自己的,描述功能本身即可。
- 发展重点:**内部各模块的贯通**——剧本→主体→分集→分镜→生成→审片→成片主线上的数据双向流动、状态一致、断点闭环,优先于堆新功能。

## 技术栈(强约束)

- 前端纯 HTML+CSS+原生 JS,**无构建、无 npm、无 CDN**;第三方库本地化到 `js/vendor/`。
- 后端零依赖 Node.js(仅 http/fs/path/crypto),`node server.js` 启动,默认 8000 端口。
- 新功能先看 README 架构与现有模块复用点;UI 组件沿用现有 `U.openModal`/`Tasks.run`/`U.toast`/`model-opt` 等模式。

## 改动纪律

- 最小改动,匹配周边代码风格;注释保持与代码行为一致。
- 计费功能一律走 `Tasks.run`(登记→扣费→执行→失败退费);上传走 `U.readAndUpload`。
- 真实/离线行为约定:登录后端时生图/生视频走真实上游,失败如实报错并退费,不用占位冒充;仅离线回退 PH 占位模拟。
- 改完跑 `node --check` 验证改动文件;README 的功能描述与 API 表保持同步。
- 全功能回归脚本 `node tests/e2e.js`(无头 Chrome + CDP,零依赖,自带临时服务与测试数据清理):**仅在用户明确要求时运行**,不要每轮自跑。
