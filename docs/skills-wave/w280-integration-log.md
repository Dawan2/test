# W280 集成日志:W279 `gen-episode` 写回换 `nthShot` 并进 W278 尖(集成线上第十条「快进形」)

我方尖:`origin/cursor/w278-integration-b345` **现取** `6e00bf9`(先 `fetch` 再 `rev-parse`)。
对侧:`origin/cursor/w279-nthshot-1daa` **现取** `b3c3eb5`。
W279 自称从 w278 尖 `6e00bf9` 起支、HEAD 自称 `b3c3eb5`,两处**以 `rev-parse` 为准**核过,与自称逐字节相同。
新支 `cursor/w280-integration-9c17` 从 w278 现取 HEAD 拉出,`checkout -b` 后立刻 `rev-parse`,与 `6e00bf9` 相同。

**结论先写:一次 `--no-ff`,零冲突。合完 live 跑出 `unit` 696 / `commands` 59 / `contract` 153 /
`domain` 44 / `skills` 104 / `issues` 22 / `integration` 152 / `cli.smoke` 115-117,
`gaps()` 20 键一个没剥,W279 八条残留一条没代修。产品语义现取核过:
`gen-episode` 每轮回最新树按 `nthShot(epLive, s.id, nthOf.get(s) || 0)` 取本轮那一行,
序数在快照全表 `ep.shots` 上数(不在 `todo` 上数),`cli.js` 的 `findShot` 由 17 降到 **15**、`nthShot` 由 6 升到 **8**。**

**本槽最要紧的一格是「两个结构性空集叠在一起」。** 快进形本就让树面恒真(`tree(M)` 与 `tree(P2)`
逐字节相同、`P1→M` 与 `B→P2` 的 numstat 逐行相同),这是第十次;而**这一槽连数数那一面也整格空了**——
对侧把那条判据**原地重写**而没有增减条数,于是 `unit` / `commands` / `contract` / `domain` / `skills` /
`issues` / `integration` / `cli.smoke` **八格现取全部 P1 == M**(696/59/153/44/104/22/152/115-117,一格没动)。
往年集成日志拿来收口的那句「`695+1=696` 自洽」在本槽退化成 `696+0=696`,而**「对侧一个字都没改」同样满足它**,
即这句对账此刻一格结论都不承载。八格里唯一还说得出话的是**名集多重集**:`+1 −1`(§二),
它是数数那一面本槽仅存的信号。**取证因此全部压在变异与反事实上**(§四、§五)。

顺着这条往下量出的第二格:**本槽合进来的那道产品保证,整张网只有数数那两层,反事实实测可被三处齐改静默删掉**
——只删用例红 2(README 对账 + 三套件下限),把 README 与 `FLOOR` 一并改小之后**红 0**(§五 CF2/CF3),
零源级判据、无第二层行为面。W278 §残留「`landed` 全族零源级判据」在本槽从 `landed` 扩到了取行路径本身,
如实登记(§六 残留 6)。

## 一、拓扑(现取,不抄自称)

| 位 | 现取 SHA |
|---|---|
| `B` 合并基(`merge-base P1 P2`) | `6e00bf92d6ba93c16ff6b03da76ae578a84a6d7c` |
| `P1` 我方尖(w278) | `6e00bf92d6ba93c16ff6b03da76ae578a84a6d7c` |
| `P2` 对侧尖(w279) | `b3c3eb50c9f8f5a02926d534b1a7b51f345a9ed0` |
| `M` merge commit | `5c441f8fa3cdba165d48622172137c363999e5d2` |

`git merge-base --is-ancestor P1 P2` 成立,`B` 与 `P1` **提交级相等**,即典型「快进形」——
本可 `--ff` 掉,仍按纪律走 `--no-ff` 留住合点。`P1..P2` 两条提交:

| 现取 SHA | 是什么 |
|---|---|
| `7756ee408f3c7e63ce58690b63abf0fda536f821` | 产品:`CMD['gen-episode']` 写回由 `findShot` 换成 `nthShot`,序数在全表上数;`tests/unit.js` 那条原地重写成六档 |
| `b3c3eb50c9f8f5a02926d534b1a7b51f345a9ed0` | 记账件 + 索引一行,记账件下限抬到 live 294 |

「自称尖是不是链尾」这一格现取核过:`b3c3eb5` 就是链尾。

树面:`tree(P1)` `5d19719`、`tree(P2)` = `tree(M)` = `0e7448e`(逐字节相同);
`P1→M` 与 `B→P2` 的 `--numstat` 逐行相同(五个文件:`README.md` 2/2、`cli.js` 9/6、
`docs/skills-wave/README.md` 2/1、`docs/skills-wave/w279-gen-episode-nth-row.md` 145/0、`tests/unit.js` 68/28)。
**这两格在快进形下是恒真的**,写在这儿只是留档,不当证据用。

## 二、并集面(README / GUARD / roster / 索引)与 live 数字

快进形下 `P2` 的文档本就含着 `P1` 的全部内容,故并集不靠手工拼:
`docs/skills-wave/README.md` 索引表里 w278 那行与 w279 那行**同时在册**,现取核过。
数字一律 live,**两侧自称的 696 都没盲抄,八格逐个现跑**:

| 面 | `P1` 基线现跑 | `M` 合完现跑 | 差 |
|---|---|---|---|
| `unit` | 696 | **696** | 0 |
| `commands` | 59 | **59** | 0 |
| `contract` | 153 | **153** | 0 |
| `domain` | 44 | **44** | 0 |
| `skills` | 104 | **104** | 0 |
| `issues` | 22 | **22** | 0 |
| `integration` | 152 | **152** | 0 |
| `cli.smoke` | 115/117 | **115/117** | 0 |

`cli.smoke` **单独整跑**(`env -u HUJING_SERVER -u MV_ACCESS_KEY -u MV_SECRET_KEY -u MV_API_KEY -u MV_REGION`),
两条失败:`未登录 whoami → exit 3 | exit=1` 与 `llm --json mock 链路 | undefined`;
同 env 下 `master` `9adcf0f` 独立 worktree 现跑 **51/53**、失败两条**同名同表现**,按纪律允许。

**八格全零差,故本槽另取名集多重集**(不 unique-sort,`^  { name: '…'` 逐条取):
`P1` 696 条、`M` 696 条,多重集 diff **`+1 −1`**,进出的是同一条被原地重写的判据——

- `−` `CLI gen-episode 回执同样分开报 ok 与 landed:同 id 多行全写在首行时,扣了几笔与到手几行不再混作一个数`
- `+` `CLI gen-episode 同 id 多行:每一轮的片落到本轮那一行,回执的 ok 与 landed 各说一件事`

改名本身就是对侧 §二 那张对照表的落点:判据的口径从「补一个诚实字段」搬成「行为已经对了」。
**零吃测**(没有一条判据被换名字掩盖着删掉)。

护栏面(合完现取,四者对齐):`GUARD_TOPICS` **19** / `GUARD_TOPICS_CLOSED` **0** /
`TOPIC_FLOOR` **19** / 花名册 **19** 行;`Skills.gaps()` **20** 键逐个在册,一个没剥;`SLACK` **3** 未动。
棘轮五格差额合完 **0/0/0/0/0**(单元 696、集成 152、冒烟 117、记账件 295、护栏主题 19),
`SLACK` 一格没用掉——记账件那格由本文从 294 抬到 **295**(三处同轮校齐:目录实况、
`docs/skills-wave/README.md` 明写份数、`tests/unit.js` 的 `FLOOR` 字面)。

## 三、产品语义现取核验(明令未改一字)

`js/` 相对 `P1` 与相对 `P2` **双向零 diff**,`cli.js` 与 `P2` 逐字节相同——产品面只动 `cli.js` 一个文件。

| 核的是什么 | 现取实况 |
|---|---|
| 写回走不走 `nthShot` | 走。`cli.js:949` `const sLive = nthShot(epLive, s.id, nthOf.get(s) || 0);` |
| 序数在哪张表上数 | 全表。`cli.js:937` `(ep.shots || []).forEach(…)`,**不是** `todo` |
| `findShot` 次数有没有下降 | `cli.js` **17 → 15**;`nthShot` **6 → 8**;`nthSubject` **3** 未动 |
| 是不是复用而非另抄一份对位 | 复用。`gen-episode`(933-937)与 `exec episode.generateVideos`(1156-1160)、主体侧(1436-1440)三处**逐句同形** |
| `nthShot` 两处兜底 | 一字未动:`rows.length ? (rows[nth] \|\| rows[0]) : findShot(ep, sid)` |
| `landed` 算法 | 未动:座位键带 id、`result.ok++` 之后才 `seats.add(seat)`、`seats.size` 现算、空跑 `landed: 0` |
| 岔开那一句 | 仍单源 `Domain.landedNote(result.ok, result.landed, '行')`,`js/domain.js` 零 diff |
| `ok` 口径与计费 | 零 diff,共位覆盖仍不退费 |

## 四、变异:六手 + 一手跨支量尺(全部本槽现跑)

| 手 | 做的什么 | 现取红数 | 报错句(原样抄出) |
|---|---|---|---|
| M1 | 写回退回 `findShot` | **红 1** | `扣了四笔就得有四行出片,不能三轮全写在首行身上…实际:首行:/uploads/gen/v4.mp4 \| 不重复的那一镜:/uploads/gen/v2.mp4 \| 第二行:无片 \| 第三行:无片:期望 4,实际 2` |
| M2 | 序数改在 `todo` 上数 | **红 1** | `已出片的首行这一趟一个字不许被改写…期望 "/uploads/gen/old.mp4",实际 "/uploads/gen/v2.mp4"` |
| M3 | 座位键摘掉 id | **红 1** | `landed 得等于真出片的行数(实测 4 行):期望 4,实际 3` |
| M4 | 摘掉 `nthShot` 两处兜底 | **红 3** | 见下 |
| M5 | `landed` 抄 `ok` | **红 1** | `landed 得等于真出片的行数(实测 3 行):期望 3,实际 4` |
| M6 | 座位提前到开跑时登记 | **红 0** | 全绿 696/696 |
| M9 | 退掉 `js/sb-views.js:1243` 的 `Domain.shotNo`(跨支反向) | **红 2** | 与 W271 §五 M1 起历次逐字相同 |

**M3 值一记:点名对象与真开火点仍是错位的。** ⑤档的注释自称钉住「座位键带 id,只按行内序数记会全记成第 0 行、
`landed` 缩成 1」,而真做这一手时**①档先抛**(四行里 `dup#0/#1/#2` 与 `solo#0` 撞成三个键),⑤档一行没跑到。
这与 W278 §四 M2/M5 量到的是**同一类**(不是 W275 §五 M3 那种「坏几半只报一半」),连续第二槽出现。

**M4 的红数本槽现取,没照抄旧账。** 同一手在起手支 `6e00bf9` 的干净 worktree 上现跑**红 2**
(`exec episode.generateVideos` 那档 + `landed` 那条),在 `M` 上现跑**红 3**——`gen-episode` 也被压了上去,
第三条报错句是 `序数越界得退回首行(原行为),不许让 undefined.image 把这一趟炸成失败:[{"shotId":"dup","error":"Cannot read properties of undefined (reading 'id')"}]`。
**往后拿这一手当「两支有没有接触」的量尺时,基准数按 3 读**;照抄 W255/W257/W259 里的 2 会读成假的分家,
照抄 W279 自称的 3 也要现取——本文这次是在合完的树上量的,与对侧自称一致。

**M6 复量确认 W278 §残留 9 本槽一格没收。** 把 `seats.add(seat)` 提到取行之后、生成之前,三套件**各红 0**、
整份 `unit` 仍 696/696。成因是结构性的:六档里跑得到失败轮的只有④档,而那两轮挂在座位赋值**之前**
(`nthShot` 委托回 `findShot` 当场抛「镜头不存在」),于是「成功才登记」这半边一次都没被真开火。
收法照旧是造一档「取行成功、`genShotVideo` 才失败」的夹具。

**M9 跨支反向连续第七次读成「分家」。** 报错句与 W271 §五 M1 / W272 §三 / W273 §五 M4 / W275 §五 M9 /
W276 §四 M9 / W278 §四 M9 逐字相同,而本槽那条判据一条不红。读数依据仍是**对侧改动落在哪个文件**
(本槽只动 `cli.js`,`js/sb-views.js` 零 diff),不是红几条。

## 五、反事实:棘轮缓冲此刻具体等于什么,以及这道保证能不能被静默删掉

两侧都把 `['单元测试', 696]` 写在 live 上(差额 0),**没人吃 `SLACK` 那格便宜**。缓冲此刻具体等于几,现取扫了一遍:

| 抄回的下限字面 | `live − 下限` | 现取红数 |
|---|---|---|
| 695 | 1 | **0** |
| 693 | 3(恰等于 `SLACK`) | **0** |
| 692 | 4(越界一格) | **红 1**,点名 `单元测试:下限 692 落后实测 696 共 4 格(上限 3 格)` |

即 W134 那句「逮不逮得住取决于 `live − 下限字面` 有没有越过缓冲」在本槽的读数是:
**能一路抄回到 693 而一条不红,692 才是第一格红**,且报错句写全了格名、落后格数与上限。

**顺着往下走一步,量的是这道保证本身有几层网**:

| 反事实 | 做的什么 | 现取红数 |
|---|---|---|
| CF3 | **只**删掉那条六档判据,文档与 `FLOOR` 一个字不改 | **红 2**:`README 数字对账`(实测 695,文档 696)+ `三套件用例数只增不减` |
| CF2 | 删掉那条判据 + `README.md` 按 live 改成 695 + `FLOOR` 改成 695(三处齐改) | **红 0**,`695/695 PASS` |

**CF2 全绿。** 即本槽合进来的「`gen-episode` 每一轮的片落到本轮那一行」这道产品保证,
整张网只有数数那两层(README 三方对账、三套件下限),两层被同一次三处齐改一并解除;
`commands` / `contract` / `skills` / `domain` 全族**一条源级判据都没有**,也没有第二层行为面接着。
这与 W278 §四 M8 量到的「`landed` 全族零源级判据」是同一件事在**取行路径**上的延伸——
W278 那次 CF2 红 1 只因被删的两条里恰有一条落在 `contract` 套件里,本槽这条落在 `commands`,故一层不响。

## 六、保留面机检(合完树上现取,与起手支逐项对照)

| 项 | 实况 |
|---|---|
| `Skills.gaps()` | **20** 键,一个没剥 |
| `TOPIC_FLOOR` / `SLACK` | 19 / 3,未动 |
| `GUARD_TOPICS` / `CLOSED` / 花名册 | 19 / 0 / 19,四者对齐 |
| 棘轮五格差额 | **0/0/0/0/0**(记账件那格由本文 294 → 295 三处校齐) |
| `cli.js` 的 `findShot` / `nthShot` / `nthSubject` | **15** / **8** / **3**(起手支 17 / 6 / 3) |
| `nthShot` 两处兜底 | 一字未动(越界退回首行 + 委托回 `findShot`) |
| `Domain.landedNote` / `dupRowsNote` / `dupSubjectRowsNote` / `emptyBatchNote` | 四句判词零 diff(`js/domain.js` 整份零 diff) |
| `ok` 计数与计费面 | 零 diff(共位覆盖仍不退费) |
| `state-put` 写入闸 | 仍只有 `need(f.file, …)` 与 `need(f.force, …)`,**没设闸** |
| `shots-dedupe` | 未动(主体侧照旧没有同类命令) |
| `js/` 下全部文件 | 相对 `P1` 与相对 `P2` **双向逐字节零 diff** |
| 残留锚点 | `js/batchops.js` 的 `.order + 1` **17** 行、`js/produce.js` **0** 处、`js/issues.js` **0** 处 |
| 本文目录内相对链接 | **0** 处 |

关键词三棵树逐格点数(全树含文档,`M` 与 `P2` 逐格相等且无一格掉到 `P1` 之下):

| 关键词 | `P1` | `P2` | `M` |
|---|---|---|---|
| `landed` | 262 | 291 | **291** |
| `landedNote` | 49 | 54 | **54** |
| `gen-episode` | 66 | 82 | **82** |
| `findShot` | 87 | 93 | **93** |
| `nthShot` | 110 | 125 | **125** |
| `nthSubject` | 67 | 71 | **71** |
| `nthOf` | 32 | 38 | **38** |
| `seats` | 34 | 36 | **36** |
| `rowIndexOf` | 80 | 80 | **80** |
| `shotNo` | 204 | 204 | **204** |
| `reviseTargets` | 202 | 202 | **202** |
| `staleShotSplit` | 85 | 85 | **85** |

`findShot` 全树净增 6 而 `cli.js` 内净减 2,两个数都对:降的是**调用点**,涨的是对侧记账件里
反复讲「此前取首行」时写下的那些字面。往后引用这一格要说清是哪一层的口径。

## 七、残留

**本槽零代修**——W279 八条残留按明令一条没动,原话逐条保留:

1. **`gen-episode` 的「座位只在本轮真成功后才登记」仍无判据**(W278 §残留 9,本槽 §四 M6 复量:提前登记**红 0**)。收法是造一档「取行成功、`genShotVideo` 才失败」的夹具,两条 `exec` 批量那边同形的洞也一并在那里收。
2. **单发那七条出口没有 `landed`**(原样传 W277 §残留 1)。本槽只改取行,一条没碰。
3. **引擎事件 `shots.batchDone` 的 `ok` 同样读不出落库实况**(原样传 W277 §残留 3)。
4. **共位那一轮的钱没有退路**(原样传 W254/W277)。换成按序数取行之后,共位只在「跑到一半别处改了表」时才发生,频次比换之前低得多,但「共位算不算交付失败」这个产品问题一个字没定。
5. **`result.shots` 按 id 记,同 id 只剩一个键**(原样传 W277 §残留 5)。它是 `gen-episode` 对外的回执字段,改键是破坏性改动;判据仍正面钉住键数并写明「落库数只看 `landed`」。
6. **`landed` 这一族仍是零源级判据**(原样传 W278 §残留)。本槽没加这一层,并现取量到它**已经不止 `landed`**:取行路径本身同样零源级,CF2 三处齐改即可静默删掉整道保证(§五)。
7. **`state-put` 不设闸**;**主体侧没有 `shots-dedupe` 同类命令**;**`loadCli()` 取出来的 `CMD` 表仍只有 `gen-episode` 一条被测到**(三条一并原样传 W277)。
8. **摘 `nthShot` 兜底这一手的红数从 2 变成 3**(§四 M4)。这不是漏,是对侧扩出来的判据面,但它同时意味着:往后拿这一手当「两支有没有接触」的量尺时,基准数得按 3 读,照抄 W255/W257/W259 那三份里的 2 会读成假的分家。

本文另新记两格:

9. **数数那一面本槽整格空了**(§二)。八个套件计数 `P1 == M` 逐格相同,历次集成日志用来收口的「旧数 + 新增 = live」那句对账在原地重写型的槽上退化成恒真——**「对侧一个字都没改」同样满足它**。往后遇到「对侧自称条数未动」的槽,别把这句对账当证据,改取名集多重集(本槽 `+1 −1`)与变异面。
10. **③档那句写死的 `landed === 3` 六手变异一次都没开火**。③档并排放着两句:先是自校准的 `landed === filmed(gone1.rows).length`,紧跟着写死的 `landed === 3`。本槽六手逐一记了开火位——M1 与 M3 落①档、M2 落②档、M4 与 M5 落③档但都停在**前一句自校准**上、M6 全绿,写死那句因为排在自校准之后,**任何让 `landed` 走样的改动都先被前一句拦下**。它此刻兜的只剩「`filmed` 与 `landed` 一起变成 4」这种前一句拦不住的形态,而六手里没有一手是这个形态,故它的覆盖面本槽**一次都没被量到**。要么给它配一手只坏并发那一趟、且让两个数一起走样的变异,要么承认它是前一句的影子。
