# W166 · 四条槽并入集成线的收敛记录(集成分支)

> 集成分支:`cursor/w166-integration-9a1c`,基线 `cursor/w161-integration-4e7d @ c5de21e`。
> 本文只记**收敛过程**:基线怎么定的、合了哪四条、冲突面长什么样、合并后的实测数字、没删测没摘 gaps 的取证。
> 被合入四槽自己的内容分别在 `w158-rename-vs-delete.md`、`w160-cycle10-audit.md`、`w162-no-placeholder-zip.md`、`w164-low-review-shotids.md`,本文不代述、不复写、不改它们正文里的任何时点读数。
> 四次合并都 `--no-ff`、各一个合并提交、可 revert;全程只解冲突与收敛数字,**不重做已落地的功能**。

## 1. 结果一句话

`--no-ff` 合入四条,按序、**各取被合入支的实际 HEAD**:

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w158-rename-vs-delete-5b53@90707ea` | `8d80072` | 护栏主题清单 `GUARD_TOPICS`:给「这道护栏还在不在」一个**不随用例名走**的落点——逐条登记那道护栏读的实况取数口(注册表键 / 源码标识符 / 文件路径片段),按锚点在全部套件的用例源码上找落点,用例名一个字都不参与判定;`blankNonCode` 加 `keepText` 位(只抹注释、字面量留着) | 零产品文件进 diff | +2 `unit`(都是 `contract`) |
| `w160-cycle10-audit-b3ee@5fae3ee` | `b79eb02` | 周期 10 独立核验件(783 行)。**自称"只写文档 + FLOOR 抬一格"现取属实**:`js/` 与四个 Node 端零 diff,`tests/` 下唯一改动是 `FLOOR` 那个字面 | 零产品文件进 diff | +0 |
| `w162-no-placeholder-zip-00c4@a632863` | `194dbe0` | 占位空壳字面不许回来,**源级 + 运行期各一条**:源级扫 `js/release.js` 全文件可执行行(零 `ZipUtil.download`、零 `PLACEHOLDER`、零「请重新打包」空壳文案)并把 `ZipUtil.download` 的调用方名单钉在素材/剪映导出一家;运行期把桩打在 `ZipUtil.download` **函数对象**上,成功/落地失败/按钮三个入口一次都不许叫到它,且逐份落地文件按 zip 解出条目对着打包清单点 | 零产品文件进 diff | +2 `unit`(`release`) |
| `w164-low-review-shotids-4d33@1dc0cde` | `de9866e` | `low-review` 条目带上派生 `shotIds` 子集(`Domain.reviseShotIds(ep)` 单源,调用侧不另抄一份投影);**空重抽面不出这个字段**(子集位上的空数组等于"整集",会把几镜的活儿放大成整集重跑);**仍不挂 `cmd`**——重抽前要先按审片意见改提示词,子集是给人挑的清单 | `js/issues.js` +13 −2 | +2 `unit`(1 `issues`、1 `contract`) |

合并后回归 **`unit 534/534`**、**`integration 141/141`**、**`cli.smoke 105/107`**(2 项失败与基线同名同表现,见 5.2),`contract` 子套件 **119/119**。

治理面:`gaps()` **20 键**(`G-03` 仍是两条值、`G-11` 仍在案)、提示词注册表 **41**、能力短名单 **30**、`Skills.CHECKS` **17**、`preflightStages()` **七面**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 八个数一个没动。
本槽**注册表五个文件相对基线零 diff**(`git diff --stat c5de21e HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js` 空输出),故这八个数是结构性成立,与 W161 / W157 同形。

**本槽最值得记下的四件事:**

1. **一条新护栏在落地当轮就被集成线的实况判红,而它红得对——处置是按它自己写明的规矩把 `hosts` 抬到实况,不是放宽它。**
   W158 的 `GUARD_TOPICS` 里 `plans-gate-read` 那条(锚点 `Domain.gateBlockers(p)` + `plans.js`)在它自己的分支上落点恰好 1 处,`hosts` 因此取缺省 1。合进集成线的那一刻当场红:实测 **2 处**——除它本来钉的那条(`前置门槛第三份拷贝已消`)之外,基线上 W153 那条逐码扇出用例(`门槛码扇出:Domain.gateCodes() 逐码在问题中心与计划层都有投影`)读的是同一份实况。
   **这不是误报,是这条判据设计时就预告的两种失准之一**(它的报错句原文:「锚点太泛会把别的用例也算成落点…一道护栏有意拆成两条用例时把 `hosts` 抬到实况」)。故处置是 `hosts: 2` 并在 `why` 里写明两处各钉什么。
   值得记的是**方向**:`hosts` 从 1 抬到 2 是**收严**不是放宽——判据要的是"落点数恰等于登记",抬到 2 之后"从 2 掉回 1"也会红,而缺省 1 的写法在集成线上压根跑不起来。**一条护栏被合进一个比它自己的叉点更丰富的树时,"锚点太泛"与"这道护栏真有两个承载"在报错句里长得一模一样,分开靠人读那两条用例在判什么**(3.1)。

2. **源级判据与运行期判据的分界这次被实测量了出来:两条源级判据同时向零,只有运行期那条报得出来。**
   任务口径点名"W162 的运行期桩也要留",而基线已从 W156 翻面得到「`downloadReleaseZip` 段内不许出现 `ZipUtil.download(`」的一半。三条判据合并后并存(3.3),看上去像叠床架屋。
   变异 M3′ 把它们分开了:在成功路径插一行 `const Z = ZipUtil; Z.download(name, [{ name: 'meta.json', data: '' }]);`——**别名写法源级扫不出来**(现取 `js/release.js` 可执行行里 `ZipUtil.download` 命中 **0 处**),且不带 `PLACEHOLDER` 字面故 W162 源级那条的第二格也够不着。实测**红 2**:W152 那条行为面(用户到手两个文件)与 W162 运行期那条(「成功路径不许叫到 `ZipUtil.download`」),**两条源级判据全绿**。
   反过来 M2(把落地失败那一路退回兜底空壳)**红 5**,源级两条与运行期一条都在里头。**即三条判据各守各的一段,没有一条是另一条的子集**——这是把"并集不要削弱"从口径落成实测的那一步。

3. **四条槽里有两对自称数逐字相同,而两对都不是答案。**
   `w158` 与 `w160` 自称的记账件份数都是 **167**;`w162` 与 `w164` 自称的份数都是 **171**、自称的单元用例数都是 **526**。
   成因是结构性的:前一对同叉在 `31d7aeb`(W151 集成记账件)、各加一份记账件;后一对同叉在 `8507194`(W157 集成记账件)、各加一份记账件加两条用例。**同一个静默窗口这一槽出现了两次**,与 W161 那次(`w155`/`w156`/(W157 三支)全是 163)同形。
   两对都没能静默,只因我方那一格已被前一次合并抬走——本槽四次逐次落到 175 / 176 / 177 / 178,四次都进冲突块(4.1)。

4. **这一槽的按名比对两轮全为 0 且第三种比法也全为 0——W161 残留 3 那个盲区本槽没有实测机会。**
   W161 记的是"翻面那条从来没有以原名进过集成线,两轮按名比对都不报,只有第三种比法(被合入支名集 ↔ 合并后)报得出来,而它是人工的"。
   本槽把第三种比法照样人工做了一遍,**四支各自的用例名在合并后 tip 上一条不缺**(5.3):没有翻面、没有改名、没有接位。故这个盲区**本槽没被触发,也没被证明收掉**——它照旧只靠人工记得比第三种,登记在残留里(6.3)。

## 2. 基线与这四个槽

### 2.1 基线

任务口径给定 `origin/cursor/w161-integration-4e7d @ c5de21e`。现取核实,并逐支核对任务给的参考 sha:

```
$ git rev-parse origin/cursor/w161-integration-4e7d
c5de21ea752db10462a2c0b0d10ed8cc3f38a296
$ git rev-parse --short origin/cursor/w158-rename-vs-delete-5b53   → 90707ea
$ git rev-parse --short origin/cursor/w160-cycle10-audit-b3ee      → 5fae3ee
$ git rev-parse --short origin/cursor/w162-no-placeholder-zip-00c4 → a632863
$ git rev-parse --short origin/cursor/w164-low-review-shotids-4d33 → 1dc0cde
```

五个都与任务给的参考值一致(四支都取的现取 HEAD)。

基线上的三套件数字(开工时现跑,不引用 `w161-integration-log.md` 里的数):

| 套件 | 基线 `c5de21e` |
|---|---|
| `node tests/unit.js` | **528 / 528**(`contract` **116 / 116**) |
| `node tests/integration.js` | **141 / 141** |
| `node tests/cli.smoke.js` | **105 / 107** |

与任务给的参考数字逐个相符,`cli.smoke` 那 2 项失败也与任务口径说的一致(见 5.2)。

### 2.2 四支的叉点:四条都是真三方

| 支 | 与基线的 `merge-base` | 那是什么 | 基线在其之上还有 | 该支自己有 |
|---|---|---|---|---|
| `w158` | `31d7aeb` | W151 集成记账件 | **19 个提交** | 2 个 |
| `w160` | `31d7aeb` | 同上 | **19 个提交** | 1 个 |
| `w162` | `8507194` | W157 集成记账件 | **10 个提交** | 2 个 |
| `w164` | `8507194` | 同上 | **10 个提交** | 3 个 |

四次都 `merge-base != HEAD`,**都是真三方合并**(与 W117 那槽"第一次合并可快进故不是三方合并"那个反例不同,本槽一次都没出现)。

基线在这两个叉点之上的提交里含 W153 / W155 / W156(翻面版)/ W159 等槽,故:

- W153 的门槛码扇出契约已在基线里,而 `w158` 叉在它**落地之前**——`GUARD_TOPICS` 的 `plans-gate-read` 那条因此在集成线上多出一个落点(1.1、3.1);
- W159 的 `mainOwnFlow` 已在基线的 `tests/unit.js` 里,插入锚点与 `w158` 的 `GUARD_TOPICS` **是同一个**——纯插入 vs 纯插入的冲突块(3.1);
- W156 翻面那条「`downloadReleaseZip` 段内不许出现 `ZipUtil.download(`」已在基线里,而 `w162` 带来的是**更宽一档的源级**加**一条运行期**(3.3);
- W155 的 `EPB` 登记表与 `Issues.epBlockers()` 已在基线的 `js/issues.js` 里,而 `w164` 改的是同一文件的另一段——任务口径预告的"两侧都留"(3.4)。

### 2.3 不合的

任务口径明确不合在飞的 **W163**(即使远端已有 `cursor/w163-review-min-literal-1c3b`)与 **W165**,本槽一条都没碰,也未做叉点核实。
除此之外远端未合入本线的 `cursor/*` 支,按任务口径不在本槽范围内。

## 3. 冲突面

四次合并共 **11 个文件次、23 个冲突块**,分布:

| 合并 | `README.md` | `docs/skills-wave/README.md` | `tests/unit.js` | 产品文件 | 小计 |
|---|---|---|---|---|---|
| `8d80072`(W158) | 2 | 2 | 3 | — | 7 |
| `b79eb02`(W160) | — | 2 | 1 | — | 3 |
| `194dbe0`(W162) | 1 | 2 | 3 | — | 6 |
| `de9866e`(W164) | 3 | 2 | 2 | **0**(`js/issues.js` 零冲突块) | 7 |

**产品文件冲突块 0 个** —— W161 那槽是"连着四槽零产品冲突块之后第一次有",本槽又回到零,而这一次**并不是因为没有产品文件同时被两侧改过**:`js/issues.js` 两侧都改过且都落地了,只是改的行不同(3.4)。

### 3.1 第一次合并(W158 `90707ea` → `8d80072`,7 块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| **`tests/unit.js`** | `reportBlockHeads` 与 `const contractTests` 之间 | **两侧都是纯插入且锚点逐字相同**:我方是 W159 的 `mainOwnFlow`,对侧是 W158 的 `GUARD_TOPICS` + `guardHostsOf` | **并集**,我方在前 |
| `tests/unit.js` | 三套件下限 | 528/141/107 / 522/141/107 | 单元现取 live **530**;集成与冒烟两侧同值,取我方 |
| `tests/unit.js` | 记账件 `FLOOR` | 174 / 167 | 现取 live **175** |
| `README.md` | 「文档数字对账契约」整句 | **两侧都是纯插入**:我方带 W159 那段,对侧在「…把其中一条吃掉),」之后另插一整段讲 `GUARD_TOPICS` | **并集**,按对侧锚点插回;`contract` 条数换 live **118** |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带全部数段 / 对侧**只改了那个数**(520 → 522) | 取我方整句,数字换 live **530** |
| `docs/skills-wave/README.md` | 明写份数 | 174 / 167 | 现取 live **175** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方 `w152`…`w161` 八行 / 对侧 `w158` 一行 | **并集**,九行按波次号排(`w158` 归到 `w157` 与 `w159` 之间) |

**那个纯插入 vs 纯插入的块与 W161 的 `js/domain.js` 同形**,只是这次落在测试文件上:两侧都是零删除,插入点是同一行之后,块尾那个 `}` 是两侧共用的(故并集时要给我方那半补一个块尾,机械两留会语法当场断——W75 那一课)。机检:

```
机检 1:结果相对我方的增删行 == 对侧相对叉点的增删行,逐字相同(唯一差别是上下文行) ✓
机检 2:GUARD_TOPICS 段在结果里恰一处,且逐字出自对侧 ✓
机检 3:W159 的 mainOwnFlow 整段在结果里逐字保留、node --check 过 ✓
机检 4:blankNonCode 的 keepText 位由 git 直接搬入、零冲突块(我方相对叉点在那几行零 diff),而 mainOwnFlow 调它时不传第二参、语义逐字不变 ✓
```

**`hosts` 那一格的处置(1.1)**:合上首跑 **529/530,红 1**,报的是

```
plans-gate-read:登记 1 处、实测 2 处 →
  contract · 门槛码扇出:Domain.gateCodes() 逐码在问题中心与计划层都有投影(漏投即红)
  contract · 前置门槛第三份拷贝已消:js/plans.js 的项目级待办按 gateBlockers 的码取材,不自己判剧本/主体/分集
```

逐条读那两个落点在判什么:后者是这条主题本来钉的(`js/plans.js` 不许自己判剧本/主体/分集),前者是 W153 那条逐码扇出(`Domain.gateBlockers(p)` 真回的码逐个在计划层有没有投影)。**两条读的确实是同一份实况、都不是被锚点误伤的路人**,故按判据自己写明的规矩把 `hosts` 抬到 2 并在 `why` 里写明两处各钉什么。改完 **530/530**。

### 3.2 第二次合并(W160 `5fae3ee` → `b79eb02`,3 块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `tests/unit.js` | 记账件 `FLOOR` | 175 / 167 | 现取 live **176** |
| `docs/skills-wave/README.md` | 明写份数 | 175 / 167 | 现取 live **176** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方九行 / 对侧 `w160` 一行 | **并集**,十行按波次号排 |

**核验槽自称「只写文档」现取属实**,与 W119 那次(核验件合上当刻单测红 3 条)不同的是本槽合上首跑就 **530/530 全绿**:

```
$ git diff --stat b79eb02^1 b79eb02 -- js/ server.js cli.js mcp.js billing.js
(无输出)
```

成因是 W104 立的分工(核验槽只交报告、索引行由集成槽补)这一次被对侧**自己同轮做了**——`w160` 那一个提交里同时改了索引行、明写份数与 `FLOOR` 三处,故 W105 那层三方对齐的三份在对侧 diff 里齐全,合上就成立。**「核验件合进来会红」不是规律,是分工没做全时的表现**,这一槽给出反例。

另按 W104/W119 那两次的教训现取核对:这份 783 行的报告里目录内相对链接**一条都不悬空**(判据现跑为绿),它讲判据时用的占位形状避开了那条正则。
**本文自己在这一格上踩了一次并当场被逮住**:初稿在这句话里把那个占位形状原样写了出来(只包了一对反引号),判据是跑在纯文本上的正则、**反引号不参与匹配**,落地即红 1 条并点名「本文 → 该占位(文件不存在)」。解法仍取"改占位形状"而不是"放宽正则"——这条断言是这个洞唯一的守卫。**这一面至此在本目录实测第六次,而这一次撞上它的是一份正在转述前两次教训的文档。**

### 3.3 第三次合并(W162 `a632863` → `194dbe0`,6 块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| **`tests/unit.js`** | `releaseTests` 尾部,「按钮落地失败播报」那条之后 | **两侧都是纯插入且锚点逐字相同**:我方是基线里 W156 翻面那条,对侧是 W162 的源级 + 运行期两条 | **并集**,我方在前(块尾同样两侧共用,给我方那半补一个) |
| `tests/unit.js` | 三套件下限 | 530/141/107 / 526/141/107 | 单元现取 live **532** |
| `tests/unit.js` | 记账件 `FLOOR` | 176 / 171 | 现取 live **177** |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带全部数段 / 对侧在同一句里另插一整段讲本槽两条判据 | **并集**,按对侧锚点插回;数字换 live **532** |
| `docs/skills-wave/README.md` | 明写份数 | 176 / 171 | 现取 live **177** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方十行 / 对侧 `w162` 一行 | **并集**,十一行按波次号排 |

**「并集不要削弱」这一格逐条核对过再落定。** 三条判据合并后并存,它们各钉的一段:

| | 钉什么 | 扫哪儿 |
|---|---|---|
| 基线里 W156 翻面那条 | 段内不许出现 `ZipUtil.download(` + 落地失败得抛出带原因的错误 | `downloadReleaseZip` 到 `execFix` 之间那一段源码 |
| W162 源级那条 | 可执行行零 `ZipUtil.download`、零 `PLACEHOLDER`、零「请重新打包」空壳文案;反面钉 `a.click()` 全函数只一次、落的是 `r.bytes`、失败仍以 `throw` 收尾;`ZipUtil.download` 调用方名单只此 `exporter.js` 一家 | **整个 `js/release.js` 的可执行行** + 全 `js/` 的调用方名单 |
| W162 运行期那条 | 桩打在 `ZipUtil.download` **函数对象**上,成功/落地失败/按钮三个入口一次都不许叫到;逐份落地文件按 zip 解出条目对着打包清单点(条目数 0/1 的空壳当场红) | 真跑三个入口 |

**没有一条是另一条的子集**:段界不同(段内 vs 整文件 vs 全 `js/`)、层次不同(源码字面 vs 运行期函数对象)。变异 M2 / M3′ 把这个分界实测出来(5.5),故四条一条不删、按并集全留。

### 3.4 第四次合并(W164 `1dc0cde` → `de9866e`,7 块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `tests/unit.js` | 三套件下限 | 532/141/107 / 526/141/107 | 单元现取 live **534** |
| `tests/unit.js` | 记账件 `FLOOR` | 177 / 171 | 现取 live **178** |
| `README.md` | 「文档数字对账契约」整句 | **两侧只差 `contract` 那个数**(我方 118 / 对侧 115) | 取我方整句,数字换 live **119** |
| `README.md` | 「🩺 问题中心」整段 | 两侧都是纯插入、锚点相同:对侧插的是「导航类条目也可能带 `shotIds`」那一段 | **并集**,按对侧锚点插回 |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带全部数段 / 对侧在 `issues.js` 那一节里另插一句 | **并集**,按对侧锚点插回;数字换 live **534** |
| `docs/skills-wave/README.md` | 明写份数 | 177 / 171 | 现取 live **178** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方十一行 / 对侧 `w164` 一行 | **并集**,十二行按波次号排 |

**`js/issues.js` 零冲突块,而它是两侧都改过的产品文件——结果是真融合、两半都在。**
任务口径预告「W164 与 W155 EPB 都可能碰 `issues.js`,两侧都留」。现取核实:

```
四棵树的 blob:M=65c72c7  P1=113afbf  P2=dafbd84  B=b23ae3d
→ M 与 P1/P2/B 三者都不等,真融合(不是 --ours、不是 --theirs、不是回退叉点)
```

`git` 之所以零冲突块,是因为两侧改的行不相干:W155 那半在 `EPB` / `EPB_SKIP` 登记表与逐集循环各分支的 `sev:` 取值上,W164 那半在 `low-review` 那一个分支体内(把一行 `out.push(...)` 拆成 `const low = ...` + `if (lows.length) low.shotIds = ...` + `out.push(low)`)与段头注释上。合并后逐面现取:

```
W164 那半:js/issues.js:236  if (lows.length) low.shotIds = Domain.reviseShotIds(ep);
W155 那半:EPB / EPB_SKIP 登记表在案,no-script / no-shots / shots-stale / failed-shots /
          stale-shots / unconfirmed / composed-stale 七个分支的 sev 仍逐个读 EPB[...]
```

**"零冲突块"与"两侧都落地"是两回事**,分开靠上面这两步而不是靠没报冲突;变异 M6 反向再证一次(5.5)。

### 3.5 四次都没有摘 gaps、没有删测

四条槽各自加自己的用例;既有用例**改了 0 条断言、删了 0 条、翻面 0 条、改名 0 条**。
本槽唯一改动的既有登记是 `GUARD_TOPICS` 里 `plans-gate-read` 那条的 `hosts` 由缺省 1 抬到 2(3.1)——**那是收严不是放宽**,且它是被合入支当轮带进来的登记、不是既有判据。
四次合并全程未用 `checkout --ours/--theirs`,也未用 `git checkout <old> -- .`。

## 4. 数字怎么定的

### 4.1 一律现取合并后 live,不抄任何一个父值

| 数 | 基线 | W158 侧 | W160 侧 | W162 侧 | W164 侧 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|---|---|
| 单元用例数 | 528 | **522** | **520** | **526** | **526** | **534** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 116 | **115** | **113** | **114** | **115** | **119** | 主 `README.md` 契约段 |
| `integration` | 141 | 141 | 141 | 141 | 141 | **141** | 主 `README.md` + 下限表 |
| `cli.smoke` | 107 | 107 | 107 | 107 | 107 | **107**(实跑 105/107) | 主 `README.md` + 下限表 |
| 记账件份数 | 174 | **167** | **167** | **171** | **171** | **178**(含本文后 179) | `docs/skills-wave/README.md` 明写 |
| 记账件 `FLOOR` | 174 | 167 | 167 | 171 | 171 | **178**(含本文后 179) | `tests/unit.js` |

逐次落定的读数(每次合完当场跑,不等四次合完再算):

| | 合入 W158 后 | 合入 W160 后 | 合入 W162 后 | 合入 W164 后 |
|---|---|---|---|---|
| `unit` | 530 / 530 | 530 / 530 | 532 / 532 | **534 / 534** |
| `contract` | 118 / 118 | 118 / 118 | 118 / 118 | **119 / 119** |
| `integration` | — | — | — | **141 / 141** |
| `cli.smoke` | — | — | — | **105 / 107** |
| 记账件份数 | 175 | 176 | 177 | **178** |

**八个自称数一个都不是答案**(`integration` / `cli.smoke` 两格四支都与 live 相等,成因是这两个套件本槽四支一条都没碰、且四支的叉点在这两个数上都够新——与 W151 那槽 `w143`、W161 那槽 `w159` 同形,**这两格自称对了是结构事实不是运气**)。两处值得记:

- **两对自称数逐字相同(1.3)**:`w158`/`w160` 的份数都是 167,`w162`/`w164` 的份数都是 171、单元数都是 526。前一对同叉 `31d7aeb`、后一对同叉 `8507194`,各加一份记账件。**同一个静默窗口这一槽出现两次**,而它两次都没能静默,只因我方那一格已被前一次合并抬走。
- **`w160` 那支自称的单元数 520 是四支里最低的**,而它一条用例都没加——那是它叉点上的旧值,它自己没动过这个字面。若照抄它,与 live 差 **14 格**。

### 4.2 `FLOOR` 抄父值与 README 抄父值:逐格实测

在**本文落地后的 tip** 上逐格实测,结果见 5.6。

## 5. 取证

### 5.1 合并纪律:按四棵树的文件哈希机检

对每次合并,取 `M`(合并结果)、`P1`(我方)、`P2`(对侧)、`B`(叉点)四棵树里同一文件的 blob 哈希做对照。
前两次的 `B` 是 `31d7aeb`,后两次是 `8507194`。

| 合并 | 文件 | `M` | `P1` | `P2` | `B` | 结论 |
|---|---|---|---|---|---|---|
| `8d80072` | `README.md` | `86821ea` | `7094da8` | `0b864f7` | `5b35354` | 真融合 |
| `8d80072` | `docs/skills-wave/README.md` | `082ed79` | `6f387a6` | `ae4683b` | `73e93eb` | 真融合 |
| `8d80072` | `tests/unit.js` | `b8d5b83` | `a3cc666` | `5cd9f44` | `f44b43c` | 真融合 |
| `b79eb02` | `docs/skills-wave/README.md` | `90cb59c` | `082ed79` | `5ed0957` | `73e93eb` | 真融合 |
| `b79eb02` | `tests/unit.js` | `f275f6a` | `b8d5b83` | `a14dd71` | `f44b43c` | 真融合 |
| `194dbe0` | `README.md` | `b8abdbd` | `86821ea` | `772e945` | `97f8f88` | 真融合 |
| `194dbe0` | `docs/skills-wave/README.md` | `6348ccc` | `90cb59c` | `36b94f2` | `9a5b0f6` | 真融合 |
| `194dbe0` | `tests/unit.js` | `47196c7` | `f275f6a` | `b77671a` | `19c218e` | 真融合 |
| `de9866e` | `README.md` | `2911253` | `b8abdbd` | `7596611` | `97f8f88` | 真融合 |
| `de9866e` | `docs/skills-wave/README.md` | `f93706d` | `6348ccc` | `863ed57` | `9a5b0f6` | 真融合 |
| `de9866e` | **`js/issues.js`** | `65c72c7` | `113afbf` | `dafbd84` | `b23ae3d` | **真融合(W155 那半与 W164 那半各占各的行,两侧都留)** |
| `de9866e` | `tests/unit.js` | `f046125` | `47196c7` | `ea9f7b5` | `19c218e` | 真融合 |

**整份取 `--ours` 零命中、整份取 `--theirs` 零命中、回退到叉点内容零命中。**
四份新记账件是纯新增文件(`P1` / `B` 里都不存在),不参与上表。
与 W161 那槽不同,本槽**一个 `=我方` / `=对侧` 的格子都没有**——十二个共有文件次全是真融合。

### 5.2 三套件实跑

| 套件 | 基线 `c5de21e` | 合入 W158 后 | 合入 W160 后 | 合入 W162 后 | 合入 W164 后(本槽 tip) |
|---|---|---|---|---|---|
| `node tests/unit.js` | 528 / 528 | 530 / 530 | 530 / 530 | 532 / 532 | **534 / 534** |
| └ `contract` 子套件 | 116 / 116 | 118 / 118 | 118 / 118 | 118 / 118 | **119 / 119** |
| `node tests/integration.js` | 141 / 141 | — | — | — | **141 / 141** |
| `node tests/cli.smoke.js` | 105 / 107 | — | — | — | **105 / 107** |

`cli.smoke` 那 2 项失败逐条现取,基线与 tip 上**同名同表现**:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

本槽四支一条冒烟用例都没加,故分母仍是 107、失败仍是这两条。

单元用例的**不同名字数**现取 534,与用例数 534 相等——**连续第十七槽全部相等**。

### 5.3 没删测:按名成集**三轮**比对

按 W157 立的口径做两轮,再按 W161 残留 3 点名的第三种比法补一轮。抽名脚本在**各 tip 的独立 `git archive` 解包树**上跑,不在工作区跑。

**第一轮:基线 `c5de21e` ↔ tip**

- **只在基线里有的:0 条**;
- 只在 tip 里有的:**6 条**,逐条点名:
  - `contract · 护栏主题不许失联:清单里每条主题都还找得到承载它的用例(同主题改名翻面照旧绿,真删掉当场点名)`(W158)
  - `contract · 护栏主题清单自身不许被架空:锚点须落在断言里、条数只增不减、取数口失效先红在这里`(W158)
  - `release · downloadReleaseZip 源级:可执行行零 ZipUtil.download 零 PLACEHOLDER 空壳字面,落地口只此一个(占位加回去当场红)`(W162)
  - `release · downloadReleaseZip 运行期:成功/失败/按钮三个入口一次都没叫到 ZipUtil.download,落地的每份都是真包不是空壳`(W162)
  - `issues · collect:低分审片带派生 shotIds 子集(恒等 Domain.reviseShotIds;空重抽面不出这个字段)`(W164)
  - `contract · 问题中心不把重抽面留在文案里:low-review 的 shotIds 现取 Domain.reviseShotIds,空面不出字段`(W164)

528 − 0 + 6 = 534,与实跑总数对得上。

**第二轮:逐次比对**

| | 上一步独有 | 新增 |
|---|---|---|
| `c5de21e` → `8d80072` | **0 条** | 2 条(W158 那两条) |
| `8d80072` → `b79eb02` | **0 条** | 0 条 |
| `b79eb02` → `194dbe0` | **0 条** | 2 条(W162 那两条) |
| `194dbe0` → `de9866e` | **0 条** | 2 条(W164 那两条) |

**第三轮:被合入支的用例名集合 ↔ 合并后**(W161 残留 3 点名的那一种,本槽照样人工做)

| 支 | 自己的用例数 | 名字在 tip 上找不到的 |
|---|---|---|
| `w158@90707ea` | 522 | **0 条** |
| `w160@5fae3ee` | 520 | **0 条** |
| `w162@a632863` | 526 | **0 条** |
| `w164@1dc0cde` | 526 | **0 条** |

**四支的用例名在合并后一条不缺**:本槽没有翻面、没有改名、没有接位,故第三轮与前两轮结论一致(1.4)。

**这一轮比对本身踩了一次坑,如实记下**:抽出来的名字先用 JS 的 `Array.sort()` 排序(UTF-16 码元序)再交给 `comm` 比对,`comm` 按 locale 排序规则判"输入没排好序"并**假报 2 条既在基线独有、又在 tip 独有**(同两个名字同时出现在两侧)。改成两侧都 `LC_ALL=C sort` 之后三轮全部干净。
**这与 W109 记的"去重口径不一致会凭空多报一条"是同一类**:比对工具与抽名工具的**排序口径**不一致时,`comm` 会同时多报两侧。判它是假报靠的是"同一个名字不可能既只在 A 又只在 B",这一步**没有任何判据要求**,是人读出来的。

### 5.4 没摘 gaps;注册表零 diff,故八个数结构性成立

```
$ git diff --stat c5de21e HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js
(无输出)
```

本槽**五个注册表文件相对基线一个字节都没动**,故八个数结构性成立;仍在合并后 HEAD 上逐个现跑确认:
提示词注册表 **41**、能力短名单 **30**、`CHECKS` **17**、`preflightStages()` **7**、`gaps()` **20 键**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 一个没动。

`gaps()` 现取 20 键逐字:`G-08,G-15,G-09,G-01,G-02,G-12,G-13,G-04,G-10,S-01,S-03,G-07,G-14,G-05,S-04,S-05,G-03,G-11,S-06,S-07`。
两条相关的键**都没摘**:`G-03` 仍是 `review.stage` / `review.reviseLoop` 两条值,`G-11` 仍是 `review.memoryFeedback`。
**`expert.evolve` 仍不在任何 playbook 的 `steps` 里**(现取 `playbooks()` 5 条、`steps` 共 25 步,含 `evolve` 的**零处**);变异 M7 钉着这一点。

产品面进 diff 的文件与行数(本槽自己那一层):

```
$ git diff --stat c5de21e HEAD -- js/ server.js cli.js mcp.js billing.js
 js/issues.js | 15 +++++++++++++--
```

四条槽里**三条零产品文件进 diff**,唯一动产品的是 W164 那 13 加 2 删。

### 5.5 抽查变异

全部在**合并后 HEAD** 上跑,一次只改一处,跑完即还原(还原后复跑 534/534 全绿、工作区 clean)。
按 W161 残留 13 立的规矩,**每条变异先现取一次被改对象的实际产出再看颜色**——本槽这一步真的接住了一次(见 M7)。

**A. 本槽新合进来的六条,各证是新覆盖**

| # | 变异 | 合并后 HEAD |
|---|---|---|
| M1a | 把 `SK-25 记账两向对账(G-03)…` 那条护栏**只改名**(同主题翻面重写的形状) | **534/534 全绿** —— W158 那条判据的正面主张成立:改名不该拦 |
| M1b | 把同一条护栏**整条删掉**(无继任者) | **红 4**:数字对账 2 条(单元数 533≠534、`contract` 118≠119)、三套件下限 1 条、**W158 失联那条 1 条**并把主题编号 `sk25-g03-ledger` 与它的锚点印出来。**M1a 与 M1b 的差别正是这条判据存在的理由**:两者在"按名成集"里都只是"只在基线里有的 1 条" |
| M2 | `js/release.js` 把落地失败那一行退回 `ZipUtil.download(name, [{name:'project_meta.json', data:'空下载兜底:请重新打包'}])` | **红 5**:W154 行为面 2 条(「落地失败必须抛出去」、按钮面把那句假成功提示 `["交付包已下载:6 个文件,…"]` 原样印出来)、基线里 W156 翻面那条 1 条、**W162 源级与运行期各 1 条** |
| M3 | 成功路径插 `const Z = ZipUtil; Z.download(name, [{ name: 'PLACEHOLDER', data: '' }]);` | **红 3**:W152 行为面、W162 源级(报的是 `PLACEHOLDER` 那一格)、W162 运行期 |
| **M3′** | 同上但把 `PLACEHOLDER` 换成 `meta.json`(**别名 + 不带占位字面**) | **红 2**:W152 行为面 + **W162 运行期**;**两条源级判据全绿**。先现取核实源级确实够不着:`js/release.js` 可执行行里 `ZipUtil.download` 命中 **0 处**。这是"运行期那条不是源级那条的子集"的实测(1.2) |
| M4 | `js/issues.js` 去掉 `if (lows.length)` 这道闸(空重抽面也挂 `shotIds`) | **红 1**,只红 W164 那条 `issues` 用例,报「空重抽面不得出 `shotIds`(空数组在子集位上等于整集重跑),实际:`[]`」 |
| M5 | `js/issues.js` 把 `Domain.reviseShotIds(ep)` 换成就地 `lows.map(x => x.shotId)`(**值一样**) | **红 1**,只红 W164 那条 `contract`,报「`low-review` 的 `shotIds` 应现取 `Domain.reviseShotIds`」。**值相同而判据仍红**,正是这条契约要的:第二份投影在改口径时不会跟着走 |

**B. 任务点名要保的,逐样反向变异一次**

| 要保的 | 反向变异 | 实测 |
|---|---|---|
| **W155 的 `EPB` 扇出**(任务点名"两侧都留") | `js/issues.js` 的 `EPB` 投影表新增一档 `'no-dubbing': 'mid'`,而 `episodeState` 那头没有这个码 | **红 1**,报「`Issues.epBlockers()` 的 `no-dubbing` 不是 `episodeState` 会回的码(码名分裂)」——证明 W155 那半在 `js/issues.js` 合并后确实还在跑,不是只留了个壳 |
| **W162 的运行期桩**(任务点名"也要留") | 见 M3′ | 源级两条全绿、只有它红 |
| **`expert.evolve` 不进 `steps`** | `js/skills.js` 里把 `expert.evolve` 掺进主线全链 playbook 的 `steps` | **红 3**:`plans` 报「投影每一步都须登记取材器,漏登记:`expert.evolve`」、`contract` 报主线全链九步字面对不上、`flow` 报「投影每一步都须在中段登记…」 |

**另记一条施加失手,与 W161 那次成因不同**:M7 第一次施加时锚点字符串在 `js/skills.js` 里根本不存在,改写脚本**抛异常退出、文件一字未改**,而随后那次全套跑出 **534/534 全绿**——**报表上与"判据没红"长得一模一样**。
W161 那次是"改上去了但没生效(合法的标签语句)",本槽这次是"压根没改上去",**两种成因不同而在跑分上同形**。接住它的正是 W161 残留 13 立的那一步:先现取被改对象的实际产出(`steps 共 26 步、含 evolve 1 处`)再看颜色。**这一步至今仍是人工的,没有任何判据要求它**(6.5)。

### 5.6 棘轮:四格 `FLOOR` 与 README 数字逐格实测

在**本文落地后的 tip** 上把四个 `FLOOR` 逐格改小重跑全套(`SLACK` 现取仍是 3):

| 格 | 值 | 与 live 的差 | 全套结果 |
|---|---|---|---|
| 单元测试(live 534) | 533 / 532 / 531 | 1 / 2 / 3 | 全绿 |
| 单元测试 | **530** | **4** | **红 1 条** |
| 单元测试 | **528**(基线父值) | **6** | **红 1 条** |
| 单元测试 | **526**(`w162` 与 `w164` 父值同为 526) | **8** | **红 1 条** |
| 单元测试 | **522**(`w158` 父值) | **12** | **红 1 条** |
| 单元测试 | **520**(`w160` 父值) | **14** | **红 1 条** |
| 记账件份数(live 179) | 178 / 177 / 176 | 1 / 2 / 3 | 全绿 |
| 记账件份数 | **174**(基线父值) | **5** | **红 1 条** |
| 记账件份数 | **171**(`w162` 与 `w164` 父值同为 171) | **8** | **红 1 条** |
| 记账件份数 | **167**(`w158` 与 `w160` 父值同为 167) | **12** | **红 1 条** |
| `integration`(live 141) | 140 / 139 / 138 | 1 / 2 / 3 | 全绿 |
| `integration` | 137 | 4 | **红 1 条** |
| `cli.smoke`(live 107) | 106 / 105 / 104 | 1 / 2 / 3 | 全绿 |
| `cli.smoke` | 103 | 4 | **红 1 条** |

**四格全部逮住全部父值,包括基线自己那个。** 与 W151 / W157 / W161 三槽同形,而本槽**没有一格压在差 4 上**——单元那格差 6、记账件那格差 5,都比 `SLACK` 3 多出两格以上。成因与前几槽的读数一致:**"抄基线自己那个父值"响不响只由本槽加了几条决定**,本槽加 6 条单元用例、5 份记账件,两项增量都超过 `SLACK`。
`integration` 与 `cli.smoke` 两格本槽增量为 0,而**四支自称的这两个数都恰好等于 live**(4.1),故这两格本槽**没有任何父值可逮**——只能靠人造值(差 4)确认它们还在守着。这与 W151 那槽 `w143`、W161 那槽 `w159` 同形,而本槽是**四支同时如此**。
十槽连读:W133 差 1 全放过、W134 放过一逮住一、W139 全放过含一次压线、W142 单元三个父值全逮住、W146 差 3 压线放过 + 差 5–11 全逮住、W148 单元全逮住 + 记账件差 3 压线放过、W151 四格差 4–22 全逮住、W157 四格差 4–14 全逮住、W161 四格差 4–18 全逮住、**W166 四格差 5–14 全逮住(第一次没有任何一格压在差 4 上)**。

README 那三处抄父值同样逐处实测:

- 主 `README.md` 单元数改成 528 / 522 / 520 / 526 → **各红 2 条**(精确对账那条报「实测 534,文档 N」+ 三套件下限那条报「README 写的 N 低于下限」,报错句不同、含义不混);
- 主 `README.md` `contract` 条数改成 116 / 115 / 113 / 114 → **各红 1 条**(契约段自报条数那条);
- `docs/skills-wave/README.md` 明写份数改成 174 / 167 / 171 → **各红 1 条**(三方对齐那条)。

### 5.7 本槽自己改了什么

除四个合并提交解冲突外,本文这一提交动了四处:新增本文、`docs/skills-wave/README.md` 补索引行并把明写份数 178 → 179、把"最新一份收敛记录"的指针从 `w161-integration-log.md` 挪到本文(旧指针进"更早的分叉"那一串)、`tests/unit.js` 的记账件 `FLOOR` 178 → 179。
单元用例数与 `contract` 条数**本文一条不加**,故 534 / 119 不动。

四个合并提交里**没有删过任何一条用例、没有放宽过任何一条断言、没有翻过面、没有改过名**;
唯一改动的既有登记是 `GUARD_TOPICS` 里 `plans-gate-read` 那条的 `hosts`(缺省 1 → 2),**方向是收严**(3.1)。

## 6. 残留与欠账

1. **`GUARD_TOPICS` 的 `hosts` 是个手填数,而"锚点太泛"与"这道护栏真有两个承载"在报错句里长得一模一样。** 本槽 `plans-gate-read` 那一格从 1 抬到 2(3.1),判定"那两条读的是同一份实况、不是路人"靠的是人读那两条用例的断言。
   假如日后某条无关用例恰好同时含 `Domain.gateBlockers(p)` 与 `plans.js` 两个字面,报错句会与本槽这次**逐字同形**,而正确处置相反(该收窄锚点而不是抬 `hosts`)。**这条判据把"失联"做成了机检,把"落点算不算数"留给了人。**
2. **`hosts` 抬上去之后,"从 2 掉回 1"会红,但红的是这条主题、报错句写的是"落点数与登记不符"** —— 与"锚点太泛"共用同一句。即同一句话现在要描述三件事(太泛 / 拆成两条 / 有一条被删了)。本槽未动它的措辞(它是被合入支自己的判据、不在本槽射程内),如实登记。
3. **W161 残留 3 那个盲区本槽没有实测机会,也没有收掉。** 本槽四支零翻面零改名,故三轮按名比对结论一致(5.3)。第三种比法本槽照旧是**人工**做的,**仍然没有任何判据要求它**。下一次遇到翻面时同样要人工记得比第三种。
4. **按名比对的排序口径没有判据,本槽踩了一次并假报 2 条。** JS `Array.sort()`(UTF-16 码元序)与 `comm`(locale 序)口径不一致时,`comm` 会**同时**在两侧多报同一批名字(5.3)。
   这是 W109「去重口径不一致会凭空多报一条」的**另一面**:那次是去重口径,这次是排序口径,两次的表现都是"凭空多报",而两次都靠人读出来。**零吃测这件取证工具自己有三个口径要对齐(去重 / 排序 / 按 `|` 切回执载荷),三个都没有判据。**
5. **变异复核仍没有"施加成功"这道闸,而本槽实测出它有第二种失手形态。** W161 记的是"改上去了但没生效",本槽 M7 是"压根没改上去、脚本抛异常"(5.5)。两种在跑分上都是全绿。W161 立的补救(先现取被改对象的实际产出)接住了这一次,**但它是人工的**。
6. **`js/release.js` 那条链上现在有三条判据钉同一件事的不同段,而"它们合起来封住了什么"没有一处写下来。** 3.3 那张表是本文人工列的;下一个人要动其中任一条时,判"删掉这条会不会留下没人管的面"仍得把三条并排读一遍。
   W162 源级那条自带一句「本条不接管 W154 那两条」,是三条里唯一写明分工的。
7. **`js/release.js` 底部那个包着 `Bus.on('*')` 注册的 `catch (_) {}` 仍开着**(W151 残留 2、W157 残留 5、W161 残留 5 原样未动)。全仓现取:`js/release.js` 空 `catch` **1 处**、`js/bus.js` **0 处**。
8. **W161 残留 1(「两支自称逐字同 diff」靠人手比 patch)本槽没有实测机会**:本槽没有出现两支改同一产品文件同一段的情形。
9. **W161 残留 4 那条弱断言(`assert(/aggFail: 0/.test(rel) || /counts/.test(rel), …)` 实际恒真)本槽原样未动**,它现在在集成线上而它并不守着什么。
10. **W161 残留 6 仍成立**:`downloadReleaseZip` 那几条钉的是"这一段 / 这个文件 / 这三个入口不许再落第二个包",**不是"不许再落第二个包"这个语义**。M3′ 恰好证明这个边界有实际后果——换个持有者(`Z`)源级就够不着,接住它的是运行期那条;而运行期那条钉死的是三个入口,**第四个入口出现时它同样够不着**。
11. **W151 残留 4 的门槛派生那条本槽又碰了一次,但没收。** `plans-gate-read` 那条主题现在有两个承载(3.1),覆盖面比登记时厚;而那句话的核心照旧成立——**"某个码该不该投、投成什么危险级/什么文案"仍是各消费方各自决定**。
12. **`G-03` 的形态面、`G-11` 的自动进化面均未动**(W151 残留 1、3 原样)。
13. **W147 登记的那条行为余面本槽未收**:`extractDone && !script` 的老项目走到 `project.splitEpisodes` 仍回 `blocked / no-script`。
14. **W142 残留 1(W135 那句 `assertEq` 与 W137 共享 `fails` 计数器)本槽未动。**
15. **W144 的行序判据仍不管同号内的先后**(连续第七槽没有实测机会:本槽四行的波次号 158/160/162/164 互不相同)。
16. **散文那半仍没有判据。** 本槽三处 README 长句取并集靠的是"把对侧相对它自己叉点的增量切出来、按锚点插回我方",这一步是人工做的;切得对不对没有判据管。本槽三处都现查过"对侧那段里有没有已被主干推翻的话"(未发现),但**这一步同样是通读**。
17. **`w160` 那份 783 行核验件的结论本槽未逐条在合并后 HEAD 上重跑。** W119 立过"把核验件的阻塞按原判据在合并后的 HEAD 上逐条重跑"这一手,本槽任务口径只要求合入,故未做;它点名的阻塞在本线上还成不成立**没有现取核实**,如实登记。
