# W304 合入:把 W303(去重那几句注释改准)并入 W302 尖

**现取三个 SHA**(全部 `git rev-parse` 现取,不抄任何一侧的自称):

| 位 | SHA | 说明 |
|---|---|---|
| P1(我方尖) | `e231046` | `origin/cursor/w302-integration-b299` |
| P2(对侧尖) | `694c16f` | `origin/cursor/w303-nth-comments-42d2`(对侧自称 `694c16f`,现取相符) |
| merge-base | `e231046` | 恰等于 P1 → **快进形** |
| merge commit | `64bd57b` | `--no-ff`,parents `e231046` + `694c16f`,零冲突 |
| 本支 | `cursor/w304-integration-2b0a` | 从 P1 现取 HEAD 拉出 |

`tree(M) == tree(P2) == efd4537`,`git diff M P2` 空集(`js/` 与 `cli.js` 相对对侧逐字节零 diff)。

## 一、这一槽的形状:第四条快进形,且对侧是**纯注释槽**——两格空证叠一格无判据

W300 / W301 / W302 三槽已经把「`tree(M) == tree(P2)` 一个字不作证」这一格读稳:
它只说明"合完与对侧长得一样",而**误合出来的树同样长这样**。本槽在此之上再叠一层麻烦:

对侧 W303 改的**全是注释、一条 `log()` 回执与 help 文案**,而源级判据取的是 `blankNonCode(...)`
(注释与字面量一并抹掉),故 W303 的产出在树上**无人看守**——W303 自己 §五 已把这一格量在明处
(两种措辞全绿),并明写"有意不给它立字面判据"。这意味着本槽**两条常规证路同时失效**:

- 空 diff 那两格(`git diff M P2` 与 `tree` 相等)照旧不作证;
- 「变异台打红」那一格也大半作不了证——退回老措辞的手一律绿(§2.2 十手现跑逐条登记)。

于是结论只能压在另外两格上:**锚点台在 P1 上逐条失配**(证"对侧真进来了"),
以及 **live 探针的措辞面在 P1 上逐格失败、行为面两侧一模一样**(证"进来的正是那几句,且它改的确实只是措辞")。

### 1.1 可执行骨架:P1→M 的两处,逐处交代

拿 `tests/unit.js` 里那份 `blankNonCode`(判据自己在用的同一份)把注释抹掉后逐行比 P1 与 M:

| 文件 | 原文相同 | 抹注释+字面量后骨架相同 | 只抹注释、字面量留着 |
|---|---|---|---|
| `js/domain.js` | 否 | **是** | **是** |
| `cli.js` | 否 | 否(见下) | 否 |
| `tests/unit.js` | 否 | 否(见下) | 否 |

`cli.js` 骨架上那一处 diff 是 `--apply` 回执由单串改成两串相接:

```
-log(scan.plan.length + );
+log(scan.plan.length +
++ );
```

即同一条回执的换行相接,除措辞外零行为改动。`tests/unit.js` 骨架上那一处是
`const FLOOR = 317;` → `318`(数字字面,故抹字面量也遮不住)。**除这两处外可执行面逐字节未动**;
`mcp.js` / `server.js` / `index.html` / `css/` / `js/roles.js` / `js/sb-views.js` / `js/storyboard.js` 一字节未动。

## 二、锚点台与变异台

### 2.1 锚点台:14 条,P1 上**命中 0**、合完 `64bd57b` 上 **14/14**

快进形槽里这是唯一说得出"对侧真进来了"的一格。锚点逐条对着 W303 的产出取:

| 锚点 | 文件 | P1 | M |
|---|---|---|---|
| A1 `shots-import` 闸头顶去掉「审片回写」三字 | `cli.js` | 失配 | 命中 |
| A2 `shots-dedupe` 头顶去掉「审片回写」三字 | `cli.js` | 失配 | 命中 |
| A3 引用面那句拆成两类(`uiSel` vs `perShot`) | `cli.js` | 失配 | 命中 |
| A4 头顶点名 `Domain.reviewRows` 的序数对位 | `cli.js` | 失配 | 命中 |
| A5 `--apply` 回执 `log()` 改口径 | `cli.js` | 失配 | 命中 |
| A6 help 那三行改口径 | `cli.js` | 失配 | 命中 |
| A7 `dupIdScan` 头顶收掉「一律」 | `js/domain.js` | 失配 | 命中 |
| A8 补「一处例外是分镜侧的整集审片报告」 | `js/domain.js` | 失配 | 命中 |
| A9 那条用例的说明改口径 | `tests/unit.js` | 失配 | 命中 |
| A10 量尺注释改口径 | `tests/unit.js` | 失配 | 命中 |
| A11 记账件 `w303-dedupe-review-nth-comments.md` 在树上 | docs | 失配(文件不存在) | 命中 |
| A12 README 声明份数 318 | docs | 失配 | 命中 |
| A13 索引表补上 w303 那一行 | docs | 失配 | 命中 |
| A14 记账件 `FLOOR` 抬到 318 | `tests/unit.js` | 失配 | 命中 |

**小计:P1 命中 0/14,M 命中 14/14。**

### 2.2 变异台:17 手打在 `64bd57b` 上,**红 6 / 绿 11**

绿的那 11 手里 **8 手是真漏网**(M1–M8:把 W303 的产出整段退回老措辞,判据一条不红),
另 2 手是设计内的绿(M9 落在 `SLACK` 缓冲格内、M16 对照手)。这 8 手不是本槽新发现的缺口——
它正是 W303 残留第 2 条「注释准不准仍然零判据」的现场,对侧明写不立字面判据,**本槽不代修**。

| 手 | 变异 | 判据 | 结果 |
|---|---|---|---|
| M1 | `shots-import` 闸那句退回带「审片回写」的老措辞 | `unit` | **绿**(718/718)——漏网 |
| M2 | `shots-dedupe` 头顶那句退回老措辞 | `unit` | **绿**(718/718)——漏网 |
| M3 | 引用面那五行整段退回 W302 老措辞(说「一律按 find 首行」) | `unit` | **绿**(718/718)——漏网 |
| M4 | `--apply` 回执整句退回老措辞(说「lastReview/uiSel 一个字未动」) | `unit` | **绿**(718/718)——漏网 |
| M4b | 同 M4 一手,另跑 `cli.smoke`(那条冒烟真读回执) | `cli.smoke` | **绿**(115/117)——漏网 |
| M5 | help 那三行退回老措辞 | `unit` | **绿**(718/718)——漏网 |
| M6 | `js/domain.js`「一处例外」那三行整段删掉 | `unit` | **绿**(718/718)——漏网 |
| M7 | `js/domain.js` 把「一律」抄回去(退回那句假话) | `unit` | **绿**(718/718)——漏网 |
| M8 | `tests/unit.js` 那条用例的说明整段退回老措辞 | `unit` | **绿**(718/718)——漏网 |
| M9 | 记账件 `FLOOR` 318→317(差额 1) | `unit` | **绿**(718/718)——落在 `SLACK` 3 缓冲格内,设计内 |
| M10 | 记账件 `FLOOR` 318→314(差额 4,越过 `SLACK`) | `unit` | **红** 717/718 |
| M11 | README 声明份数 318→317(文件与索引行都留着) | `unit` | **红** 717/718 |
| M12 | 删掉 w303 记账件本体(索引行留着) | `unit` | **红** 714/718 |
| M13 | 删掉索引表里 w303 那一行(文件留着) | `unit` | **红** 714/718 |
| M14 | `TOPIC_FLOOR` 30→31(抬过头:live 在册 30) | `unit` | **红** 716/718 |
| M15 | `reviseTargets` 不再走 `reviewRows`、自己 `findIndex` 到首行 | `unit` | **红** 692/718(26 条) |
| M16 | 对照手:头顶另加一行无关注释 | `unit` | **绿**(718/718),设计内 |

M4b 单列的理由:`tests/cli.smoke.js:327` 那条真跑 `--apply` 并读回执,是全树离"回执措辞"最近的一条,
量过才敢说"连它也不看那句话"——它读的是 `state-get` 回来的引用字段,一个字不读 `log()` 的输出。

M15 是**对照方向**的一手:W303 §五 说「真会漂的那一层(`reviseTargets` 长在 `reviewRows` 之上)
`contract` 早有两句钉着」。现跑取证——把那条委托摘掉当场 26 条红。
**注释无人看守 ≠ 行为无人看守**,这两件事在本槽里各有一手现跑读数,不许混着讲。

## 三、live 探针(真起服务 + 真 `state-put` 灌脏树 + 真跑 `shots-dedupe --apply`)

隔离:`MV_DATA_DIR` / `MV_UPLOADS_DIR` / `MV_CONFIG` / `HUJING_CONFIG_DIR` 全指向 `/tmp` 下临时目录,
`env -u HUJING_SERVER`,临时端口,跑完即删。

夹具(比 W303 那份四行夹具多要一格:同 id 给**三条**逐镜分,把"塌几条"量出来):
四行分镜,`dup` 占第 1/3/4 行、`solo` 占第 2 行;`lastReview.perShot` 四条,
`dup` 三条分别 4/5/6 分、`solo` 一条 9 分(`REVIEW_MIN` 现取 7,故 `dup` 那三条都在重抽面上)。

同一份探针在 P1 与合完 `64bd57b` 上各跑一遍:**6/12 → 12/12**,翻绿恰 6 格。

| 格 | 内容 | P1 | M |
|---|---|---|---|
| 1 | `--apply` 真跑得动:两行撞车行改发新 id、首行留原 id(`renamedIds=2`) | PASS | PASS |
| 2 | 去重前 `reviewRows` 逐条落在自己那一行:`0,1,2,3` | PASS | PASS |
| 3 | 去重后落在首行之外的那两条一起退回首行:`0,1,0,0` | PASS | PASS |
| 3b | 重抽面 `reviseTargets` 跟着塌:`1,3,4 → 1,1,1` | PASS | PASS |
| 4 | `uiSel` 去重前后同为第 0 行(它那句「落到同一行」至今仍准) | PASS | PASS |
| 5 | `perShot` 报告本身逐字节不变(只改 id、一个字不改报告) | PASS | PASS |
| 6 | `--apply` 回执说得出「整集报告按行序数对位…退回首行」 | **FAIL** | PASS |
| 7 | 回执不再说「引用按首行解析,故 lastReview/uiSel 一个字未动」 | **FAIL** | PASS |
| 8 | help 里那格不再写「lastReview/uiSel 等引用一律不动——它们按首行解析」 | **FAIL** | PASS |
| 9 | help 里说得出「整集报告按行序数对位」 | **FAIL** | PASS |
| 10 | `cli.js` 头顶那段说得出 `Domain.reviewRows` 的序数对位 | **FAIL** | PASS |
| 11 | `js/domain.js` `dupIdScan` 头顶写明那处例外(不再说「一律」) | **FAIL** | PASS |

**这一格是本槽最该留的读数**,理由是它把两件事同屏摆在了一起。P1 那一侧同一次运行里:

- 量出来的实况:`reviewRows` `0,1,2,3 → 0,1,0,0`、`reviseTargets` `1,3,4 → 1,1,1`;
- 同一次运行打出来的回执原话:
  `2 镜的 id 与表内在前的行重复,已改发新 id(首行留原 id;引用按首行解析,故 lastReview/uiSel 一个字未动)`。

**回执朝着让人放心的方向说了假话,而它下面那两行读数就是反证**——这就是 W303 收的那条残留的现场。
合完那一侧同一份夹具、同一份读数,回执改成:

`2 镜的 id 与表内在前的行重复,已改发新 id(首行留原 id;uiSel 那类按 id 取镜的引用仍落到同一行;lastReview 一个字未动,但整集报告按行序数对位,落在首行之外的逐镜结论会退回首行——回位请重跑整集审片)`

行为面 6 格两侧全绿且**读数逐字相同**,这不是探针没用,恰是本槽要证的那一半:
W303 自称"零运行时"——除回执与 help 两句人读的文案外,现跑量出来确实一格没动。

## 四、四格核对(明令逐条)

1. **`js/` 与 `cli.js` 可执行面相对对侧零 diff。**
   `git diff 694c16f HEAD -- js/ cli.js mcp.js server.js index.html css/` 空集;
   相对 P1 的那点 diff 逐处判过(§1.1):`js/domain.js` 骨架完全相同、`cli.js` 只有回执换行相接、
   `tests/unit.js` 只有 `FLOOR` 那个数字。**仅注释/帮助,且都已在对侧。**
2. **`shots-dedupe` 帮助不再写「审片回写按 find 首行」。**
   真跑 `node cli.js --help` 现取(探针格 8/9):那格已不含「lastReview/uiSel 等引用一律不动——它们按首行解析」,
   改写成「整集报告按行序数对位,落在首行之外的逐镜结论会退回首行,回位靠去重后重跑整集审片」。
   `shots-import` 闸与 `shots-dedupe` 头顶那两句里的「审片回写」三字亦已摘掉(锚点 A1/A2)。
3. **W303 明令不代修的那句仍原样在。** `cli.js:790` 现取:
   `* 而点名子集是按行筛的(ids.has(s.id)),点名一次两行都跑,一个 id 收两笔视频钱、写回的还是首行。`
   ——属视频写回那一面,本槽同样不代修(§七第 1 条)。
4. **`Domain.reviseTargets` 那一处点名仍在且现在是准的。** `cli.js:827` 现取
   「重抽面 `Domain.reviseTargets` 长在它之上」,与探针格 3b 量出来的 `1,3,4 → 1,1,1` 对得上;
   `cli.js:1287` 那句(达标线/判旧/取交集判在派生里)W303 判过仍准、原样留着,本槽复核无误。

## 五、live 数字(全部现跑,一个数不抄)

两侧都自称 `unit` 718,仍按纪律在 P1 与合完各整跑一遍:

| 项 | P1 现跑 | 合完现跑 | 说明 |
|---|---|---|---|
| `unit` 总数 | 718/718 | **718/718** | 注释槽不带测试条数;两侧自称相符,但都是现跑量的 |
| `unit · commands` | 75 | **75** | 未动 |
| `unit · domain` | 49 | **49** | 未动 |
| `unit · contract` | 153 | **153** | 未动 |
| `unit · sb-views` | 11 | **11** | 未动 |
| `unit · skills` | 104 | **104** | 未动 |
| `integration` | 152/152 | **152/152** | 未动 |
| `cli.smoke` | — | **115/117** | 单独跑、`env -u HUJING_SERVER` 及 `MV_*` |
| `TOPIC_FLOOR` / 在册 / 销号 / 花名册 | — | **30 / 30 / 0 / 30** | 对侧自称未抬,live 现取四数一致后才写 |
| 记账件三方对齐 | 317 | **319** | 目录实况 == README 声明 == 索引行数;`FLOOR` 318→**319** |
| `gaps()` 键数 | 20 | **20** | 一条没剥 |

`cli.smoke` 那两条失败:`未登录 whoami → exit 3`(实得 `exit=1`)与 `llm --json mock 链路`。
同 env 下在 `origin/master`(`9adcf0f`)另起一棵 worktree 现跑对照,**同名同表现**(那边 51/53),明令允许。

`node --check` 过 `cli.js` / `js/domain.js` / `tests/unit.js` / `server.js` / `mcp.js`。**不跑 `e2e`。**

### 5.1 五格棘轮(live 现取,差额全 0)

| 格 | live | floor | 差额 |
|---|---|---|---|
| 单元测试 | 718 | 718 | 0 |
| 集成测试 | 152 | 152 | 0 |
| CLI 冒烟 | 117 | 117 | 0 |
| 记账件份数 | 319 | 319 | 0 |
| 护栏主题 | 30 | 30 | 0 |

`SLACK` 现取 **3**,一格没用掉(M9/M10 两手把这条缓冲的边界钉在明处:差额 1 绿、差额 4 红)。

## 六、并集与棘轮

快进形槽里"并集"仍要**逐格现取**,不许因为 diff 空集就当没丢:

- **README 索引**:W302 那一行与 W303 那一行都在,本槽再补 W304 自己这一行,按波次号递增插在 w303 之后;
  声明份数 318 → **319**,目录实况 319 份、索引表 319 行,三方对齐。
- **`FLOOR`**:318 → **319**(抬到当轮实况,差额 0)。
- **`TOPIC_FLOOR` / 花名册**:对侧自称未抬,现取核过 `30 / 30 / 0 / 30` 四数一致后照写 30,**未动**。
  本槽不新登记护栏主题(注释准不准这一格明令不立字面判据,故没有可登记的主题)。
- **GUARD 主题表**:`GUARD_TOPICS` 30 条、`GUARD_TOPICS_CLOSED` 0 条,一条没增没销。
- **`gaps()`**:20 键现取(`G-01…G-15` 缺 `G-06`,加 `S-01`/`S-03`…`S-07`),**不剥**。
- **既有清单保留**:`Domain.dupIdScan` 四处同读(`cli.js` 两处、`js/roles.js`、`js/storyboard.js`)一处没动;
  `data-dupjump` 全树 6 处(`js/roles.js` 3、`js/sb-views.js` 3)一处没动。

## 七、残留(本槽**不代修**,W303 原话逐条保留)

W303 §七四条原话:

> 1. **`cli.js:790` 那句「一个 id 收两笔视频钱、写回的还是首行」也已经不准了。**
>    CLI 两条批量路径(`gen-episode` 与 `exec episode.generateVideos`)自 W248 起都按 `nthShot`
>    逐行写回(`cli.js:969-986` / `:1196-1211` 现取),只有 `gen-shot-video <sid>` 这类按 id 点名单镜的
>    仍落首行。**本槽明令只改审片对位那几处,故这一句原样留着**,登记在此:它与本槽收的是同一种病
>    (寻址口径换了、旁边的说明没跟着走),但属于**视频写回**那一面,收它得连着两条批量路径与单镜路径
>    一起现跑量一遍再动笔。
> 2. **注释准不准仍然零判据。** §五 量过:两种措辞全绿。下一次同类漂移照旧只能靠人读到。
> 3. **主体侧与镜头侧的去重说明仍各写一份**(W287 残留第 5 条、W299/W302 原样带下来的那条)。
>    本槽在 `cli.js` help 与 `js/domain.js` 注释里又各让两侧的措辞分开了一点(镜头那一侧多出报告那半句),
>    合并成一份模板的代价比 W302 记的时候更高了一点点。
> 4. 明令没碰的照旧没碰:不给入口加闸、`state-put` 不设闸、`gaps()` 不剥、SK-04、一对多跳转、
>    `board`/`bb` 落点、回程、`Bus`、搜索缓存、主体侧那条绑变量名的判据。

第 4 条是个收口句,展开来是 W302 §七里仍活着的那几条(W302 第 1 条已由 W303 收掉,不再计入)。
W302 原话逐条转录:

> 2. **发现存量重复仍得先走到主体库那一页或分集工作区顶栏。** 项目详情面与项目列表那几枚角标点过来落的仍是整页,
>    不是撞车那张卡;整树导入/恢复与 `state-put` 照旧能写进重复 id(明令不给入口加闸),这仍是**存量**出口。
> 3. **跳转仍只有"去第 1 行/第 1 位"一个方向。** 站在第 1 行那张卡上,看不到"还有哪几行撞了我这个 id"——
>    它那一枚一个跳转都不挂(挂上就是跳自己)。要收得让第 1 行那一枚指向后续那几行(不止一行时还要挑一个或列出来),
>    那是一对多的另一种交互(W299 残留第 4 条同形,本槽没动)。
> 4. **`board` / `bb` 两档仍只看得见顶栏那个总数。** 分镜脚本渲场次与节拍、节拍板渲五段节拍,一行分镜都不渲,
>    故那两档上既没有小标也没有落点;"这一集哪几行撞了"要看得见,仍得先切到另外三档之一。
> 5. **主体侧那条「定位动作只许有一处」的判据仍绑在接收者变量名上**(W300 残留第 7 条原样在)。
> 7. **切过去之后没有回程。** 跳到第 1 行那张卡之后,"回到刚才那一行"没有路(高亮 2.2s 褪掉就没了),
>    只能自己往回滚。

七条一条没代修,逐条现取核过:

- W303 第 1 条:`cli.js:790` 现取仍是「一个 id 收两笔视频钱、写回的还是首行」,一字未动;
- W303 第 2 条:§2.2 现跑取证——M1–M8 八手退回老措辞**全绿**,连 `cli.smoke` 那条真读回执的(M4b)也绿;
- W303 第 3 条 / W302 第 6 条:两句展示文案现取仍各一份——`js/sb-views.js:75`
  (「…第 1 行那张卡(就在这一档里,不换视图也不换选中镜)…」)与 `js/roles.js:312`
  (「…第 1 位那张卡(在别的分类下就先切过去)…」);
- W302 第 2 条:`js/episodes.js` / `js/projects.js` 本槽逐字节未动,那几枚角标落点一行没改;
- W302 第 3 条:`data-dupjump` 全树 6 处(`js/roles.js` 3、`js/sb-views.js` 3),反方向一条路都没有;
- W302 第 4 条:`board` / `bb` 两档仍一行分镜不渲,`dupRowTag` 的三个消费处是 `js/sb-views.js:116`、
  `js/shotgroups.js:217` 与缩略图带那一档,两档 board 上一处都没有;
- W302 第 5 条:`tests/unit.js:4037` 现取仍是 `card.scrollIntoView(`(连着接收者变量名数),
  而镜头侧 `:4417` 是 `\.scrollIntoView\(`(不带接收者)——两条判据的形状一字未变;
- W302 第 7 条:本槽没动任何交互,回程仍没有。

**本槽不新记残留。** §2.2 那 8 手漏网量到的是 W303 第 2 条那一件事的现场,
把同一件事记两遍会让下一槽以为多欠了一笔。

## 八、这一槽可复用的两件事

- **合一条"对侧只改注释"的槽时,变异台会大面积失效,得提前说清哪些绿是漏网、哪些绿是设计内。**
  照抄 §2.2 那张表的分栏:退回老措辞的手(漏网,登记但不代修)、缓冲格内的手(设计内)、对照手(设计内)、
  以及**对照方向那一手**(M15:把注释描述的那条真委托摘掉,当场 26 条红)。
  最后这一手是防止把结论讲歪的关键——"注释无人看守"不等于"这块行为无人看守",两句话各要一份现跑读数。
- **注释槽的 live 探针要把"量出来的读数"与"同一次运行打出来的那句话"摆在同屏。**
  照抄 §三:P1 那一侧同一次 `--apply` 里,上面两行读数是 `0,1,2,3 → 0,1,0,0`,
  下面那句回执写着"lastReview/uiSel 一个字未动"——**假话与它的反证同屏**,
  比任何源级断言都说得清这一槽收的是什么。行为面那 6 格两侧读数逐字相同,同时也就把"零运行时"这半句证掉了。
