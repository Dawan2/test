# W63 · 周期 6 独立核验

> 核验槽独立作业:**只读代码、只写本文件**。全部测试数字由本槽在各支独立 `git worktree` 上重新跑出,
> 不引用被核分支文档里的数字;引文与自测不一致处逐条标注。本轮不开 PR、不合并、不改任何业务代码。
> 成熟度沿用 `w5-cycle1-audit.md` 第 1 节的 M0–M4 标尺(M3 = 代码+断言、分支内自洽全绿;**M4 = 已合入 `master`**)。
> 周期 6 范围取 **W49–W61**,集成线 **W50 → W55 → W57 → W60**;`w47-integration-2fed` 作为周期 5 的收槽 tip
> 纳入,只作基线不重复评分。`w46-g12-playbook-proj-deb4` 是周期 5 的晚推槽,单列在 0.1。
> **W62 远端零引用**(`git branch -r` 无 `w62-*`),其状态**未知**,本报告不评分、不推测。

## 0. 核验时点与锚定 SHA

| 分支 | head | 相对 `master` | `master` 是严格祖先 | 角色 |
|---|---|---|---|---|
| `master` | `9adcf0ff` | —(全仓 8 提交) | — | 主干 |
| `cursor/w46-g12-playbook-proj-deb4` | `f3c4611a` | 领先 213 | 是 | 周期 5 晚推槽 |
| `cursor/w47-integration-2fed`(周期 5 收槽 tip,基线) | **`8366727d`** | 领先 222 | 是 | 集成线 |
| `cursor/w49-agent-system-prompt-96c0` | `a3960a6b` | 领先 224 | 是 | 功能槽 |
| `cursor/w50-integration-dad5` | `371c75e2` | 领先 230 | 是 | **集成线** |
| `cursor/w51-browser-agent-personas-da99` | `f46a3601` | 领先 232 | 是 | 功能槽 |
| `cursor/w52-g12-mcp-mid-templates-d660` | `12607c65` | 领先 234 | 是 | 功能槽 |
| `cursor/w53-memall-headless-seed-3653` | **`00459625`** | 领先 233 | 是 | 功能槽(**head 未进集成线**,见 3.2) |
| `cursor/w54-issues-unreviewed-sk19-cfc0` | `bb0e9af4` | 领先 232 | 是 | 功能槽 |
| `cursor/w55-integration-8f21` | `3968658e` | 领先 242 | 是 | **集成线** |
| `cursor/w56-aiscript-digest-prompt-e8c8` | `6f9df876` | 领先 231 | 是 | 功能槽 |
| `cursor/w57-integration-a697` | `450c29f8` | 领先 249 | 是 | **集成线** |
| `cursor/w58-sk25-publish-cmd-3b7a` | `e842dfbe` | 领先 246 | 是 | 旁支(未合) |
| `cursor/w59-issues-umd-a3d7` | `97e67845` | 领先 248 | 是 | 旁支(未合) |
| `cursor/w60-integration-edd3` | **`4967ae91`** | 领先 **252** | 是 | **集成线 tip** |
| `cursor/w61-sk26-front-writeback-b09b` | `8b63f7fe` | 领先 250 | 是 | 进行中(未合,见 4.11) |

十六支的 `git merge-base --is-ancestor master <branch>` **全部成立**,任一支单独并入主干都是零冲突快进。
核验时点远端共 **81 条分支**(不含 `master`;周期 5 末为 69 条,本周期新增 12 条)。
核验全程十六支 head 未移动。

### 0.1 W48 有两条结论在本槽复核时已不成立——不是它算错,是它把一次 `fetch` 当了终局

这一条必须放在最前面,因为**它与本周期最重的那个病灶是同一个机制**(见 3.2、第 6 节)。

W48 报告第 0.1 节写了两件事,本槽实测都已被推翻:

| W48 的结论 | 本槽实测 | 提交时点对照 |
|---|---|---|
| 「**`w46-*` 没有任何远端引用**,不是漏 push,是这个槽号**从未登记过**」 | `origin/cursor/w46-g12-playbook-proj-deb4` **存在**,head `f3c4611a`,并已随 W50 进集成线、记账件 `w46-g12-playbook-proj.md` 已登进索引 | `w46` 两个提交 `3645d22`(18:52:08)、`f3c4611`(18:52:18);W48 报告 head `05836ee` 是 **19:15:32** |
| 「`w47` 自称「整合(1/4)」**实测只合了第一支**……该支**也还没有自己的记账件**」 | `w47` head 现为 `8366727d`,**四步全部做完**(`8400399` 合 W43、`682f3c8` 合 W44、`f22dcaf` 合 W45),且**已有记账件** `w47-integration-log.md` | `w47` 收槽提交 `8366727`(**18:50:51**)早于 W48 报告 head 24 分钟 |

W48 自己留了免责句(「若复核时点晚于本报告,请先比对 head SHA 再采信数字——尤其 `w47`」),
**这一条本槽照做了,所以两条结论按时点予以更正,不算记账不实。** 但它的**成因**值得单独记:
提交时点显示这两支在 W48 写下报告 head 的时刻**已经存在**(仓库里只留得下 commit 时点,push 时点不可查,
故本槽**不据此判 W48 记账不实**);W48 的判据「远端零引用」是一个**时点结论**,却写成了终局措辞
(「从未登记过」)。

**周期 6 的实况是:这个形态在本周期内又重复了三次**——W55 把 `w53`/`w54` 记成「开工时前缀无匹配」
(两支的 commit 时点 19:48 / 19:55,W55 收槽 19:59)、W57 把 `w53` 的记账件记成「不存在」(见 3.2,**这一次造成了实质损失**)、
W60 把 `w58-*` 记成「远端零匹配」(`w58` 首个提交 20:31:41,W60 收槽 20:44:39)。
其中 W55 与 W60 那两处**都如实标了「开工时」这个限定词**,是合规的时点声明;**只有 W57 那处写成了无限定的事实判断**。

**最讽刺的一处**:这条口径的正解**就写在集成线自己身上,而且早了两个槽**。
`w50-integration-log.md` 第 2.4 节原文:

> **口径**:每推一版就重做一次包含性实测,直到某一次 `fetch` 后清单为空为止。……
> **「不存在」是一个时点结论而不是终局结论**……同一条分支合过一次也不等于合完了:**它很可能还欠一笔记账件提交**。

最后那半句逐字预言了 W57 七分钟后踩的坑。**W50 立了口径、写进了记账件,W57 没有执行。**

## 1. 一句话结论

**周期 6 把周期 5 报告最狠的那条指控(功能槽基线乱取)从 2/10 修到 9/9,却在同一时间丢了一份 143 行的记账件,
并且丢法恰好落在对账契约的盲区里——「两边都能绿而实质错」这一类,本周期从周期 5 的推测变成了三处实证。**

先说修好的。周期 5 的核心指控是「十个功能槽只有两个以集成线 head 为基线,还出了两次取非 head 中间点的新形态」;
本周期**九个功能槽的基线逐支实测全部等于当时的集成线 head**(`w49` 取 `w47` head;`w51`/`w52`/`w53`/`w54`/`w56` 五支同取 `w50` head;
`w58`/`w59` 取 `w55` head;`w61` 取 `w57` head)。周期 5 那条离集成线 4 跳的支链、那两次取中间点,
**本周期一次未复发**。周期末在外的 tip 由周期 5 的 4 条降到 **3 条**(`w58` / `w59` / `w61`),
且三条全部以集成线 head 为基线、**互不重叠**(各改各的文件,`w60 ← w61` 零冲突)。

记账面同样是干净的一轮:**14 支的 README unit 断言数、CHECKS 条数、注册表提示词条数与本槽实跑逐个对上,零失真**,
`docs/skills-wave/` 索引与目录**双向零差**(14 支 × 2 向全部比对)。本槽三路变异(改错 unit 数、删掉整句、改错提示词条数)**三路全部转红**,
W23 那套契约的强度第六次独立复验成立。

**但周期 5 报告点名最狠的那条治理债,一个字没做,而且这次它当场兑现了。**
周期 4 提出、周期 5 重申为「唯一能同时压掉三条阻塞」的第 2 步——
**在 `docs/skills-wave/README.md` 固定位置写当前集成线 head + 加契约断言**——
本槽在 `w60` 的该文件里搜「当前集成线 / 集成线 head / 指针」**零命中**,`tests/` 里搜「集成线 / 祖先 / is-ancestor」**零命中**。
**也就是说,基线从 2/10 修到 9/9 是靠人守出来的,不是靠机制钉住的。**
这个区别在本周期已经有代价:**9 次集成合并里有 2 次没取被合入支的 head**(见 3.1),
其中 W50 那次**当轮自纠**(补一次合 head),**W57 那次至今未补**。

**本轮最实的一条新发现:集成线丢了一份记账件,而且集成线的散文里写着「这份记账件不存在」。**
`w53-memall-headless-seed-3653` 的 head `00459625` 是一个 143 行的记账件提交 + 索引登记;
W57 只合了它的**中间点** `09887be8`,并在 `w57-integration-log.md` 与 `docs/skills-wave/README.md` 里
写下「**W53 没有留下自己的记账件**」「该槽远端 tip 是完成态但**没有留下自己的记账件**」。
本槽实测:该记账件在 `w53` 的远端 tip 上**存在**,commit 时点 **20:03:44**,而 W57 的合并提交是 **20:17:17**、
它写下这句话的记账提交是 **20:24:21**。W60 在 **20:44:39** 又把这句话**原样重申了一次**
(`w60-integration-log.md` 第 8 节:「W53 那处没有记账件的缺口**仍如实留在** `w57-integration-log.md`……本槽不补造一份 `w53-*.md`」)。

**这处失真为什么全绿:因为文件和它的索引行是一起丢的。** 索引契约查的是「索引与目录**双向一致**」,
不是「合进来的槽**必须有**记账件」。两边一起少,双向依然零差。
本槽做了决定性变异证明(第 6 节变异 6):在 `w60` 上**同时删掉** `w56-aiscript-digest-prompt.md` 与它那行索引,
`node tests/unit.js` **仍是 400/400 全绿**。

**第二处实证是数字面的,而且现在还挂在一条打开的旁支上。**
本槽实测 README 的 `cli.smoke` 数字在 **`w52` / `w55` / `w59` 三支上失真 −8**(自称 64、实跑 72),
其中 **`w55` 是一条集成线 tip**。成因:W23 的对账契约钉的是 **unit** 用例数,
**`integration` 与 `cli.smoke` 两个数字没有任何断言**(本槽在 `tests/unit.js` 里 grep `integration.js` / `cli.smoke` **零命中**,
并用变异 7、8 把这两个数字分别改错,**两次都是 400/400 全绿**)。
这处失真起于 W52(它加了 8 条冒烟用例没同步 README),随 W55 继承,**在 W57 被冲突解法顺手改对**(取了 live 值),
但**在 `w59` 上活到现在**——`w59` 叉自 `w55`,把那个错的 64 一并带走了。

**第三处实证是前向的,而且是「假欠账」方向——这是五份核验报告都没写过的形态。**
`w61` 落地的正是 `core.memoryDual` 的 `note` 里点名的最后一处余量(前段四步结论回流协作记忆),
但它**一行没碰 `js/skills.js`**。本槽实跑 `w60 ← w61`:**零冲突、合完 400/400 全绿**,
而合入后 `core.memoryDual` 的 `note` 仍写着「理解/分镜/拆集/提取主体几步的结论**仍不回流**」,
源级实测那四步**每一处都已接上 `memFeedback`/`memWrite`**。
原因是那张 `facts` 表(W36 立、W39 收紧)查的是「`note` 的**仍欠**段有没有点名这几处」,
**不查这几处是不是真的还欠**。所以「说做了没做」会红(W36 的设计目标),**「做了还说没做」完全无声**。

能力面这一轮换了方向:**`CHECKS` 全程 **17 条**一动没动、`preflightStages()` 全程七面、`gaps()` 全程 20 键、
短名单 30 条全程 **0 pending**——周期 6 零新校验项。** 增量全部落在**贯通面**:
提示词注册表 10 键 → **14 键**(W49 `agent.system`、W51 三份多轮人设),
G-12 两半合拢(W46 计划步骤投影 + W52 中段流程模板),记忆播种/迁移与问题中心补上 headless 出口(W53、W59),
发布留痕命令化(W58)。这与 `AGENTS.md` 里「发展重点是内部各模块的贯通」一致,是对的方向;
但它同时意味着**头号数字(unit 用例数)与 pending 两个指标在本周期都不再有区分度**。

`master` 上 `js/skills.js` 与 `docs/skills-wave/` **仍然都不存在**(`js/prompts.js` 在,`docs/` 下只有两份指南)。
周期 1–5 五份核验报告最重要的那条结论,在周期 6 结束时**第六次逐字成立**——
81 条远端分支、六个周期、17 条校验项,在主干上一件都不可见,最深的一支已领先 **252** 个提交。

## 2. 独立复跑对账(全部数字为本槽实测)

在每支的独立 `git worktree` 上跑 `node tests/unit.js`、`node tests/integration.js`、`node tests/cli.smoke.js`。

| 分支 | unit 实测 | README 自称 | integration 实测 | README 自称 | cli.smoke 实测 | README 自称 | 对账 |
|---|---|---|---|---|---|---|---|
| `master`(基线) | **201/201** | 163 ✗ | **79/79** | — | **51/53** | — | 基线(unit 差 **38**,主干既有,第六个周期) |
| `w47-integration`(周期 5 收槽) | **375/375** | 375 ✅ | **93/93** | 93 ✅ | **62/64** | 64 ✅ | ✅ |
| `w49-agent-system-prompt` | **376/376** | 376 ✅ | **93/93** | 93 ✅ | —(未跑) | 64 | ✅ |
| `w50-integration` | **380/380** | 380 ✅ | **93/93** | 93 ✅ | **62/64** | 64 ✅ | ✅ |
| `w51-browser-agent-personas` | **381/381** | 381 ✅ | **93/93** | 93 ✅ | **62/64** | 64 ✅ | ✅ |
| `w52-g12-mcp-mid-templates` | **388/388** | 388 ✅ | **93/93** | 93 ✅ | **70/72** | **64** ✗ | ⚠️ **冒烟数差 −8**(失真起点) |
| `w53-memall-headless-seed` | **386/386** | 386 ✅ | **105/105** | 105 ✅ | **70/72** | 72 ✅ | ✅ |
| `w54-issues-unreviewed-sk19` | **383/383** | 383 ✅ | **93/93** | 93 ✅ | **62/64** | 64 ✅ | ✅ |
| `w55-integration` | **389/389** | 389 ✅ | **93/93** | 93 ✅ | **70/72** | **64** ✗ | ⚠️ **冒烟数差 −8**(继承 W52,**集成 tip**) |
| `w56-aiscript-digest-prompt` | **382/382** | 382 ✅ | **93/93** | 93 ✅ | —(未跑) | 64 | ✅ |
| `w57-integration` | **398/398** | 398 ✅ | **105/105** | 105 ✅ | **78/80** | 80 ✅ | ✅ 冒烟数在此被改对 |
| `w58-sk25-publish-cmd`(旁支) | **394/394** | 394 ✅ | **106/106** | 106 ✅ | **80/82** | 82 ✅ | ✅ |
| `w59-issues-umd`(旁支) | **392/392** | 392 ✅ | **93/93** | 93 ✅ | **70/72** | **64** ✗ | ⚠️ **冒烟数差 −8,现仍挂着** |
| `w60-integration`(**集成 tip**) | **400/400** | 400 ✅ | **105/105** | 105 ✅ | **78/80** | 80 ✅ | ✅ |
| `w61-sk26-front-writeback`(进行中) | **398/398** | 398 ✅ | **105/105** | 105 ✅ | **78/80** | 80 ✅ | ✅ 数字对;见 4.11 |

**零失败项**:十五支的 unit / integration 全部 0 FAIL。

**unit / CHECKS / 提示词条数三个受契约保护的数字:14 支全对,零失真**——这是连续第二个周期零失真。
**未受契约保护的 `cli.smoke` 数字:14 支里 3 支错**(`w52`/`w55`/`w59`,一律 −8)。
这个对比本身就是结论:**同一批作者、同一批文档,受断言保护的数字零错、不受保护的数字 21% 错。**

**`cli.smoke` 的 2 项失败是 `master` 基线态,已第六次独立证实。** 本槽在未改动的 `master` 上直接跑,
得到与各支**逐字同名、同表现**的两项:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

本槽在 `master` 与 `w47`/`w50`/`w55`/`w57`/`w60`/`w58`/`w59`/`w61` **共 9 支上逐支抽出 `^FAIL` 行比对,九支完全一致**。
各支「不是回归、未删测换绿」的说法成立;**这两项已连续六个周期挂在同一位置**。
`w55` 那一支 70/72 与 `w60` 那一支 78/80 的失败项也是同这两条,**新增的 8 / 16 条冒烟全部通过**。

### 2.1 注册表口径实测(直读数据,不采信文档)

| 分支 | `CHECKS` | README 架构框 | 差 | pending 条/面 | `preflightStages()` | `gaps()` 键 | `KB.SECTIONS` | `Prompts` 键 | `playbooks()` |
|---|---|---|---|---|---|---|---|---|---|
| `w47`(周期 5 收槽,基线) | 17 | 十七条 | ✅ | **0 / 0** | 七面 | 20 | 18 | 10 | 5 |
| `w49-agent-system-prompt` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | **11** | 5 |
| `w50-integration` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 11 | 5 |
| `w51-browser-agent-personas` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | **14** | 5 |
| `w52-g12-mcp-mid-templates` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 11 | 5 |
| `w53-memall-headless-seed` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 11 | 5 |
| `w54-issues-unreviewed-sk19` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 11 | 5 |
| `w55-integration` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 14 | 5 |
| `w56-aiscript-digest-prompt` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 11 | 5 |
| `w57-integration` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 14 | 5 |
| `w58-sk25-publish-cmd` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 14 | 5 |
| `w59-issues-umd` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 14 | 5 |
| `w60-integration`(tip) | 17 | 十七条 | ✅ | **0 / 0** | 七面 | 20 | 18 | **14** | 5 |
| `w61-sk26-front-writeback` | 17 | 十七条 | ✅ | 0 / 0 | 七面 | 20 | 18 | 14 | 5 |

「差」那一列本周期 **14/14 全对**。

**这张表最该被读出来的是它有多平。** `CHECKS`、面表、`gaps()`、`KB.SECTIONS`、`playbooks()`、pending
**六列在十四支上一个数字没变**。唯一动的是 `Prompts` 键(10 → 11 → 14)。
即**周期 6 在注册表口径上只前进了一件事:人设句收编收尾**。

`Prompts` 系统人设键的收编链(实测 `Prompts.list().map(x => x.key)`):
`10 键 → +agent.system`(W49)`→ +agent.panelSystem / agent.drawerSystem / agent.previsSystem`(W51,一次 +3)。
W56 是**复用** `extract.system` 而非新开键(本槽实测 `w56` 与 `w52`/`w53`/`w54` 同为 11 键),
它自己的记账件与 W60 的记录都如实写了「注册表条数不变」,**本槽复核成立**。

至此 14 键里 13 个是 `*System` / `*.system` 形态的人设键,单轮与多轮、浏览器与服务端的人设句全部在注册表内。
**这是一条真正做完的线**,也是周期 6 唯一一条能用注册表数字看出来的线。

## 3. 分支拓扑:集成槽 7/9 取 head,功能槽 9/9 在集成线 head 上

```
master 9adcf0f
  └─ …周期1–4… ── w47-integration 8366727「整合(4/4)」+ 记账件   ◀ 周期5 收槽(W48 报的是 9af01e7 那一版)
       │    └─ … ── w49-agent-system-prompt a3960a6            ◀ 基线 = w47 head ✅
       └─ b86ab6e(合 w46 head f3c4611)
            └─ 81baf4a(合 w49 中间点 264f9cf) ── w50-integration 371c75e(合 w49 head a3960a6)◀ 当轮自纠 ✅
                 ├─ … ── w51-browser-agent-personas f46a360   ◀ 基线 = w50 head ✅
                 ├─ … ── w52-g12-mcp-mid-templates 12607c6     ◀ 基线 = w50 head ✅
                 ├─ … ── w53-memall-headless-seed 0045962      ◀ 基线 = w50 head ✅  head 未进集成线 ❌
                 │         └─ 09887be(中间点,W57 合的是这一版)
                 ├─ … ── w54-issues-unreviewed-sk19 bb0e9af    ◀ 基线 = w50 head ✅
                 ├─ … ── w56-aiscript-digest-prompt 6f9df87    ◀ 基线 = w50 head ✅
                 └─ c96beaa(合 w48 head)── f34cffb(合 w51 head)── 7f75f0d(合 w52 head)
                      └─ w55-integration 3968658
                           ├─ … ── w58-sk25-publish-cmd e842dfb  ◀ tip 1,基线 = w55 head ✅
                           ├─ … ── w59-issues-umd 97e6784        ◀ tip 2,基线 = w55 head ✅
                           └─ 61e60a9(合 w54 head)── 9fab31c(合 w53 中间点 09887be ❌)
                                └─ w57-integration 450c29f
                                     ├─ … ── w61-sk26-front-writeback 8b63f7f  ◀ tip 3,基线 = w57 head ✅
                                     └─ e876a4e(合 w56 head)── w60-integration 4967ae9  ◀ 集成线 tip
```

### 3.1 集成槽:9 次合并 7 次取 head,`--no-ff` 语义 9/9 成立

本槽逐条实测每个「整合」提交的 `parent2`,与被合入支**当时的 head** 比对;
并逐条验 `git merge-base --is-ancestor parent1 parent2` 为**假**(即都是真三方合并、不是本可 fast-forward 的伪合并):

| 集成波 | 合入的分支 | `parent2` | 是该支 head? | `--no-ff` |
|---|---|---|---|---|
| W50(三次) | `w46-g12-playbook-proj` | `f3c4611a` | ✅ = head | 真三方合并 |
| | `w49-agent-system-prompt` | `264f9cf1` | ❌ 中间点(head 后还有 1 提交) | 真三方合并 |
| | `w49-agent-system-prompt`(补) | `a3960a6b` | ✅ = head | 真三方合并 |
| W55(三次) | `w48-cycle5-audit` | `05836eeb` | ✅ = head | 真三方合并 |
| | `w51-browser-agent-personas` | `f46a3601` | ✅ = head | 真三方合并 |
| | `w52-g12-mcp-mid-templates` | `12607c65` | ✅ = head | 真三方合并 |
| W57(两次) | `w54-issues-unreviewed-sk19` | `bb0e9af4` | ✅ = head | 真三方合并 |
| | `w53-memall-headless-seed` | `09887be8` | ❌ **中间点(head 后还有 1 提交,至今未补)** | 真三方合并 |
| W60(一次) | `w56-aiscript-digest-prompt` | `6f9df876` | ✅ = head | 真三方合并 |

**`--no-ff` 那一项 9/9 成立**,九次全部留下可 revert 的合并提交,无一是伪合并。

**「取 head」那一项 7/9。两次未取 head 的严重度差别极大,不能并列**:

- **W50 那次当轮自纠,零代价。** `81baf4a` 合的是 `264f9cf1`,其后 `w49` 还有 1 个提交
  (`a3960a6` 记账件 + 索引);同一槽内 `371c75e` 又合了一次,取的正是 head。
  W50 的记账件把这件事写明了(「它的记账件又是在本槽第三次推送之后才补上来的——两次都是靠
  『每推一版重做一次包含性实测』的口径捞回来的」),**本槽复核属实,这是一次教科书式的自纠**。
- **W57 那次至今未补,有实质代价。** `9fab31c` 合的是 `09887be8`,其后 `w53` 还有 1 个提交
  `00459625`——**那是一个 143 行的记账件 `docs/skills-wave/w53-memall-headless-seed.md` + 索引登记**。
  本槽实测 `git merge-base --is-ancestor 00459625 origin/cursor/w60-integration-edd3` **为假**:
  **该记账件至今不在集成线上。** 详见 3.2。

**周期 3 报告的阻塞 3(集成方合并未取 head)在连续两个周期不复发之后,本周期复发两次。**
W50 那次靠它自己立的口径捞回来了;W57 那次说明**口径写在记账件里是不够的**——
它没有可执行的落点(第 8 节)。

### 3.2 W57 丢的那份记账件:失真、代价与它为什么全绿

时点逐条实测(commit 时点;push 时点仓库不可查,本槽不据此判动机):

| 时点 | 提交 | 事件 |
|---|---|---|
| 19:48:36 | `b76ee04` | `w53` 功能提交 |
| 19:57:02 | `09887be` | `w53` 测试提交(**W57 合的就是这一版**) |
| **20:03:44** | **`0045962`** | **`w53` 记账件 + 索引登记(143 行,`w53` 的真 head)** |
| 20:17:17 | `9fab31c` | W57 合入 `09887be`(**晚于记账件 13 分 33 秒**) |
| 20:24:21 | `450c29f` | W57 记账件写下「**W53 没有留下自己的记账件**」(**晚 20 分 37 秒**) |
| 20:44:39 | `4967ae9` | W60 记账件**原样重申**该结论(**晚 40 分 55 秒**) |

W57 记账件第 2 节的候选表把 `w53` 的 outstanding 记成 **2**;本槽实测
`git rev-list --count 371c75e2..09887be8` = **2**、`..00459625` = **3**。
**即 W57 的 `fetch` 确实早于 20:03:44,它量到的 2 是当时的真数。** 问题不在那次测量,
在它**没有按 W50 立的口径复测**,并且把一个时点结论写成了无限定的事实判断。

**失真的落点是三处散文**,本槽逐处直读 `w60` tip:

1. `docs/skills-wave/w57-integration-log.md` 第 5 行与第 6 节:「**W53 没有留下自己的记账件**,故它那部分的落点与取证由本文代记」(2 处命中);
2. `docs/skills-wave/README.md` 记账诚实位:「该槽远端 tip 是完成态但**没有留下自己的记账件**,故本行没有可指的 `w53-*.md`」(1 处命中);
3. `docs/skills-wave/w60-integration-log.md` 第 8 节:「W53 那处没有记账件的缺口**仍如实留在**……本槽不补造一份 `w53-*.md`」。

**同一份 README 还在同时给 W53 记功**:本槽实测 `w60` 的 `README.md` 里
「`tests/integration.js` — ……**W53 扩至 105 项断言**」「`tests/cli.smoke.js` — ……**W53 扩至 80 项断言**」。
**即主干文档一边把两个测试数字的增量署名给 W53,一边断言 W53 没留下记账件。**

**为什么全绿:** 那 143 行文件与它那行索引是**同一个提交**里的,一起没进来。
索引契约(`contract · docs/skills-wave 索引与目录实况双向对齐`)比的是两个**集合是否相等**,
不是「合进来的槽是否都有记账件」。两边一起少,集合依然相等。
本槽实测 `w60` 索引 76 行 / 目录 76 份、**双向零差**;`w55`/`w57` 同为零差。

**决定性变异证明见第 6 节变异 4/5/6:单加索引行会红、单加文件会红、两个一起删全绿。**

**代价:** 一份 143 行的设计记录(为什么不做成"读记忆时自动播种"、板块词表为什么由调用方注入、
空板/未知板名为什么报错、幂等怎么让调用方省掉写盘、与 SK-26 回流面的边界、五条变异实测)
在集成线上不可见,而 `README.md` 与索引里指向它的位置写着"它不存在"。
**按本目录「记账件是给下一个接手的人看的」这条用途,这是本周期唯一一处实质损失。**

**修复成本极低,本槽实跑验证过**(只读克隆,不落提交到本仓库引用):
`w60 ← w53` **冲突 2 处、全在 `docs/skills-wave/README.md`**(索引表一处、记账诚实位一处),
143 行的记账件**自动合入无冲突**。按「索引行按波次序并入 + 记账诚实位取集成线侧」机械解开后
**400/400 全绿**,文件与索引行同时到位。

**但本槽必须同时报告一件事:解完之后那三处散文里的假话仍然在,而且测试仍然全绿。**
本槽在合并后直接 grep:`docs/skills-wave/README.md` 仍有 1 处、`w57-integration-log.md` 仍有 2 处
「没有留下自己的记账件」,`node tests/unit.js` **400/400**。
**即合并前后两种状态都全绿,而其中一种的散文与同目录下明摆着的那个文件直接矛盾。**
这正是本轮必核项 6 要找的那一类,而且是它最难看的一种形态。

### 3.3 功能槽:9/9 在集成线 head 上——周期 6 最实的改善

| 功能槽 | 基线实测 | 形态 |
|---|---|---|
| `w49-agent-system-prompt` | `8366727d` = **`w47` head** | ✅ 集成线 head |
| `w51-browser-agent-personas` | `371c75e2` = **`w50` head** | ✅ 集成线 head |
| `w52-g12-mcp-mid-templates` | `371c75e2` = **`w50` head** | ✅ 集成线 head |
| `w53-memall-headless-seed` | `371c75e2` = **`w50` head** | ✅ 集成线 head |
| `w54-issues-unreviewed-sk19` | `371c75e2` = **`w50` head** | ✅ 集成线 head |
| `w56-aiscript-digest-prompt` | `371c75e2` = **`w50` head** | ✅ 集成线 head |
| `w58-sk25-publish-cmd` | `3968658e` = **`w55` head** | ✅ 集成线 head |
| `w59-issues-umd` | `3968658e` = **`w55` head** | ✅ 集成线 head |
| `w61-sk26-front-writeback` | `450c29f8` = **`w57` head** | ✅ 集成线 head |
| —— | —— | —— |
| `w46-g12-playbook-proj`(周期 5 晚推槽) | `b86ead50` = **`w44` head**(功能槽 head) | ❌ 周期 5 形态,见下 |

**9/9,零例外。周期 5 是 2/10。** 五支同取 `w50` head(`w51`/`w52`/`w53`/`w54`/`w56`)说明
**同一时点开槽的五个并行槽拿到了同一个基线**——周期 5 报告担心的「开槽者要在每一个时点判断现在哪一支覆盖最多」
这件事,本周期实际上没有出错。

**`w46` 是唯一的反例,而它属于周期 5。** 基线 `b86ead50` = `w44` head,是周期 5 那种「以另一个功能槽的 head 为基线」;
时点上 `w44` head 是 18:32:13、`w47` 四步收槽是 18:50:51、`w46` 的功能提交是 18:52:08——
**即 `w46` 开槽时集成线还没收完,它取的是当时能拿到的最深功能槽**。这条**不计入周期 6 的 9/9**,
但它的代价为零:W50 取它的 head 合入,`w46` 的成果完整进了集成线。

**必须说清的是这个 9/9 靠什么维持的。** 本槽实测:

- `docs/skills-wave/README.md` 里搜「当前集成线 / 集成线 `head` / 指针 / 基线取」→ **仅 1 处命中,是 W48 核验件那一行的摘要文字**,**没有任何指向当前集成线 head 的固定条目**;
- `tests/*.js` 里搜「集成线 / 祖先 / `is-ancestor`」→ **零命中**。

**周期 4 提出、周期 5 判定为「唯一能同时压掉三条阻塞」的那件事,第三个周期一个字没做。**
9/9 是人守出来的。而同一周期里**集成槽那一侧的「取 head」就退步了两次**(3.1)——
**同一条纪律,在有人自纠的槽上守住了,在没人复测的槽上丢了一份文件。**

### 3.4 三条在外的 tip:互不重叠,合流成本极低

本槽在只读克隆里逐条 `git merge-tree --write-tree` 数冲突块:

| 步 | 合并 | 冲突块 | 落点 |
|---|---|---|---|
| — | `w60 ← w53`(**补回丢掉的记账件**) | **2 处** | `docs/skills-wave/README.md` 2 |
| 1 | `w60 ← w61` | **0 处** | —(零冲突) |
| 2 | `w60 ← w58` | **6 处** | `README.md` 3、`docs/skills-wave/README.md` 2、**`tests/integration.js` 1** |
| 3 | `w60 ← w59` | **8 处** | `README.md` 2、`docs/skills-wave/README.md` 1、**`js/issues.js` 4、`tests/unit.js` 1** |

**三条 tip 无一是另一条的超集,但三条互不冲突**:`w58` 动发布留痕(`js/release-core.js` 新增 + 命令注册表),
`w59` 动问题中心 UMD 化(`js/issues.js`),`w61` 动前段四步回流(`js/wf-core.js` + 四个消费点)。
`w60 ← w61` 零冲突这一点本槽实跑确认过(见 4.11,**这恰恰是它的风险所在**)。

## 4. 逐项目核验

以下每支单列:增量、本槽实测证据、阻塞。成熟度一律先给分支内自洽的等级,再给主干等级。
**主干等级全部为 M0**(`master` 上 `js/skills.js` 不存在),下文不再逐条重复。

### 4.1 W49 · `w49-agent-system-prompt-96c0` — Agent 单轮人设收编两端

**成熟度 M3(分支内自洽全绿)。** 增量:`Prompts` 新键 `agent.system`(10 键 → **11 键**),unit 376/376。
**基线取 `w47` head ✅。**

本槽实测该键在 `Prompts.list()` 里;`WfCore.buildAgentSystem(ctx, ov)` 开始收覆盖表参数,
服务端 `/api/wf/agent` 显式传 `settings.promptOverrides`。
它**只收人设句、有意不收 ops 协议半**这一点在 `note` 与索引摘要里都写明了理由
(ops 协议是 `agentNormalize` 的解析契约,做成可覆盖变量会被用户改坏),
并由「覆盖只换人设句、协议半逐字节不变」一条断言钉住——**这是一次把"有意不做"写清楚的记账**。

**阻塞**:无未闭项(已随 W50 进集成线,两步:先合中间点、再补合 head)。

### 4.2 W50 · `w50-integration-dad5` — 必核项:W46 + W49 收敛,并立下「每推一版重做一次包含性实测」

**成熟度 M3。** 增量:三次合并(`w46` head、`w49` 中间点、`w49` head),unit 380/380,`Prompts` 11 键。

**这一支是周期 6 治理面最有价值的一个槽,理由与它合了什么无关。** 它的记账件第 2.4 节立下的口径:

> 每推一版就重做一次包含性实测,直到某一次 `fetch` 后清单为空为止。……
> **「不存在」是一个时点结论而不是终局结论**……**同一条分支合过一次也不等于合完了:它很可能还欠一笔记账件提交。**

本槽逐条复核这条口径在本槽自己身上**真的执行了**:`w48-cycle5-audit-*` 前缀在开工与五次复核时都不存在(本槽实测 W55 才合到它,属实);
`w49` 开工时不存在、第一次推送后才出现、**记账件又是第三次推送之后才补上来的**——
对应实测:`81baf4a` 合 `264f9cf1`(功能提交)、`371c75e` 合 `a3960a6b`(记账件),**两次合并的 SHA 与它的叙述逐条对上**。

**它还立了另外三条本槽复核成立的口径**:冲突两侧「一侧对、一侧是分叉时旧实况」的判据是**数字往回退的那侧一定不对**;
并入会让**非冲突区散文**过期而这类没有断言兜底;按实况手写的摘要句等作者补推记账件后要换成作者原话。

**阻塞**:无未闭项。**但本槽必须指出一处方法论上的空缺**:这四条口径**只写在记账件的散文里,没有任何落点**——
没有进 `AGENTS.md`、没有进 `docs/skills-wave/README.md` 的固定位置、没有断言。
**W57 七分钟后踩的正是第一条,而 W60 记录的「折回散文字节级核对」踩的是第三条的延伸。**

### 4.3 W51 · `w51-browser-agent-personas-da99` — 浏览器多轮三份人设收编

**成熟度 M3。** 增量:`Prompts` 一次新增 **3 键**(`agent.panelSystem` / `agent.drawerSystem` / `agent.previsSystem`,
11 键 → **14 键**),unit 381/381。**基线取 `w50` head ✅。**

本槽实测三键都在 `Prompts.list()` 里,装配口分别是 `AgentCore.panelSystem`、`AgentG.buildGlobalPrompt`、
`agent-ops.js` 的 `prearrPrompt`。**它有一处如实降级的记账值得记**:三键**只有浏览器一个消费点**
(headless 多轮不存在),故它把「两端」如实记成落在**取值口**而非两个消费点——
**没有为了凑"双端单源"的说法而虚报第二个消费点**,`note` 与索引摘要都写明了。

至此单轮与多轮的人设句全部在注册表内,`Prompts` 14 键在 `w55` 之后全周期未再变动。

**阻塞**:无未闭项(已随 W55 进集成线,取 head)。

### 4.4 W52 · `w52-g12-mcp-mid-templates-d660` — G-12 另一半:中段流程模板

**成熟度 M3。** 增量:`js/flow-tpl.js` 主线中段(主体/分集/分镜/生成)可调用流程模板 + `flow` 套件,
unit 388/388(净 +7 用例,本周期功能槽最高),cli.smoke **72**(净 +8)。**基线取 `w50` head ✅。**

本槽实测 `flow` 套件的判据很硬:四段分开取再拼接**逐字节等于整段**(切片不重不漏)、
断点码**逐个在命令层/领域层源码里存在**(不自造错误码)、文本渲染的工具名**由调用方注入**且模块内零工具名字面、
待办标注取 `Domain` 实况且 `clear` 不冒充"这一步做过了"。

**阻塞(本周期第二处实质失真的起点)**:**它加了 8 条 `cli.smoke` 用例,README 的冒烟数字仍写 64。**
本槽实测该支 `node tests/cli.smoke.js` 回 **70/72**,README 自称 `二十四轮扩至 64 项断言`,**差 −8**。
**这处失真不是作者疏忽被契约漏掉,而是契约根本不覆盖这个数字**:
本槽在 `tests/unit.js` 里 grep `cli.smoke` **零命中**,并用变异 7 把 `w60` 的该数字从 80 改成 64,
**400/400 全绿**(第 6 节)。该失真随 W55 继承、在 W57 被冲突解法顺手改对、**在 `w59` 上活到现在**。

### 4.5 W53 · `w53-memall-headless-seed-3653` — 记忆播种/板块迁移双端单源 + headless 入口

**成熟度 M3;`w60` 上**功能可见、记账件不可见**(见 3.2)。**
增量:`WfCore.memSeed` / `memMigrateBoard` 双端单源,浏览器 `memAll` 改为委托,
headless 补 `POST /api/wf/memory-seed` + CLI `memory seed|migrate` + 两个同名 MCP 工具;
unit 386/386、**integration 105/105(净 +12,本周期最大的集成测试增量)**、cli.smoke 72(净 +8)。
**基线取 `w50` head ✅,README 三个数字全部对上 ✅。**

本槽实测它的判据覆盖了"不静默空成功"这一类最容易糊过去的地方:
空板 / 未知板名 / 迁移零命中**一律抛错**、幂等不重复种、迁移**条目不丢不双写**(条目数不变、旧板名清零、正文原样)、
播种保持**显式动作**(读记忆时不自动跑,免得读一次写一次盘)、全程**零计费**。
源级另有断言钉住种子表只在 `wf-core` 一份(`agent.js` 再内联即红)。

**阻塞:它是本周期唯一一支「远端 tip 完成、成果只进了一半」的分支,而且不是它的问题。**
它的 head `00459625`(143 行记账件 + 索引登记)**至今不在集成线上**,
且集成线的三处散文写着「该槽没有留下自己的记账件」。**归因在 W57(见 3.2),不在本支。**
本槽在此明确记一句:**`w53` 按本目录的纪律做完了它该做的四件事**(功能、测试、README 同步、记账件 + 索引登记),
**四件齐全**,不存在"欠记账件"的情况。

### 4.6 W54 · `w54-issues-unreviewed-sk19-cfc0` — 问题中心补未审/判旧两类投影

**成熟度 M3。** 增量:`no-review` 与 `review-stale` 两条中危投影 + SK-19 稳定词面接进问题中心,
unit 383/383。**基线取 `w50` head ✅。**

本槽实测:判旧那条与发布门 G3「视为未审」对齐、三态(未审/判旧/低分)**互斥同集只报一条**、
**判据一行不新写**(现取 `Domain.episodeState`),并有一条源级断言
`assert(!/reviewStaleByScript|lastReview\.avg/.test(isrc))` 钉住"问题中心不写第二份判定"。
稳定词面挂低危 warn、**不抬成 G2 fail**。这一支闭合的是 SK-23 `note` 点名的余量,
本槽核对 `w60` 上 SK-23 的仍欠段已换成「报告好坏优劣的语义面(方法论门那一半)仍待 G-10」,**换得对**。

**阻塞**:无未闭项(已随 W57 进集成线,取 head)。

### 4.7 W55 · `w55-integration-8f21` — 必核项:W48 + W51 + W52 收敛

**成熟度 M3。** 增量:三次合并(`w48`/`w51`/`w52` **三个 head**),unit 389/389,`Prompts` 14 键。
**三次合并全部取 head ✅,`--no-ff` 3/3 ✅。**

它把 W48 周期 5 核验件合了进来(`c96beaa`,取 head `05836ee`)——
本槽核对该文件在 `w60` 上完整存在且索引有登记,**周期 5 报告本身没有在集成时丢失**。

它对 `w53`/`w54` 的处置**如实标了时点限定词**:「开工时前缀无匹配……三条合完之后再 `fetch` 才出现……
按任务口径**本槽不合**,留给下一个集成槽」。本槽实测两支的 commit 时点(19:48 / 19:55)与 W55 收槽(19:59)吻合,
**这是一次合规的时点声明**(与 W57 那处无限定的事实判断形成直接对照)。

**阻塞:它的 README `cli.smoke` 数字错 −8(自称 64、实跑 72),而它是一条集成线 tip。**
成因是继承 W52(4.4),不是它自己引入;但**集成槽的职责本就包含"README 三个数字按实跑重算"**,
这一处漏了,而且**没有任何断言会提醒它**。

### 4.8 W57 · `w57-integration-a697` — 必核项:W54 + W53 收敛;本周期唯一一处实质损失

**成熟度 M3(已合部分自洽全绿);槽本身有一处未收敛。**
增量:两次合并,unit 398/398、integration 105/105、cli.smoke 78/80。
**`--no-ff` 2/2 ✅;取 head 1/2 ❌。**

**做得好的部分本槽逐条复核成立,而且水准很高。** 它的记账件第 4 节那张三行 `facts` 表的取侧判断:

| 行 | 取谁 | W57 给的理由 | 本槽复核 |
|---|---|---|---|
| `core.personaCtx` | `w55` 侧 | 浏览器多轮三份人设已随 W51 收编,`w53` 侧锚点是分叉时旧实况 | ✅ 属实 |
| `core.memoryDual` | **`w53` 侧** | `memSeed` 是本槽刚落地的出口,锚点必须改指自动沉淀那一半 | ✅ 属实 |
| `review.stage` | `w55` 侧 | 未审片投影已随 W54 落地,`w53` 侧锚点指的是已补掉的余量 | ✅ 属实 |

**三行逐行取侧、三行都取对了**,而这张表「两侧都是完整三行、`--ours`/`--theirs` 跑起来都绿而内容都不完整」
——**这正是 W48 报告点名的那处唯一可静默解错的合并点,W57 把它解对了,还把判据写下来了。**
它另一处 `cli.smoke` 数字的判断也很硬:「**两侧同为 72 却都不对**:两条分支各自独立加了 8 条,
基线 64 + 8 = 72 两边都成立,合到一起才是 80」——本槽实测 live 80,**属实**;
顺手把 W52/W55 那处 −8 的失真改对了。

**阻塞(本周期最重的单支问题)**:① 合 `w53` 取了中间点 `09887be`,**漏掉它的 head `0045962`——
一份 143 行的记账件 + 索引登记,至今不在集成线上**;
② 基于①,它在三处散文里写下「**W53 没有留下自己的记账件**」这个**与实况相反的事实判断**,
时点上晚于该文件出现 20 分 37 秒,且**没有加"开工时"这类限定词**(与它同槽合的 W54、以及 W55/W60 的写法都加了);
③ W50 立的「每推一版重做一次包含性实测」口径未执行。**逐条证据、时点表与修复成本见 3.2。**

**本槽对这一支的判定**:它在**最难的地方(逐行取侧)做对了,在最容易的地方(再 `fetch` 一次)漏了**。
这不是能力问题,是**那条口径没有可执行落点**——它只是上一个槽记账件里的一段散文。

### 4.9 W56 · `w56-aiscript-digest-prompt-e8c8` — 剧本摘要人物小传步人设改经注册表

**成熟度 M3。** 增量:`EpisodeUtil.aiScriptDigest` 第五步的人设改经 `Prompts.get('extract.system')` 同键取值,
同字面内联归零,unit 382/382。**基线取 `w50` head ✅,`Prompts` 条数不变(11 键)✅。**

**它对"复用键还是新开键"给了三条判据**,本槽复核成立:字面同 / 角色同(都是"剧本分析助手"从剧本里认人)/
产物落点同(都往 `p.subjects` 写 `character` 条目并过同一道可信性校验)。
**它另有一处有意的不做**:只取人设句、**不接方法论段**——理由是「主体参考」那条 KB 正文讲的全是生图与参考图组纪律,
而该步不生图。本槽直读 `KB.pick('主体参考').length` = **278**,与 W60 记录里引的字数一致。

W45 为它留的「该字面在 `js/episode-util.js` 恰好 1 处」计数路障**反转为 0**,
本槽复核这不是抬松:期望"出现 0 次"比"恰好 1 次"更严(退回内联即红),且同一用例另加了「注册表命中恰好一条」的正向判据。

**它还点名了一处先于它存在的记账不实**(`SK-11`/`SK-10` 的 `note` 仍写「人设句入注册表待 G-13」,
而那句人设在 W45 就已进注册表)。本槽复核**属实且至今未改**:`w60` 上 `SK-11` 的 `gaps` 仍为 `['G-13']`。
W60 把它「转登记」了一次(见 4.10),**但两个槽都没有人接**。

**阻塞**:无未闭项(已随 W60 进集成线,取 head)。

### 4.10 W60 · `w60-integration-edd3` — 必核项:W56 收敛;集成线 tip

**成熟度 M3。** 增量:一次合并(`w56` head ✅),unit **400/400**、integration 105/105、cli.smoke 78/80。
**`--no-ff` 1/1 ✅,取 head 1/1 ✅,README 三个数字全部对上 ✅。**

**它留下的两件方法面的事,本槽复核都成立且都有独立价值**:

1. **折回对侧散文时"内容对"不等于"字节对"。** 它记录两处折回的散文里中文引号被抄成全角 `“”`
   而作者原文是半角 `""`,字数相同、语义相同、通读看不出、`git diff` 也不显眼,
   靠按首尾锚点切段逐字节比对才捞出来。**这一类既没有断言兜底也不会被通读发现,是本目录里第一次有人把它量出来。**
2. **`facts` 表"这次没有取侧动作"这个结论本身是实测出来的,不是"git 没报冲突"推出来的。**
   它先直读确认 `w56` 零改动 `js/skills.js`,再把表的三行锚点与合入后 `note` 的仍欠段逐行对了一遍。
   **本槽独立复跑了这一遍**,三行与 live `note` 的仍欠段**逐条对上**:

| 行 | 表里的锚点 | 本槽直读 live `note` 的仍欠段 | 对上? |
|---|---|---|---|
| `core.personaCtx` | `['ops 协议', '不开放覆盖']` | 「四处的 ops 协议/字段面/命令白名单/返回 JSON 约定仍由各自装配口拼、**不开放覆盖**」 | ✅ |
| `core.memoryDual` | `['理解/分镜/拆集/提取主体', 'SK-26']` | 「自动沉淀本轮结论只有审片/发布两个闭环(那一面归 **SK-26** 的回流面),**理解/分镜/拆集/提取主体**几步的结论仍不回流」 | ✅ |
| `review.stage` | `['SK-24', 'G-10']` | 「校验面已随 **SK-24** 落地,而报告好坏优劣的语义面(方法论门那一半)仍待 **G-10**」 | ✅ |

它的数字取证方式也是对的:`unit` 那一个由契约现算(合完先红、改完即绿),其余六个直读注册表现取,
连折回散文里出现的「278 字」都现取验过。

**阻塞**:① **它把 W57 那句假话原样重申了一次**(第 8 节:「W53 那处没有记账件的缺口**仍如实留在**……
本槽不补造一份 `w53-*.md`」)。**本槽的判定是:「不补造一份」这个决定是对的**
(集成槽不该代写别人的记账件),**错的是没有先复核"它到底有没有"**——
真正该做的第三种选择是**再 `fetch` 一次、把 `w53` 的 head 补合进来**,成本是 2 处文档冲突(3.2)。
② `w56` 转登记的那处 `SK-11`/`SK-10` 记账不实**仍未有人接**。
③ 对 `w58` 的判定写的是「该前缀远端**零匹配**」,`w58` 首个提交 commit 时点 20:31:41、W60 收槽 20:44:39——
**本槽不判此处失真**(push 时点不可查,且该句写在「开工 `git fetch` 后」的语境里),
但它与 W57 那处是同一个机制,列在此处备查。

### 4.11 W61 · `w61-sk26-front-writeback-b09b` — 进行中;本周期唯一一处前向假绿风险

**成熟度:进行中,不评级。** 本槽的判定依据:该支只有 **1 个提交**(`8b63f7f`,20:49:02),
**没有自己的记账件**、**README/docs 零改动**、**没有新增任何断言**——
按本目录「功能 + 测试 + README 同步 + 记账件」四件齐全才算收槽的纪律,**它显然还没收槽**。
下文只报**实测事实与前向风险**,不作成熟度评分,也不作"该做没做"的指控。

实测增量:主线前段四步(理解/分镜/拆集/提取主体)闭环结论回流协作记忆,
改动 `js/wf-core.js`(+56/-4)、`js/understanding.js`、`js/sb-llm.js`、`js/proj-upload.js`、`js/commands.js`、
`server.js`、`cli.js` 共 **102 插入 / 11 删除**;unit 398/398、integration 105/105、cli.smoke 78/80,
README 自称 398 ✅(与其基线 `w57` 同数)。**基线取 `w57` head ✅。**

**它对 `tests/` 的全部改动只有两行,而且两行都是为适配新回流点而修既有断言,零新增断言**:

```
tests/integration.js:  sMg.filter(m => m.scope === '导演').length === 1
                    →  sMg.filter(m => m.text === '旧板名条目:夜戏偏冷色' && m.scope === '导演').length === 1
tests/unit.js:         srv.indexOf('tree.agentMemory = WfCore.memWrite(')
                    →  srv.indexOf('tree.agentMemory = WfCore.memWrite(', iLast)   // 从 lastReview 起找审片那一处
```

本槽实测 `assert*` 调用数:`w61` **2352**、其基线 `w57` **2352**——**逐个相同,净 0**。
两处都是合理的适配(新回流点使原先的"恰好一条导演条目"和"文件里第一处 `memWrite`"不再成立),
**但第一处确实把"导演板块恰好一条"这个不变量收窄成了"某条特定文本恰好一条"**。
**该支是进行中状态,断言与记账很可能还在路上,本槽不据此判它抬松断言。**

**真正要报的是前向风险,而且本槽实跑验证过。** `w61` 落地的正是 `core.memoryDual` 的 `note` 里
**点名的最后一处余量**(「理解/分镜/拆集/提取主体几步的结论仍不回流」),但它**一行没碰 `js/skills.js`**
(`git diff 450c29f8 origin/cursor/w61-* -- js/skills.js` **零行**),也没碰那张 `facts` 表。

本槽在只读克隆里实跑 `w60 ← w61`:

| 实测项 | 结果 |
|---|---|
| 冲突块 | **0 处**(完全自动合并) |
| 合入后 `node tests/unit.js` | **400/400 PASS, 0 FAIL** |
| 合入后 `core.memoryDual` 的 `note` 仍欠段 | 「……**理解/分镜/拆集/提取主体几步的结论仍不回流**;播种是显式动作……」 |
| 合入后源级实测那四步是否真的回流了 | **真的回流了**:`js/understanding.js` / `js/sb-llm.js` / `js/proj-upload.js` / `js/commands.js` **各 2 处** `memFeedback`/`memWrite` 命中 |

**即:零冲突、全绿、而注册表的 `note` 在说一件与代码相反的话。**

**机制原因(这是本周期最有方法论价值的一条)**:那张 `facts` 表的判据是

```
const owedText = (s.note || '').split('仍欠').slice(1).join('仍欠');
owed.forEach(k => assert(owedText.includes(k), id + ' 的 note 须在「仍欠」段里点名:' + k));
```

它查的是**「仍欠」段里有没有点名这几处**,**不查这几处是不是真的还欠**。
W36 立这条断言、W39 把它收紧成"只认「仍欠」之后那段",治的都是**「假清账」**
(把余量补完了却把锚点留在"已落地"那半蒙过去)。
**反方向的「假欠账」——余量真的补完了、`note` 还写着没补——它完全接不住,而且落地越顺利越容易发生:
`w61` 恰恰因为一行没碰 `js/skills.js`,才做到了零冲突。**

**这不是 `w61` 的过错**(它还没收槽,同步记账很可能就在下一个提交里),
**但它是一条"合并者不会收到任何提示"的路径**:如果下一个集成槽像 W60 对 `w56` 那样
"先直读确认对侧没碰 `js/skills.js`、于是判定 `facts` 表无需取侧",**结论会是对的而记账会是错的**——
因为这一次的问题不在表里,在**表所指向的那段 `note` 已经过期**。

## 5. 短名单 30 条与缺口投影

`SK` 条数实测 **30**(十四支全部),`kinds` 字段逐条无漂移。

**pending 在周期 6 全程为 0 条 / 0 面**——本槽逐支实测 `Skills.list().filter(s => s.pending.length)` 为空数组,
十四支无一例外。周期 5 报告的阻塞 3(「最后两条 pending 的关闭件都不在集成线上」)
**已由 `w47` 收完四步闭合**:`8400399` 合 W43(关 SK-26)、`682f3c8` 合 W44(关 SK-05),
本槽实测 `w47` head 上 pending 即为 0。**周期 6 是第一个全程零 pending 的周期。**

**但零 pending 不等于短名单做完,本槽实测仍有三处明确的覆盖余量**(直读 `note` 的仍欠段,原文见 4.10 那张表):

| 条目 | 仍欠什么 | 状态 |
|---|---|---|
| `SK-03` `core.personaCtx` | 四处装配口的 ops 协议/字段面/命令白名单/返回 JSON 约定不开放覆盖 | **有意不做**(理由已写明:是 `agentNormalize` 的解析契约) |
| `SK-04` `core.memoryDual` | 理解/分镜/拆集/提取主体几步的结论不自动回流 | **`w61` 正在做**;若合入而不同步 `note` 即成假欠账(4.11) |
| `SK-23` `review.stage` | 审片报告好坏优劣的语义面(方法论门那一半) | 待 G-10,**产品口径未定**(见 6.2) |

`pending` 字段的语义是「该机制面在主线上有没有出口」,不是「这条做完了」——
**这一点六个周期以来口径稳定**,本轮 W51(如实把"两端"记成落在取值口)、W56(如实点名先于它存在的记账不实)
两处都按这个口径写。

### 5.1 `gaps()` 20 键,口径已定;按 `pending` 过滤的未闭清单投影仍不存在

本槽实测 `w60` 上 `Object.keys(Skills.gaps())` = **20 键**:
`G-01…G-05`、`G-07…G-15`(14 个)+ `S-01`、`S-03`、`S-04`、`S-05`、`S-06`、`S-07`(6 个)。
十四支逐支相同,**周期 6 全程未变**。

**周期 4 第 6 步的后半句、周期 5 第 5 条药方——「补一条按 `pending` 过滤的未闭清单投影」——未做,第三个周期。**
本槽实测 `Skills` 的出口:

```
CHECKS, CROSS, KINDS, REG, STAGES, WAVES,
block, byId, check, covering, forExpert, gaps, list, playbook, playbooks, preflightStages, stageOf, stages, validate
```

**与周期 5 实测逐字相同,没有按 `pending` 过滤的投影。**
不过本周期这条药方的**价值已经变了**:pending 全程为 0,按 `pending` 过滤的投影现在会**恒回空**。
真正需要投影的已经不是 `pending` 而是**上表那三处 `note` 里的"仍欠"段**——
它们现在只能靠人读散文,而 4.11 已经证明散文会过期且过期无声。
**本槽建议把这条药方改写(见 8.3 第 3 条),不建议原样再提第四次。**

### 5.2 S-08 连续六个周期未决

本槽实测 `'S-08' in Skills.gaps()` = **false**,`Skills.REG.length` = **30**——
S-08(发布后→上游回路)**仍未进 30 条**,短名单第 7 节仍是「已登记编号、需决定做还是明确拒绝」。
`w43` 那条「为什么现在不挂在已清 `pending` 的条目上充数」的书面理由仍在索引摘要里,
**但"做还是明确拒绝"这个结论第六个周期没有。**

## 6. 机械合并假绿:三处实证 + 变异证明

这是本轮的必核项 6。**周期 5 报告把它列为"唯一一处可静默解错的合并点"(那处 `js/skills.js` 的 `note` 冲突),
本周期实测:那一处 W57 解对了,而假绿从另外三个方向各出现了一次,且全部有实证。**

### 6.1 三处实证

| # | 形态 | 实证 | 现状 |
|---|---|---|---|
| 1 | **记账件与索引行一起丢** → 双向契约仍零差 | `w53` head `0045962`(143 行记账件 + 索引)不在集成线上;集成线三处散文写「W53 没有留下自己的记账件」;`w60` 索引 76 行 / 目录 76 份**双向零差**、unit **400/400 全绿** | **仍存在**(3.2、4.8) |
| 2 | **未受契约保护的数字写错** → 无任何反应 | README `cli.smoke` 数字在 `w52`/`w55`/`w59` 上失真 **−8**(自称 64、实跑 72);`tests/unit.js` 里 grep `cli.smoke`/`integration.js` **零命中** | `w52`/`w55` 已被 W57 顺手改对;**`w59` 上仍挂着**(4.4、4.7) |
| 3 | **余量真的补完了、`note` 还写着没补**(假欠账) | `w60 ← w61` **零冲突**、合完 **400/400 全绿**,`core.memoryDual` 的 `note` 仍写「理解/分镜/拆集/提取主体几步的结论仍不回流」,而那四步源级各 2 处 `memFeedback`/`memWrite` 命中 | **前向风险,尚未发生**(4.11) |

**三处的共同点:测试全绿,而且是真的全绿——不是断言写松了,是这三件事根本不在任何断言的射程里。**

**三处的差别在于契约与它的距离**:
第 1 处**有断言但比错了东西**(比集合相等,不比存在性);
第 2 处**根本没有断言**;
第 3 处**有断言且方向反了**(只防假清账,不防假欠账)。
**这三种"离得多远"决定了各自的修法完全不同**(见 8.3)。

### 6.2 变异实测(全部在 `/tmp` 的副本上做,已删除;不落任何改动到本仓库)

以 `w60` tip(基线 **400/400 PASS, 0 FAIL**)为底,八路变异:

| # | 变异 | 结果 | 说明 |
|---|---|---|---|
| 1 | README unit 断言数 `400` → `399` | **红** `contract · README 数字对账:单元测试用例数由 tests/unit.js 实计` | 头号数字有断言 ✅ |
| 2 | 删掉「CHECKS 已落地十七条」整句 | **红** `架构框 CHECKS 条数:README.md 找不到该数字表述` | 删句绕过被堵 ✅ |
| 3 | README 提示词条数 `14` → `13` | **红** `注册表提示词数:README.md 与实测不符(实测 14,文档 13)` | 注册表口径有断言 ✅ |
| 4 | 只加一行 `w53` 索引(目录无该文件) | **红** `contract · docs/skills-wave 索引与目录实况双向对齐` | 索引指向空文件会红 ✅ |
| 5 | 只加 `w53` 文件(索引无该行) | **红** 同上 | 记账件漏登记会红 ✅ |
| 6 | **同时删掉 `w56` 记账件 + 它那行索引** | **绿 400/400** | ❗ **两边一起少,双向契约看不见——这正是 `w53` 的形态** |
| 7 | README `cli.smoke` 数字 `80` → `64` | **绿 400/400** | ❗ **这正是 `w52`/`w55`/`w59` 那处 −8 的形态** |
| 8 | README `integration` 数字 `105` → `93` | **绿 400/400** | ❗ 同一个洞的另一半 |

变异 6 本槽**在两份互相独立的副本上各跑一次**,两次都是 400/400,并确认副本里
`docs/skills-wave/w56-aiscript-digest-prompt.md` **确实已不存在**。

**变异 1–5 说明 W23 那套契约的强度是真的**(第六次独立复验);
**变异 6–8 说明它的边界也是真的**:它是一套**一致性**契约(文档数字 vs 实测、索引 vs 目录),
不是**完备性**契约(该有的东西是否都在)。

### 6.3 一处"两边都能绿"的实跑对照:补回 `w53` 前后都全绿

为了把第 1 处实证钉死,本槽实跑了完整的修复路径(只读克隆,`merge --abort` 收尾,不落提交到本仓库引用):

| 状态 | `w53` 记账件 | 索引行 | 索引/目录双向 | `node tests/unit.js` | 那三处散文 |
|---|---|---|---|---|---|
| 合并前(= 当前 `w60` tip) | **不存在** | **不存在** | 零差 ✅ | **400/400 全绿** | 「W53 没有留下自己的记账件」 |
| 合并后(`w60 ← w53`,2 处文档冲突机械解开) | **存在** | **存在** | 零差 ✅ | **400/400 全绿** | **「W53 没有留下自己的记账件」(原样还在)** |

**两种状态都全绿,而其中至少一种的散文与实况直接矛盾**——
合并后那一行更难看:索引里有一行指向 `w53-memall-headless-seed.md`,同一份文件的记账诚实位里
写着"本行没有可指的 `w53-*.md`"。
**即修复这处失真需要动的不只是合并,还有那三处散文;而两件都不做、只做一件、全做,测试都是 400/400。**

## 7. 相对 `master` 的状态

### 7.1 主干:第六个周期一件未合

- `master` head 仍是 **`9adcf0ff`**,全仓 **8 个提交**,六个周期内**一次未动**。
- `js/skills.js` **不存在**;`docs/skills-wave/` **不存在**(`docs/` 下只有 `AI助手接入指南.md` 与 `用户操作指南-V0.1.md`)。
  `js/prompts.js` 存在,但它是主干既有模块,**不含本波新增的任何 `*.system` / `*System` 键**。
- **81 条**远端分支全部以 `master` 为严格祖先,**任一支单独并入都是纯快进、零冲突**。
- 最深的一支领先 **252** 个提交(周期 2 末 103、周期 3 末 125、周期 4 末 170、周期 5 末 212、周期 6 末 **252**)。
- 主干 README 的 unit 断言数自称 **163**、实跑 **201**,**差 38**,连续六个周期未修。
- `cli.smoke` **2 项失败**连续六个周期未修(本槽第六次独立复现,同名同表现,见第 2 节)。

**本轮不开 PR、不合并**(任务书明令),以上仅为状态记录。

### 7.2 合并后会红这件事,本周期新增一条

W23 的对账契约随任一条周期 6 分支进主干后,**`master` 那 38 之差会立刻变成红**——
本槽变异 1 实测该断言的报错原文即 `单元测试用例数:README.md 与实测不符`,是可复现的行为,不是推测。
**本周期新增的是它的边界**:变异 7、8 证明**主干 README 的 `integration` 与 `cli.smoke` 两个数字进主干后不会红**
(那两个数字没有断言)。所以「合并会自动暴露主干数字失真」这句话在本周期要修正成
**「只暴露 unit 那一个,另两个仍需人工核」**。

## 8. 阻塞汇总与下一目标

### 8.1 W48 八条药方的处置结果(先记账)

| W48 第 8.3 节的建议 | 周期 6 处置 | 证据 |
|---|---|---|
| 1 · 把周期 5 收成一条线(三步 `w47 ← w45 ← w44 ← w43`,13 处冲突、代码面 3 处) | **已闭合**。`w47` 四步做完(`8400399`/`682f3c8`/`f22dcaf`),unit 375/375,**pending 归零**,并补了记账件 `w47-integration-log.md` | 0.1、第 5 节 |
| 2 · 把当前集成线 head 做成可查的值 + 契约断言(W48 判为「唯一能同时压掉阻塞 2/5/6」) | **一个字没做,第三个周期**。`docs/skills-wave/README.md` 搜「当前集成线/head/指针」零命中(仅 W48 摘要文字 1 处),`tests/` 搜「集成线/祖先/is-ancestor」零命中。**但功能槽基线本周期 9/9 正确**——靠人守,不靠机制 | 3.3 |
| 3 · 决定主干策略 | **未处置,第六个周期**。`master` head 未动,`js/skills.js` 与 `docs/skills-wave/` 仍不存在 | 7.1 |
| 4 · 收口 G-10 发布门半边(先定产品口径) | **未开工,第四个周期**。本槽在 `w47`/`w50`/`w55`/`w57`/`w60`/`w58`/`w59`/`w61` **八支实测 `js/release.js` 对 `Skills` 引用全为 0**。`w58` 动了发布留痕命令化但那是 SK-25 的出口,**不是 `Skills.check` 的消费** | 6.2 下表 |
| 5 · 补一条按 `pending` 过滤的未闭清单投影 | **未做,第三个周期**。`Skills` 出口与周期 5 实测逐字相同。**但 pending 已全程为 0,该投影现在会恒回空——建议改写而非原样重提** | 5.1 |
| 6 · 把头号数字换成能反映断言增量的口径(建议加 `assert*` 调用数断言) | **未做,且同形态复发**。本槽实测 `assert*` 调用数:`w47` 2129 → `w60` **2369**,而 README 只有用例数一个口径;**`w61` 是最极端的一例:102 行新功能、`assert*` 净 0** | 4.11 |
| 7 · 对 S-08 给出明确结论 | **仍未决,第六个周期** | 5.2 |
| 8 · 补两处记账卫生:① 给 W47 补记账件;② 槽号 46 跳空记一行 | **①已闭合**(`w47-integration-log.md` 已在,索引已登记)。**②已自然消解**:`w46` 分支实际存在并已随 W50 进集成线,记账件 `w46-g12-playbook-proj.md` 已登记——W48 那条「槽号跳空」的结论本身被时点推翻 | 0.1 |

**八条里两条闭合(1、8)、一条自然消解(8②)、五条未动(2、3、4、5、6、7)。**
闭合的全是「本轮就能做完的技术动作」;未动的五条里 3/4/7 需要先定口径,**2/5/6 是纯治理动作却一件没做**。

**这个分布与周期 4、周期 5 一模一样,连比例都接近——这是第三次。**
周期 5 报告说「技术动作那一栏又全做完了,口径与治理那一栏又全没动」;
**周期 6 的实况完全相同,但本周期第一次可以指出这个分布的代价**:
药方 2 没做,基线仍然靠人守——**同一周期里集成槽那一侧就退步了两次,其中一次丢了一份文件**(3.1、3.2)。
**"靠人守住了"和"钉住了"的区别,在周期 6 第一次以实质损失的形式兑现。**

### 8.2 本周期阻塞(按严重度)

| # | 阻塞 | 严重度 | 依据 |
|---|---|---|---|
| 1 | **主干停滞六个周期**:`master` 上 `js/skills.js` 与 `docs/skills-wave/` 均不存在,81 条分支无一进主干,最深的一支领先 252 提交 | 高 | 7.1 |
| 2 | **集成线丢了一份 143 行记账件,且散文里写着它不存在**。`w53` head `0045962` 不在集成线上;三处散文(`w57-integration-log.md` ×2、`docs/skills-wave/README.md` ×1)写「W53 没有留下自己的记账件」,时点晚于该文件 20 分 37 秒且无限定词;W60 又重申一次。**同一份 README 还把 integration 105 / cli.smoke 80 两个数字的增量署名给 W53** | 高 | 3.2、4.8 |
| 3 | **索引契约是一致性契约,不是完备性契约**。变异 6 实测:同时删掉记账件与它那行索引,unit **400/400 全绿**。「合进来的槽必须有记账件」**没有任何断言** | 高 | 6.2 |
| 4 | **README 的 `integration` 与 `cli.smoke` 两个数字没有任何断言**,实际已失真:`w52`/`w55`/`w59` 三支冒烟数差 **−8**(`w55` 是集成 tip),**`w59` 上仍挂着**。变异 7、8 实测改错这两个数字全绿 | 高 | 4.4、4.7、6.2 |
| 5 | **`facts` 表只防「假清账」,不防「假欠账」**。`w60 ← w61` 实测**零冲突、400/400 全绿**,而 `core.memoryDual` 的 `note` 仍写「前段四步结论仍不回流」,源级实测那四步已全部回流。**落地越干净越容易发生**(`w61` 恰因一行没碰 `js/skills.js` 才零冲突) | 高 | 4.11 |
| 6 | **周期 4 第 3 步②/周期 5 第 2 步(集成线 head 指针 + 断言)第三个周期一个字没做**。功能槽基线本周期 9/9 正确是**人守**的结果;同期集成槽「取 head」**退步两次**(9 次里 2 次取中间点),其中一次即阻塞 2 | 高 | 3.1、3.3 |
| 7 | **W50 立的四条集成口径只写在记账件散文里,没有落点**(未进 `AGENTS.md`、未进目录索引固定位置、无断言)。**W57 七分钟后违反了第一条,W60 记录的字节级失配是第三条的延伸** | 中高 | 0.1、4.2 |
| 8 | **头号数字对「加在既有用例里的断言」仍然无感,且本周期出现极端例**:`w61` 落地 102 行功能而 `assert*` 调用数净 **0**(2352 → 2352)、用例数净 0。周期 4 阻塞 10 / 周期 5 阻塞 7 第三次复发 | 中高 | 4.11、8.1 |
| 9 | **周期 6 零新校验项**:`CHECKS` 全程 17 条、面表全程七面、`gaps()` 全程 20 键、pending 全程 0。增量全在贯通面(合方向正确),但**用例数与 pending 两个指标在本周期都失去区分度** | 中 | 2.1、第 5 节 |
| 10 | **G-10 发布门半边连续四个周期零开工**,八支实测 `js/release.js` 对 `Skills` 引用为 0。`w58` 的发布留痕命令化是 SK-25 出口,**不是方法论门**。卡点仍是产品口径未定,非技术前置 | 中 | 8.1 |
| 11 | **按 `pending` 过滤的未闭清单投影第三个周期未做,而这条药方已经过期**:pending 全程 0,该投影会恒回空;真正需要投影的是三条 `note` 的「仍欠」段,而阻塞 5 已证明那段散文会过期且过期无声 | 中 | 5.1 |
| 12 | **`w56` 转登记的记账不实无人接**:`SK-11`/`SK-10` 的 `note` 仍写「人设句入注册表待 G-13」,而那句人设 W45 已进注册表;`SK-11` 的 `gaps` 仍为 `['G-13']`。W56 点名、W60 转登记,**两个槽都没人接** | 中 | 4.9、4.10 |
| 13 | **散文折回的字节级失配无任何兜底**:W60 实测两处全角/半角引号失配,字数相同、语义相同、通读与 `git diff` 都看不出。它给了做法(按首尾锚点切段 `===` 比对),**但这个做法同样只写在记账件里** | 中 | 4.10 |
| 14 | **S-08 连续六个周期未决** | 低 | 5.2 |
| 15 | **`master` 的 2 项 `cli.smoke` 失败连续六个周期未修;README unit 断言数差 38 也未修**。且变异 7/8 说明进主干时**只有那 38 会自动报红**,另两个数字不会 | 低 | 第 2 节、7.2 |
| 16 | **`w61` 为适配新回流点把一条不变量收窄**:`导演板块恰好一条` → `某条特定文本恰好一条`。该支进行中,**本槽不判其抬松断言**,列此备下一个集成槽核 | 低 | 4.11 |

**一条方法性观察(承接周期 3、4、5 报告末尾那条;本轮第一次能给出因果闭环)。**

四个周期以来这条口径一路演进:周期 3 定「同一时刻只允许一条集成线,新集成波以上一条集成线 head 为基线」;
周期 4 发现它对集成槽生效、对功能槽无约束,提出让集成线 head 可查;
周期 5 实测功能槽十次基线里两次连"取 head"都没做到,判定**「把判断变成查询」是唯一能同时压掉三条阻塞的动作**。

**周期 6 提供了这条判断的对照实验,而且结论是它对的。**
本周期**功能槽基线 9/9 正确**——五个并行槽拿到同一个 `w50` head,周期 5 担心的"每个时点心算哪支覆盖最多"没有出错。
**但集成槽那一侧,同一条纪律退步了两次**:W50 取了 `w49` 中间点(**当轮自纠,零代价**)、
W57 取了 `w53` 中间点(**至今未补,丢了一份 143 行记账件,并写下一句与实况相反的话**)。

**两次的差别不在人,在有没有复测。** W50 立的口径原文就是「每推一版重做一次包含性实测……
同一条分支合过一次也不等于合完了:**它很可能还欠一笔记账件提交**」——
**这句话逐字预言了 W57 七分钟后踩的坑,而它当时只是上一个槽记账件里的一段散文。**

**所以周期 6 给出的判据不是"再加一条规则",也不只是周期 5 那条"把判断变成查询",而是更窄的一条:
口径必须有落点。** 本周期实测的三类假绿(第 6 节)按"离契约多远"恰好分成三档,
而它们的修法都是同一个形状——**把一句已经写下来的话,换成一个会红的断言**:

- 阻塞 3(记账件可以静默不存在)→ 断言「集成线上每个合进来的槽号都有记账件」,而不只是索引与目录相等;
- 阻塞 4(两个数字没人管)→ 把 `integration` / `cli.smoke` 两个数字并进 W23 那套现算契约;
- 阻塞 5(假欠账无声)→ `facts` 表除了查 `note` 点了名,再查**那处余量是否真的还欠**(源级反查);
- 阻塞 7/13(W50、W60 立的四条口径)→ 挪进 `AGENTS.md` 或目录索引的固定位置。

**本轮最值得记的一句是**:周期 5 报告说「W23 的契约让所有其他失真都会自动报红,只剩『基线选错』这一类完全无声」。
**周期 6 实测这句话说小了。** 基线选错这一类**本周期被人守住了 9/9**;
真正无声的是另外三类——**记账件可以静默消失、两个测试数字可以静默写错、余量可以静默假欠**,
而且三类**在本周期各出现了一次实证,一次造成了实质损失**。

### 8.3 下一目标(建议次序;本槽不执行)

1. **先把 `w53` 的 head 补合进来,并改掉那三处散文。这是本周期唯一一处实质损失,成本最低、代价最明确。**
   `w60 ← w53` 实测**冲突 2 处、全在 `docs/skills-wave/README.md`**(索引表一处、记账诚实位一处),
   143 行的记账件自动合入无冲突,机械解开后 **400/400 全绿**(本槽已实跑)。
   **但合并只做一半**:必须同时把三处「W53 没有留下自己的记账件」改掉——
   `w57-integration-log.md`(2 处)、`docs/skills-wave/README.md` 记账诚实位(1 处),
   `w60-integration-log.md` 第 8 节那句也应加一条更正。
   **本槽已实测:改与不改,测试都是 400/400,所以这一步没有任何断言会提醒你**(6.3)。
2. **把三类假绿各补一条断言。这是本周期唯一能把"靠人守"换成"会报红"的一件事,也是阻塞 2–5 的共同根因。**
   三条都是纯只读断言,不动任何判据、不改产品行为:
   ① **记账件完备性**:从集成线的「整合」提交信息里抽槽号,断言每个槽号在 `docs/skills-wave/` 下都有对应记账件
   ——变异 6 实测现有索引契约看不见这一类(6.2),而阻塞 2 就是它放过去的;
   ② **两个测试数字进契约**:把 README 的 `integration` / `cli.smoke` 数字按 W23 的做法现算钉住
   ——变异 7、8 实测这两个数字现在改错全绿(6.2),`w59` 上那处 −8 至今没人发现;
   ③ **`facts` 表补反方向**:除了查 `note` 的「仍欠」段点了名,再对每处余量做一条**源级反查**
   (例如 `core.memoryDual` 那行:断言前段四步的消费点里**确实还没有** `memFeedback`/`memWrite`)
   ——`w60 ← w61` 实测零冲突全绿而 `note` 与代码相反(4.11),这是三类里唯一会随下一次合并当场发生的。
3. **把 W50 / W60 立的四条集成口径挪出记账件,给它们一个落点。**
   四条分别是:每推一版重做一次包含性实测(**W57 违反,即阻塞 2**)、
   冲突两侧"数字往回退的那侧一定不对"、并入让非冲突区散文过期要通读捞、
   折回对侧散文后按首尾锚点逐字节 `===` 比对(**W60 实测两处全角/半角失配**)。
   **这四条本槽逐条复核都成立且都有实测支撑,问题只是它们写在两份记账件的散文里,新开槽的人不会读到。**
   建议进 `AGENTS.md` 的集成纪律段或 `docs/skills-wave/README.md` 的固定位置;
   第一条与第四条可以顺手做成断言/脚本(第一条即上一步的①)。
4. **把集成线 head 做成可查的值 + 契约断言。这是周期 4 提出、周期 5 判为最高优先、如今第三个周期未做的那条。**
   ① `docs/skills-wave/README.md` 固定位置写一行「当前集成线:`<分支名>` head `<SHA>`」;
   ② 加一条断言:该 SHA 必须是当前分支的祖先。
   **本周期的新证据是它的优先级要重新排**:功能槽基线已经 9/9(3.3),这条对功能槽的边际收益变小了;
   但它对**集成槽**仍然有效——阻塞 2 那次取中间点,如果集成线 head 是个可查的值、
   并且断言「被合入支的 head 必须是本分支祖先」,W57 那次会当场红。
   **建议把这条的重点从"给开槽者查基线"改成"给集成者查有没有合全"**,与第 2 步①合并实现。
5. **决定主干策略。这是第 1 阻塞,已是连续六个周期的同一条结论。**
   技术面无障碍(81 条分支全部纯快进、零冲突);要定的是分批口径。
   拓扑上的天然切点:`w6 head`(周期 1)、`w9 head`(周期 2)、`w21 head`(周期 3)、`w34 head`(周期 4)、
   `w47 head`(周期 5 全量,**已收完四步且 pending 归零**)、第 1 步做完的 `w60`(周期 6 全量)。
   进主干那一刻 W23 的契约会把 `master` 那 38 之差判红(7.2,本槽变异 1 复现过),
   **但本周期新增一条修正**:变异 7、8 实测主干 README 的 `integration` / `cli.smoke` 两个数字进主干后**不会红**,
   所以合并前那两个数字与 2 项 `cli.smoke` 失败需要**人工一并清掉**,不能指望契约。
6. **给「加在既有用例里的断言」补一个可见量(周期 4 第 9 步 / 周期 5 第 6 步,第三次提)。**
   本槽实测 `assert*` 调用数 `w47` 2129 → `w60` **2369**,可机械统计;
   **本周期出现了最极端的一例**:`w61` 落地 102 行功能而 `assert*` 净 **0**、用例数净 **0**(4.11)。
   建议把 `assert*` 调用数并进 README 的现算契约(与第 2 步②同一处改动,顺手做)。
   **周期 4 那条"要求新机制面必须开新用例名"的约定已经三个周期没人执行,不宜第四次原样重提。**
7. **收口 G-10 的发布门半边——仍是先定产品口径,不要再当技术任务排(第四个周期)。**
   本槽八支实测 `js/release.js` 对 `Skills` 引用为 0。
   **本周期需要澄清一处可能的误读**:`w58` 动了 `js/release.js` 一侧的发布留痕命令化(SK-25 的欠段),
   那是**命令出口**,不是 `Skills.check` 的消费,门禁判据未动。
   真正卡住的仍是「方法论门要不要挂成默认 `warn` 的可选门」这个产品决定。
8. **把「按 `pending` 过滤的未闭清单投影」这条药方改写,不要原样第四次重提。**
   pending 已全程为 0(第 5 节),该投影会恒回空。
   真正需要投影的是三条 `note` 的「仍欠」段(`SK-03` 有意不做 / `SK-04` `w61` 正在做 / `SK-23` 待 G-10),
   而阻塞 5 已证明那段散文会过期且过期无声。**建议合并进第 2 步③:让"仍欠"段有源级反查,
   顺带就有了一个可读的未闭清单。**
9. **两处低优先记账卫生**(合流时顺手):
   ① `w56` 点名、`w60` 转登记的那处记账不实至今无人接
   (`SK-11`/`SK-10` 的 `note` 仍写「人设句入注册表待 G-13」,而 W45 已进注册表;`SK-11.gaps` 仍 `['G-13']`)——
   改它要连着动 `Skills.gaps()` 投影与 `tests/unit.js` 的期望串,是定性动作,需要一个功能槽而非集成槽;
   ② 对 **S-08** 给出明确结论(第六个周期):做则进 30 条(短名单变 31 条,需同步波次配比断言),
   不做则在短名单第 7 节标记「明确拒绝」。**这条六个周期没动,已经不该再进下一份核验报告的待办栏。**

## 9. 本报告的复核方式

全部数字可按下述步骤独立重跑(本槽即如此取得;`/tmp` 下的探针脚本、只读克隆与变异副本**均已删除,不入仓库**)。

```bash
# 1. 各支独立工作树,互不干扰
for b in w47-integration-2fed w49-agent-system-prompt-96c0 w50-integration-dad5 \
         w51-browser-agent-personas-da99 w52-g12-mcp-mid-templates-d660 \
         w53-memall-headless-seed-3653 w54-issues-unreviewed-sk19-cfc0 \
         w55-integration-8f21 w56-aiscript-digest-prompt-e8c8 w57-integration-a697 \
         w58-sk25-publish-cmd-3b7a w59-issues-umd-a3d7 w60-integration-edd3 \
         w61-sk26-front-writeback-b09b; do
  git worktree add --detach /tmp/wt/$b origin/cursor/$b
done
git worktree add --detach /tmp/wt/master origin/master

# 2. 逐支复跑(master 基线同法)
cd /tmp/wt/w60-integration-edd3
node tests/unit.js          # 400/400
node tests/integration.js   # 105/105
node tests/cli.smoke.js     # 78/80(2 项与 master 同样失败)

# 3. cli.smoke 那 2 项:九支逐支抽 FAIL 行比对,应完全一致
for d in /tmp/wt/master /tmp/wt/w47-* /tmp/wt/w50-* /tmp/wt/w55-* /tmp/wt/w57-* \
         /tmp/wt/w60-* /tmp/wt/w58-* /tmp/wt/w59-* /tmp/wt/w61-*; do
  echo "## $(basename $d)"; (cd $d && node tests/cli.smoke.js 2>&1 | grep '^FAIL'); done
#   九支应逐字相同:未登录 whoami → exit 3 | exit=1  /  llm --json mock 链路 | undefined

# 4. 注册表状态直接读数据,不读文档(第 2.1 节那张表)
node -e "const S=require('./js/skills.js');const K=require('./js/knowledge.js');
  const P=require('./js/prompts.js');
  const p=S.REG.filter(x=>x.pending.length);
  console.log('SK',S.REG.length,'| CHECKS',Object.keys(S.CHECKS).length,
              '| KB',Object.keys(K.SECTIONS).length,'| gaps',Object.keys(S.gaps()).length,
              '| Prompts',P.list().length,'| 面',S.preflightStages().length,
              '| playbooks',S.playbooks().length);
  console.log('pending',p.length,'条 /',p.reduce((n,x)=>n+x.pending.length,0),'面');
  console.log('Skills 出口:',Object.keys(S).filter(k=>typeof S[k]==='function').sort().join(','));"
# w60 应回:CHECKS 17 / KB 18 / gaps 20 / Prompts 14 / 七面 / playbooks 5 / pending 0 条 0 面
# 出口列表应与周期 5 逐字相同(即仍无按 pending 过滤的投影)

# 5. README 三个数字对账(第 2 节那张表;注意后两个没有断言保护)
for b in w52-g12-mcp-mid-templates-d660 w55-integration-8f21 w59-issues-umd-a3d7 w60-integration-edd3; do
  echo "## $b"
  git show origin/cursor/$b:README.md | grep -oE '单元测试\([0-9]+ 项断言'
  git show origin/cursor/$b:README.md | grep -oE 'tests/integration\.js` — [^:]*' | grep -oE '[0-9]+ 项断言' | head -1
  git show origin/cursor/$b:README.md | grep -oE 'tests/cli\.smoke\.js` — [^:]*'  | grep -oE '[0-9]+ 项断言' | head -1
done
#   w52 / w55 / w59 的冒烟数应为 64,而实跑为 72 —— 差 −8(w60 为 80,实跑 80,对)

# 6. 集成槽逐次合并:parent2 是否 = 被合入支当前 head,且是否真三方合并
for m in b86ab6e 81baf4a 371c75e c96beaa f34cffb 7f75f0d 61e60a9 9fab31c e876a4e; do
  p1=$(git rev-parse $m^1); p2=$(git rev-parse $m^2)
  git merge-base --is-ancestor $p1 $p2 && echo "$m 本可 ff" || echo "$m 真三方合并"
done
git rev-parse --short=8 origin/cursor/w49-agent-system-prompt-96c0   # a3960a6b
git rev-parse --short=8 81baf4a^2                                    # 264f9cf1 ← 中间点,W50 当轮自纠
git rev-parse --short=8 371c75e^2                                    # a3960a6b ← head ✅
git rev-parse --short=8 origin/cursor/w53-memall-headless-seed-3653   # 00459625
git rev-parse --short=8 9fab31c^2                                    # 09887be8 ← 中间点,至今未补
git rev-list --count 09887be8..origin/cursor/w53-memall-headless-seed-3653   # 1
git merge-base --is-ancestor 00459625 origin/cursor/w60-integration-edd3 \
  && echo "w53 head 在集成线上" || echo "w53 head 不在集成线上 ← 应回这条"

# 7. 功能槽基线 = merge-base(集成侧, 被合支);旁支 = merge-base(该支, w60)
for pair in "b86ab6e W46" "81baf4a W49" "f34cffb W51" "7f75f0d W52" "61e60a9 W54" "9fab31c W53" "e876a4e W56"; do
  set -- $pair; echo "$2 基线 $(git rev-parse --short=8 $(git merge-base $1^1 $1^2))"; done
#   W49 → 8366727d = w47 head ✅;W51/W52/W53/W54/W56 → 371c75e2 = w50 head ✅
#   W46 → b86ead50 = w44 head(周期 5 形态,唯一反例)
for fs in w58-sk25-publish-cmd-3b7a w59-issues-umd-a3d7 w61-sk26-front-writeback-b09b; do
  echo "$fs 基线 $(git rev-parse --short=8 $(git merge-base origin/cursor/$fs origin/cursor/w60-integration-edd3))"; done
#   w58/w59 → 3968658e = w55 head ✅;w61 → 450c29f8 = w57 head ✅

# 8. 目录索引双向对账(14 支应全部零差 —— 注意这恰恰是它看不见 w53 的原因)
for b in w55-integration-8f21 w57-integration-a697 w60-integration-edd3; do
  git show origin/cursor/$b:docs/skills-wave/README.md \
    | grep -oE '\bw[0-9]+[a-z0-9-]*\.md' | sort -u > /tmp/i
  git ls-tree -r --name-only origin/cursor/$b -- docs/skills-wave \
    | sed 's#docs/skills-wave/##' | grep -v '^README.md$' | sort > /tmp/f
  echo "$b: $(wc -l < /tmp/i) 行 / $(wc -l < /tmp/f) 份"; LC_ALL=C comm -3 /tmp/i /tmp/f   # 应无输出
done
git show origin/cursor/w60-integration-edd3:docs/skills-wave/README.md | grep -c 'w53'
#   应回 1 —— 那 1 处是记账诚实位里「没有可指的 w53-*.md」那句,不是索引行

# 9. 集成线 head 指针与断言:两处都应零命中(药方 2 未做)
git show origin/cursor/w60-integration-edd3:docs/skills-wave/README.md \
  | grep -niE '当前集成线|集成线 *head|指针'      # 仅 W48 摘要文字 1 处
cd /tmp/wt/w60-integration-edd3 && grep -rniE '集成线|is-ancestor|祖先' tests/*.js   # 零命中

# 10. G-10 发布门半边:八支应全为 0
for d in /tmp/wt/w47-* /tmp/wt/w50-* /tmp/wt/w55-* /tmp/wt/w57-* \
         /tmp/wt/w60-* /tmp/wt/w58-* /tmp/wt/w59-* /tmp/wt/w61-*; do
  echo "$(basename $d): release.js×Skills = $(grep -c Skills $d/js/release.js)"; done

# 11. 断言强度的变异测试(在副本上做,勿在工作树上改;第 6.2 节八路)
cp -r /tmp/wt/w60-integration-edd3 /tmp/mut && cd /tmp/mut && rm -f .git
node tests/unit.js                                              # 基线 400/400
perl -0pi -e 's/单元测试\(400 项断言/单元测试(399 项断言/' README.md
node tests/unit.js   # 红:contract · README 数字对账(单元测试用例数)
cp /tmp/wt/w60-integration-edd3/README.md README.md
perl -0pi -e 's/CHECKS 已落地十七条//' README.md
node tests/unit.js   # 红:架构框 CHECKS 条数 —— 删句绕过被堵
cp /tmp/wt/w60-integration-edd3/README.md README.md
perl -0pi -e 's/14 条注册表提示词/13 条注册表提示词/' README.md
node tests/unit.js   # 红:注册表提示词数
cp /tmp/wt/w60-integration-edd3/README.md README.md

# 【关键三路:全绿,即本报告第 6 节的三类假绿】
rm -f docs/skills-wave/w56-aiscript-digest-prompt.md
perl -ni -e 'print unless /^\| \[w56-aiscript-digest-prompt\.md\]/' docs/skills-wave/README.md
node tests/unit.js   # 绿 400/400 ← 记账件与索引行一起删,双向契约看不见(= w53 的形态)
cp -r /tmp/wt/w60-integration-edd3/docs/skills-wave/. docs/skills-wave/

perl -0pi -e 's/CLI 真实服务端冒烟\(W53 扩至 80 项断言/CLI 真实服务端冒烟(W53 扩至 64 项断言/' README.md
node tests/unit.js   # 绿 400/400 ← 冒烟数字无断言(= w52/w55/w59 那处 −8 的形态)
cp /tmp/wt/w60-integration-edd3/README.md README.md
perl -0pi -e 's/W53 扩至 105 项断言/W53 扩至 93 项断言/' README.md
node tests/unit.js   # 绿 400/400 ← 集成数字同样无断言
cp /tmp/wt/w60-integration-edd3/README.md README.md

# 12. 三条在外 tip 的合流成本 + 两处前向假绿(只读克隆,不落提交到本仓库引用)
git clone --shared . /tmp/sim && cd /tmp/sim
git fetch .. "+refs/remotes/origin/cursor/*:refs/w/*"
for b in w53-memall-headless-seed-3653 w61-sk26-front-writeback-b09b \
         w58-sk25-publish-cmd-3b7a w59-issues-umd-a3d7; do
  echo "== w60 ← $b"
  out=$(git merge-tree --write-tree refs/w/w60-integration-edd3 refs/w/$b); t=$(echo "$out" | head -1)
  for f in $(echo "$out" | awk '$1~/^100/{print $4}' | sort -u); do
    n=$(git show $t:$f 2>/dev/null | grep -c '^<<<<<<<'); [ "$n" -gt 0 ] && echo "   $f: $n 处"
  done
done
# 应回:w53 → docs/skills-wave/README.md 2(共 2);w61 → 零冲突
#       w58 → README.md 3 + docs 2 + tests/integration.js 1(共 6)
#       w59 → README.md 2 + docs 1 + js/issues.js 4 + tests/unit.js 1(共 8)

# 12a. 补回 w53:解完全绿,而那三处散文里的假话原样还在(第 6.3 节)
git checkout --detach refs/w/w60-integration-edd3
git merge --no-ff --no-edit refs/w/w53-memall-headless-seed-3653   # 2 处冲突,全在 docs 索引
#   按「索引行按波次序并入 + 记账诚实位取集成线侧」解开后:
node tests/unit.js                                                  # 400/400 全绿
ls docs/skills-wave/w53-memall-headless-seed.md                     # 文件到位
grep -c '没有留下自己的记账件' docs/skills-wave/README.md \
       docs/skills-wave/w57-integration-log.md                      # 仍回 1 与 2 ← 假话还在,且全绿
git merge --abort

# 12b. w61 的假欠账:零冲突、全绿、而 note 与代码相反(第 4.11 节)
git checkout --detach refs/w/w60-integration-edd3 && git clean -qfd
git merge --no-ff --no-edit refs/w/w61-sk26-front-writeback-b09b    # 零冲突
node tests/unit.js                                                  # 400/400 全绿
node -e "const S=require('./js/skills.js');
  console.log((S.byId('core.memoryDual').note||'').split('仍欠').slice(1).join('仍欠'))"
#   仍回「……理解/分镜/拆集/提取主体几步的结论仍不回流……」
grep -c 'memFeedback\|memWrite' js/understanding.js js/sb-llm.js js/proj-upload.js js/commands.js
#   四个文件各回 2 ← 那四步其实已经回流了
git merge --abort

# 13. facts 表三行与 live note 逐行对(第 4.10 节)
cd /tmp/wt/w60-integration-edd3
node -e "const S=require('./js/skills.js');
  ['core.personaCtx','core.memoryDual','review.stage'].forEach(id=>{const s=S.byId(id);
    console.log(id, JSON.stringify(s.gaps), JSON.stringify(s.pending));
    console.log('  仍欠:', (s.note||'').split('仍欠').slice(1).join('仍欠').slice(0,200));});"
sed -n '6118,6145p' tests/unit.js    # facts 表本体与那两条 forEach 判据

# 14. assert* 调用数(阻塞 8;README 无此口径)
for d in /tmp/wt/w47-* /tmp/wt/w57-* /tmp/wt/w60-* /tmp/wt/w61-*; do
  echo "$(basename $d): $(grep -oE '\bassert[A-Za-z]*\(' $d/tests/unit.js | wc -l)"; done
#   w47 2129 / w57 2352 / w60 2369 / w61 2352 ← w61 与其基线 w57 逐个相同,净 0

# 15. 时点链(0.1、3.2;仓库只留 commit 时点,push 时点不可查)
for c in 3645d22 f3c4611 8366727 05836ee b76ee04 09887be 0045962 \
         9fab31c 450c29f 038b5d1 e876a4e 4967ae9 8b63f7f; do
  printf "%-9s %s  %s\n" "$(git rev-parse --short=7 $c)" \
    "$(git log -1 --format=%cI $c)" "$(git log -1 --format=%s $c | cut -c1-46)"; done
```

> 提醒:第 0.1 节已说明 **W48 有两条结论被时点推翻**(`w46` 实际存在;`w47` 四步做完且已有记账件),
> 成因与本周期阻塞 2 是同一个机制。**若复核时点晚于本报告,请先比对 head SHA 再采信数字**——
> 尤其 `w61`(**进行中,1 个提交、无记账件、README 未同步**,随时可能前进,届时 4.11 与 2.1 那两行会整体变化)、
> `w58` / `w59`(在外 tip,可能被下一个集成槽合入),以及 **`w62`(本槽核验时远端零引用,状态未知)**。

`node tests/e2e.js` 按仓库纪律**未跑**(需用户明确要求)。
本轮未开 PR、未合并任何分支、未改动任何业务代码;本分支的唯一改动是本文件。

> **未改目录索引的声明**:本文件**没有**登进 `docs/skills-wave/README.md` 的索引表。
> 理由是那张表由契约断言 `contract · docs/skills-wave 索引与目录实况双向对齐` 双向钉住——
> 只加文件不加索引行会红、只加索引行不加文件也会红(本槽变异 4、5 实测,见 6.2)。
> 本槽按任务口径**只提交本报告一个文件**,故索引侧必然缺这一行,`node tests/unit.js` 在本分支上
> **会报该条断言失败(399/400)**,这是**预期结果、如实登记**,不是回归。
> **本槽有意不动索引**:改了索引就要改契约的另一侧,而那属于业务改动。
> 接手的槽把本文件合入时,请**顺手补上索引那一行**(按波次序排在 `w60-integration-log.md` 之后),补完即绿。
> 顺带一提:本槽这一处「文件在、索引行不在」**会红**,而阻塞 2 那处「文件和索引行一起不在」**全绿**
> ——两者放在一起,恰好就是第 6 节那个洞的形状。
