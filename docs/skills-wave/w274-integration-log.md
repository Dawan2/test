# W274 集成日志:W273 生成面智能审片面板镜号并进 W272 尖(集成线上第七条「快进形」)

我方尖:`origin/cursor/w272-integration-3fe8` **现取** `ffa5e76`(先 `fetch` 再 `rev-parse`)。
对侧:`origin/cursor/w273-shotno-produce-b231` **现取** `1b903da`。
W273 自称从 w272 尖 `ffa5e76` 起支,自称与现取相同,但**以 `rev-parse` 为准**。
新支 `cursor/w274-integration-a6d1` 从 w272 现取 HEAD 拉出,`checkout -b` 后立刻 `rev-parse`,与 `ffa5e76` 逐字节相同。

**结论先写:一次 `--no-ff`,零冲突。合完 live 跑出 `unit` 693 / `contract` 152 / `skills` 104 /
`integration` 152 / `cli.smoke` 115-117,`gaps()` 20 键一个没剥,W273 八条残留一条没代修。
本槽是集成线上第七条快进形,`tree(M)` 与 `tree(P2)` 逐字节相同、`P1→M` 与 `B→P2` 的 numstat 逐行相同——
这两格照旧**恒真**,一格结论都不承载,故本文的证据全在变异与 live 上。
本槽新量出来的一格是:对侧那行产品语义的 `|| '?'` 兜底半边**一条判据都没护着**(§四 M7),如实记成残留。**

## 一、拓扑(现取,不抄自称)

| 位 | 现取 SHA |
|---|---|
| `B` 合并基(`merge-base P1 P2`) | `ffa5e7656b1d7063f5aa453840ecd29d5b427834` |
| `P1` 我方尖(w272) | `ffa5e7656b1d7063f5aa453840ecd29d5b427834` |
| `P2` 对侧尖(w273) | `1b903da9ac340000f1ef57817ba57148643da8d2` |
| `M` merge commit | `419005d9fa26da9c16992e0ef0a4977588c95fbf` |

`git merge-base --is-ancestor P1 P2` 成立,`B` 与 `P1` **提交级相等**,即典型「快进形」——
本可 `--ff` 掉,仍按纪律走 `--no-ff` 留住合点。`P1..P2` 三条提交:

| 现取 SHA | 是什么 |
|---|---|
| `a4ad2f169bbec2f17ae7e6da6aed3ad2aed44120` | 产品:面板逐镜行改走 `Domain.shotNo` |
| `7670472762c688aa03f64d2e36526915a4ebda75` | 加测两条 + 记账同步 |
| `1b903da9ac340000f1ef57817ba57148643da8d2` | 记账按实跑订正 |

「自称尖是不是链尾」这一格现取核过:`1b903da` 就是链尾。

树面:`tree(P1)` `58078e0`、`tree(P2)` = `tree(M)` = `575de56`(逐字节相同);
`P1→M` 与 `B→P2` 的 `--numstat` 逐行相同(五个文件、`3/2`、`2/1`、`215/0`、`4/1`、`51/2`)。
**这两格在快进形下是恒真的**,写在这儿只是留档,不当证据用。

## 二、并集面(README / GUARD / roster / 索引)

快进形下 `P2` 的文档本就含着 `P1` 的全部内容,故并集不靠手工拼:
`docs/skills-wave/README.md` 索引表里 w272 那行与 w273 那行**同时在册**,现取核过。
数字一律 live,**W272 自称的 691 一个字没抄**(反事实见 §五):

| 面 | `P1` 基线现取 | `M` 合完现取 | 差 |
|---|---|---|---|
| `unit` | 691 | **693** | +2(全部来自对侧) |
| `contract` | 151 | **152** | +1 |
| `skills` | 103 | **104** | +1 |
| `integration` | 152 | **152** | 0 |
| `cli.smoke` | 115/117 | **115/117** | 0 |
| 记账件 | 287 | **289**(w273 一份 + 本文) | +2 |

GUARD / roster 现取四者对齐、本槽与对侧都没登记新护栏主题:
`GUARD_TOPICS` **19** / `GUARD_TOPICS_CLOSED` **0** / `TOPIC_FLOOR` **19** /
花名册(`w178-topic-floor-unlist.md` 的花名册节)**19**。三棵树上逐树现算,三处都是这四个数。
`gaps()` 键集三棵树逐树现算均为 **20** 键,**一个没剥**。

棘轮与差额:`['单元测试', 693]`(对侧已按 live 抬)、`['集成测试', 152]`、`['CLI 冒烟', 117]`、
`TOPIC_FLOOR` 19 均未动;记账件 `const FLOOR` 288 → **289**(本文一份),
`docs/skills-wave/README.md` 明写份数 288 → **289**、索引补本文一行(按波次号插在 w273 之后)。
五格差额现取全为 0,`SLACK` **3** 一格没用掉。

## 三、快进形的空集不能当没丢东西

`git diff P2 M` 是空集——这在快进形下是**定义使然**,它证明的只是"合入没引入新东西",
证不了"对侧那一处语义在合完的树上还活着、判据还咬得住"。故取证换成三类:**变异**、**live**、**点数对账**。

### 产品语义现取核过

```
$ rg -n '▶ 镜头 ' js/produce.js
279:        say(`▶ 镜头 ${Domain.shotNo(ep.shots, s) || '?'} 评审中${attempt ? `(第 ${attempt} 次重生成后)` : ''}…`);

$ rg -c '\.order \+ 1' js/produce.js      →  0 处(rg 无命中)
$ rg -c '\.order \+ 1' js/batchops.js     →  17
$ git diff --stat ffa5e76 HEAD -- js/batchops.js  →  (空,逐字节未动)
```

即明令要核的两格都成立:面板那一行走 `Domain.shotNo`,`js/produce.js` 里 `.order + 1` 现取 **0**;
batchops **17 行整族一字未动**,落库文本与瞬时串照旧读 `order`。W273 §二那张分类表也逐格核过——
`batchops.js:454`(多镜审片汇总清单行)现取已是 `Domain.shotNo`,W267 那槽收的、不在 17 行点数里,
故对侧"生成面共 19 行"= 18 行读 `order`(produce 1 + batchops 17)+ 这 1 行已收,自洽。

### 三棵树关键词点数(全仓,`P1` / `P2` / `M` 逐格)

`M` 与 `P2` **逐格相等**,无一格 `M < P1`;变的几格全是对侧净增:

| 关键词 | `P1` | `M` | | 关键词 | `P1` | `M` |
|---|---|---|---|---|---|---|
| `landed` | 159 | 160 | | `shotNo` | 129 | **146** |
| `shots-dedupe` | 183 | 184 | | `rowIndexOf` | 72 | 73 |
| `state-put` | 174 | 175 | | `gaps()` | 626 | 629 |
| `GUARD_TOPICS` | 304 | 305 | | `TOPIC_FLOOR` | 264 | 266 |
| `runBatchOp` | 21 | 23 | | `autoSmartReview` | 143 | 149 |
| `bgDock` | 29 | 34 | | | | |

未动的十格(`M` = `P2` = `P1`):`nthShot` 92、`nthOf` 28、`pickerNarrowHits` 46、`dupRowsNote` 113、
`floorLagVerdict` 61、`stateDigest` 60、`lastRep` 38、`mergeReviewPerShot` 51、`reviewRows` 60、
`openReviewSummary` 30、`openConfirmGateModal` 17。

### 名集多重集对账

`unit` 用例名逐条排序后比对:`M` 与 `P2` **逐字节相同**;`P1 → M` **只多 2 条、一条没少**:

```
+ contract · 生成面展示镜号(源级):量产审片面板取 Domain.shotNo,产线邻面的任务名/退费摘要/留痕文件名照旧留 order
+ skills   · 量产智能审片面板镜号按实位:面板逐镜行与同屏分镜卡片同报一个号(order 字段漂了也不跟着漂)
```

`691 + 2 = 693` 自洽,与 §二 那张表对得上。

## 四、变异八手(逐手在合完的树上做、跑完还原,`git status` 复核 clean)

M1–M5 是对侧自称五手的逐手复现;M6–M8 是本槽新加的三手,各打一个此前没人单独量过的半边。

| 手 | 改法 | 红几条 | 报的是 |
|---|---|---|---|
| M1 | 面板逐镜行退回 `` `▶ 镜头 ${s.order + 1} 评审中…` `` | **2** | `contract` 1:「智能审片面板逐镜行的镜号应取 Domain.shotNo(表内实位):say(\`▶ 镜头 ${s.order + 1} 评审中…\`);」(犯规那一行原样抄出);`skills` 1:「面板逐镜行按分镜表实位出(读 order 字段时是 6,10,1):期望 "1,2,3",实际 "6,10,1"」 |
| M2 | **越界**:`js/batchops.js` 智能修片的任务名改走 `Domain.shotNo` | **1** | `contract` 报「智能修片的任务名是要落库的文本,应照旧按 s.order 记(与面板那个实位号有意不同源)」;`skills` **0** |
| M3 | **越界**:智能修片的 `Tasks.done` 留痕文件名改走 `Domain.shotNo` | **1** | `contract` 报「智能修片的任务中心留痕文件名是要落库的文本,应照旧按 s.order 记(与面板那个实位号有意不同源)」;`skills` **0** |
| M4 | **跨支反向**:退掉 W272 已落的 `js/sb-views.js` 更多工具页头 | **2** | 全落在 W271 那两条上,报错句与 W271 §五 M1 / W272 §三 / W273 §五 M4 **逐字相同**;**本槽两条判据一条没红** |
| M5 | **只越界地加**:面板那行原样不动,另处新增一个 `order + 1` | **1** | `contract` 报「js/produce.js 不许再按 order 字段出镜号:if (!r) { say(\`&nbsp;&nbsp;镜头 ${s.order + 1} 积分不足,审片中止\`); … }:期望 0,实际 1」 |
| **M6** | **锚点计数那一半**:面板那行原样不动,另加一行同前缀 `▶ 镜头 ` 的面板字、**它自己也走 `Domain.shotNo`** | **1** | `contract` 报「智能审片面板逐镜那一行按字面锚不到或不止一处(文案变了就同轮改这条判据):期望 1,实际 2」;`skills` **0** |
| **M7** | **摘掉 `\|\| '?'` 兜底**(面板那行其余一字不改) | **0** | 两个套件**一条不红**——见下 |
| **M8** | 同一个半边换一种退法:兜底改成 `\|\| (s.order + 1)` | **1** | `contract` 只报往下削那句「不许再按 order 字段出镜号:…:期望 0,实际 1」,没有一句判的是兜底本身 |

M2 / M3 用的是同一族里落点不同的两处(任务名 / 留痕文件名),报的是两句不同的话,
与对侧自称同形(对侧 M3 取的是字幕擦除那一处,本槽取智能修片那一处,同族异处,报错句照样各说各的)。

### M6:复合判据的第四个半边

对侧那条 `contract` 用例是一句四半的复合:①`▶ 镜头 ` 锚点恰一处、②这一行走 `Domain.shotNo`、
③整份 `js/produce.js` 的 `.order + 1` 恰 0 处、④邻面 8 处落库文本仍读 `order`。
对侧 M1 打的是半②、M5 打的是半③、M2/M3 打的是半④——**半①从没被单独量过**。
M6 补的就是它:新加的那一行自己也走 `Domain.shotNo`,故半②(取的是 `hit[0]`)照旧绿、
半③(`.order + 1` 仍 0)照旧绿、半④没碰,只有半①从 1 变 2 而报出它自己那句话。
四半各有各的判词,不是一半靠另一半顺带绿——这是 W272 §四 M8 立下的「复合判据可分性」这一格
在本槽上补齐的最后一角。

**顺带量出这一档的代价,如实写在这儿**:半① 钉的是「恰一处」,故这一面将来**合法**多出一行面板镜号字
(哪怕它自己就走实位号)同样当场红,得同轮改判据。对侧 §四只为半③(恰 0 处)写了这句代价说明,
半① 有同样的代价而没写;本槽把它记在这儿,收下一面时不必再量一遍。

### M7 / M8:兜底那半边一条判据都没护着(本槽新记的残留)

面板那行的产品语义是两半:**取实位号**,以及**表里没有这一镜时出 `?`**。
后半在 `js/produce.js:271-273` 的注释里写明了(「表里已没有这一镜时出 ?(0 不是任何一镜的号)」),
在 `js/domain.js:665-671` 里也写明了(`D.shotNo` 表外回 `0`,「0 不是任何一镜的镜号,展示面据此按『未知』出」)。

- **M7**:把 `|| '?'` 整个摘掉,面板那行其余一字不改 → **`contract` 0、`skills` 0**。
  源级那半只正则匹配 `Domain\.shotNo\(ep\.shots, s\)`,兜底在不在它管不着;
  行为面那条用例的三行夹具全在 `ep.shots` 里,`shotNo` 一次都回不到 0,那条分支根本没跑到。
  摘掉之后表外镜在面板上印的是「▶ 镜头 0 评审中」——一个长得像镜号、而任何一镜都不是的号,
  正是这条线要收的那种毛病,而它退回去一条测都不红。
- **M8**:同一个半边换一种退法,兜底改成 `|| (s.order + 1)` → **红 1**,但红的是**往下削那句计数**
  (`.order + 1` 从 0 变 1),不是任何一句判兜底的话。

两手摆在一起画出的边界很干脆:**兜底这半边唯一拦得住的退法,是那种恰好把 `order` 三个字写出来的退法**;
换成 `|| '0'`、`|| ''` 或干脆不写,四个半边一个都不动。这与 W272 §四 M2、W273 §七 8 记的那两格"半覆盖"
不是同一类——那两格是"源级有、行为面没有",本槽这一格是**两层都没有**,
且它护的不是"顺手多收了别人的地盘",而是对侧自己那行产品语义的一半。如实记成残留(§六 9),本槽一条测都没加。

### M4:连续第四次读成「分家」

M4 与 W271 §五 M7 / W272 §三 / W273 §五 M4 形状相同:退掉一处别的面已落的镜号,
本槽两条判据一条不红,只红那一面自己的两条,报错句逐字相同。
镜号这条线上四个面(审片 `js/review.js` / 分镜 `js/sb-*.js` / 生成 `js/produce.js` / 待收的问题中心 `js/issues.js`)
的判据**各管各的文件、不叠着**——W270 那次读成「叠着」是因为对侧改的是我方刚改过的同一个文件,
判据形状从没变过,变的只是改动落在谁的地盘上。

## 五、反事实:抄回旧数字

| 手 | 改法 | 红几条 | 报的是 |
|---|---|---|---|
| CF1 | 把 W272 自称的 691 抄回 `README.md` | **2** | 「单元测试用例数:README.md 与实测不符(实测 693,文档 691)」+「README.md 写的 691 低于下限 693(把文档数字改小来迁就删测即红)」 |
| CF2 | `README.md` 与 `['单元测试', 693]` 那个下限**一起**改小 | **1** | 下限那条被一并改小后不响了,「与实测不符(实测 693,文档 691)」照旧报——棘轮往下走这一路仍拦得住 |

## 六、残留(按明令一条没代修,前八条原话逐条保留)

1. **产线邻面 `js/batchops.js` 那 17 行仍读 `order`**(W273 有意整族留下):落库文本 8 行
   (三条任务名 / 两条退费摘要 / 两个留痕文件名 / 入库素材名)、瞬时串 8 行(5 处 toast、
   一键审片确认弹窗正文那句 `'视频 #N·M'`、两处 `U.runTask` 的 `steps.label`)、导出 TXT 正文 1 行。
   与 W271 留下的分镜面 38 行同族,收的时候要一起定"进度坞行与它同行的扣费摘要按什么切开"
   ——`batchops.js:290`/`:291` 与 `:298`/`:301` 正是那个形状(退费摘要与 toast 相邻)。
   另记一处判词上的口子:**一键审片确认弹窗正文那句归了瞬时族**(`U.openModal` 的 `body`,
   不是 `U.toast`),按 W271 的分类它更像"持久 chrome";照明令的字面(confirm 串不改)放过,
   收瞬时族时须先把"弹窗正文算不算持久"这一句判词补上,别留成两槽各判各的。**产品口径未定,本槽不定。**

2. **两份待确认清单行只有一份有行为面判据**(W271 §五 M2 量出来、W272 §四复量过):
   `js/sb-batch.js:154`(`runBatchOp` 出片前置检查)只有源级那一句锚着。**W273 一条测都没加**
   (明令点名不顺手扩测别的面),如实照旧记成一格。本槽同样一条没加。

3. **「按指令改」页头(`js/sb-views.js:1096`)只有源级判据护着**(W272 §六 7 起在册):
   行为面那条用例真跑的是另外四扇门,`openImgEdit` 那一屏没进去。照旧开着。

4. **问题中心 `js/issues.js` 那 2 处仍读 `order`**(W267 §六 4 起在册):进的是 `detail` 文案,
   与命令回执同源、编排层也读,口径要单独定。镜号这条线至此收完审片面、分镜面、生成面三面,
   **问题中心是最后一面**,而它恰恰是唯一一面"给人看的串同时也被机器读"的——不宜照前三面的判词收。

5. **`js/skills.js` 十余处 `(+s.order || 0) + 1` 与 `js/wf-core.js` / `js/sb-llm.js` 里的镜号**:
   回执与提示词口径(LLM 读的那份),按明令未碰。

6. **审片面任务名与扣费摘要那 4 行**(W269 §六 1 起在册):`js/review.js` 未碰,判据照旧钉着。

7. **浏览器闭环 `common` / `cut` 空壳、`openEpisodeReview` 整表与 generating 过滤**
   (W261 §六 2/3 起在册)、**同 id 多行源头**(W226 起在册)、**库里 `order` 字段本身会漂**:一律未碰。

8. **邻面落库文本那一族只有源级判据护着**(W273 §五 M2/M3 量出来的):把 `js/batchops.js` 的任务名
   或留痕文件名顺手改成实位号,`contract` 红 1 而 `skills` 一条不红——那一族没有真跑到任务中心
   那一屏的行为面用例。与 W272 §六 3/7 是同一类"半覆盖",落点不同(那两格是已收面的行为面缺口,
   这一格是**有意不收**那一族的边界只有一层)。要补得让 `hdPanel`/`erasePanel` 在单测沙箱里跑到
   `U.runTask` 那一屏(前面压着选择模式、成本预估与 `Media` 就绪判定几层),W273 没做,本槽也没做。

9. **(本槽新记)面板那行的 `|| '?'` 兜底半边两层都没有判据**(§四 M7 量出来的):
   摘掉它 `contract` 与 `skills` 各红 0,表外镜在面板上会印出「镜头 0」。
   源级那半只匹配 `Domain.shotNo(ep.shots, s)` 管不到兜底,行为面那条用例的夹具全在表内、
   那条分支跑不到。要补只需在行为面夹具里加一行**不在 `ep.shots` 里**的镜(或改用源级把 `|| '?'` 也锚进去),
   本槽照明令一条测都没加。**同族的兜底还有十几处**——`js/sb-views.js` 七处页头、`js/sb-gen.js` 三处、
   `js/sb-batch.js` 两处、`js/review.js` 四处都是 `|| '?'` 这个写法,大概率同一格开着,
   本槽只量了生成面这一处、没顺手去量别的面。

10. **(本槽新记)源级那条判据的半① 钉的是「恰一处」**(§四 M6):这一面将来合法多出一行面板镜号字
    也当场红,得同轮改判据。与半③(恰 0 处)是同一种"与产品耦合更紧"的代价,
    对侧只为半③ 写了这句说明,半① 没写。不算毛病,是这一档判据的已知定价,记下来免得下一面重量一遍。

## 七、跑了什么

- `node tests/unit.js` **693/693**(`P1` 基线 `ffa5e76` 上现跑 **691**);
- `node tests/unit.js contract` **152/152**(基线现跑 151);
- `node tests/unit.js skills` **104/104**(基线现跑 103);
- `node tests/integration.js` **152/152**(`env -u HUJING_SERVER` 及 `MV_*` 隔离;未加测,
  服务端与 CLI 一个字没动,全绿本身是"没搬坏"的判据);
- `node tests/cli.smoke.js` **单独整跑**、`env -u HUJING_SERVER -u MV_API_KEY -u MV_BASE -u MV_MODEL -u MV_IMG_MODEL -u MV_VIDEO_MODEL`:
  **115/117**,两条失败现取:

  ```
  FAIL | 未登录 whoami → exit 3 | exit=1
  FAIL | llm --json mock 链路 | undefined
  ```

  同 env 下在 `master` `9adcf0f` 的独立工作树上现跑 **51/53**,两条失败**同名同表现**,逐字相同;
- 不跑 `tests/e2e.js`(按明令);
- `node --check`:`js/produce.js`、`tests/unit.js` 均过。

## 八、交接

我方尖:见本支 `git rev-parse HEAD`(现取,不在此写死自称)。合点 `419005d`,基线 `ffa5e76`。
本槽动过的文件只有三个:`docs/skills-wave/README.md`(明写份数 288→289 + 索引一行)、
`tests/unit.js`(记账件 `const FLOOR` 288→289)与本文。
**`js/` 下逐字节未动**,`README.md` 未动(对侧已按 live 写到 693/152)。
未开 PR、未合 master、未开第三条功能支。
