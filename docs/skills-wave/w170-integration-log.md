# W170 · 三条槽并入集成线的收敛记录(集成分支)

> 集成分支:`cursor/w170-integration-98b2`,基线 `cursor/w166-integration-9a1c @ 4eaea86`。
> 本文只记**收敛过程**:基线怎么定的、合了哪三条、冲突面长什么样、合并后的实测数字、没删测没摘 gaps 的取证。
> 被合入三槽自己的内容分别在 `w163-review-min-literal.md`、`w165-review-issues-align.md`、`w167-extractdone-split.md`,本文不代述、不复写、不改它们正文里的任何时点读数。
> 三次合并都 `--no-ff`、各一个合并提交、可 revert;全程只解冲突与收敛数字,**不重做已落地的功能**。

## 1. 结果一句话

`--no-ff` 合入三条,按序、**各取被合入支的实际 HEAD**:

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w163-review-min-literal-1c3b@80ed5a1` | `c889fcd` | 审片达标线九处现取 `Domain.REVIEW_MIN`:确认闸 / 闭环开场文案 / 跑批中心整集均分标 / `review.js` 重抽入口与整集报告 / `sb-views.js` 状态条与版本区 / `agent-ops.js` 对话流两处 / `wf-core.js` 的 `MEM_LOW_SCORE`(删掉那个登记过的第二份常量而不是改成别名);源级判据**分严格档与分档档**两级,达标线那个数现取常量拼进正则、判据自己不当第十份字面;发布门可配阈值 `releaseMinReviewScore` 与提示词里给模型读的评分三档**有意不收编**并各有一句反向断言钉住此刻确实还各是各的 | `js/agent-ops.js` `js/batchops.js` `js/produce.js` `js/review.js` `js/sb-views.js` `js/wf-core.js` 六个文件 41 加 20 删 | +2 `unit`(1 `produce`、1 `domain` 源级) |
| `w165-review-issues-align-e7a0@fb5ffa6` | `8787438` | 分集级审片门槛收成 `episodeState().reviewGate` 一处:先判这一集**当下能不能审**(缺正文 / 未拆镜 / 分镜判旧一律 `unready`),过了门槛才按 `review-stale` / `no-review` / `low-review` / `pass` 四档归码;`workflow` 的 `review` 步三档集数与推荐动作、问题中心的分集审片条目同读这一份,两处不各设一道门 | `js/domain.js` +21 −9、`js/issues.js` +8 −7 | +3 `unit`(1 `domain`、1 `issues`、1 `contract`) |
| `w167-extractdone-split-3bb2@6726ac9` | `5fdef56` | `Domain.projectScript` 立成「项目整本原文这一份下游输入」的单源,与门槛派生的「剧本这一步走过没有」分成两问(`gateBlockers` 认 `extractDone` 也算走过,而拆集切的是原文);计划层的拆集步据此分两态——原文在库出可执行的命令步,原文缺位出**补原文的导航步**而不是推一条注定 `blocked / no-script` 的命令步 | `js/domain.js` +6、`js/plans.js` +10 −2、`js/commands.js` 2 行换取数口 | +3 `unit`(1 `commands`、1 `plans`、1 `contract`) |

合并后回归 **`unit 542/542`**、**`integration 141/141`**、**`cli.smoke 105/107`**(2 项失败与基线同名同表现,见 5.2),`contract` 子套件 **121/121**。

治理面:`gaps()` **20 键**(`G-03` 仍是两条值、`G-11` 仍在案)、提示词注册表 **41**、能力短名单 **30**、`Skills.CHECKS` **17**、`preflightStages()` **七面**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 八个数一个没动。
本槽**注册表五个文件相对基线零 diff**(`git diff --stat 4eaea86 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js` 空输出),故这八个数是结构性成立,与 W166 / W161 / W157 同形。

**本槽最值得记下的五件事:**

1. **产品文件冲突块回来了,而且两个都落在同一次合并里——`js/domain.js` 与 `js/issues.js`,两处的形态还不一样。**
   `domain.js` 那块是**两侧改同三行**:我方(基线里 W155)把 `bl('no-script', …)` 这类码字面换成了 `EPB.*` 登记表取值,对侧(W165)把同三行里的 `(ep.content || '').trim()` 提成 `hasScript` 变量供 `reviewGate` 复用。两侧改的是同一批行的不同关切,取并集(`EPB.*` 留着 + `hasScript` 提出来)。
   `issues.js` 那块**不是两侧改同一行**:对侧改的是审片段的段头注释,而 `git` 把注释上面那一行 `unconfirmed` 条目一并划进了块内——那一行我方是 `sev: EPB['unconfirmed']`、对侧是 `sev: 'low'`。处置是取我方那一行 + 对侧的注释。
   **而同一次合并里 W164 的 `shotIds` 那三行零冲突块自动留住**(`reviewGate` 换的是分支条件、`shotIds` 那三行一个字没动),即"给冲突块"与"两侧都落地"这两件事在**同一个文件的相邻两段**上同时出现了正反两例(3.2)。

2. **达标线挪一格的对照变异在合并后仍然只红 2,而 W165 新加的两个消费面一条都没红——这是"两槽真撞、两侧都留"最有力的实测。**
   W163 的记账件写着"挪达标线自此只动**两处**(常量 + 那一句产品口径断言)"。本槽把它那一手在集成线上逐字重跑(M2′):`D.REVIEW_MIN` 7→8 并同轮改 `assertEq(sb.Domain.REVIEW_MIN, …)`,实测**红 2**——按旧线挑的那个夹具与审片报告 `>= 8` 色阶撞号,**九个消费点一个没红**。
   值得记的是 W165 在同一条线上新开的两个消费面(`workflow` 的 `review` 步三档集数、问题中心的分集审片条目)**也一个没红**:它们读的是 `reviewGate`,而 `reviewGate` 里那句 `reviewAvg < D.REVIEW_MIN` 读的正是同一份常量。**两槽收的是同一条线的两段——W163 收"值",W165 收"档位"——合起来之后"挪一格只动两处"这句话仍然成立**,而不是变成三处四处。
   另记一格差别:只挪常量、不改那句断言(M2)是**红 3**,多出来的那条正是 W163 有意留着的 `assertEq(sb.Domain.REVIEW_MIN, 7)`——它就是那句话里的"第二处"(5.5)。

3. **"值一样也是第二份投影"这一课本槽第二次被实测,而这次红的只有源级那一条。**
   M3 把 `js/issues.js` 的 `if (st.reviewGate === 'low-review')` 换回等价的 `if (!st.reviewStale && st.reviewAvg !== null && … < Domain.REVIEW_MIN)`——**行为完全一致**,`issues` 与 `domain` 的行为面用例、双向相等那条**全绿**,只有 W165 源级那条报「问题中心的 `low-review` 应按 `st.reviewGate` 取,取值点丢了即红」。
   这与 W164 那槽的 M5(把 `Domain.reviseShotIds(ep)` 换成就地 `lows.map(x => x.shotId)`)**同形**:两次都是"值相同而判据仍红",两次都只有源级那一条接得住。**双向相等那条判据看起来最像能接住它,实际接不住**——因为它比的是两侧,而这一改把两侧一起改了。

4. **三支自称的记账件份数里有两支逐字相同的 171,静默窗口连着第三槽出现。**
   `w163` 与 `w165` 自称的份数都是 **171**(两支同叉在 `8507194`,各加一份记账件),`w167` 自称 **175**(叉在 `c5de21e`)。W161 那槽是三支全 163、W166 那槽是两对各自相同,本槽是一对相同 + 一支落单。
   三个都没能静默,只因我方那一格已被前一次合并抬到 179;三次合并里那一格**一次都没变过**(本槽三支没有一支加记账件,记账件只有本文这一份,故 `FLOOR` 直到本文落地才从 179 抬到 183)。

5. **按名比对的排序口径本槽又踩了一次,形态与 W166 那次不同而后果一样。**
   W166 记的是"JS `Array.sort()` 与 `comm` 的排序口径不一致会同时多报两侧"。本槽抽名脚本里已经写了 `LC_ALL=C sort`,**而调用 `comm` 的那一层没有**——`comm` 按当前 locale 判"输入没排好序",第一轮当场**假报 8 条"只在基线里有的"**(全是 `produce ·` 打头的既有用例)。
   判它是假报靠的仍是人读:那 8 条在 tip 上一条不缺,而"删掉 8 条既有 `produce` 用例"这件事本槽三支谁都没做。改成两侧与 `comm` 都在 `LC_ALL=C` 下跑之后三轮全部干净(5.3)。**这是 W166 残留 4 的第二次发作,而它两次的表现不同**:那次是抽名与比对的口径不一致,这次是脚本内与脚本外的口径不一致——**同一个洞有两种踩法,而两次都没有任何判据管**(6.4)。

## 2. 基线与这三个槽

### 2.1 基线

任务口径给定 `origin/cursor/w166-integration-9a1c @ 4eaea86`。现取核实,并逐支核对任务给的参考 sha:

```
$ git rev-parse origin/cursor/w166-integration-9a1c
4eaea86b45dd4852bf912802083b04ca306e1f41
$ git rev-parse --short origin/cursor/w163-review-min-literal-1c3b  → 80ed5a1
$ git rev-parse --short origin/cursor/w165-review-issues-align-e7a0 → fb5ffa6
$ git rev-parse --short origin/cursor/w167-extractdone-split-3bb2   → 6726ac9
```

四个都与任务给的参考值一致(三支都取的现取 HEAD)。

基线上的三套件数字(开工时现跑,不引用 `w166-integration-log.md` 里的数):

| 套件 | 基线 `4eaea86` |
|---|---|
| `node tests/unit.js` | **534 / 534**(`contract` **119 / 119**) |
| `node tests/integration.js` | **141 / 141** |
| `node tests/cli.smoke.js` | **105 / 107** |

与任务给的参考数字逐个相符,`cli.smoke` 那 2 项失败也与任务口径说的一致(见 5.2)。

### 2.2 三支的叉点:三条都是真三方

| 支 | 与基线的 `merge-base` | 那是什么 | 基线在其之上还有 | 该支自己有 |
|---|---|---|---|---|
| `w163` | `8507194` | W157 集成记账件 | **23 个提交** | 2 个 |
| `w165` | `8507194` | 同上 | **23 个提交** | 2 个 |
| `w167` | `c5de21e` | W161 集成记账件 | **13 个提交** | 2 个 |

三次都 `merge-base != HEAD`,**都是真三方合并**。

基线在这两个叉点之上的提交里含 W155 / W158 / W159 / W160 / W162 / W164 / W166 等槽,故:

- W155 的 `EPB` 登记表已在基线的 `js/domain.js` 与 `js/issues.js` 里,而 `w165` 叉在它**落地之前**——两个产品冲突块都由此而来(1.1、3.2);
- W164 的 `low-review` 派生 `shotIds` 已在基线的 `js/issues.js` 里,而 `w165` 改的是同一段的分支条件——`git` 零冲突块自动融合,两半都在(3.2);
- `w163` 与 `w165` 都碰审片这条线,任务口径预告"真撞两侧都留"。实测**两支在产品文件上一个字都没撞**(`w163` 动的六个文件 `w165` 一个没动,反之亦然),撞的是**语义**而不是行:两槽收的是同一条达标线的两段,合起来仍是一份(1.2);
- `w167` 叉在 W161 之后,故它看得见 W153 的门槛派生,`js/plans.js` 那处改动是在 W153 已落地的 `TODO_OF` 上加分流——与基线零冲突块。

### 2.3 不合的

任务口径明确不合在飞的 **W168** 与 **W169**,本槽一条都没碰,也未做叉点核实。
除此之外远端未合入本线的 `cursor/*` 支,按任务口径不在本槽范围内。

## 3. 冲突面

三次合并共 **11 个文件次、21 个冲突块**,分布:

| 合并 | `README.md` | `docs/skills-wave/README.md` | `tests/unit.js` | 产品文件 | 小计 |
|---|---|---|---|---|---|
| `c889fcd`(W163) | 1 | 2 | 2 | — | 5 |
| `8787438`(W165) | 2 | 2 | 3 | **2**(`js/domain.js` 1、`js/issues.js` 1) | 9 |
| `5fdef56`(W167) | 3 | 2 | 2 | **0** | 7 |

**产品文件冲突块 2 个** —— W166 那槽是零,本槽回到有,且两个都在第二次合并里(3.2)。

### 3.1 第一次合并(W163 `80ed5a1` → `c889fcd`,5 块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `tests/unit.js` | 三套件下限 | 534/141/107 / 526/141/107 | 单元现取 live **536**;集成与冒烟两侧同值,取我方 |
| `tests/unit.js` | 记账件 `FLOOR` | 179 / 171 | 本槽这一步没加记账件,取我方 **179** |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带全部数段 / 对侧**只改了那个数**(524 → 526) | 取我方整句,数字换 live **536** |
| `docs/skills-wave/README.md` | 明写份数 | 179 / 171 | 现取 live **180**(对侧新增 `w163-review-min-literal.md` 一份) |
| `docs/skills-wave/README.md` | 索引表尾 | 我方 `w158`…`w166` 七行 / 对侧 `w163` 一行 | **并集**,八行按波次号排(`w163` 归到 `w162` 与 `w164` 之间) |

**六个产品文件零冲突块,而它们不是"没被两侧改过"的那种零**:`w163` 动的是 `agent-ops` / `batchops` / `produce` / `review` / `sb-views` / `wf-core`,基线自 `8507194` 之后在这六个文件上一行都没动过(`git diff 8507194 4eaea86 -- <六个文件>` 只在 `wf-core.js` 有他槽的无关改动且不在同一段)。故 `git` 直接搬入。机检:

```
机检 1:合并后这六个文件与对侧 tip 逐字节相同(git diff 80ed5a1 c889fcd -- <六个文件> 空输出) ✓
机检 2:合并后 js/ 相对对侧的差只有 domain.js / issues.js / flow-tpl.js 三个(全是基线自己那几槽的改动,与 w163 无关) ✓
机检 3:六个文件 node --check 全过 ✓
```

**这一次合上首跑就 536/536 全绿**,一条都没红——与 W166 第一次合并(`hosts` 那格当场红 1)不同,`w163` 带来的判据在集成线上没有多出落点。

### 3.2 第二次合并(W165 `fb5ffa6` → `8787438`,9 块,含两个产品块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| **`js/domain.js`** | `episodeState` 开头三行 | **两侧改同三行、关切不同**:我方(W155)把码字面换成 `EPB.*` 登记表取值,对侧把 `(ep.content \|\| '').trim()` 提成 `hasScript` 供 `reviewGate` 复用 | **并集**:`EPB.noEpisode` / `EPB.script` / `EPB.shots` 留着,`hasScript` 提出来 |
| **`js/issues.js`** | 审片段段头注释 + 上面那一行 | 对侧只改注释,而 `git` 把注释上面那一行 `unconfirmed` 条目一并划进块内:我方是 `sev: EPB['unconfirmed']`、对侧是 `sev: 'low'` | 取我方那一行 + 对侧的注释 |
| `tests/unit.js` | `issues` 套件尾部 | **两侧都是纯插入且锚点逐字相同**:我方是 W164 那条 `contract`,对侧是 W165 源级那条 | **并集**,我方在前(块尾 `  } },` 两侧共用,给我方那半补一个) |
| `tests/unit.js` | 三套件下限 | 536/141/107 / 527/141/107 | 单元现取 live **539** |
| `tests/unit.js` | 记账件 `FLOOR` | 179 / 171 | 仍取 **179** |
| `README.md` | 「文档数字对账契约」整句 | **两侧只差 `contract` 那个数**(我方 119 / 对侧 115) | 取我方整句,数字换 live **120** |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带全部数段 / 对侧在 `issues.js` 那一节里另插一整段讲双向相等 | **并集**,按对侧锚点(「审片记录判旧报 `review-stale` 且与未审/低分三态互斥(快照对齐后消失)」之后)插回;数字换 live **539** |
| `docs/skills-wave/README.md` | 明写份数 | 180 / 171 | 现取 live **181** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方八行 / 对侧 `w165` 一行 | **并集**,九行按波次号排 |

**两个产品冲突块的形态不一样,而"要不要取并集"的答案一样。**

`domain.js` 那块是教科书式的"同三行两侧各改各的一半":

```
我方(P1,含 W155):  if (!(ep.content || '').trim()) bl(EPB.script, '缺剧本正文');
对侧(P2):           const hasScript = !!(ep.content || '').trim();
                      if (!hasScript) bl('no-script', '缺剧本正文');
结果(M):            const hasScript = !!(ep.content || '').trim();
                      if (!hasScript) bl(EPB.script, '缺剧本正文');
```

`issues.js` 那块**两侧改的其实不是同一行**——对侧只重写了段头那五行注释,`git` 把注释紧邻的上一行也划进块内,而那一行恰好是我方 W155 改过的。这一格如果机械取对侧,`sev` 会从 `EPB['unconfirmed']` 退回字面 `'low'`,而**行为完全一样**(`EPB['unconfirmed']` 现取就是 `'low'`)、`git` 不报任何东西、单测也不会红在这一格上——**它只会在下次 `EPB` 那张表改档位时才悄悄分叉**。这一处是靠逐行读块内容认出来的,没有判据。

**而同一个文件的相邻一段(W164 的 `shotIds` 三行)`git` 零冲突块自动留住**,合并后现取:

```
js/issues.js:238   if (st.reviewGate === 'low-review') {          ← W165 那半
js/issues.js:247   if (lows.length) low.shotIds = Domain.reviseShotIds(ep);  ← W164 那半
js/domain.js:585   const reviewGate = !hasScript || counts.total === 0 || shotsStale ? 'unready'  ← W165 那半
js/domain.js:571   if (!hasScript) bl(EPB.script, '缺剧本正文');  ← W155 + W165 两半
```

即**"给冲突块"与"两侧都落地"在同一个文件的相邻两段上同时出现了正反两例**:相邻段自动合上、注释块反倒给了块。变异 M8(给 `EPB` 加一档)与 M9(去掉 `if (lows.length)` 这道闸)分别反向再证 W155 那半与 W164 那半确实都还在跑(5.5)。

### 3.3 第三次合并(W167 `6726ac9` → `5fdef56`,7 块,产品面零冲突块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `README.md` | 「文档数字对账契约」整句 | 两侧只差 `contract` 那个数(我方 120 / 对侧 117) | 取我方整句,数字换 live **121** |
| `README.md` | 「🩺 问题中心」与「📋 持久计划」**相邻两行** | 我方改的是上面那行(W164 的 `shotIds` 那段)、对侧改的是下面那行(拆集分两态那段) | **并集**:我方那行 + 对侧那行,各取各的 |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带全部数段 / 对侧**只改了那个数**(528 → 531) | 取我方整句,数字换 live **542** |
| `tests/unit.js` | 三套件下限 | 539/141/107 / 531/141/107 | 单元现取 live **542** |
| `tests/unit.js` | 记账件 `FLOOR` | 179 / 175 | 仍取 **179** |
| `docs/skills-wave/README.md` | 明写份数 | 181 / 175 | 现取 live **182** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方九行 / 对侧 `w167` 一行 | **并集**,十行按波次号排 |

**`js/domain.js` 这次零冲突块,而两侧都改了它。** 对侧把 `D.projectScript` 插在 `episodeState` 那个 `};` **之后**,我方(上一次合并刚落的 W165)改的是同函数**体内**的 `reviewGate` 与 `return` 行。两侧改的行不相干,`git` 直接融合。现取核实两半都在:

```
js/domain.js:585  const reviewGate = … ? 'unready' : …    ← W165 那半
js/domain.js:606  return { …, reviewGate, … };            ← W165 那半
js/domain.js:613  D.projectScript = p => String((p && p.script) || '').trim();  ← W167 那半
js/domain.js:650  const rvOf = code => epStates.filter(st => st.reviewGate === code).length;  ← W165 那半
js/plans.js:64    return Domain.projectScript(p) ? … : …  ← W167 那半
js/commands.js:184 const text = Domain.projectScript(p);  ← W167 那半
```

**`js/plans.js` 与 `js/commands.js` 两个文件合并后与对侧逐字节相同(`M == P2`),而这不是取 `--theirs`**:这两个文件我方相对叉点零 diff(`P1 == B`),即基线自 `c5de21e` 之后一行都没动过它们,`git` 只能搬对侧那份。分开靠 `P1 == B` 这一格,不是靠哈希对上(5.1)——与 W161 那槽「`M == P1` 与 `--ours` 在哈希上分不出来」是同一件事的另一侧。

### 3.4 三次都没有摘 gaps、没有删测

三条槽各自加自己的用例;既有用例**改了 0 条断言、删了 0 条、翻面 0 条、改名 0 条**。
`GUARD_TOPICS` 十条一条没动(`plans-gate-read` 那条的 `hosts: 2` 原样,W167 改了 `js/plans.js` 而落点数没变)。
三次合并全程未用 `checkout --ours/--theirs`,也未用 `git checkout <old> -- .`。

## 4. 数字怎么定的

### 4.1 一律现取合并后 live,不抄任何一个父值

| 数 | 基线 | W163 侧 | W165 侧 | W167 侧 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|---|
| 单元用例数 | 534 | **526** | **527** | **531** | **542** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 119 | **114** | **115** | **117** | **121** | 主 `README.md` 契约段 |
| `integration` | 141 | 141 | 141 | 141 | **141** | 主 `README.md` + 下限表 |
| `cli.smoke` | 107 | 107 | 107 | 107 | **107**(实跑 105/107) | 主 `README.md` + 下限表 |
| 记账件份数 | 179 | **171** | **171** | **175** | **182**(含本文后 183) | `docs/skills-wave/README.md` 明写 |
| 记账件 `FLOOR` | 179 | 171 | 171 | 175 | **183**(本文落地后一次抬到位) | `tests/unit.js` |

逐次落定的读数(每次合完当场跑,不等三次合完再算):

| | 合入 W163 后 | 合入 W165 后 | 合入 W167 后 |
|---|---|---|---|
| `unit` | 536 / 536 | 539 / 539 | **542 / 542** |
| `contract` | 119 / 119 | 120 / 120 | **121 / 121** |
| `integration` | — | — | **141 / 141** |
| `cli.smoke` | — | — | **105 / 107** |
| 记账件份数 | 180 | 181 | **182** |

**六个自称数一个都不是答案**(`integration` / `cli.smoke` 两格三支都与 live 相等,成因是这两个套件本槽三支一条都没碰、且三支的叉点在这两个数上都够新——与 W166 那槽四支同时如此是同一个结构事实,不是运气)。三处值得记:

- **`w163` 与 `w165` 自称的份数逐字相同的 171**(1.4),两支同叉 `8507194`、各加一份记账件。W161(三支全 163)、W166(两对各自相同)之后,**同一个静默窗口连着第三槽出现**。
- **`w163` 自称的 `contract` 条数 114 是三支里最低的,而它一条 `contract` 用例都没加**——那是它叉点上的旧值,它自己没动过这个字面。若照抄它,与 live 差 **7 格**。
- **记账件 `FLOOR` 这一格本槽三次合并一次都没动过**:三支谁都没加记账件,故它一直停在我方的 179,直到本文落地才一次抬到 183。这与前几槽"逐次抬"的形态不同,如实记下。

### 4.2 `FLOOR` 抄父值与 README 抄父值:逐格实测

在**本文落地后的 tip** 上逐格实测,结果见 5.6。

## 5. 取证

### 5.1 合并纪律:按四棵树的文件哈希机检

对每次合并,取 `M`(合并结果)、`P1`(我方)、`P2`(对侧)、`B`(叉点)四棵树里同一文件的 blob 哈希做对照。
前两次的 `B` 是 `8507194`,第三次是 `c5de21e`。只列三者不全等的文件次。

| 合并 | 文件 | `M` | `P1` | `P2` | `B` | 结论 |
|---|---|---|---|---|---|---|
| `c889fcd` | `README.md` | `cf9b143` | `2911253` | `13a5bd3` | `97f8f88` | 真融合 |
| `c889fcd` | `docs/skills-wave/README.md` | `a1c2800` | `ebc3e7f` | `134990d` | `9a5b0f6` | 真融合 |
| `c889fcd` | `tests/unit.js` | `9a99807` | `00b3bec` | `22a92c9` | `19c218e` | 真融合 |
| `c889fcd` | `js/domain.js` | `f4d427e` | `f4d427e` | `843ebcf` | `843ebcf` | `M == P1` **且 `P2 == B`**:对侧没动过它 |
| `c889fcd` | `js/issues.js` | `65c72c7` | `65c72c7` | `b23ae3d` | `b23ae3d` | 同上 |
| `8787438` | `README.md` | `47f80a0` | `cf9b143` | `029ce2b` | `97f8f88` | 真融合 |
| `8787438` | `docs/skills-wave/README.md` | `f9c83f3` | `a1c2800` | `a658995` | `9a5b0f6` | 真融合 |
| `8787438` | **`js/domain.js`** | `f1d0fbb` | `f4d427e` | `e36494f` | `843ebcf` | **真融合(W155 的 `EPB` 那半与 W165 的 `hasScript`/`reviewGate` 那半都在)** |
| `8787438` | **`js/issues.js`** | `20ac99a` | `65c72c7` | `8f9bd2a` | `b23ae3d` | **真融合(W155 的 `EPB` 档位、W164 的 `shotIds`、W165 的 `reviewGate` 三半都在)** |
| `8787438` | `tests/unit.js` | `5c2a99f` | `9a99807` | `c801f7e` | `19c218e` | 真融合 |
| `5fdef56` | `README.md` | `6ce143d` | `47f80a0` | `1d0a9f5` | `7094da8` | 真融合 |
| `5fdef56` | `docs/skills-wave/README.md` | `acf2f6a` | `f9c83f3` | `fc3f05f` | `6f387a6` | 真融合 |
| `5fdef56` | **`js/domain.js`** | `dd6c57f` | `f1d0fbb` | `2660043` | `f4d427e` | **真融合(零冲突块,`reviewGate` 与 `projectScript` 两半都在)** |
| `5fdef56` | `js/issues.js` | `20ac99a` | `20ac99a` | `113afbf` | `113afbf` | `M == P1` 且 `P2 == B`:对侧没动过它 |
| `5fdef56` | `js/plans.js` | `8a77052` | `c2b7f93` | `8a77052` | `c2b7f93` | `M == P2` **且 `P1 == B`**:我方没动过它 |
| `5fdef56` | `js/commands.js` | `9c976e3` | `555685b` | `9c976e3` | `555685b` | 同上 |

**整份取 `--ours` 零命中、整份取 `--theirs` 零命中、回退到叉点内容零命中。**
上表里四个 `M == P1` 与两个 `M == P2` 的格子**全部伴随另一侧 `== B`**——即那一侧根本没改过这个文件,`git` 只能搬另一份。这一格是本槽把 W161 那条教训("`M == P1` 与 `--ours` 在哈希上分不出来")落成机检的写法:**判"是不是 `--ours`/`--theirs`"要看的是另一侧等不等于叉点,不是看 `M` 等于谁**。
三份新记账件是纯新增文件(`P1` / `B` 里都不存在),不参与上表。

### 5.2 三套件实跑

| 套件 | 基线 `4eaea86` | 合入 W163 后 | 合入 W165 后 | 合入 W167 后(本槽 tip) |
|---|---|---|---|---|
| `node tests/unit.js` | 534 / 534 | 536 / 536 | 539 / 539 | **542 / 542** |
| └ `contract` 子套件 | 119 / 119 | 119 / 119 | 120 / 120 | **121 / 121** |
| `node tests/integration.js` | 141 / 141 | — | — | **141 / 141** |
| `node tests/cli.smoke.js` | 105 / 107 | — | — | **105 / 107** |

`cli.smoke` 那 2 项失败逐条现取,基线与 tip 上**同名同表现**:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

本槽三支一条冒烟用例都没加,故分母仍是 107、失败仍是这两条。

单元用例的**不同名字数**现取 542,与用例数 542 相等——**连续第十八槽全部相等**。

### 5.3 没删测:按名成集**三轮**比对

按 W157 立的口径做两轮,再按 W161 残留 3 / W166 那槽照做过的第三种比法补一轮。抽名脚本在**各 tip 的独立 `git archive` 解包树**上跑,不在工作区跑;抽名与 `comm` 两侧一律 `LC_ALL=C`(踩坑见 1.5)。

**第一轮:基线 `4eaea86` ↔ tip `5fdef56`**

- **只在基线里有的:0 条**;
- 只在 tip 里有的:**8 条**,逐条点名:
  - `produce · autoSmartReview 达标线现取 Domain.REVIEW_MIN:压线达标、常量挪一格闭环跟着挪(不再冻一份字面)`(W163)
  - `domain · REVIEW_MIN 消费点零分叉(源级):达标判定与确认闸不得再冻第二份达标数字面`(W163)
  - `domain · workflow:审片步只数 episodeState.reviewGate——尚不可审的集(缺正文/未拆镜/分镜判旧)不进任何一档,留着的旧报告也不冒充结论`(W165)
  - `issues · collect:分集审片条目与主线 review 步逐集同判——同一集同一份报告,两侧双向相等(任一侧多报/漏报即红)`(W165)
  - `contract · 分集级审片门槛单源:达标线/判旧/"这一集当下能不能审"只在 episodeState.reviewGate 一处,流程条与问题中心都不另判`(W165)
  - `commands · splitEpisodes:切分输入现取 Domain.projectScript(提取过主体的老项目仍 blocked no-script,提取结论不冒充原文)`(W167)
  - `plans · fromWorkflow:拆集步按整本原文在不在分两态(缺原文出补原文导航步,不推注定 blocked 的命令步)`(W167)
  - `contract · 拆集的切分输入单源:整本原文在不在只在 Domain.projectScript 一处,命令层与计划层同读`(W167)

534 − 0 + 8 = 542,与实跑总数对得上。

**第二轮:逐次比对**

| | 上一步独有 | 新增 |
|---|---|---|
| `4eaea86` → `c889fcd` | **0 条** | 2 条(W163 那两条) |
| `c889fcd` → `8787438` | **0 条** | 3 条(W165 那三条) |
| `8787438` → `5fdef56` | **0 条** | 3 条(W167 那三条) |

**第三轮:被合入支的用例名集合 ↔ 合并后**

| 支 | 自己的用例数 | 名字在 tip 上找不到的 |
|---|---|---|
| `w163@80ed5a1` | 526 | **0 条** |
| `w165@fb5ffa6` | 527 | **0 条** |
| `w167@6726ac9` | 531 | **0 条** |

**三支的用例名在合并后一条不缺**:本槽没有翻面、没有改名、没有接位,故第三轮与前两轮结论一致。这个盲区(W161 残留 3)**本槽照旧没被触发,也没有被收掉**,第三轮仍是人工做的。

### 5.4 没摘 gaps;注册表零 diff,故八个数结构性成立

```
$ git diff --stat 4eaea86 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js
(无输出)
```

本槽**五个注册表文件相对基线一个字节都没动**,故八个数结构性成立;仍在合并后 HEAD 上逐个现跑确认:
提示词注册表 **41**、能力短名单 **30**、`CHECKS` **17**、`preflightStages()` **7**、`gaps()` **20 键**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 一个没动。

`gaps()` 现取 20 键逐字:`G-08,G-15,G-09,G-01,G-02,G-12,G-13,G-04,G-10,S-01,S-03,G-07,G-14,G-05,S-04,S-05,G-03,G-11,S-06,S-07`。
两条相关的键**都没摘**:`G-03` 仍是 `review.stage` / `review.reviseLoop` 两条值,`G-11` 仍是 `review.memoryFeedback`。
**`expert.evolve` 仍不在任何 playbook 的 `steps` 里**(现取 `playbooks()` 5 条、`steps` 共 25 步,含 `evolve` 的**零处**);变异 M11 钉着这一点。

产品面进 diff 的文件与行数(本槽自己那一层):

```
$ git diff --stat 4eaea86 HEAD -- js/ server.js cli.js mcp.js billing.js
 js/agent-ops.js |  6 +++---
 js/batchops.js  |  8 ++++----
 js/commands.js  |  4 ++--
 js/domain.js    | 36 +++++++++++++++++++++++++++---------
 js/issues.js    | 15 ++++++++-------
 js/plans.js     | 12 ++++++++++--
 js/produce.js   |  6 +++---
 js/review.js    | 10 +++++-----
 js/sb-views.js  |  6 +++---
 js/wf-core.js   |  5 +++--
```

**三条槽都动了产品文件**,合计十个文件 68 加 40 删——与 W166 那槽(三条零产品改动、只有 `js/issues.js` 一个文件)形态相反。

### 5.5 抽查变异

全部在**合并后 HEAD** 上跑,一次只改一处,跑完即还原(还原后复跑 542/542 全绿、工作区 clean)。
按 W161 残留 13 / W166 立的规矩,**每条变异先现取一次被改对象的实际产出再看颜色**——本槽这一步每条都做了,记在下表「现取」列。

**A. 本槽新合进来的八条,各证是新覆盖**

| # | 变异 | 现取 | 合并后 HEAD |
|---|---|---|---|
| M1 | `js/produce.js` 把确认闸那行换回 `if (r.score >= 7)` | `>= 7` 命中 1 处、`>= Domain.REVIEW_MIN` 命中 0 处 | **红 2**:W163 行为面(「达标线挪高一格后,原压线分不再达标」)+ W163 源级严格档点名 `js/produce.js` |
| M2 | `D.REVIEW_MIN` 7 → 8,**只改常量** | `Domain.REVIEW_MIN = 8` | **红 3**:按旧线挑的夹具、`review.js` 的 `>= 8` 色阶撞号、**以及 W163 有意留着的 `assertEq(sb.Domain.REVIEW_MIN, 7)`** |
| **M2′** | 同上但按 W163 记的那一手**同轮改那句产品口径断言** | `Domain.REVIEW_MIN = 8`、那句断言已改成 8 | **红 2**,与 W163 自称逐字一致;**九个消费点一个没红,W165 新加的两个 `reviewGate` 消费面也一个没红**(1.2) |
| M3 | `js/issues.js` 把 `st.reviewGate === 'low-review'` 换回等价的 `reviewAvg` 三段式(**值一样**) | `reviewGate` 取值点从 3 处降到 2 处、`Domain.REVIEW_MIN` 从 0 处升到 1 处 | **红 1**,只红 W165 那条 `contract`;**行为面与双向相等那条全绿**(1.3) |
| M4 | `js/domain.js` 去掉 `reviewGate` 的 `unready` 那一档 | `'unready'` 在 `domain.js` 里从 2 处降到 1 处(只剩注释) | **红 2**:`domain` 那条报「缺正文:该集当下不可审,期望 `unready` 实际 `no-review`」、`issues` 双向相等那条报「e5 两侧不等」 |
| M5 | `js/plans.js` 把拆集那两态改回单态命令步 | `Domain.projectScript` 在 `plans.js` 里命中 0 处 | **红 3**:W167 的 `plans` 那条、W167 的 `contract` 那条,**外加基线里一条既有用例**(「剧本这一步与流程条同口径」——W167 同轮把它的期望改成了 `goto:split`) |
| M6 | `js/commands.js` 把 `splitEpisodes` 的取数口换回 `String(p.script \|\| '').trim()` | `Domain.projectScript` 在 `commands.js` 里从 2 处降到 1 处 | **红 2**:W167 的 `commands` 行为面 + `contract` 报「拆集命令不得再自己读 `p.script`」 |
| **M7** | `Domain.projectScript` 改成**把 `extractDone` 当原文**(读不到 `p.script` 时回一句「(已提取主体,原文从略)」) | `projectScript({extractDone:true})` 现取 `"(已提取主体,原文从略)"` | **红 4**:`commands` 报拆集从 `blocked` 变成了 `done`、`plans` 两条(前提夹具与两态分流)、`contract` 报「提取结论(主体库/卖点梗概)都不是原文:它们切不出逐字保留的分集正文」。**这是任务口径点名的那条纪律("不要把 `extractDone` 当原文")的正面实测** |

**B. 任务点名要保的,逐样反向变异一次**

| 要保的 | 反向变异 | 现取 | 实测 |
|---|---|---|---|
| **W155 的 `EPB` 扇出**(任务点名"两侧都留") | `js/domain.js` 的 `EPB` 表新增一档 `dubbing: 'no-dubbing'` | `epBlockerCodes()` 多出 `no-dubbing` | **红 1**,报「分集阻塞码 `no-dubbing` 既没有触发夹具、也没在 `Issues.epSkips()` 登记不投的理由」——证明 W155 那半在合并后确实还在跑 |
| **W164 的 `low-review` `shotIds`** | `js/issues.js` 去掉 `if (lows.length)` 这道闸 | 该闸命中 0 处 | **红 1**,报「空重抽面不得出 `shotIds`(空数组在子集位上等于整集重跑),实际:`[]`」 |
| **PLACEHOLDER 禁令**(源级 + 运行期) | `js/release.js` 落地前插一行占位空壳 `ZipUtil.download(name, [{name:'PLACEHOLDER', data:'空下载兜底:请重新打包'}])` | 可执行行里 `ZipUtil.download` 1 处、`PLACEHOLDER` 1 处 | **红 6**,含 W156 翻面那条、W162 源级那条(把命中行号与整行原样印出来)、W162 运行期那条与三条行为面 |
| **同上,别名写法**(M10′,W166 那槽 M3′ 的复跑) | 成功路径插 `const Z = ZipUtil; Z.download(name, [{ name: 'meta.json', data: '' }]);` | 可执行行里 `ZipUtil.download` **0 处**、`PLACEHOLDER` **0 处** | **红 3**:W152 行为面(用户到手 2 个文件)、W154 落地失败那条、**W162 运行期那条**;**两条源级判据全绿**。与 W166 那次红 2 的差别只在插入位置(我这次插在 `try` 之前,落地失败那一路也先落了一个包),**"源级够不着别名、运行期够得着"这个结论逐字复现** |
| **`GUARD_TOPICS` 护栏主题清单** | 把 `gate-blockers-single` 那条主题的**唯一承载用例整条删掉**(无继任者) | 该用例名在文件里剩 0 处 | **红 4**:数字对账 2 条、三套件下限 1 条、**W158 失联那条 1 条**并把主题编号 `gate-blockers-single` 与它的锚点、`why` 原样印出来 |
| **`expert.evolve` 不进 `steps`** | `js/skills.js` 把 `expert.evolve` 掺进主线全链 playbook 的 `steps` | `playbooks()` 5 条、`steps` 共 **26** 步、含 evolve **1** 处 | **红 3**:`plans` 报「投影每一步都须登记取材器,漏登记:`expert.evolve`」、`contract` 报主线全链九步字面对不上、`flow` 报「投影每一步都须在中段登记」 |

**C. 一条对照变异:本槽新加的判据自己没进 `GUARD_TOPICS`**

| # | 变异 | 实测 |
|---|---|---|
| M13 | 把 **W165 那条源级用例**整条删掉(与 M12 同一种手法,只是换成本槽新加的那条) | **红 3**:数字对账 2 条 + 三套件下限 1 条。**`GUARD_TOPICS` 失联那条不报**——本槽三支新加的三条源级护栏一条都没登记进那张表 |

M12 与 M13 的差就是 `GUARD_TOPICS` 那一格:**登记过的护栏被删会被点名并印出主题编号,没登记的只剩数字层兜着**,而数字层拦不住"删一条、同轮把 README 与 `FLOOR` 一起改小"。登记在残留(6.1)。

### 5.6 棘轮:四格 `FLOOR` 与 README 数字逐格实测

在**本文落地后的 tip** 上把四个 `FLOOR` 逐格改小重跑全套(`SLACK` 现取仍是 3):

| 格 | 值 | 与 live 的差 | 全套结果 |
|---|---|---|---|
| 单元测试(live 542) | 541 / 540 / 539 | 1 / 2 / 3 | 全绿 |
| 单元测试 | **534**(基线父值) | **8** | **红 1 条** |
| 单元测试 | **531**(`w167` 父值) | **11** | **红 1 条** |
| 单元测试 | **527**(`w165` 父值) | **15** | **红 1 条** |
| 单元测试 | **526**(`w163` 父值) | **16** | **红 1 条** |
| 记账件份数(live 183) | 182 / 181 / 180 | 1 / 2 / 3 | 全绿 |
| 记账件份数 | **179**(基线父值) | **4** | **红 1 条** |
| 记账件份数 | **175**(`w167` 父值) | **8** | **红 1 条** |
| 记账件份数 | **171**(`w163` 与 `w165` 父值同为 171) | **12** | **红 1 条** |
| `integration`(live 141) | 140 / 139 / 138 | 1 / 2 / 3 | 全绿 |
| `integration` | 137 | 4 | **红 1 条** |
| `cli.smoke`(live 107) | 106 / 105 / 104 | 1 / 2 / 3 | 全绿 |
| `cli.smoke` | 103 | 4 | **红 1 条** |

**四格全部逮住全部父值,包括基线自己那两个。** 单元那格差 8(本槽加 8 条用例)、记账件那格**差 4**——刚好越过 `SLACK` 3 一格,是本槽最险的一格:本槽三支一份记账件都没加,记账件的全部增量就是本文这一份加上……不,本槽记账件从 179 涨到 183 是四份(三支各带一份自己的记账件 + 本文),故差 4 恰好越线。**若本槽只合两支,基线那个 179 就会被差 3 蒙过去**——与 W162 那槽"只加 2 条被差 2 蒙过去"同形,如实登记(6.5)。
十一槽连读:W133 差 1 全放过、W134 放过一逮住一、W139 全放过含一次压线、W142 单元三个父值全逮住、W146 差 3 压线放过 + 差 5–11 全逮住、W148 单元全逮住 + 记账件差 3 压线放过、W151 四格差 4–22 全逮住、W157 四格差 4–14 全逮住、W161 四格差 4–18 全逮住、W166 四格差 5–14 全逮住、**W170 四格差 4–16 全逮住(记账件那格差 4,压着 `SLACK` 上沿过线)**。

README 那三处抄父值同样逐处实测:

- 主 `README.md` 单元数改成 534 / 531 / 527 / 526 → **各红 2 条**(精确对账那条报「实测 542,文档 N」+ 三套件下限那条报「README 写的 N 低于下限」);
- 主 `README.md` `contract` 条数改成 119 / 117 / 115 / 114 → **各红 1 条**(契约段自报条数那条);
- `docs/skills-wave/README.md` 明写份数改成 179 / 175 / 171 → **各红 1 条**(三方对齐那条)。

### 5.7 本槽自己改了什么

除三个合并提交解冲突外,本文这一提交动了四处:新增本文、`docs/skills-wave/README.md` 补索引行并把明写份数 182 → 183、把"最新一份收敛记录"的指针从 `w166-integration-log.md` 挪到本文(旧指针进"更早的分叉"那一串)、`tests/unit.js` 的记账件 `FLOOR` 179 → 183。
单元用例数与 `contract` 条数**本文一条不加**,故 542 / 121 不动。

三个合并提交里**没有删过任何一条用例、没有放宽过任何一条断言、没有翻过面、没有改过名**;`GUARD_TOPICS` 十条一个字没动。

## 6. 残留与欠账

1. **本槽三支新加的三条源级护栏一条都没进 `GUARD_TOPICS`,而 M12 / M13 把这个差实测了出来。** 删掉一条**登记过**的护栏红 4 并点名主题编号,删掉本槽新加的那条只红 3(全是数字层)。数字层拦不住"删一条 + 同轮把 README 与 `FLOOR` 一起改小"这种成套改法,而那正是 `GUARD_TOPICS` 立起来要挡的形态。补登记不在本槽射程内(那是被合入支自己的登记面),如实登记。
2. **`js/issues.js` 那个冲突块里"取对侧就退回字面 `'low'`"这一格,没有任何判据管。** `EPB['unconfirmed']` 现取就是 `'low'`,机械取对侧行为一模一样、`git` 不报、单测全绿,**只会在下次改 `EPB` 那张表的档位时才悄悄分叉**(3.2)。认出它靠的是逐行读块内容。这与 W155 立 `EPB` 表时要挡的正是同一件事,而"合并时别把它退回去"这一层没人守。
3. **`comm` 的排序口径本槽再次发作,而这次是脚本内外不一致。** 抽名脚本里写了 `LC_ALL=C sort`,调 `comm` 那一层没写,第一轮**假报 8 条"只在基线里有的"**(1.5)。W166 残留 4 记的是抽名与比对口径不一致,本槽这次是同一个洞的另一种踩法。**零吃测这件取证工具的三个口径(去重 / 排序 / 按 `|` 切回执载荷)仍然一个都没有判据**,而排序这一格现在有两种踩法了。
4. **第三轮按名比对(被合入支名集 ↔ 合并后)仍是人工的,本槽也没有实测机会。** 三支零翻面零改名,故三轮结论一致。W161 残留 3 原样未收。
5. **记账件那格棘轮本槽差 4 过线,只多出 `SLACK` 一格。** 成因是本槽合三支、连本文共四份记账件。**若只合两支,基线那个 179 就会被差 3 蒙过去**——即"抄基线自己那个父值响不响"照旧只由本槽加了几份决定,与 W162 / W148 两次压线同形。
6. **`js/release.js` 底部那个包着 `Bus.on('*')` 注册的 `catch (_) {}` 仍开着**(W151 残留 2、W157 残留 5、W161 残留 5、W166 残留 7 原样未动)。全仓现取:`js/release.js` 空 `catch` **1 处**、`js/bus.js` **0 处**。
7. **W166 残留 6 仍成立且本槽又量了一次**:`downloadReleaseZip` 那三条钉的是"这一段 / 这个文件 / 这三个入口不许再落第二个包",不是"不许再落第二个包"这个语义。M10′ 换个持有者(`Z`)源级就够不着,接住它的是运行期那条;而运行期那条钉死的是三个入口,**第四个入口出现时它同样够不着**。
8. **W166 残留 1、2(`GUARD_TOPICS` 的 `hosts` 是手填数,「锚点太泛」与「真有两个承载」报错句同形)本槽没有实测机会**:`plans-gate-read` 那条的落点数在 W167 改了 `js/plans.js` 之后仍是 2,`hosts` 一格没动。
9. **W161 残留 4 那条弱断言(`assert(/aggFail: 0/.test(rel) || /counts/.test(rel), …)` 实际恒真)本槽原样未动。**
10. **W151 残留 4 的门槛派生那条本槽被 W167 从侧面碰了一次,但没收。** W167 立的是"'这一步走过了'是进度,'原文读不读得到'是输入,两问不许互相冒充";而那句话的核心照旧成立——**"某个码该不该投、投成什么危险级/什么文案"仍是各消费方各自决定**。
11. **`G-03` 的形态面、`G-11` 的自动进化面均未动**(W151 残留 1、3 原样);W163 明确写了它**不摘 `gaps`**,现取核实 `G-03` 仍是两条值。
12. **W147 登记的那条行为余面被 W167 收掉了一半**:`extractDone && !script` 的老项目走到 `project.splitEpisodes` 仍回 `blocked / no-script`(W167 的用例反而把这一点钉死了),但**计划层不再推那条注定 blocked 的命令步、改推补原文的导航步**。即"命令那一头照旧 blocked"是有意保留的,收掉的是"计划层推一条点下去只换来一句报错的步"。
13. **W142 残留 1(W135 那句 `assertEq` 与 W137 共享 `fails` 计数器)本槽未动。**
14. **W144 的行序判据仍不管同号内的先后**(连续第八槽没有实测机会:本槽三行的波次号 163/165/167 互不相同)。
15. **散文那半仍没有判据。** 本槽三处 README 长句取并集靠的是"把对侧相对它自己叉点的增量切出来、按锚点插回我方",这一步是人工做的;切得对不对没有判据管。W167 那次的两行相邻块尤其如此——两侧各改一行,取并集靠的是逐行认出"哪一行是谁改的"。
16. **`w160` 那份 783 行核验件的结论至今未在集成线上逐条重跑**(W166 残留 17 原样);本槽任务口径同样只要求合入,故未做。
