# W244 集成记账:两条槽并进同一条集成线(W243 抹码器跟丢 500 行修回 + W242 主体生图那一拍说出「同 id 多位」)

两条已完成功能支按次序 `--no-ff` 并进同一条集成线,两次 merge 的三个 SHA 全部 `fetch` 后 `rev-parse` **现取**:

- **merge 一** `8cb7ccd4`:`P1` = `0a6114c3`(`origin/cursor/w241-integration-6b2d` 现取 tip)、
  `P2` = `ff5be8d0`(`origin/cursor/w243-callsite-census-live-9a3c` 现取 head,自称同值);叉点 `B` = `0a6114c3`
- **merge 二** `f70ab0b8`:`P1` = `8cb7ccd4`(上面那条 merge)、
  `P2` = `44e29146`(`origin/cursor/w242-subject-dup-rows-note-9f3c` 现取 head,自称同值);叉点 `B` = `0a6114c3`
- 全程没用 `--ours`、没用 `checkout <old> -- .`;`master` 没合、没开第三条功能支、没跑 `e2e.js`

---

## 〇、交接自称的三个 SHA:这一槽三个全对,而「全对」是核出来的

| 交接自称 | 现取 | 判 |
|---|---|---|
| W241 tip `0a6114c` | `0a6114c3`(单亲提交,把份数与下限校到 256 的那条 docs 提交) | **是真 tip**,不是链中间的 merge |
| W243 head `ff5be8d` | `ff5be8d0` | 相等 |
| W242 head `44e2914` | `44e29146` | 相等 |

连着九槽的「自称 tip 是合入链中间那个 merge」这一次**没有复发**——W241 那条集成线的末尾压的是 docs 提交而它自己就是 tip
(`git cat-file -p` 只有一个 `parent`,不是 merge)。值得记的不是"这次对了",是**判定成本不变**:
三条 `rev-parse` 加一条 `cat-file` 与自称错时花的力气一样多,而省掉这一步在错的那八槽里代价是整条集成线接错点。

`merge-base` 三条现取全部落在 `0a6114c3`:两条被合入支是**从同一个点平行叉出去的**,
这个拓扑直接决定了下面第二节两次 merge 的形状完全不同——与上一槽同形,交接文事先按上一槽的口径说了。

---

## 一、两侧相对叉点各带来什么

`git diff --numstat B P2` 两支全量:

| 文件 | W243 | W242 | 面 |
|---|---|---|---|
| `js/domain.js` | — | **+27 −0** | 新开双端派生 `D.dupSubjectRowsNote(picked, subs)` |
| `js/commands.js` | — | **+4 −0** | 回执组装后挂 `result.note` |
| `cli.js` | — | **+5 −0** | 跑之前 `log` 一句 + 回执上同挂一份 |
| `tests/unit.js` | **+51 −14** | **+182 −2** | W243 改 `blankNonCode` 躯干 + 抬扫描下限 + 加 1 条 `contract`;W242 加 4 条(`domain`/`commands`/`release`/`contract`) |
| `README.md`(根) | +2 −2 | +2 −2 | **两侧动的是同两行**,见第四节 |
| `docs/skills-wave/README.md` | +2 −1 | +2 −1 | 份数行 + 索引行 |
| 各自记账件 | +140 | +299 | 新文件 |

**两侧产品面零文件重叠**(W242 三个 `js`/`cli` 文件,W243 一个产品文件都没碰,它改的是判据自己用的抹码器),
**而判据面与文档面三个文件全重叠**——`tests/unit.js`、根 `README.md`、`docs/skills-wave/README.md`。
与上一槽的差别在根 `README.md`:上一槽两侧各动一行、角色互换;这一槽两侧动的是**同两行**(第 363 行契约段、第 612 行单元段),
于是其中一行两侧写出了逐字节相同的内容——那正是第四节要记的那一格。

---

## 二、四棵树机检:一次拓扑退化、一次真合

### 2.1 merge 一:整棵树层面 `P1 == B` **成立**

被合入支从我方 tip 直接叉出,`merge-base` 就是 `P1` 本身,于是 `B` 树 536 个文件**全部** `P1 == B`,
其中「`P2` 真有改动」的 **4** 个(`README.md` / `docs/skills-wave/README.md` /
`docs/skills-wave/w243-callsite-census-live.md` / `tests/unit.js`)。git 一处都不用合,
合完的树与 `P2` 逐字节相同(`git diff 8cb7ccd4 ff5be8d0` 空)。零冲突是**拓扑退化**不是判据保住的,
`--no-ff` 在这里买到的只有「历史上留下一个能指名道姓的合入点」。

它同时意味着 merge 一之后的 live(unit **660** / `contract` **144** / 记账件 **257**)
**恰好等于 W243 自称的三个数**——而这三个数在合完两支的 tip 上一个都不对,见第六节。
这一格连着两槽同形,已经可以当成口径写下来:**两条平行叉出的槽,第一次 merge 的 live 永远等于第一支的自称数,
它证明不了任何事**。

### 2.2 merge 二:按 blob 筛 538 个,真形态 **3** 个

| 文件 | `B` | `P1` | `P2` | tip | 判定 |
|---|---|---|---|---|---|
| `js/domain.js` | `d21ce53` | `d21ce53` | `b89b70c` | `b89b70c` | **`P1 == B` 真形态**,整份取对侧 |
| `js/commands.js` | `fe7493e` | `fe7493e` | `72f9920` | `72f9920` | **`P1 == B` 真形态**,整份取对侧 |
| `cli.js` | `ec911d7` | `ec911d7` | `6198ebc` | `6198ebc` | **`P1 == B` 真形态**,整份取对侧 |
| `docs/skills-wave/w242-subject-dup-rows-note.md` | — | — | `49b9c06` | `49b9c06` | **`P1 == B` 真形态**(新文件) |
| `docs/skills-wave/w243-callsite-census-live.md` | — | `cb64535` | — | `cb64535` | `P2 == B`,整份取我方 |
| `README.md` | | 只动 363/612 | 只动 363/612 | 逐行合 | **两侧都改**,见第四节 |
| `docs/skills-wave/README.md` | | 份数 + 索引行 | 份数 + 索引行 | 逐行合 | **两侧都改** |
| `tests/unit.js` | | +51 −14 | +182 −2 | 逐行合 | **两侧都改** |
| 其余 530 个 | | 四树全同 | | | 不进合入面 |

四槽连看「两侧都改」的文件数:1 / 9 / 4 / 277 / 0 / 2 / **3**,量的照旧是「改动面重不重叠」而不是难度。
`js/domain.js` **几乎只有 W242**——W243 那一侧对 `js/` 一个字节都没碰(它改的是 `tests/unit.js` 里那份抹码器),
这一格是交接文点名要复核的,机检结论与它一致。

### 2.3 `numstat` 三向对账(减数先点名再相减)

- `tests/unit.js`:`(51 − 2) + (182 − 2) + 2 = 231` 加、`12 + 2 = 14` 减 → 合完 **+231 −14** ✅
  (两侧各有 2 行新增是各写各的 `FLOOR` 字面、合完取的是第三个值即 live,故先减去再加回 2)
- `docs/skills-wave/README.md`:`(2 − 1) + (2 − 1) + 1 = 3` 加、`1` 减 → 合完 **+3 −1** ✅
  (两侧各有 1 行新增是同一句份数声明,合完取 live 的第三个值)
- `README.md`:两侧各 `+2 −2` 动同两行,合完 **+2 −2** ✅
- **反向对账**:`git diff --numstat P1 tip` 与 `B P2` **逐字节相同**、`P2 tip` 与 `B P1` **逐字节相同**——
  即两侧带来的改动一格没丢也一格没串,这一条比逐文件看更快也更硬。

---

## 三、名集多重集:`659 + 1 + 4 = 664`

四棵树各整跑一遍 `node tests/unit.js`,`套件 · 用例名` 整条按 `|` 切做多重集比对(不是集合——重名会被集合吃掉):

| 树 | 条数 | 去重后 | 相对 `B` |
|---|---|---|---|
| `B`(W241 tip) | 659 | 659 | — |
| `P1`(W243) | 660 | 660 | **+1 −0** |
| `P2`(W242) | 663 | 663 | **+4 −0** |
| tip | **664** | 664 | 并集期望 664,**缺失零、多出零** |

W243 那 1 条:`contract · 抹码器扫描面自报:blankNonCode 抹出的代码骨架逐文件净配平`。
W242 那 4 条:`domain · dupSubjectRowsNote…` / `commands · subject.generateImage 点名同 id 多位真跑那一趟…` /
`release · G9 一键处置遇上同 id 多位的主体库…` / `contract · 点名的 id 在主体库存着多位那句实话双端单源…`。
四棵树都零重名(条数 == 去重数),故多重集这一步这次没派上用场,但它是唯一能证明"没派上用场"的量法。

`tests/unit.js` 是两边都加的那个文件,并集**两侧新增都在**:W243 的抹码器自报条与 W242 的四条主体 note 条同在一棵树上,
抹码器躯干(上下文栈那版)与 `dupSubjectRowsNote` 的四条用例互不相干、也互不覆盖。

---

## 四、这一槽最值得抄走的一格:两侧写出逐字节相同的错值,git 一声不吭

根 `README.md` 第 363 行(契约段)两侧**各自把 `143` 改成 `144`,整行其余部分一个字节没动**。
于是 git 在三方合并里看到"两边做了同一件事",**静默合成 `144`、一处冲突都不报**——
而合完两支的 live 是 **145**(`143 + 1 + 1`,两侧各加一条 `contract` 用例)。

与上一槽正好互补,两格并排记:

| | 上一槽(W241) | 本槽(W244) |
|---|---|---|
| 两侧对这一行做了什么 | 一侧只动数字、另一侧还插了 184 字散文 | **两侧只动数字,且改成同一个值** |
| git 报不报冲突 | 报(冲突是那段**散文**引起的,与数字无关) | **不报** |
| 数字错不错 | 错(两侧都自称 142,live 143) | 错(两侧都自称 144,live 145) |
| 谁最终接住 | 冲突面把人叫到这一行,再由判据把值校到 live | **只有判据**——`contract` 那条自对账用例 |

反事实①现跑确认:把这一行留成 git 合出来的 `144`,`contract` **144/145 红 1 条**,
报错句直接点名「实测 145,文档 144」。**所以这一格不是"静默错到底",是"git 层零信号、判据层照旧咬得住"**。
真正没有任何一层接住的是**散文**:反事实②把 W242 在第 612 行插的那 310 字整段删掉、数字仍校到 live 664,
`contract` **145/145 全绿**——与上一槽量出的结论逐字相同。**机器只替你看住那几个数;散文丢了全套照旧全绿**,
故长行冲突照旧只有一种解法:取带散文那一侧的整行,再把数字校到 live。

本槽第 612 行(单元段)正是这个形状:`P1` 只把 `659` 改成 `660`,`P2` 改成 `663` **并插了 310 字**主体 note 散文,
逐行 opcode 加掩数字逐字节各量一遍确认「掩掉数字后 `P1` 那一行是 `B` 那一行的原样」,故整行取 `P2` 再把数字校到 **664**。

---

## 五、`FLOOR` 静默落后的第五种机械形状:两侧同写同一个下限

五格 `FLOOR` 这一槽的实况分成两类,而它们的分界不是"改没改",是**两侧改成的值一不一样**:

| 格 | `B` | W243 | W242 | git 行为 | 合完 live | 处置 |
|---|---|---|---|---|---|---|
| 单元用例数 | 659 | **660** | **663** | **报冲突**(两值不同) | 664 | 冲突面把人叫到这一行,校到 664 |
| 记账件份数 | 256 | **257** | **257** | **静默合成 257** | 258 | **没有任何信号**,靠合完现跑五格才发现落后 1 格 |
| `integration` / `cli.smoke` / 护栏主题 | 148 / 117 / 19 | 未动 | 未动 | — | 同值 | 差额 0 |

份数那一格落后 1 格在 `SLACK = 3` 内,五层判据**一条不红**(反事实⑥现跑确认全绿)。
而同一份文档里的**明写份数**(`docs/skills-wave/README.md` 那句「索引表共 N 份记账件」)两侧同样同写 `257`、
同样被 git 静默合成,却**红了**(反事实⑤:`257` vs 实况 `258`)——差别在判据类别:
明写份数钉的是**相等**,`FLOOR` 钉的是**差额 ≤ SLACK**。

> **四种形状之外的第五种,并且它是四种里唯一「两侧都尽了责」的**:W240 那次是一侧明说不抬(共谋都不需要),
> 本槽两侧**都按自己那支的 live 抬了**、都没做错任何事,而合完照旧落后——
> **`FLOOR` 那一格永远量不出「合完是多少」,它只记得某一棵父树上是多少**。
> 唯一的工序不变:合完把五格全部现跑重取。本槽收成 **664 / 148 / 117 / 258 / 19**,差额全 0。

反事实④(单元 `FLOOR` 留 W243 自称的 660,落后 4 格)红在棘轮那条上;
反事实③(留 W242 自称的 663,落后 1 格)全绿——落后几格才红这件事本身与 `SLACK` 同口径,没有新发现。

---

## 六、数字全部合完 live

| | `B` | merge 一后 | **tip(合完两支)** | 两侧自称 |
|---|---|---|---|---|
| `unit` | 659 | 660 | **664 / 664 PASS** | W243 660、W242 663,**都不是答案** |
| `contract` | 143 | 144 | **145 / 145 PASS** | 两侧都自称 144,**都不是答案**;`143 + 1 + 1 = 145` |
| `integration` | 148 | 148 | **148 / 148 PASS** | 两侧未动(净增都是 0,故这次"相等"是真的) |
| `cli.smoke` | 115/117 | 115/117 | **115 / 117** | 两侧未动 |
| 记账件份数 | 256 | 257 | **259**(含本文) | 两侧都自称 257 |

`cli.smoke` 单独整跑 `env -u HUJING_SERVER node tests/cli.smoke.js`,失败恰两条:
**`未登录 whoami → exit 3`** 与 **`llm --json mock 链路`**,与 `master`(`9adcf0f`)现跑的 51/53 **同名同表现**,
不是本槽带进来的。

---

## 七、合完的树上真跑:两侧各自那句话都在,而且互不干扰

**W242 那一侧**(主体库 `dup` / `solo` / `dup` / `dup`,三位同 id):

- 用户手打点名 `['dup']` → 真跑 3 位 → 「点名的 1 位主体…(dup 3 位):这一趟按 3 位逐位跑、逐位计费,**比点名数多花了 2 位的钱**。
  主体侧没有去重命令…(删除按 id 匹配、同 id 那几位一并删光,先确认要留哪一位)」
- 发布门 G9 一键处置派生 `['dup','dup']` → 真跑 2 位 → **一个字都不说**(`extra = 2 − 2 = 0` 早退)
- 不点名整库 / 点名不重复的 `['solo']` → 一个字都不说

**W243 那一侧**:合完的树上把那条普查按同一份口径重跑一遍,**写死下发 21 处、变量站点 5 处**,
`js/storyboard.js` 的 6 处(`episode.smartReview` ×1 / `episode.compose` ×3 / `episode.generateStoryboard` ×1 /
`shot.generateVideo` ×1)确实在扫描面上,即抹码器那版上下文栈躯干过 merge 后原样生效。

---

## 八、十三手反事实:各红各的,两格有意留白

| # | 变异 | 结果 |
|---|---|---|
| ① | 根 README 契约行留 git 合出来的 144 | `contract` 红 1(点名「实测 145,文档 144」) |
| ② | 根 README 单元行丢掉 W242 那 310 字散文、数字仍校到 664 | **全绿**——散文一条判据都没有 |
| ③ | 单元 `FLOOR` 留 663(落后 1 格) | **全绿**(`SLACK` 3 内) |
| ④ | 单元 `FLOOR` 留 660(落后 4 格) | `contract` 红 1(棘轮下限) |
| ⑤ | 记账件明写份数留 257 | `contract` 红 1(三方对齐) |
| ⑥ | 记账件 `FLOOR` 留 257(落后 1 格) | **全绿**(第五节那一格) |
| ⑦ | 记账件 `FLOOR` 退到 254 | `contract` 红 1(棘轮下限) |
| ⑧ | 索引表只留 w243 行、丢掉 w242 行 | `contract` **红 4**(双向对齐 / 逐份点名 / 三方份数 / 行序取数口) |
| ⑨ | 索引两行互换(波次号倒退) | `contract` 红 1(行序,点名「w243 之后排了 w242」) |
| ⑩ | `extra` 退回按去重 id 减 | `domain` + `release` **各红 1**——G9 那句假话当场复现 |
| ⑪ | 主体那句话串用镜头字眼(改指 `shots-dedupe`) | `commands` + `domain` + `contract` **红 3** |
| ⑫ | CLI 那一端不挂 `note` | `commands` 红 1(两端须逐字同一句) |
| ⑬ | `blankNonCode` 退回只数 `${` 插值深度 | `contract` **红 2**——普查下限报「字面 15 处」、抹码自报报出一串净差不为 0 的文件 |

⑬ 这一手值得单记:退回旧躯干后,那条**被 W243 抬到 21 的下限**与**新加的抹码自报**各红各的,
两条报错含义不重复(一条说"点不到调用点",一条说"抹码器在这些文件上跟丢了")——
即 W243 把"扫描面完不完整"从下限身上摘出去这件事,过 merge 之后仍然是两条独立的判据。

---

## 九、不代修的残留(原样留着,并把它们现跑核过)

1. **镜头那份 `dupRowsNote` 的「多花几行」仍按去重 id 减**——交接明令不代修,一个字未动。
   合完的树上现跑复现:`dupRowsNote(['dup','dup'], 2 行)` 照旧播出「比点名数多花了 **1** 行的钱」,
   而 G6 处置的 `firstFailed.shotIds` 正是每行各排一条的机器派生清单,这句话在自家按钮上就是假话。
   收法与 W242 逐字同形(`extra = 真跑行数 − 点名清单原始条数` + `extra <= 0` 早退),
   判据照抄那条 `release` 用例即可,但要连着 G4/G6 两门一起判(G4 还叠着 `staleShotSplit` 两堆分法)。
   **这一格现在是"两侧同一个错、只修了一侧"的状态**,谁接谁跑。
2. **CLI `findSubject` 取 `.find` 首位**:点名 `['dup']` 三轮全写在首位身上,扣 3 笔只有 1 位到手图,
   而回执报 `ok:3 failed:[]`。W242 已登记,要动寻址语义与 `ok` 口径,不是文案能承担的。
3. **`state-put` 逃生舱不设闸**:`cli.js` 仍只有 `need(f.force)` 一道,整树原样落库是它的契约,因果没解除。
4. **选人闸按行/按位筛一字未动**(`ids.has(s.id)` 两端六处全在),按 id 去重会让第二、三行/位永远跑不到。

---

## 十、保留面机检(合完的树上现取)

`gaps()` **20 键**一个没剥(SK-04/G-11/G-13 原样开着)、`GUARD_TOPICS_CLOSED` **0 条**、
`TOPIC_FLOOR` 与花名册仍 **19**、`SLACK` 四树都是 **3**;
镜头 `dupRowsNote` 与主体 `dupSubjectRowsNote` **两份派生并存**(`js/domain.js` 第 315 / 372 行)、
`shots-dedupe` 5 处引用在册、`floorLagVerdict` 纯函数在册、`shots-import` 写入闸未动、
`produce` 侧提 `note` 未动、MCP 对账那条(运行期 `tools/list` 39)未动、
四堆按 id(`emptyBatchNote` / `emptySubjectImageNote` 那两份分档)未动、
`plans.gateSkips()` / `issues.epSkips()` / `flowTpl.stopSkips()` 三张过滤表未动。
`js/` 目录下除 `domain.js` / `commands.js` 外**四树 blob 全同**,`server.js` / `mcp.js` / `billing.js` 三个根文件同样全同;
`cli.js` 5 行改动全部落在 `subject.generateImage` 段(1378–1406),与 W226 那道镜头闸(721–760)不相交。

另记 W236 那条口子随记账件线性变宽:**259 份里除索引行外零链接的有 138 份**
(上一槽是 255 份里 134 份,再上一槽 252 份里 131 份),口径本身不变。
