# W20 · 周期 3 独立核验

> 核验槽独立作业:**只读代码、只写本文件**。全部测试数字由本槽在各支独立 `git worktree` 上重新跑出,
> 不引用被核分支文档里的数字;引文与自测不一致处逐条标注。本轮不开 PR、不合并、不改任何业务代码。
> 成熟度沿用 `w5-cycle1-audit.md` 第 1 节的 M0–M4 标尺(M3 = 代码+断言、分支内自洽全绿;**M4 = 已合入 `master`**)。
> 周期 3 范围取 **W11–W19**;`cursor/w9-integration-f8f9` 作为周期 2 的收敛点纳入,只作基线不重复评分。

## 0. 核验时点与锚定 SHA

| 分支 | head | 相对 `master` | `master` 是严格祖先 |
|---|---|---|---|
| `master` | `9adcf0ff` | — | — |
| `cursor/w9-integration-f8f9`(周期 2 收敛点) | `641a1f5a` | 领先 103,落后 0 | 是 |
| `cursor/w11-preflight-film-assert-883b` | `a2709a3b` | 领先 106,落后 0 | 是 |
| `cursor/w12-size-gap-check-df6c` | `79c3b78f` | 领先 105,落后 0 | 是 |
| `cursor/w13-integration-a394` | `2b671019` | 领先 116,落后 0 | 是 |
| `cursor/w14-review-skills-check-5291` | `8a1f1b4d` | 领先 109,落后 0 | 是 |
| `cursor/w15-gen-block-d9a3` | `1cc6aa72` | 领先 113,落后 0 | 是 |
| `cursor/w16-integration-3488` | `01e4295c` | 领先 125,落后 0 | 是 |
| `cursor/w17-preflight-stages-0857` | `5728aca4` | 领先 118,落后 0 | 是 |
| `cursor/w18-gen-prompt-unify-0df1` | `6043f8bc` | 领先 115,落后 0 | 是 |
| `cursor/w19-g06-inject-a20c` | `34cf63fe` | 领先 117,落后 0 | 是 |

十支的 `git merge-base --is-ancestor master <branch>` **全部成立**,任一支单独并入主干都是零冲突快进。

### 0.1 复核期间的分支移动

任务书预告的两条并行动作在核验期间**都发生了**,本槽对新状态做了完整复测,下文正文数据一律以上表 SHA 为准:

| 分支 | 变化 | 对结论的影响 |
|---|---|---|
| `cursor/w16-integration-3488` | `c87da63` → `01e4295c`(新增 `01e4295` W16 漏合补齐与审片消费收敛记录 + 目录索引登记) | **两条阻塞消除**:`w16-integration-log.md` 已补;`docs/skills-wave/README.md` 索引由「33 行索引 / 32 份文件」变为 **33 / 33 齐平**。`unit` 复跑仍 **309/309**,注册表口径未动。 |
| `cursor/w19-g06-inject-a20c` | 核验开始时不存在,末次 `git fetch` 时出现 | 已按同规格完整核验,见 4.9。基线是 `w18` head,**继续延长了 W15→W18→W19 这条与集成线互不包含的支线**。 |

W11–W18 八支在全程 head 未动,数字可直接采信。

## 1. 一句话结论

周期 3 的九波**全部达到 M3、无一达到 M4**;十支的 `unit` / `integration` 全部 0 FAIL,
`cli.smoke` 的 2 项失败经本槽在 `master` 上独立复现为基线态。周期 2 核验报告列的三条主要阻塞
(漏合 3 提交、两条线未合流、SK-03/SK-04 记账与实况矛盾)**在周期 3 内被逐条清掉**——
这是本周期最实的治理成果,且解法与周期 2 报告第 8.2 节的建议次序一致。

但**同一种病换了个位置复发,而且更重**。周期 2 是「一条集成线漏了 3 个提交」,周期 3 结束时是
**三条互不包含的 tip 并存**:`w16`(集成线,含 SK-18 与 G-10 审片消费)、`w17`(基于 `w13` head,含面表单源但不含 W14)、
`w19`(基于 `w15`→`w18` 的支线,含 SK-21 生成步注入与 G-06 残留,但**连 SK-18 都不含**)。
实测无一支是另一支的超集,**周期 3 不存在「当前全量分支」**。

短名单 30 条:三条 tip **各自都是 13 条 pending**,合流后为 **12 条**(净清 4 条);
`CHECKS` 实现 **6 → 9 条**(并集 9,`w19` 只有 8)。但九波里只有 **W12(SK-18)与 W15(SK-21)两波**
清掉了短名单条目,其余七波做的是集成、断言、单源收编与记账诚实位——**治理占比远高于能力增量**,
这是周期 3 的真实形态,不是缺陷,但需要在下一周期显式定价。

本轮另有一处前两份核验报告都漏掉的失真被查实:README 架构框那句「CHECKS 已落地 X 条」
**在集成线上从周期 2 收敛点一直错到 `c87da63` 之前的每一个提交**(最大差 6 条),
十支里只有 `w16` 修对;根因是**没有任何断言把 README 数字绑到代码实况**——
`master` 自己的断言数就写 163、实测 201,差 38。给这两个数字加断言是本轮性价比最高的建议。

`master` 上 `js/skills.js` 与 `docs/skills-wave/` **仍然都不存在**。
周期 1、周期 2 两份核验报告最重要的那条结论,在周期 3 结束时**第三次逐字成立**——
39 条远端分支、三个周期、9 条校验项、全部缺口收敛,在主干上一件都不可见。

## 2. 独立复跑对账(全部数字为本槽实测)

在每支的独立 `git worktree` 上依次跑 `node tests/unit.js`、`node tests/integration.js`、`node tests/cli.smoke.js`。

| 分支 | unit 实测 | README 自称 | 记账件自称 | integration | cli.smoke | 对账 |
|---|---|---|---|---|---|---|
| `master`(基线) | **201/201** | 163 | — | **79/79** | **51/53** | 基线 |
| `w9-integration`(周期 2 收敛点) | **288/288** | 288 | 288 | **93/93** | **62/64** | ✅ |
| `w11-preflight-film-assert` | **290/290** | 290 | 290 | **93/93** | **62/64** | ✅ |
| `w12-size-gap-check` | **296/296** | 296 | 296 | **93/93** | **62/64** | ✅ |
| `w13-integration` | **306/306** | 306 | 306 | **93/93** | **62/64** | ✅ |
| `w14-review-skills-check` | **293/293** | 293 | 293 | **93/93** | **62/64** | ✅ |
| `w15-gen-block` | **299/299** | 299 | 299 | **93/93** | **62/64** | ✅ |
| `w16-integration` | **309/309** | 309 | 309 | **93/93** | **62/64** | ✅ |
| `w17-preflight-stages` | **307/307** | 307 | 307 | **93/93** | **62/64** | ✅ |
| `w18-gen-prompt-unify` | **300/300** | **299** ✗ | 300 | **93/93** | **62/64** | ⚠️ README 差 1 |
| `w19-g06-inject` | **301/301** | 301 | 301 | **93/93** | **62/64** | ✅ |

**零失败项**:十支的 unit / integration 全部 0 FAIL。

**`cli.smoke` 的 2 项失败是 `master` 基线态,已独立证实**:本槽在未改动的 `master` 上直接跑,同样得到
`FAIL | 未登录 whoami → exit 3 | exit=1` 与 `FAIL | llm --json mock 链路 | undefined`,与各支上失败的是**同两项、同表现**。
各支「不是回归、未删测换绿」的说法成立。这两项已连续三个周期挂在同一位置。

**`master` 自己的 README 就对不上**:实测 `unit 201/201`,README 写「163 项断言」,**差 38**。
这不是任何一条波次分支引入的,是主干既有状态(详见第 5.3 节末段)。下表「README 自称」一列里
`master` 的 163 因此不构成对账依据,只作现状记录。

**波次线上唯一一处数字对账不上的是 `w18` 的 README**:实测 300,README 写 299。
其自身记账件 `w18-gen-prompt-unify.md` 第 83 行写的是「**300/300 PASS, 0 FAIL**(基线 299,净增 1 项断言套件)」——
**记账件是对的,README 漏改**。该支提交 `6043f8b` 的消息声明「README 两个取用口口径同步」,同步了取用口描述但没重算断言数。
按仓库纪律(README 与代码行为同步),这是必须补的一步。`w19` 基于 `w18` head,但**它把这个数字改对了**(README 301 = 实测 301)。

`node --check` 抽验:三条 tip 上 `js/skills.js`、`js/wf-core.js`、`js/domain.js`、`js/review.js`、
`server.js`、`cli.js`、`tests/unit.js` **全部通过**。

## 3. 分支拓扑:周期 3 结束时是三条 tip,不是一条线

这是本轮最关键、也最容易读错的地方,先钉住。周期 2 末尾是「一条集成线 + 一条待合流分支」,
周期 3 把待合流那条合掉了,却**新分出两条**:

```
master 9adcf0f
  └─ …周期1+2… ── w9-integration 641a1f5(周期2 收敛点)
       ├─ w11-preflight-film-assert a2709a3(纯断言波)
       │    ├─ w14-review-skills-check 8a1f1b4(G-10 审片侧首个消费点)
       │    └─ e899a71 整合:合入 w9-eps(SK-14/15)
       │         ├─ 070b50d 整合:合入 w12(SK-18)
       │         │    ├─ 676b090 ── w13-integration 2b67101 ── w17-preflight-stages 5728aca  ◀ tip 2
       │         │    └─ 4d89a62(补合 w12 head)── fa8d6e5(合入 w14)── 6640b93(补合 w7 head 文档)
       │         │         └─ 43e9b0b(补合 w10 核验报告)── c87da63 ── w16-integration 01e4295  ◀ tip 1
       │         └─ w15-gen-block 1cc6aa7 ── w18-gen-prompt-unify 6043f8b ── w19-g06-inject 34cf63f  ◀ tip 3
       └─ w12-size-gap-check 79c3b78(基线 = w9 head,不含 w11)
```

实测 merge-base 佐证:`w13 ∩ w16 = 070b50d`(**不是** `w13` head)、`w15 ∩ w16 = e899a71`、
`w13 ∩ w15 = e899a71`、`w17 ∩ w19 = e899a71`、`w12 ∩ w11 = 641a1f5`(两支互不包含)。
`w19 ∩ w18 = 6043f8b`(`w18` head,即 W19 **正确地取了 head 作基线**)。

**三条 tip 无一是另一条的超集**,逐项实测:

| 成果 | `w16` | `w17` | `w19` |
|---|---|---|---|
| SK-14/15 分集段校验项(周期 2 余量) | 有 | 有 | 有 |
| SK-18 `shots.sizeLinkage` 分镜景别衔接(W12) | 有 | 有 | **无** |
| G-10 审片侧 `Skills.check` 只读消费(W14) | 有 | **无** | **无** |
| SK-03/SK-04 记账诚实位 `note`(W14) | 有 | **无** | **无** |
| SK-21 生成步注入面 `block('gen')` 非空(W15) | **无** | **无** | 有 |
| `WfCore.optimizeSystem` 双端收编(W18) | **无** | **无** | 有 |
| G-06 残留两条进拆镜/主体人设(W19) | **无** | **无** | 有 |
| `Skills.preflightStages()` 面表双端单源(W17) | **无** | 有 | **无** |
| `docs/…/w7-integration-log.md`、`w10-cycle2-audit.md` | 有 | **无** | **无** |
| `docs/…/w13-integration-log.md` | **无** | 有 | **无** |
| `docs/…/w15-gen-block.md` | **无** | **无** | 有 |

所以各支的测试数字**不可横向比大小**:`w16` 的 309、`w17` 的 307、`w19` 的 301
分别是在不同基线上加出来的,不存在「309 比 301 做得多」的关系——`w19` 有 `w16` 完全没有的三项成果。

## 4. 逐项目核验

### 4.1 `cursor/w9-integration-f8f9` — 周期 2 收敛点(基线,不重复评分)

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。相对 `master` 领先 103 提交、71 文件、+7664/−470 行。 |
| **本轮相关性** | 它是周期 3 全部九波的**公共祖先**(实测对十支 `--is-ancestor` 全成立),也是周期 2 三条阻塞的载体。本槽复跑 `unit 288/288`、`integration 93/93`、`cli.smoke 62/64`,与周期 2 报告第 0.1 节数字一致。 |
| **周期 2 遗留的落点** | 周期 2 报告点名的三个漏合提交在它上面**依然全部缺失**(实测 `--is-ancestor` 对 `072f342` / `97f6ba2` / `bbd7ebb` 三条全部不成立),`docs/skills-wave/w7-integration-log.md` 与 `w8-script-check.md` 仍不存在。这三条是在 W13 与 W16 上被分两次补掉的,见 4.4 与 4.7。 |

### 4.2 `cursor/w11-preflight-film-assert-883b` — 纯断言波(本周期唯一零代码波)

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。改动面 4 文件 `+149/−2`,**`js/` 下零改动**——本周期唯一一波只加断言不加能力。 |
| **增量** | 给「就绪检查消费成片字幕面」补两条断言:行为层断言 `result.checks` 是三面并集(script / subjects / film,按主线步序),源级断言双端 `preflight` 实现段内是**同一条 `checks` 表达式**(算出来却不并进 `result.checks` 也算漏消费)。README 断言数 288 → 290。 |
| **证据(本槽实测)** | `unit 290/290`(基线 `w9` 的 288 + 2)、`integration 93/93`、`cli.smoke 62/64`,与其记账件第 1 节的数字**逐个对上**。注册表口径实测与 `w9` 完全一致(`CHECKS` 6 条、pending 16 条 / 17 面),佐证「不加能力」的声明成立。 |
| **证据(本槽变异测试,最有价值的一条)** | 本槽在 `w16` 的副本上把 `js/commands.js` 的 `checks` 表达式里 `Skills.check('shots', …)` 摘掉,复跑 `unit` 由 309/309 转为 **306/309, 3 FAIL**——行为并集断言、SK-18 消费点断言、源级并集断言**三条同时转红**,且报错文本精确指出「`js/commands.js` checks 表达式应并入 shots 面(只在段外调用不算消费)」。**W11 这两条断言确实有牙**,不是形式主义。 |
| **阻塞** | ① 未进主干。② 其基线是 `w9` head,不含 W12,因此它自己的三面口径在后续被抬到五面(见 4.4)。③ 已被 W13/W16/W17/W19 四支全部吸收(`--is-ancestor` 实测成立),**本支无余量**。 |
| **下一目标** | 已完成,无余量。其方法(先补并集断言、再让后续波在断言逼迫下补消费)在 W12/W13 上连续兑现两次,**建议固化为新增校验面的标准前置动作**。 |

### 4.3 `cursor/w12-size-gap-check-df6c` — SK-18 分镜段景别衔接(本周期能力增量之一)

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。 |
| **增量** | 落地 `CHECKS['shots.sizeLinkage']`(SK-18 景别递进与跳切):`flat-run` 连续三镜以上同景别、`jump-cut` 相邻两镜级差达两极、`no-progression` 整集最大级差不到隔一级,一律只判 `warn`。消费点复用既有两处(`episode.preflight` 的 `result.checks`、问题中心 `shot-size-linkage` 低危)。 |
| **证据(本槽实测)** | `unit 296/296`(基线 `w9` 的 288 + 8)、`integration 93/93`、`cli.smoke 62/64`,与其记账件自称的 296 **对上**。`Skills.CHECKS` 实测 **7 条**、SK-18 的 `pending` 实测为**空**,「先有实现再登记」的纪律成立。**单源声明在代码层证实**:`js/skills.js` 第 269 行 `const sizeGap = wfCore().sizeGap;`、第 284 行 `sizeGap(prev, cur)`,景别阶梯的唯一定义仍在 `js/wf-core.js` 第 62 行 `W.sizeGap = function (a, b)`,校验层**没有写第二份阶梯**。 |
| **阻塞** | ① 未进主干。② **基线是 `w9` head 而不是 `w11` head**,因此它自己不含 W11 的并集断言——这也是它落地时没有被断言逼着改并集口径的原因(改口径的活落到 W13 身上,见 4.4)。③ 代码已被 `w13` / `w16` / `w17` 吸收;**`w19` 完全不含它**。 |
| **下一目标** | 已完成,无余量。剩余风险只在 tip 分裂:`w19` 这条线上 SK-18 仍是 pending,若将来从 `w19` 方向收敛需注意别把已落地的校验项漏掉。 |

### 4.4 `cursor/w13-integration-a394` — 周期 3 前段收敛(合并**取了 head**)

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。领先 `master` 116 提交、76 文件、+8760/−470 行。 |
| **增量** | 三步收敛:`e899a71` 合入 `w9-eps-structure-check` head(把周期 2 悬空的那条线并进来)、`070b50d` 合入 `w12`、`676b090` 补合 `w12` 尾部,末尾 `2b67101` 补收敛记录。 |
| **证据(本槽实测)** | `unit 306/306`、`integration 93/93`、`cli.smoke 62/64`,0 FAIL,与其记账件第 1 节的 306 **对上**。`Skills.CHECKS` 实测 **9 条**(剧本 3 + 主体 2 + 分镜 1 + 成片 1 + 分集 2),pending 由 16 条降到 **13 条**。 |
| **证据:周期 2 阻塞 2/3 已闭合** | 周期 2 报告点名的 `97f6ba2`、`bbd7ebb` 两个漏合提交在本支上 `--is-ancestor` **均成立**(随 `w9-eps` head 一起被带进来),`docs/skills-wave/w8-script-check.md` 与 `w9-eps-structure-check.md` 两份**都已存在**。周期 2 阻塞 3(两条线合流)也在此闭合。**「合并取分支 head」这条口径在 W13 上如实执行。** |
| **证据:W11 断言接住漏接的说法成立** | 其记账件 4.2 节自称「W11 的并集断言当场接住了两次漏接」。本槽用变异测试独立验证了这条断言的判据强度(见 4.2),并核对了口径抬档不是删测:源级面清单由 `['script','subjects','film']` 改为 `['script','subjects','eps','shots','film']`,登记侧反查那句原样保留、另加两句显式要求——**判据只增不减**。 |
| **阻塞** | ① 未进主干。② **`072f342`(W7 收敛记录)在本支上仍缺**(`--is-ancestor` 不成立、`w7-integration-log.md` 不存在)——周期 2 的三条漏合它只补了两条。③ **README 架构框的校验项条数失真**:框里写「CHECKS 已落地**七条**」,实测 **9 条**,少算了分集段两条(见第 5.3 节的成因分析)。④ **`w13` head 未被 `w16` 吸收**(差 `676b090`、`2b67101` 两提交),即它自己反过来成了下游漏合的对象。 |
| **下一目标** | 把 `2b67101`(W13 收敛记录)带进 `w16`——实测该合并只有 **2 个文件 4 处冲突**(`README.md` 2 处、`docs/skills-wave/README.md` 2 处),**零代码冲突面**。不补则 W13 这一波的冲突解法记账在集成线上丢失。 |

### 4.5 `cursor/w14-review-skills-check-5291` — G-10 审片侧首个消费点 + 记账诚实位

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。 |
| **增量** | 两件:① `js/review.js` 接入 `Skills.check` **只读消费**——这是 G-10 在**审片侧的第一个消费点**,此前 9 条校验项全部只挂在就绪检查与问题中心;② `SK-03`/`SK-04` 的 `infra` 面 `pending` 补 `note`,说明 G-01/G-02 已落地实况。 |
| **证据(本槽实测)** | `unit 293/293`(基线 `w11` 的 290 + 3)、`integration 93/93`、`cli.smoke 62/64`,与其记账件自称的 293 **对上**。`js/review.js` 对 `Skills` 的引用实测 **4 次**、符号为 `Skills.byId` 与 `Skills.check`——对照:`w9`/`w11`/`w13`/`w17`/`w19` 上该文件对 `Skills` 引用实测均为 **0**。 |
| **证据:周期 2 阻塞 6 已闭合** | 本槽直接读注册表,SK-03 / SK-04 / SK-23 三条的 `note` **已收成同一规格**,三条都明写「G-0x 已落地:…本条 pending 的 infra 面留的是注册表侧记账收敛(改 pending 会动 gaps 投影,单列一轮)」,SK-03/SK-04 另尾附「口径同 SK-23」。周期 2 报告第 5.3 节列的「三条记账诚实度不一致」**逐字消除**。 |
| **阻塞** | ① 未进主干。② **`js/release.js` 对 `Skills` 的引用实测仍为 0**——G-10 的发布门那一半没动,校验结论进不了发布门。③ **它没有清掉任何短名单条目**:SK-24 `review.methodDim` 的 `pending: ['check']` 实测仍在——本波接的是**消费通道**,不是审片方法论维度的校验实现。④ 已被 `w16` 吸收;**`w17` 与 `w19` 两条 tip 都不含它**,即两条较新的分支上审片侧消费点**不存在**。 |
| **下一目标** | ① 让 `w17` / `w19` 两条线也拿到这个消费点(经 `w16` 合流即可,无需重做)。② 发布门那一半(`js/release.js`)是 G-10 剩下的主要缺口,也是 SK-29 `film.deliverContract` 的前置。 |

### 4.6 `cursor/w15-gen-block-d9a3` — SK-21 生成步注入面(本周期能力增量之二)

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。 |
| **增量** | 闭掉周期 1、2 连续两波都点名的那条零消费:生成步提示词改写人设按键取「抽卡公式」+「抽卡军规」,使 `Skills.block('gen')` 由空变非空。 |
| **证据(本槽实测)** | `unit 299/299`、`integration 93/93`,0 FAIL,与其记账件自称的 299 **对上**。`Skills.block('gen')` 实测**长度 344**(对照 `w9`/`w11`/`w12`/`w13`/`w14`/`w16`/`w17` 上实测均为 **0**),内容开头为「AI 视频提示词八维万能公式:主体+动作+场景+光影+镜头语言+风格+画质+约束…」;SK-21 的 `pending` 实测为**空**。装配口在 `js/wf-core.js` 第 307 行 `W.genPromptSystem = ov => Prompts.get('gen.promptSystem', ov) + KB.pick('抽卡公式', '抽卡军规')`,**KB 正文没有第二份**。 |
| **证据:周期 1/2 的预判兑现** | 周期 2 报告第 5.3 节曾把 SK-21 的 pending 列为「诚实的 pending」,并实测 `block('gen')` 长度为 0 与其 `note` 完全一致。本波正是按那条 `note` 指的方向落地的,**连续两个周期的记账指向与最终落地点一致**。 |
| **阻塞** | ① 未进主干。② **基线是 `e899a71`(`w13` 的中间点)而非任何分支 head**——这是周期 3 分裂的起点:它因此不含 W12(SK-18)、不含 `w13` head、不含 W14。③ **README 架构框失真最严重的一支**:框里写「CHECKS 已落地**三条**」,实测 **8 条**,少算五条。④ 已被 `w18` / `w19` 吸收;`w16` / `w17` 两条 tip **都不含它**。 |
| **下一目标** | 并入集成线。实测 `w16 ← w15` 只有 **2 文件 4 处冲突**(`README.md` 与 `docs/skills-wave/README.md` 各 2 处),`js/skills.js` 与 `tests/unit.js` **自动合并零冲突**——但按第 3 节的矩阵,更省事的做法是直接合 `w19`(它已含 W15+W18+W19 三波),冲突面实测同为 2 文件 4 处。 |

### 4.7 `cursor/w16-integration-3488` — 周期 3 集成线 tip(漏合治理最彻底的一支)

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。当前**内容最多的单支**(领先 `master` 125 提交、79 文件、+9705/−471 行),但**不是全量**——缺 W15/W18/W19 三波与 `w13` head 两提交。 |
| **增量** | 六步:`4d89a62` 补合 `w12` **head**(不只是功能提交)、`fa8d6e5` 合入 `w14`、`6640b93` **补合 `w7` head 文档**、`43e9b0b` **补合 W10 周期 2 核验报告**、`c87da63` 口径修正、`01e4295` 补收敛记录与索引登记。 |
| **证据(本槽实测)** | `unit 309/309`、`integration 93/93`、`cli.smoke 62/64`,0 FAIL,与其收敛记录自称的 309 **对上**。`Skills.CHECKS` 实测 **9 条**、`Skills.REG` 30 条、pending **13 条 / 14 面**、`gaps()` 键数 22。 |
| **证据:周期 2 阻塞 2 完全闭合** | `072f342` / `97f6ba2` / `bbd7ebb` **三条 `--is-ancestor` 全部成立**,`docs/skills-wave/w7-integration-log.md` **已存在**——这是十支里**唯一**做到的一支(`w9`/`w13`/`w17`/`w19` 上该文件均缺失)。周期 2 报告第 8.2 节第 1 步的①**在此完整兑现**。 |
| **证据:README 口径修正是真的** | 架构框实测写「CHECKS 已落地**九条**」= 实测 9 条,断言数 309 = 实测 309,**是十支里唯一两个数字都对的一支**。 |
| **阻塞(本轮最实的一条)** | **它自己就是「合并未取分支 head」的新案例**:`w13 ∩ w16 = 070b50d`,不是 `w13` head。漏掉 `676b090` 与 `2b67101` 两提交,可直接验证的表征是 `docs/skills-wave/w13-integration-log.md` 在本支上**不存在**。**周期 2 报告第 8.1 节末尾定的那条口径(「合并一律取分支 head」)在本支上没有被遵守。** |
| **阻塞(第二条)** | **`checkout --ours` 造成过实际内容丢失**。其提交 `c87da63` 的消息自陈「补回被 `checkout --ours` 连带丢弃的四处 README 增量 + 断言数按实跑重算 305 → 309」。本槽做了独立的残留丢失核查:把 `w11` / `w12` / `w14` 三支各自新增的测试名与 `w16` 的 `tests/unit.js` 逐条比对,**不在 `w16` 上的条数为 0**——即**测试层没有残留丢失**,`unit 309` 里三支的独有断言一条不少。但该形态说明冲突解法用了「整файл取一侧」的粗粒度动作,是记账丢失的直接机制。 |
| **阻塞(第三条)** | 不含 W15/W18/W19 三波:`Skills.block('gen')` 实测**长度 0**、`WfCore.optimizeSystem` 实测 **0 次命中**、SK-21 `pending: ['inject']` 仍在。 |
| **下一目标** | 按优先级:① 补合 `w13` head 的 `2b67101`(2 文件 4 处冲突,零代码面);② 合入 `w19`(带 W15+W18+W19,2 文件 4 处冲突);③ 合入 `w17`(3 文件 5 处冲突,含 `tests/unit.js` 1 处)。三步做完 `w16` 即成为周期 1–3 全量的唯一收敛点,合流后判据:`Object.keys(Skills.CHECKS).length === 9`、pending **12 条 / 13 面**、`Skills.block('gen')` 非空、`js/review.js` 对 `Skills` 引用非 0。 |

### 4.8 `cursor/w17-preflight-stages-0857` — 就绪检查面表双端单源

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。基线是 `w13` **head**(取了 head,纪律正确)。 |
| **增量** | 把双端就绪检查里各写一份的校验面清单收成 `Skills.preflightStages()` 一处,`js/commands.js` 与 `cli.js` 两端改为 `Skills.preflightStages().reduce(…)`。 |
| **证据(本槽实测)** | `unit 307/307`、`integration 93/93`、`cli.smoke 62/64`,0 FAIL,与其记账件自称的 307 **对上**。两端取用口实测各 1 处(`js/commands.js:54`、`cli.js:1026`),`preflightStages` 在该支上实测命中 18 次(含注释与断言)、在其余九支上均为 **0**。 |
| **证据:单源是「派生」而非「另一份清单」** | 本槽原打算用变异测试摘掉面表里的 `'shots'` 字面,**结果发现没有可摘的字面**——实现是 `STAGES.map(x => x.key).filter(k => REG.some(s => s.stage === k && live(s, 'check') && s.cmds.indexOf('episode.preflight') >= 0))`,即从注册表**推导**而来。实测 `Skills.preflightStages()` 回 `["script","subjects","eps","shots","film"]`,顺序即 `STAGES` 主线步序。**这比「把清单写在一处」强一档:新增一面只需在条目上登记 `checks` 与 `cmds`,两端自动跟上**,该支注释里的这句声明成立。 |
| **阻塞** | ① 未进主干。② **不含 W14**(基线 `w13` 不含 W14):`js/review.js` 对 `Skills` 引用实测 **0**,SK-03/SK-04 的诚实位 `note` 也没有——即**审片侧消费点与记账诚实位在这条较新的 tip 上不存在**。③ **README 架构框沿用了 `w13` 的失真值「七条」**(实测 9 条),本波未修。④ 不含 W15/W18/W19。 |
| **下一目标** | 并入 `w16`(3 文件 5 处冲突,`tests/unit.js` 那 1 处需注意别丢面表断言)。合流后这条派生式面表会自动接住 W19 之后新增的面,是**降低后续分裂成本的一项基础设施**,建议在三条 tip 里**优先合它**。 |

### 4.9 `cursor/w18-gen-prompt-unify-0df1` — 提示词改写人设双端收编

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。基线是 `w15` **head**(取了 head)。 |
| **增量** | `WfCore.optimizeSystem` 双端单源:浏览器一键优化与 CLI 修订重抽同经一个装配口取人设句。改动面 8 文件 `+118/−5`,是本周期最小的一波。 |
| **证据(本槽实测)** | `unit 300/300`、`integration 93/93`、`cli.smoke 62/64`,0 FAIL。单源在代码层证实:`js/wf-core.js:308` `W.optimizeSystem = ov => Prompts.get('gen.promptSystem', ov)`、`:311` `W.genPromptSystem = ov => W.optimizeSystem(ov) + KB.pick(…)`(**派生而非第二份**),两个取用口分别是 `js/review.js:331` 与 `cli.js:1130`,`optimizeSystem` 在该支实测命中 5 次、在 `w9`…`w17` 九支上均为 **0**。 |
| **阻塞** | ① 未进主干。② **README 断言数写 299、实测 300**——本周期唯一一处 README 与实跑对账不上的地方(其自身记账件写的 300 是对的,见第 2 节)。③ **README 架构框沿用 `w15` 的失真值「三条」**(实测 8 条)。④ 不含 W12/W13 head/W14/W16/W17。 |
| **下一目标** | 已被 `w19` 完整吸收(`--is-ancestor` 成立),**本支不需单独推进**;README 那个 299 也已在 `w19` 上被改对为 301。剩余价值只在于它是 `w19` 的拓扑父节点。 |

### 4.10 `cursor/w19-g06-inject-a20c` — G-06 残留两条(核验期间新出现)

任务书列为「并行可能:W19 G-06 注入」。**核验开始时确无此分支,末次 `fetch` 时出现,且已落地。**

| 项 | 结论 |
|---|---|
| **成熟度** | **M3**。两提交(`6131fe7` 功能 + `34cf63f` 记账),基线是 `w18` **head**,纪律正确。 |
| **增量** | 处置 G-06 的残留两条知识:「多镜头写法」进拆镜系统人设、「主体参考」进主体提取系统人设。W15 只把「抽卡公式」「抽卡军规」接进生成步,G-06 当时只闭一半。 |
| **证据(本槽实测)** | `unit 301/301`(基线 `w18` 的 300 + 1)、`integration 93/93`、`cli.smoke 62/64`,0 FAIL,与其记账件自称的 301 **对上**;README 也写 301,**对上**。代码层证实:`js/wf-core.js:306` `W.sbSystem = ov => Prompts.get('sb.system', ov) + KB.pick('景别运镜', '轴线匹配', '多镜头写法')`(对照 `w18` 同行只有前两个键)、`:615` `W.extractSystem = () => W.EXTRACT_SYSTEM + KB.pick('主体参考')`(`w18` 上 `extractSystem` **不存在**)。取用口四处实测在位:`server.js:3429`(拆镜)、`server.js:3641` 与 `js/episode-util.js:134`(主体提取双端)、`js/sb-llm.js:29`(浏览器拆镜)。 |
| **证据:缺口投影确实收窄** | `Skills.gaps()['G-06']` 实测由 `w18` 的 4 条关联条目 `["subjects.refDiscipline","subjects.crossShot","shots.promptEightDim","gen.videoTpl"]` 收窄为 `w19` 的 **2 条** `["subjects.refDiscipline","subjects.crossShot"]`。 |
| **阻塞** | ① 未进主干。② **它没有清掉任何短名单条目**:pending 实测 13 条 / 14 面,与基线 `w18` **逐条一致**(SK-11 的 `check` 面、SK-19 的 `check` 面都还在)。G-06 的注入半闭合了,校验半没动,所以短名单口径不变。③ **是三条 tip 里内容最窄的一支**:不含 SK-18、不含 G-10 审片消费、不含 W17 面表单源、不含 `w7`/`w10`/`w13` 三份文档。④ **README 架构框沿用「三条」失真值**(实测 8 条)——这条失真已在 `w15 → w18 → w19` 这条线上传了三波无人修。 |
| **下一目标** | 并入 `w16`(2 文件 4 处冲突,零代码面)。它是 W15+W18+W19 三波的唯一载体,**合它一次即等于合三波**,是三条 tip 收敛里性价比最高的一步。 |

## 5. 短名单 30 条:pending 已清 / 仍缺

口径:以 `Skills.REG` 直接读出的实际数据为准(不采信文档描述),周期 2 收敛点 `w9-integration @ 641a1f5`
实测 **30 条中 16 条带 pending、17 面待落地、`CHECKS` 6 条** 作为本周期基线。

### 5.1 周期 3 的净清:4 条(但只有 2 条是本周期的新落地)

| 分类 | SK | 落在哪支 | 说明 |
|---|---|---|---|
| **周期 2 余量,周期 3 才进集成线** | SK-14 `eps.structureStage`、SK-15 `eps.payoffPoint` | `w13`(`e899a71` 合入 `w9-eps` head) | 实现是周期 2 做的,周期 2 结束时悬在集成线外;W13 的合流动作让它**首次与其他成果在同一支上并存** |
| **周期 3 新落地(2 条)** | **SK-18 `shots.sizeProgression`**(W12)、**SK-21 `gen.videoTpl`**(W15) | `w12` → `w13`/`w16`/`w17`;`w15` → `w18`/`w19` | 两条**至今不在同一支上**:SK-18 不在 `w19`,SK-21 不在 `w16`/`w17` |

三条 tip 的 pending 实测**各自都是 13 条 / 14 面**,但**清掉的不是同一批**:

| | `w16` | `w17` | `w19` |
|---|---|---|---|
| pending 条数 | 13 | 13 | 13 |
| 已清 SK-18 | ✅ | ✅ | ❌ |
| 已清 SK-21 | ❌ | ❌ | ✅ |
| `CHECKS` 实现 | **9** | **9** | **8** |

**合流后口径(三条 tip 全部并起来,当前尚未在任何一支上成立)**:
pending **12 条 / 13 面**、`CHECKS` **9 条**。相对周期 2 基线净清 **4 条 / 4 面**,`CHECKS` **6 → 9**。

机制面口径:待落地面 **W2 的 23 面 → 周期 2 末的 17 面 → 周期 3 合流后的 13 面**。
四面净清里 **3 面是 check、1 面是 inject**——**inject 面在本周期首次被清**(SK-21),
此前三个周期的净清全部是 check 面。

### 5.2 仍缺(三条 tip 合流后 12 条)

| SK | id | 待落地面 | 缺口 | 本周期是否被触及 |
|---|---|---|---|---|
| SK-03 | `core.personaCtx` | infra | G-01 | 记账诚实位已补(W14),pending 未动 |
| SK-04 | `core.memoryDual` | infra | G-02 | 同上 |
| SK-05 | `core.playbookProjection` | orchestrate | G-12 | 未触及 |
| SK-10 | `script.aiToneBan` | **inject + check** | S-02 G-13 G-10 | 未触及(唯一还剩两面的条目) |
| SK-11 | `subjects.refDiscipline` | check | G-06 G-13 | **注入半经 W19 闭合**,check 面仍开 |
| SK-19 | `shots.promptEightDim` | check | G-15 G-06 G-05 G-10 | **G-06 关联经 W19 摘除**,check 面仍开 |
| SK-20 | `shots.motionGate` | check | S-04 | 未触及 |
| SK-22 | `gen.renderCredential` | check | S-05 | 未触及 |
| SK-23 | `review.stage` | infra | G-03 | 记账诚实位早已如实,pending 未动 |
| SK-24 | `review.methodDim` | check | G-10 | **消费通道经 W14 打通**,check 实现仍缺 |
| SK-26 | `review.memoryFeedback` | orchestrate | G-11 G-02 | 未触及 |
| SK-29 | `film.deliverContract` | check | G-10 S-07 | 未触及(发布门侧 `js/release.js` 对 `Skills` 引用实测仍为 0) |

### 5.3 README 架构框的校验项条数:一处跨三个周期的失真(本轮新发现)

这是本槽独立发现、**周期 2 核验报告没有覆盖到**的一处。`README.md` 第 37/38 行架构框里那句
「校验型扩展点 CHECKS 已落地 X 条」在十支上的实测对账:

| 分支 | 实测 `CHECKS` | README 框自称 | 差 |
|---|---|---|---|
| `w9-integration` | 6 | 三条 | **−3** |
| `w11` | 6 | 三条 | **−3** |
| `w12` | 7 | 七条 | ✅ |
| `w13` | 9 | 七条 | **−2** |
| `w14` | 6 | 三条 | **−3** |
| `w15` | 8 | 三条 | **−5** |
| `w16` | 9 | **九条** | ✅ |
| `w17` | 9 | 七条 | **−2** |
| `w18` | 8 | 三条 | **−5** |
| `w19` | 8 | 三条 | **−5** |

**本槽逐提交追溯了这句话在集成线上的取值,与同一提交上的实测条数并列**:

| 提交 | 实测 `CHECKS` | README 框 | 是否对上 |
|---|---|---|---|
| `641a1f5`(`w9` head) | 6 | 三条 | ✗ |
| `37bc257`(`w9-eps` head,被合入方) | 7 | **主体面两条**(措辞另成一套) | ✗ |
| `e899a71`(合入 `w9-eps`) | 8 | 三条 | ✗ |
| `070b50d`(合入 `w12` 功能提交) | 9 | 三条 | ✗ |
| `676b090`(补合 `w12` 尾部) | 9 | 七条 | ✗ |
| `2b67101`(`w13` head) | 9 | 七条 | ✗ |
| `4d89a62` / `fa8d6e5`(`w16` 线) | 9 | 三条 | ✗ |
| `c87da63`(口径修正) | 9 | **九条** | ✅ **首次对上** |
| `01e4295`(`w16` head) | 9 | 九条 | ✅ |

**结论有三点,都比「某支写错了」更结构性**:

1. **这句话从来没对过**。自周期 2 收敛点 `641a1f5` 起,一路到 `c87da63` 之前的**每一个中间提交上都是错的**,
   最大差 6 条(`070b50d` 实测 9、框写三条)。`c87da63` 是它**第一次**与实测对上。
2. **被合入方自己也是错的,且措辞不同**:`w9-eps` head 的框写「主体面**两条**」而实测 7 条。
   两条线各自漂移、措辞还不统一,所以这不是一次冲突解错,而是**每条线独立漂移后在合并点叠加**。
3. **根因是没有断言绑住**:`CHECKS` 条数与 README 文本之间**没有任何断言**,
   `tests/unit.js` 的 contract 套件断言的是注册表内部一致性(登记的校验项必有实现、实现必被引用),不看 README。
   这也是它能潜行三个周期、跨十支、被前两份核验报告漏掉的唯一原因。

**旁证:`master` 自己也一样。** 本槽实测 `master` 的 `unit` 为 **201/201**,而 `master` 的 README 写
「单元测试(**163** 项断言」——**差 38**。即「README 数字与实跑对不上」不是波次线引入的问题,
**主干上早已存在**,波次线只是继承并放大了它。这一条把第 8.3 节第 3 步(给 README 数字加断言)
从「顺手补一下」抬成**应当优先做的一条**:它是唯一能让这类失真在 CI 层自动暴露的手段,
且成本只有两条断言。

## 6. 缺口编号闭合状态(G-01…G-15 / S-01…S-08)

按「该缺口关联的条目是否还有 pending」判定,**取三条 tip 合流后的口径**(当前尚未在任何一支上成立):

| 状态 | 缺口 |
|---|---|
| **已闭合** | G-07、G-08、G-09、G-14、**S-03**(W6 闭)、**S-06**(W7 闭)、**S-01**(剧本半 W8 + 分集半 W9,**周期 3 的 W13 首次让两半在同一支上并存**) |
| **本周期新推进** | **G-05**:SK-21 `gen.videoTpl` 的 inject 面经 W15 清空(`block('gen')` 由 0 变 344),该缺口的生成步注入半闭合;**G-06**:注入半经 W19 闭合(关联条目由 4 条收窄到 2 条),校验半(SK-11 / SK-19 的 check 面)仍开;**G-10**:审片侧首个消费点经 W14 打通,但发布门侧未动、SK-24 的 check 实现仍缺 |
| **部分闭合** | G-03 / G-04 / G-05 / G-06 / G-10 / G-12 / G-15 |
| **仍全开** | G-01、G-02、G-11、G-13、S-02、S-04、S-05、S-07 |
| **无关联入选项** | **S-08**(发布后回写上游)。实测 `gaps()` 键数在十支上**一律为 22**,S-08 不在内。W6 / W7 / W10 三份记录连续把它标为「需决定做还是明确拒绝」,**周期 3 九波过去仍未决,已连续三个周期挂着**。 |

**G-10 仍是牵连面最广的一条**,实测被 9 条 SK 引用(SK-07/08/09/10/15/18/19/24/29),
合流后仍有 5 条 pending。周期 2 报告给它的定性是「已落地的校验项全部只挂在就绪检查与问题中心,
一条都没有进审片报告或发布门」——**这条在周期 3 松动了一半**:W14 让审片路径成为第三个消费点
(`js/review.js` 对 `Skills` 引用 0 → 4)。但**发布门那一半分毫未动**:`js/release.js` 对 `Skills` 的引用
在十支上实测**全部为 0**。

## 7. 相对 `master` 未合入主干的测量

> 按任务书要求:本槽**不开 PR、不合并**,只做测量与风险量化。

### 7.1 主干现状:三个周期的成果一件未进

`master @ 9adcf0f` 上实测:

- `js/skills.js` —— **不存在**
- `docs/skills-wave/` —— **不存在**(整个目录)
- `server.js` 的 `wfPersonaNote` —— **0 次**(G-01 未进)
- `js/wf-core.js` 的 `WF_BOARD` —— **0 次**
- `js/domain.js` 的 `step('review', …)` —— **0 次**(G-03 未进,主干上主线仍是六步)
- `js/wf-core.js` 的 `sizeGap` —— **0 次**(SK-18 的单源阶梯未进)
- `js/wf-core.js` 的 `optimizeSystem` —— **0 次**(W18 收编未进)
- `js/review.js` 的 `Skills` —— **0 次**(G-10 审片消费未进)

即:三个周期、39 条远端分支、30 条 skill 索引、9 条校验项、全部缺口收敛,**在主干上都不可见**。
`w5-cycle1-audit.md` 与 `w10-cycle2-audit.md` 里那条同样的结论,**第三次逐字成立**。

### 7.2 未合入清单(`master` 外共 39 条远端分支,全部未进主干)

> 39 条是本报告推送**之前**的计数;本核验分支推上去之后为 40 条。

| 归属 | 分支 | 是否已被某条 tip 吸收 |
|---|---|---|
| W1–W10 共 29 条 | `w1-*`…`w10-*`、`agent-flow-sota-analysis` | 代码面全部已被 `w16` 吸收;**文档面 `w7-integration-log.md` / `w10-cycle2-audit.md` 只在 `w16` 上有**,`w17` / `w19` 两条 tip 缺 |
| 周期 3 | `w11-preflight-film-assert-883b` | 已被三条 tip **全部**吸收 |
| 周期 3 | `w12-size-gap-check-df6c` | 被 `w16` / `w17` 吸收,**`w19` 未吸收** |
| 周期 3 | `w13-integration-a394` | **head 差 2 提交未被 `w16` 吸收**;已被 `w17` 完整吸收 |
| 周期 3 | `w14-review-skills-check-5291` | 被 `w16` 吸收,**`w17` / `w19` 均未吸收** |
| 周期 3 | `w15-gen-block-d9a3` | 被 `w18` / `w19` 吸收,**`w16` / `w17` 均未吸收** |
| 周期 3 | `w18-gen-prompt-unify-0df1` | 已被 `w19` 完整吸收 |
| 周期 3 | `w16` / `w17` / `w19` | 三条 tip 自身,**互不包含** |

**即:周期 1–3 的成果分裂在三条 tip 上,距全量差三件——`w13` head 的 2 个提交、`w17` 一支、`w19` 一支(带 W15+W18)。**

### 7.3 合并风险量化(read-only `git merge-tree` 模拟,未落任何提交)

| 模拟合并 | 冲突文件 | 冲突块 |
|---|---|---|
| `master` ← `w16` / `w17` / `w19` 任一 | **0** | **0**(`master` 是严格祖先,快进) |
| `w16` ← `w13` head(补 W13 收敛记录) | **2** | `README.md` 2 / `docs/skills-wave/README.md` 2 |
| `w16` ← `w19`(**一次带 W15+W18+W19 三波**) | **2** | `README.md` 2 / `docs/skills-wave/README.md` 2 |
| `w16` ← `w17`(面表单源) | **3** | `README.md` 2 / `docs/skills-wave/README.md` 2 / **`tests/unit.js` 1** |
| `w17` ← `w19` | **2** | `README.md` 2 / `docs/skills-wave/README.md` 2 |

三点结论:

1. **主干方向零风险**:`master` 是全部分支的严格祖先,任一 tip 并入主干都是纯快进。
   风险不在合并动作上,而在「主干长期停滞、分支持续加深」本身——`w16` 已领先 **125** 个提交
   (周期 2 末是 103,周期 3 又加了 22)。
2. **三条 tip 的合流冲突面极薄,且集中在两份 README**:`js/skills.js`、`js/wf-core.js`、`js/commands.js`、
   `js/review.js` 在**全部四种模拟里都自动合并零冲突**。周期 2 报告担心的「`CHECKS` 追加式结构在并行下的冲突」
   在本周期**没有复发**:`w16 ← w15` / `w16 ← w19` 的 `js/skills.js` 均零冲突。
   唯一的代码面冲突是 `w16 ← w17` 的 `tests/unit.js` 1 处(两侧都在改并集断言的面清单)。
3. **`README.md` 的两处冲突每次都在同一位置**——正是第 5.3 节那句「CHECKS 已落地 X 条」与断言数。
   解冲突时**必须按实跑重算、不能取任一侧**,否则失真会继续传递。合流后判据:
   框里应为**九条**、断言数按实跑填(合流全量后 `unit` 预期在 310 以上,**须实跑,勿照抄本报告**)。

## 8. 阻塞汇总与下一目标

### 8.1 周期 2 三条阻塞的处置结果(先记账)

| 周期 2 阻塞 | 周期 3 处置 | 证据 |
|---|---|---|
| 2 · `w9` 漏合 3 个提交 | **已闭合**。`97f6ba2` / `bbd7ebb` 经 W13 补进(随 `w9-eps` head),`072f342` 经 W16 的 `6640b93` 补进 | `w16` 上三条 `--is-ancestor` 全成立,`w7-integration-log.md` 存在 |
| 3 · 两条互不包含的线未合流 | **已闭合**。W13 的 `e899a71` 合入 `w9-eps` head | `w13` 上 `CHECKS` 实测 9 条,分集两条与其余七条并存 |
| 6 · SK-03/SK-04 pending 与实况矛盾且无说明 | **已闭合**。W14 的 `a927e25` 补 `note`,三条口径统一 | 本槽直读注册表,SK-03/04/23 三条 `note` 同规格 |
| 7 · G-10 校验结论进不了审片与发布门 | **半闭合**。W14 打通审片侧;发布门未动 | `js/review.js` 引用 0 → 4;`js/release.js` 十支实测全为 0 |
| 8 · S-08 连续两波未决 | **未处置**,已连续三个周期 | `gaps()` 键数十支实测一律 22 |
| 9 · `master` 的 2 项 `cli.smoke` 失败 | **未处置**,已连续三个周期 | 本槽在 `master` 上独立复现 |

### 8.2 本周期阻塞(按严重度)

| # | 阻塞 | 严重度 | 依据 |
|---|---|---|---|
| 1 | **主干停滞三个周期**:`master` 上 `js/skills.js` 与 `docs/skills-wave/` 均不存在,39 条分支无一进主干,最深的一支已领先 125 提交 | 高 | 第 7.1 节实测 |
| 2 | **周期 3 结束时是三条互不包含的 tip**,无「当前全量分支」:`w16` 缺 SK-21 与 W18/W19,`w17` 缺 W14 的审片消费与诚实位,`w19` 连 SK-18 都缺 | 高 | 第 3 节矩阵、第 5.1 节 |
| 3 | **「合并取分支 head」这条口径没被遵守,在 `w16` 上复发**:`w13 ∩ w16 = 070b50d`,漏 `676b090`+`2b67101`,`w13-integration-log.md` 在集成线上缺失 | 中高 | 第 4.7 节 |
| 4 | **`checkout --ours` 式粗粒度冲突解法造成过实际内容丢失**(`c87da63` 自陈补回四处 README 增量)。本槽已核实测试层无残留丢失,但机制仍在 | 中高 | 第 4.7 节 |
| 5 | **README 与实测脱钩,且主干上早已如此**:架构框校验项条数在 7/10 支上失真(最大差 5 条),集成线上该句**从周期 2 收敛点到 `c87da63` 之前每一个提交都是错的**;`master` 自己的断言数也差 38。根因是**没有任何断言绑住 README 数字** | 中 | 第 5.3 节逐提交追溯 |
| 6 | **`w18` README 断言数写 299、实测 300**(其记账件写的 300 是对的);`w19` 已改对 | 低 | 第 2 节 |
| 7 | **G-10 的发布门半边分毫未动**:`js/release.js` 对 `Skills` 引用在十支上实测全为 0;SK-29 因此仍全开 | 中 | 第 6 节 |
| 8 | **九波里只有两波清掉短名单条目**(W12 / W15),治理与单源收编占七波。不是缺陷,但「一个周期净清 2 条」的速率下,剩余 12 条需要显式排期 | 中 | 第 5.1 节 |
| 9 | **S-08 连续三个周期未决** | 低 | 第 6 节 |
| 10 | **`master` 的 2 项 `cli.smoke` 失败连续三个周期未修** | 低 | 第 2 节 |

**一条方法性观察**(承接周期 2 报告末尾那条,本轮有新证据):周期 2 报告把「合并一律取分支 head」
定为口径后,**W13 / W17 / W18 / W19 四波都遵守了**(基线分别取 `w9-eps` head、`w13` head、`w15` head、`w18` head),
**只有集成方 W16 在合 `w13` 时又没取 head**。也就是说口径对功能槽生效了、对集成槽没生效。
根因不同于周期 2:周期 2 是「按父提交合并」,周期 3 是「集成槽从另一条集成线的中间点起分支」——
`w16` 从 `070b50d` 起而不是从 `w13` head 起,于是两条集成线并行推进,谁也不含谁。
**建议把口径从「合并取 head」扩成「同一时刻只允许一条集成线,新集成波必须以上一条集成线的 head 为基线」**,
否则 tip 数量会随波次继续增长(周期 2 末 2 条 → 周期 3 末 3 条)。

### 8.3 下一目标(建议次序;本槽不执行)

1. **把周期 3 收成一条线,三步,全部零代码冲突或近似零**:
   ① `w16 ← w19`(**一次带 W15+W18+W19 三波**,2 文件 4 处冲突,收益最大,优先做);
   ② `w16 ← w17`(3 文件 5 处冲突,`tests/unit.js` 那 1 处别丢面表断言;W17 的派生式面表能自动接住后续新增面,**做早了能降低以后每一波的合流成本**);
   ③ `w16 ← w13` head 的 `2b67101`(纯文档,补回 `w13-integration-log.md`)。
   **合流后判据**(逐条实测,勿照抄本报告):`Object.keys(Skills.CHECKS).length === 9`、
   pending **12 条 / 13 面**、`Skills.block('gen')` 非空、`js/review.js` 对 `Skills` 引用非 0、
   `Skills.preflightStages()` 回五面、`docs/skills-wave/` 下 `w7` / `w10` / `w13` / `w15` 四份记账件齐在。
   最后按实跑重算 README 三个数字**并把架构框改成「九条」**。
2. **决定主干策略。这是第 1 阻塞,且已是连续三个周期的同一条结论,不宜再推。**
   技术面无障碍(纯快进、零冲突);要定的是分批口径——若分批,`w6 head`(周期 1 全量)、
   `w9 head`(周期 2 全量)、第 1 步做完的 `w16`(周期 3 全量)是拓扑上的三个天然切点。
3. **给 README 的两个数字加断言。这是本轮最便宜、收益最确定的一条改动,建议与第 1 步同轮做。**
   第 5.3 节那处失真能潜行三个周期、跨十支、被前两份核验报告漏掉,唯一原因是没有断言绑住;
   且 `master` 自己的断言数就差 38,说明这不是波次线的偶发问题。
   建议在 `tests/unit.js` 的 contract 套件里加两条:README 架构框的校验项条数须等于
   `Object.keys(Skills.CHECKS).length`、README 断言数须等于本次实跑总数。
   加完之后第 1 步合流时那两处 README 冲突会**自动获得判据**——解错就转红,不必再靠人工核对。
4. **收口 G-10 的发布门半边**:`js/release.js` 接 `Skills.check` 的只读消费,形态可直接复用 W14 在
   `js/review.js` 上验证过的那一套。这一步同时是 SK-29 `film.deliverContract` 与 SK-24 `review.methodDim` 的前置,
   一次性能松开 2~3 条 pending。需先定「方法论门要不要改既有 fail/warn 口径」的产品口径。
5. **按 `Skills.gaps()` 投影挑剩余校验项**:G-06 的校验半(SK-11 / SK-19 的 check 面,W19 刚把注入半闭掉,接着做最顺)、
   S-02(SK-10,需先补 KB 条目正文,且是唯一还剩两面的条目)、S-04(SK-20)、S-05(SK-22)四条各自独立,
   宿主形态已被剧本半 / 分集半 / 分镜段连续验证三次,可并行开槽——但按 8.2 的方法性观察,
   **基线一律取当前唯一集成线的 head**,且开工前先按 W11 的做法补并集断言(变异测试已证明这类断言有牙)。
6. **对 S-08 给出明确结论**:做则进 30 条(短名单变 31 条,需同步波次配比断言),不做则在短名单第 7 节标记「明确拒绝」。
   连续三个周期挂着不决,本身就是记账噪音。
7. **修 `master` 那 2 项 `cli.smoke` 失败**。连续三个周期由三份核验报告独立复现同两项,与本波次线无关,
   但它是主干上唯一的红,合并主干前应清掉,否则第一次快进之后主干就带着红。

## 9. 本报告的复核方式

全部数字可按下述步骤独立重跑(本槽即如此取得;`/tmp` 下的探针脚本不入仓库):

```
# 1. 各支独立工作树,互不干扰
for b in w9-integration-f8f9 w11-preflight-film-assert-883b w12-size-gap-check-df6c \
         w13-integration-a394 w14-review-skills-check-5291 w15-gen-block-d9a3 \
         w16-integration-3488 w17-preflight-stages-0857 w18-gen-prompt-unify-0df1 \
         w19-g06-inject-a20c; do
  git worktree add --detach /tmp/wt/$b origin/cursor/$b
done
git worktree add --detach /tmp/wt/master origin/master

# 2. 逐支复跑(master 基线同法)
cd /tmp/wt/w16-integration-3488
node tests/unit.js          # 309/309
node tests/integration.js   # 93/93
node tests/cli.smoke.js     # 62/64(2 项与 master 同样失败)

# 3. 注册表状态直接读数据,不读文档
node -e "const S=require('./js/skills.js');
  console.log('SK', S.REG.length, '| CHECKS', Object.keys(S.CHECKS).length,
              '| block(gen)', S.block('gen').length);
  console.log(S.REG.filter(x=>x.pending.length).map(x=>x.sk+':'+x.pending).join(' '));
  console.log('preflightStages', JSON.stringify(S.preflightStages ? S.preflightStages() : 'n/a'));
  console.log('G-06', JSON.stringify(S.gaps()['G-06']));"

# 4. README 对账(第 5.3 节那处失真)
grep -oE "CHECKS 已落地[一二三四五六七八九十]+条|单元测试\([0-9]+ 项断言" README.md

# 5. 拓扑与漏合测量(read-only,不落提交)
git merge-base --is-ancestor origin/master origin/cursor/w16-integration-3488 && echo "master 是严格祖先"
git log --oneline origin/cursor/w16-integration-3488..origin/cursor/w13-integration-a394   # 应回 2 条
git log --oneline origin/cursor/w16-integration-3488..origin/cursor/w19-g06-inject-a20c
git merge-base origin/cursor/w13-integration-a394 origin/cursor/w16-integration-3488       # 070b50d,非 w13 head

# 6. 合并风险(read-only 模拟)
git merge-tree --write-tree --messages origin/cursor/w16-integration-3488 \
                                       origin/cursor/w19-g06-inject-a20c

# 7. 断言强度的变异测试(在副本上做,勿在工作树上改)
cp -r /tmp/wt/w16-integration-3488 /tmp/mut && cd /tmp/mut
perl -0pi -e "s/Skills\.check\('shots', \{ p, ep \}, ck\), //" js/commands.js
node tests/unit.js          # 应由 309/309 转为 306/309, 3 FAIL
```

> 提醒:第 0.1 节已注明 `w16` 在核验期间前进一个提交、`w19` 在核验期间才出现。
> 若复核时点晚于本报告,请先比对 head SHA 再采信数字——尤其 `w16-integration-3488`(它当时正在活动)
> 与 `w19-g06-inject-a20c`(它当时刚出现)。

`node tests/e2e.js` 按仓库纪律**未跑**(需用户明确要求)。
本轮未开 PR、未合并任何分支、未改动任何业务代码;本分支的唯一改动是本文件。
