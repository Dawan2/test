# W288 · 把 W287 合入 W286 尖(集成线上第十四条「快进形」)

一条槽:W287 给分镜面补 `shots-dedupe` 入口(集级顶栏一处挂住五档视图,预览一屏不写库、
确认那一下才改 id),并进同一条集成线。一次 `--no-ff`,零冲突。

产品语义一个字没动;记账、索引、份数下限三处按合入纪律同轮校齐。

---

## 一、支尖与形状(全部现取 `rev-parse`,不采信任何一侧的自称)

| 位 | 现取 SHA | 与自称对不对得上 |
|---|---|---|
| 我方尖 `P1` = `origin/cursor/w286-integration-05c0` | `e254ffdcf949d39f755935fa0dfe0f3308925140` | W287 自称从 `e254ffd` 起支,**逐字节相同** |
| 对侧 `P2` = `origin/cursor/w287-shots-dedupe-ui-38eb` | `04ef3cf773892da3bcb245a7cf25f705bb479b42` | 自称 HEAD `04ef3cf`,**逐字节相同**,且是链尾 |
| `merge-base(P1, P2)` | `e254ffd` | **恰等于 `P1`**,即 `P2..P1` 空 → **快进形**(集成线上第十四条) |
| 新支 | `cursor/w288-integration-4291`,从 `e254ffd` 拉出 | |
| merge commit `M` | `095a1ae5faf93ddb63287a2ef7f8cbff93ce602c`,parents `e254ffd` + `04ef3cf` | |
| `tree(M)` / `tree(P2)` | 两边都是 `a0d19b9c72233c3cb458c28c660c5e67ca6a4927` | 逐字节相同 |

`P1..P2` 三条提交:`798f3b3`(分镜面补入口)、`54168d8`(记账 + 索引与花名册补行)、
`04ef3cf`(记账补齐变异表与 live 数字)。

**快进形让整树 diff 恒空,这一格一条结论都不承载**(第十四次)。`git diff M P2` 空、
`tree(M) == tree(P2)`,两句在任何一次快进形合入上都恒真——包括「对侧一个字都没改」和
「我方把东西丢干净了」这两种正相反的情形。故取证全部压在**变异**与 **live** 两面。

产品面 `git diff P1 M` 逐文件现取:**只有 `js/storyboard.js` 一个文件**(+90 / −1),
`cli.js` / `mcp.js` / `server.js` / `index.html` / `js/roles.js` / `js/domain.js` /
`js/sb-views.js` / `js/produce.js` **八个文件 diff 全空**——两条 CLI 命令、MCP 工具表、
主体库那条入口、去重规则派生、删除语义那几处一格未动。但"产品面不空"只证明**对侧改了东西**,
仍不证明**我方没丢东西**。

---

## 二、本槽真正承载结论的那一格:同一手变异,起点红几条 / 合完红几条

**M1**:把 `Domain.dupIdScan` 派生里「首次出现那一行/位留原 id」改成首行也发一个新 id
(规则本身被改坏,四处同读者都该红)。同一手改法在两处各整跑一遍 `node tests/unit.js`:

| 跑在哪 | 结果 | 红哪几条 |
|---|---|---|
| 起点 `e254ffd` 干净 worktree | **`698/703 PASS, 5 FAIL`** | CLI `shots-dedupe` / CLI `subjects-dedupe` / 两条命令同读一份规则 / **主体库页面入口** / `domain · dupIdScan` |
| 合完 `095a1ae` | **`699/705 PASS, 6 FAIL`** | 上面五条 **+ `commands · 分镜面那条去重入口`** |

**同一手概念,起点红 5 / 合完红 6**,第六条红来自新接上的**第四个消费点**。
这是快进形这一槽里唯一说得出「真收到一道保证」的证据,不靠任何一侧的文档口径。

对照 W284(起点红 1 / 合完红 4)与 W286(起点红 4 / 合完红 5):同一手概念的红数
`1 → 4 → 5 → 6`,消费点每接上一个就多一层现跑出来的红,`Domain.dupIdScan` 此刻**四处同读**。

**M2–M6 在起点上锚点全部失配**(`M2 锚点失配` / `M3 锚点失配` / … 逐手报出),
即起点那棵树上压根没有这段代码——这是「合进来的东西是新的」的反向一格。

---

## 三、变异(逐手本槽在合完的树上现跑,`git checkout -- .` 逐手复原)

| # | 变异 | 红几条 | 报错句(现取) |
|---|---|---|---|
| M1 | 派生里首行也发新 id | **红 6** | 见 §二 |
| M2 | 摘掉确认闸(落库提到 `onMount`,开弹窗那一下就写库) | **红 2** | 行为面:`预览这一屏不许写库(这条入口的"可撤销"就落在这一格上):期望 0,实际 1`;源级:`写库只许有一处` |
| M3 | 摘掉集级顶栏那一处入口 | **红 2** | 行为面:`视图 board 上看不见去重入口(四视图与剪辑台共用集级顶栏那一行,挂漏一处就有一条进来的路看不见)`;源级:`入口只该插一处…期望 1,实际 0` |
| M4 | 把规则抄回分镜面(**行为完全等价**) | **红 1** | 只有源级那条接住:`页面这一端的扫描须委托 Domain.dupIdScan(几处各写一份规则迟早对同一份脏数据给出几种计划)` |
| M5 | 「会塌几条」改成页面自己数一套「第几条同 id」 | **红 1** | `"会塌几条"须由同一份 Domain.reviewRows 在当下这棵表与按计划改过 id 的表上各跑一遍现算` |
| M6 | 落库那一下顺手改写 `lastReview.perShot[].shotId` | **红 2** | 行为面:`旧报告那几条按 id 解析退回首行…期望 "0,1,0,0",实际 "2,3,0,0"`;源级:`引用会塌只许在预览文案里说清:弹窗段不许顺手改报告或改选中镜` |
| CF-floor | `TOPIC_FLOOR` 23 → 22 | **红 1** | `下限 22 低于花名册的 23 条:下限只增不减,改小它就是给"往后白删几条"腾地方` —— 即明令那句「禁止仍写 22」有判据接着 |
| CF1 | 只把两条新用例删掉(护栏登记、花名册、文档数字一概不动) | **红 4** | README 三方对账(`实测 703,文档 705`)+ 三套件下限 + **护栏主题失联**(点名 `dedupe-shots-ui-single` 与它的锚点)+ 主题抽样那条 |

**M4 / M5 只有源级那条接住**:行为完全等价的"抄一份"永远量不出两份计算会漂,行为面在这两手上全绿。
**M2 / M3 / M6 两面各红一条**,行为面与源级各说一件事、没有一手落到同一句话上。

---

## 四、live 数字(逐格现跑,两侧自称一律不抄)

| 项 | W286 自称 | W287 自称 | **本槽 live** | 说明 |
|---|---|---|---|---|
| `unit` 总数 | 703 | 705 | **705** | `705/705 PASS, 0 FAIL`;24 套件逐格求和 `= 705` 自洽。**W286 自称的 703 是起点数,没抄** |
| `unit · commands` | 65 | 67 | **67** | +2 全在这一套(行为面一条 + 源级一条) |
| `unit · contract` | 153 | 153 | **153** | 未动 |
| `unit · domain` | 45 | 45 | **45** | 未动 |
| `unit · skills` | 104 | — | **104** | 未动 |
| `unit · issues` | 22 | — | **22** | 未动 |
| `integration` | 152 | 152 | **152** | `152/152 PASS, 0 FAIL` |
| `cli.smoke` | 115/117 | 115/117 | **115/117** | 见下 |
| MCP `tools/list` | 40 | 40 | **40** | 运行期 `tools/list` 现取,不数源码字面 |
| CLI `Object.keys(CMD)` | 50 | — | **50** | bracket **26** + dotted **24**,两种写法之和 |
| `Domain.dupIdScan` 同读处 | 3 | 4 | **4** | `cli.js:727`、`cli.js:814`、`js/roles.js:284`、`js/storyboard.js:36` |
| `gaps()` 键数 | 20 | 20 | **20** | 一条没剥 |

**名集多重集 `+2 −0`、`703 + 2 = 705` 自洽**(两处各自 `git checkout -- .` 后在干净整树上取用例名排序比对):
进来的正是那两条新判据、**一条没出**。

- `commands · 分镜面那条去重入口:四视图与剪辑台共用的集级顶栏上真长出来,点开只预览、确认那一下才改 id(一行不删、零计费;uiSel 与镜头组落到同一行,旧审片报告那几条退回首行只报不改)`
- `commands · 分镜面那条去重入口不许另抄一份规则:页面这一端只注入自己的发号器与单位词,规则仍现取 Domain.dupIdScan;「旧报告会塌几条」那句只现取 Domain.reviewRows(纯改表零计费)`

`cli.smoke` **单独整跑**,`env -u HUJING_SERVER -u HUJING_TOKEN` 加
`MV_DATA_DIR` / `MV_UPLOADS_DIR` / `MV_CONFIG` 隔离,**115/117**;两条失败与 `master`
(`9adcf0ff964891dc17c352f6ae06db6ee7a9383b`)在同 env 下现取的 **51/53** 同名同表现,明令允许:

- `FAIL | 未登录 whoami → exit 3 | exit=1`
- `FAIL | llm --json mock 链路 | undefined`

`node --check` 逐个跑过:`js/storyboard.js` / `js/domain.js` / `js/roles.js` / `cli.js` / `tests/unit.js` 全 OK。
**按明令不跑 `tests/e2e.js`。**

---

## 五、`TOPIC_FLOOR`、花名册与棘轮(对齐到 live)

| 格 | live | `FLOOR` 字面 | 差额 |
|---|---|---|---|
| 单元测试 | 705 | 705 | 0 |
| 集成测试 | 152 | 152 | 0 |
| CLI 冒烟 | 117 | 117 | 0 |
| 记账件份数 | **303**(本文进来那一份) | 302 → **303**(本槽抬) | 0 |
| 护栏主题 | 24(在册 23 + 销号 1) | 23 | 1 |

`SLACK` **3**,五格差额 `0/0/0/0/1`,**一格都没超**。

**`TOPIC_FLOOR` 与花名册对齐到 23**(明令:对侧自称 23,禁止仍写 22):
`GUARD_TOPICS` 在册 **23** 条 / `GUARD_TOPICS_CLOSED` **1** 条 / `TOPIC_FLOOR` **23** /
花名册 **23** 行,四者对账。W285 立的 `dedupe-ui-rule-single` 与 W287 立的
`dedupe-shots-ui-single` **两条都在**,不是二选一。CF-floor 现跑证明「仍写 22」当场红 1。

**真并集逐处现取核过**(快进形让并集自动成立,但不采信"自动"这两个字):

- `README.md` 第 **299** 行「🧹 主体 id 去重」那段(W285)与第 **300** 行「🧹 镜头 id 去重」那段(W287)**两段都在**;
  第 **618** 行单元数写 **705**、第 **620** 行集成写 **152**、第 **624** 行冒烟写 **117**,三处与 live 相等。
- `GUARD_TOPICS` 花名册末三行:`landed-seat-order`(W281)/ `dedupe-rule-single`(W283)/
  `dedupe-ui-rule-single`(W285)/ `dedupe-shots-ui-single`(W287),在册集合与花名册集合双向差集都空。
- `docs/skills-wave/README.md` 索引表:`w285` / `w286` / `w287` 三行都在且按波次号递增,本文那一行插在 `w287` 之后。

---

## 六、核过的四格(明令逐条)+ live 探针

| 明令那一格 | 现取核法 | 结果 |
|---|---|---|
| **顶栏一处入口** | `js/storyboard.js` 里 `${dedupeBtn}` **恰 1 处**(第 156 行),插入点在模板里排在视图分档之前 | ✔ 五档视图共用 |
| **确认闸** | 去重那一段里 `Store.save()` **恰 1 处**,与 `ep.shots[x.order].id = x.to` 同在 `[data-x=apply]` 的 handler 内;预览段零写库 | ✔ |
| **`dupIdScan` 四处同读** | `cli.js:727`(主体)、`cli.js:814`(镜头)、`js/roles.js:284`(主体库入口)、`js/storyboard.js:36`(分镜面入口) | ✔ 四处 |
| **`lastReview` 字段未改写** | `js/storyboard.js` 里 `lastReview` 只出现在读的位置(`reviewCollapseCnt` 判空与 `Domain.reviewRows` 入参、以及原有那两处均分标签);去重段零写入 | ✔ |

**live 探针 23/23 通过**(真起服务 + 无头 Chrome + 真 `Store` / 真 `U.openModal` / 真 DOM,
探针只在 `/tmp` 跑、不进仓库)。夹具是「四行分镜,第 1/2/4 行同 id、第 3 行独立」,
另带一份按行出条目的旧整集审片报告(四条,`reportId` 各是各的):

- 五档视图 `shots` / `strip` / `board` / `groups` / `cut` **各真渲一遍**,顶栏那条入口逐档现取都是
  `🧹 镜头 id 去重(2)`——「一处入口挂住五条进来的路」是渲出来的,不是读代码读来的。
- 点开只预览:弹窗真开、主按钮是 `✓ 确认去重(改 2 行的 id)`,而**库里 id 仍是 `sh_dup,sh_dup,sh_solo,sh_dup`**(一个字没写)。
- 预览黄字现算现报**塌 2 条**,并写明首行留原 id 与「一行不删」。
- 确认那一下才落库:首行留 `sh_dup`、撞车两行各发新 id(`sh_` 前缀、两个互不相同)、
  独立那行 `sh_solo` 未动;**仍是四行**、剧情与提示词逐字未改。
- 引用面:`ep.uiSel` 仍是 `sh_dup`、`shotGroups` 逐字节原样、
  **`lastReview` 落库前后逐字节相同**(服务端那份 `perShot` 现取仍是 `sh_dup@r0,sh_dup@r1,sh_solo@r2,sh_dup@r3`)。
- 计费面:积分 **100 → 100** 一分没动、任务台 **0** 条登记;扣费流水里唯一那条是
  `新用户注册赠送`(注册那一下就在,与本入口无关)。
- 收拾干净后重开分集页,入口**自己消失**;全程页面异常 **0** 条。

明令没碰而现取核过一遍的:`js/roles.js` 主体库入口(diff 空)、`js/sb-views.js:745` 与
`js/roles.js:560` 两处按 id 删(**删除语义不是去重语义**,一字未改)、
`js/domain.js:415` 的 `unique` 口径、`state-put` 不设闸、`gaps()` 20 键不剥。
残留锚点现取复核:`js/batchops.js` 的 `.order + 1` **17** 行、`js/produce.js` 与 `js/issues.js` 各 **0** 处。

---

## 七、残留(按明令一条没代修,原话逐条保留)

W287 那五条,原话照搬:

1. **`cli.js` 的 `shots-dedupe` 注释里点名 `Domain.reviseTargets` 的那半句已经不准了。**
   它仍写着 `lastReview.perShot[].shotId` / `ep.uiSel` / `Domain.reviseTargets`「一律按 `find` 首行语义解析…
   去重前后落到的是同一行」;而 W263/W267 把行对位改成序数口径之后,后两者是经 `Domain.reviewRows`
   按序数落行、**去重后才退回首行**。就**行为**而言两端一致(本槽行为面逐格量过),不准的只是那句注释。
   本槽没改它:明令只做一条产品缺口,且它属于「审片合入那一问」的交代面。改的时候连 §2.2 那张表一起搬。
2. **旧报告回位仍只有「重跑一次整集审片」一条路。** 本槽把「会塌几条」现算现报进预览、并把回位办法写在同一格里,
   但那是让用户自己再花一次审片的钱。真正的收法是让 `perShot` 的行对位不再靠序数(例如落库时按行写一个稳定的行键),
   那是改审片合入,明令不在本槽。
3. **「同 id 多行」在页面上仍只有这一个出口,没有入口侧的闸。** 整树导入/恢复那几条路照旧能写进重复 id
   (明令不给 `state-put` 加闸),这条入口是**存量**出口;发现它仍靠打开分集那一下顶栏上多出来的按钮,
   项目详情面与分集卡片上没有对应的角标。
4. **一位不删的那笔代价照旧摆在预览文案里。** 用户看完计划想「少几行」的话,单镜删除仍按 id 匹配、
   会把同 id 那几行一并删光(那是删除语义不是去重语义,明令不碰)。预览里把这笔代价说清了,
   但页面上没有「删某一行」的路。
5. **主体侧与镜头侧两条入口的文案各写一份。** 两条入口同形(同读 `Domain.dupIdScan`、同一道确认闸),
   但弹窗里那几句话是各写的;要合并成一份模板得先把单位词(位/行)与引用面交代那一段参数化。

W286 起未收项,原话照搬(第 1 条「分镜面仍无 shots-dedupe 入口」已由 W287 收掉,不再列):

2. **卡片无重复标记**——同 id 那几位在卡片上与几个不同主体逐字一样;live 探针现取:三张卡里带「同 id / 重复 / dup」字样的 **0 张**。页面上发现它只靠页头那个数。
3. **dupSubjectRowsNote 页面按钮半句无判据**——本槽量过:M6 把末句退回「先用 CLI subjects-dedupe」现跑 **红 0**,那半句可以静默退回去。
4. **unique 扫描前口径**——`js/domain.js:415` 的 `unique` 数的是**进来那张表**上的相异 id 数,不是去重之后的位数;调用方拿它当「收拾完剩几位」会读错。
5. **按 id 删仍清每一位**——`js/roles.js:560` 一字未动。那是删除语义不是去重语义,本槽不碰它。
6. **落库无 rev 闸**——确认那一下直接 `Store.save()`,不比 rev、不检冲突;别处同时改了同一棵树时以后写的那一份为准。
7. **state-put 不设闸**——逃生舱 `state-put --force` 整树原样落库、一处领域校验都不做,主体 id 唯一性仍由调用方自己负责。
8. **单发 `landed` / 共位 / 座位键等 W282 起未收项**——原样挂着。残留锚点本槽现取复核:`js/batchops.js` 的 `.order + 1` **17** 行、`js/produce.js` 与 `js/issues.js` 各 **0** 处。

另新记两格,如实登记本槽没做的量:

- **快进形第十四次,树面那两句仍是恒真句,本槽一条判据都没为它补。** `git diff M P2` 空与
  `tree(M) == tree(P2)` 在这十四次里逐次成立,而它们对「我方丢没丢东西」始终一言不发;
  取证照旧全靠「同一手变异在起点与合完各跑一遍」。要让树面说得出话得换判据类别(例如把起点树
  与合完树在**产品面文件集**上做定向比对),那是另一条线的事。
- **`js/roles.js` 与 `js/storyboard.js` 两条入口的弹窗文案本槽没做重复度量。** W287 残留第 5 条
  记的是「各写一份」,本槽只核了两处 diff(主体侧 diff 空)、没有量两段文案实际重合多少行,
  故「参数化的收益有多大」这个数此刻仍是空的。

---

## 八、一句话

快进形第十四条,整树 diff 恒空与 `tree(M) == tree(P2)` 两格都不承载结论;
承载的是**同一手 M1 在起点红 5、合完红 6**——第四个消费点接上之后,
「这份规则只有一份」又多了一层现跑出来的红,而 M2–M6 在起点上锚点全部失配,
反向说明合进来的这一段在起点那棵树上压根不存在。
数字逐格现跑、**W286 自称的 703 与 W287 自称的 705 都没盲抄**(703 是起点数、705 是 live),
名集多重集 `+2 −0` 自洽;`TOPIC_FLOOR` 与花名册**对齐到 23**、CF-floor 现跑证明「仍写 22」当场红;
`SLACK` 3 一格没用掉、`gaps()` 20 键一个没剥;live 探针 23/23 且积分一分没动;
W287 五条与 W286 起七条残留一条没代修、原话逐条保留。
