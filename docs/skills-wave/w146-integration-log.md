# W146 · 三条判据槽并入集成线的收敛记录(集成分支)

> 集成分支:`cursor/w146-integration-dbd5`,基线 `cursor/w142-integration-4266 @ d877051`。
> 本文只记**收敛过程**:基线怎么定的、合了哪三条、冲突面长什么样、合并后的实测数字、没删测没摘 gaps 的取证。
> 被合入三槽自己的内容分别在 `w140-gaps-keys.md`、`w141-report-if.md`、`w144-index-order.md`,本文不代述、不复写、不改它们正文里的任何时点读数。
> 三次合并都 `--no-ff`、各一个合并提交、可 revert;全程只解冲突与收敛数字,**不重做已落地的功能**,也**没有改到任何一条别人的判据**(与 W142 那次不同,本槽三条槽互不相干,3.4 有取证)。

## 1. 结果一句话

`--no-ff` 合入三条,按序、**各取被合入支的实际 HEAD**:

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w140-gaps-keys-c2fe@9635416` | `0050647` | `Skills.gaps()` 的键集此前只有几处"键数 20"的顺带断言,数得出少一个却看不出改名。补一条 `contract` 用例四层各判一件事:20 键逐字点名、只收两份缺口图谱登记过的编号、反向对账(图谱有而键集没有的须明写闭合理由)、落地一面不摘键 | **零** | +1 条(`contract`) |
| `w141-report-if-ef18@c07dcab` | `a7ab716` | `integration`/`cli.smoke` 的用例数按行首 `report(` 静态点数对账,既有五层封的清一色是"静态 < 实跑",反方向零判据——把一条 `report(...)` 搬进 `if (…) { … }` 的花括号里四个静态口径一个不变而实跑少一条。补一条反过来立白名单:登记点的块链上只许有 `main` 函数体与裸 `{ }` 分节块 | **零** | +1 条(`contract`) |
| `w144-index-order-eca5@9030fcb` | `065ec2e` | `docs/skills-wave` 索引表既有三条契约钉的全是"哪些行在场",一行也不管"排在哪"。补一条:波次号沿表往下不许倒退(同号内不限先后),并自带取数口守卫 | **零** | +1 条(`contract`) |

合并后回归 **`unit 504/504`**、**`integration 130/130`**、**`cli.smoke 100/102`**(2 项失败与 `master` 同名同表现,见 5.2),`contract` 子套件 **112/112**。

治理面零变动:提示词注册表 41 条、能力短名单 30 条、`Skills.CHECKS` 17 条、`preflightStages()` 七面、`gaps()` 20 键(`G-03` 仍是两条值)、`KB.SECTIONS` 18 条、`playbooks()` 5 条——七个数一个没动,且是**结构性成立**:`js/`、`server.js`、`cli.js`、`mcp.js` 相对基线**一个文件都没进 diff**(5.4)。

**本槽最值得记下的三件事:**

1. **这是这条集成线第一次一整槽只加判据、零产品代码。**
   三条槽合起来 `js/` 零 diff,全部改动落在 `tests/unit.js`(+88 行)、两份 README 的数字与索引行、三份新记账件。
   与之配套的是三个自称数 **493 / 493 / 496 全部低于基线的 501**——它们各自叉在更旧的点上,谁也不知道基线已经走到哪。
   合并后 live 是 **504**(501 + 1 + 1 + 1),三个自称数一个都不是答案。

2. **W142 残留 6「索引表行序仍无断言」本槽由 W144 收掉,而它收得比"从此以后"更宽:它回溯覆盖整张表。**
   本槽三次合并的索引表尾都是**并集按波次号归位**(人工排的第六次),但只有第三次合完之后才有判据。
   判据落地后在 tip 上一并复核了全部 158 行——变异把 `w130` 那行整行搬到表尾当场红并点名「`w144-index-order.md` 之后排了 `w130-cycle9-audit.md`」(4.4 的 M3),
   而**同一手在基线上 501/501 全绿**。故它不是"从下一行起管用",是"每次跑都把整张表重判一遍"。
   顺带的读数:**只删一行索引现在红 4 条**(此前是 3 条),多出来的那条是 W144 自带的取数口守卫。

3. **W142 残留 2 写的「散文那半 `git` 不提醒且没人接」本槽只对了一半,该按"同不同行"重述。**
   本槽有两处真正的散文改动(W141 往契约段插了条件块白名单那一层,W144 往同一段插了行序判据那一句),
   两处**都进了冲突块、`git` 都请我过目**,全槽**零处静默取对侧**。
   原因不是判据变多了,而是位置:那句散文与 `contract` 条数写在**同一行**,而那个条数**每槽必改**。
   故准确的形状是:**散文没有判据这半仍成立**(至今没有任何一条用例读那段话),
   **但"`git` 不提醒"只发生在散文与一个每槽必改的数字不同行的时候**——W138 那次改的是主 README 问题中心那一整段,那段里没有数字,所以静默;本槽两次都同行,所以都响。

## 2. 基线与这三个槽

### 2.1 基线

任务口径给定 `origin/cursor/w142-integration-4266` 的 HEAD,参考值 `d877051`。现取核实:

```
$ git rev-parse origin/cursor/w142-integration-4266
d877051...
$ git rev-parse origin/cursor/w140-gaps-keys-c2fe
9635416...
$ git rev-parse origin/cursor/w141-report-if-ef18
c07dcab...
$ git rev-parse origin/cursor/w144-index-order-eca5
9030fcb...
```

前三个与任务给的参考值一致;`w144` 任务口径只给了分支名没给 sha,现取它的实际 HEAD `9030fcb`。基线上的四套件数字(开工时现跑,不引用 `w142-integration-log.md` 里的数):

| 套件 | 基线 `d877051` |
|---|---|
| `node tests/unit.js` | **501 / 501**(`contract` **109 / 109**) |
| `node tests/integration.js` | **130 / 130** |
| `node tests/cli.smoke.js` | **100 / 102** |

与任务给的参考数字逐个相符。

### 2.2 三支的叉点:三条都是真三方,而且落后的程度不一样

| 支 | 与基线的 `merge-base` | 那是什么 | 基线在其之上还有 |
|---|---|---|---|
| `w140` | `42b2798` | W134 集成记账件 | 四个提交(合 W137、合 W138、W139 与 W142 两份记账件) |
| `w141` | `42b2798` | 同上 | 同上 |
| `w144` | `2851d97` | W139 集成记账件 | 三个提交(合 W137、合 W138、W142 记账件) |

三次都 `merge-base != HEAD`,**都是真三方合并**,三次都进冲突块。另外:

- `w140` 与 `w141` 彼此的 `merge-base` 也是 `42b2798`,即**它们互相看不见对方**,也都看不见基线上已合掉的 W137/W138;
- `w144` 叉得晚一些(它看得见 W135/W136,看不见 W137/W138);
- 三支两两之间的 `merge-base` 全部是 `42b2798`,故 `w144` 也看不见 `w140`/`w141`。

自称数与合并后 live 的关系:`w140` 自称 unit 493、`w141` 自称 493、`w144` 自称 496。合并后 live 是 **504**,**三个自称数都不是答案**,且都低于基线的 501。

### 2.3 不合的两条

远端另有两支在飞,按任务口径**不合**,现取核实它们确实在:

```
8c657bf  refs/heads/cursor/w143-evolve-headless-b087
c0ecdfb  refs/heads/cursor/w145-browser-gate-catch-5c11
```

本槽未取其内容、未做叉点核实,只登记"任务明确要求留着"。故本刻"未合入数"是 **2**,这个 2 是任务口径而不是"还没到"。

## 3. 冲突面

三次合并共 **9 个文件次、18 个冲突块**,每次都是同样的 3 文件 6 块,全部落在文档数字、索引表与两处 `FLOOR` 字面上。
**`js/` 一个文件都没进 diff**,三条新增的 `contract` 用例**各自零冲突块插入**(三处插入点在 `tests/unit.js` 里互不相邻)。

> 更正一处:三个合并提交的说明里都写了"3 文件 5 块",实数是 **6 块**(那条说明把两个 `FLOOR` 并成了一条列举)。以本节的表为准。

### 3.1 第一次合并(W140 `9635416` → `0050647`)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `README.md` | 契约段条数 | 109 / 108 | 现取 live **110** |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带 W135/W138 两处插入 / 对侧是叉点原句只换了数 | 纯插入,取我方整句、数字换 live **502** |
| `docs/skills-wave/README.md` | 明写份数 | 155 / 150 | 现取 live **156** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方 `w135`–`w139` + `w142` 六行 / 对侧 `w140` 一行 | **并集**,七行按波次号排 |
| `tests/unit.js` | 单元测试下限 | 501 / 493 | 现取 live **502** |
| `tests/unit.js` | 记账件 `FLOOR` | 155 / 150 | 现取 live **156** |

主 `README.md` 那一块按 W133 立的分辨法「差异里有没有删除」判:把对侧整句按字符序在叉点原句里走一遍,**零删除**,即它只是"叉点原句 + 换个数",
故取我方整句(W135 与 W138 那两处插入一个字不删)、只把数字换成 live。

### 3.2 第二次合并(W141 `c07dcab` → `a7ab716`)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `README.md` | 契约段整句 | 我方只改了数 / **对侧零删除地插了一整段散文**并换了数 | 见下 |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带插入 / 对侧叉点原句只换数 | 取我方整句、数字换 live **503** |
| `docs/skills-wave/README.md` | 明写份数 | 156 / 150 | 现取 live **157** |
| `docs/skills-wave/README.md` | 索引表尾 | 七行 / `w141` 一行 | **并集**,八行按波次号排 |
| `tests/unit.js` | 单元测试下限 | 502 / 493 | 现取 live **503** |
| `tests/unit.js` | 记账件 `FLOOR` | 156 / 150 | 现取 live **157** |

契约段那一块**不是数字二选一**:机器比对下来对侧那句是叉点原句加上 457 字的插入(条件块白名单那一层的散文),
按字符序把叉点原句在对侧那句里走一遍**零删除**,而我方那侧与叉点原句**逐字相等、只差那个数**。
故处置是**取对侧整句**(散文一字不删)、数字换 live **111**——与 3.1 那处方向相反,因为这次带内容的那一侧是对侧。

### 3.3 第三次合并(W144 `9030fcb` → `065ec2e`)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `README.md` | 契约段整句 | **两侧各插了一段散文,都零删除** | 见下 |
| `README.md` | 「单元测试(N 项断言」整句 | 我方带插入 / 对侧叉点原句只换数 | 取我方整句、数字换 live **504** |
| `docs/skills-wave/README.md` | 明写份数 | 157 / 153 | 现取 live **158** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方 `w140`/`w141`/`w142` 三行 / 对侧 `w144` 一行 | **并集**,四行按波次号排 |
| `tests/unit.js` | 单元测试下限 | 503 / 496 | 现取 live **504** |
| `tests/unit.js` | 记账件 `FLOOR` | 157 / 153 | 现取 live **158** |

契约段那一块是**本线第一次两侧都是纯插入**:我方那一侧带着 W141 刚合进来的条件块白名单散文,对侧那一侧带着行序判据那一句,
两者相对同一个叉点(`2851d97`)各自零删除。二选一会丢掉一整段,故处置是**并集**——
按对侧那段插入在叉点原句里的锚点(「三处一起改小则红在下限上。**」之后、「三个套件的用例数同样只增不减**」之前)把它插回我方那句里,
两段散文都在,数字换 live **112**。核实方式:改完后把两侧整句各按字符序在结果里走一遍,双向零删除即两侧一个字没丢。

### 3.4 三次都没有改到任何一条别人的判据

W142 那槽有一处"代码合得上、判据合不上"(W135 的 `assertEq` 与 W137 的计数撞车),本槽**零处**:
三条槽各加一条新用例、改的是各自的下限字面,既有用例一条未动(5.3 的名集比对里"只在基线里有的 0 条"同时也说明没有改名)。
三次合并全程未用 `checkout --ours/--theirs`,也未用 `git checkout <old> -- .`。

## 4. 数字怎么定的

### 4.1 一律现取合并后 live,不抄任何一个父值

| 数 | 基线 | W140 侧 | W141 侧 | W144 侧 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|---|
| 单元用例数 | 501 | 493 | 493 | 496 | **504** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 109 | 108 | 108 | 109 | **112** | 主 `README.md` 契约段 |
| 记账件份数 | 155 | 150 | 150 | 153 | **158**(含本文后 159) | `docs/skills-wave/README.md` 明写 |
| 记账件 `FLOOR` | 155 | 150 | 150 | 153 | **158**(含本文后 159) | `tests/unit.js` |
| `integration` | 130 | 130 | 130 | 130 | **130** | 未动 |
| `cli.smoke` | 102 | 102 | 102 | 102 | **102**(实跑 100/102) | 未动 |

逐次落定的读数(每次合完当场跑,不等三次合完再算):

| | 合入 W140 后 | 合入 W141 后 | 合入 W144 后 |
|---|---|---|---|
| `unit` | 502 / 502 | 503 / 503 | **504 / 504** |
| `contract` | 110 / 110 | 111 / 111 | **112 / 112** |
| 记账件份数 | 156 | 157 | **158** |

`contract` 那格值得单记一句:`w144` 那侧写的 109 与**基线的 109 逐字相同**,即 W142 踩过三次的那个"两侧同值"窗口本槽第四次出现。
但这次它进了冲突块——因为同一行上的散文两侧都改了,`git` 由此请我过目(见 1.3)。

### 4.2 `FLOOR` 抄父值:两格各放过一个、各逮住两个

在合并后 HEAD 上把两个 `FLOOR` 逐格改小重跑全套(`SLACK` 现取仍是 3):

| 格 | 值 | 与 live 的差 | 全套结果 |
|---|---|---|---|
| 单元测试(live 504) | 503 / 502 | 1 / 2 | 全绿 |
| 单元测试 | **501**(基线父值) | **3**(恰等于 SLACK) | **绿** |
| 单元测试 | 500 / 499 | 4 / 5 | **各红 1 条**(差额上限) |
| 单元测试 | **496**(W144 父值) | **8** | **红 1 条** |
| 单元测试 | **493**(W140 / W141 父值) | **11** | **红 1 条** |
| 记账件份数(live 158) | 157 / 156 | 1 / 2 | 全绿 |
| 记账件份数 | **155**(基线父值) | **3**(恰等于 SLACK) | **绿** |
| 记账件份数 | 154 | 4 | **红 1 条** |
| 记账件份数 | **153**(W144 父值) | **5** | **红 1 条** |
| 记账件份数 | **150**(W140 / W141 父值) | **8** | **红 1 条** |

即:**两格的形状完全一样**——三个被合入支的父值全部逮住,而**基线自己那个父值恰好压在 SLACK 边界上(差 3)被放过**。
五槽连起来读:W133 差 1(全放过)、W134 差 2 / 差 4(放过一、逮住一)、W139 差 1 / 差 3(全放过,一侧压线)、
W142 差 2 / 差 4–6(单元三个父值全逮住)、**W146 差 3 压线放过 + 差 5–11 全逮住**。
判据没变,本槽给出的新读数是:**"抄哪一个父值"现在有了稳定的分层**——抄叉得早的支必被逮住,抄基线自己那个则取决于本槽加了几条,
本槽一次只加 3 条用例、3 份记账件,恰好把基线那个父值顶在边界上。

### 4.3 README 那三处抄父值必被逮住(逐处实测)

同样在合并后 HEAD 上试抄:

- 主 `README.md` 单元数改成 501 / 496 / 493 → **各红 2 条**(精确对账那条报「实测 504,文档 N」+ 三套件下限那条报「README 写的 N 低于下限」,报错句不同、含义不混);
- 主 `README.md` 契约段条数改成 109(基线与 W144 同值的那个)/ 108 → **各红 1 条**;
- `docs/skills-wave/README.md` 明写份数改成 155 / 153 / 150 → **各红 1 条**(三方对齐那条)。

### 4.4 索引表:并集与行序两向都实测

| # | 变异 | 期望 | 实测 |
|---|---|---|---|
| M1 | 只摘 `w140` 那行索引(文件与份数都不动) | 红 | **红 4 条**:双向对齐、按份点名(点名 `w140-gaps-keys.md`)、行数三方对齐,**外加 W144 自带的取数口守卫** |
| M2 | 只摘 `w141` / `w144` 那行 | 同上 | 各红 4 条,分别点名各自的文件 |
| M3 | 把 `w130` 那行整行搬到表尾 | 红 | 红 1,报「`w144-index-order.md` 之后排了 `w130-cycle9-audit.md`」;**同一手在基线上 501/501 全绿** |
| M4 | 把本槽新插的 `w140` 那行改成追加到表尾(即"并集不归位"这条真实歧路) | 红 | 红 1,报「索引表应按波次号递增排列(新记账件插回它那个波次的位置,别追加到表尾;合并取并集时同样按号归位)」 |

M1/M2 那多出来的第 4 条是本槽新增的读数:此前摘一行索引红 3 条,W144 合进来后变 4 条,因为它的取数口守卫要求"索引表里取到的 wNN 行数 == 目录实况",摘行时那一句先红。

## 5. 取证

### 5.1 合并纪律:按四棵树的文件哈希机检

对每次合并,取 `M`(合并结果)、`P1`(我方)、`P2`(对侧)、`B`(叉点)四棵树里同一文件的 blob 哈希做对照。三个冲突文件在三次合并里**全部四者互不相等**:

| 合并 | 文件 | `M` | `P1` | `P2` | `B` | 结论 |
|---|---|---|---|---|---|---|
| `0050647` | `README.md` | `97484a5` | `ca0fc2d` | `9978898` | `45b06db` | 真融合 |
| `0050647` | `docs/skills-wave/README.md` | `1251a8e` | `fc145f3` | `0c76b14` | `f447fb0` | 真融合 |
| `0050647` | `tests/unit.js` | `cc8d8fa` | `89373ee` | `9435998` | `afd9b68` | 真融合 |
| `a7ab716` | `README.md` | `48d89f6` | `97484a5` | `df14748` | `45b06db` | 真融合 |
| `a7ab716` | `docs/skills-wave/README.md` | `9a3c14d` | `1251a8e` | `b0522da` | `f447fb0` | 真融合 |
| `a7ab716` | `tests/unit.js` | `f22ca2c` | `cc8d8fa` | `0ffd1e1` | `afd9b68` | 真融合 |
| `065ec2e` | `README.md` | `407ff11` | `48d89f6` | `f9f013c` | `71ca689` | 真融合 |
| `065ec2e` | `docs/skills-wave/README.md` | `3395b63` | `9a3c14d` | `209f3bb` | `33a3830` | 真融合 |
| `065ec2e` | `tests/unit.js` | `c536ca3` | `f22ca2c` | `b954fa0` | `44b0921` | 真融合 |

**整份取 `--ours` 零命中、整份取 `--theirs` 零命中、回退到叉点内容零命中。** 三份新记账件是纯新增文件,结果等于对侧,是单边改动的必然而不是取整份。

### 5.2 四套件实跑

| 套件 | 基线 `d877051` | 合入 W140 后 | 合入 W141 后 | 合入 W144 后(本槽 tip) |
|---|---|---|---|---|
| `node tests/unit.js` | 501 / 501 | 502 / 502 | 503 / 503 | **504 / 504** |
| └ `contract` 子套件 | 109 / 109 | 110 / 110 | 111 / 111 | **112 / 112** |
| `node tests/integration.js` | 130 / 130 | — | — | **130 / 130** |
| `node tests/cli.smoke.js` | 100 / 102 | — | — | **100 / 102** |

`cli.smoke` 那 2 项失败逐条现取,并在 `origin/master` 的独立 worktree 上同跑一遍对照,**同名同表现**:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

(`master` 那份的总数是 51/53——它还是二十三轮那个版本,套件本身小得多;比的是这两条的名字与表现,不是比总数。)

单元用例的**不同名字数**现取 504,与用例数 504 相等——**连续第十二槽全部相等**。

### 5.3 没删测:按名成集双向比对

在基线与合并后 HEAD 上各开 worktree 跑一遍全套,把用例名(含 `套件 · 用例名` 前缀)成集比对:

- **只在基线里有的:0 条**(即一条用例都没被删掉、没被改名);
- 只在合并后 HEAD 里有的:**恰好 3 条**,逐条点名:
  - `contract · skill 索引缺口键集:gaps() 20 键逐个点名并与两份缺口图谱双向对账(摘键/改名先红在本条)`(W140)
  - `contract · 集成/冒烟 report(...) 不许落在会跳过的条件块里(块链只许 main 函数体与裸 { } 分节块)`(W141)
  - `contract · docs/skills-wave 索引行序:索引表按波次号递增排列(同号内不限先后;整行挪位不再只靠人眼看住)`(W144)

501 + 3 = 504,与实跑总数对得上。

### 5.4 没摘 gaps、零产品代码:两处零 diff

```
$ git diff --name-only d877051 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js
(空)
$ git diff --stat d877051 HEAD -- js/ server.js cli.js mcp.js
(空)
```

故七个数不用现跑也成立;为求稳仍在合并后 HEAD 上现取核对了一遍:
提示词注册表 **41**、能力短名单 **30**、`CHECKS` **17**、`preflightStages()` **7**、`gaps()` **20 键**(`G-03` 仍是 `review.stage` / `review.reviseLoop` 两条)、`KB.SECTIONS` **18**、`playbooks()` **5**。

整槽相对基线的全部 diff:

```
 README.md                            |   4 +-
 docs/skills-wave/README.md           |   5 +-
 docs/skills-wave/w140-gaps-keys.md   | 138 +++++++++++
 docs/skills-wave/w141-report-if.md   | 163 +++++++++++++
 docs/skills-wave/w144-index-order.md | 107 +++++++++
 tests/unit.js                        |  88 ++++++-
```

### 5.5 抽查变异:任务点名的三组

全部在**合并后 HEAD** 上跑,一次只改一处,跑完即还原(还原后复跑 504/504 全绿、工作区 clean);
每组都同时在**基线 worktree** 上跑一遍同样的改动作对照,以证明红的是本槽新合进来的判据而不是既有的。

| # | 变异 | 合并后 HEAD | 基线 `d877051` |
|---|---|---|---|
| M5 | `js/skills.js` 里把 `gaps: ['S-06']` 改名成 `['S-99']`(**任务点名**) | **红 1**,报「gaps() 键集应逐字对齐…期望 `…,S-05,S-06,S-07`,实际 `…,S-05,S-07,S-99`」——两串都印出来 | `unit` **501/501**、`integration` **130/130** 全绿 |
| M6 | 把 `tests/integration.js` 里一条 `report(...)` 原样搬进 `if (process.env.MV_NEVER_SET) { … }` 的花括号里(**任务点名**) | **红 1**,报「集成测试:report(...) 的块链上只许有 main 函数体与裸 `{ }` 分节块…实际 `tests/integration.js:145 外层「if (process.env.MV_NEVER_SET) {」`」 | `unit` **501/501 全绿**,而 `integration` 静静掉到 **129/129**、README 仍写着 130 —— 这正是 W141 收的那个洞 |
| M7 | 把 `w130` 那行索引整行搬到表尾(**任务点名**) | **红 1**,点名「`w144-index-order.md` 之后排了 `w130-cycle9-audit.md`」 | **501/501 全绿** |

另有 4.2 / 4.3 / 4.4 三节的抄父值与索引变异共 **19 格**,逐格结果已列在各节表里。

### 5.6 反向抽查:任务点名要保的五样,逐样变异一次

| 要保的 | 反向变异 | 实测 |
|---|---|---|
| `Domain.gateBlockers` + `Issues.gates`(W138) | 门槛投影表摘掉 `no-script` 一条 | **红 3 条**,含双向码集那条的正向与 G2 那条 |
| 同上(危险级不许抬) | `no-script` 危险级 low → mid | **红 12 条**,含「三条门槛态一律低危」与四条"提醒不得产出高/中危、门禁状态不变" |
| `release-core` G4–G6 `aggErr`(W137) | 整段退回 `catch (_) {}` 形态 | **红 2 条**,分别报「`g4-stale` 判不出来不得报 pass」与「遍历抛错时按判不出来记 warn」 |
| `release-core` G1 回执(W135) | blocker 键名 `label` 退回 `action` | **红 1 条**,报「每集按『集名(状态:推荐动作)』点名」 |
| `Domain.reviseTargets` 派生(W131) | CLI 闭环退回摘回执 `lowShots` | **红 1 条**,报「重抽面单源:CLI 不摘回执 lowShots 当 shotIds」 |
| 同上 | `Domain.reviseShotIds` 改回调用侧自筛 `score < 7` | **红 1 条**,报「reviseShotIds 是重抽面 ∩ 分镜表实位」 |
| `FLOOR` 的 SLACK | 字面现取仍是 **3**(`tests/unit.js` 里 `const SLACK = 3;`),4.2 的报错句里逐条印着"上限 3 格" | 在 tip 上成立 |
| SK-25 护栏(W136) | 把「仍欠(G-03)」三行整段抹掉 | **红 1 条**,报「SK-25 的 note 须仍有『仍欠(G-03)』段」 |

五样在本槽 tip 上**都还在、都还有判据护着**。

### 5.7 本槽自己改了什么

除三个合并提交解冲突外,本文这一提交动了三处:新增本文、`docs/skills-wave/README.md` 补索引行并把明写份数 158 → 159 与最新收敛记录指针改到本文、`tests/unit.js` 的记账件 `FLOOR` 158 → 159。
单元用例数与 `contract` 条数**本文一条不加**,故 504 / 112 不动。
三个合并提交里**没有删过任何一条用例、没有放宽过任何一条断言、没有改到任何一条别人的判据**(3.4 与 5.3)。

## 6. 残留与欠账

1. **W142 残留 6 已由 W144 收掉,但它不管同号内的先后。** 同一个波次号下多份记账件对调两行仍全绿(W144 自己如实登记为不判的那一面)。
   本槽的索引表里没有同号多份的情形,故这一面本槽没有实测机会。
2. **散文那半仍没有判据,但"`git` 不提醒"这句要按同不同行来说。** 见 1.3 与 3.2/3.3:
   本槽两处散文改动都与 `contract` 条数同行,故两次都进冲突块。W138 那次静默,是因为它改的那段里没有每槽必改的数字。
   **至今仍没有任何一条用例读那段散文**——响不响取决于位置,对不对仍旧无人接。
3. **`FLOOR` 抄父值的分层。** 4.2 已量:抄被合入支的父值(差 5–11)必被逮住,抄基线自己那个(差 3)恰好压在 SLACK 边界上被放过。
   这与 W142 记的"并槽越大越管用"是同一件事的另一面:**并槽的体量决定的是"抄基线那个"会不会响**,抄叉得早的支那几个从来都响。
4. **W141 的白名单只管 `report(...)` 的块链。** 它自己交接的那一路仍开着:`main` 里提前 `return`/`throw` 会让后面所有登记点一条不跑,而块链上一层条件块都没有。本槽未动。
5. **W140 留了一个有意的人工出口。** 在闭合名单 `off` 里给某个编号写上理由、同轮再改另两处字面,那条键集用例就转绿(它自己的 M7 如实登记过)。本槽未动,也未收窄。
6. **W142 残留 1(那句 `assertEq` 与 W137 共享 `fails` 计数器)本槽未动**,耦合照旧:失败含义不混、失败条数会串。
7. **W142 残留 4(G1 与 G4–G6 是两套写法)本槽未动**,理由同上槽:不重做已落地的功能。
8. **SK-25 的两个余面仍欠**(收敛次数 `maxRetry` 的登记口径、两端闭环形态不同构)。W136 之后无人动,护栏实测仍红得出来(5.6)。
9. **远端两支在飞未合**:`w143`(evolve headless)`@8c657bf`、`w145`(browser gate catch)`@c0ecdfb`,按任务口径不合。
   其中 `w145` 收的是浏览器那一侧的门禁空 `catch`,与本线已合的 W137(headless 那半)是同一件事的另一端,下一槽合它时**大概率与 `js/release.js` 撞不上、而与 W137 落地的那两条用例撞得上**,值得先看一眼再动手。
