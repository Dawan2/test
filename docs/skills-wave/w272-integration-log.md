# W272 集成日志:W271 分镜面十二处持久 chrome 镜号并进 W270 尖(集成线上第六条「快进形」)

我方尖:`origin/cursor/w270-integration-af81` **现取** `b1202bf`(先 `fetch` 再 `rev-parse`)。
对侧:`origin/cursor/w271-sbshotno-7c4e` **现取** `3634e57`。两个自称与现取恰好相同,但**都以 `rev-parse` 为准**。
新支 `cursor/w272-integration-3fe8` 从 w270 现取 HEAD 拉出,`checkout -b` 后立刻 `rev-parse` 与 `b1202bf` 逐字节相同。

**结论先写:一次 `--no-ff`,零冲突。合完 live 跑出 `unit` 691 / `contract` 151 / `skills` 103 /
`integration` 152 / `cli.smoke` 115-117,`gaps()` 20 键一个没剥,W271 六条残留一条没代修。
本槽是集成线上第六条快进形,`tree(M)` 与 `tree(P2)` 逐字节相同、`P1→M` 与 `B→P2` 的 numstat 逐行相同——
这两格照旧**恒真**,一格结论都不承载,故本文的证据全在变异与 live 上。**

## 一、拓扑(现取,不抄自称)

| 位 | 现取 SHA |
|---|---|
| `B` 合并基(`merge-base P1 P2`) | `b1202bf872c5d6f6e3b490624517fd7dfa339f69` |
| `P1` 我方尖(w270) | `b1202bf872c5d6f6e3b490624517fd7dfa339f69` |
| `P2` 对侧尖(w271) | `3634e575105cb283de20e1d9e2ed47a466442997` |
| `M` merge commit | `4dad123e972d3b52b93f98ccb51f06024d3cef19` |

`git merge-base --is-ancestor P1 P2` 成立,`B` 与 `P1` **提交级相等**,即典型「快进形」——
本可 `--ff` 掉,仍按纪律走 `--no-ff` 留住合点。`P1..P2` 四条提交
(`d756392` 产品 / `bc54bbb` 加测 / `235c100` 判词收紧 / `3634e57` 记账),
「自称尖是不是链尾」这一格现取核过:`3634e57` 就是链尾。

树面:`tree(P1)` `8ab7da2`、`tree(P2)` = `tree(M)` = `b612186`(逐字节相同);
`P1→M` 与 `B→P2` 的 `--numstat` 逐行相同(`diff` 无输出)。
**这两格在快进形下是恒真的**,写在这儿只是留档,不当证据用——
下面第三、四节那些"变异是否红、报错句是不是那一句"才是本槽真正的取证面。

## 二、并集面(README / GUARD / roster / 索引)

快进形下 `P2` 的文档本就含着 `P1` 的全部内容,故并集不靠手工拼:
`docs/skills-wave/README.md` 索引表里 w270 那行与 w271 那行**同时在册**,现取核过。
数字一律 live,**W270 自称的 688 一个字没抄**:

| 面 | `P1` 基线现取 | `M` 合完现取 | 差 |
|---|---|---|---|
| `unit` | 688 | **691** | +3(全部来自对侧) |
| `contract` | 150 | **151** | +1 |
| `skills` | 101 | **103** | +2 |
| `integration` | 152 | **152** | 0 |
| `cli.smoke` | 115/117 | **115/117** | 0 |
| 记账件 | 285 | **287**(w271 一份 + 本文) | +2 |

GUARD / roster 现取四者对齐、本槽与对侧都没登记新护栏主题:
`GUARD_TOPICS` **19** / `GUARD_TOPICS_CLOSED` **0**(空表,那份 `id: '主题编号'` 只是形状注释)/
`TOPIC_FLOOR` **19** / 花名册(`w178-topic-floor-unlist.md` 的花名册节)**19**。
`gaps()` 键集现取 **20** 键(`G-01…G-05`、`G-07…G-15`、`S-01`、`S-03…S-07`),**一个没剥**。

棘轮与差额:`['单元测试', 691]`(对侧已抬)、`['集成测试', 152]`、`['CLI 冒烟', 117]`、
`TOPIC_FLOOR` 19 均未动;记账件 `const FLOOR` 286 → **287**(本文一份)。
五格差额现取全为 0,`SLACK` **3** 一格没用掉。

## 三、快进形的空集不能当没丢东西

`git diff P2 M` 是空集——这在快进形下是**定义使然**,它证明的只是"合入没引入新东西",
证不了"对侧那 12 处语义在合完的树上还活着"。故本槽的取证换成两类:**变异**与 **live**。

### 十二处逐处变异(方向一:持久 chrome 退回读 `order`)

逐处单独改、跑完还原,`contract` 与 `skills` 两族分开记:

| 处 | 文件 | `contract` | `skills` |
|---|---|---|---|
| 引用资产页头 | `js/sb-views.js:894` | 红 1 | 红 1 |
| 分镜提示词页头 | `:938` | 红 1 | 红 1 |
| 历史提示词页头 | `:1027` | 红 1 | 红 1 |
| 按指令改页头 | `:1096` | 红 1 | 0 |
| 存为主体形态页头 | `:1152` | 红 1 | 红 1 |
| 存入资产库页头 | `:1190` | 红 1 | 红 1 |
| 更多工具页头 | `:1243` | 红 1 | 红 1 |
| 历史版本页头 | `js/sb-gen.js:25` | 红 1 | 红 1 |
| 首帧海选页头 | `:263` | 红 1 | 红 1 |
| 批量失败清单行 | `:807` | 红 1 | 红 1 |
| 待确认清单行(两份同字面,一起改) | `js/sb-batch.js:53` `:154` | 红 1 | 红 1 |

十二处**无一漏网**。「按指令改页头」只有源级那一层护着(行为面没有真跑这一屏),
这一格如实记进残留(§六 7),不当它两层都闭合。
报错句逐条把犯规那一行**原样抄出来**,例:
`更多工具弹窗页头的镜号应取 Domain.shotNo(表内实位):title: '更多工具(镜头 ' + (s.order + 1) + ')'`;
行为面那半报的是真跑产物,例:`期望 "更多工具(镜头 3)",实际 "更多工具(镜头 1)"`。

### 边界四处变异(方向二:落库文本 / 素材内容顺手改成实位号)

| 处 | 是什么 | 红 |
|---|---|---|
| 入库默认主体名(`js/sb-views.js`) | 落库文本(进 `p.subjects`) | `contract` 1 + `skills` 1 |
| 便捷工具预填名(`js/sb-views.js`) | 落库文本(带进任务名) | `contract` 1 |
| 首尾帧占位图标注(`js/sb-gen.js`) | 素材内容(图一生成就定死) | `contract` 1 |
| `opIds` 镜头标签(`js/sb-batch.js`) | 任务留痕 | `contract` 1 |

四处全红:**「落库/素材仍读 `order`」这条边界在合完的树上仍在**,顺手多收一处当场点名。
入库默认主体名那处红在两族上,且行为面同一条用例里页头报 3 而默认名报 1,两句断言互不代偿。

## 四、本槽真正新增的两格

前五条快进形各自补过一格(W268 依赖方向、W270 复合判据可分性)。本槽补的两格如下。

### M2 复量:两份待确认清单行的覆盖**不对等**,合完仍不对等

W271 §五 M2 自称"只收一处红 1 不红 2",这是它自己记成残留的一格。合入最容易吞掉的正是这种
**"半覆盖"**——源级判据是全的、行为面只护住一半,合完跑全绿看不出来。故本槽对着两份分别定点变异:

| 只退哪一份 | `contract` | `skills` | 读法 |
|---|---|---|---|
| `js/sb-batch.js:154`(`runBatchOp` 出片前置) | 红 1 | **0** | 只有源级锚护着 |
| `js/sb-batch.js:53`(`openConfirmGateModal` 确认闸) | 红 1 | **红 1** | 源级 + 行为面两层 |

两行**在同一个文件里、字面逐字相同、源级判据同一句**,而行为面一份护得住一份护不住——
把它俩摆成对照跑,是"半覆盖"这件事唯一看得见的形状。`:53` 那份行为面报的是
`待确认清单行按实位出(读 order 字段时是 6,10,1):期望 "1,2,3",实际 "6,10,1"`,
`:154` 那份连一句都报不出。与 W271 自称逐字一致,**残留 3 仍开着**(§六 3)。

### M7 跨支反向手:这次读成「分家」,且**红的那两条报错句与对侧自称逐字相同**

退掉 W270 那半(`js/review.js:731` 审片记录列表标题退回 `s.order + 1`):

- `contract` 红 1:`审片记录列表的镜号应取 Domain.shotNo(表内实位)`;
- `skills` 红 1:`记录列表标题的镜号应报第三镜:期望 "审片记录 · 镜头 3(1)",实际 "审片记录 · 镜头 1(1)"`;
- **W271 本槽那三条一条都没红。**

两句报错与 W271 §五 M7 那一格**逐字相同**——这才是"我方那半没被对侧的改动挤走或改判"的证据,
关键词点数只量得出"引用还在不在",量不出"那些引用的行为判据还红不红"。
本槽结论方向是**分家**(对侧动的是另外三个文件),与 W270 那次读成「叠着」形状相同而方向相反;
判据形状没变,变的只是改动落点——这一格与 W271 §五自己的读法对上了。

### 名集多重集对账

按「用例名」逐条比 `B` 与 `M`:**净增 +3、净减 −0**,`688 + 3 − 0 = 691` 与合完实况自洽;
`M` 与 `P2` 名集逐条相等(快进形应然)。净增三条正是对侧加的那三条(源级一条 + 行为面两条)。

### 反事实

把 README 那几个数字抄回旧值,逐个当场红,且**报的话各不相同**:

| 改法 | 红 | 报的是 |
|---|---|---|
| 单元用例数 691 → 688 | **2** | 一条报"与实测不符(实测 691,文档 688)",另一条报"文档数字改小来迁就删测";两条含义不同 |
| `contract` 自报 151 → 150 | 1 | "contract 套件断言条数:与实测不符(实测 151,文档 150)" |
| 记账件明写 286 → 285 | 1 | "目录里的 wNN-*.md 份数应等于 README 明写的份数:期望 285,实际 286" |

**禁抄的 688 是双层红**,不是单点。

## 五、核过的事实(现取)

- **12 处走 `shotNo`**:`js/sb-views.js` 7 / `js/sb-gen.js` 3 / `js/sb-batch.js` 2(不含三处文件头注释),合计 **12**。
- **落库/素材仍读 `order` 的边界仍在**:三个文件里读 `order` 出号的行现取剩
  `sb-views` **6** / `sb-gen` **45** / `sb-batch` **18**,合计 **69**,与 W271 §二 那张表逐格对得上;
  余下这些逐行落在下载名、`U.toast`/`U.confirm`、进度坞 `dock.say`/`upd()`、
  `Tasks.start({ target })`/`U.charge`/`U.refund`/`Tasks.done({ filename })`/`U.uploadData`/`opIds`
  与 `PH.image`/`PH.gridCells` 上,一处都不是弹窗页头或清单行标签。
- **三棵树关键词点数**:`landed` 99、`shots-dedupe` 41、`dupRowsNote` 30、`stateDigest` 26、
  `reviewRows` 18、`mergeReviewPerShot` 15、`nthShot` 12、`nthOf` 9、`pickerNarrowHits` 9、
  `floorLagVerdict` 7、`lastRep` 4、`openReviewSummary` 4、`rowIndexOf` 23、`state-put` 18、
  `gaps()` 40、`GUARD_TOPICS` 25、`TOPIC_FLOOR` 9、`runBatchOp` 10 —— 三树**逐格相等**;
  变的两格 `shotNo` 39→59、`openConfirmGateModal` 8→10 **全是对侧净增**,无一格 `M < P1`。
- **`node --check`** 过:`js/sb-views.js`、`js/sb-gen.js`、`js/sb-batch.js`、`tests/unit.js`、
  `js/domain.js`、`server.js`、`cli.js`。
- **`cli.smoke` 单独整跑**、`env -u HUJING_SERVER -u MV_SERVER -u MV_TOKEN -u MV_BASE`:**115/117**,
  两条失败 `未登录 whoami → exit 3` 与 `llm --json mock 链路`,与 `master`(`9adcf0f`)同 env 下
  现取 51/53 **同名同表现**,按明令允许。
- **不跑 `tests/e2e.js`**(按明令)。
- 计费一处未触(12 处都是纯展示串);`landed` 未动、`state-put` 没加闸、SK-04 未动。

## 六、残留(按明令一条没代修,W271 原话逐条保留)

W271 §七 六条原话照录,一条未动、一条未改判:

1. > **分镜面「展示·瞬时」那 38 行仍读 `order`**(本槽有意整族留下):`js/sb-gen.js` 21 行
   > (批量循环的 `dock.say` / `upd()` 进度行、内容安全拦截与断点续查等 toast、`U.runTask` 的 `steps.label`)、
   > `js/sb-batch.js` 13 行(配音与混流两条批量的进度行与失败 toast)、`js/sb-views.js` 4 行
   > (`downloadShot` 三个浏览器下载名 + 「按新提示词重新生成」那句 `U.confirm`)。
   > 另有 `js/sb-views.js` 4 处 toast 已是 `ep.shots.indexOf(s) + 1`——号是对的,但那是**展示层第二份对位**,
   > 同族收编时一起换成 `Domain.shotNo`。收这一族要先定一句:进度坞行与它同行的扣费摘要
   > (`js/sb-gen.js:696`/`:697` 那种)按什么切开。判据这一侧已把"持久 chrome"钉死,
   > 收瞬时族时须同轮把 §四 那道 `title:` / `class="tag cyan"` 护栏扩成新的边界句。

2. > **生成面(`js/produce.js:276`)与问题中心(`js/issues.js` 2 处)仍读 `order`**(W267 §六 4 起在册):
   > 本槽一处未扩。问题中心那两处进的是 `detail` 文案(与命令回执同源,编排层也读),
   > `js/produce.js` 那处是量产跑批的进度行——两者都不是"屏幕上给人看"这一件事,口径要单独定。

3. > **两份待确认清单行只有一份有行为面判据**(本槽 §五 M2 量出来的):
   > `js/sb-batch.js:53`(确认闸)由 `SB.openConfirmGateModal` 真跑覆盖,
   > `:154`(`runBatchOp` 里的出片前置检查)只有源级那一句锚着——把它退回读 `order`,行为面一条不红。
   > 要补得让 `runBatchOp` 在单测沙箱里跑到那一屏(它前面压着 `HumanReview.guard`、成本预估与
   > `Domain.voiceCfgOf` 几层),本槽没做,如实记成一格。

   本槽 §四 定点复量过,现取仍如原话:`:154` 源级红 1、行为面 0。

4. > **`js/skills.js` 十余处 `(+s.order || 0) + 1` 与 `js/wf-core.js` / `js/sb-llm.js` 里的镜号**:
   > 回执与提示词口径(LLM 读的那份),按明令未碰。

5. > **审片面任务名与扣费摘要那 4 行**(W269 §六 1 起在册):`js/review.js` 未碰,判据照旧钉着。

6. > 浏览器闭环 `common` / `cut` 空壳、`openEpisodeReview` 整表与 generating 过滤
   > (W261 §六 2/3 起在册)、**同 id 多行源头**(W226 起在册)、**库里 `order` 字段本身会漂**:一律未碰。

本槽现取新记一格(不是代修,是把变异量出来的实况记上):

7. **「按指令改」页头(`js/sb-views.js:1096`)只有源级判据护着**:方向一逐处变异里,
   十二处只有这一处退回读 `order` 时行为面一条不红(其余十一处两族都红)。
   与残留 3 是同一类"半覆盖",但落点不同(那一格是清单行两份里的一份,这一格是七处页头里的一处);
   行为面那条用例真跑的是 `openPromptPanel` / `openPromptHistory` / `openAssetPicker` / `openMoreTools`
   四扇门,`openImgEdit` 那一屏没进去。要补得把它也拉进同一个沙箱,本槽没做,如实记成一格。

## 七、交接

我方尖:见本支 `git rev-parse HEAD`(现取,不在此写死自称)。
merge parents `b1202bf` + `3634e57`,merge commit `4dad123`,零冲突。
动过的文件(合入本身之外):`docs/skills-wave/README.md`(明写份数 286→287 + 索引补本文一行)、
`tests/unit.js`(记账件 `FLOOR` 286→287,只此一处字面)与本文。
**产品语义一字未改**:`js/` 下逐字节与 `P2` 相同,W271 那 12 处原样。
未开 PR、未合 master、未开第三条功能支。
