# W96 · 周期 7 独立核验

> 核验槽独立作业:**只读代码、只写本文件**。全部测试数字由本槽在各支独立 `git worktree` 上重新跑出,
> 不引用被核分支文档里的数字;引文与自测不一致处逐条标注。本轮不开 PR、不合并、不改任何业务代码。
> 成熟度沿用 `w5-cycle1-audit.md` 第 1 节的 M0–M4 标尺(M3 = 代码+断言、分支内自洽全绿;**M4 = 已合入 `master`**)。
> 本轮任务口径给定的核验面是**集成线 `w80 → w85 → w90`** 与**旁支 W88–W95**;
> W62–W79 只作基线与因果链取证,不逐支评分(那一段的逐支评分留在各自记账件与 `w63-cycle6-audit.md` 的延长线上)。
> **W93 / W95 远端零引用**(`git branch -r` 无 `w93-*` / `w95-*`),其状态**未知**,本报告不评分、不推测。

## 0. 核验时点与锚定 SHA

| 分支 | head | 相对 `master` | `master` 是严格祖先 | 角色 |
|---|---|---|---|---|
| `master` | `9adcf0ff` | —(全仓 8 提交) | — | 主干 |
| `cursor/w70-integration-ad31` | `87aa62a5` | 领先 280 | 是 | 集成线(**曾脱线,W80 补合**,见 3.2) |
| `cursor/w72-integration-8d3f` | `d2e7c43d` | 领先 288 | 是 | 集成线(两线合一) |
| `cursor/w75-integration-c4a7` | `fbefd0c2` | 领先 295 | 是 | 集成线 |
| `cursor/w80-integration-5369` | **`4c45f89d`** | 领先 **312** | 是 | **集成线(本轮起点)** |
| `cursor/w81-gsettings-prompt-9f2e` | `7222a4dd` | 领先 296 | 是 | 功能槽(已合) |
| `cursor/w82-proj-shell-prompt-6f73` | `0994fc41` | 领先 297 | 是 | 功能槽(已合) |
| `cursor/w83-proj-upload-prompt-00b3` | `819a90df` | 领先 296 | 是 | 功能槽(已合) |
| `cursor/w84-sb-views-prompt-2934` | `9b647b42` | 领先 297 | 是 | 功能槽(已合) |
| `cursor/w85-integration-171f` | **`2a05c725`** | 领先 **324** | 是 | **集成线** |
| `cursor/w86-agent-ops-prompt-8d6f` | `b02b86a9` | 领先 314 | 是 | 功能槽(已合) |
| `cursor/w87-role-editor-prompt-9c2a` | `fb74c31d` | 领先 314 | 是 | 功能槽(已合) |
| `cursor/w88-experts-forge-prompt-a3f7` | `f4525cec` | 领先 315 | 是 | **旁支(未合)** |
| `cursor/w89-plans-prompt-835b` | `77e0ff83` | 领先 314 | 是 | **旁支(未合)** |
| `cursor/w90-integration-9004` | **`35695c81`** | 领先 **336** | 是 | **集成线 tip** |
| `cursor/w91-intent-router-prompt-6ea6` | `80742f60` | 领先 326 | 是 | **旁支(未合)** |
| `cursor/w92-proj-planner-prompt-b845` | `3f68c876` | 领先 326 | 是 | **旁支(未合)** |
| `cursor/w94-persona-remainder-prompt-3f1f` | `77a99163` | 领先 325 | 是 | **旁支(未合,零代码改动)** |

十八支的 `git merge-base --is-ancestor master <branch>` **全部成立**,任一支单独并入主干都是零冲突快进。
核验时点远端共 **113 条分支**(不含 `master`;周期 6 末为 81 条,本周期新增 32 条)。核验全程各支 head 未移动。

### 0.1 W63 的两条重指控在本槽复核时**都已被修掉**——这是六份核验报告里第一次

先把这一条放在最前面,因为它决定了本报告后面所有条目的基调。

| W63(周期 6)的结论 | 本槽实测 | 谁修的、什么时候 |
|---|---|---|
| **阻塞 2**:`w53` head `0045962`(143 行记账件 + 索引行)不在集成线上;三处散文写「W53 没有留下自己的记账件」 | **已闭合**。`git merge-base --is-ancestor 0045962 origin/cursor/w90-integration-9004` 为**真**;`docs/skills-wave/w53-memall-headless-seed.md` 在 `w90` 上**存在**(11285 字节)、索引行在;三处散文**全部就地更正**并留了 `W67 更正` 标记 | W67 的 `6b14a0f`「补合 W53 …真正的 tip」+ `9058963`「更正『W53 没有留下自己的记账件』这个与实况相反的判断」 |
| **阻塞 5**:`core.memoryDual` 的 `note` 仍写「前段四步结论仍不回流」,而四步源级已全部回流(假欠账) | **已闭合**。本槽直读 `w90` 上 `core.memoryDual` 的仍欠段,已改写为真实余量:「生成与合成两步没有可判定的结构化结论可回流……浏览器剧本解析向导走自己的入库路径,提取主体的回流只挂在命令层」;`memFeedback`/`memWrite` 在四个消费点各 2 处,与 `note` 不再矛盾 | W68(其记账件标题即「W63 预判的假欠账为何不成立」) |

**而且不止是改了散文——反方向的断言真的补上了。** W63 药方 2③要的「除了查 `note` 点了名,再查那处余量是否真的还欠」,
本槽在 `w90` 的 `tests/unit.js` 里逐条读到并实测:

```
['js/experts.js', 'js/plans.js'].forEach(rel =>
  assert(/system: ['`]你是/.test(fs.readFileSync(path.join(ROOT, rel), 'utf8')),
    '仍欠段点名的 ' + rel + ' 此刻确实还有内联人设(收编后须同步改 SK-11 的仍欠段)'));
```

同形的翻面断言在 SK-10 / SK-11 上共有六对(`js/episodes.js` / `js/episode-util.js` / `js/agent-ops.js` /
`js/role-editor.js` 四处已收编的写成 `!owed.includes(...)` + 该文件内联计数归零,`js/experts.js` / `js/plans.js`
两处未收编的写成正向存在)。**本槽用变异 11、12 实跑验证了这套翻面确实会红**(第 6.2 节)。

**但同一条药方的另一半没做全,而本槽找到了它现在漏在哪里:仍欠段只点了 4 处余量里的 2 个文件。**
live 的 A 名单余量是 `js/agent-global.js:1, js/experts.js:2, js/plans.js:1, js/proj-planner.js:2`(4 文件 6 处),
而 SK-10 / SK-11 的仍欠段只点名 `js/experts.js` 与 `js/plans.js`。
**`js/agent-global.js` 与 `js/proj-planner.js` 那 3 处在任何一条 `note` 的仍欠段里都不出现。**
接住它们的是另一路断言(全仓持有者名单的期望串,见 5.3),不是记账散文——
**即「余量清单」现在有两个来源,而两个来源不一致。**

## 1. 一句话结论

**周期 7 是六个周期以来治理面唯一一次真正向前走的一轮:W63 点名的两条高危阻塞(丢记账件、假欠账)都被就地修掉,
而且是被下游的功能槽/集成槽自己发现并改的,不是等下一份核验报告去指;
但同一个「把时点结论写成终局措辞」的机制在本周期又发作了两次(W72 对 `w70`、W90 对 `w89`),
其中一次让一份记账件在线外挂了两个波次——治理债的偿还速度第一次超过了它的产生速度,但产生机制一次没被堵。**

先说做完的。W63 的阻塞 2 与阻塞 5 都已闭合(0.1),阻塞 3(索引契约只查一致性不查完备性)**被 W67 加固了一半**——
本槽实测新增了一条 `contract · docs/skills-wave 索引完备性` 断言,变异实测「删文件留索引行」与「删索引行留文件」**两向都红**,
比周期 6 那套只比集合相等的写法严了一档。

数字面这一轮**干净得反常**:`w75`→`w90` 十七支的 README `unit` / `integration` / `cli.smoke` / 提示词条数
**四个数字与本槽实跑逐个对上,零失真**——包括周期 6 里那两个**至今仍然没有任何断言**的
`integration` 与 `cli.smoke`(本槽变异 4b、5b 把它们分别改错,**两次都是 453/453 全绿**)。
周期 6 在 `w52`/`w55`/`w59` 三支上那个 −8 的冒烟数失真,本周期**一支未见**。
**这就是本轮最尖锐的一处对照:同一个洞一个字没堵,而掉进去的人从 3/14 变成了 0/17。**

集成面**13 次合并全部取被合入支的 head、全部是真三方合并(`--no-ff` 13/13)**,
用例增量相加与合并后 live **三个槽逐个相等**(428+0+1+2+2+2=435、435+2+1+2+3=443、443+2+3+2+3=453),
用例名集合双向对照**零条消失**。功能槽基线**13/13 落在集成线 head 上**,周期 5 那种「以另一个功能槽 head 为基线」
或「取非 head 中间点」的形态**一次未复发**。

**但本周期的病灶换了个位置:从功能槽的基线,搬到了集成槽对『远端现在有什么』的测量。**
两次实证:

1. **W72 把 `w70` 的 tip 记成 `1e4627c`,而 `w70` 的真 head 是 `87aa62a`**(记账件 + 索引行,commit 时点 22:10:18,
   比 W72 那次合并的 22:16:19 **早 6 分 12 秒**)。后果是 `w70-integration-log.md` 与它那行索引
   **在集成线外挂了两个波次**(W72、W75),直到 W80 的 `f7bb032` 才补合。
   **这是 W57 丢 `w53` 那件事的同形复发,只是这次在同一个周期内被自己捞回来了。**
2. **W90 把 `w89-*` 记成「现取远端零匹配、不存在」,把 `w88` 的 tip 记成 `78feca2`。**
   live:`w89-plans-prompt-835b` **存在**,head `77e0ff8`(00:11:38),首个提交 00:06:01;
   `w88` 的真 head 是 `f4525ce`(00:08:30),`78feca2` 是它的中间点。
   W90 的四次合并发生在 00:09:36–00:21:24,记账件写于 00:35:17——
   **`w89` 的两个提交都早于 W90 的第二次合并,`w88` 的 head 早于 W90 的第一次合并 1 分 06 秒。**
   **但这一处本槽判定为「记账失真、无实质损失」**:W88/W89 是**任务口径明令排除**的,不合是对的,
   丢的只是「剩余未合」那一栏的准确性——不像 W57/W72 那样把东西漏在了线外。

**本轮唯一一处新形态的发现,在用例名上。** 本槽实测 `w90` 跑 **453 条用例、只有 452 个不同的用例名**——
`w84` 与 `w87` 各自独立写了一条**同名且判据几乎逐字相同**的断言
(`注册表全仓持有者名单:每条 def 的字面持有者恰好只有 js/prompts.js`,分别在 4970 行与 5498 行,
唯一实质差别是 `list.length >= 26` 与 `>= 20`),W90 按并集解法把两条都留下了。
`w75` / `w80` / `w85` 三支的用例名**逐支唯一**(428/435/443 全不重复),**这是全仓第一对同名用例**。
它本身无害(两条都跑、都绿),但它**打掉了本目录从 W21 起一直在用的那件取证工具**:
「用例名集合比对证明没删测」在 `w85 → w90` 这一步会报**新增 9 条**,而真实增量是 **10 条**。
**W90 自己没被这个坑到**——它用的是单向 `comm -23`(十五次全空)加上增量相加对账,两种口径都不受重名影响。

能力面本周期仍是**零新校验项**:`CHECKS` 全程 **17 条**、`preflightStages()` 全程七面、`gaps()` 全程 **20 键**、
`KB.SECTIONS` 全程 18、`playbooks()` 全程 5、短名单 30 条全程 **0 pending**。
增量**全部落在提示词注册表**:`w75` **19 键** → `w80` **25** → `w85` **29** → `w90` **34**,
在外的四条旁支还各带 1–2 键(`w91` 30、`w92` 31、`w88` 27、`w89` 26)。
**周期 7 是一条主题极其单一的线:把全仓内联人设句收进注册表(G-13)。**
本槽 live grep 的结论是这条线**已经走到只剩 4 个文件 6 处**,
而**这 6 处恰好被四条在外的旁支一一对应地收掉**(`w88`→`js/experts.js:2`、`w89`→`js/plans.js:1`、
`w91`→`js/agent-global.js:1`、`w92`→`js/proj-planner.js:2`)——
**即 G-13 的 A 名单余量,全部押在四条没并线的分支上。**

`master` 上 `js/skills.js` 与 `docs/skills-wave/` **仍然都不存在**。
周期 1–6 六份核验报告最重要的那条结论,在周期 7 结束时**第七次逐字成立**——
113 条远端分支、七个周期、17 条校验项,在主干上一件都不可见,最深的一支已领先 **336** 个提交。

## 2. 独立复跑对账(全部数字为本槽实测)

在每支的独立 `git worktree` 上跑 `node tests/unit.js`、`node tests/integration.js`、`node tests/cli.smoke.js`。

| 分支 | unit 实测 | README | integration 实测 | README | cli.smoke 实测 | README | 提示词 live | README | 对账 |
|---|---|---|---|---|---|---|---|---|---|
| `master`(基线) | **201/201** | 163 ✗ | **79/79** | — | **51/53** | 53 | —(无注册表) | — | 基线(unit 差 **38**,主干既有,第七个周期) |
| `w70-integration` | **417/417** | 417 ✅ | —(未跑) | 126 | —(未跑) | 97 | — | — | ✅ |
| `w72-integration` | **424/424** | 424 ✅ | —(未跑) | 126 | —(未跑) | 97 | — | — | ✅ |
| `w73-voice-director-prompts` | **418/418** | 418 ✅ | —(未跑) | 126 | —(未跑) | 97 | — | — | ✅ |
| `w74-digest-three-prompts` | **419/419** | 419 ✅ | —(未跑) | 126 | —(未跑) | 97 | — | — | ✅ |
| `w75-integration` | **428/428** | 428 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | 19 | 19 ✅ | ✅ |
| `w76-sb-board-prompts` | **426/426** | 426 ✅ | —(未跑) | 126 | —(未跑) | 97 | — | — | ✅ |
| `w77-event-graph-prompt` | **426/426** | 426 ✅ | —(未跑) | 126 | —(未跑) | 97 | — | — | ✅ |
| `w78-beatboard-prompt` | **426/426** | 426 ✅ | **126/126** | 126 ✅ | —(未跑) | 97 | 15 | 15 ✅ | ✅ |
| `w79-editors-prompt` | **425/425** | 425 ✅ | **126/126** | 126 ✅ | —(未跑) | 97 | 15 | 15 ✅ | ✅ |
| `w80-integration` | **435/435** | 435 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | **25** | 25 ✅ | ✅ |
| `w81-gsettings-prompt` | **430/430** | 430 ✅ | **126/126** | 126 ✅ | —(未跑) | 97 | 20 | 20 ✅ | ✅ |
| `w82-proj-shell-prompt` | **431/431** | 431 ✅ | **126/126** | 126 ✅ | —(未跑) | 97 | 20 | 20 ✅ | ✅ |
| `w83-proj-upload-prompt` | **430/430** | 430 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | 20 | 20 ✅ | ✅ |
| `w84-sb-views-prompt` | **431/431** | 431 ✅ | **126/126** | 126 ✅ | —(未跑) | 97 | 20 | 20 ✅ | ✅ |
| `w85-integration` | **443/443** | 443 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | **29** | 29 ✅ | ✅ |
| `w86-agent-ops-prompt` | **437/437** | 437 ✅ | **126/126** | 126 ✅ | —(未跑) | 97 | 27 | 27 ✅ | ✅ |
| `w87-role-editor-prompt` | **438/438** | 438 ✅ | **126/126** | 126 ✅ | —(未跑) | 97 | 26 | 26 ✅ | ✅ |
| `w88-experts-forge-prompt`(旁支) | **437/437** | 437 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | 27 | 27 ✅ | ✅ |
| `w89-plans-prompt`(旁支) | **437/437** | 437 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | 26 | 26 ✅ | ✅ |
| `w90-integration`(**集成 tip**) | **453/453** | 453 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | **34** | 34 ✅ | ✅ 见 2.2 重名 |
| `w91-intent-router-prompt`(旁支) | **445/445** | 445 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | 30 | 30 ✅ | ✅ |
| `w92-proj-planner-prompt`(旁支) | **445/445** | 445 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | 31 | 31 ✅ | ✅ |
| `w94-persona-remainder-prompt`(旁支) | **443/443** | 443 ✅ | **126/126** | 126 ✅ | **95/97** | 97 ✅ | 29 | 29 ✅ | ✅ 零代码改动,见 4.9 |

**零失败项**:二十三支的 unit / integration 全部 0 FAIL。

**四个数字这一轮全对,一处失真都没有。** 这是**连续第三个周期** unit / CHECKS / 提示词条数零失真,
也是**第一个** `integration` / `cli.smoke` 两个**无断言保护**的数字也零失真的周期。
周期 6 报告里那句「受断言保护的数字零错、不受保护的数字 21% 错」在本周期**不成立**——
但下一节会说明,这不是因为洞被堵上了。

### 2.1 `cli.smoke` 那 2 项失败:第七次独立复现,九支逐字一致

本槽在 `master` 与 `w75`/`w80`/`w85`/`w90`/`w83`/`w88`/`w89`/`w91`/`w92`/`w94` **共 11 支上逐支抽 `^FAIL` 行比对,
十一支完全一致**:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

`master` 为 51/53、其余十支一律 **95/97**。各支「不是回归、未删测换绿」的说法成立;
**这两项已连续七个周期挂在同一位置。**

**本槽另记一条与被核分支无关、但会影响任何复核者的实测事实**:`tests/cli.smoke.js` **不是并行安全的**。
本槽第一轮把多支放进同一批并发跑,同一支 `w90` 两次分别得到 **83/97** 与 **54/97**,
且 `w88`/`w89` 上多出四条与记忆桶/播种相关的假 FAIL;改成**严格串行(每支之间 `sleep 3`)后十支稳定 95/97**。
成因是各支共用同一套临时端口与配置目录。**复核时务必串行跑,否则会误报回归。**

### 2.2 `w90` 上有一对同名用例——本仓库第一次

| 分支 | 用例数(live) | 不同用例名数 | 差 |
|---|---|---|---|
| `w75-integration` | 428 | 428 | 0 |
| `w80-integration` | 435 | 435 | 0 |
| `w85-integration` | 443 | 443 | 0 |
| `w90-integration` | **453** | **452** | **1** |

重名的那条是 `注册表全仓持有者名单:每条 def 的字面持有者恰好只有 js/prompts.js(谁在别处抄第二份即红)`,
`w90` 上 **4970 行与 5498 行各一份,同属套件 17**。本槽逐字比对两处函数体:
**判据、扫描范围、`byDef` 同 def 归并的期望串全部相同**,唯一实质差别是注册表条数下限
(`assert(list.length >= 26)` 与 `assert(list.length >= 20)`)与两个局部变量名。

溯源:`w84` 与 `w87` **各自独立**把「逐槽各写一份的持有者名单」改写成了「按注册表现取的一条」,
两条名字撞了;W90 按它一贯的并集解法把两侧都留下。**取并集这个决定本身没错**
(它不能替两个槽判断哪一份该弃),**代价是留下了一条冗余用例:用例数 +1、覆盖面 +0**。

**它的实际危害不在这条用例,在取证方法上。** 本目录从 W21 起用的「用例名集合比对证明没删测」
在 `w85 → w90` 这一步会得到**新增 9 条 / 消失 0 条**,而真实增量是 **10 条**——
集合口径把重名那条吃掉了。本槽实测 `tests/unit.js` 里**没有任何断言要求用例名唯一**
(grep `用例名` / `重名` / `duplicate` 零命中)。
**W90 自己没被坑到**,因为它用的是单向 `comm -23`(十五次全空)+ 增量相加(443+2+3+2+3=453),两种都不受重名影响;
**但这两种口径是它自己选的,不是有断言逼它选的。**

### 2.3 注册表口径实测(直读数据,不采信文档)

| 分支 | `CHECKS` | pending 条/面 | `preflightStages()` | `gaps()` 键 | `KB.SECTIONS` | `Prompts` 键 | `playbooks()` | `SK` |
|---|---|---|---|---|---|---|---|---|
| `w75-integration` | 17 | **0 / 0** | 七面 | 20 | 18 | **19** | 5 | 30 |
| `w78-beatboard-prompt` | 17 | 0 / 0 | 七面 | 20 | 18 | 15 | 5 | 30 |
| `w79-editors-prompt` | 17 | 0 / 0 | 七面 | 20 | 18 | 15 | 5 | 30 |
| `w80-integration` | 17 | 0 / 0 | 七面 | 20 | 18 | **25** | 5 | 30 |
| `w81`/`w82`/`w83`/`w84` | 17 | 0 / 0 | 七面 | 20 | 18 | 20 | 5 | 30 |
| `w85-integration` | 17 | 0 / 0 | 七面 | 20 | 18 | **29** | 5 | 30 |
| `w86-agent-ops-prompt` | 17 | 0 / 0 | 七面 | 20 | 18 | 27 | 5 | 30 |
| `w87-role-editor-prompt` | 17 | 0 / 0 | 七面 | 20 | 18 | 26 | 5 | 30 |
| `w88-experts-forge-prompt` | 17 | 0 / 0 | 七面 | 20 | 18 | 27 | 5 | 30 |
| `w89-plans-prompt` | 17 | 0 / 0 | 七面 | 20 | 18 | 26 | 5 | 30 |
| `w90-integration`(tip) | 17 | **0 / 0** | 七面 | 20 | 18 | **34** | 5 | 30 |
| `w91-intent-router-prompt` | 17 | 0 / 0 | 七面 | 20 | 18 | 30 | 5 | 30 |
| `w92-proj-planner-prompt` | 17 | 0 / 0 | 七面 | 20 | 18 | 31 | 5 | 30 |
| `w94-persona-remainder-prompt` | 17 | 0 / 0 | 七面 | 20 | 18 | 29 | 5 | 30 |

**和周期 6 那张表一样平,而且更平**:`CHECKS`、面表、`gaps()`、`KB.SECTIONS`、`playbooks()`、`SK`、pending
**七列在十七支上一个数字没变**。唯一动的仍是 `Prompts` 键——**从 14 键(周期 6 末)推到 34 键,净 +20**。

`w90` 上 34 键实测(`Prompts.list().map(x => x.key)`):

```
split.system rip.system narration.system reading.system concept.system light.system dirset.system
extract.system voice.recommendSystem voice.recommendBatchSystem persona.promptSystem persona.editSystem
digest.planSystem sb.boardSceneSystem sb.boardDraftSystem graph.system sb.system sb.reviewUser
sb.reviewSystem und.system beat.system gen.promptSystem gen.editSystem review.system review.sumSystem
review.finalSystem dist.copySystem agent.system agent.panelSystem agent.drawerSystem agent.previsSystem
agent.selfFixSystem agent.compactSystem comic.bubbleSystem
```

**周期 7 在注册表口径上只前进了一件事,和周期 6 一样:人设句收编。**
区别是周期 6 收的是 Agent 侧那 4 键,周期 7 收的是**创作主线上的 20 键**——
剧本(4)、配音(2)、摘要(1)、分镜创作层(2)、事件图谱(1)、节拍板(1)、漫剧气泡(1)、导演设定(1)、
发行文案(1)、拉片建集(1)、镜头按指令改(1)、主体按指令改(1)、文生图八维(1)、Agent 两辅助步(2)。

## 3. 分支拓扑:13 次合并 13 次取 head,功能槽 13/13 在集成线 head 上

```
master 9adcf0f
  └─ …周期1–6… ── w62-integration 9dfad8d ── w64-integration c36abaa
       └─ 52965b5(W58 发布留痕并入 w64 线)          ◀ 两条集成线在这里分叉
            ├─ 6b14a0f(补合 W53 真 tip 0045962)── … ── w67-integration cbb2b24
            │    └─ 99343e1(合 W65)── 1e4627c(合 W66)── w70-integration 87aa62a   ◀ w70 真 head
            │                            │
            │                            └─ ddc8994(合 w68 head 1d222d8)── w72-integration d2e7c43
            │                                 ▲ W72 基线取 1e4627c = w70 的中间点,漏掉 87aa62a ❌
            └─ … ── w68-integration 1d222d8(W61 前段四步回流)
                 │
                 └─(经 ddc8994 并入 w72)
                      └─ … ── w75-integration fbefd0c
                           └─ f7bb032(**补合 w70 真 tip 87aa62a**)◀ W80 当槽自纠 ✅
                                └─ 57cd89e(合 w73 head)── 5d2b35f(合 w74 head)
                                     └─ 960c092(合 w76 head)── c8bc2b7(合 w77 head)
                                          └─ w80-integration 4c45f89
                                               ├─ … ── w86-agent-ops b02b86a      ◀ 基线 = w80 head ✅(已合)
                                               ├─ … ── w87-role-editor fb74c31    ◀ 基线 = w80 head ✅(已合)
                                               ├─ … ── w88-experts-forge f4525ce  ◀ tip 1,基线 = w80 head
                                               ├─ … ── w89-plans 77e0ff8          ◀ tip 2,基线 = w80 head(见 3.3)
                                               └─ 8a075a5(合 w78)── 2a35d49(合 w79)
                                                    └─ 5352786(合 w81)── f757337(合 w82)
                                                         └─ w85-integration 2a05c72
                                                              ├─ … ── w91-intent-router 80742f6   ◀ tip 3
                                                              ├─ … ── w92-proj-planner 3f68c87    ◀ tip 4
                                                              ├─ … ── w94-persona-remainder 77a9916 ◀ tip 5(见 3.3)
                                                              └─ c914e39(合 w83)── 4376623(合 w84)
                                                                   └─ 8ee5138(合 w86)── bc9a38e(合 w87)
                                                                        └─ w90-integration 35695c8 ◀ 集成线 tip
```

### 3.1 集成槽:13 次合并 **13/13 取 head**,`--no-ff` **13/13** 成立

本槽逐条实测每个「整合」提交的 `parent2`,与被合入支**当前 head** 比对;
并逐条验 `git merge-base --is-ancestor parent1 parent2` 为**假**(真三方合并,不是本可 fast-forward 的伪合并):

| 集成波 | 合并提交 | 合入的分支 | `parent2` | 是该支 head? | `--no-ff` |
|---|---|---|---|---|---|
| W80(五次) | `f7bb0321` | `w70-integration`(**补合**) | `87aa62a5` | ✅ = head | 真三方合并 |
| | `57cd89eb` | `w73-voice-director-prompts` | `cd05444f` | ✅ = head | 真三方合并 |
| | `5d2b35f0` | `w74-digest-three-prompts` | `fc0dcf74` | ✅ = head | 真三方合并 |
| | `960c0929` | `w76-sb-board-prompts` | `e1993d11` | ✅ = head | 真三方合并 |
| | `c8bc2b78` | `w77-event-graph-prompt` | `8235d6f6` | ✅ = head | 真三方合并 |
| W85(四次) | `8a075a59` | `w78-beatboard-prompt` | `e467ea93` | ✅ = head | 真三方合并 |
| | `2a35d496` | `w79-editors-prompt` | `9dd486a8` | ✅ = head | 真三方合并 |
| | `53527867` | `w81-gsettings-prompt` | `7222a4dd` | ✅ = head | 真三方合并 |
| | `f7573370` | `w82-proj-shell-prompt` | `0994fc41` | ✅ = head | 真三方合并 |
| W90(四次) | `c914e39d` | `w83-proj-upload-prompt` | `819a90df` | ✅ = head | 真三方合并 |
| | `4376623a` | `w84-sb-views-prompt` | `9b647b42` | ✅ = head | 真三方合并 |
| | `8ee51385` | `w86-agent-ops-prompt` | `b02b86a9` | ✅ = head | 真三方合并 |
| | `bc9a38e4` | `w87-role-editor-prompt` | `fb74c31d` | ✅ = head | 真三方合并 |

**13/13 取 head、13/13 真三方合并,零例外。** 三个槽的第一父也逐个对上上一条集成线的 head
(`f7bb0321^1` = `fbefd0c2` = `w75` head、`8a075a59^1` = `4c45f89d` = `w80` head、`c914e39d^1` = `2a05c725` = `w85` head)。

**「取 head」这项周期 6 是 7/9(两次取中间点),周期 7 是 13/13。**
但下一节说明:**这个 13/13 只覆盖 W80 之后**;本周期真正的那次漏合发生在 W72,而 W80 的第一次合并就是去补它。

### 3.2 W53 与 W70 两处漏合的处置结果:**两处都已补合,一处是新发生的**

| 漏合 | 谁漏的 | 漏了什么 | 谁补的 | 现状(本槽实测) |
|---|---|---|---|---|
| **W53**(周期 6 阻塞 2) | W57 合了中间点 `09887be`,漏 head `0045962` | 143 行记账件 + 索引行 | **W67** `6b14a0f`「补合 W53 …真正的 tip」 | `is-ancestor 0045962 w90` = **真**;文件在、索引行在;三处散文由 `9058963` **就地更正**,`w57-integration-log.md` 里留了 5 处 `W67 更正` 标记 ✅ |
| **W70**(本周期新发生) | W72 基线取 `1e4627c`(= `w70` 的中间点),漏 head `87aa62a` | `w70-integration-log.md` 记账件 + 索引行 | **W80** `f7bb0321`「补合 W70 集成线真 tip」 | `is-ancestor 87aa62a w90` = **真**;`is-ancestor 87aa62a w72` / `w75` = **假**(在线外挂了两个波次)✅ 已闭合 |

**W70 那次的时点链**(commit 时点;push 时点仓库不可查,本槽不据此判动机):

| 时点 | 提交 | 事件 |
|---|---|---|
| 22:00:07 | `1e4627c` | `w70` 槽最后一次合并(合 W66 head) |
| **22:10:18** | **`87aa62a`** | **`w70` 记账件 + 索引行(`w70` 的真 head)** |
| 22:16:19 | `ddc8994` | W72 合 `w68` head(**晚于记账件 6 分 01 秒**) |
| 22:22:41 | `d2e7c43` | W72 记账件,第 58 行写下 `git rev-parse origin/cursor/w70-integration-ad31 # 1e4627c` |
| 22:51:10 | `fbefd0c` | W75 收槽(**未复测,漏合延续**) |
| 22:56:09 | `f7bb032` | **W80 补合 `87aa62a`**(距漏合 39 分 50 秒) |

**W72 那句 `# 1e4627c` 不是编的,是它那次 `fetch` 的真值**——和 W57 量到 `w53` outstanding = 2 是一样的情形。
问题同样不在测量,在**它把一个时点结论当成了基线依据,并且此后没有任何一步会提醒它复测**。
**W50 在周期 6 立的那条「每推一版重做一次包含性实测」口径,到本周期仍然只是一段散文**——
本槽实测 `docs/skills-wave/README.md` 里搜「当前集成线 / 集成线 head / 指针」**零命中**,
`tests/*.js` 里搜「集成线 / 祖先 / is-ancestor」**零命中**。**W63 药方 4 第四个周期未做。**

**两次漏合的差别是本轮最值得记的一条治理观察**:
W53 那次由**下一个周期的核验报告**指出来、隔了 7 个波次才补;
W70 那次由**同一条线上第三个集成槽**自己发现、隔 2 个波次就补了,而且补的时候连
「把 W72 取基线时漏在线外的记账件与索引行接成祖先」都写进了提交信息。
**同一个机制、同一种失误,发现它的成本从"一份独立核验报告"降到了"下一个集成槽顺手一查"。**
这个改善不是靠断言,是靠 W67 补 W53 那次留下的做法被后面的槽照着做了一遍。

### 3.3 功能槽基线:**13/13 落在集成线 head 上**;两支「慢了一个波次」

| 功能槽/旁支 | 基线实测 | 等于 | 形态 |
|---|---|---|---|
| `w73-voice-director-prompts` | `87aa62a5` | **`w70` head** | ✅ 集成线 head |
| `w74-digest-three-prompts` | `87aa62a5` | **`w70` head** | ✅ 集成线 head |
| `w76-sb-board-prompts` | `d2e7c43d` | **`w72` head** | ✅ 集成线 head |
| `w77-event-graph-prompt` | `d2e7c43d` | **`w72` head** | ✅ 集成线 head |
| `w78-beatboard-prompt` | `d2e7c43d` | **`w72` head** | ✅ 集成线 head |
| `w79-editors-prompt` | `d2e7c43d` | **`w72` head** | ✅ 集成线 head |
| `w81-gsettings-prompt` | `fbefd0c2` | **`w75` head** | ✅ 集成线 head |
| `w82-proj-shell-prompt` | `fbefd0c2` | **`w75` head** | ✅ 集成线 head |
| `w83-proj-upload-prompt` | `fbefd0c2` | **`w75` head** | ✅ 集成线 head |
| `w84-sb-views-prompt` | `fbefd0c2` | **`w75` head** | ✅ 集成线 head |
| `w86-agent-ops-prompt` | `4c45f89d` | **`w80` head** | ✅ 集成线 head |
| `w87-role-editor-prompt` | `4c45f89d` | **`w80` head** | ✅ 集成线 head |
| `w88-experts-forge-prompt`(tip) | `4c45f89d` | **`w80` head** | ✅ 集成线 head |
| —— | —— | —— | —— |
| `w89-plans-prompt`(tip) | `4c45f89d` | **`w80` head** | ⚠️ 见下,慢一个波次 |
| `w91-intent-router-prompt`(tip) | `2a05c725` | **`w85` head** | ✅ 集成线 head |
| `w92-proj-planner-prompt`(tip) | `2a05c725` | **`w85` head** | ✅ 集成线 head |
| `w94-persona-remainder-prompt`(tip) | `2a05c725` | **`w85` head** | ⚠️ 见下,慢一个波次 |

**周期 5 那两种病形(以另一个功能槽 head 为基线、取非 head 的中间点)本周期零复发,连一次都没有。**
十七支的基线**无一例外落在某条集成分支的 head 上**,没有一支落在中间点或功能槽上。

**两支「慢一个波次」的实测与本槽的判定**:

- `w89` 首个提交 **00:06:01**,而 `w85` head `2a05c72` 的 commit 时点是 **00:00:28**——早 5 分 33 秒。
  它取的是 `w80` head(上一条集成线),与同期的 `w86`/`w87`/`w88` 相同。
- `w94` 唯一一个提交 **00:38:41**,而 `w90` head `35695c8` 是 **00:35:17**——早 3 分 24 秒。
  它取的是 `w85` head。

**本槽不把这两支判为基线取错,理由是判据不足**:`merge-base` 反映的是**开槽时刻**的选择,
而仓库里只留得下**提交时刻**;分支完全可能在更早时点就已创建。
`w86`(head 23:46:22)、`w87`(23:55:23)、`w88`(首提交 23:56:19)三支取 `w80` head 是**当时唯一收完的集成线 head**
(`w85` 的四次合并到 23:44:59 才做完,记账件 00:00:28 才落),**这三支的选择无可指摘**。
`w89` 与 `w94` 只是**同一批槽里落笔最晚的那两个**。
**列在此处是为了下一个集成槽备查:合它们时冲突面会比同批的其他槽大一档**(3.4 的实测支持这一点)。

**这个 13/13 仍然是人守出来的,不是机制钉住的。** 判据同 3.2:集成线 head 指针与祖先断言两处**都零命中**。

### 3.4 五条在外的 tip:四条各收一个文件,合起来正好把 G-13 的 A 名单清零

本槽在只读克隆里逐条 `git merge-tree --write-tree` 数冲突块:

| tip | 冲突块合计 | 落点 | 改的产品文件 |
|---|---|---|---|
| `w90 ← w88-experts-forge` | **7 处** | `README.md` 3、`docs/skills-wave/README.md` 2、`js/prompts.js` 1、`tests/unit.js` 1 | `js/experts.js` |
| `w90 ← w89-plans` | **9 处** | `README.md` 3、`docs` 1、`js/prompts.js` 1、**`js/skills.js` 3**、`tests/unit.js` 1 | `js/plans.js` |
| `w90 ← w91-intent-router` | **9 处** | `README.md` 3、`docs` 1、**`js/skills.js` 2**、`tests/unit.js` 3 | `js/agent-global.js` |
| `w90 ← w92-proj-planner` | **10 处** | `README.md` 3、`docs` 2、**`js/skills.js` 2**、`tests/unit.js` 3 | `js/proj-planner.js` |
| `w90 ← w94-persona-remainder` | **1 处** | `docs/skills-wave/README.md` 1 | —(零代码改动) |

**四条代码 tip 各改一个互不相同的产品文件,但五份共用文件全撞**
(`js/prompts.js` / `js/skills.js` / `tests/unit.js` / `README.md` / `docs/skills-wave/README.md`)——
**即四条彼此之间也必然逐条冲突,不能指望任何两条自动合上。**
`w89` 与 `w92` 的 `js/skills.js` 各 2–3 处冲突,与 3.3 那条「落笔最晚的两支冲突面更大」吻合。

**它们合起来的意义**:A 名单的 6 处余量是 `js/agent-global.js:1` / `js/experts.js:2` / `js/plans.js:1` / `js/proj-planner.js:2`,
四条 tip **一一对应地各收一处**。**四条全部并线后,G-13 的 A 名单会归零**——
按 W36 立的关联索引口径,那时才轮到「摘 G-13 标记 + 一次改齐六条关联索引的 `gaps` 与 `note`」这个动作。
**这是本周期留给下一个集成槽最明确的一件事,也是七个周期里第一次有一条缺口具备"这一波就能闭合"的条件。**

### 3.5 并行 tip 有没有又裂成两条集成线:**周期内裂过一次,现在没有**

- **裂过**:`52965b5`(W58 发布留痕并入 `w64` 线)之后分出两条集成线——
  一条走 `w53` 补合 → W63 → W67 → W65 → W66 → **`w70`**,另一条走 **`w68`**(W61 前段四步回流)。
  本槽实测 `merge-base(w70, w68)` = `52965b5`,两条互不为祖先。
- **收成一条**:W72 的 `ddc8994` 把 `w68` head 合进 `w70` 线——**但基线取的是 `w70` 的中间点**,
  于是"两线合一"这件事做成了,"把 `w70` 的记账件接成祖先"这件事漏了(3.2)。
- **现在**:本槽逐支实测 `w62` / `w64` / `w67` / `w68` / `w70` / `w72` / `w75` / `w80` / `w85` 九条集成分支
  **全部是 `w90` 的祖先**,`w90` 是**唯一的集成线 tip**。
  五条在外的 tip **全是功能槽**(`w88`/`w89`/`w91`/`w92`/`w94`),**没有第二条集成线**。

**结论:第 7 项必核为"周期内裂过一次并已收拢,当前无并行集成线"。**
但要记一句:那次分裂**没有任何机制发现它**——是 W72 拿到的任务口径里恰好写了「二选一规则」才把两条并回来的
(`w72-integration-log.md` 第 53 行原文:「`origin/cursor/w70-integration-*` 存在**且已包含 w67 HEAD** 就从 w70 tip 起」)。
**规则写在任务书里,不在仓库里。**

## 4. 逐项核验(集成线三槽 + 五条旁支)

**主干等级全部为 M0**(`master` 上 `js/skills.js` 不存在),下文不再逐条重复。

### 4.1 W80 · `w80-integration-5369` — 必核项:补合 W70 + 四条收编槽并线

**成熟度 M3。** 增量:五次合并(`w70` head 补合、`w73`/`w74`/`w76`/`w77` 四个 head),
unit **435/435**、integration 126/126、cli.smoke 95/97,`Prompts` 19 → **25 键**。
**`--no-ff` 5/5 ✅,取 head 5/5 ✅,四个 README 数字全部对上 ✅。**

**这是周期 7 治理面最有价值的一个槽,理由与它合了什么无关:它是第一个"顺手把上上个槽的漏合捞回来"的集成槽。**
`f7bb0321` 的提交信息写得很直白:「补合 W70 集成线真 tip(`w70-integration-ad31` head `87aa62a`),
把 W72 取基线时漏在线外的 W70 记账件与索引行接成祖先」。
本槽实测该合并的 `parent2` = `87aa62a5` = `w70` head ✅,且 `is-ancestor 87aa62a w72` / `w75` **均为假**——
**它说的漏合属实,补的位置也对。**

用例增量对账(本槽独立复跑,不引用它的数字):基线 `w75` 428;
`w70` 相对 `1e4627c` 增量 **0**(只有记账件)、`w73` 417→418 **+1**、`w74` 417→419 **+2**、
`w76` 424→426 **+2**、`w77` 424→426 **+2**;`428+0+1+2+2+2 = 435`,**与 live 435 逐个相等**。
用例名集合 `w75 → w80`:**新增 7 条、消失 0 条**。

**阻塞**:无未闭项。

### 4.2 W85 · `w85-integration-171f` — 必核项:四条收编槽并线

**成熟度 M3。** 增量:四次合并(`w78`/`w79`/`w81`/`w82` 四个 head),
unit **443/443**、integration 126/126、cli.smoke 95/97,`Prompts` 25 → **29 键**。
**`--no-ff` 4/4 ✅,取 head 4/4 ✅,四个 README 数字全部对上 ✅。**

用例增量对账:基线 435;`w78` 424→426 **+2**、`w79` 424→425 **+1**、`w81` 428→430 **+2**、`w82` 428→431 **+3**;
`435+2+1+2+3 = 443`,**与 live 逐个相等**。用例名集合 `w80 → w85`:**新增 8 条、消失 0 条**。

它记账件标题里点出的那件事本槽复核成立且有独立价值:
**「索引行在冲突块外自动合上导致同名两行」**——两个槽各自在索引表尾追加一行时,`git` 会把两块都留下,
若两行指同一份文件就成了重复登记。**本槽实测 `w80`/`w85`/`w90` 三支的索引表逐支去重后无重复行**
(`grep -oE '^\| \[[^]]+\]' | sort | uniq -d` 三支皆空),**说明它当时确实清干净了**。
**但这一类同样没有断言**:现有索引契约比的是"索引行集合"与"目录文件集合"相等,
**集合去重之后,重复一行是看不出来的。**

**阻塞**:① 它记账件第 6 节自己登记的「三张持有者名单口径仍未统一」未做(见 5.3);
② 上述"索引表可以有重复行而契约不红"这一处,本槽为它补记(它发现了现象,没有留下防线)。

### 4.3 W90 · `w90-integration-9004` — 必核项:四条收编槽并线;集成线 tip

**成熟度 M3。** 增量:四次合并(`w83`/`w84`/`w86`/`w87` 四个 head),
unit **453/453**、integration 126/126、cli.smoke 95/97,`Prompts` 29 → **34 键**。
**`--no-ff` 4/4 ✅,取 head 4/4 ✅,四个 README 数字全部对上 ✅。**

用例增量对账:`443+2+3+2+3 = 453`,**与 live 逐个相等**;用例名单向 `comm -23` 本槽复跑亦为空。

**它做对且值得单独记的三件事**:

1. **「同一条仍欠段在一条线里翻了三次面,每次都连断言一起翻」**——本槽复核成立。
   SK-10 / SK-11 的仍欠段在基线上点名 `js/sb-views.js` 与 `js/agent-ops.js`,
   这条线上 W84 收 `sb-views`、W86 收 `agent-ops`、W87 收接替上去的 `role-editor`;
   本槽实测 `w90` 的 `tests/unit.js` 里对这三处**各有一对断言**
   (`!owed.includes(<file>)` + 该文件 `system: '你是` 计数为 0),**三对齐全,一个没漏**。
2. **「注册表之外一格没动」的取证是直读出来的,不是"git 没报冲突"推出来的**——
   本槽复跑 `git diff --name-only 2a05c72 35695c8 --` 那 12 个路径,**确为零输出**;
   `Issues.reminders()` 现取 7 条、`KB.SECTIONS` 18、`gaps()` 20 键,与它记的一致。
3. **`prompts` 数组三次真冲突取并集**——本槽实测 34 键 = 29 + 5(`rip.system`、`gen.editSystem`、
   `agent.selfFixSystem`、`agent.compactSystem`、`persona.editSystem`),**并集没有吃掉任何一侧**。

**阻塞**:

① **对 `w88` / `w89` 的现取记账失真(第 1 节第 2 条)。** live:`w89-plans-prompt-835b` **存在**,
head `77e0ff8`(00:11:38)、首提交 00:06:01;`w88` 真 head 是 `f4525ce`(00:08:30),它记的 `78feca2` 是中间点。
W90 的四次合并在 00:09:36–00:21:24、记账件 00:35:17。
**本槽的判定:这是记账失真,不是漏合。** W88/W89 由任务口径明令排除,不合是对的;
且它用了"现取"这个限定词,不是 W57 那种无限定的事实判断。
**但「`w89-*` 现取远端零匹配、不存在」这句话,在它写下的时刻已经不成立了 23 分 39 秒**,
而它第 6 节「剩余未合与残留」正是给下一个人看的那一栏——**下一个槽照着这一栏做,会漏掉一整条 tip**。

② **它留了一处有意的路障但没留给正确的人。** 它把 SK-10/SK-11 仍欠段的锚点落在 `js/experts.js`,
并写明「下一槽合 W88 时那两条源级反向断言会先红」。本槽实测这条路障**真实存在且会红**(变异 11/12 同形)。
问题是它同时把 W89 记成"不存在"——**而 `js/plans.js` 也在同一条仍欠段里被点名**,
`w89` 落地后同样会触发翻面。**路障留对了,但"谁会来撞"这一栏写错了一半。**

③ **重名用例**(2.2):并集解法保住了 `w84` 与 `w87` 两侧的断言,代价是全仓第一对同名用例、用例数 +1 覆盖面 +0。
它自己的取证口径没被这个影响,但它没有登记这件事。

### 4.4 W88 · `w88-experts-forge-prompt-a3f7` — 旁支:专家工坊两处人设收编

**成熟度 M3(分支内自洽全绿)。** 三个提交(功能 23:56:19 / 用例 00:02:21 / 记账件 00:08:30),
unit **437/437**、integration 126/126、cli.smoke 95/97,`Prompts` 25 → **27 键**
(`forge.system` / `forge.evolveSystem`)。**基线 `4c45f89d` = `w80` head ✅,四个 README 数字全对 ✅。**

**四件齐全**(功能、用例、README 同步、记账件 + 索引行),按本目录纪律**它已经收槽了**。
它收的正是 `js/experts.js` 的元智能体两处——`w90` 上 SK-10 / SK-11 仍欠段点名的两个文件之一。

**阻塞**:未并线;`w90 ← w88` 实测 **7 处冲突**(3.4)。合它时 SK-10 / SK-11 两条源级反向断言会先红,
**须连记账一起翻**(这是 `w90` 有意留的路障,不是回归)。

### 4.5 W89 · `w89-plans-prompt-835b` — 旁支:制作计划生成步人设收编

**成熟度 M3。** 两个提交(功能 00:06:01 / 记账件 00:11:38),unit **437/437**、integration 126/126、
cli.smoke 95/97,`Prompts` 25 → **26 键**(`plan.system`)。**基线 `4c45f89d` = `w80` head,四个 README 数字全对 ✅。**

它收的是 `js/plans.js` 的制作计划器——SK-10 / SK-11 仍欠段点名的另一个文件。
它记账件标题里写的「G-13 11→10 盘点」用的是**它自己那张名单的口径**;
本槽按 `w90` 的 A 名单口径实测余量为 **6 处 / 4 文件**(5.3),**两个数字口径不同、不互相折算**。

**阻塞**:① 未并线,`w90 ← w89` 实测 **9 处冲突**,其中 `js/skills.js` **3 处**(本批 tip 里最多);
② `w90` 第 6 节把它记成"不存在"(4.3 阻塞①),**下一个集成槽若照着那一栏做会整条漏掉**;
③ 基线慢一个波次(3.3,本槽不判其取错)。

### 4.6 W91 · `w91-intent-router-prompt-6ea6` — 旁支:意图路由步人设收编

**成熟度 M3。** 两个提交(功能 00:21:22 / 记账件 00:27:07),unit **445/445**、integration 126/126、
cli.smoke 95/97,`Prompts` 29 → **30 键**(`agent.routeSystem`)。**基线 `2a05c725` = `w85` head ✅,四个数字全对 ✅。**

它收的是 `js/agent-global.js` 的意图路由器——**A 名单里那处从未被任何 `note` 的仍欠段点名的余量**(0.1)。
它记账件标题点的「取值口求值时机第一次写成断言」本槽复核成立:它把"模块顶层不预取"写成了断言,
理由是加载期取值会把覆盖表冻在加载那一刻。

**阻塞**:未并线,`w90 ← w91` 实测 **9 处冲突**(`tests/unit.js` 3 处、`js/skills.js` 2 处)。

### 4.7 W92 · `w92-proj-planner-prompt-b845` — 旁支:项目实验台两步人设收编

**成熟度 M3。** 两个提交(功能 00:23:52 / 记账件 00:30:33),unit **445/445**、integration 126/126、
cli.smoke 95/97,`Prompts` 29 → **31 键**(`planner.chatSystem` / `trans.localizeSystem`)。
**基线 `2a05c725` = `w85` head ✅,四个数字全对 ✅。**

它收的是 `js/proj-planner.js` 两处——**同样从未被任何仍欠段点名**,而且**连 C 名单都漏了它**(5.3)。
它记账件里写明了这一点(「def 字面名单拦不住带变量那处的退化」「三张全仓名单逐张按 live 重算」),
**本槽复核这条自述属实**:C 名单的判据 `(?:system:|return|const|let|var …=)\s*['\`]你是` 匹配不到
`{ role: 'system', content: '你是资深短剧策划…' }` 这种写法。

**阻塞**:未并线,`w90 ← w92` 实测 **10 处冲突**(本批最多)。

### 4.8 W94 · `w94-persona-remainder-prompt-3f1f` — 旁支:核实为零余量,**零代码改动的一个槽**

**成熟度:不适用常规评级(无功能增量)。** 单个提交(00:38:41),
改动**只有两份文档**:`docs/skills-wave/w94-persona-remainder-prompt.md`(153 行,新增)+ 索引行 1 行。
unit **443/443**、integration 126/126、cli.smoke 95/97、`Prompts` **29 键**——
**与其基线 `w85` 逐个相同**(unit 443、`assert*` 调用数 2830 = `w85` 的 2830,净 0)。

**本槽的判定:这是一个合规的"核实无余量"槽,不是空跑。**
它的任务是核实 `js/persona.js` 还剩多少内联人设;本槽独立复跑其结论:
`js/persona.js` 里 `['\`"]你是` 匹配 **0 处**、`Prompts.get` **3 处**——
**该文件三个 LLM 步全部经注册表现取,全文零内联,余量确已随 W69/W73 并入基线而消失。**
它按口径**没有为了产出而改代码**,并在记账件里逐条点出基线上已经钉住这条结论的五处断言。
**这一点值得肯定:它是六个周期里第一个"查完发现没得做、于是只交一份记账"的槽。**

**阻塞**:未并线,`w90 ← w94` 实测 **1 处冲突**(只在索引表,索引行按波次序插入即可)。
**它是五条 tip 里合流成本最低的一条。**

### 4.9 W93 / W95:远端零引用,状态未知

`git branch -r` 无 `w93-*` / `w95-*` 前缀命中(核验时点 113 条分支逐条比对)。
**本槽按 W50 立的口径,把这写成时点结论而非终局结论**:
这两个槽号可能从未登记,也可能只是尚未推送——**仓库里查不到 push 时点,本报告不评分、不推测**。
**这正是本周期两次失真(3.2、4.3)的同一个位置,故此处刻意写足限定词。**

## 5. 短名单 30 条、缺口与 G-13 余量

`SK` 条数实测 **30**(十七支全部),`kinds` 字段逐条无漂移。
**pending 在周期 7 全程为 0 条 / 0 面**——本槽逐支实测 `Skills.list().filter(s => s.pending.length)` 为空数组。
**这是连续第二个全程零 pending 的周期。**

### 5.1 三条 `note` 的仍欠段现况(直读 `w90`)

| 条目 | 仍欠段(本槽直读原文摘) | 状态 |
|---|---|---|
| `SK-03` `core.personaCtx`(`gaps: ['G-01']`) | 「四处的 ops 协议/字段面/命令白名单/返回 JSON 约定仍由各自装配口拼、**不开放覆盖**……音色推荐两条同理只收人设句……漫剧气泡那处同口径……多轮那三份与音色推荐两份都没有 Node 第二消费点,两端只落在取值口」 | **有意不做**,理由逐句写明;本周期新收的五条键都写在"已落地"半,有断言禁止挤进仍欠段 |
| `SK-04` `core.memoryDual`(`gaps: ['G-02']`) | 「生成与合成两步没有可判定的结构化结论可回流(素材产出的判定面归发布门 G3/G7),浏览器剧本解析向导走自己的入库路径,提取主体的回流只挂在命令层」 | **周期 6 的假欠账已修**(0.1);现措辞与 live 代码一致 |
| `SK-23` `review.stage` | 「校验面已随 SK-24 落地,而报告好坏优劣的语义面(方法论门那一半)仍待 **G-10**;审片不作分集级硬阻塞是既定口径,不算欠账」 | 待 G-10,**产品口径未定,第五个周期** |

`SK-10` `script.aiToneBan`(`gaps: ['G-13','G-10']`)与 `SK-11` `subjects.refDiscipline`(`gaps: ['G-13']`)的仍欠段
都只点名 `js/experts.js` 与 `js/plans.js` 两处(见 5.3 的口径差)。

### 5.2 `gaps()` 20 键与 S-08:两条都和周期 6 逐字相同

本槽实测 `w90` 上 `Object.keys(Skills.gaps())` = **20 键**、`Skills.gaps()['G-13']` =
`script.hookType,script.aiToneBan,subjects.refDiscipline,eps.structureStage,gen.videoTpl,film.rhythmInject`
**六条逐字节未变**(有用例钉住,只收取值口不预支摘标记)。

`Skills` 的函数出口实测:

```
block, byId, check, covering, forExpert, gaps, list, playbook, playbooks,
preflightStages, stageOf, stages, validate
```

**与周期 5、周期 6 实测逐字相同**——按 `pending` 过滤的未闭清单投影**第四个周期未做**。
**但这条药方 W63 已经判定过期了**(pending 恒为 0,该投影会恒回空),本槽复核这个判定仍然成立,
**不再作为待办重提**(见 8.3 第 5 条的改写)。

`'S-08' in Skills.gaps()` = **false**,`Skills.REG.length` = **30**——
S-08(发布后→上游回路)**仍未进 30 条,连续七个周期未决**。

### 5.3 G-13 余量 live grep:三张名单、三个口径、三个数——本槽并列写清

**这是本轮必核项 5 的核心。** 本槽不采信任何一份记账件里的数字,直接在 `w90` 的工作树上按多个判据各扫一遍:

| 口径 | 判据 | live 结果 | 处数 |
|---|---|---|---|
| **A 名单**(`tests/unit.js:3893 inlinePersonaHolders()`) | `(?:system\s*:\|content\s*:\|=)\s*['\`]你是` | `js/agent-global.js:1, js/experts.js:2, js/plans.js:1, js/proj-planner.js:2` | **6 处 / 4 文件** |
| **C 名单**(`tests/unit.js:5302`) | `(?:system:\|return\|(?:const\|let\|var) X =)\s*['\`]你是` | `js/agent-global.js:1, js/experts.js:2, js/plans.js:1, js/wf-core.js:1` | **5 处 / 4 文件** |
| **反向断言判据 D**(SK-10 / SK-11 的仍欠段那两条) | `system: ['\`]你是` | `js/experts.js:1, js/plans.js:1` | **2 处 / 2 文件** |
| **宽口径**(任意引号后紧跟"你是",含数据与 UI) | `['\`"]你是` | `agent-global:1, api:2, experts-data:16, experts:2, gsettings:1, plans:1, proj-planner:2, prompts:33, wf-core:1` | **59 处 / 9 文件** |

**三张名单各自都有断言钉住其期望串**(本槽变异 11、12 实测:动 `js/agent-global.js` 红 3 条、
动 `js/proj-planner.js` 红 2 条),**所以余量不会被静默清零。** 这是周期 7 相对周期 6 的实质进步。

**但三个数并列摆出来,就能看到三处口径裂缝**:

1. **C 名单漏了 `js/proj-planner.js:2`**——因为它的判据里没有 `content:`,
   而那两处写成 `{ role: 'system', content: '你是资深短剧策划…' }`。
   `w90-integration-log.md` 第 5.4 节**如实登记了这处差额**(「C 名单少 `js/proj-planner.js:2`、多一个 `js/wf-core.js:1`」),
   **本槽复核属实**;但登记了不等于修了——**动 `js/proj-planner.js` 时 C 名单那条不会红**(变异 12 实测:只红 2 条,C 名单那条全绿)。
2. **仍欠段只点了 4 个文件里的 2 个**(0.1)。`js/agent-global.js` 与 `js/proj-planner.js` 那 3 处
   **在任何一条 `note` 的仍欠段里都不出现**,只在 A/C 名单的期望串里。
   **即"余量清单"有两个来源,而两个来源不一致——读 `note` 的人会以为只剩 2 个文件。**
3. **宽口径那 59 处里,49 处是有意不计的**(注册表 `js/prompts.js` 33 条 def + 专家库 `js/experts-data.js` 16 条产品数据),
   另有 `js/api.js:2`(调用方不给 system 时的层内兜底 `'你是专业助手。'`)、
   `js/gsettings.js:1`(输入框 `placeholder`,不发给模型)、`js/wf-core.js:1`(单镜审片的 user 半)。
   **这三类的排除理由本槽逐处直读源码复核,全部成立**——不是为了把数字做小。

**本槽给出的口径建议(不执行)**:A 名单是四张里最贴近 G-13 语义的一张(它同时覆盖 `content:` 形态),
统一时应以 A 为准、把 C 并进去,并让 `note` 的仍欠段**由名单反推生成**而不是各写各的。

### 5.4 G-10 发布门半边:连续五个周期零开工

本槽在 `w75`/`w80`/`w85`/`w90`/`w88`/`w89`/`w91`/`w92`/`w94` **九支上实测 `js/release.js` 对 `Skills` 的引用为 0**。
卡点仍是「方法论门要不要挂成默认 `warn` 的可选门」这个产品决定,**不是技术前置**。

## 6. 假绿:三类的现状与十二路变异实测

W63 第 6 节把假绿分成三类并按"离契约多远"排了序。本周期三类的现状**各不相同**,逐类交代。

### 6.1 三类的现状

| # | 周期 6 的形态 | 周期 7 现状 | 本槽实证 |
|---|---|---|---|
| 1 | **记账件与索引行一起丢** → 双向契约仍零差 | **加固了一半**。W67 新增 `contract · docs/skills-wave 索引完备性` 断言:逐份文件点名要索引行(正向)+ 目录内任何 `](./x.md)` 相对链接不许悬空(反向)。**但反向那半只在"别处散文已经点了名"时才生效** | 变异 6/7 两向都红;变异 8 红;**变异 9/10 全绿**(见 6.2) |
| 2 | **`integration` / `cli.smoke` 两个数字无断言** | **一个字没做,第二个周期**。`tests/unit.js` 里 grep `cli.smoke` / `integration.js` **零命中**;README 数字对账契约仍只有三条(CHECKS+面数 / unit 用例数 / 注册表口径) | 变异 4b、5b **两次全绿** |
| 3 | **`facts` 表只防假清账不防假欠账** | **已闭合(该实例)且推广成了通用做法**。SK-10 / SK-11 上有六对翻面断言;三张全仓持有者名单各有精确到"文件:处数"的期望串 | 变异 11、12 **分别红 3 条 / 红 2 条** |

**三类里两类动了、一类没动。** 没动的那一类(第 2 类)**恰恰是本周期数字零失真的那两个数字**——
**即"没人掉进去"和"洞被堵上了"是两回事,本周期把这两件事分得干干净净。**

### 6.2 变异实测(全部在 `/tmp` 副本上做,已删除;不落任何改动到本仓库)

以 `w90` tip(基线 **453/453 PASS, 0 FAIL**)为底,十二路变异:

| # | 变异 | 结果 | 说明 |
|---|---|---|---|
| 1 | README unit 断言数 `453` → `452` | **红 452/453** `contract · README 数字对账:单元测试用例数由 tests/unit.js 实计` | 头号数字有断言 ✅ |
| 2 | 删掉「CHECKS 已落地十七条」整句 | **红 452/453** `架构框 CHECKS 条数` | 删句绕过被堵 ✅ |
| 3 | README 提示词条数 `34` → `33` | **红 452/453** `注册表提示词数` | 注册表口径有断言 ✅ |
| 4b | README `integration` 数字 `126` → `105` | **绿 453/453** | ❗ **第二个周期没有断言** |
| 5b | README `cli.smoke` 数字 `97` → `64` | **绿 453/453** | ❗ 同一个洞的另一半 |
| 6 | 只删记账件 `w87-role-editor-prompt.md`(索引行留着) | **红 451/453**(双向对齐 + 索引完备性 **各 1 条**) | 索引指向空文件会红 ✅ |
| 7 | 只删该记账件的索引行(文件留着) | **红 451/453**(同上两条) | 记账件漏登记会红 ✅ |
| 8 | **同时删掉 `w87` 记账件 + 它那行索引** | **红 452/453** `目录内相对链接不许悬空:README.md → w87-role-editor-prompt.md(文件不存在)` | ✅ **周期 6 这一路是全绿的,W67 的加固在这里生效** |
| 9 | **同时删掉 `w73-voice-director-prompts.md` + 它那行索引** | **绿 453/453** | ❗ **假绿仍在**,只是收窄了——见下 |
| 10 | **同时删掉 `w63-cycle6-audit.md`(上一份核验报告)+ 它那行索引** | **绿 453/453** | ❗ 同上 |
| 11 | 模拟 W91 落地:`js/agent-global.js` 那句改走注册表形态 | **红 450/453**(三张全仓名单**各 1 条**) | ✅ **假欠账方向已被接住** |
| 12 | 模拟 W92 落地:`js/proj-planner.js` 两处不再匹配 | **红 451/453**(**只红 2 条**,C 名单那条不红) | ✅ 接住了,但 5.3 那处口径裂缝在此暴露 |

**变异 8 与变异 9/10 的差别,就是 W67 那条加固的确切边界,本槽把它量了出来。**

反向那半的判据是「目录内任何 `](./x.md)` 形式的相对链接都必须指到真文件」。
所以一份记账件能不能被它接住,**取决于目录里除了索引行之外还有没有第二处 markdown 链接指向它**:

- `w87`(以及本周期新落的 `w83`/`w84`/`w86`/`w90`)**各有 2 处链接**——索引表一行 + `docs/skills-wave/README.md`
  第 126 行「记账诚实位」或第 138 行「最新收敛记录」那一段。删掉索引行后另一处仍在 → **悬空 → 红**。
- 本槽逐份统计:`w90` 上 **107 份 `wNN-*.md` 记账件里,34 份全目录只有 1 处 markdown 链接**(即只有索引行)。
  **这 34 份可以连文件带索引行一起消失而 453/453 全绿。**
  周期 7 范围内的两份是 `w73-voice-director-prompts.md` 与 **`w63-cycle6-audit.md`**——
  **上一份周期核验报告本身就在这 34 份里面。**

**所以第 1 类假绿的准确说法是**:它从"任何记账件都能静默消失"收窄成了
"**没被别处散文用 markdown 链接点过名的记账件**能静默消失",覆盖面 73/107;
**而这份保护靠的是一个写作习惯(新记账件顺手在诚实位/收敛记录里再链一次),不是一条不变量**——
本槽实测 `tests/unit.js` 里搜「记账诚实位 / 最新收敛记录」**零命中**,没有任何断言要求那两段存在或包含谁。

**W63 药方 2① 要的那条(「从集成提交信息里抽槽号,断言每个槽号都有记账件」)仍未做**:
本槽在 `tests/unit.js` 里 grep `整合` / `槽号` **零命中**。
**不过 live 层面这条现在是满的**:`w90` 上 `w62`–`w87` **26 个已合槽号的记账件一份不缺**
(逐份 `git ls-tree` 比对),`w53` 的也在。**即完备性目前成立,只是没有东西守着它。**

## 7. 相对 `master` 的状态

### 7.1 主干:第七个周期一件未合

- `master` head 仍是 **`9adcf0ff`**,全仓 **8 个提交**,七个周期内**一次未动**。
- `js/skills.js` **不存在**;`docs/skills-wave/` **不存在**(`docs/` 下只有 `AI助手接入指南.md` 与 `用户操作指南-V0.1.md`)。
  `js/prompts.js` 存在,但它是主干既有模块,**不含本波新增的任何 `*.system` / `*System` 键**。
- **113 条**远端分支全部以 `master` 为严格祖先,**任一支单独并入都是纯快进、零冲突**。
- 最深的一支领先 **336** 个提交(周期 2 末 103、周期 3 末 125、周期 4 末 170、周期 5 末 212、周期 6 末 252、周期 7 末 **336**)。
- 主干 README 的 unit 断言数自称 **163**、实跑 **201**,**差 38**,连续七个周期未修。
- `cli.smoke` **2 项失败**连续七个周期未修(本槽第七次独立复现,同名同表现,见 2.1)。

**本轮不开 PR、不合并**(任务书明令),以上仅为状态记录。

### 7.2 合并后会红哪些、不会红哪些:与周期 6 的结论逐字相同

W23 的对账契约随任一条周期 7 分支进主干后,**`master` 那 38 之差会立刻变成红**(变异 1 复现)。
**变异 4b、5b 实测主干 README 的 `integration` 与 `cli.smoke` 两个数字进主干后仍然不会红**——
这两个数字**第二个周期没有断言**。所以「合并会自动暴露主干数字失真」这句话在周期 7 仍要修正成
**「只暴露 unit 那一个,另两个仍需人工核」**。

## 8. 阻塞汇总与下一目标

### 8.1 W63 九条药方的处置结果(先记账)

| W63 第 8.3 节的建议 | 周期 7 处置 | 证据 |
|---|---|---|
| 1 · 先把 `w53` 的 head 补合进来,并改掉那三处散文 | **已闭合,而且两半都做了**。W67 `6b14a0f` 补合 + `9058963` 就地更正三处散文,并留 `W67 更正` 标记 | 0.1、3.2 |
| 2① · 记账件完备性断言(从集成提交抽槽号) | **未做**(grep `整合`/`槽号` 零命中)。**但 W67 补了另一条更强的索引完备性断言**,把周期 6 那路全绿变成了红——只是只封住 73/107 | 6.1、6.2 变异 8/9/10 |
| 2② · 把 `integration` / `cli.smoke` 两个数字并进现算契约 | **一个字没做,第二个周期**。变异 4b、5b 两次全绿 | 6.2 |
| 2③ · `facts` 表补反方向(源级反查余量是否真的还欠) | **已闭合,而且推广了**。SK-10/SK-11 六对翻面断言 + 三张持有者名单期望串;变异 11、12 实测会红 | 0.1、5.3、6.2 |
| 3 · 把 W50/W60 立的四条集成口径挪出记账件给落点 | **未做**。四条仍只写在记账件散文里;`AGENTS.md` 与 `docs/skills-wave/README.md` 固定位置皆无 | 3.2 |
| 4 · 集成线 head 做成可查的值 + 祖先断言 | **未做,第四个周期**。两处 grep 皆零命中。**而本周期 W72 那次漏合正是它会当场拦下的那一类** | 3.2、3.3 |
| 5 · 决定主干策略 | **未处置,第七个周期** | 7.1 |
| 6 · 给「加在既有用例里的断言」补可见量(`assert*` 调用数进契约) | **未做,第四次**。本槽实测 `assert*` 调用数 `w75` 2661 → `w80` 2749 → `w85` 2830 → `w90` **2937**,可机械统计;README 里 `assert` 零出现 | 8.2 阻塞 7 |
| 7 · 收口 G-10(先定产品口径) | **未开工,第五个周期**。九支实测 `js/release.js` 对 `Skills` 引用全为 0 | 5.4 |
| 8 · 把「按 `pending` 过滤的投影」改写而非原样重提 | **本槽照做**:不再作为待办列出,并入 8.3 第 5 条 | 5.2 |
| 9 · 两处记账卫生(① `SK-11`/`SK-10` 的 `note` 记账不实;② S-08 结论) | **①已闭合**——本槽实测 `w90` 上 `!/人设句入注册表待 G-13/.test(sk11.note)` 有断言钉住,`note` 已改写。**②仍未决,第七个周期** | 0.1、5.2 |

**九条里三条闭合(1、2③、9①)、一条按建议改写(8),五条未动(2②、3、4、5、6、7、9②)。**

**这个分布与周期 4、5、6 都不同,是四个周期里第一次有变化。**
前三个周期的定式是「技术动作全做完、口径与治理全没动」;
**周期 7 闭合的三条里,有两条(1、2③)恰恰是治理性的、而且是最难的那种——
它们要求后来的槽去改前面槽写下的判断和断言,而不是往前加东西。**
剩下五条未动的里,2②/4/6 仍是纯治理动作,3/5/7 需要先定口径。

**闭合的那三条有一个共同点,值得单独指出**:它们**都不是被核验报告"要求"后由某个专门的槽去做的**,
而是被**下游的功能槽/集成槽在自己那一轮里顺手做掉的**——
W67 补 `w53` 是它开槽时复测发现的,W68 改 `core.memoryDual` 的 `note` 是它合 W61 时撞上的,
SK-11 那条记账不实是 W66 改的。
**即:药方生效的路径不是"排一个专门的槽",而是"让下一个碰到它的人有判据可依"。**
这一条与 W63 结尾那句「口径必须有落点」是同一个意思,但周期 7 提供了它的正例。

### 8.2 本周期阻塞(按严重度)

| # | 阻塞 | 严重度 | 依据 |
|---|---|---|---|
| 1 | **主干停滞七个周期**:`master` 上 `js/skills.js` 与 `docs/skills-wave/` 均不存在,113 条分支无一进主干,最深的一支领先 **336** 提交 | 高 | 7.1 |
| 2 | **「把时点结论写成终局措辞」本周期又发作两次,一次造成两个波次的线外滞留**。W72 把 `w70` tip 记成 `1e4627c`(真 head `87aa62a` 早 6 分 12 秒),W80 才补合;W90 把 `w89-*` 记成「现取远端零匹配、不存在」(该分支两个提交都早于 W90 第二次合并) | 高 | 3.2、4.3 |
| 3 | **README 的 `integration` / `cli.smoke` 两个数字仍无任何断言,第二个周期**。变异 4b/5b 改错全绿。**本周期十七支数字全对,靠的是人守** | 高 | 6.1、6.2 |
| 4 | **索引完备性只封住 73/107**。W67 加固后「文件+索引行一起删」在多数情况会红,**但 34 份只被索引行链接过的记账件仍可静默消失**——其中包括 **`w63-cycle6-audit.md`(上一份核验报告本身)**。保护靠的是"新记账件顺手再链一次"的写作习惯,无断言 | 高 | 6.2 变异 9/10 |
| 5 | **集成线 head 指针 + 祖先断言第四个周期未做**。两处 grep 零命中。**W72 那次漏合正是它会当场拦下的**;13/13 取 head 与 13/13 基线正确全靠人守 | 高 | 3.2、3.3 |
| 6 | **G-13 余量清单有两个不一致的来源**。A 名单 6 处 / 4 文件,而 SK-10/SK-11 的仍欠段只点名其中 2 个文件;C 名单又漏 `js/proj-planner.js:2`(变异 12 实测该条不红)。**读 `note` 的人会以为只剩两处** | 中高 | 5.3、0.1 |
| 7 | **头号数字对「加在既有用例里的断言」仍无感,第四个周期**。`assert*` 调用数 `w75` 2661 → `w90` **2937** 可机械统计,README 无此口径 | 中高 | 8.1 |
| 8 | **用例名不再唯一,而无断言守它**。`w90` 跑 453 条、只有 452 个不同名字(`w84` 与 `w87` 各写了一条同名同判据的断言,W90 按并集都留下)。后果:本目录自 W21 起使用的「用例名集合比对证明没删测」在这一步会少报 1 条 | 中 | 2.2 |
| 9 | **索引表可以有重复行而契约不红**。W85 记录了「索引行在冲突块外自动合上导致同名两行」这个现象并当场清了,但契约比的是集合相等,**去重之后重复行看不出来**。本槽实测三支现无重复行 | 中 | 4.2 |
| 10 | **周期 7 零新校验项**:`CHECKS` 全程 17、面表七面、`gaps()` 20 键、`KB.SECTIONS` 18、`playbooks()` 5、pending 全程 0。增量全在提示词注册表(14 → 34 键)。**用例数与 pending 两个指标连续两个周期无区分度** | 中 | 2.3 |
| 11 | **G-10 发布门半边连续五个周期零开工**,九支实测 `js/release.js` 对 `Skills` 引用为 0。卡点是产品口径未定 | 中 | 5.4 |
| 12 | **W50/W60 立的四条集成口径仍无落点**(未进 `AGENTS.md`、未进目录索引固定位置、无断言)。**W72 违反的正是第一条** | 中 | 3.2 |
| 13 | **`tests/cli.smoke.js` 不是并行安全的**。本槽实测同一支并发跑得到 83/97 与 54/97 两种结果,串行稳定 95/97。**这会让任何批量复核者误报回归**,且无任何文档提示 | 中 | 2.1 |
| 14 | **S-08 连续七个周期未决** | 低 | 5.2 |
| 15 | **`master` 的 2 项 `cli.smoke` 失败连续七个周期未修;README unit 差 38 也未修**。变异 4b/5b 说明进主干时只有那 38 会自动报红 | 低 | 2.1、7.2 |

**一条方法性观察(承接周期 3–6 报告末尾那条;本轮第一次是正面的)。**

W63 结尾判定:「口径必须有落点——把一句已经写下来的话,换成一个会红的断言」。
**周期 7 部分验证了它,但方式和预期不同。**

预期的路径是:核验报告开药方 → 某个槽照方抓药 → 加断言。
**实际发生的是:W67 在开槽时复测发现 `w53` 漏合,顺手补合并就地更正三处散文;
W68 合 W61 时撞上 `core.memoryDual` 的假欠账,顺手改了 `note` 并补了源级反查;
W80 复测发现 `w70` 漏合,顺手补合。三条都不是"专门去做"的,是"碰上了、有判据、就做了"。**

**而没做的那五条,恰好都是"没人会在自己那一轮里碰上"的**:
`integration`/`cli.smoke` 两个数字没人碰(它们从不冲突)、集成线 head 指针没人碰(缺它也能干活)、
`assert*` 口径没人碰、主干策略与 G-10 需要产品决定。

**所以周期 7 给出的判据比 W63 那条更窄一层:落点要落在"下一个人一定会经过的地方"。**
索引契约有效,因为每个槽都要动索引;仍欠段的翻面断言有效,因为每个收编槽都要改那句 `note`;
而 `cli.smoke` 那个数字之所以两个周期没人管,**正是因为没有任何一条工作路径会经过它**。
**同理可以预测:阻塞 4 那 34 份"只被索引行链接过"的记账件、阻塞 8 那对同名用例,
如果不做成断言,就只会在下一份核验报告里再出现一次——因为日常路径不经过它们。**

### 8.3 下一目标(建议次序;本槽不执行)

1. **先把四条收编 tip 并线,一次把 G-13 的 A 名单清零。这是七个周期里第一次有缺口具备"这一波就能闭合"的条件。**
   `w88`(`js/experts.js:2`)、`w89`(`js/plans.js:1`)、`w91`(`js/agent-global.js:1`)、`w92`(`js/proj-planner.js:2`)
   四条**各收一处、正好覆盖 A 名单全部 6 处**。冲突面本槽实测:7 / 9 / 9 / 10 处,
   共用文件是 `js/prompts.js`、`js/skills.js`、`tests/unit.js`、`README.md`、`docs/skills-wave/README.md`——
   **四条彼此必然冲突,要按次序逐条解,不能指望自动合。**
   合到最后一条时,SK-10 / SK-11 的仍欠段会**同时失去它点名的两个文件**,
   三张持有者名单的期望串会**同时变空**——**那一刻才轮到"摘 G-13 标记 + 一次改齐六条关联索引"这个动作**。
   **顺手把 `w94` 也合了**(1 处索引冲突,零代码),它是五条里成本最低的一条。
   **合之前请先 `fetch`——本槽核验时 `w88` 的真 head 是 `f4525ce` 而不是 `w90` 记的 `78feca2`,`w89` 确实存在**(4.3)。
2. **把 `integration` / `cli.smoke` 两个数字并进 W23 那套现算契约。第二次提,而且这次有反例价值。**
   变异 4b、5b 实测改错这两个数字**全绿**。
   **本周期十七支数字全对这件事不能当作理由跳过它**——周期 6 是 3/14 错、周期 7 是 0/17 错,
   同一个洞、不同的人,**差别只是运气**。做法与 unit 那条完全同形(由套件表求和现算)。
   `cli.smoke` 那条要注意 2.1 说的并行不安全问题,**现算时须串行取数**。
3. **把索引完备性那条反向断言从"靠链接"改成"靠槽号"。**
   现状:73/107 有保护,34/107 无保护,而无保护的那批里有上一份核验报告(6.2)。
   两种做法,建议第二种:
   ① 弱化版——断言 `docs/skills-wave/README.md` 的「最新收敛记录」与「记账诚实位」两段必须存在且各链到指定文件
   (把写作习惯变成不变量,成本最低,但只保住新的);
   ② **推荐**——W63 药方 2① 那条:**从集成线的「整合」提交信息里抽槽号,断言每个槽号在 `docs/skills-wave/` 下都有对应记账件**。
   本槽实测 live 完备性目前是满的(`w62`–`w87` 26 个槽号一份不缺),**所以这条现在加上去就是绿的,加的成本最低**。
4. **把集成线 head 做成可查的值 + 祖先断言。第四次提,但本周期的证据比前三次都硬。**
   ① `docs/skills-wave/README.md` 固定位置写一行「当前集成线:`<分支名>` head `<SHA>`」;
   ② 断言:该 SHA 必须是当前分支的祖先。
   **W72 那次漏合会被②当场拦下**——它把 `w70` 的 tip 记成 `1e4627c`,而 `87aa62a` 不是它的祖先。
   **本周期还多了一条新用法**:同一条断言也能拦住 4.3 那种「把存在的分支记成不存在」——
   只要把"剩余未合"那一栏也做成现取而非手写。
5. **给「余量清单」定一个单一来源,并让 `note` 的仍欠段由它反推。**
   这条取代 W63 药方 8(按 `pending` 过滤的投影,已因 pending 恒为 0 而过期)。
   现状是三张名单三个数(6 / 5 / 2)加一段只点了 2 个文件的 `note`(5.3)。
   建议:以 A 名单判据为准,C 名单并进去(补 `content:` 形态),
   并加一条断言「SK-10 / SK-11 仍欠段点名的文件集合 = A 名单的文件集合」——
   **这样第 1 步四条 tip 合完时,漏改任何一处都会当场红。**
6. **决定主干策略。这是第 1 阻塞,已是连续七个周期的同一条结论。**
   技术面无障碍(113 条分支全部纯快进、零冲突)。拓扑上的天然切点:
   `w6`(周期 1)、`w9`(周期 2)、`w21`(周期 3)、`w34`(周期 4)、`w47`(周期 5)、
   `w60`/`w62`(周期 6)、第 1 步做完的那个 `w9x`(周期 7 全量)。
   进主干那一刻 W23 的契约会把 `master` 那 38 之差判红(变异 1 复现),
   **但 `integration` / `cli.smoke` 两个数字与那 2 项冒烟失败需要人工一并清掉**(7.2)。
7. **两条低成本的记账卫生,合流时顺手做**:
   ① 加一条**用例名唯一性**断言,并把 `w84`/`w87` 那对同名用例合成一条
   (两份判据本槽逐字比对**实质相同**,只有 `>= 26` 与 `>= 20` 之差,保严的那份即可)——阻塞 8;
   ② 索引表**重复行**检查(集合比对之前先 `uniq -d`)——阻塞 9,W85 撞到过一次。
8. **把 `tests/cli.smoke.js` 的并行不安全写进 `docs/skills-wave/README.md` 的复核须知**(阻塞 13)。
   本槽实测并发跑会得到 83/97、54/97 这类假红,**任何批量复核者都会踩**。
   如果要做成断言,更彻底的修法是让它按 pid 取端口与配置目录;但那是产品改动,不在核验槽的范围。
9. **G-10 与 S-08 两条老账**:G-10 第五个周期零开工,卡在「方法论门要不要挂成默认 `warn` 的可选门」这个产品决定;
   S-08 第七个周期未决。**这两条已经不该再进下一份核验报告的待办栏,应该进一次产品决策。**

## 9. 本报告的复核方式

全部数字可按下述步骤独立重跑(本槽即如此取得;`/tmp` 下的探针脚本、只读克隆与变异副本**均已删除,不入仓库**)。

```bash
# 0. 锚定:十八支 head 与相对 master 的领先数
for b in w70-integration-ad31 w72-integration-8d3f w75-integration-c4a7 w80-integration-5369 \
         w81-gsettings-prompt-9f2e w82-proj-shell-prompt-6f73 w83-proj-upload-prompt-00b3 \
         w84-sb-views-prompt-2934 w85-integration-171f w86-agent-ops-prompt-8d6f \
         w87-role-editor-prompt-9c2a w88-experts-forge-prompt-a3f7 w89-plans-prompt-835b \
         w90-integration-9004 w91-intent-router-prompt-6ea6 w92-proj-planner-prompt-b845 \
         w94-persona-remainder-prompt-3f1f; do
  printf "%-42s %s ahead=%s\n" "$b" "$(git rev-parse --short=8 origin/cursor/$b)" \
    "$(git rev-list --count origin/master..origin/cursor/$b)"
done
git branch -r | grep -cE 'w9[35]-'      # 0 ← W93/W95 远端零引用(第 4.9 节)

# 1. 各支独立工作树,互不干扰
for b in w70-integration-ad31 w72-integration-8d3f w73-voice-director-prompts-b4b4 \
         w74-digest-three-prompts-3508 w75-integration-c4a7 w76-sb-board-prompts-c5a1 \
         w77-event-graph-prompt-fd01 w78-beatboard-prompt-ea0c w79-editors-prompt-153a \
         w80-integration-5369 w81-gsettings-prompt-9f2e w82-proj-shell-prompt-6f73 \
         w83-proj-upload-prompt-00b3 w84-sb-views-prompt-2934 w85-integration-171f \
         w86-agent-ops-prompt-8d6f w87-role-editor-prompt-9c2a w88-experts-forge-prompt-a3f7 \
         w89-plans-prompt-835b w90-integration-9004 w91-intent-router-prompt-6ea6 \
         w92-proj-planner-prompt-b845 w94-persona-remainder-prompt-3f1f; do
  git worktree add --detach /tmp/wt/$b origin/cursor/$b
done
git worktree add --detach /tmp/wt/master origin/master

# 2. 逐支复跑(第 2 节那张表)
cd /tmp/wt/w90-integration-9004
node tests/unit.js          # 453/453
node tests/integration.js   # 126/126
node tests/cli.smoke.js     # 95/97

# 2a. ⚠️ cli.smoke 必须串行:并发跑会得到 83/97、54/97 这类假红(第 2.1 节)
for d in master w75-* w80-* w85-* w90-* w88-* w89-* w91-* w92-* w94-*; do
  (cd /tmp/wt/$d && node tests/cli.smoke.js 2>&1 | grep -E '^FAIL|通过'); sleep 3; done
#   master 51/53,其余十支一律 95/97;FAIL 两条逐字相同:
#   未登录 whoami → exit 3 | exit=1   /   llm --json mock 链路 | undefined

# 3. 注册表状态直读数据,不读文档(第 2.3 节)
node -e "const S=require('./js/skills.js');const K=require('./js/knowledge.js');
  const P=require('./js/prompts.js');const p=S.REG.filter(x=>x.pending.length);
  console.log('SK',S.REG.length,'|CHECKS',Object.keys(S.CHECKS).length,
    '|KB',Object.keys(K.SECTIONS).length,'|gaps',Object.keys(S.gaps()).length,
    '|Prompts',P.list().length,'|面',S.preflightStages().length,'|pb',S.playbooks().length,
    '|pending',p.length+'条/'+p.reduce((n,x)=>n+x.pending.length,0)+'面');
  console.log('出口:',Object.keys(S).filter(k=>typeof S[k]==='function').sort().join(','));"
# w90 应回:SK 30 | CHECKS 17 | KB 18 | gaps 20 | Prompts 34 | 七面 | pb 5 | pending 0条/0面
# 出口列表应与周期 5、6 逐字相同(仍无按 pending 过滤的投影)

# 4. README 四个数字对账(注意后两个没有断言保护)
for b in w75-integration-c4a7 w80-integration-5369 w85-integration-171f w90-integration-9004 \
         w88-experts-forge-prompt-a3f7 w89-plans-prompt-835b w91-intent-router-prompt-6ea6 \
         w92-proj-planner-prompt-b845 w94-persona-remainder-prompt-3f1f; do
  git show origin/cursor/$b:README.md > /tmp/r.md
  printf "%-42s unit=%s intg=%s smoke=%s prompts=%s\n" "$b" \
    "$(grep -oE '单元测试\([0-9]+' /tmp/r.md | grep -oE '[0-9]+' | head -1)" \
    "$(grep -oE 'W61 扩至 [0-9]+ 项断言' /tmp/r.md | grep -oE '[0-9]+' | head -1)" \
    "$(grep -oE '冒烟\(W61 扩至 [0-9]+' /tmp/r.md | grep -oE '[0-9]+' | head -1)" \
    "$(grep -oE '[0-9]+ 条注册表提示词' /tmp/r.md | grep -oE '[0-9]+' | head -1)"
done
#   十七支应逐个等于第 2 节表里的 live 实测值 —— 本周期零失真

# 5. 集成槽 13 次合并:parent2 是否 = 被合入支 head,且是否真三方合并(第 3.1 节)
for m in f7bb0321 57cd89eb 5d2b35f0 960c0929 c8bc2b78 \
         8a075a59 2a35d496 53527867 f7573370 \
         c914e39d 4376623a 8ee51385 bc9a38e4; do
  git merge-base --is-ancestor $m^1 $m^2 && echo "$m 本可 ff" || \
    echo "$m 真三方  p2=$(git rev-parse --short=8 $m^2)"
done
#   十三次都应回"真三方",且 p2 逐个等于 3.1 表里那一列

# 6. W53 / W70 两处漏合是否已补(第 3.2 节)
for t in w70-integration-ad31 w72-integration-8d3f w75-integration-c4a7 \
         w80-integration-5369 w85-integration-171f w90-integration-9004; do
  printf "%-28s w53=%s w70=%s\n" "$t" \
    "$(git merge-base --is-ancestor 00459625 origin/cursor/$t && echo IN || echo OUT)" \
    "$(git merge-base --is-ancestor 87aa62a5 origin/cursor/$t && echo IN || echo OUT)"
done
#   w53 应六支全 IN(W67 已补);w70 应在 w72/w75 上 OUT、w80 起 IN(W80 才补)
grep -rc '没有留下自己的记账件' /tmp/wt/w90-integration-9004/docs/skills-wave/*.md | grep -v ':0'
#   仍有命中,但逐处应都在「W67 更正」的语境里 —— 逐条读一遍,不要只看计数

# 7. 功能槽基线 = merge-base(合并提交两侧);在外 tip = merge-base(该支, w90)
for m in 57cd89eb 5d2b35f0 960c0929 c8bc2b78 8a075a59 2a35d496 \
         53527867 f7573370 c914e39d 4376623a 8ee51385 bc9a38e4; do
  echo "$m 基线 $(git rev-parse --short=8 $(git merge-base $m^1 $m^2))"; done
#   87aa62a5=w70 head / d2e7c43d=w72 head / fbefd0c2=w75 head / 4c45f89d=w80 head
for fs in w88-experts-forge-prompt-a3f7 w89-plans-prompt-835b w91-intent-router-prompt-6ea6 \
          w92-proj-planner-prompt-b845 w94-persona-remainder-prompt-3f1f; do
  echo "$fs 基线 $(git rev-parse --short=8 $(git merge-base origin/cursor/$fs origin/cursor/w90-integration-9004))"; done
#   w88/w89 → 4c45f89d = w80 head;w91/w92/w94 → 2a05c725 = w85 head

# 7a. 时点链(第 3.2、4.3 节;仓库只留 commit 时点,push 时点不可查)
for c in 1e4627c 87aa62a ddc8994 d2e7c43 fbefd0c f7bb032 4c45f89 2a05c72 \
         331be96 78feca2 f4525ce ae9ade0 77e0ff8 c914e39 bc9a38e 35695c8 77a9916; do
  printf "%-9s %s  %s\n" "$c" "$(git log -1 --format=%cI $c)" "$(git log -1 --format=%s $c | cut -c1-44)"; done
#   87aa62a 22:10:18 早于 ddc8994 22:16:19 → W72 那句「w70 tip = 1e4627c」是时点结论
#   f4525ce 00:08:30 早于 c914e39 00:09:36;77e0ff8 00:11:38 早于 4376623 00:14:39
#   → W90 那句「w89 不存在 / w88 tip 78feca2」在写下时已不成立

# 8. 是否又裂成两条集成线(第 3.5 节)
for b in w62-integration-9c4e w64-integration-23c3 w67-integration-a5c1 w68-integration-4e71 \
         w70-integration-ad31 w72-integration-8d3f w75-integration-c4a7 \
         w80-integration-5369 w85-integration-171f; do
  printf "%-28s %s\n" "$b" "$(git merge-base --is-ancestor origin/cursor/$b origin/cursor/w90-integration-9004 && echo IN || echo OUT)"; done
#   九条应全 IN → w90 是唯一集成线 tip
git rev-parse --short=8 $(git merge-base origin/cursor/w70-integration-ad31 origin/cursor/w68-integration-4e71)
#   52965b5 ← 两条集成线曾在此分叉(W72 收成一条)

# 9. 用例增量相加 = 合并后 live(第 4.1–4.3 节)
#   w80: 428 +0(w70 记账件)+1(w73)+2(w74)+2(w76)+2(w77) = 435
#   w85: 435 +2(w78)+1(w79)+2(w81)+3(w82)             = 443
#   w90: 443 +2(w83)+3(w84)+2(w86)+3(w87)             = 453
# 用例名集合双向对照(注意 w90 有一对重名,集合口径会少报 1 条 —— 第 2.2 节)
names(){ (cd /tmp/wt/$1 && node tests/unit.js | grep -E '^(PASS|FAIL) \| ' \
  | sed 's/^[A-Z]* | //; s/ | .*$//' | LC_ALL=C sort); }
names w85-integration-171f > /tmp/a; names w90-integration-9004 > /tmp/b
wc -l < /tmp/b; LC_ALL=C sort -u /tmp/b | wc -l   # 453 与 452 ← 差 1,重名
LC_ALL=C sort /tmp/b | uniq -d
#   应回:注册表全仓持有者名单:每条 def 的字面持有者恰好只有 js/prompts.js(谁在别处抄第二份即红)
grep -nF '注册表全仓持有者名单:每条 def' /tmp/wt/w90-integration-9004/tests/unit.js   # 4970 与 5498

# 10. 索引双向对账 + 完备性边界(第 6.2 节)
for b in w80-integration-5369 w85-integration-171f w90-integration-9004; do
  git show origin/cursor/$b:docs/skills-wave/README.md | grep -oE '\bw[0-9]+[a-z0-9-]*\.md' | sort -u > /tmp/i
  git ls-tree -r --name-only origin/cursor/$b -- docs/skills-wave \
    | sed 's#docs/skills-wave/##' | grep -v '^README.md$' | sort > /tmp/f
  echo "$b: $(wc -l < /tmp/i) 行 / $(wc -l < /tmp/f) 份"; LC_ALL=C comm -3 /tmp/i /tmp/f
  git show origin/cursor/$b:docs/skills-wave/README.md | grep -oE '^\| \[[^]]+\]' | sort | uniq -d
done
#   三支应各自零差、无重复行(97/97、102/102、107/107)
cd /tmp/wt/w90-integration-9004 && node -e "
const fs=require('fs'),p=require('path'),d='docs/skills-wave';
const all=fs.readdirSync(d).filter(f=>f.endsWith('.md')),c={};
all.forEach(f=>[...fs.readFileSync(p.join(d,f),'utf8')
  .matchAll(/\]\(\.\/([A-Za-z0-9._-]+\.md)\)/g)].forEach(m=>c[m[1]]=(c[m[1]]||0)+1));
const w=all.filter(f=>/^w\d+-/.test(f)), v=w.filter(f=>(c[f]||0)<=1);
console.log('记账件',w.length,'| 只被索引行链接过(可静默消失)',v.length);
console.log('其中周期 7 范围:',v.filter(f=>/^w(6[2-9]|[7-9]\d)-/.test(f)).join(', '));"
#   应回 107 / 34,且周期 7 那两份是 w63-cycle6-audit.md、w73-voice-director-prompts.md

# 11. 集成线 head 指针与祖先断言:两处仍应零命中(药方 4 未做)
git show origin/cursor/w90-integration-9004:docs/skills-wave/README.md \
  | grep -niE '当前集成线|集成线 *head|集成线指针'
cd /tmp/wt/w90-integration-9004 && grep -rniE '集成线|is-ancestor|祖先' tests/*.js
grep -nE '整合|槽号' tests/unit.js       # 零命中 ← 记账件完备性断言仍未做

# 12. G-13 余量:三张名单三个口径,并列取数(第 5.3 节)
cd /tmp/wt/w90-integration-9004 && node -e "
const fs=require('fs');
const F=fs.readdirSync('js').filter(f=>f.endsWith('.js')).map(f=>'js/'+f)
  .concat(['server.js','cli.js','mcp.js','billing.js']).sort();
const L=(re,skip)=>F.filter(f=>!skip.includes(f))
  .map(f=>[f,(fs.readFileSync(f,'utf8').match(re)||[]).length]).filter(x=>x[1])
  .map(x=>x[0]+':'+x[1]).join(', ');
console.log('A:',L(/(?:system\s*:|content\s*:|=)\s*['\`]你是/g,['js/prompts.js','js/experts-data.js']));
console.log('C:',L(/(?:system:\s*|return\s*|(?:const|let|var)\s+[A-Za-z_\$][\w\$]*\s*=\s*)(?:'|\`)你是/g,['js/prompts.js']));
console.log('D:',L(/system: ['\`]你是/g,['js/prompts.js']));
console.log('宽:',L(/['\`\"]你是/g,[]));"
#   A = agent-global:1, experts:2, plans:1, proj-planner:2   (6 处 / 4 文件)
#   C = agent-global:1, experts:2, plans:1, wf-core:1        (5 处;漏 proj-planner)
#   D = experts:1, plans:1                                    (仍欠段反向断言实际覆盖到的)
node -e "const S=require('./js/skills.js');
  ['script.aiToneBan','subjects.refDiscipline','core.memoryDual'].forEach(id=>{
    const s=S.byId(id);console.log(id,JSON.stringify(s.gaps));
    console.log('  仍欠:',(s.note||'').split('仍欠').slice(1).join('仍欠').slice(0,180));});"
#   前两条的仍欠段只点名 experts / plans 两个文件 ← 与 A 名单的 4 个文件不一致(阻塞 6)
#   core.memoryDual 应已改写为「生成与合成两步…」← 周期 6 的假欠账已修

# 13. 断言强度的变异测试(在副本上做,勿在工作树上改;第 6.2 节十二路)
cp -r /tmp/wt/w90-integration-9004 /tmp/mut && cd /tmp/mut && rm -f .git
cp README.md /tmp/README.bak && cp -r docs/skills-wave /tmp/sw.bak
node tests/unit.js                                                    # 基线 453/453
perl -0pi -e 's/单元测试\(453 项断言/单元测试(452 项断言/' README.md
node tests/unit.js   # 红:README 数字对账(单元测试用例数)
cp /tmp/README.bak README.md
perl -0pi -e 's/CHECKS 已落地十七条//' README.md
node tests/unit.js   # 红:架构框 CHECKS 条数
cp /tmp/README.bak README.md
perl -0pi -e 's/34 条注册表提示词/33 条注册表提示词/' README.md
node tests/unit.js   # 红:注册表提示词数
cp /tmp/README.bak README.md

# 【无断言的两个数字:改错全绿】
perl -0pi -e 's/W61 扩至 126 项断言/W61 扩至 105 项断言/' README.md
node tests/unit.js   # 绿 453/453 ← integration 数字无断言
cp /tmp/README.bak README.md
perl -0pi -e 's/CLI 真实服务端冒烟\(W61 扩至 97/CLI 真实服务端冒烟(W61 扩至 64/' README.md
node tests/unit.js   # 绿 453/453 ← cli.smoke 数字无断言
cp /tmp/README.bak README.md

# 【索引完备性:W67 加固的边界】
rm -f docs/skills-wave/w87-role-editor-prompt.md
node tests/unit.js   # 红 451/453(双向对齐 + 完备性各一条)
cp -r /tmp/sw.bak/. docs/skills-wave/
perl -ni -e 'print unless /\]\(\.\/w87-role-editor-prompt\.md\)/' docs/skills-wave/README.md
node tests/unit.js   # 红 451/453 ← 注意这会删掉 README 里两处链接
cp -r /tmp/sw.bak/. docs/skills-wave/
rm -f docs/skills-wave/w87-role-editor-prompt.md
perl -ni -e 'print unless /^\| \[w87-role-editor-prompt\.md\]/' docs/skills-wave/README.md
node tests/unit.js   # 红 452/453「README.md → w87-…(文件不存在)」← 周期 6 这一路是全绿的
cp -r /tmp/sw.bak/. docs/skills-wave/
rm -f docs/skills-wave/w73-voice-director-prompts.md
perl -ni -e 'print unless /\]\(\.\/w73-voice-director-prompts\.md\)/' docs/skills-wave/README.md
node tests/unit.js   # ❗绿 453/453 ← 只被索引行链接过的那 34 份仍可静默消失
cp -r /tmp/sw.bak/. docs/skills-wave/
rm -f docs/skills-wave/w63-cycle6-audit.md
perl -ni -e 'print unless /\]\(\.\/w63-cycle6-audit\.md\)/' docs/skills-wave/README.md
node tests/unit.js   # ❗绿 453/453 ← 上一份核验报告本身就在那 34 份里
cp -r /tmp/sw.bak/. docs/skills-wave/

# 【假欠账方向:已被接住】
cp js/agent-global.js /tmp/ag.bak
perl -0pi -e 's/const sys = `你是意图路由器。/const sys = X + `/' js/agent-global.js
node tests/unit.js   # 红 450/453(三张全仓名单各一条)← 模拟 W91 落地不改记账
cp /tmp/ag.bak js/agent-global.js
cp js/proj-planner.js /tmp/pp.bak
perl -0pi -e "s/content: '你是资深短剧策划/content: X + '/; s/content: \`你是资深短剧出海本土化译制专家/content: Y + \`/" js/proj-planner.js
node tests/unit.js   # 红 451/453(只两条;C 名单那条不红 ← 阻塞 6 的口径裂缝)
cp /tmp/pp.bak js/proj-planner.js
node tests/unit.js   # 恢复后应回 453/453

# 14. 五条在外 tip 的合流成本(只读克隆,不落提交到本仓库引用)
git clone --shared . /tmp/sim && cd /tmp/sim
git fetch /workspace "+refs/remotes/origin/cursor/*:refs/w/*"
for b in w88-experts-forge-prompt-a3f7 w89-plans-prompt-835b w91-intent-router-prompt-6ea6 \
         w92-proj-planner-prompt-b845 w94-persona-remainder-prompt-3f1f; do
  echo "== w90 ← $b"
  out=$(git merge-tree --write-tree refs/w/w90-integration-9004 refs/w/$b); t=$(echo "$out" | head -1)
  for f in $(echo "$out" | awk '$1~/^100/{print $4}' | sort -u); do
    n=$(git show $t:$f 2>/dev/null | grep -c '^<<<<<<<'); [ "$n" -gt 0 ] && echo "   $f: $n 处"
  done
done
#   w88 → 7 处 / w89 → 9 处 / w91 → 9 处 / w92 → 10 处 / w94 → 1 处(第 3.4 节)

# 15. assert* 调用数(阻塞 7;README 无此口径)
for d in /tmp/wt/w75-* /tmp/wt/w80-* /tmp/wt/w85-* /tmp/wt/w90-* /tmp/wt/w94-*; do
  echo "$(basename $d): $(grep -oE '\bassert[A-Za-z]*\(' $d/tests/unit.js | wc -l)"; done
#   w75 2661 / w80 2749 / w85 2830 / w90 2937 / w94 2830(= 其基线 w85,净 0)
grep -c assert /tmp/wt/w90-integration-9004/README.md    # 0 ← README 里没有这个口径

# 16. G-10 与主干(第 5.4、7.1 节)
for d in /tmp/wt/w75-* /tmp/wt/w80-* /tmp/wt/w85-* /tmp/wt/w90-* /tmp/wt/w88-* \
         /tmp/wt/w89-* /tmp/wt/w91-* /tmp/wt/w92-* /tmp/wt/w94-*; do
  echo "$(basename $d): release.js×Skills = $(grep -c Skills $d/js/release.js)"; done   # 九支全 0
git log --oneline origin/master | wc -l                          # 8
git cat-file -e origin/master:js/skills.js 2>/dev/null || echo "master 无 js/skills.js"
git ls-tree origin/master docs/skills-wave | wc -l               # 0
git branch -r | grep -v 'origin/HEAD' | grep -v 'origin/master$' | wc -l   # 113
```

> 提醒:**若复核时点晚于本报告,请先比对 head SHA 再采信数字**——
> 尤其五条在外的 tip(`w88` / `w89` / `w91` / `w92` / `w94`,随时可能被下一个集成槽合入,
> 届时第 2 节、3.3、3.4、5.3 与 8.3 第 1 步会整体变化),
> 以及 **`w93` / `w95`(本槽核验时远端零引用,状态未知)**。
> 本报告已按 W50 立的口径,把所有「不存在」都写成了带时点的结论——
> **这正是本周期两次失真(W72 对 `w70`、W90 对 `w89`)的位置。**

`node tests/e2e.js` 按仓库纪律**未跑**(需用户明确要求)。
本轮未开 PR、未合并任何分支、未改动任何业务代码;本分支的唯一改动是本文件。

> **未改目录索引的声明**:本文件**没有**登进 `docs/skills-wave/README.md` 的索引表。
> 理由是那张表由两条契约断言双向钉住——
> `contract · docs/skills-wave 索引与目录实况双向对齐` 与
> `contract · docs/skills-wave 索引完备性:每份 wNN-*.md 各有自己的索引行,散文点到的记账件不许悬空`。
> 只加文件不加索引行会红、只加索引行不加文件也会红(本槽变异 6、7 实测,见 6.2)。
> 本槽按任务口径**只提交本报告一个文件**,故索引侧必然缺这一行,`node tests/unit.js` 在本分支上
> **会报这两条断言失败(451/453)**,这是**预期结果、如实登记**,不是回归。
> **本槽有意不动索引**:改了索引就要改契约的另一侧,而那属于业务改动。
> 接手的槽把本文件合入时,请**顺手补上索引那一行**(按波次序排在 `w90-integration-log.md` 之后),补完即绿。
> **顺带记一条本轮新量出来的事**:本槽这一处「文件在、索引行不在」**会红两条**(周期 6 只红一条),
> 而 6.2 变异 9/10 那处「文件与索引行一起不在、且没有别处链接点过它」**仍然全绿**
> ——两者放在一起,就是 W67 那条加固**封住了什么、又漏下了什么**的准确形状。
