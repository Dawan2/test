# W278 集成日志:W277 `gen-episode` 分报 ok/landed 并进 W276 尖(集成线上第九条「快进形」)

我方尖:`origin/cursor/w276-integration-3f7c` **现取** `49033fb`(先 `fetch` 再 `rev-parse`)。
对侧:`origin/cursor/w277-landed-audit-ec5d` **现取** `49b1905`。
W277 自称从 w276 尖 `49033fb` 起支、HEAD 自称 `49b1905`,两处**以 `rev-parse` 为准**核过,与自称逐字节相同。
新支 `cursor/w278-integration-b345` 从 w276 现取 HEAD 拉出,`checkout -b` 后立刻 `rev-parse`,与 `49033fb` 相同。

**结论先写:一次 `--no-ff`,零冲突。合完 live 跑出 `unit` 696 / `contract` 153 / `commands` 59 /
`domain` 44 / `skills` 104 / `issues` 22 / `integration` 152 / `cli.smoke` 115-117,`gaps()` 20 键一个没剥,
W277 八条残留一条没代修。本槽是集成线上第九条快进形,`tree(M)` 与 `tree(P2)` 逐字节相同、
`P1→M` 与 `B→P2` 的 numstat 逐行相同——这两格照旧**恒真**,一格结论都不承载,故本文的证据全在变异与 live 上。
本槽新量出来的三格是:①**棘轮缓冲被对侧主动收掉之后,W276 §六 11 那格残留当场消失**——
对侧把 `['单元测试', 696]` 抬到了 live(差额 0),于是「抄回旧数字」这一反事实从 W276 那次的**红 1** 回到
**红 2**,而「真删掉新用例 + 文档按 live 改小」(CF2)也从 W276 那次的**全绿**变成**红 1**(§五);
②**CF2 与 M8 红的都只是数数那一层,产品语义面一条不响**——`landed` 全族(对侧两条 + 本槽这条共三条命令)
**零源级判据**,删掉那条行为面用例之后把 `landed` 整格退回去,`commands`/`skills`/`domain` 各红 0,
只剩 README 对账与棘轮下限两句响(§四 M8);③**「座位只在本轮真成功后才进集合」这半边两端都没有判据**
——把座位提前到开跑时登记,`gen-episode`(M10)与 `exec episode.generateVideos`(M11)**各红 0**,
这是 W254 那两条就带着、对侧原样继承的洞,如实新记成残留 9。**

## 一、拓扑(现取,不抄自称)

| 位 | 现取 SHA |
|---|---|
| `B` 合并基(`merge-base P1 P2`) | `49033fbec14dd1a478efd1052ddadbb891cad0d5` |
| `P1` 我方尖(w276) | `49033fbec14dd1a478efd1052ddadbb891cad0d5` |
| `P2` 对侧尖(w277) | `49b190503e7307f6d8c8ec71316bf240f15c52a9` |
| `M` merge commit | `d69357d56b512e479064420a7cac67fdb34468f9` |

`git merge-base --is-ancestor P1 P2` 成立,`B` 与 `P1` **提交级相等**,即典型「快进形」——
本可 `--ff` 掉,仍按纪律走 `--no-ff` 留住合点。`P1..P2` 两条提交:

| 现取 SHA | 是什么 |
|---|---|
| `839ea44…` | 产品:`CMD['gen-episode']` 回执分开报 `ok` 与 `landed`,并补 `loadCli()` 的 `CMD` 取值口 + 1 条新用例 |
| `49b190503e7307f6d8c8ec71316bf240f15c52a9` | 记账件 + 索引一行,数字按 live 校准 |

「自称尖是不是链尾」这一格现取核过:`49b1905` 就是链尾。

树面:`tree(P1)` `b8bb2a9`、`tree(P2)` = `tree(M)` = `2cf3bd4`(逐字节相同);
`P1→M` 与 `B→P2` 的 `--numstat` 逐行相同(五个文件:`README.md` 2/2、`cli.js` 17/2、
`docs/skills-wave/README.md` 2/1、`docs/skills-wave/w277-landed-gen-episode.md` 163/0、`tests/unit.js` 51/3)。
**这两格在快进形下是恒真的**,写在这儿只是留档,不当证据用。

## 二、并集面(README / GUARD / roster / 索引)

快进形下 `P2` 的文档本就含着 `P1` 的全部内容,故并集不靠手工拼:
`docs/skills-wave/README.md` 索引表里 w276 那行与 w277 那行**同时在册**,现取核过。
数字一律 live,**W276 自称的 695 一个字没抄**(反事实见 §五):

| 面 | `P1` 基线现取 | `M` 合完现取 | 差 |
|---|---|---|---|
| `unit` | 695 | **696** | +1(全部来自对侧) |
| `contract` | 153 | **153** | 0 |
| `commands` | 58 | **59** | +1 |
| `domain` | 44 | **44** | 0 |
| `skills` | 104 | **104** | 0 |
| `issues` | 22 | **22** | 0 |
| `integration` | 152 | **152** | 0 |
| `cli.smoke` | 115/117 | **115/117** | 0 |
| 记账件 | 291 | **293**(w277 一份 + 本文) | +2 |

`P1` 基线是在 `49033fb` 的独立 worktree 上现跑出来的,不是拿合完的树掐掉几行凑的——
第一次对账时误用了「合完的 `README.md` + `P1` 的 `tests/unit.js`」这种混树,
那道 README 对账判据当场按混树报红(实测 696、文档 695),`wc -l` 于是数出 694 而总数 695;
换成干净 worktree 之后 695/695 全绿。**这一步值得记:名集对账的基线必须整树取,不能只换一个文件。**

GUARD / roster 现取四者对齐、本槽与对侧都没登记新护栏主题:
`GUARD_TOPICS` **19** / `GUARD_TOPICS_CLOSED` **0** / `TOPIC_FLOOR` **19** /
花名册(`w178-topic-floor-unlist.md` 的花名册节,按判据同一条正则现取)**19**。
`SLACK` **3** 一格没用掉——五个 `FLOOR` 格现取差额 **0 / 0 / 0 / 0 / 0**
(`单元测试` 696=696、`集成测试` 152=152、`CLI 冒烟` 117=117、`记账件份数` 293=293、`护栏主题` 19=19)。
记账件那格本槽按集成槽惯例抬到 live(291 → **293**),`docs/skills-wave/README.md` 的明写份数同轮改齐。

## 三、快进形的空集不能当没丢东西

`git diff M P2` 是空集、`tree(M) == tree(P2)`,这两件事在 `B == P1` 时**由构造保证**,
即无论合入者做了什么(哪怕 `--ours` 或 `checkout <old> -- .` 之后再补一次)都推不出「一格没丢」。
故本槽的取证分三路:产品语义逐格现取、名集多重集对账、变异。

### 产品语义现取核过(§合入纪律 4)

| 项 | 现取实况 |
|---|---|
| `gen-episode` 回执带 `landed` | `cli.js:932` 初值 0、`cli.js:971` 由 `seats.size` 现算 |
| 空跑那一路带 `landed: 0` | `cli.js:930`,在场 |
| 岔开时那句话读单源 | `cli.js:973` `Domain.landedNote(result.ok, result.landed, '行')`,不另造第二份说法 |
| 写回路径仍 `findShot` 取首行 | `cli.js:946`,明令未改 |
| 座位键带 id | `cli.js:947` `sLive.id + '#' + …indexOf(sLive)` |
| `js/` 相对对侧 | **零 diff**(`git diff M P2 -- js/` 空集) |
| `js/` 相对起点 | **零 diff**——产品面只动 `cli.js` 一个文件 |
| `cli.js` 相对对侧 | **零 diff**(逐字节相同,没在合入时被改写) |
| `cli.js` 的 `findShot` / `nthShot` / `nthSubject` | **17** / **6** / **3** 处,与对侧逐格相等 |
| `js/produce.js` / `js/commands.js` / `js/domain.js` / `js/billing.js` / `js/review.js` | 相对起点零 diff |

### 名集多重集对账

`P1` 干净 worktree 现跑 695 条名、`M` 现跑 696 条名,逐条比对:

- 只在 `M`:**1 条** —— `commands · CLI gen-episode 回执同样分开报 ok 与 landed:同 id 多行全写在首行时,扣了几笔与到手几行不再混作一个数`
- 只在 `P1`:**0 条**
- `695 + 1 − 0 = 696`,与 live 自洽。

### 三棵树关键词点数

十四个关键词逐格点数,`M` 与 `P2` **相等**且无一格掉到 `P1` 之下:

| 关键词 | `P1` | `P2` | `M` |
|---|---|---|---|
| `landed` | 222 | 319 | **319** |
| `landedNote` | 44 | 59 | **59** |
| `gen-episode` | 19 | 54 | **54** |
| `findShot` | 78 | 94 | **94** |
| `nthShot` | 108 | 120 | **120** |
| `seats` | 25 | 28 | **28** |
| `dupRowsNote` | 125 | 126 | **126** |
| `shots-dedupe` | 199 | 203 | **203** |
| `state-put` | 198 | 201 | **201** |
| `shotNo` | 241 | 242 | **242** |
| `rowIndexOf` | 87 | 87 | **87** |
| `nthOf` | 30 | 30 | **30** |
| `reviseTargets` | 215 | 215 | **215** |
| `staleShotSplit` | 86 | 86 | **86** |

变的几格全是对侧净增;后四格一格没动。
残留锚点现取复核:`js/batchops.js` 的 `.order + 1` 仍是 **17 行**、相对 `P1` 逐字节未动;
`js/produce.js` 与 `js/issues.js` 的 `.order + 1` 各 **0** 处。

## 四、变异(逐手在本支上做、跑完 `git checkout` 还原,`git status --porcelain` 复核 clean)

基线三套件 `commands` 59 / `contract` 153 / `domain` 44。

| 手 | 退法 | 结果 |
|---|---|---|
| M1 | `result.landed = seats.size` → `= result.ok`(抄 `ok` 而不是算座位) | `commands` **红 1**:`landed 得等于真出片的行数(实测 2 行):期望 2,实际 4` |
| M2 | 座位键摘掉 id,只留行内序数 | `commands` **红 1**,但红在**①档**(`期望 2,实际 1`)而不是对侧为它写的②档 |
| M3 | 相等时也挂 `note`(空串) | `commands` **红 1**:`相等时一句不加(重复一遍是废话)::期望 undefined,实际 ""` |
| M4 | 空跑那一路不带 `landed` | `commands` **红 1**:`期望 0,实际 undefined` |
| M5 | 自造第二份说法(不读 `Domain.landedNote`) | `commands` **红 1**,只有**④档**响 |
| M6 | 写回换 `nthShot`(残留 2 那一步) | `commands` **红 1**,只有写死的 `landed === 2` 那句响 |
| M7 | 摘掉 `sb.CMD = vm.runInContext('CMD', sb)` | `commands` **红 1**:`Cannot read properties of undefined (reading 'gen-episode')` |
| M8 | 删掉新用例后把 `landed` 整格退回 | `commands` **红 0**、`skills` **红 0**、`domain` **红 0**;只有 `contract` **红 2**(都是数数那层) |
| M9 | 跨支反向手:退掉 W272 已落的 `js/sb-views.js:1243` | `contract` **红 1** + `skills` **红 1**,本槽两条判据一条不红 |
| M10 | `gen-episode` 座位提前到开跑时登记 | 三套件**各红 0** |
| M11 | `exec episode.generateVideos` 座位同样提前登记 | 三套件**各红 0** |

### M2:对侧点名的那半不是真开火的那半(本槽新量)

对侧②档写着「座位只按行内序数记会全记成第 0 行、`landed` 缩成 1 —— **正面钉住而不靠副作用**」。
真做这一手时:①档的 dup 夹具里 `dup` 三行 + `solo` 一行,摘掉 id 之后四轮的座位全成了 `#0`,
`landed` 缩成 1,**①档先抛**,②档一行都没跑到。即这条复合判据里②档对这个退法**是冗余的**,
点名它的那句注释描述的是设计意图、不是实测开火点。
配对量了一手:②档的**另一半**(`note` 相等时不许在场)确实独立承载——M3 让 `note` 变成空串,
①档全绿而②档报出 `期望 undefined,实际 ""`。故②档不是空壳,只是它挡住的是**另一个**退法。
这与 W275 §五 M3 / W276 §四 M3 那格「复合判据同时坏几半时只报第一半」**不同类**:
那格是坏了几半而只报一半,本格是**一半的点名对象与它真拦得住的东西错位**。

### M5:①档的正则会被"长得像"的手写句子放过(本槽新量)

M5 把 `Domain.landedNote(...)` 换成一句手写的 `'本趟 4 次调用成功,产物只落到 2 行(几轮共用了同一行)'`。
①档那句 `assert(/产物只落到 2 行/.test(dup.r.note))` **照旧全绿**——手写句恰好含着这个字面;
真正响的只有④档那句逐字节相等(`gen-episode 不许另造第二份说法`)。
即单源这件事**只有④档一层在守**,而①档那条正则拦的是"有没有说这句话"、拦不住"这句话是谁写的"。

### M6:残留 2 那一步该同轮改的到底是哪一句(本槽新量)

对侧残留 2 写着「换 `nthShot` 之后 `landed` 会恒等于 `ok`,本槽这条判据的第一档会随之改口径」。
真把写回换成 `nthShot`(并按 `exec` 那一处的形状把序数在**全表**上数)之后:
`assertEq(dup.r.landed, gotRows, …)` 这句**仍然全绿**——磁盘上真出片的行数跟着变成 4,
它是**自校准**的那一句;红的只有写死的 `assertEq(dup.r.landed, 2, '共位那一趟 landed 是 2')`。
即残留 2 那句「第一档得同轮改口径」精确地只指**那一个字面 2**,`landed === gotRows` 那半是形态无关的。
这一格把「换 `nthShot` 时要动哪几个字」从一句提醒缩成了一处坐标。

### M8:`landed` 全族零源级判据(本槽新量,与 W276 §四 M13 反向)

W276 M13 那格里,删掉行为面用例之后原变异**仍红在源级**,故那槽丢的只是"一格证据"。
本槽照同一形状量:删掉新用例、再把 `gen-episode` 的 `landed` 整格退回(`landed: 0` 两处 + `seats.size` + 那句 `note`),
`commands` 58/58、`skills` 104/104、`domain` 44/44 **各红 0**,只有 `contract` 红 2(README 对账 + 棘轮下限)。
顺着往下查了一层:全树 `landed` 的断言只出现在 `tests/unit.js` 两个用例里
(`commands` 套件 3904 行那条覆盖对侧两条批量、4007 行本槽这条),**零源级断言**——
没有任何一句扫 `cli.js`/`js/commands.js` 的源码去问「这条出口还带不带 `landed`」。
即 `landed` 这一族**整体**只有行为面一层网,与 W276 那格的"行为面丢了、源级还在"**不同类**。

### M10 / M11:「只在本轮真成功后才进集合」两端都没有判据(本槽新量)

`cli.js:933-939` 那段注释与对侧记账件都写着座位「只在本轮真成功后才进集合」。
把 `seats.add(seat)` 从成功分支提前到 `withProject` 回调开头(`gen-episode`,M10),
以及把 `exec` 那一处的 `seats.add(...)` 提到内层 `try` 之前(M11),两手**三套件各红 0**。
原因是结构性的:两条判据的所有档都是全成功夹具(①档头一句就是 `assertEq(dup.r.failed.length, 0, '前提:四次调用都成功')`),
**没有任何一档跑过失败轮**。而失败轮恰是 `landed` 存在的理由的另一半——
一趟里有几镜失败时,提前登记会让 `landed` 报得比真落库的行数高,
即这个「诚实字段」在失败轮上会不诚实,且两端都没人拦。
这不是本槽改出来的(W254 那两条就这样),但对侧的注释把这半边写成了已保证的性质,故新记成残留 9。

### M9:连续第六次读成「分家」

退掉 W272 已落的 `js/sb-views.js:1243`(更多工具弹窗页头改回 `s.order + 1`):
`contract` 红 1(`更多工具弹窗页头的镜号应取 Domain.shotNo(表内实位)`)+ `skills` 红 1
(`更多工具页头应报第三镜:期望 "更多工具(镜头 3)",实际 "更多工具(镜头 1)"`),
报错句与 W271 §五 M1 / W272 §三 / W273 §五 M4 / W275 §五 M9 / W276 §四 M9 逐字相同;
本槽两条判据(`commands` 那条新用例、`sb-views` 套件)**一条不红**。
判据落在**哪个文件**决定读成「叠着」还是「分家」——本槽对侧只动 `cli.js`,W272 那手落在 `js/sb-views.js`,
故第六次仍读成分家,不是红几条决定的。

## 五、反事实:抄回旧数字 / 静默删测(棘轮缓冲被对侧收掉之后的读数)

W276 §六 11 记着一格残留:`['单元测试', 693]` 那格下限落后 live 两格,
落后的正好就是那一槽合进来的两条用例,故「抄回旧数字」只红 1、「真删掉那两条 + 文档改小」全绿。
**对侧把这格抬到了 live(695 → 696,差额 0)**,本槽照同两手复量:

| 反事实 | W276 那次 | 本槽现取 |
|---|---|---|
| CF1:`README.md` 抄回上一支自称的 695,下限不动 | **红 1**(缓冲吃掉一层) | **红 2** |
| CF2:真删掉新用例 + `README.md` 按 live 改小 | **全绿** | **红 1** |

CF1 现取两句:
`单元测试用例数:README.md 与实测不符(实测 696,文档 695)` 与
`单元测试用例数:README.md 写的 695 低于下限 696(把文档数字改小来迁就删测即红)`。
CF2 现取一句:`单元测试用例数不得少于 696(实测 695);确要删测须同轮说明理由,新增用例时把下限抬到当轮实况`。

**读数:W134 那句「逮不逮得住取决于 `live − 下限字面` 有没有越过缓冲」在本槽是从另一头验的**——
W276 是"对侧没抬下限 → 少红一层",本槽是"对侧抬到 live → 那一层回来了"。
差额 0 时静默删测的窗口宽度是 **0 条**;而对侧本可以吃 `SLACK` 那格便宜
(它自己记账件 §六 写明:加 1 条后差额会变成 3、恰等于 `SLACK`、一条判据都不会红),它没吃。
本槽记账件那格同样按惯例抬到 live(291 → 293,差额回 0),**五格差额全 0、`SLACK` 一格没用掉**。

**但 CF2 与 M8 摆在一起还给出第二个读数**:响的那一句是**棘轮下限**,不是任何一句产品判据。
即差额 0 只把"静默"变成"有声",它并没有给 `landed` 这格补上第二层网——
真要撤这道护栏,把 `FLOOR` 字面一并改小仍然够(W134 已记的那格,本槽没有改进它)。

## 六、残留(按明令一条没代修,W277 八条原话逐条保留)

W277 §九 那条「部分销号」原样传下:`landed` 收下第三条出口 `gen-episode`,
**不整条销号**——普查表第 6~12 行那七条单发出口仍没有这个字段(见下 1)。

1. **单发那七条出口没有 `landed`**(`shot.generateVideo` 两端、`gen-shot-video`、`gen-shot-image`、
   `subject-image`、`subject-add --gen-image`、`subject-copy`)。它们一次只写一位/一行,共位形态在单轮里不成立,
   而且都没有 `ok` 计数可与之对照——补进去只会是恒 1 的死字段。但「回执字段两类命令不齐」这件事本身仍是登记项:
   按 `result.landed` 写通用消费逻辑的人得先知道它只在**批量**那三条上。
2. **`gen-episode` 的写回路径仍取首行**。`findShot` 按 id 取首行这一格没动(明令:本槽只补诚实字段)。
   要让同 id 三行各得一条片,得把它换成 `nthShot` 并把序数在**全表**上数(与 `exec episode.generateVideos` 那一处同形),
   那是一次行为面改动,得单开一槽——换掉之后 `landed` 会恒等于 `ok`,本槽这条判据的第一档会随之改口径,一并同轮改。
   (本槽 §四 M6 把「第一档」量成了一处坐标:红的只有写死的 `landed === 2` 那句,
   `landed === gotRows` 那句自校准、换法之后仍全绿。)
3. **引擎事件 `shots.batchDone` 的 `ok` 同样读不出落库实况**(W277 §1.1)。它不是命令出口、没有 `result`,
   但它经 Agent 对话流转译给用户看,「三镜成功」这句话在共位那一趟同样偏高。
   本槽不收:那要先定「引擎事件要不要跟着回执的字段走」。
4. **共位那一轮的钱没有退路**(原样传 W254 §残留 6)。`landed < ok` 说清了「有几笔钱的产物被盖了」,
   但没有任何一端据此退费或重排。按现行约定那几次调用都真发生、真交付了产物(只是落到了同一行上),
   退费口在 `Tasks`/服务端而不在回执层。要把它变成可操作,得先定「共位算不算交付失败」这个产品问题。
5. **`result.shots` 按 id 记,同 id 只剩一个键**(W277 §四)。对侧把它钉成判据、
   并在注释与 README 里写明「落库数只看 `landed`」,但没有改它的形状——
   改成按行记就得换键(行序数或行对象),而它是 `gen-episode` 对外的回执字段,改键是破坏性改动。
6. **`state-put` 不设闸**:整树原样落库是刻意的,没设闸(明令)。
7. **主体侧没有 `shots-dedupe` 同类命令**:`dupSubjectRowsNote` 末句给的仍是「回主体库删掉或改 id」这条现有修法。
8. **`loadCli()` 此前够不着 `CMD` 表**(W277 §五)。对侧为那一条把 `CMD` 取了出来,但**只有 `gen-episode` 一条被测到**
   ——那张表里还有四十余条命令原语,单测覆盖率仍是零。这不是那一槽要收的,
   但「沙箱够不着 = 结构性测不到」这个形状值得记:`landed` 这个漏在 `gen-episode` 上躺了两轮没被量到,原因就在这里。
9. **(本槽新记)「座位只在本轮真成功后才进集合」这半边两端都没有判据**(§四 M10/M11)。
   `cli.js` 那段注释与对侧记账件都把它写成已保证的性质,而把座位提前到开跑时登记时
   `gen-episode` 与 `exec episode.generateVideos` **各红 0**。成因是结构性的:
   两条判据的所有档都是全成功夹具(①档头一句就是「前提:四次调用都成功」),没有一档跑过失败轮。
   失败轮上提前登记会让 `landed` 报得比真落库的行数高——这个「诚实字段」恰在它最该诚实的那一档上没人拦。
   这是 W254 那两条就带着的洞、对侧原样继承,不是本槽改出来的,**按明令未代修、一条测都没加**。
10. **(本槽新记)`landed` 全族零源级判据**(§四 M8)。全树关于 `landed` 的断言只在
    `tests/unit.js` 两个用例里(`commands` 3904 与 4007 两行),没有任何一句扫源码去问
    「这条出口还带不带 `landed`」。故删掉那条行为面用例之后,把 `landed` 整格退回去
    `commands`/`skills`/`domain` 各红 0,只剩数数那两句响。与 W276 §四 M13 那格
    「行为面丢了、源级还在」**不同类**,记下来免得下一槽照 W276 的读数去推本族。
11. **(本槽新记)对侧②档点名的对象与它真拦得住的东西错位**(§四 M2/M5)。
    ②档注释说自己「正面钉住座位键带 id 而不靠副作用」,而真做那一手时①档先抛、②档一行没跑到;
    ②档独立承载的其实是另一半(`note` 相等时不许在场,M3 单独量到)。同族地,
    ①档那句 `/产物只落到 2 行/` 拦的是"有没有说这句话"、拦不住"这句话是谁写的"(M5 量到)。
    判据没有被削弱,**记的是注释里的点名对象不能当实测开火点读**——
    与 W275 §五 M3 那格「坏几半只报一半」不同类。
12. **产线邻面 `js/batchops.js` 那 17 行仍读 `order`**(W273 有意整族留下,W274/W275/W276 逐槽复核):
    落库文本 8 行、瞬时串 8 行、导出 TXT 正文 1 行。与 W271 留下的分镜面 38 行同族,
    收的时候要一起定"进度坞行与它同行的扣费摘要按什么切开"。**产品口径未定,本槽不定。**
    (本槽现取复核:`.order + 1` 仍是 **17 行**,相对 `P1` 逐字节未动。)
13. **W276 §六 2~10 那几条原样传下、一条没代修**:两份待确认清单行只有一份有行为面判据
    (`js/sb-batch.js:154`)、「按指令改」页头(`js/sb-views.js:1096`)只有源级判据、
    `js/skills.js` 那一族回执与提示词镜号(连同"同屏两号"未收干净)、审片面任务名与扣费摘要那 4 行、
    浏览器闭环 `common`/`cut` 空壳与 `openEpisodeReview` 整表、同 id 多行源头、库里 `order` 字段本身会漂、
    邻面落库文本那族只有源级判据、生成面 `|| '?'` 兜底半边两层都没判据、
    过期镜明细按 id 建 `Set` 会把同 id 的鲜镜一并算进去、复合判据同时坏几半只报第一半。
14. **W276 §六 11 那格(下限落后 live 两格、缓冲此刻等于哪两条)本槽已由对侧收掉**:
    对侧把 `['单元测试', 696]` 抬到 live、差额回 0,§五 两手复量证实那一层判据回来了。
    这一格**不是本槽代修的**——是对侧自己在它那一槽里抬的,本槽只是现取复核并记下读数。

## 七、跑了什么(数字全部 live 现取)

- `node tests/unit.js` **696/696**(`P1` 基线 `49033fb` 干净 worktree 上现跑 **695**);
- `node tests/unit.js commands` **59/59**(基线现跑 58);
- `node tests/unit.js contract` **153/153**(基线现跑 153,**未动**);
- `node tests/unit.js domain` **44/44**、`skills` **104/104**、`issues` **22/22**(三格未动);
- `node tests/integration.js` **152/152**(基线现跑 152,**未动**);
- `env -u HUJING_SERVER -u MV_ACCESS_KEY -u MV_SECRET_KEY -u MV_API_KEY -u MV_REGION node tests/cli.smoke.js`
  **单独整跑** **115/117**,两条失败:`未登录 whoami → exit 3 | exit=1` 与 `llm --json mock 链路 | undefined`;
  同 env 下 `master` `9adcf0f` 独立 worktree 现跑 **51/53**、失败两条**同名同表现**,故按纪律允许;
- `node --check cli.js tests/unit.js js/domain.js` 逐个通过;
- `e2e` 按明令**没跑**。

## 八、保留面机检(合完的树上现取)

| 项 | 实况 |
|---|---|
| `Skills.gaps()` | **20** 键,一个没剥 |
| `TOPIC_FLOOR` / `SLACK` | 19 / 3,未动 |
| `GUARD_TOPICS` / 销号台账 / 花名册 | 19 / 0 / 19,四者对齐 |
| 五个 `FLOOR` 差额 | 0 / 0 / 0 / 0 / 0(记账件那格本槽 291 → 293) |
| `gen-episode` 回执的 `landed` | 在场(空跑 0、岔开走 `Domain.landedNote`) |
| `cli.js` 的 `findShot` / `nthShot` / `nthSubject` | **17** / **6** / **3** 处,与对侧逐格相等 |
| `dupRowsNote` / `dupSubjectRowsNote` / `landedNote` | 三句判词一字未改 |
| `emptyBatchNote` / `emptySubjectImageNote` | 未动 |
| `state-put` 写入闸 | 仍只有 `need(f.file, …)` 与 `--force`,**没设闸** |
| `shots-dedupe` | 未动(主体侧照旧没有同类命令) |
| 选人闸六处按行/按位筛 | 一个字没动 |
| `js/` 全树 | 相对起点与相对对侧**双向零 diff** |
| `billing.js` / 计费入口 | 零 diff |
| 镜号那一族(`Domain.shotNo` 四面) | 零 diff |
| `js/produce.js` / `js/issues.js` 的 `.order + 1` | 各 **0** 处 |
| `js/batchops.js` 的 `.order + 1` | **17** 行,未动 |
| 本文目录内相对链接 | **0** 处 |

## 九、交接

- 主干现在是 `cursor/w278-integration-b345`,尖 `d69357d`(merge commit),
  parents `49033fb` + `49b1905`;**没开 PR、没合 master、没开第三条功能支**。
- 下一槽要合的槽若动 `gen-episode` 的写回路径,先读残留 2 与 §四 M6:该同轮改的是那一处写死的 `2`。
- 要给第四条出口补 `landed` 之前,先读残留 10:这一族目前只有行为面一层网,
  加出口时把源级那层一起立起来比事后补便宜。
- 要照抄"棘轮缓冲被收掉之后反事实该怎么读"看 §五:差额 0 时静默删测窗口是 0 条,
  但响的仍只是数数那层——差额 0 不等于产品面多了一层网。
- 要照抄"对侧注释点名的那半不是真开火的那半"这一手看 §四 M2/M5:
  先按注释点名的档做一手,若红在别的档上,再单独设计一手让其余全绿而只破坏被点名那档。
