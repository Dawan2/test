# W70 · 两条叉在 W62 的旁支收敛记录(集成分支)

> 集成分支:`cursor/w70-integration-ad31`,基线 `cursor/w67-integration-a5c1 @ cbb2b24`(开工 `fetch` 后的 tip)。
> 本文只记**收敛过程**:合了哪两条、每处冲突按什么判据取侧、合并后的实测数字、没删测的取证。
> 两次合并均 `--no-ff`、一次合并一个合并提交、逐条可 revert;**不重做任何一条分支已落地的功能**。
> 本槽不开 PR、不合 `master`、不删测。

## 1. 结果一句话

两条旁支都**叉在 `w62` 的 tip `9dfad8d`** 上,而基线此后又走过 `w64`(`release-core` / 领域命令 `project.release`)
与 `w67`(`w53` 记账件补合 + 目录索引完备性契约)。因此本槽 8 处冲突里,**对侧文本没有一处是错的——
它们只是分叉那一刻的实况**;判据只能逐处回答一个问题:*这一块在分叉之后主干上又落了什么*。

- `w65-g11-mem-scope-filter-d5cf @ be91e1e`(专家自进化蒸馏输入按生效板块过滤)→ 合并提交 `99343e1`
- `w66-sk11-extract-note-257a @ e09946d`(SK-10/SK-11 的 `note` 按实况改写)→ 合并提交 `1e4627c`

合并后回归:`unit 417/417`、`integration 118/118`、`cli.smoke 88/90`
(2 项失败与 `master` 同名同表现,见 5.2)。

## 2. 开工前的包含性实测(合的是哪两条)

逐条 `git log -1` 取 tip、`git merge-base` 取分叉点、`git rev-list --count` 取 outstanding:

| 候选分支 | tip | merge-base(与基线) | outstanding | 判定 |
|---|---|---|---|---|
| `cursor/w65-g11-mem-scope-filter-d5cf` | `be91e1e` | `9dfad8d`(= `w62` tip) | 2 | **合** |
| `cursor/w66-sk11-extract-note-257a` | `e09946d` | `9dfad8d`(同上) | 2 | **合** |
| `cursor/w61-sk26-front-writeback-b09b` | `1c459fc` | `450c29f` | 3 | **不合**:任务口径排除(交给 W68) |
| `cursor/w68-integration-4e71` | `f93f4ab` | `52965b5`(= `w64` tip) | 5 | **不合**:任务口径排除 |
| `w69-*` | — | — | — | **不合**:远端零匹配(`git branch -r \| grep w69` 零命中) |

被排除的三项只做**登记**,不做完成度判定:`W61` 那 3 条与 `W68` 那 5 条提交本槽一行未碰。

### 2.1 两条槽的分叉点相同,落点却不重叠

两条都从 `9dfad8d` 起跑,各 2 条提交(1 条实现 + 1 条记账件),改的文件有四份重叠
(`README.md`、`docs/skills-wave/README.md`、`js/skills.js`、`tests/unit.js`),
但**在这四份里各自动的段落互不相交**——`w65` 动 SK-26 的 `note` 与 `memory` 套件,
`w66` 动 SK-10/SK-11 的 `note` 与一条新的记账面断言。
故两次合并的冲突全部是**"我方 vs 分叉时旧实况"**,没有一处是两条旁支互撞。

## 3. 冲突与取侧判据

### 3.1 合入 W65(`be91e1e`,合并提交 `99343e1`):冲突 3 文件 5 处

**(1) `js/skills.js` — SK-26 `review.memoryFeedback` 的 `note` 尾段。** 两侧整段重写同一处:

| 侧 | 写的是 |
|---|---|
| 我方 | `steps` 两步都是已注册命令,发布留痕的命令化出口(G-12 第三个落点)已接上,出口是 `project.release` |
| 对侧 | `steps` 只登记审片这一步、命令化待 G-12(**分叉时的实况**)+ 蒸馏侧板块面已补齐 + 新的「仍欠(G-11)」 |

取**我方那段 + 对侧后两段**:对侧第一句是 `w64` 之后就不再成立的旧实况,折回去等于把
`project.release` 从记账里抹掉(该条目的 `steps` 数组在自动合并区已是两步,`note` 说"只登记审片这一步"当场自相矛盾);
「仍欠」段则必须取对侧——蒸馏侧已经不再"读记忆时不按板块过滤",我方那句话本槽之后就是假欠账。

**(2) `README.md` — 主线 skill 索引那一行。** 两侧只差一个数:领域命令 `11`(对侧)vs `12`(我方)。
取我方并现取复核:`CmdRegistry.names().length` 为 **12**(`project.release` 已注册),
这一处有 `contract · README 数字对账:注册表口径` 兜底,取错侧当场红。

**(3) `README.md` — 专家雇佣扩充那一行。** 我方无增量、对侧加了一段蒸馏按板块过滤的说明,**取对侧**。

**(4) `README.md` — 单元测试套件清单那一行(整行长散文,3 处词级差异)。** 逐处给判据,**不整行取任一侧**:

| 差异处 | 我方 | 对侧 | 取法 |
|---|---|---|---|
| `release.js` / `experts.js` 段 | 带 `release-core` 双端单源与 `project.release` 命令化四端齐备两大段 | 这两段整段不存在,`experts.js` 里多一段蒸馏板块过滤 | **并集**:保我方两段,插入对侧那段 |
| `memory` 套件段 | 无蒸馏面 | 多一段 `expertBoards`/`memForBoards` 与召回侧的分工对照 | **取对侧**(插在播种段之前) |
| 用例数 | `409` | `410` | **两侧都不要**,见 5.1 |

> 整行取任一侧都会**绿而内容不完整**:取对侧则 `release-core` 与 `project.release` 那两段整段消失,
> 而这一行的散文**没有断言兜底**(只有用例总数那一个数字被 `contract` 数字对账钉着)。

**(5) `docs/skills-wave/README.md` — 索引表 + 一分钟摘要末条。**
索引表取**并集**并按槽号插(`w63` → `w65` → `w67`);
一分钟摘要「记忆的写入面」那条我方已写到 W58(发布留痕写入点改走 `/api/wf/release`),
对侧停在「发布留痕的命令化出口仍待 `G-12`」,故**取我方整条 + 追加对侧的 W65 那段**。

### 3.2 合入 W66(`e09946d`,合并提交 `1e4627c`):冲突 2 文件 3 处

**(1) `README.md` — 单元测试套件清单那一行(又一次,3 处词级差异)。**
这一次对侧的整行是**分叉时的旧实况叠加它自己的增量**,而我方那一行刚在 3.1(4) 并进了 W65 的两处。
取我方 + 只并入对侧的两处真增量:「记账面另有三组断言」→「四组」,以及 SK-10/SK-11 那条新锚点的描述。
若这里图省事取对侧,一次性丢掉的是 `release-core` / `project.release` / W65 蒸馏板块面**三段**。

**(2) `docs/skills-wave/README.md` — 索引表。** 按槽号插 `w66` 行(`w65` 与 `w67` 之间)。

**(3) `docs/skills-wave/README.md` — 一分钟摘要「记账诚实位」整条。**
这条最值得记:对侧那一整条里**同时埋着两句已被更正过的话**——

- 「该槽远端 tip 是完成态但**没有留下自己的记账件**,故本行没有可指的 `w53-*.md`」(W67 已就地更正为:
  真 tip 是 `0045962`,记账件一直在,W57 当时只合到中间点);
- 「`G-12` …只剩发布留痕两端的命令化出口未接」(W58/W64 已接上)。

两句都在**非增量区**——对侧并没有改它们,只是把整条重写时原样带着走。
故取我方整条 + 追加对侧末尾的 W66 那段(`**W56/W60 登记的那处基线记账不实随 W66 清掉**:…`)。
**机械取 `theirs` 会一次退回两处已更正的判断**,而这两句同样没有断言兜底,只能靠通读认出来。

> 全程没有对任何一份 `README` 用 `git checkout --ours/--theirs`:8 处冲突的判据各不相同
> (并集 3 处、取我方 + 追加对侧 3 处、取对侧 1 处、两侧都不要 1 处)。

### 3.3 没有出现"整块测试冲突"

`tests/unit.js` 两次合并都是 `Auto-merging`、零冲突标记:
两条槽各自把用例追加在不同套件的不同位置(`w65` 进 `memory`/`experts`,`w66` 进 `contract`/`skills` 记账面)。
故本槽**没有需要"两块都留"的整块测试冲突**;若有,判据仍是两块都留——
用例名集合只能只增不减(5.3)。

## 4. 合并后现取复核的数字(两侧写的都不能用)

### 4.1 用例总数:两侧的绝对值都错,两侧的增量相加才对

| 取点 | `unit` 实测 | 该处 `README` 写的 |
|---|---|---|
| 分叉点 `9dfad8d`(`w62` tip) | 403 | 403 |
| `w65` tip `be91e1e` | 410 | 410(它自己那条线上对) |
| `w66` tip `e09946d` | 404 | 404(同上) |
| 基线 `cbb2b24`(`w67` tip) | 409 | 409 |
| **本槽 HEAD** | **417** | **417**(合并后改的值) |

`409 + (410-403) + (404-403) = 417`,与实测相等——但 `410`/`404` 这两个**绝对值**一个都不能折回,
它们各自只在自己那条线上成立。这一处有 `contract` 断言兜底,取错当场红并给出实测值。

### 4.2 `memory` 套件条数:三个值,活的是第四个

我方 `19`、对侧 `12`(对侧那句是分叉之前就已过期的,`w65` 根本没碰它)、
而 `w65` tip 上真跑是 `21`。本槽 HEAD 实测 **21**,改为「现 21 条,末 6 条覆盖 W53 的播种/迁移面,
另 2 条覆盖 W65 的蒸馏板块过滤面」。**这个数没有断言兜底**,靠通读捞出来。

### 4.3 顺手复核、结论是"不用改"的几处

| 数字 | 出处 | 现取实测 | 动作 |
|---|---|---|---|
| 领域命令 12 条 | `README.md` 主线 skill 索引行 | `CmdRegistry.names().length` = 12 | 已在 3.1(2) 取对,不再改 |
| MCP 37 工具 | `docs/AI助手接入指南.md` | `mcp.js` `TOOLS.length` = 37 | 两条槽都没碰 `mcp.js`,不动 |
| `integration` 118 项 | `README.md` 套件清单 | 118 | 相符 |
| `cli.smoke` 90 项 | `README.md` 套件清单 | 90 | 相符 |
| 注册表提示词 14 条 | `README.md` 主线 skill 索引行 | `contract` 数字对账绿 | W66 给 SK-11 补的是 `prompts` 引用键、不是新键,条数不变 |

## 5. 实测与取证

### 5.1 三套件数字(全部本槽现跑,不引用任何分支文档里的数)

| 套件 | 基线 `w67 @ cbb2b24` | 本槽 HEAD | 说明 |
|---|---|---|---|
| `node tests/unit.js` | 409/409 | **417/417** | +7 = W65、+1 = W66 |
| `node tests/integration.js` | 118/118 | **118/118** | 两条槽都没碰服务端编排 |
| `node tests/cli.smoke.js` | 88/90 | **88/90** | 2 项失败与 `master` 同名同表现(5.2) |

### 5.2 `cli.smoke` 那 2 项失败:与 `master` 同名同表现

现开 `git worktree add /tmp/wt-master master` 实测 `master @ 9adcf0f`:**51/53**,失败两条:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

本槽 HEAD 的 2 项失败**名称与表现逐字相同**。即这 2 项先于本线存在,
不是本槽或合入的两条分支引入的;本槽**不顺手改它**(不在任务范围,改动会碰 `cli.js` 登录路径)。

### 5.3 用例名集合:三份 tip 的并集,零丢失

按套件取 `PASS|FAIL` 行的用例名(切掉 `|` 之后的运行期载荷,免得像 W62 记的那样得出假差集)排序去重后 `comm -23` 比对:

| 比对 | `unit` | `integration` | `cli.smoke` |
|---|---|---|---|
| 基线 `cbb2b24` 名集 ⊆ HEAD | **零缺失**(409 → 417) | **零缺失**(117 → 117) | **零缺失**(90 → 90) |
| `w65` tip 名集 ⊆ HEAD | **零缺失**(410) | **零缺失**(105) | **零缺失**(80) |
| `w66` tip 名集 ⊆ HEAD | **零缺失**(404) | **零缺失**(105) | **零缺失**(80) |

两条槽的 `integration`/`cli.smoke` 名数比基线低,**不是删测**:它们叉在 `w62`,
基线之后又并入了 `w58`/`w64` 的用例(那两套件本槽零改动,HEAD 名集与基线逐条相等)。
`integration` 117 名对 118 项断言的差是一组同名断言,与 W67 记的同一处。

### 5.4 两条槽的实现原样进线(不是重写)

```
git diff 9dfad8d origin/cursor/w65-g11-mem-scope-filter-d5cf -- js/experts.js js/wf-core.js
git diff 9dfad8d HEAD                                        -- js/experts.js js/wf-core.js
# → 两份 diff 逐字节相同(W65 的实现一行未改地进了线)
```

`js/skills.js` 侧另按登记面反查:`Skills.byId('review.memoryFeedback')` 的 `steps` 仍是
`['episode.smartReview','project.release']` 两步、`note` 里 `project.release` 与 `expertBoards` 同时在;
`Skills.byId('subjects.refDiscipline')` 的 `prompts` 已含 W66 补登的 `extract.system`。

### 5.5 点名保住的五处(合并前列成清单,合并后逐条现查)

| 要保的东西 | 现查方式 | 结果 |
|---|---|---|
| 领域命令 `project.release` | `js/skills.js` 命中 7 次、`js/cmd-registry.js` 命中 1 次、`js/release-core.js` 在 | 在 |
| 问题中心 UMD | `js/issues.js` + `js/issues-ui.js` 两份都在,`Issues.reminders()` 表 7 行 | 在 |
| SK-19 稳定词那行提醒 | `REMINDERS` 表里 `shot-stable-lexicon`(低危,`stage: 'shots'`) | 在 |
| `w53` 记账件 | `docs/skills-wave/w53-memall-headless-seed.md`(143 行)+ 它那行索引 | 在 |
| 「每份 `wNN-*.md` 各要有自己那行索引」契约 | `tests/unit.js` 的 `docs/skills-wave 索引完备性` 用例 | 在且绿 |

`facts` 表(`tests/unit.js` `记账对齐:infra 三条…`)与 `Skills.gaps()` **两条槽都没碰**,按现况保留:
`G-11` 仍挂在 SK-26 上(蒸馏仍是人手动作、仍只对自定义专家开放),
`G-13` 仍挂在 SK-10/SK-11 上(全仓内联人设未清完)——落地一面不摘标记,是关联索引口径,不是漏改。

## 6. 没做什么

- **没开 PR、没合 `master`**:只把 `cursor/w70-integration-ad31` 推上远端。
- **没合 W61 / W68 / W69**:`W61`(交给 W68)与 `W68` 按任务口径排除,那 3 / 5 条提交本槽一行未碰;
  `w69-*` 远端零匹配(第 2 节)。
- **没删测、没改名顶替、没抬松断言下限**:名集三份 tip 逐条包含(5.3);
  两条槽新加的 8 条用例原样进线,既有断言一条未删。
- **没改产品行为**:本槽自己零代码改动——`js/experts.js`/`js/wf-core.js`/`js/skills.js` 的变化
  全部是两条槽原样带进来的,集成层只解冲突与改数字。
- **没碰发布门与计费**:`js/release.js`、`js/release-core.js`、`billing.js` 零改动;
  W65 的两道跳过闸落在扣费之前,计费口径一字未动。
- **没改 `facts` 表、没摘 `gaps`**:两条槽都没动,本槽也不代动(5.5)。
- **没重写两条槽的记账件**:`w65-*.md` 与 `w66-*.md` 是作者原文原样带进来的,本文不复述其内容。
- **没删 W53/W58/W62/W67 留下的历史记录**:3.2(3) 那两句被对侧带回的旧判断是**没有折回**,
  不是把对侧的记录删了——对侧分支上的原文一字未动。
- **没修 `master` 那 2 项 `cli.smoke` 失败**:先于本线存在,现开工作树取证(5.2)。
