# W55 · 周期 5 核验件、浏览器多轮人设与 G-12 中段流程模板的收敛记录(集成分支)

> 集成分支:`cursor/w55-integration-8f21`,基线 `cursor/w50-integration-dad5 @ 371c75e`(开工时的最新集成分支 head)。
> 本文只记**收敛过程**:合了哪几条、每处冲突怎么解、合并后的实测数字、没删测的取证。各项内容本身的说明在各自记账件里
> (`w48-cycle5-audit.md`、`w51-browser-agent-personas.md`、`w52-g12-mcp-mid-templates.md`),本文不复述。
> 三次合并均 `--no-ff`、一次合并一个合并提交、逐条可 revert;全程只解冲突与收敛双口径,**不重做任何一条分支已落地的功能**。

## 1. 结果一句话

三条候选**全部以各自 head 合入**(`w48 @ 05836ee`、`w51 @ f46a360`、`w52 @ 12607c6`),合并后回归
`unit 389/389`、`integration 93/93`、`cli.smoke 70/72`(2 项失败与 `master` 同名同表现,见 5.2)。

本槽的实质增量是三件事:

| 增量 | 来源 | 落点 |
|---|---|---|
| 周期 5(W33–W47)独立核验报告 | `w48` | `docs/skills-wave/w48-cycle5-audit.md`(纯文档,零代码) |
| 浏览器多轮三份系统人设收编为三条独立注册表键(`agent.panelSystem`/`agent.drawerSystem`/`agent.previsSystem`) | `w51` | `js/prompts.js`、`js/agent.js`、`js/agent-global.js`、`js/agent-ops.js`、`js/skills.js` |
| G-12 另一半:主线中段流程模板单源 `js/flow-tpl.js` + `cli flow-template` + MCP 工具/提示模板 | `w52` | `js/flow-tpl.js`(新增)、`cli.js`、`mcp.js`、`js/skills.js` |

合并后的主干口径变化只有两处:注册表提示词 **11 条 → 14 条**(单轮与多轮的人设句至此全部在注册表内),
`G-12` 由「只剩 MCP 流程模板补主线中段」缩成**只剩发布留痕两端的命令化出口未接**。
校验面仍是**七面十七条**、`Skills.playbooks()` 仍投影五条、短名单 30 条仍无 `pending`、发布门 G1–G10 一个字未动。

**本槽值得留下的三件方法面的事**:一是**核验槽的目录索引行不会随分支带来**——`w48` 只提交了报告本身,
索引那一行得由合入方补,而这一处恰好有契约断言兜底(第 4.1 节);二是三份 README 数字冲突里,
**两侧都是各自分支上的旧实况**,正确值只能由合入后 live 实测给出(第 4.2 节);
三是这一槽第一次出现「对侧增量已在**同一行**的非冲突改动里自动合进来」——`docs/skills-wave/README.md` 那句
`G-12 因此只剩…` 是自动合并的产物,若照惯性再手改一遍就会写重(第 4.3 节)。

## 2. 开工前与合入后的包含性实测(合的是哪几条)

`git fetch --all --prune` 后逐条 `git rev-list --count HEAD..<branch>` + `git log -1` 核对 tip:

| 候选分支 | tip | outstanding | 判定 |
|---|---|---|---|
| `w48-cycle5-audit-63b5` | `05836ee` | 1 | **合**(见 3.1) |
| `w51-browser-agent-personas-da99` | `f46a360` | 2 | **合**(见 3.2) |
| `w52-g12-mcp-mid-templates-d660` | `12607c6` | 4 | **合**(见 3.3) |
| `w53-*` / `w54-*` | 开工时前缀无匹配 | — | **跳过**:开工那次 `fetch` 里两个前缀都不存在;三条合完之后再 `fetch` 才出现 `w53-memall-headless-seed-3653` 与 `w54-issues-unreviewed-sk19-cfc0`,按任务口径**本槽不合**,留给下一个集成槽 |
| 基线 `w50-integration-dad5` 自己 | `371c75e` | 0 | 基线本槽期间未再前进 |

三条候选合完后重新 `fetch` 并逐条 `git merge-base --is-ancestor origin/<branch> HEAD` 复核:
**三条的 head 都是合并后主干的祖先**,期间三条 tip 均未移动(与 W50 那槽「候选清单变了两回」的情况不同,本槽零翻转)。

`w48` 的 merge-base 是 `master @ 9adcf0f`(它叉自主干、不叉自任何集成线),
因此 `git diff HEAD..origin/w48` 看着像要删掉一万八千行——那是**分叉侧的旧快照**,不是它要改的东西。
实际按 merge-base 合入只带来它自己那 1 笔提交(898 行新文档),`git merge` 的三方合并不会回退集成线上的任何一处。
> **口径**:核验槽这类叉自 `master` 的分支,判"合进来会带走什么"要看 `git show --stat <它的提交>`,
> 不能看 `git diff HEAD..<branch>`——后者把"基线领先它的部分"一并显示成删除,读起来像一次大回退。

## 3. 合入次序与逐步测试数字

次序按任务给定:核验件(纯文档)先、功能槽后、冲突面最大的 `w52` 最后。`w51` 与 `w52` 都直接叉自基线 `371c75e`、互不叠加,
故 `w51` 合入零冲突,两侧的重叠面全部落到第 3 步(见 4.2)。

| # | 合入 | 合并提交 | 冲突(文件/块) | unit | integration | cli.smoke |
|---|---|---|---|---|---|---|
| 0 | 基线 `w50 @ 371c75e` | — | — | 380/380 | 93/93 | 62/64 |
| 1 | `w48-cycle5-audit-63b5`(head `05836ee`) | `c96beaa` | 0(另补一笔索引行 `d975045`,见 4.1) | 380/380 | 93/93 | 62/64 |
| 2 | `w51-browser-agent-personas-da99`(head `f46a360`) | `f34cffb` | 0 | **381/381** | 93/93 | 62/64 |
| 3 | `w52-g12-mcp-mid-templates-d660`(head `12607c6`) | `7f75f0d` | 2 文件 / 3 块 | **389/389** | 93/93 | **70/72** |

第 2、3 步都长用例数,故 README 的用例数连着改了两次(380 → 381 → 389),
每次都照 `contract` 对账断言报出的实测值改,不手算(沿用 W47 4.5、W50 3 节)。
第 1 步纯文档,三套数字一个不动;第 3 步的 8 条 `cli.smoke` 新用例全是 `flow-template` 与 MCP 那两条链。

### 3.1 `w48`:周期 5 核验件(0 冲突,但少一行索引)

只有一笔 `docs` 提交,`docs/skills-wave/w48-cycle5-audit.md` 在本槽此前不存在,自动合并。
它**没有改 `docs/skills-wave/README.md`**,故合完之后目录索引里没有 w48 那一行,详见 4.1。

### 3.2 `w51`:浏览器多轮三份人设(0 冲突)

`js/prompts.js`(第 12–14 个键)、`js/agent.js`(抽出 `AgentCore.panelSystem`)、`js/agent-global.js`、`js/agent-ops.js`、
`js/skills.js`(SK-03 的 `prompts` 三键与 `note` 改写)、`tests/unit.js`(+148 行)与两份 README 全部自动合并——
它叉自基线 head,而基线在本槽期间只多了 w48 那两笔纯文档提交,唯一相接的 `docs/skills-wave/README.md`
两处改动落在不同行(索引行 vs 摘要行),`git` 自动并了。

### 3.3 `w52`:G-12 中段流程模板(冲突 2 文件 / 3 块)

`js/flow-tpl.js`(新增 211 行)、`cli.js`(`flow-template` 子命令)、`mcp.js`(工具 + 提示模板)、
`docs/AI助手接入指南.md`、`tests/cli.smoke.js`(+44 行)全部自动合并;
**`js/skills.js` 与 `tests/unit.js` 也是自动合并**——两侧改的是同一文件的不同段(`w51` 改 SK-03,`w52` 改 SK-05/SK-25;
`w51` 往 `contract` 套件加用例,`w52` 新开 `flow` 套件),合完逐条比对过两侧增量都在(见 4.4)。
冲突只在两份 README 的记账面,三块的解法见 4.2 与 4.3。

## 4. 四类处置的解法

### 4.1 核验槽只提交报告、不登记索引行——这一处有契约断言兜底

`w48` 的提交只有 `docs/skills-wave/w48-cycle5-audit.md` 一个文件,`docs/skills-wave/README.md` 的目录索引它一行没改。
合完之后目录里多了一份记账件、索引里没有它,`contract` 套件那条
「`docs/skills-wave` 索引与目录实况双向对齐(记账件不漏登记、索引行不指向空文件)」**立刻报红**,
两侧文件名清单逐条打印、差的正是 `w48-cycle5-audit.md`。

补法是按同类槽(`w5`/`w10`/`w20`/`w32` 四次周期核验)的格式补一行摘要 + 「何时读」,单独一笔提交 `d975045`,不混进合并提交。
补完 380/380 全绿。为确认这一行是**必须**而不是可选,把它撤掉单跑了一次 `node tests/unit.js contract`:
50/51,红的正是上面那条;恢复后 51/51。

> **口径**:合入方要把「这条分支的记账件登记了吗」当成一次独立检查,不能假定它随分支带来——
> 核验槽的作业纪律是「只读代码、只写本文件」,索引那一行按定义就不在它的作业面内。
> 这一处的运气是有断言兜底(W23 那套对账契约的一部分);**摘要区的散文没有**,那部分仍得通读(4.3)。

### 4.2 三处 README 冲突:两侧都是旧实况,正确值取合入后 live 实测

| # | 文件 / 块 | 两侧字面 | 解法 |
|---|---|---|---|
| 1 | `README.md` 回归测试长行 | HEAD 侧 `381 项断言`(带 `w51` 的数)、`w52` 侧 `388 项断言`(带它自己那条分支的数),且 `w52` 侧另有 `flow` 套件的整段描述与单套件名 `…|split|memory|flow` | 整行取 **`w52` 侧**(它是唯一带描述增量的一侧;word-diff 出来 HEAD 侧除数字外零增量),断言数按合入后实跑改 **389** |
| 2 | `docs/skills-wave/README.md` 目录索引行 | 两侧各添一行(`w51` 一行 / `w52` 一行) | **并集**,按槽号排序:`w48` → `w49` → `w50` → `w51` → `w52` |
| 3 | `docs/skills-wave/README.md`「记账诚实位」长行 | HEAD 侧带 `w51` 新写的「W49 点名的那三处余量随 W51 收掉」整段;`w52` 侧没有那段,但尾部 `G-12` 那半句是它更新过的 | 保留 HEAD 侧那一整段,**只把尾部 `G-12` 半句换成 `w52` 侧**(「计划步骤那一半随 W46 落地、MCP 中段流程模板那一半随 W52 落地,只剩发布留痕两端的命令化出口未接」) |

第 1 块是 W50 4.2 那条口径的第三种形态:**两侧数字都不对**,一侧是 380+1、一侧是 380+8,
而合入后是 380+1+8=389。取哪一侧的数都会红,`contract` 那条对账断言直接把实测值打出来
(`单元测试用例数:README.md 与实测不符(实测 389,文档 388)`),照它改一次即绿。

> **口径**:README 里的数字一律**合入后 live 实测取值**,不在两侧字面里挑、也不手算。
> 「较旧功能槽的偏小数字」尤其不能用来盖写已经收敛的事实——本槽第 1 块若取了 HEAD 侧的 381,
> `flow` 套件那 8 条就会在文档上凭空消失,而这一处恰好有断言;没有断言的地方就靠 4.3 的通读。

### 4.3 对侧增量已在**同一行的非冲突改动**里自动合进来——别再手改一遍

`docs/skills-wave/README.md` 里「前段命令全过 wf 通道」那条长行的结尾原本是
「`G-12` 因此只剩 `mcp.js` 流程模板补主线中段那一半挂账,见 w46…」。
`w52` 落地的恰好就是这一半,按 W50 4.3 的经验这类散文会过期、需要手改。
**但本槽这一处不需要动**:该行在 `w52` 侧也被改过,而 HEAD 侧这一行没动,`git` 直接取了对侧,
合完现搜一遍即得实况「`G-12` 因此只剩发布留痕两端的命令化出口挂账,见 `w52-g12-mcp-mid-templates.md`」,
与 `w52` 分支上的字面逐字节相同。

反过来,`docs/skills-wave/README.md`「记账诚实位」那行的尾部**是冲突块**,必须手工并(4.2 第 3 块)——
同一份文件里同一个主题(`G-12` 挂账口径)的两处描述,一处自动合并、一处冲突,原因只是 HEAD 侧动没动那一行。

> **口径**:沿用 W47 4.2 与 W50 4.1——**折回或手改之前先在整份文件里搜一遍关键词**。
> 本槽逐条查证了四项:`G-12 因此只剩`(已自动合入,不改)、`34 个 hujing_*` 工具与 `hujing_flow_template` 那段
> (在 `README.md` 非冲突区,已自动合入)、`docs/AI助手接入指南.md` 整份(`git status` 显示 `M` 无 `UU`)、
> `14 条注册表提示词` / `14 条主线 LLM 提示词`(`w51` 的两处数字,搜 `11 条` 得 0)。四项全部命中"已自动合入",零重复折回。

### 4.4 自动合并的功能代码逐条比对两侧增量(不静默丢 hunk)

`js/skills.js` 与 `tests/unit.js` 两侧都改、`git` 都自动合并了。这类"没报冲突"的文件按 W47/W50 的口径仍要**双向比对**:
`git diff HEAD -- <file>`(看 `w52` 侧增量到位没有)与 `git diff origin/w52-… -- <file>`(看 `w51` 侧增量还在没在)各跑一遍。

| 文件 | `w51` 侧增量 | `w52` 侧增量 | 结论 |
|---|---|---|---|
| `js/skills.js` | SK-03 的 `prompts` 补三键 + `note` 改写(仍欠段换成四处协议半 + 取值口口径) | SK-05 `note` 补中段模板落点、`G-12` 挂账口径改成「只剩发布留痕命令化出口(见 SK-25)」 | 两侧都在,零丢失 |
| `tests/unit.js` | `loadAgentChat()` 三文件装配 + `contract` 套件 1 条新用例 + 既有那条的锚点换成 `ops 协议`/`不开放覆盖` | 新开套件 23 `flow`(7 条)+ `contract` 1 条 MCP 中段模板契约 + 头注单套件名补 `flow` | 两侧都在,零丢失 |

`js/skills.js` 这一处正是 `w48` 报告 8.2 阻塞 11 点名的那一类风险面(「`note` 的仍欠清单机械取任一侧都会绿而内容都不完整」)。
本槽没有撞上:两侧改的是**不同条目**的 `note`(`w51` 改 SK-03、`w52` 改 SK-05 与 SK-25),
`git` 按行合并的结果与"两边都要"一致,并由两侧各自的点名断言分别钉住(合并后 389/389 全绿即两套点名断言同时成立)。

`node --check` 逐个跑过本槽触及的文件:`cli.js`、`mcp.js`、`js/flow-tpl.js`、`js/skills.js`、`js/prompts.js`、
`js/agent.js`、`js/agent-ops.js`、`js/agent-global.js`、`tests/unit.js`、`tests/cli.smoke.js` **10/10 通过**。

## 5. 没删测的取证

### 5.1 名集比对:并集与合并后**逐条相等**

按 W29 起的做法,用**用例名集合**而不是数字证明没删测:基线与两条功能候选各开独立 `git worktree` 跑一遍,
取运行器打印的 `PASS|FAIL | <套件> · <用例名>` 去重排序,与合并后的名集比对(`w48` 纯文档,与基线同名集)。

| 来源 | unit | integration | cli.smoke |
|---|---|---|---|
| 基线 `w50 @ 371c75e` | 380 | 93 | 64 |
| `w51-browser-agent-personas-da99` | 381 | 93 | 64(未动冒烟) |
| `w52-g12-mcp-mid-templates-d660` | 388 | 93 | 72 |
| 名集**并集** | **389** | 93 | **72** |
| **合并后主干** | **389** | **93** | **72** |

三套的 `comm -13`(并集里有、主干里没有)**均无输出**,即并集与合并后主干逐条相等。
逐条配对也对得上:`w51` 独有 1 条(`contract · 浏览器多轮三份人设:…各一键,缺省逐字节等于收编前的模板串`)、
`w52` 独有 8 条(`flow` 套件 7 条 + `contract · MCP 中段流程模板:工具与提示模板同取一份投影`),
380 + 1 + 8 = 389,一条不多一条不少。`cli.smoke` 同理:64 + 8(5 条 `flow-template` + 3 条 MCP)= 72。

### 5.2 `cli.smoke` 那 2 项:现开 `master` 工作树取证,不照抄上一槽

沿用 W50 5.2 的做法,`git worktree add` 一个 `origin/master` 工作树现跑了一遍:

| 分支 | cli.smoke | 失败用例 |
|---|---|---|
| `master` | 51/53 | 「未登录 whoami → exit 3」(实得 exit=1)、「llm --json mock 链路」 |
| 基线 `w50` | 62/64 | 同上两条(同名) |
| 合并后主干 | **70/72** | 同上两条(同名) |

三处失败的是**同名的同两条**,通过数 51 → 62 → 70 是这一系列槽陆续加出来的新用例。
本槽**未删任何用例、未放宽任何断言、未跳过任何失败**;`unit` 与 `integration` 三步全程 0 FAIL。

## 6. 合并后的主干实况(一句话版)

- 注册表提示词 **14 条**:`/api/wf/*` 六个 LLM 端点的人设句加浏览器多轮三份措辞(分集面板 / 全局抽屉 / 预排模式各一键),
  **单轮与多轮的人设句至此全部在注册表内**、全部可被用户覆盖;四处的 ops 协议 / 字段面 / 命令白名单 / 返回 JSON 约定
  仍由各自装配口拼、有意不开放覆盖(那半是 ops 解析契约),已按实况写进 SK-03 的「仍欠」段。
- `js/flow-tpl.js` 是主线中段流程模板的单一来源:步序取 SK-05 主线全链投影的**有序切片**(主体/分集/分镜/生成四段),
  参数面取 `CmdRegistry`、状态与缺前置取 `Domain.workflow`;出口是 `cli flow-template`、MCP 工具 `hujing_flow_template`
  与提示模板 `hujing_mid_pipeline`,三处只读零计费、授权位与子集位一律不代授权。MCP 工具数 **34**。
- 短名单 30 条**仍无 `pending`**;`G-11`(自进化)按关联索引口径仍挂账,`G-12` 由「只剩 MCP 流程模板」缩成
  **只剩发布留痕两端的命令化出口未接**(SK-25 `note` 点名:两处都在领域命令注册表之外,编排层为它挂不出命令名)。
- 校验面仍是**七面十七条**(`script`/`subjects`/`eps`/`shots`/`gen`/`review`/`film`),`Skills.playbooks()` 仍投影五条,
  `gaps()` 仍 20 键,发布门 G1–G10 与 `overall` 计数口径逐字未动。
- 周期 5(W33–W47)的独立核验报告已进主干(`w48-cycle5-audit.md`),其 8.3 给的下一目标次序与本槽实际合入次序无冲突:
  本槽合的三条都不在它评的十八支之内。
- 回归:`unit 389/389`、`integration 93/93`、`cli.smoke 70/72`(2 项失败与 `master` 同源,见 5.2)。
