# docs/skills-wave · 主线 skill 层方案索引

主线口径:**剧本 → 主体 → 分集 → 分镜 → 生成 → 审片 → 成片**。
本目录放方案、判定标准、落地记账与核验件;实现落在 `js/` 与 `server.js`/`cli.js`/`mcp.js`。

## 索引

| 文档 | 内容 | 什么时候看 |
|---|---|---|
| [w1-pipeline-skill-map.md](./w1-pipeline-skill-map.md) | 现有 skill/专家/智能体资产清单(五层)、主线七步对照表、15 项缺口(G-01…G-15)、建议纳入与建议丢弃的 skill 概念、UMD/注册表单一来源接入方案 | 想知道"现在有什么、缺什么" |
| [w1-architecture-spec.md](./w1-architecture-spec.md) | 分层与扩展点、W2/W3/W4 可执行拆分(范围/接口契约/验收/风险)、全波通用验收标准、禁止项 | 想知道"接下来怎么改、改完怎么验" |
| [w1-inventory.md](./w1-inventory.md) | 仓库真实盘点:文件级清单、UI–CLI–server–MCP 四端加载矩阵、持久化键位 | 想查某个资产在哪一端加载、落哪个键 |
| [w1-relevance-rubric.md](./w1-relevance-rubric.md) | 相关性判定标准:R1–R4 正向维度 + C1 成本、否决项 V1–V11、三档判定与降级去向 | 判断某个能力该不该做 |
| [w1-selected-skills.md](./w1-selected-skills.md) | 入选短名单 30 条内部能力 SK-01…SK-30、波次配比、键位覆盖、新登记缺口 S-01…S-07 | 查某条能力的编号、波次与落点 |
| [w1-feishu-doc-a-extract.md](./w1-feishu-doc-a-extract.md) | 外部资料 A 的结构化提取(成片产线清单),含抓取阻塞与计数出入的如实记录 | 追溯短名单的素材来源 |
| [w1-feishu-doc-b-extract.md](./w1-feishu-doc-b-extract.md) | 外部资料 B 的结构化提取(视频能力集合 53 条,12 层分类)与许可约束 | 同上 |
| [w1-feishu-raw-notes.md](./w1-feishu-raw-notes.md) | 抓取记录与交叉核对:七条抓取路径的实测过程,供复现 | 需要重跑抓取或核对原文时 |
| [w2-kb-sections-wiring.md](./w2-kb-sections-wiring.md) | `KB.SECTIONS` 升为按键取用面、5 个消费点改按键取用、压缩块由 `DIGESTS` 同键拼装 | 改知识库条目或注入点前 |
| [w2-skills-align-30.md](./w2-skills-align-30.md) | `js/skills.js` 索引对齐短名单 30 条的落地口径与记账(pending 纪律) | 往注册表加条目前 |
| [w3-g01-expert-persona.md](./w3-g01-expert-persona.md) | G-01 雇佣专家 persona 进 `/api/wf/*`:板块雇佣 > 全局雇佣,双端唯一装配口 | 改人设注入链路前 |
| [w3-g02-memory.md](./w3-g02-memory.md) | G-02 长期记忆双端复核件(零代码槽):覆盖度取证与 5 项残留登记 | 改记忆召回/注入前 |
| [w3-g03-review-step.md](./w3-g03-review-step.md) | G-03 审片升为 `Domain.workflow` 主线一等步骤及四个消费面接通 | 改主线步骤集合前 |
| [w3-g04-headless-front.md](./w3-g04-headless-front.md) | G-04 剧本拆集下沉 `wf-core` + `/api/wf/split-episodes` + 领域命令/CLI/MCP 入口 | 改拆集或 headless 起跑链路前 |
| [w4-g05-tpl-video.md](./w4-g05-tpl-video.md) | G-05 `settings.tplVideo` 定性与接入(改在提示词成型阶段,不动生成指纹) | 改模板三件套前 |
| [w4-shot-size-glossary.md](./w4-shot-size-glossary.md) | G-07 机位词表归一:景别阶梯/运镜/角度结构表下沉 `wf-core.js`、`sizeGap` 级差、15 个消费点逐点对照、指纹影响面 | 改景别/运镜取值或其消费点前 |
| [w4-subject-ref-check.md](./w4-subject-ref-check.md) | SK-12 分镜↔主体引用完备性校验(`Skills.CHECKS` 首条实现,只报不拦) | 加校验项前 |
| [w4-sk13-consistency.md](./w4-sk13-consistency.md) | SK-13 跨镜头主体一致性校验,与 SK-12 成对闭合 S-03 | 同上 |
| [w4-film-caption-check.md](./w4-film-caption-check.md) | SK-28 成片字幕时间轴与阅读速度校验,切段口径下沉 `Domain.subtitleSegs` 与合成/SRT 同源 | 改字幕切段、烧录截断线或成片面校验前 |
| [w4-audio-meta.md](./w4-audio-meta.md) | 配音渲染清单单源:音色配置/配音文本/渲染凭据口径下沉 `Domain`,成片路径按清单取音轨 | 改配音生成或成片音轨取值前 |
| [w5-cycle1-audit.md](./w5-cycle1-audit.md) | 周期 1 逐项目独立核验报告:成熟度分档、分叉风险实测、合入次序建议 | 想知道每项做到哪一步、分叉在哪 |
| [w6-integration-log.md](./w6-integration-log.md) | 周期 1 成果收敛到集成分支的记录:冲突解法、合并后测试数字、剩余分叉 | 想知道主干现在是什么状态 |
| [w6-extract-subjects-wf.md](./w6-extract-subjects-wf.md) | 提取主体接入 `/api/wf/extract-subjects`(前段命令吃到人设与协作记忆,提示词拼装/结果规整下沉服务端) | 改提取主体链路前 |
| [w7-integration-log.md](./w7-integration-log.md) | 周期 2 第 7 波收敛记录:4 条分支的合入次序与冲突解法、记忆套件补测、剩余分叉 | 想知道第 7 波收敛成了什么样 |
| [w8-split-episodes-inject.md](./w8-split-episodes-inject.md) | 剧本拆集端点补齐人设/记忆注入(`WF_BOARD` 加「剧本」板块、`buildSplitUser` 注入位、两端同装配口) | 改拆集提示词或新增 `/api/wf/*` LLM 步前 |
| [w8-script-check.md](./w8-script-check.md) | SK-07/08/09 剧本段三条校验项,闭合 S-01 的剧本半 | 加校验项前;或想知道剧本正文怎么被判定 |
| [w9-eps-structure-check.md](./w9-eps-structure-check.md) | SK-14/SK-15 分集段两条校验项,与 W8 成对闭合 S-01(六阶段覆盖 + 付费卡点位置) | 同上;或想知道分集表与集序怎么被判定 |
| [w9-integration-log.md](./w9-integration-log.md) | 剩余分支收敛到集成分支的记录:包含性实测、并集型冲突解法、合并后测试数字、剩余未合 | 想知道主干现在是什么状态 |
| [w10-cycle2-audit.md](./w10-cycle2-audit.md) | 周期 2 集成与 skill 落地独立核验报告:分支逐条成熟度、按父提交漏合的实测佐证、「合并一律取分支 head」的方法性结论 | 想知道某条分支做到哪一步;开集成槽前 |
| [w11-preflight-film-assert.md](./w11-preflight-film-assert.md) | 就绪检查消费面并集补断言:字幕面被摘掉的两种写法实测、双端行为/结构断言分工、五种摘法转红验证 | 改 `episode.preflight` 的 `result.checks` 或新增校验面前 |
| [w12-size-gap-check.md](./w12-size-gap-check.md) | SK-18 分镜景别衔接校验(`WfCore.sizeGap` 单源落成校验项:连续同景别/两极对切/整集无递进) | 加分镜面校验项或改景别级差判据前 |
| [w13-integration-log.md](./w13-integration-log.md) | SK-14/15 分集段、SK-18 分镜段与 W11 断言收敛到集成分支的记录:五面并集冲突解法、被并集断言接住的转红点、合并后测试数字 | 想知道主干现在是什么状态 |
| [w14-review-skills-check.md](./w14-review-skills-check.md) | 审片路径接入 `Skills.check` 只读消费(报告独立字段 `checks` + 弹窗/导出展示,只报不拦)、SK-03/SK-04 的 infra 面记账诚实位对齐 | 改审片报告结构、往审片路径加校验面,或动 `pending` 记账前 |
| [w15-gen-block.md](./w15-gen-block.md) | SK-21 生成步注入面落地:提示词改写人设装配口 `WfCore.genPromptSystem`、`Skills.block('gen')` 从 0 到逐字节可对账、为什么不注生成请求构造点 | 改生成侧提示词注入或去某条目 `pending` 前 |
| [w16-integration-log.md](./w16-integration-log.md) | W12 head 补合(w13 只合到父提交的漏合)与 W14 审片消费收敛到集成分支的记录:并集型冲突解法、被登记侧反查断言接住的转红点、合并后测试数字 | 想知道主干现在是什么状态 |
| [w17-preflight-stages.md](./w17-preflight-stages.md) | 就绪检查校验面清单收成双端单源表 `Skills.preflightStages()`(冲突热点收口:新增一面只改一处),含推导规则、逐字节等价取证与变异验证 | **新增校验面前必读**;改 `episode.preflight` 的 `result.checks` 前 |
| [w18-gen-prompt-unify.md](./w18-gen-prompt-unify.md) | `gen.promptSystem` 收编两端内联:`WfCore.optimizeSystem` 与 `buildOptimizeUser` 配对、CLI 侧覆盖表显式传参、为什么修订链路不接方法论块 | 改审片修订提示词或往注册表收编内联人设前 |
| [w19-g06-inject.md](./w19-g06-inject.md) | G-06 残留两条落地:「多镜头写法」进拆镜人设 `sbSystem`、「主体参考」进主体人设 `extractSystem`、键为什么只挂一个宿主、缺省提示词变长的兼容影响 | 改拆镜/提取主体提示词,或往某一步补 KB 注入前 |
| [w20-cycle3-audit.md](./w20-cycle3-audit.md) | 周期 3(W11–W19)逐项目独立核验报告:锚定 SHA、逐支成熟度 M0–M4、独立 worktree 重跑的测试数字、阻塞与下一目标 | 想知道某条分支做到哪一步;开集成槽前 |
| [w21-integration-log.md](./w21-integration-log.md) | W15/W17/W18/W19 收敛到集成分支的记录:并集型冲突逐处解法、用例名集合比对证明没删测、包含性实测的有效期口径 | 想知道主干现在是什么状态 |
| [w22-g06-check.md](./w22-g06-check.md) | G-06 校验半落地:SK-11 主体参考纪律与 SK-13 多镜头写法的生成前置 warn、判定输入为什么必须是真实生成请求、条目五条里哪几条判得动哪几条不判、新增校验项为什么不用改消费点 | 加生成侧校验项,或想知道参考图组/提示词写法怎么被判定前 |
| [w23-readme-count-assert.md](./w23-readme-count-assert.md) | 文档数字对账契约:README 的「已落地 N 条 / N 项断言 / N 面 N 条」由代码实况反推,取数口径表、变异验证与未纳入对账的部分 | 改 `CHECKS`、加用例或改这两份 README 的数字前 |
| [w24-sk10-ai-voice.md](./w24-sk10-ai-voice.md) | SK-10 文案 AI 味的校验半落地(套话硬禁词/台词书面腔/修饰副词密度三码词法命中):三张词表的取舍口径、为什么不冒充语义审片、注入半为什么仍挂 `pending` | 加剧本面校验项、改 AI 味判据,或想知道文案怎么被词法判定 |
| [w25-integration-log.md](./w25-integration-log.md) | W22 G-06 校验半、W23 文档数字对账契约与 W24 SK-10 文案 AI 味收敛到集成分支的记录:长行文档的句级三方合并解法、合并后必须重对齐的断言逐条说明、用例名集合比对证明没删测 | 想知道主干现在是什么状态 |
| [w26-sk19-stable-lex.md](./w26-sk19-stable-lex.md) | SK-19 抽卡稳定词校验面(判定输入是 `Domain.buildVideoRequest` 装出的真实提示词):三条命中码、词表从条目正文现筛的退空行为、面表自动跟上时两端实现零改动的取证 | 加分镜面校验项,或想知道生成前置 warn 判的是哪一份提示词 |
| [w27-sk10-kb-inject.md](./w27-sk10-kb-inject.md) | SK-10 注入半落地闭合 S-02:自撰「文案AI味」条目进 `KB.SECTIONS`、校验层三张词表改现取条目正文解析(不留第二份)、注入落点为什么选剧本板块方法论清单而非拆镜人设或 `DIGESTS` | 往 KB 加条目、改 AI 味词表,或要让某条校验项的判据与注入面共用一份正文时 |
| [w28-sk22-gen-check.md](./w28-sk22-gen-check.md) | SK-22 生成凭据与确认失效校验落地(生成面从零到一):五个码各报既有机制的哪一处失效点、为什么不复述 `Domain` 已有计数、`preflightStages()` 由登记推导自动多出一面的实测(两端 preflight 实现零改动) | 加生成面校验项,或想验证「新增一面只改一处」前 |
| [w29-integration-log.md](./w29-integration-log.md) | W26 SK-19 稳定词、W27 SK-10 注入半与 W28 SK-22 生成凭据收敛到集成分支的记录:两侧同处各插一块校验实现的解法、长行文档「取一侧 + 按 word-diff 折回对侧」的合并口径、面表首次从五面变六面时两端实现零改动的实测、用例名集合比对证明没删测 | 想知道主干现在是什么状态 |
| [w30-kb-skill-cover.md](./w30-kb-skill-cover.md) | KB 条目登记面契约收紧:并集断言对索引宿主那一向为什么是盲的、`SK-01.kb` 与 `KB.SECTIONS` 双向逐条对齐 + 不重复登记 + 不得有第二个全库宿主、六条变异实测、`剧本诊断`/`场面调度` 的步条目归属为什么留后续 | 往 `KB.SECTIONS` 加条目,或改索引宿主 SK-01 的 `kb` 前 |
| [w31-sk16-playbook.md](./w31-sk16-playbook.md) | SK-16 主线前段编排补齐拆集与主体提取(playbook 2 步 → 4 步):步序为什么取 `Domain.workflow` 同源、`cmds` 为什么改由 `steps` 推出、`args` 为什么一律留空(不替调用方预授权 `overwrite`) | 改编排型条目的 `steps`/`playbook` 产出前 |
| [w32-cycle4-audit.md](./w32-cycle4-audit.md) | 周期 4(W21–W31)逐项目独立核验报告:锚定 SHA、逐支成熟度 M0–M4、独立 worktree 重跑的测试数字、阻塞与下一目标 | 想知道某条分支做到哪一步;开集成槽前 |
| [w33-next-pending-check.md](./w33-next-pending-check.md) | SK-20 镜头动态感准入校验面落地(动作幅度/运镜条数/整集镜长分布三码):判定输入为什么分两段取、与 SK-13 首尾帧插值面的归属边界、S-04 为什么不清账,附短名单里剩余 pending 面的交接清单 | 加分镜面校验项、想知道大幅动作与多运镜怎么被判定,或要接手剩下那几面 pending 时 |
| [w34-integration-log.md](./w34-integration-log.md) | W30 KB 登记面契约、W31 SK-16 前段编排、W33 SK-20 动态感准入与 W32 周期 4 核验收敛到集成分支的记录:分支自测全绿为什么不等于并入主干全绿(契约覆盖面随基线走)、`cmds` 由 `steps` 推出的零实现改动实测、面内新增项两端实现零改动的实测、长行 README 按 word-diff 折回对侧的合并口径、"每推一版就重查一次包含性"连着救回两条分支的实测 | 想知道主干现在是什么状态 |
| [w35-sk24-check.md](./w35-sk24-check.md) | SK-24 方法论维度进审片报告的校验半落地(审片面从零到一):六个码各报报告里哪一处维度没到位、为什么整份判旧的报告不产出结论、`cmds` 是条目级登记时审片路径反查断言该怎么收口、面表从六面变七面的实测 | 加审片面校验项,或想知道审片报告怎么被本地判定前 |
| [w36-infra-pending-align.md](./w36-infra-pending-align.md) | `infra` 三条(SK-03/SK-04/SK-23)的 `pending` 按实况清账:清账的判据取哪几处实况、三条各自仍欠的覆盖余量点在哪、「改 `pending` 会动 `gaps` 投影」为什么实测不成立(附退回旧记账后逐字节比对投影的用例)、`gaps` 标记为什么按关联索引口径保留 | 要动条目的 `pending`/`gaps` 记账,或想知道人设与记忆注入还差哪几步时 |
| [w37-sk29-check.md](./w37-sk29-check.md) | SK-29 交付契约门校验面落地(上游定稿契约两向三码):判定输入为什么取"看板阶段 × `Domain.workflow` 实况"两份而不另立词表、判定范围为什么窄于看板那条 toast 软闸门、S-07 清了哪一半而 G-10 为什么不清账、面内新增项两端消费实现零改动的实测、十条变异逐条转红 | 加成片面校验项、想知道定稿倒序与"定稿无产物背书"怎么被判定,或要接 G-10 发布门那一半时 |
| [w38-integration-log.md](./w38-integration-log.md) | W35 SK-24 审片方法论维度、W36 infra 面记账清账与 W37 SK-29 交付契约门收敛到集成分支的记录:被点名的分支前缀在开工时还不存在时怎么捞回(连着三次改变候选清单)、两侧对同一条断言各放宽一半各加严一半时为什么要取严格并集、三处"剩余 `pending` 快照"互相打脸时为什么把判据改钉真实约束、分支记账件里的数字为什么不随并入更新 | 想知道主干现在是什么状态 |
| [w39-review-persona-mem.md](./w39-review-persona-mem.md) | 审片侧三步(分镜评审/整集共性汇总/四维成片评审)接上人设与记忆 ctx 通道:唯一注入段装配口 `WfCore.reviewCtxNote`、三处口径各取自哪条先例、缺省逐字节不变靠哪三层钉住、各步接哪个板块与召回输入怎么定、为什么不新增 `wfPersonaNote` 调用点,附「点名断言全文查其实点不住」的实测与改法 | 改审片侧提示词、往某一步补人设/记忆注入,或要给记账件的点名断言加把手时 |
| [w40-sum-system-prompt.md](./w40-sum-system-prompt.md) | 整集共性汇总的系统人设收编两端:注册表新键 `review.sumSystem`、缺省逐字节不变靠哪两层钉住、服务端为什么必须显式传覆盖表、SK-03 的仍欠段换成剧本拆集步后点名断言怎么保持点得住 | 往注册表收编内联人设、改整集审片三步的 system 半,或要接手 SK-03 剩下那处余量时 |
| [w41-integration-log.md](./w41-integration-log.md) | W37 SK-29 判定范围自纠、W39 审片侧人设/记忆通道与 W40 共性汇总人设收编收敛到集成分支的记录:点名的分支已在基线里该怎么如实记账、复核范围为什么要含基线分支自己(基线在本槽期间前进两版)、「记账诚实位」那一句被三条分支各改一次时按段分工的解法、余量清单为什么必须跟着 `note` 实况走、四类改名逐条证明没删测 | 想知道主干现在是什么状态 |
| [w42-split-system-prompt.md](./w42-split-system-prompt.md) | 剧本拆集的系统人设收编两端:注册表新键 `split.system`、拆集步为什么不接方法论块(锚点协议只切不写)、SK-03 的仍欠段换成主体提取步后点名断言为什么改查注册表与装配口签名而不查字面(同字面在另一条链路还有一处) | 往注册表收编内联人设、改剧本拆集步的 system 半,或要接手 `EXTRACT_SYSTEM` 那处余量时 |
| [w43-sk26-memory-writeback.md](./w43-sk26-memory-writeback.md) | SK-26 回流面落地(审片/发布闭环结论按板块写回既有记忆桶):回流哪几个可判定字段、整集均分为什么有意不回流、回流键 `fb` 为什么必须原地更新、四处写入点各走哪条通道且不多一次 IO、发布留痕为什么进不了 `steps`、S-08 为什么不挂到已清 `pending` 的条目上充数 | 要动记忆写入面、往闭环收尾加回流,或接手 G-11 自进化 / S-08 决策时 |
| [w44-sk05-playbook.md](./w44-sk05-playbook.md) | SK-05 编排面落地:主线全链 playbook 由已注册命令投影(`playbook()` 从 `null` 变九步)——步序为什么取 `Domain.workflow` 同源、SK-16/SK-25/SK-30 三条与全链是分段还是并列、为什么有两条命令有意不进链、`cmds` 为什么改由 `steps` 推出、`args` 为什么一律留空,附 G-12 两个投影落点为什么仍挂账与六条变异实测 | 改编排型条目的 `steps`/`playbook` 产出前;要接 G-12 的计划步骤或 MCP 流程模板那两处时 |
| [w45-extract-system-prompt.md](./w45-extract-system-prompt.md) | 主体提取的系统人设收编两端:注册表新键 `extract.system`、这一处缺的为什么不是单源而是覆盖口、`EXTRACT_SYSTEM` 常量为什么撤掉而不留别名、为什么不能照前两槽直接两端 `Prompts.get`(方法论块会被挤出装配口)、人设句字面只剩一份怎么钉(另一条链路有同字面,断言钉的是出现次数)、SK-03 的仍欠段换成 Agent 单轮对话步 | 往注册表收编内联人设、改主体提取步的 system 半,或要接手 `buildAgentSystem` 那处余量时 |
| [w46-g12-playbook-proj.md](./w46-g12-playbook-proj.md) | G-12 一半:计划步骤(`Plans.fromWorkflow`)改由 SK-05 主线全链 playbook 投影生成——投影给命令与步序、计划层只给"待不待办"的状态取材器、取材器为什么给不出命令名、需授权或需人工挑选的状态为什么一律出导航步、九步里为什么有两步不占计划步(自省出口把"选择性不占"与"漏接"分开),另给 `mcp.js` 那个零使用的 `require` 接上第一个只读出口 `hujing_playbook`(不经 CLI、零计费、`args` 不预授权),附产出的两处实际变化与六条变异实测 | 改计划步骤推导、要给 playbook 接消费方,或要接 G-12 剩下的 MCP 流程模板那一半时 |
| [w47-integration-log.md](./w47-integration-log.md) | W42/W45 两处人设收编、W43 SK-26 结论回流与 W44 SK-05 主线全链编排收敛到集成分支的记录:折回对侧增量前为什么要先查它是不是已在非冲突区自动合进来(写重一条摘要句的实测与撤法)、两条分支各清一条 `pending` 时冲突两侧为什么都不对、自动合并的断言下限被并入抬松时怎么取严格并集、叠在别的候选分支上的那条为什么排后合、名集差集里的三条旧名逐条配对证明没删测 | 想知道主干现在是什么状态 |
| [w48-cycle5-audit.md](./w48-cycle5-audit.md) | 周期 5(W33–W47)逐项目独立核验报告:锚定 SHA、逐支成熟度 M0–M4、独立 worktree 重跑的 unit/integration/cli.smoke 数字与 README 对账(本周期 README 失真为零)、注册表口径直读实测、集成槽与功能槽的基线取点分布、16 条阻塞与下一目标次序;另点名槽号 46 跳空、`w47` 自称「整合(1/4)」只做了 1 步、`js/skills.js` 那处「仍欠」清单是唯一「机械取任一侧都会绿而内容都不完整」的合并点 | 想知道某条分支做到哪一步;开集成槽前 |
| [w49-agent-system-prompt.md](./w49-agent-system-prompt.md) | Agent 单轮对话的系统人设收编:注册表新键 `agent.system`、为什么只抽人设句而把 ops 协议留在装配口(那半是 `agentNormalize` 的解析契约,开成开关就能被改坏)、浏览器多轮三份措辞为什么不并进同一个键、这一处的「两端」为什么落在取值口而不是消费点(装配口只有服务端一个消费点)、覆盖只换人设句怎么钉成断言、SK-03 的仍欠段换成浏览器多轮三份人设 | 往注册表收编内联人设、改 Agent 单轮的 system 半,或要接手浏览器多轮那三份人设时 |
| [w50-integration-log.md](./w50-integration-log.md) | W46 G-12 计划步骤投影与 W49 Agent 单轮人设收编收敛到集成分支的记录:「每推一版重做一次包含性实测」第一次捞回的是开工时根本不存在的分支、冲突里「一侧对一侧是分叉时旧实况」为什么数字往回退的那侧一定不对、并入让**非冲突区散文**过期这一类没有断言兜底该怎么通读捞出来(两处实测)、手写的摘要句被作者原话取代时怎么撤、名集并集与主干逐条相等时为什么仍要跑比对、`master` 那 2 项冒烟失败现开工作树取证 | 想知道主干现在是什么状态 |
| [w51-browser-agent-personas.md](./w51-browser-agent-personas.md) | 浏览器多轮对话三份系统人设收编:注册表新键 `agent.panelSystem`/`agent.drawerSystem`/`agent.previsSystem`、为什么取三条独立键而不是一个带变量的键(合成即失真,拆回来还会废掉用户已写的覆盖)、分集面板那处为什么要先把 system 模板抽成装配口 `AgentCore.panelSystem`、这一处的「两端」为什么只落在取值口(三处都只有浏览器一个消费点,没有 Node 第二消费点)、缺省逐字节靠哪四层钉住(多出「覆盖不串台」那层)、8 种 ctx/模式与收编前对跑的逐字节实测 | 往注册表收编内联人设、改多轮对话的 system 半,或要给多轮接 headless 消费点时 |
| [w52-g12-mcp-mid-templates.md](./w52-g12-mcp-mid-templates.md) | G-12 另一半:主线中段(主体/分集/分镜/生成)可调用流程模板——模板为什么是「调用顺序 + 参数从哪取 + 断点」而不是第二套提示词、中段为什么必须是投影的**有序切片**(四段拼接逐字节等于整段)、`STEP_META` 登记 `null`(有意不在中段)与漏登记怎么分开、`clear` 为什么不叫 `done`、缺前置为什么进 `gaps` 并 `ready=false` 而不吞掉步骤序列、断点码怎么钉成"各端真会回的码"、工具名为什么由 MCP 注入而模板层零工具名字面(`toolOf` 现取工具表 `cmd` 字段),附与 W46 的边界与六条变异实测 | 要改中段推进的步序/参数出处/断点,给流程模板加消费方,或接 G-12 剩下的发布留痕命令化时 |
| [w53-memall-headless-seed.md](./w53-memall-headless-seed.md) | 记忆播种与板块迁移的 headless 收口:`WfCore.memSeed`/`memMigrateBoard` 两个纯函数把种子表与改名表收成一份,浏览器 `memAll` 改为委托,headless 补 `POST /api/wf/memory-seed` + CLI `memory seed|migrate` + 两个同名 MCP 工具——为什么不做成"读记忆时自动播种"(读一次写一次盘、还会背着用户改提示词字面)、板块词表为什么由调用方注入而不在 `wf-core` 里写第二份、空板/未知板名/迁移零命中为什么报错而已种过再播不报错、幂等怎么让调用方省掉写盘(浏览器逐条 `Store.save()` 收敛成一次)、与 SK-26 回流面的边界(写什么/何时写/哪个派生函数)与五条变异实测 | 改记忆种子表或板块名、要给 headless 补记忆入口,或想分清播种与回流各管哪半时 |
| [w54-issues-unreviewed-sk19.md](./w54-issues-unreviewed-sk19.md) | 问题中心补两类投影:未审分集(`no-review` / `review-stale` 两条中危,与 `Domain` 审片步同码同口径、判旧那条与发布门 G3「视为未审」对齐)与 SK-19 稳定词(`shot-stable-lexicon` 低危,warn 不抬成 G2 fail)——"已有镜头"为什么靠挂载位置保证而不是新写条件、危险级为什么选 mid(G2 由 pass 变 warn 而非 fail)、为什么不挂 `episode.smartReview`(计费动作不代按)、分镜面为什么改成一次跑完按条目分挂、两处旧断言按实况翻转的理由与干净夹具为什么要补稳定词,附五条变异实测 | 往问题中心加条目类型、改审片三态口径,或要接手 `js/issues.js` 的 UMD 化与「提醒投影表」时 |
| [w55-integration-log.md](./w55-integration-log.md) | W48 周期 5 核验件、W51 浏览器多轮三份人设与 W52 G-12 中段流程模板收敛到集成分支的记录:核验槽只提交报告不登记目录索引行(这一处有契约断言兜底,撤掉即红)、叉自 `master` 的分支为什么不能看 `git diff HEAD..<branch>` 判增量、三处 README 冲突里两侧都是各自分支上的旧实况(正确值只能由合入后 live 实测给出,380+1+8=389)、同一份文件同一主题为什么一处自动合并一处冲突、自动合并的 `js/skills.js`/`tests/unit.js` 双向比对两侧 `note` 与套件增量、名集并集与主干逐条相等的逐条配对 | 想知道主干现在是什么状态 |
| [w56-aiscript-digest-prompt.md](./w56-aiscript-digest-prompt.md) | 剧本摘要人物小传步的人设改经注册表:同字面的最后一处内联为什么复用 `extract.system` 而不新开键(字面同 / 角色同 / 产物落点同三条判据,同 `def` 开两个键实测转红 4 条)、为什么取 `Prompts.get` 而不是带方法论段的装配口(那段讲的全是生图与参考图组纪律)、这一处的「另一端」为什么是"不许长出第二端"(摘要链路无服务端对端)、W45 留的「恰好 1 处」计数路障怎么反转成零内联 + 注册表命中、`aiScriptDigest` 第一条行为面断言怎么搭(沙箱真跑、逐步截获 system/user) | 往注册表收编同字面内联、改剧本摘要四步的 system 半,或要接手摘要链路的服务端出口时 |
| [w57-integration-log.md](./w57-integration-log.md) | W54 问题中心两类投影与 W53 记忆播种/板块迁移收敛到集成分支的记录:同一张 `facts` 断言表的三行为什么必须逐行取侧(两侧都是完整三行、`--ours`/`--theirs` 跑起来都绿而内容都不完整,判据只能是合入后 `js/skills.js` 三条 `note` 的仍欠段)、冲突里四个数字为什么两侧都不能要(`cli.smoke` 两侧同为 72 却都错——各自加 8 条,合到一起才是 80,同一数字两侧相等 ≠ 合并后仍正确)、并入让非冲突区散文过期的三处怎么通读捞出(接入指南 34→36 工具与两处「仍欠」清单)、W53 那部分落点为什么另代记一份(**该槽合 `w53` 取了中间点 `09887be`,漏掉 tip `0045962` 上的 143 行记账件与索引行,「W53 没有留下自己的记账件」这个判断已随 W67 就地更正**)、名集三份 tip 逐条包含的取证 | 想知道主干现在是什么状态 |
| [w58-sk25-publish-cmd.md](./w58-sk25-publish-cmd.md) | G-12 第三个落点:发布留痕做成领域命令 `project.release`——判定与写回收进 `js/release-core.js` 双端 UMD 单源(门禁结论由调用方注入、环境差异经参数注入)、headless 七项核心门与浏览器十项门的差集为什么是有意的、空项目为什么补在 `precheck` 而不是加成第八道门(加门就是抬门)、浏览器按钮为什么必须改走命令表(绕过即红)、服务端为什么不采信客户端门禁结论、`force` 是授权位而不是万能位、这个动作为什么本来就不计费、发布留痕为什么有意不串进主线全链投影,附顺带修掉的 `exec` 参数合流 bug(MCP 只传 `--args` 时 `pid` 被抹掉)与五条变异实测 | 改发布留痕/发布门的判定与写回、给发布接新出口,或想知道 SK-25 还欠什么时 |
| [w59-issues-umd.md](./w59-issues-umd.md) | 问题中心投影核收成与 `js/domain.js` 同形的双端 UMD:环境句柄(`Media`)与前端状态桶为什么必须经参数注入才能让同一份推导跑到 Node、`Skills` 为什么传解析器而 `Domain` 直接绑(两个测试沙箱有意不加载 skills)、`collect(p)` 变 `collect(p, ctx)` 为什么不算破坏、UI 段整段移出后 `window.Issues` 全局名与成员怎么保持不变;同轮把 W17 记的**提醒投影表**抽出来(面 → 校验项 id → kind/sev/挂载级别 + 早退分支前后 `phase` 与明细写法),`phase`/`cap: null`/整面结论按面缓存/`ctx` 统一传这四处为什么是"收表不改结论"的全部难点,六处逐面消费点断言按 W17 同一手法从"源码里出现过面字面量"改钉表(判据只增不减,第一版"只此一处"漏拦的那条变异当场收紧),新出口 CLI `issues` 与 MCP `hujing_issues` 为什么只列不处置,附七条变异实测与四条残留 | 要给问题中心加/改一条方法论提醒、给它接第二个 headless 消费点,或想照这个手法把别的浏览器侧推导收成双端时 |
| [w60-integration-log.md](./w60-integration-log.md) | W56 剧本摘要人物小传步人设收编收敛到集成分支的记录:折回对侧散文时「内容对不等于字节对」(两处半角 `""` 被抄成全角 `“”`,无断言兜底、通读也看不出,按首尾锚点切段逐字节比对才捞出)、`facts` 表这一槽为什么没有取侧动作而这个结论仍要实测出来(先查对侧根本没碰 `js/skills.js`,再把三行锚点与合入后 `note` 的仍欠段逐行对一遍)、叉得早的分支为什么三个数字都比基线低却不是删测、折回的散文里每个数字都要现取复核(含 278 字那个)、被点名排除的分支怎么如实记账而不做完成度判定 | 想知道主干现在是什么状态;要折回对侧长行散文,或要接 W59 那条被排除的分支时 |
| [w61-sk26-front-writeback.md](./w61-sk26-front-writeback.md) | SK-26 回流面补齐主线前段四步(理解/分镜/拆集/提取主体各按自己那一步的板块回流既有记忆桶):四步各回流哪几个数与为什么不回流集标题/主体名/模型评分(回流内容会被下一轮照抄)、`scope` 取 `WF_BOARD` 为什么让"回流下去下一轮召回得到"成为结构保证而非约定、理解为什么是三处写入点(分镜端点内部那次理解步)、提取主体为什么只能挂命令层(端点只出候选不写 state)、`withProject` 的可选 `memFeed` 为什么必须写在里面(409 重试要按最新 state 重建 meta 桶,写外面会覆盖并发写入)、四支各自的"没有结论就不写"判据、SK-04 剩三处余量为什么是判断而不是漏做,附五条变异实测与「源级位置断言整文件搜 `wfSave` 会捞到别的端点」那处补强 | 要往闭环收尾加回流、改 `memFeedback` 的派生字段,或接手 G-11 蒸馏按板块过滤时 |
| [w62-integration-log.md](./w62-integration-log.md) | W59 问题中心投影核 UMD 双端化与提醒投影表收敛到集成分支的记录:对侧叉在 W54 并入之前又整块重写同一文件时,哪些冲突块能取侧只能靠「逐条清点对侧分叉后主干在这一块落了什么」查出来(W54 三件事两件落在自动合并区、一件落在冲突块,机械取 `theirs` 会整条消失)、往对侧新结构里补回那一面为什么只能按表补一行而不能把手写取值点粘回去(表外第二处取值点当场撞断言)、收表前后同夹具逐字节对跑怎么证明"搬完结论不变"、对侧**纯新增文件**(`git status` 记 `A`、零冲突标记、散文无断言兜底)为什么同样会带回分叉时的旧实况、`cli.smoke` 名集比对为什么必须先切掉运行期载荷(照搬 W60 手法会得 7 条假差集)、上一槽记下的 tip 为什么不能当本槽输入(W60 记 `ad135f8`,实际 tip 已推到 `97e6784`)、并入让非冲突区过期的五处(三处「六条」计数 + 工具数 36→37 + 两处先于本槽存在的分级枚举不实) | 想知道主干现在是什么状态;要合一条与主干在同一文件上分头重写的分支,或要接 W58 那条被排除的发布留痕命令化时 |
| [w63-cycle6-audit.md](./w63-cycle6-audit.md) | 周期 6(W49–W61)逐项目独立核验报告:锚定 SHA、逐支成熟度 M0–M4、独立 worktree 重跑的 unit/integration/cli.smoke 数字与 README 对账、集成线 W50→W55→W57→W60 逐次取点复查与旁支 W58/W59/W61 现状、16 条阻塞与下一目标次序;另点名 W57 合 W53 取了中间点因而漏掉一份 143 行记账件与它那行索引(且集成线三处散文写成「W53 没有留下自己的记账件」)、目录索引契约只锁一致性不锁完备性(文件与索引行一起缺仍全绿)、`facts` 表只防假清账不防假欠账 | 想知道某条分支做到哪一步;开集成槽前 |
| [w64-integration-log.md](./w64-integration-log.md) | W58 发布留痕命令化 `project.release` 收敛到集成分支的记录:「两侧在同一插入点各新增一整块」这类冲突为什么不能二选一(两个「测试 23」块,机械取侧就整块丢 13 条或 12 条断言而剩下那侧全绿,解法是两块都留 + 顺号)、"合了一条分支计数就该 +1"为什么会写错数字(对侧只把 `hujing_release` 改走 `exec` 同链路,MCP 工具数从头到尾是 37;同一槽里领域命令是真的 11→12,两个数字一动一不动只能逐个 live 现取)、对侧在一整段 10742 字里只改的那**一个字符**怎么用首尾锚点 char-diff 找出来(它落在冲突块对侧、且 `contract` 的 README 数字对账只管 unit 用例数)、把合并基也跑一遍怎么用"对侧自己加了几条 == HEAD 相对基线多了几条"证明没有断言被冲突解法吃掉、`facts` 表这一次为什么三行都不动(对侧改的是 SK-26「已回流那两个闭环走哪条通道」,不是 SK-04「回流面还差哪几步」,两句话里都出现「发布留痕」和「回流」)、对侧新模块漏进架构树这类零断言兜底的漏登记怎么通读捞出、`integration` 名集 117 与用例数 118 差 1 为什么不是丢测(对侧块里的 `项目不存在 404` 与主干拆集块同名,对侧 tip 上就有) | 想知道主干现在是什么状态;要合一条与主干在同一插入点各写一块新代码的分支,或要接 W61 那条被排除的主线前段结论回流时 |
| [w65-g11-mem-scope-filter.md](./w65-g11-mem-scope-filter.md) | G-11 三面里的一面:专家自进化的蒸馏输入按该专家的生效板块硬过滤——召回串错板块只坏这一轮上下文而蒸馏是**写死进 `persona`**(注入到该专家每条链路且无撤回口),故两侧为什么不能共用一个取样口、生效板块为什么取"雇佣事实"(`板块雇佣 > 全局雇佣`,与 `personaFor` 同一套)而不新造 `role → 板块` 映射、无 `scope` 的条目为什么一条也不收、两道闸为什么都落在扣费之前(计费口径一字未动)、"回空数组是跳过的信号而不是全部板块的简写"怎么钉、既有两条用例为什么只改夹具不改结论、W43 留的那条源级路障怎么翻转并收严成"`agentMemory` 在 `experts.js` 里恰好 1 次",附三条变异实测(第三条只坏 toast 文案而不漏全桶)与 G-11 仍欠两面的如实记账 | 要动自进化的记忆源或触发时机、要给预置专家开进化入口,或想照这个手法把别的"写死型"消费点与召回侧分开时 |
| [w66-sk11-extract-note.md](./w66-sk11-extract-note.md) | SK-10/SK-11 的「人设句入注册表待 G-13」旧账按实况改写:那句人设三槽前就已进注册表(`extract.system`),这处旧账为什么能活到第六个周期(全仓唯一提到它的是一句**断言语**而不是被断言的值,`note` 正文零断言覆盖,先只改 note 照样全绿)、SK-10 那句为什么是双重失真(本条根本没有专属人设句,注入走板块方法论通道)、「仍欠」段的新锚点怎么定(只写落在本条目自己登记键上的余量,且每处先核源码还在不在,再各配一条收编即红的源级断言)、`settings.tplImage` 为什么不算欠(用户在全局默认值里改得到,写成缺口是把已能做的事记成缺口)、`gaps` 为什么一个不摘(G-13 治的是全仓 21 处内联人设,按关联索引口径落地一面不摘标记,故 `gaps()` 键与值逐字节不变)、新用例的双向性(旧账写回来红、锚点被收编也红)与五条变异实测 | 要改 skill 条目的 `note`/`gaps` 记账、要给某条缺口的"仍欠"段换锚点,或想知道某条记账是否真有断言兜住时 |
| [w67-integration-log.md](./w67-integration-log.md) | W53 真 tip 补合与 W63 周期 6 核验件收敛到集成分支的记录:上一条集成线只合到 `w53` 的中间点因而连 143 行记账件带索引行一起漏掉,怎么用 `merge-base`/`rev-list` 把这处损失定位成"1 条 outstanding"并补成祖先、同一份 README 上两处冲突为什么判据不同(索引表取并集、摘要段取我方只把指针改对,整份取任一侧都会绿而内容不完整)、纯新增文件零冲突为什么照样要接一手(索引行不补当场红)、与实况相反的那句判断怎么就地更正而不删掉历史记录(更正追加在原句旁,读者能看到当时怎么想的与实况是什么两层)、目录索引契约从"两个集合相等"收严到"每份各有自己那行 + 相对链接不许悬空"的三条变异对照,以及这条加固覆盖不到的情况 | 想知道主干现在是什么状态;要补合一条只合到中间点的分支,或要动目录索引契约时 |
| [w68-integration-log.md](./w68-integration-log.md) | W61 主线前段四步闭环结论回流收敛到集成分支的记录:任务给的两条基线规则怎么按"前缀零匹配"落到第二条、**两侧各把同一句话改了半句**为什么必须合成一句而不能取侧(SK-26 那条 `note` 里「写入点在哪几处」与「`steps` 登记几步」,一侧写"发布已命令化"一侧写"补上前段四步",取任一侧都把另一半功能写成没做;而 `steps` 数组自己干净自动合并了,散文不手改就是数组说六步散文说两步)、**三处"两侧各自成立、合到一起才失效"的源级断言**怎么只能靠合完真跑捞出(整文件搜 `memWrite` 的那条被对侧新回流点误触发、服务端写入点 5→6、切片锚点被中间插入的 `EXEC` 撑破),判据为什么只能收窄不能放宽、W63 预判的"假欠账无声"这一次为什么**不成立**(对侧自己改了 `facts` 表锚点)而同一条 `note` 里紧挨着的另一句仍是零断言(删 406 字全绿)、四个数字两侧全错时怎么逐个 live 现取(含一个先于本槽就已漂移的 `memory` 套件条数),附七条变异实测与"合并基一起跑 + 名集双向 `comm`"的零吃测取证 | 想知道主干现在是什么状态;要合一条与主干改在同一句散文上的分支,或要接 W63 核验件与 W65 那条 `G-11` 板块过滤时 |
| [w69-persona-tplimage-prompt.md](./w69-persona-tplimage-prompt.md) | G-13 欠段收一处:`settings.tplImage` 取用点(`js/persona.js` 八维度重写文生图提示词那步)的人设进注册表新键 `persona.promptSystem`——与只差一个字的 `gen.promptSystem` 为什么不能合成一个键(产物形态/参考图口径/下游消费点都不同)、键名前缀为什么取 `persona` 而不是 `subject`、为什么只收人设句(参考模板归偏好设置面、返回 JSON 归解析契约)、`settings.tplImage` 本身为什么不算缺口(用户一直改得到,记成缺口就是把已能做的事写成欠账)、纯浏览器链路的"另一端"为什么写成"不许长出第二端"、收编解决的是可覆盖而不是可 headless、「仍内联」tripwire 怎么反转并收严成"全仓持有者恰好只有注册表一份"、SK-11 的仍欠段为什么改指别人家的余量并配反向断言,附六条变异实测与 W66 并入时的取侧判据 | 要往注册表收编别的内联人设(尤其剧本模块那几步)、要改主体八维度重写那一步,或想知道 G-13 还欠哪 20 处时 |
| [w70-integration-log.md](./w70-integration-log.md) | W65 蒸馏输入按板块过滤与 W66 记账按实况改写收敛到集成分支的记录:两条槽同叉在 `w62` tip 而基线已走过 `w64`/`w67`,故 8 处冲突的对侧文本**全是分叉那一刻的实况**、判据只能逐处问"这一块分叉后主干又落了什么"(整行取任一侧会绿而内容不完整:一次丢掉 `release-core`/`project.release`/W65 蒸馏面三段)、对侧在**非增量区**原样带回两句已被 W67 更正过的判断(W53 没留记账件 / G-12 只剩发布留痕未接)怎么靠通读认出来、用例数为什么两侧的绝对值都不能折回而两侧的增量相加恰等于合并后 live(403 → 410/404 → 417)、`memory` 套件那个数为什么有三个候选而活的是第四个、点名要保的五处怎么在合并后逐条现查、`facts` 与 `gaps` 为什么按现况一字不动 | 想知道主干现在是什么状态;要合两条分叉点相同但落点不重叠的旁支,或要接 W61/W68 那两条被排除的分支时 |
| [w71-script-four-prompts.md](./w71-script-four-prompts.md) | 剧本板块四步内联人设收编:注册表新增四条独立键 `narration.system`/`reading.system`/`concept.system`/`light.system`——四步角色(解说编剧/导演组围读会/开拍前定调导演/摄影指导)与产物落点都不同,为什么不合成带变量的一个键也不与既有导演类人设复用、注册表顺序为什么按产品流程而不是源文件行号、四份返回 JSON 字段契约为什么留在 user 半、这四步的「两端」为什么只落在取值口(纯浏览器链路,无服务端/CLI 对端)、行为面为什么没有沙箱真跑那一层(handler 在 DOM 闭包里)而配对正则怎么补上这一面、键为什么登记在 SK-03 名下而 SK-10 的 `note` 只改记账、SK-10 那句「人设句入注册表待 G-13」两层不实各是什么、仍欠段的新锚点怎么现场核过再写、「四步仍内联」的反向判据在本槽基线上根本不存在时怎么直接立正向那一侧并按全仓持有者名单收严 | 往注册表收编内联人设、改剧本页/构思页/制片页那四步的 system 半,或要接手剧本摘要三处策划人设时 |
| [w72-integration-log.md](./w72-integration-log.md) | 周期 6 之后两条并行集成线收成一条的记录:两条二选一基线规则怎么按"`merge-base --is-ancestor` 现取"落到第一条、"基线已含就不重复合"怎么用祖先关系而不是分支名判、**同一次冲突的相邻两行为什么取了不同的侧**(判据是"哪一侧描述的是合并后的实况"而不是哪一侧更新:余量句取回流面补齐后的一侧、专家段取有蒸馏板块过滤的一侧)、一条 `note` 的三句怎么两句取一侧一句取另一侧(`steps` 数组自动合成六步而散文说两步,与"读记忆不按板块过滤"那句合并后已为假)、W67 就地更正过的那句判断为什么机械取对侧会被改回去、**整行重写长行散文时半角引号被打成全角**怎么靠逐字符 `charCodeAt` 比对捞出、两侧各加一段时相对次序怎么对着套件实测排、四个数字里两个两侧都错且错法不同怎么 live 现取,附十条变异实测(含 W67 索引加固对"连人带账一起撤"只封住散文点过的那一半、零引用的记账件仍全绿)与"合并基一起跑 + 三套件名集双向 `comm` 全空"的零吃测取证 | 想知道主干现在是什么状态;要把两条并行集成线收回一条,或要合一条与主干在同一条 `note`/同一句散文上各改一半的分支时 |
| [w73-voice-director-prompts.md](./w73-voice-director-prompts.md) | G-13 欠段里配音导演那两处内联人设的收编:两处 `system` 字面逐字节相同(按口径允许共用一键)为什么仍取两条独立键(键位是持久化面、现在拆零成本以后拆要动用户数据;批量那步要顾角色间的音色区分度而单个那步没有这个约束)、契约半为什么留在调用点(音色库取值范围与返回 JSON 约定连着 `voices.includes` 与按名回填,改坏就整条退随机)、这两条的「两端」为什么只落在取值口(headless 没有"按人设推音色"这个动作)、两步零计费怎么钉、W66 留在 SK-11 那条用例上的「仍内联」判据怎么按实况翻转成"同文件恰好 1 处"、`gaps` 为什么一个不摘(G-13 基线 24 处 → 本槽后 22 处),附九条变异实测与 G-13 逐文件余量清单 | 要往注册表收编别处的内联人设、要动音色推荐两步的提示词,或想知道"允许共用一键"时该怎么判时 |
| [w74-digest-three-prompts.md](./w74-digest-three-prompts.md) | 剧本摘要链路通读/汇总/集纲三步的策划人设收编为注册表新键 `digest.planSystem`:"一个键三个取用口还是三个键"为什么必须先逐字节核对三处 `system`(`cat -A` 比对引号内字节序列,含全角句号,缩进不同只是嵌套层级)再按"字面同/角色同/产物落点同"三条判据判、同函数里的第五步人物小传为什么不能一起收(角色与落点都不同,仍听 `extract.system`,界线由"两键不同值"正向钉住与"本文件里 `extract.system` 仍恰好 1 个取用口"反向钉住)、为什么不为它包第四个装配函数(KB 里没有可接的方法论条目,接「主体参考」会让缺省从 14 字变 292 字)、取用口计数为什么钉 3 而不是写"包含"或"不得出现"(两种写法各点不住"三处只改两处"与"三口合并成一处")、变异 2 与变异 3 为什么要分开跑 | 要收编一处同字面重复的内联人设、要判"一键多口还是拆多键",或要动剧本摘要链路那四步提示词时 |
| [w75-integration-log.md](./w75-integration-log.md) | 两槽内联人设收编(W69 主体八维度重写文生图那步 / W71 剧本板块四步)并进同一条集成线的记录:注册表两处插入点相邻却不重叠因而干净合成并集、**三处「对方仍内联」反向断言只有一处出现在冲突里**(另两处压在基线的同一条 W66 用例里、两槽的 `merge-base` 都早于它并入,`git` 全程干净合上,只能靠合完真跑捞出)、两侧在同一插入点各加一块时**块尾那一行是两侧共用的**(机械两留语法当场断,`node --check` 报 `Unexpected token`)、同一句仍欠段为什么在一个槽内两次失效(前一槽点名的正是后一槽要收的)、**同形记账两侧断言锚点宽窄不同导致一边红一边全绿**(`owed.includes('js/episodes.js')` 在真句假句里都成立)、四个数字三处口径各自 live 现取、指针那行只做定点替换并按公共前后缀复核零全角引号,附十一条变异实测与三方名集双向 `comm` 全空的零吃测取证;另记 `w70` 记账件漏在集成线外(W53 那个洞的第二次发生) | 想知道主干现在是什么状态;要合两条收编面首尾相接的分支,或要接 W73/W74 与补合 `w70` 记账件时 |
| [w76-sb-board-prompts.md](./w76-sb-board-prompts.md) | 分镜脚本创作层两处内联人设收编:**"两处该一键还是两键"怎么先 grep 原文再判**(两句 def 不同,角色/产物落点/失败行为三面都不同,故两键)、为什么不并进只差前六个字的 `sb.system`、键名怎么排(前缀取分镜域 `sb.`,中段 `board` 取所属板、尾段对上两步的产物字段 `scriptBoard.scenes`/`beat.shotsDraft`)、注册表位置为什么按 tab 次序排在智能分镜之前、返回 JSON 的 `key`/`shots` 为什么必须留在 user 半不开放(② 的回填按 `场次号.节拍号` 正则定位,改一个字整轮解析失败)、**handler 藏在闭包里的两步怎么做出行为面真跑**(绑定用的是具名选择器,给个只认那两个选择器的假 `host` 就能取回 `onclick` 直接 `await`,从上游请求体上截获 `system`)、"翻转 tripwire"在基线上找不到反向那一侧时该怎么落地(逐条 grep 取证后直接立正向,并把第二条收严成全仓持有者名单),附八条变异实测(含"越界去收 `js/episode-util.js` 三处"当场红在 W56 那条行为面上、别处抄第二份字面红在全仓名单上)与 G-13 由 20 处减为 18 处的逐文件盘点 | 要把某个模块的内联人设收进注册表、要判两处人设该合一键还是分两键,或想给"handler 挂在 DOM 闭包里"的步骤补行为面用例时 |
| [w77-event-graph-prompt.md](./w77-event-graph-prompt.md) | 分集页事件图谱拆解步的人设收进注册表独立键 `graph.system`:任务给的"仍欠哪一步"前提为什么先 grep 核实再动手(基线上 W71 未合入,五处内联里只有第 1075 行属本槽,靠计费登记名/user 半锚点/产物落点三个锚点认步)、为什么是独立键而不与剧本板块其余四步合成(五步角色互不相同——拆/改写/评/定/排,合成等于改一处改掉五个角色的定位;没有同字面就谈不上复用)、JSON 契约那半为什么不开放(`events` 字段名改一个字整集拆不出事件且逐集退费)、缺省逐字节靠哪四层钉住(注册表字面 + 取值口与 user 半锚点配对 + 全仓唯一持有者 + 不冒充双端)、SK-03 与 SK-10 各记什么(键宿主在 SK-03、SK-10 只写"这一处已收编"且写在「仍欠」之前)、W66 那三处「仍内联」断言怎么按方向翻面,附十三条变异实测(含"合成复用"与"反向撞车"两个方向,以及变异 6 为什么红在另外两条断言上) | 要把某处内联人设收进注册表、要判断两处人设该不该合成一个键,或要给某条记账的"仍欠"段翻面时 |
| [w78-beatboard-prompt.md](./w78-beatboard-prompt.md) | 节拍板 AI 拆解那步的人设句收进注册表**独立键** `beat.system`:为什么是新键而不是复用既有键(字面/角色/产物落点三条判据全不成立,改取 `und.system` 当场红 2 条)、契约半为什么"半不开放"且那半到底是哪半(user 半的 5 段段名/宫格数/衔接词表/返回结构各连着 `applyAIFill` 的一个消费点,段数写成 6 段第 6 段直接被丢)、缺省逐字节靠哪三层钉住、**取用点为什么只能"按源码原文取出那一行表达式求值"而不是沙箱真跑整条链路**(`aiFillBeats` 是私有函数、只经整屏 `innerHTML` 的按钮绑定触发,DOM 归 e2e)以及这个形态钉不住什么的诚实位、新键为什么登记在 SK-14 而不是 SK-03(节拍板本来就是 SK-14 的注入落点;那一步不注生效人设也没有服务端对端,登进 SK-03 是把不实的事记成实的);另**新立全仓内联人设持有者名单断言**——G-13 的余量第一次有机器可查的判据(文件:处数,收编一处即少一处、任何文件新长出一处即多一处,双向都实测过),四类有意的例外与"旧口径 19 处 / 名单口径 27 处不是忽然多出 8 处"的口径说明,附十一条变异实测与名单判据的已知盲区 | 要收编某处模块内联人设、要判"这句人设该新开键还是复用键"、想知道 G-13 还剩哪些文件哪几处,或要动那张全仓名单的判据时 |
| [w79-editors-prompt.md](./w79-editors-prompt.md) | 漫剧编辑器「AI 生成对白」那处内联人设收编为独立键 `comic.bubbleSystem`:一整串字面里哪一段是措辞、哪一段是**解析契约**(type 词表就是 `TYPES.includes` 的判据、JSON 形状就是两种解析分支、条数就是 `slice(0,4)`),故只收人设句、契约半提成装配口常量 `BUBBLE_CONTRACT` 让断言现取;提示词说「30 字」而代码钳 60 字这处既有落差为什么一字不动(改它是行为变更不是收编);新键为什么挂 SK-03 却要在「仍欠」段写清"该步不过本条的 ctx 通道"(不注入生效人设与协作记忆,只是人设句进了表);`gaps` 为什么一个不摘;以及**全仓人设字面持有者名单**怎么立(按「以 `你是` 开头的字面」逐文件普查,注册表 `def`、专家人设库与模块内联三类混在同一张表里如实计数,兜底缺省与占位文案也计进去——例外条款越多名单越容易被绕过;新增一处与收编一处都先红),名单判据封不住什么,附九条变异实测 | 要收编某一处内联人设、要给"人设散落在哪些文件"这件事立一张查得动的名单,或想知道某处提示词的可覆盖边界该切在哪一刀时 |
| [w80-integration-log.md](./w80-integration-log.md) | 补合 `w70` 集成线真 tip(W72 当时只取到 `1e4627c`,漏掉后来那条 W70 自己的记账件与索引行——W53 那个洞的第二次发生)并把四条内联人设收编槽(W73 配音导演两步 / W74 剧本摘要三步 / W76 分镜脚本创作层两步 / W77 事件图谱拆解步)并进同一条线的记录:注册表同插入点撞车时怎么按产品流程排并集(19 → 25 条)、**反向断言是单向的且不止一条**(基线上压着 W66/W69/W71 三条"某某步仍内联",四个槽的 `merge-base` 都早于它们的现措辞,`git` 一路干净合上,合 W74 后 2 红、合 W77 后 4 红,只能靠合完真跑捞出)、翻面为什么要连记账一起翻并给新锚点配反向断言、`} },` 块尾这次怎么用"替换 `=======` 那行"固定成三步不再踩、**同名 helper 两块都留会静默互相覆盖**(`loadPersona` 后定义的赢,红的却是别人家的断言值)怎么合成一个按上下文回罐头的 helper、`note` 措辞与对侧断言锚点不合时为什么改措辞留断言、一个数字四处口径怎么各自 live 现取,附"每个 tip 相对自己 `merge-base` 的用例增量相加恰等于合并后 live(428+0+1+2+2+2=435)"与三套件名集十八次 `comm` 全空的零吃测取证,以及 G-13 由 19 处减为 11 处的逐文件盘点 | 想知道主干现在是什么状态;要补合一条只合到中间点的分支、要一次合多条收编面相邻的槽,或要接 W78/W79 那两条被排除的分支时 |
| [w81-gsettings-prompt.md](./w81-gsettings-prompt.md) | `js/gsettings.js` 剩余内联人设收编:导演设定五维生成步的人设进注册表新键 `dirset.system`——同文件三处 `你是` 命中里为什么只有一处算内联人设(另两处是输入框 `placeholder` 与正文在 `js/experts.js` 的 `FORGE_SYS`)、为什么不与只差「影视/短剧」两字的 `und.system` 复用(产物落点是全剧五维 vs 本集理解)、键前缀为什么取 `dirset` 而不是 `director`(后者已被注入面占着)、这一步比前几槽多出的那条边界(LLM 失败走 `dirFallback` 回退模板,覆盖只作用于 LLM 那一路、回退文案逐字节不跟随)、为什么本槽能有行为面真跑那一层(`window.genDirectorSetting` 本就为精细模式挂在 window 上,不必造 DOM)、**全仓内联人设持有者名单**的第三种口径:判据怎么写成机械扫描的三种形态(`system:` 值位 / 具名人设常量 / 装配函数直接 `return`)、为什么 `js/experts-data.js` 的预置专家 `persona` 与 `placeholder` 有意不在判据内、**这个数与 W71 记的 15 为什么不矛盾**(旧 grep 只数单引号直接字面,多出的几处一直都在)、本槽落点为什么钉成"`js/gsettings.js` 整条从名单消失"而不是减到 0 还挂着,附七条变异实测(含名单数少了与数多了的两向守卫) | 往注册表收编内联人设、要改导演设定生成步,或想知道 G-13 还欠哪些文件各几处时 |
| [w82-proj-shell-prompt.md](./w82-proj-shell-prompt.md) | 剧壳发行文案包内联人设收编:注册表新增独立键 `dist.copySystem`——"点名 1 处"怎么先 grep 核实再动手、键名前缀为什么取 `dist` 而不是 `shell`/文件名、后缀为什么是 `copySystem` 而不是 `system`、一条 system 串"人设句 + 现拼 KB 尾 + user 半契约"三段怎么分(只收第一段:KB 正文单源归 `KB.SECTIONS`、六字段返回契约留 user 半不开放覆盖,两种开法各配一条变异)、注册顺序为什么排在成片审片之后而不是末尾、行为面为什么仍没有沙箱真跑那一层(handler 在 DOM 闭包里)、键为什么又登记在 SK-03 名下而 SK-10/SK-11 的 `note` 一字未动、**全仓持有者名单怎么从"每槽各写一次局部扫描"收成一条按 `Prompts.list()` 现推的用例**(新增键自动进名单、`tests/` 有意不进名单、为什么不改写 W69/W71 那两条),附十一条变异实测(含"越界去收别人家余量"先红的是别人家的记账那一条) | 往注册表收编内联人设、改剧壳页发行文案包那步的 system 半,或要接手其余内联人设时 |
| [w83-proj-upload-prompt.md](./w83-proj-upload-prompt.md) | G-13 欠段再收一处:拉片建集逐段画面理解那步的人设进注册表新键 `rip.system`——键名前缀为什么取步名 `rip` 而不是文件名、为什么不与 `split.system`/`und.system` 并键(建分集的两条入口角色不同、判定输入一个是剧本文本一个是关键帧)、注册表位置为什么紧跟 `split.system` 并把"两条入口相邻"钉成断言、为什么只收人设句(段号时段的现拼提示与 `{"shot_desc":…}` 契约留在 user 半)、行为面为什么没有沙箱真跑那一层(handler 在 `openRip` 的 DOM 闭包里)而配对正则怎么补上、`js/proj-upload.js` 的内联人设计数怎么从 1 钉成 0、全仓持有者名单为什么新立一条而不是并进既有那两条,附变异实测与全仓内联人设余量清单(15 → 14 处) | 要往注册表收编别的内联人设、要改拉片建集那一步的 system 半,或想知道 G-13 还欠哪 14 处时 |
| [w84-sb-views-prompt.md](./w84-sb-views-prompt.md) | 镜头「按指令改」内联人设收编:注册表新键 `gen.editSystem`——键名前缀为什么取 `gen` 而不是 `sb`(与同层四策略优化相邻落位)、与只隔一条的 `gen.promptSystem` 为什么不能复用(那条经装配口还要接 KB 抽卡块,合成即改变两步的缺省)、"契约半"这一刀切在哪(人设句 + 改写纪律可覆盖,返回 JSON 字段名与镜头上下文摘取不开放)、handler 挂在 DOM 闭包里时行为面怎么补上沙箱真跑那一层(两层弹窗用极简选择器桩逐层驱动到 `chatJSON`)、同形的主体「按指令改」为什么有意不收并配反向断言、逐槽各写一份的持有者名单为什么收成**按注册表现取**的一条(每条 `def` 全仓扫一遍 + 反查同 `def` 双键),附九条变异实测与 G-13 余量的现场清点 | 往注册表收编内联人设、改镜头「按指令改」那步的 system 半,或要接手 `js/role-editor.js`/`js/sb-board.js` 那几处内联人设时 |
| [w85-integration-log.md](./w85-integration-log.md) | 四条内联人设收编槽(W78 节拍板拆解 / W79 漫剧气泡对白 / W81 导演设定五维 / W82 剧壳发行文案包)并进同一条线的记录:注册表四处插入点互不重叠因而四次全自动合成并集(25 → 29 条)、**同一件事三个槽各立了一张持有者名单且判据互不相同**(只数系统人设位 / 数全部 `你是` 字面含注册表 `def` 与专家库 / 三种形态且排除注册表),三张的期望串都写在各自 `merge-base` 那一刻的实况上、四次合并里每张都要按合并后 live 重算一遍(逐次取值表见 4.4)、**"全表互不相同"这类全称断言碰上别的槽有意留的同值组**怎么收窄而不是放宽(点名唯一那一组:别处再抄一份仍红、把那组合成一键也红)、`} },` 块尾为什么三次里踩到三次而第四次两侧块尾各自完整不能按经验去补、`js/skills.js` 的 `prompts` 数组三次都是真冲突(机械两留会得到两行 `],`)、仍欠段为什么必须逐句合并而不能取侧(两侧各自带着查自己那几个锚点的断言)、**索引表那行在冲突块外自动合上导致同名索引出现两行**(索引契约报得出重复但报不出措辞过期),附"每个 tip 相对自己 `merge-base` 的用例增量相加恰等于合并后 live(435+2+1+2+3=443)"与三套件名集十五次 `comm` 全空的零吃测取证(含"抽名前要先切掉回执载荷否则假报 7 条缺失"),以及 G-13 三张名单各自的余量盘点 | 想知道主干现在是什么状态;要合多条各自新立了同类全仓名单的槽,或要统一那三张持有者名单的口径时 |
| [w86-agent-ops-prompt.md](./w86-agent-ops-prompt.md) | `js/agent-ops.js` 两处内联人设收编为两条独立键(执行回执核验修复 `agent.selfFixSystem` / 会话纪要蒸馏 `agent.compactSystem`):"这个文件到底有几处内联人设"为什么要三条 grep 交叉取证再动手(`system:` 五处里两处是注释、一处是已进表的 `prearrPrompt`)、"一键还是两键"的判据为什么在**逐字节比对那一步就结束**(两句 `def` 不同即不可能共用,角色不同只是佐证;反过来 `def` 相同也不自动合并——键位是持久化面)、"人设句到哪里为止"怎么按下游解析分支切(回执的 `✕`/`⊘` 记号对着 `receipts.filter`、修复 `ops` 白名单对着 `splitOps`/`applyOps`、`≤150字` 对着 `slice(0,300)`,故契约半一律不开放覆盖)、handler 在模块闭包里但入口挂在 `window.AgentOps` 上时行为面怎么真跑截获 `system`(含 fire-and-forget 那步等微任务)、缺省逐字节靠哪四层钉住(注册表字面 + 取值口与该步锚点配对 + 行为面覆盖只换人设句 + 全仓持有者名单)、**记账件在「仍欠」前后怎么断句**(第一版把"已收编"那句写在了「仍欠」之后,切出来的仍欠段仍带 `js/agent-ops.js` 字面而当场红),附九条变异实测(含"退回内联红 3 条——两条红在别人家翻过面的路障上")与一处先于本槽就已过期的散文怎么通读捞出 | 要把某个模块的内联人设收进注册表、要判两处人设该合一键还是分两键,或要给某条记账的「仍欠」段翻面时 |
| [w87-role-editor-prompt.md](./w87-role-editor-prompt.md) | 主体编辑页「按指令改」那步的人设收进注册表独立键 `persona.editSystem`:**处数先 grep 核实再动手**(全文件恰好 1 处,`chatJSON` 调用点也只有 1 个)、类别插值那一句为什么取"一条键 + `{kind}` 变量"而不是三条键也不是写死类别(角色同、产物落点同,写死则缺省当场变——这是人设类键里第一条带变量的)、为什么不与分镜那侧同形入口合成(设定师 vs 改图师、`s.prompt` 重出主体参考图 vs 重出镜头素材,措辞也不同字面)、契约半这一刀切在哪(`{"prompt":…}` 连着回填与那次已扣的 `llm.optimize`,改坏即整轮失败;开放的恰是用户最想调的字数与保留纪律)、**逐槽各写一份的全仓持有者名单怎么更新成按注册表现取的一条**(26 条 `def` 逐条扫,新键自动进名单;反查不能照搬"全部 def 互不相同"——音色推荐那一对本就明写同字面,故写成"同 def 的键集合恰好是那一对";带变量的 `def` 另按不变量段再扫一遍)、**handler 挂在 `openModal` 闭包里且模块无出口的步骤怎么做行为面真跑**(极简 DOM 桩逐层驱动编辑页→二级弹窗→改写→应用,顺带实测"应用后真的进了 `genMainImage`"),附十三条变异实测(含"变量被写死"只红一条的如实口子、"反向撞车"红到 5 条里 3 条是既有守卫、"越界去收 `js/sb-views.js`"当场红在两条别人家的反向断言上)与 G-13 由 11 处减为 10 处的逐文件盘点 | 要把某处内联人设收进注册表、遇到人设句里带变量不知该分键还是带变量、要给"handler 挂在弹窗闭包里"的步骤补真跑用例,或要把逐槽维护的持有者名单收成一条时 |
| [w88-experts-forge-prompt.md](./w88-experts-forge-prompt.md) | 专家工坊两步内联人设收编:注册表新增两条独立键 `forge.system`(锻造器)/`forge.evolveSystem`(进化器)——任务点名 `FORGE_SYS` 时那句"gsettings 只引用常量"为什么是**落点判据**(人设的持有者是 `experts.js`,收编不该让工坊页多知道一件它不需要知道的事,故消费侧一行未改)、同一个页面上的两处为什么仍是两条键(输入/动作/产物落点/计费标签/多轮全不同——无中生有铸新专家 vs 就地改写已有专家)、`js/experts-data.js` 预置 persona 库为什么不在收编面内(那是专家数据不是提示词,越界收它当场红 9 条)、**为什么必须导出成 getter 而不是模块级常量**(照原样写 `const X = Prompts.get(…)` 会把覆盖表冻结在页面加载那一刻,用户改完提示词切回工坊 tab 吃到的还是旧值——覆盖看着生效其实没生效,比不收编更糟)、人设句与「只返回严格 JSON:」原在同一行时为什么直接相接不补 `\n`、进化器的契约半为什么是全仓最该留在外面的那一处(别处改坏是报错+退费,这一处改坏落到"无新增条款"分支**静默白扣**)、两条键为什么各归 SK-02/SK-26 而不都挂 SK-03、`G-11` 为什么要补一句"改得到提炼口径,改不出自动触发"划清界线,附十七条变异实测(含**变异 14 第一版没咬住**:锚点字面在改动前就存在于 note 第一句,与 W39 那个洞同形,须收窄到「仍欠」段内两个词)与 G-13 计数口径漏掉常量形态的补记 | 要往注册表收编内联人设(尤其"消费侧只引用一个常量"这种形态)、要判同一页面上的两处人设该合一键还是拆两键,或要接 `G-11` 自进化与 `G-09` 专家条目 `skills[]` 时 |
| [w89-plans-prompt.md](./w89-plans-prompt.md) | 制作计划生成步(项目页「计划 → 让助手规划」)的内联人设收进注册表独立键 `plan.system`:任务给的"可能已无内联"前提为什么先按 `js/plan*.js` glob 与 `grep` 核实再动手(命中一处,故是真收编而不是只写核实)、为什么不与同一个助手名下那四条对话人设合成(产物形态不同——出的是要落库并逐步执行的步骤表,不是对话回复;并成一键等于改对话口吻时顺手改掉拆步定位)、注册表位置为什么排在对话四条之后、命令白名单与 `title`/`steps` 契约为什么不开放(白名单现取命令表、契约连着步骤钳制与失败退费,改坏即整轮拆不出步骤)、缺省逐字节靠哪六层钉住(注册表字面 + 沙箱真跑整条比对 + 覆盖只换人设句 + 取值口与 user 半配对 + 全仓唯一持有者 + 不冒充双端)、**"缺省不变"与"覆盖跟得上"两条断言为什么必须都在**(变异 2 退回内联时前者反而全绿)、键为什么挂 SK-03 而 SK-05 只补一句"另一条生成路径不切主线投影",附十四条变异实测与 G-13 由 11 处减为 10 处的逐文件盘点及"计数口径不覆盖 `{role:'system',content:…}` 形态"的交接 | 要往注册表收编别处的内联人设、要动制作计划的 LLM 规划步,或想给"可直接 await 的 LLM 步"补行为面用例时 |
| [w90-integration-log.md](./w90-integration-log.md) | 四条内联人设收编槽(W83 拉片建集 / W84 镜头按指令改 / W86 Agent 两个辅助步 / W87 主体按指令改)并进同一条线的记录:注册表四处插入点里三处干净合成并集、只有一处两侧同抢表尾(29 → 34 条,块尾那行 `},` 落在冲突块之后由两侧共用,机械两留 `node --check` 当场断)、**同一条「仍欠」段在一条线里被翻了三次面**(基线点名 `js/agent-ops.js`+`js/sb-views.js`,而这条线上 W84 收掉 `sb-views`、W86 收掉 `agent-ops`、W87 收掉接替上去的 `role-editor`),每次都要同时改 `note` 的仍欠段、把反向断言改指新锚点、给刚收掉的那处补一条翻面后的反向断言,锚点为什么一律选"这条线上不会再被收掉"的那几处(最终落在 `js/experts.js` 与 `js/plans.js`)、四个槽给的三张全仓名单期望串**没有一个在合并后成立**且判据还互不相同故一律按 live 现取重写(逐次取值表见 4.4),而 W82/W84 已收成"按 `Prompts.list()` 现推"的那两条**一次没改过**——现推形态第一次拿到实测对照、`} },` 块尾四次里踩到三次而三次的块尾各不相同(用例块尾 / 表项块尾 / 顶层 helper 的函数尾,补哪一行只能逐次看,W83 那次两侧各自完整一个字都不能补)、`js/skills.js` 四次都撞在 SK-03 的 `prompts` 数组或紧跟的 `note` 上、"全表 `def` 互不相同"这类全称断言碰上音色推荐那组有意同值怎么收窄不放宽,附"每个 tip 相对自己 `merge-base` 的用例增量相加恰等于合并后 live(443+2+3+2+3=453)"与三套件名集十五次 `comm` 全空的零吃测取证,以及 G-13 三张名单各自的余量盘点与"W88 收的正是本槽给仍欠段挑的锚点"这处有意留下的路障 | 想知道主干现在是什么状态;要合多条收编面在同一条「仍欠」段上首尾相接的槽,或要接 W88 那条被排除的分支时 |
| [w91-intent-router-prompt.md](./w91-intent-router-prompt.md) | 全局抽屉意图路由那步的内联人设收编为独立键 `agent.routeSystem`:旧 `system: '你是` 口径为什么漏计它(system 不是对象字面量里的值而是局部 `const sys` 接一串模板拼接)、同文件另两句上下文框定语为什么有意不收(它们是同一个三元的两支——块尾那句引用的是按板块表与项目实况现算的数据,收一支不收另一支等于让两条路一条可覆盖一条不可)、一条 system 串四段(人设句 / 按 `AGENT_BOARDS` 现拼的板块清单 / 判据句 / `{"board","reason"}` 返回契约)怎么只切第一刀——**板块清单为什么不能进覆盖面**(板块 key 正是下一步 `find` 的匹配键,允许用户存一份过期板块表就是让路由整轮命不中)、判据句为什么也留在装配口(要开放它就得把清单做成 `{boards}` 变量走 `Prompts.fill`,把这一条从"纯人设句 + `Prompts.get`"的形状里拆出去)、**取值口的求值时机第一次被写成断言**(在 `routeIntent` 函数体内现取 vs 提成 IIFE 顶层常量把覆盖表冻在加载那一刻,与 W88 `FORGE_SYS` 同纪律,变异 8 实测)、IIFE 私有函数怎么做出行为面(按源码原文取出那段装配式在沙箱求值)以及这个形态钉不住什么的诚实位(证明不了它真被交给了 `API.chatJSON`,由 `step:'route'` 计费槽位的配对断言补)、**三张判据不同的全仓名单为什么要按各自口径分别重算**(前两张是 8→7 文件各减一处,第三张含注册表 `def` 故总数不变、只是从模块搬进 `js/prompts.js`,照前两张的经验去减总数会把它改错),附十一条变异实测(含变异 1 为什么红 6 条、变异 9/10 的两向守卫) | 要收编某处"人设句 + 现拼数据 + 判据 + 契约"四段形状的内联人设、要判某个键的取值口该写在哪一层,或要动那三张持有者名单时 |
| [w92-proj-planner-prompt.md](./w92-proj-planner-prompt.md) | 项目实验台两步内联人设收编:AI 策划对话 → `planner.chatSystem`、剧本译制 → `trans.localizeSystem`——"`def` 相同才可共用键"这条判据怎么落到两处真实字面上、**带变量的键**怎么收(`${tgt.name}`/`${tgt.lang}` 进 `vars` 走 `Prompts.fill`,登记了占位符还要正查 `def` 里真有它)、**契约半在 system 半而不在 user 半**时怎么切(「第X集」分集标记留成取值口常量 `TRANS_CONTRACT`,并新加一层"契约与解析侧同集"——把源码那条 split 正则现取出来真切两集)、**为什么两处取值口都不能提到模块顶层**(IIFE 顶层预取会把覆盖表冻在页面加载那一刻,缺省与覆盖两条路径都还是对的,只有源级看得见)、`def` 字面名单为什么拦不住带变量那处的退化(内联版是插值、字面对不上,只有源级配对断言拦得住)、三张判据不同的全仓持有者名单本槽一张不变一张减两处一张减两处又加两处的逐张重算,附十五条变异实测 | 要收编带运行期变量的内联人设、要判"契约半该留在哪一侧",或要动那三张持有者名单时 |
| [w101-mem-cap-priority.md](./w101-mem-cap-priority.md) | 记忆桶满 50 条时的淘汰优先级改为「先挤最旧的自动回流条(带 `fb`)、用户自沉淀的「记住…」留住」:为什么 `fb` 原地更新只压住"同一集反复跑"而压不住"跑二十集"(`fb` 键粒度里有集号,每集稳定占 3 条,`3 + 3×集数` 到第 16 集即 51 条开始砍数组头部,而用户手打的条目正是最早写进去那批)、两类条目的可再生性为什么不对等(自动条下一轮原样重写、用户条永久丢失且无提示)、**本次刚写下/刚更新的条目为什么必须排除在淘汰范围外**(否则全是用户条的桶里新回流唯一命中的就是自己,这次回流静默失效)、为什么不抬 50 也不给用户条留固定名额也不分桶(容量不是问题所在;两端写入面各写一份 50 字面且有源级断言钉着;分桶要连召回一起重做)、为什么用 `while` 逐条挑而不是按多出 k 条砍头 k 条(前段四步一次调用可能写四条),附"老写法回来即红"的两层变异实测与"两处用户写入面仍是裸 `slice(-50)`、用户新加一条仍可能挤掉别的用户条"这处残留的接法与代价 | 要动记忆桶的上限、淘汰顺序或两处用户写入面,或要给记忆条目加第三类优先级(如知识库种子)时 |
| [w103-memremember-memwrite.md](./w103-memremember-memwrite.md) | 用户手打那半个记忆写入面(`js/agent.js` `memRemember` / `cli.js` `memory add`)从裸 `slice(-50)` 接进 `WfCore.memWrite`:为什么 W101 落地之后残留的形态更别扭(**自动条写入时绕开用户条,用户条写入时反而只砍用户条**——`memWrite` 留下来的头部正是那些受保护的用户条)、为什么是"接进去"而不是在两个写入面各复制一份优先级(`memWrite` 对无 `fb` 的条目本来就走追加,接入不加参数不加分支;复制就是把同一淘汰口径写成三份且其中两份没断言)、"用户条挤用户条"为什么只缩范围不彻底解决(桶里一条自动条都没有那一路是容量不够而非顺序选错,要答"用户偏好按什么排序丢"得引新判据)、**两条认字面的源级断言怎么翻**(整文件搜 `slice(-50)` 在 `js/agent.js` 改后仍有 8 处 `agentChat` 命中会恒真失效,故改按函数段取并加"段内不得再自己截尾"的反向条件,判据收窄不放宽)、CLI 那一层为什么落在 `cli.smoke` 而不是 `integration`(淘汰发生在那次 `PUT` 之前,纯 HTTP 复现不出来),附两层变异实测(各自点名"少了哪一条用户条") | 要动记忆桶的写入面、上限口径或淘汰顺序,或要给记忆条目加第三类优先级(知识库种子)时 |
| [w95-integration-log.md](./w95-integration-log.md) | 四条内联人设收编槽(W88 专家工坊两步 / W89 制作计划生成 / W91 意图路由 / W92 项目实验台两步)并进同一条线的记录:注册表四处插入点里两处干净合成并集、两处两侧同抢表尾(34 → 40 条),**共用块尾那行 `},` 只能给一侧补**(按"每块各补一个"机械补就多一个括号,`node --check` 当场断在 `Unexpected token`)、**同一条「仍欠」段在一条线里翻了四次面**(基线点名 `js/experts.js`+`js/plans.js`,而这条线上 W88 收掉 `experts`、W89 收掉 `plans`、W91 收掉接替上去的 `agent-global`、W92 收掉最后接替的 `proj-planner`),**最后一次没有"这条线上不会再被收掉"的窄口径锚点可挑了**——W78 那张判据下的余量四个文件全在本槽收编面里、合完归零,仍欠段因此改指判据更宽那张名单还计着的那一处(单镜视频审片 `js/wf-core.js` 把人设句写在 user 半开头),而**一张名单归零时归零本身要配一条"例外仍在"的正向断言**(把 `return` 形态这个已知例外写成机器可查的,否则会出现"名单空着而余量还在"却没人报)、翻面断言用哪条正则只能逐处看(局部常量形态与 `content:` 值位都钉不住 `system:` 那条)、"已收编"那句写在「仍欠」之后当场红(W86 那个洞踩了第二次)、四个槽给的三张全仓名单期望串**没有一个在合并后成立**故一律 live 现取重写,而按 `Prompts.list()` 现推那两条**四次合并加了 6 个键一个字没改**(现推形态第二次拿到实测对照)、接进来的散文里余量数字一律删掉且**其中一句在本槽内就过期**(W88 拿意图路由举例说明常量形态漏计,而 W91 正好收掉那处),摘要长行的段界为什么要回退到句号再切,附"每个 tip 相对自己 `merge-base` 的用例增量相加恰等于合并后 live(453+2+2+2+2=461)"与三套件名集十五次 `comm` 全空的零吃测取证,以及 G-13 三张名单各自的余量盘点(A 归零、C 仍剩一处、标记一个不摘) | 想知道主干现在是什么状态;要合多条收编面在同一条「仍欠」段上首尾相接的槽、遇到某张全仓名单即将归零,或要接单镜视频审片那处 user 半人设(G-13 最后一处)时 |

## 一分钟摘要(周期 2 收敛后)

- 资产不缺,缺索引:知识在 `js/knowledge.js`(18 条目)、提示词在 `js/prompts.js`(40 条)、人设在 `js/experts-data.js`(16 专家)、编排在 `js/cmd-registry.js`,层与层之间的按主线步骤索引由 `js/skills.js`(30 条内部能力)承担。**KB 条目的登记面已收成契约**:`KB.SECTIONS` 与索引宿主 SK-01 的 `kb` 双向逐条对齐、宿主内不重复登记、不得出现第二个全库宿主,新增条目漏登即红并点名缺哪个键(见 [w30-kb-skill-cover.md](./w30-kb-skill-cover.md))。
- 主线七步在代码里齐了:`Domain.workflow` 已含"审片"步(G-03),`js/skills.js` 的 `STAGES` 七步全部 `wfStep: true`。
- 贯通缺口已收口的部分:专家人设(G-01)与协作记忆(G-02 由 agent-flow 覆盖)进 `/api/wf/*`,CLI/MCP 同链路吃到;剧本拆集(G-04)补上机读入口,headless 可从"一份整部剧本"起跑,其 LLM 步也已接入同一注入链(见 [w8-split-episodes-inject.md](./w8-split-episodes-inject.md))。
- 空挂已清:`settings.tplVideo`(G-05)与 `KB.SECTIONS`(G-15/G-08 的 KB 侧)都有了消费方,并有断言防回退。生成步的注入面亦已出块——`Skills.block('gen')` 逐字节等于提示词改写人设 `WfCore.genPromptSystem` 的方法论段(抽卡公式+抽卡军规),SK-21 的 `pending` 随之清空(见 [w15-gen-block.md](./w15-gen-block.md))。G-06 的**注入半到此闭合**:残留两条也已进主线提示词构造点——「多镜头写法」进拆镜人设 `WfCore.sbSystem`、「主体参考」进主体提取人设 `WfCore.extractSystem`,两处缺省提示词随之变长,拼块逐字节对账与兼容影响见 [w19-g06-inject.md](./w19-g06-inject.md)。**G-06 的校验半随后也闭上**:主体参考纪律(SK-11 `subjects.genRefDiscipline`:参考人物超上限 / 有图被参考图组上限挤出 / 三视图当视频参考)与多镜头写法(SK-13 `subjects.multiShotPrompt`:图生视频缺一致性声明 / 首尾帧写大幅动作 / 一镜切太碎)以生成前 warn 落地,判定输入是 `Domain.buildVideoRequest` 那份真实请求,经就绪检查双端消费、只报不拦,`Skills.gaps()['G-06']` 随之归空(见 [w22-g06-check.md](./w22-g06-check.md))。该人设键的取值口已收编到底:审片一键优化与 CLI 修订重抽改经 `WfCore.optimizeSystem` 只取人设句(缺省逐字节不变),覆盖两端一并跟随(见 [w18-gen-prompt-unify.md](./w18-gen-prompt-unify.md))。
- 校验宿主七面齐了:剧本面(S-01,SK-07/08/09;文案 AI 味 SK-10 的校验半随 W24 落地,见 [w24-sk10-ai-voice.md](./w24-sk10-ai-voice.md),注入半随 W27 闭合 S-02——自撰条目进 `KB.SECTIONS` 后校验层的三张词表改现取条目正文,注入与校验共用一份判据,见 [w27-sk10-kb-inject.md](./w27-sk10-kb-inject.md))、主体面(S-03,SK-11/12/13 共四条)、分集面(SK-14/15)、分镜面(SK-18 景别衔接,级差取 `WfCore.sizeGap` 词表单源;SK-19 抽卡稳定词,判定输入是 `Domain.buildVideoRequest` 装出的真实提示词,见 [w26-sk19-stable-lex.md](./w26-sk19-stable-lex.md);SK-20 镜头动态感准入,动作幅度判动作描述那段、运镜条数判装好的真实提示词、镜长分布取该请求的 `duration`,见 [w33-next-pending-check.md](./w33-next-pending-check.md))、生成面(S-05,SK-22 生成凭据与确认失效,判旧与就绪取 `Domain.shotVideoStale/shotVideoReady` 单源,见 [w28-sk22-gen-check.md](./w28-sk22-gen-check.md))、审片面(G-10 的第二半起步,SK-24 方法论维度进审片报告:判定输入是已成型的报告,四维维度名取 `WfCore.normalizeCut`、整份判旧取 `Domain.reviewStaleByScript` 单源,见 [w35-sk24-check.md](./w35-sk24-check.md))、成片面(S-06,SK-28 字幕质检;SK-29 上游定稿契约,板块链序取 `Skills.STAGES` 主线七步、产物实况取 `Domain.workflow` 同键步骤,见 [w37-sk29-check.md](./w37-sk29-check.md))共十七条 `Skills.CHECKS` 校验项,剧本面与分集面成对闭合 S-01、主体面另接住 G-06 校验半;两端就绪检查按主线步序同挂 `result.checks`,问题中心同挂低危提醒——纯本地零 LLM 零计费,只报不拦。**问题中心那侧的七条低危提醒也收成了一张单源表** `Issues.reminders()`(面 → 校验项 id → `kind`/`sev`/挂载级别 + 早退分支前后与明细写法),`collect` 只按表跑、模块里只剩一处校验项取值点;同轮 `js/issues.js` 收成与 `js/domain.js` 同形的双端 UMD(项目树与 `online` 经参数注入、不碰 `window`),浏览器 UI 段移到薄封装 `js/issues-ui.js` 而全局名与成员不变,headless 侧新出 CLI `issues` 与 MCP `hujing_issues`(只列不处置、零计费),见 [w59-issues-umd.md](./w59-issues-umd.md)。**面清单现为双端单源表 `Skills.preflightStages()`**,由注册表现推(校验面已落地 + 登记 `episode.preflight` 消费点),两端只读该表 `concat`,**新增一面只改一处**(在条目上登记 `checks` 实现与 `cmds`),见 [w17-preflight-stages.md](./w17-preflight-stages.md);W22 的两条、W24 与 W26、W33 的各一条都落在已在表内的面上,故两端消费实现一行未改;W28 落地的是表里原本没有的 `gen` 面、W35 落地的是 `review` 面,两端消费实现同样一行未改——面表由登记推导,自动从五面变六面再变七面(主线七步至此全部在表内);W37 的 SK-29 落在已在表内的 `film` 面上,两端消费实现同样一行未改。七面并集与步序由行为断言(浏览器端真跑命令看回执)+ 面表源级断言(表与登记侧双向对齐 + 两端只读该表且写法逐字节相同 + 段内不得写死面名)锁死,断言分工的由来见 [w11-preflight-film-assert.md](./w11-preflight-film-assert.md);新增一面漏接或单端退回写死清单时这些断言先红,W13 收敛 SK-18 与 W17 收表时均实测接住过。
- 校验结论已进审片路径(G-10 的第一半):审片报告按镜只读消费剧本面与主体面,命中挂报告独立字段 `checks` 并在弹窗/导出各列一区——不并入 `issues`、不参与评分与达标线、不改发布门计数与计费动作,见 [w14-review-skills-check.md](./w14-review-skills-check.md)。**反向那一半也起步了**:审片报告本身成了判定输入——SK-24 的校验面(`review.methodDim`)逐集看报告里那几个方法论维度到位没有,经就绪检查双端消费、只报不拦,见 [w35-sk24-check.md](./w35-sk24-check.md);报告好坏优劣的语义判断仍归 LLM 审片。**发布门那一半仍未清**:SK-29 的**校验半随 W37 落地**(上游定稿契约的两向判定,只报不拦、不动 `js/release.js` 的 G1–G10 与 overall 计数,见 [w37-sk29-check.md](./w37-sk29-check.md)),方法论门挂进发布门的产品口径未定,G-10 因此仍挂账。
- 记账诚实位:`SK-03`/`SK-04`/`SK-23` 的 `infra` 面 `pending` 已按实况清空(三处出口 G-01/G-02/G-03 都已落地),三条 `note` 改为逐处点名各自仍欠的**覆盖余量**(四处装配口的 ops 协议半仍不开放覆盖、生成与合成两步没有可判定的结构化结论可回流记忆(主线其余六步的结论已回流)、审片报告好坏优劣的语义面仍待 G-10),并由断言钉在三处出口的实况上;缺口标记按**关联索引**口径保留——`gaps()` 只投影 `gaps` 字段、不看 `pending`,故「改 `pending` 会动 `Skills.gaps()` 投影」这条早先的顾虑**实测不成立**(有用例把三条退回后逐字节比对投影钉住),清账的行为面为零,见 [w36-infra-pending-align.md](./w36-infra-pending-align.md)。**W36 点名的第一处余量随 W39 补掉**:审片侧三步(分镜评审 / 整集共性汇总 / 四维成片评审)已接上人设与记忆通道,三步的 user 模板同经唯一注入段装配口 `WfCore.reviewCtxNote`,分镜评审随分镜板块 ctx、集级两步随成片板块 ctx 并按集标题召回,未雇佣专家且无沉淀记忆时输出逐字节不变,服务端不新增 `wfPersonaNote` 调用点,见 [w39-review-persona-mem.md](./w39-review-persona-mem.md)。**W39 点名的那处余量随 W40 收掉**:共性汇总步的人设句进注册表新键 `review.sumSystem`(`def` 与原内联字面逐字节相同),浏览器与服务端同键取值、服务端显式传 `settings.promptOverrides`,整集审片三个 LLM 步的 system 半至此全部可被用户覆盖;SK-03 的仍欠段随之换成剧本拆集步的内联人设,见 [w40-sum-system-prompt.md](./w40-sum-system-prompt.md)。**W40 点名的那处余量随 W42 收掉**:剧本拆集步的人设句进注册表新键 `split.system`(`def` 与原内联字面逐字节相同),浏览器 `js/episode-util.js` 与服务端 `/api/wf/split-episodes` 同键取值、服务端显式传 `settings.promptOverrides`;SK-03 的仍欠段换成主体提取步的人设句(`wf-core` 常量 `EXTRACT_SYSTEM`,双端同源但覆盖不到,故 `WfCore.extractSystem` 不收覆盖表参数),点名断言随之改为查注册表与装配口签名——那句字面在 `aiScriptDigest` 人物小传步还有一处同字面内联,查字面点不住,见 [w42-split-system-prompt.md](./w42-split-system-prompt.md)。**W42 点名的那处余量随 W45 收掉**:主体提取的人设句进注册表新键 `extract.system`(`def` 与原 `wf-core` 常量字面逐字节相同,常量随之撤掉),装配口 `WfCore.extractSystem(ov)` 开始收覆盖表参数、方法论段仍是 `KB.pick('主体参考')` 整条注入且不随人设覆盖变动,服务端显式传 `settings.promptOverrides`——`/api/wf/*` 五条工作流的 system 半至此**全部可被用户覆盖**;SK-03 的仍欠段换成 Agent 单轮对话步(人设句与 ops 协议同写在 `WfCore.buildAgentSystem` 一个模板串里,双端各一份措辞),见 [w45-extract-system-prompt.md](./w45-extract-system-prompt.md)。**W45 点名的那处余量随 W49 收掉**:Agent 单轮的人设句进注册表新键 `agent.system`(`def` 与原模板串开头字面逐字节相同),装配口 `WfCore.buildAgentSystem(ctx, ov)` 开始收覆盖表参数、服务端 `/api/wf/agent` 显式传 `settings.promptOverrides`——`/api/wf/*` 六个 LLM 端点的人设句至此**全部在注册表内**;**只收人设句是有意的**:其后的 ops 协议 / 命令白名单 / 返回 JSON 约定是 `agentNormalize` 的解析契约,做成可覆盖变量就能被用户一改改坏,故仍由 `wf-core` 拼,并由「覆盖只换人设句、协议半逐字节不变」一条断言钉住;SK-03 的仍欠段随之换成浏览器多轮那三份人设(分集面板 / 全局抽屉 / 预排模式,三种运行模式各一份措辞,不并进同一个键),见 [w49-agent-system-prompt.md](./w49-agent-system-prompt.md)。**W49 点名的那三处余量随 W51 收掉**:三份人设各成一条独立键(`agent.panelSystem` / `agent.drawerSystem` / `agent.previsSystem`,`def` 与三处模板串开头字面逐字节相同),装配口分别是 `AgentCore.panelSystem`(面板那处收编前没有装配口,system 模板夹在 `sendInner` 闭包里,本槽原样抽出)、`AgentG.buildGlobalPrompt` 与 `agent-ops.js` 的 `prearrPrompt`,浏览器隐式读 `settings.promptOverrides`——**单轮与多轮的人设句至此全部在注册表内**,且「覆盖只换对应那一份、另两份逐字节不动」有断言钉住;三条键**只有浏览器一个消费点**(headless 多轮不存在),故其「两端」如实记成落在取值口而非两个消费点;SK-03 的仍欠段随之换成四处协议半有意不开放覆盖 + 这条取值口口径,见 [w51-browser-agent-personas.md](./w51-browser-agent-personas.md)。**SK-23 点名的那处余量随 W54 收掉**:问题中心补上未审(`no-review`)与记录判旧(`review-stale`)两条中危投影,与 `Domain` 审片步的 `blockers` 同码同口径、判旧那条与发布门 G3「视为未审」对齐,三态互斥同集只报一条;判据一行不新写(现取 `Domain.episodeState`),挂载位置在未拆镜/分镜判旧的早退分支之后即"该集已有镜头"。同槽把 SK-19 稳定词面接进问题中心(`shot-stable-lexicon` 低危,warn 不抬成 G2 fail),W26 残留的第一条随之清账;SK-23 的仍欠段只剩审片报告好坏优劣的语义面(G-10),见 [w54-issues-unreviewed-sk19.md](./w54-issues-unreviewed-sk19.md)。**SK-04 点名的那处余量随 W53 收掉**:记忆补种与旧板块名迁移下沉 `WfCore.memSeed`/`memMigrateBoard` 双端单源,浏览器 `memAll` 与 headless 三个入口(`/api/wf/memory-seed` + CLI `memory seed|migrate` + MCP 同名工具)吃同一份种子表与迁移表,空板/未知板名如实报错不静默空成功,播种保持显式动作(读记忆时不自动跑,免得读一次写一次盘);SK-04 的仍欠段随之只剩自动沉淀那一半——理解/分镜/拆集/提取主体几步的结论仍不回流(回流面本身归 SK-26);**那四步随后随 W61 补齐**,SK-04 的仍欠段现只剩生成与合成两步(没有可判定的结构化结论可回流,素材产出的判定面归发布门 G3/G7)。见 [w53-memall-headless-seed.md](./w53-memall-headless-seed.md)——这份记账件一直在该槽的 tip 上,W57 当时只合到它的中间点,故连文件带索引行一起漏在集成线之外,随 W67 补合(见 [w67-integration-log.md](./w67-integration-log.md))。**注册表之外的同字面内联也清了一处**:剧本摘要全文通读(`EpisodeUtil.aiScriptDigest`)第五步「人物小传」原先写死了与 `extract.system` 逐字节相同的那句人设,现改经 `Prompts.get('extract.system')` 同键取值——字面同 / 角色同(都是"剧本分析助手"从剧本里认人)/ 产物落点同(都往 `p.subjects` 写 `character` 条目并过同一道可信性校验),故复用键而非新开,注册表条数不变;**只取人设句不接方法论段**(「主体参考」那 278 字讲的全是生图与参考图组纪律,该步不生图),缺省四步 `system` 逐字节不变、覆盖时提取步与小传步一并跟随;W45 为它留的「该字面在 `js/episode-util.js` 恰好 1 处」计数路障随之反转为零内联 + 注册表命中恰好一条,`aiScriptDigest` 由此有了第一条行为面断言(沙箱真跑、逐步截获 `system`/`user`),见 [w56-aiscript-digest-prompt.md](./w56-aiscript-digest-prompt.md)。W39 另修掉一个洞:记账件的点名断言原先**全文查 `note`**,余量补完后把锚点留在"已落地"那半即可蒙过去(实测把仍欠段换成「仍欠:无」仍全绿),现已收紧为只认「仍欠」之后那段。`check` 面随 W35/W37 全部落地,编排面剩下的两条随 W43(`SK-26` 回流面)与 W44(`SK-05` 主线全链投影)相继落地,**短名单至此已无 `pending`**;`G-11`(自进化)与 `G-12`(投影落点)按关联索引口径仍挂在各自条目上——`G-12` 的三个落点已全部接上——计划步骤随 W46、MCP 中段流程模板随 W52、发布留痕的命令化出口随 W58(判定与写回收进 `js/release-core.js` 双端单源,出口是领域命令 `project.release`);标记按关联索引口径仍挂在 SK-05/SK-25 上,SK-25 自己仍欠 `G-03`(修订循环的 `shotIds` 子集不由编排层推导)。**W56/W60 登记的那处基线记账不实随 W66 清掉**:SK-10/SK-11 的 `note` 原先都写着「人设句入注册表待 G-13」,而那句人设(`extract.system`)自 W45 就在注册表里、装配口也收覆盖表参数——两条改为写明人设句已在表,「仍欠」段各自改指真正还在的那一处(SK-11 是 `tplImage` 取用点即 `js/persona.js` 八维度重写步那份内联人设,SK-10 则先写明本条没有专属人设句、仍欠的是剧本模块解说体改写/导演阐述/光影总控/剧本围读四处内联),两处锚点各配一条"收编即红"的源级断言;SK-11 另按 SK-17/SK-21 同口径补登记注入落点键 `prompts: ['extract.system']`。`gaps` 一字未动(G-13 治的是全仓 21 处内联人设,未闭合),故 `Skills.gaps()` 的键数与每个键的值逐字节不变——W56/W60 顾虑的"改 note 会动投影"实测不成立,成因是被误当成期望值的其实是一句断言语,见 [w66-sk11-extract-note.md](./w66-sk11-extract-note.md)。**G-13 欠段又收一处**:SK-11 另一登记键 `settings.tplImage` 的取用点——`js/persona.js` 八维度重写文生图提示词那步——人设句进注册表新键 `persona.promptSystem`(`def` 与原内联字面逐字节相同),与只差一个字的 `gen.promptSystem` 有意不合并(一个出主体立绘、一个出镜头视频,参考图口径与下游消费点都不同);只收人设句(参考模板仍取 `settings.tplImage`、返回 JSON 约定仍由该步装配口拼),该步是纯浏览器链路故收编解决的是"可覆盖"而不是"可 headless";`settings.tplImage` 本身不记成缺口(用户一直改得到),SK-11 的 `note` 随之改写、仍欠段当时改指剧本板块那四步并配反向断言,`gaps` 与 `Skills.gaps()` 逐字节不变——**G-13 缺口仍开着**,见 [w69-persona-tplimage-prompt.md](./w69-persona-tplimage-prompt.md)。**那四步随后随 W71 收编**:旁白解说体改写 `narration.system`/剧本围读 `reading.system`/构思导演阐述 `concept.system`/全剧光影总控 `light.system` 各成一条独立键(四步角色分别是解说编剧、导演组围读会、开拍前定调导演、摄影指导,措辞与产物落点都不同,不合成带变量的一个键,也不与既有导演类人设复用),取值口都在 `js/episodes.js` 经 `Prompts.get` 隐式读覆盖表、`def` 与收编前的内联字面逐字节相同,同样是纯浏览器链路故收编解决的仍是可覆盖而非可 headless,返回 JSON 字段契约留在各自 user 半不开放覆盖;四键登记在 SK-03 名下,SK-10 本条没有专属人设句故只改记账,SK-10 与 SK-11 的仍欠段一并改指真正还在的那几处——`js/episodes.js` 事件图谱拆解步与 `js/episode-util.js` 剧本摘要的通读/汇总/集纲三步;`gaps` 仍一个不摘(**G-13 缺口仍开着,全仓内联人设现存 15 处**),见 [w71-script-four-prompts.md](./w71-script-four-prompts.md)。**W56 交接第 1 条点名的那三处随 W74 收掉**:剧本摘要链路(`EpisodeUtil.aiScriptDigest`)的分块通读/汇总/集纲三步写着同一句策划人设,先按 `cat -A` 逐字节核对三处 `system` 确认是同一句话的三份拷贝(不是三份措辞),再按"字面同/角色同/产物落点同"三条判据定为**一键三口**——注册表新键 `digest.planSystem`(`def` 与三处内联字面逐字节相同),三步各经 `Prompts.get` 取值,缺省四步输出逐字节不变;同函数第五步人物小传是另一个角色(剧本分析助手把人物落进主体库),仍听 `extract.system` 不合并,界线由"两键不同值"与"本文件里 `extract.system` 仍恰好 1 个取用口"两条正反断言钉住。SK-03 只补一个 `prompts` 键与一句 `note`,`gaps`/`pending` 一字未动(按同一口径实测,全仓内联人设自 W66 记的 21 处降到 17 处,G-13 仍开着),见 [w74-digest-three-prompts.md](./w74-digest-three-prompts.md)。**同口径又收两处半随 W76/W77 落地**:分镜脚本创作层两步(场次节拍拆解 `sb.boardSceneSystem` 是编剧、文字分镜拆解 `sb.boardDraftSystem` 是分镜师,措辞与产物落点都不同故两键不合并)在 `js/sb-board.js` 同经 `Prompts.get` 取值,注册表位置按 tab 次序排在智能分镜之前,见 [w76-sb-board-prompts.md](./w76-sb-board-prompts.md);分集页事件图谱拆解步的人设收进独立键 `graph.system`(角色是"剧本结构分析师",与剧本板块其余几步不同,不合成一个键),取值口在 `js/episodes.js` 逐集拆解那步就地经 `Prompts.get`,返回 JSON 的 `events` 字段契约仍留在该步 user 半,见 [w77-event-graph-prompt.md](./w77-event-graph-prompt.md)。**剧本模块两个文件(`js/episodes.js` / `js/episode-util.js`)至此内联人设归零**,SK-10 与 SK-11 的仍欠段随之改指真正还在的那几处(`js/agent-ops.js` 的执行核验器与会话纪要整理器、`js/sb-views.js` 的分镜改图专家),两处各配一条"收编即红"的源级反向断言;`gaps` 仍一个不摘(**G-13 缺口仍开着**),收敛过程见 [w80-integration-log.md](./w80-integration-log.md)。**W66 点名的"余量只在散文里有数字"随 W78 补上判据**:节拍板 AI 拆解步的人设句收进注册表独立键 `beat.system`(`def` 与原内联字面逐字节相同,其后 KB「六阶段结构/打脸四步」方法论段按键原样接,user 半的 5 段式返回 JSON 约定有意不开放——那半是 `applyAIFill` 的解析契约),新键登记在 SK-14(节拍板本就是它的注入落点,而该步不注生效人设、没有服务端对端,故 SK-03 一行未动);同槽**新立全仓内联人设持有者名单断言**(文件:处数,收编一处即少一处、任何文件新长出一处即多一处),G-13 的余量从此机器可查而不是每槽各写一个数字,`gaps` 按关联索引口径照旧一个不摘,见 [w78-beatboard-prompt.md](./w78-beatboard-prompt.md)。**注册表之外的内联人设又收掉一处随 W79**:漫剧编辑器「AI 生成对白」那句人设进注册表新键 `comic.bubbleSystem`(`def` 与原字面开头逐字节相同),取值改经 `Prompts.get`;**只收人设句同样是有意的**——同一串里的气泡条数 / 返回 JSON 形状 / `type` 词表是那一步的解析判据(`TYPES.includes` 归类、两种解析分支、`slice(0,4)`),故提成装配口常量 `BUBBLE_CONTRACT` 不开放覆盖,并由「覆盖只换人设句」与「契约半的 type 词表与解析侧 `TYPES` 同集」两条断言钉住;新键挂在 SK-03 名下,但「仍欠」段写清它**不过那条 ctx 通道**(编辑器工具步不注入生效人设与协作记忆,只是人设句进了表)。同槽另给 G-13 立了第二张口径的**全仓人设字面持有者名单**——按「以 `你是` 开头的字面」逐文件普查,注册表 `def`、专家人设库与其余模块内联混在一张表里如实计数(`js/api.js` 两处兜底缺省与 `js/gsettings.js` 一处占位文案也计进去,例外条款越多名单越容易被绕过),与 W78 那张只数系统人设位的名单口径不同、两张各自为政不互相折算,见 [w79-editors-prompt.md](./w79-editors-prompt.md)。**W76 点名 `js/gsettings.js` 的那一处随 W81 收掉**:导演设定五维(光影/色调/情感氛围/服化道审美/表演气质)的 AI 生成步人设进注册表新键 `dirset.system`(`def` 与原内联字面逐字节相同),取值口在 `js/gsettings.js` 的 `genDirectorSetting` 经 `Prompts.get` 隐式读覆盖表;**不与只差「影视/短剧」两字的 `und.system` 复用**——一个给全剧定一次风格五维、一个逐集读本,产物落点(`settings.directorSetting` / `ep.understanding`)与消费面都不同,合成一个键就是让用户改一次改掉两条链路。同为纯浏览器链路故收编解决的仍是可覆盖而非可 headless,五维返回 JSON 的字段契约与剧本前段摘取留在 user 半不开放覆盖;这一步比前几槽多一条边界——LLM 失败走 `dirFallback` 按风格回退,**覆盖只作用于 LLM 那一路、回退文案逐字节不跟随**,并专配一条断言钉住。同槽另立第三种口径的全仓内联人设持有者名单(判据收成机械扫描的三种形态:`system:` 值位 / 具名人设常量 / 装配函数直接 `return`,`js/experts-data.js` 的预置专家 `persona` 与输入框 `placeholder` 有意不在判据内),本槽落点钉成"`js/gsettings.js` 整条从名单消失",`gaps` 仍一字未动(**G-13 缺口仍开着**),见 [w81-gsettings-prompt.md](./w81-gsettings-prompt.md)。**同口径再收一处随 W82**:剧壳页发行文案包那步的人设句进注册表新键 `dist.copySystem`(`def` 与原内联字面逐字节相同),其后按键现拼的 KB「钩子六型+付费卡点」仍归 `KB.SECTIONS` 单源、六字段返回契约留在 user 半不开放覆盖;同槽把全仓持有者名单再往前收了一步——由「每槽各写一次局部扫描」改成按 `Prompts.list()` 现推的一条用例(新增键自动进名单),`gaps` 照旧一个不摘,见 [w82-proj-shell-prompt.md](./w82-proj-shell-prompt.md)。**W83 再收一处**:拉片建集(参考视频 → 场景切段 → 逐段画面理解)那步的人设进注册表新键 `rip.system`(`def` 与 `js/proj-upload.js` 原内联字面逐字节相同),登记位置紧跟 `split.system`——两者是建分集的两条入口,但角色与判定输入都不同故不并键;同样只收人设句(段号时段的现拼提示与返回 `{"shot_desc":…}` 的字段契约仍留在该步 user 半),纯浏览器链路故收编解决的仍是可覆盖而非可 headless,键登记在 SK-03 名下并新立一条全仓持有者名单;`gaps` 与 `Skills.gaps()` 逐字节不变(**G-13 缺口仍开着**),见 [w83-proj-upload-prompt.md](./w83-proj-upload-prompt.md)。**G-13 欠段再收一处**:镜头「按指令改」那步(`js/sb-views.js` 的 `openCommentGen`)的人设进注册表新键 `gen.editSystem`(`def` 与收编前的内联字面逐字节相同),与同层四策略优化的 `gen.promptSystem` 有意不合并——那条经装配口 `WfCore.genPromptSystem` 还要接 KB 抽卡公式+抽卡军规,复用即把两步的缺省一起改掉;收的是人设句连同改写纪律(写坏只是效果差),镜头上下文摘取与返回 JSON 契约仍留在该步 user 半不开放覆盖;该步同为纯浏览器链路,收编解决的仍是可覆盖而非可 headless;键登记在 SK-03 名下,同形的主体「按指令改」(`js/role-editor.js`,角色与产物落点都不同)有意不在本槽收编并配了反向断言;同轮把逐槽各写一份的"这一句只剩注册表一份"改成**按注册表现取的全仓持有者名单**(每条 `def` 全仓扫一遍,持有者必须恰好是 `js/prompts.js`,下一槽新收的键自动进名单),`gaps` 与 `Skills.gaps()` 逐字节不变(**G-13 缺口仍开着**),见 [w84-sb-views-prompt.md](./w84-sb-views-prompt.md)。**`js/agent-ops.js` 那两处随 W86 收掉**:Agent 对话闭环的两个辅助步——执行回执核验修复(`AgentOps.selfFixRound`,`step:'fix'`)与会话纪要蒸馏(`AgentOps.compactChat`,`step:'cmp'`)——各成一条独立键 `agent.selfFixSystem`/`agent.compactSystem`,两句 `def` 逐字节不同故判据在比对那一步就结束(不可能共用一键),取值口都在 `js/agent-ops.js` 就地经 `Prompts.get` 隐式读覆盖表、`def` 与收编前的内联字面逐字节相同;同样只收人设句——回执的 `✕`/`⊘` 记号、修复 `ops` 白名单、蒸馏字数与保留项、返回 JSON 约定都对着下游解析分支,仍写在各自调用点不开放覆盖;两步只有浏览器一个消费点(headless 没有自修复轮与对话纪要这两个动作),收编解决的是可覆盖不是可 headless,计费仍走所属那条消息的辅助步槽位不另扣费。SK-10 与 SK-11 的仍欠段随之只剩 `js/role-editor.js` 的角色设定师(仍配"收编即红"的源级反向断言),`gaps` 仍一个不摘(**G-13 缺口仍开着**),见 [w86-agent-ops-prompt.md](./w86-agent-ops-prompt.md)。**主体域随 W87 内联归零**:主体编辑页「按指令改」那步的人设收进独立键 `persona.editSystem`——类别是插值进去的,故取"一条键 + `{kind}` 变量"(角色同、产物落点同,分三键等于同一句话改三遍;写死类别则缺省当场变),是人设类键里第一条带变量的;与分镜那侧同形入口有意不合成(设定师出主体参考图、改图师出镜头素材,措辞也不同字面),契约半仍留在该步 user 半不开放覆盖。同槽把逐槽各写一份的全仓持有者名单更新成**按注册表现取的一条**(逐条扫每条 `def`,下一槽新收的键自动进名单;同 `def` 反查钉在音色推荐那一对上),既有那几份逐槽写的一条不删;`gaps` 仍一个不摘(**G-13 缺口仍开着**),见 [w87-role-editor-prompt.md](./w87-role-editor-prompt.md)。**专家工坊那两处随 W88 收掉**:锻造器 `forge.system`(从一句描述铸出一位新专家)与进化器 `forge.evolveSystem`(把已有专家的使用记录蒸馏成进化条款)各成一条独立键——同在「🧪 专家工坊」页上仍不合并,因为输入/动作/产物落点/计费标签/多轮全不同(无中生有 vs 就地改写);取值口都留在 `js/experts.js`,锻造器那处**导出成 getter**(照原样写成模块级 `const` 会把覆盖表冻结在页面加载那一刻,用户改完提示词切回工坊 tab 吃到的还是旧值),`js/gsettings.js` 工坊页因此一行未改、仍只引用 `Experts.FORGE_SYS` 这一个常量;`js/experts-data.js` 的 16 位预置专家 `persona` **不在收编面内**(那是专家数据不是提示词);两条键分别登记在 SK-02(专家条目的铸造口,并写明工坊那份字段面同样没有 `skills[]` 即 `G-09` 的另一头)与 SK-26(`evolveExpert` 的记账宿主)名下,**`G-11` 如实保留**并补一句划界——改得到提炼口径,改不出自动触发;`gaps` 仍一个不摘(**G-13 缺口仍开着**;另**补记 `system: '你是` 那个计数口径漏计的一类**:局部常量形态(`const X = \`你是…\``)不匹配该口径,`FORGE_SYS` 与意图路由那处都属这一类),见 [w88-experts-forge-prompt.md](./w88-experts-forge-prompt.md)。**W89 点名 `js/proj-planner.js` 的那两处随 W92 收掉**:项目实验台的 AI 策划对话与剧本译制各成一键(`planner.chatSystem` / `trans.localizeSystem`,两处 `def` 字面不同故不共用),策划那步只收人设句、其后的「当前项目信息:」项目实况(项目名称/风格影调/目标市场/剧本前段/分集列表)仍由取值口现拼;译制那步是**第一处带运行期变量的收编**——`${tgt.name}`/`${tgt.lang}` 登记成 `vars: ['{market}', '{lang}']` 走 `Prompts.fill`,并正查 `def` 里真有这两个占位符(登记了填不到是静默失效);它的**契约半写在 system 半而不在 user 半**,「保留『第X集』分集标记」那一条留成取值口常量 `TRANS_CONTRACT` 不开放覆盖(它就是「应用译制结果」按标记拆分回写分集的判据),并新加一层"契约与解析侧同集"——把源码那条 split 正则现取出来真切两集,拦住"契约文字还在、解析侧那一支被删了"的单侧漂移。另钉住**两处取值口都不许提到模块顶层**(IIFE 顶层预取会把覆盖表冻在页面加载那一刻,缺省与覆盖两条路径看起来都对,只有源级看得见);三张判据不同的全仓名单本槽逐张按 live 重算(按注册表 `def` 现推那张期望值不变,逐文件手写处数那两张各自减一批加一批),`gaps` 照旧一个不摘,见 [w92-proj-planner-prompt.md](./w92-proj-planner-prompt.md)。
- 人设/记忆注入面覆盖五条工作流:理解、分镜、审片、提取主体、剧本拆集(`WfCore.WF_BOARD` 五键单源,服务端唯一装配口 `wfPersonaNote` 由契约断言锁死调用点数);工作流内部的审片侧三步随 W39 一并接上,ctx 各随所属工作流的板块。
- 词表分叉已收口:景别/运镜/视角/角度四张词表的单一来源在 `js/wf-core.js`,`camera.js`/`review.js`/`sb-io.js`/`agent.js` 全派生(G-07,见 `w4-shot-size-glossary.md`)。
- 前段命令全过 wf 通道:剧本拆集(G-04)与提取主体都有服务端端点,人设与协作记忆按板块注入,headless 与浏览器同口径。**编排层也已跟上**:`Skills.playbook('eps.frontPipeline')`(SK-16)从集内两步补成前段四步(提取主体 → 剧本拆集 → 本集理解 → 智能分镜,步序与 `Domain.workflow` 同源),条目 `cmds` 改由 `steps` 推出、四步 `args` 一律留空不预授权,见 [w31-sk16-playbook.md](./w31-sk16-playbook.md)。**编排层随后补上了全链那一条**:SK-05 的 `steps` 从空变成主线端到端九步(提取主体 → 主体生图 → 剧本拆集 → 本集理解 → 智能分镜 → 就绪检查 → 批量生成 → 智能审片 → 合成成片,步序同取 `Domain.workflow` 主线步序),`Skills.playbook('core.playbookProjection')` 从 `null` 变成九步,SK-16 前段四步是它的有序子序列(分叉即红);`shot.generateVideo`(断点补拍)与 `episode.produce`(三步聚合)有意不进链、各由别的条目登记;`args` 同样一律留空,`gaps()`/面表/拼块逐字节不变,见 [w44-sk05-playbook.md](./w44-sk05-playbook.md)。**那张步骤表随 W46 有了第一个消费方**:`Plans.fromWorkflow` 的命令名与步序改由该投影现取(计划层只留一张按命令名索引的状态取材器,回答"这一步当下待不待办",命令名由投影盖写故取材器给不出第二条链),项目级步按投影步序排在前、集级步逐集取首个待办步,需授权或需人工挑选的状态(重拆覆盖 / 过期镜子集 / 确认闸 / 补剧本)一律出导航步且不预设任何 `args`,本集理解与就绪检查按理由登记为不占计划步(自省出口 `Plans.projection()` 把"选择性不占"与"漏接"分开);`mcp.js` 那个零使用的 `require('./js/skills.js')` 同轮接上第一个只读出口 `hujing_playbook`(playbook 步骤表 + 就绪检查各面校验项,不经 CLI、不打服务端、零计费、未登录也答得出)。见 [w46-g12-playbook-proj.md](./w46-g12-playbook-proj.md)。**那张步骤表的第二个消费方随 W52 落地**:同一份投影切出中段(主体/分集/分镜/生成)做成可调用的流程模板 `js/flow-tpl.js`——步序与命令名取投影、参数面与用法串取 `cmd-registry`、待办与缺前置取 `Domain.workflow`,本层只登记注册表答不出的三件事(这一步落在中段哪一主线步、待办由哪几个阻塞码判、跑砸了断在哪一码),四段分开取拼接逐字节等于整段、缺前置一律进 `gaps` 并 `ready=false` 而不吞掉步骤序列、授权位与子集位一律写明要用户明示;出口是 CLI `flow-template` 与 MCP 工具 `hujing_flow_template`(CLI 薄包装)加提示模板 `hujing_mid_pipeline`(正文由同一份模板渲染,工具名经 `toolOf` 从工具表 `cmd` 字段现取,模板层零工具名字面)。见 [w52-g12-mcp-mid-templates.md](./w52-g12-mcp-mid-templates.md)。**G-12 的第三个落点随 W58 补上**:发布留痕做成领域命令 `project.release`——准入判定(空项目 / 缺门禁结论 / 未过门各给明确错误码)与写回(打版本号 + 追加一条 `p.releases` + 内容摘要)收进 `js/release-core.js` 双端 UMD 单源,浏览器 `Release.stampRelease`、服务端 `POST /api/wf/release`(CLI `exec project.release` / `release` 与 MCP `hujing_release` 同链路)调同一个 `stamp()`,环境差异(时钟/用户名/版本号推进器/落库)经参数注入;浏览器交付检查的「打版本」按钮改走命令表(绕过即红),`force` 是"未过门强打"的授权位而不是万能位(空项目带 `force` 仍拒),门禁判据、fail/warn 计数与 overall 四级口径一字未动(G10 仍是 warn,不抬门);这个动作本来就零 LLM 零上游零计费,两端 `meter:false` 且都不进 `Tasks.run`。见 [w58-sk25-publish-cmd.md](./w58-sk25-publish-cmd.md)。
- `WfCore.memRecall/memBlock` 有了直接单测(memory 套件现 28 条,末 6 条覆盖 W53 的播种/迁移面,另 7 条覆盖 W61 的前段四步回流面、2 条覆盖 W65 的蒸馏板块过滤面),不再是"实现存在但零断言"的模块。
- 记忆的**写入面不再全靠人打字**(SK-26 回流面):整集审片与发布留痕两个闭环收尾,把该轮的**可判定**结论(待返工镜数、共性问题类型、四维最弱维、发布门状态与未过门项)按板块写回既有 `state.agentMemory`,派生只此一份 `WfCore.memFeedback/memWrite`(记忆数组经参数注入),四处写入点各走自己那端既有的落盘通道、不多一次 IO;按回流键原地更新故反复闭环只留最新一条;整集均分**有意不回流**——成片板块的记忆会被下一轮逐镜审片召回,回灌分数等于给打分设锚点。"回流专家"的自动那一半由此闭合(条目带板块 `scope`,下一轮同板块提示词按 `memBlock` 召回即吃到),蒸馏进 `persona` 仍要人点自进化(`G-11`),见 [w43-sk26-memory-writeback.md](./w43-sk26-memory-writeback.md);发布留痕的命令化出口随 W58 接上后,那一处写入点从「CLI 自己拼 meta 桶随 `PUT /api/state` 写回」改成服务端 `/api/wf/release` 随同一次落盘写回,回流内容与原地更新口径一字未改。**W61 把回流面从这两处补到主线六处**:前段四步(本集理解 → 六维产出数与缺的维名归导演板块、智能分镜 → 镜数/预估总时长与缺提示词·未挂主体两处缺口归分镜板块、剧本拆集 → 集数/切分模式/最长集与超长集数归剧本板块、提取主体 → 本轮新增·已有位数与库存·缺参考图归主体板块)各在成功且**可判定**时回流一条,浏览器与 headless 各一套写入点、都走同一份 UMD 派生并挂在原本那次落盘之前;`scope` 一律取 `WF_BOARD` 单源——那正是各步提示词召回记忆用的同一个键,故"回流下去下一轮同一步吃得到"是结构上保证的。失败路径(理解回退模板、拆镜回退本地、零产出、LLM 报错)一律不写;回流内容有意只取"做到什么程度、还差什么",不取集标题/主体名/模型评分(那些会被下一轮照抄)。SK-26 至此不再有"某步不回流"的欠账,`G-11`/`G-02` 两条仍开,见 [w61-sk26-front-writeback.md](./w61-sk26-front-writeback.md)。**W43 点名的 `G-11` 三面里,"读记忆不按板块过滤"那一面随 W65 收掉**:`evolveExpert` 的蒸馏输入改经 `WfCore.expertBoards`(生效板块 = 板块雇佣 > 全局雇佣,判据与 `personaFor` 同一套,专家/项目树/雇佣状态/板块词表全经参数注入)+ `WfCore.memForBoards`(只收 `scope` 命中的条目,无 `scope` 的手工沉淀不收,按 `text` 去重)硬过滤——**召回串错板块只坏这一轮的上下文,蒸馏是写死进 `persona` 且没有撤回口**,故蒸馏侧不补召、取不到就回空数组让调用方跳过;板块或条目取不到的两道闸都落在 `Tasks.start`/`U.charge` 之前(不登记不扣费,计费口径一字未动),文案点名是哪个板块;`system` 与 `user` 两侧都点明生效板块,断言钉的是别的板块正文在**两侧都不得出现**。人手触发点原样保留(三个调用点一个没动),**`G-11` 因此没有清零**——蒸馏仍是人手动作、且仍只对自定义专家开放,两面照旧记在 SK-26 的 `note` 里,见 [w65-g11-mem-scope-filter.md](./w65-g11-mem-scope-filter.md)。**W43 那句"反复闭环不刷满 50 条上限挤掉用户偏好"随 W101 补上第二层**:`fb` 原地更新压住的是"同一个键被反复写",压不住"键本身在变多"——`fb` 键粒度里有集号(`und`/`sb`/`review` 每集各一条),`3 + 3×集数` 到第 16 集即 51 条,原先按 `slice(-MEM_MAX)` 砍数组头部,而用户手打的「记住…」正是最早写进去那批。现改为满桶按优先级淘汰:先挤最旧的自动回流条,用户自沉淀的留住,本次刚写下/刚更新的条目不参与淘汰,桶里没有自动条时退回先进先出(空桶与全用户条两种情形与改前逐字相同);`MEM_MAX = 50`、计费、发布门与六处写入点一字未动,改动全在 `memWrite` 那一个纯函数里,见 [w101-mem-cap-priority.md](./w101-mem-cap-priority.md)。**W103 把用户手打那一半的写入面也接进同一个 `memWrite`**:`js/agent.js` 的 `memRemember` 与 `cli.js` 的 `memory add` 此前各写一份裸 `slice(-50)`,而 W101 之后桶头部留下的正是那些受保护的用户条——于是形成"自动条写入时绕开用户条、用户条写入时只砍用户条"的别扭形态,两条用户偏好里先写的那条会被后写的顶掉,而桶里同时躺着几十条随时可再生的回流统计。两处各删一行截断改成一行 `memWrite`(无 `fb` 的条目本来就走追加,优先级顺带生效),`MEM_MAX`、120 字截断、落盘时机与七个调用点一字未动;50 这个数自此只在 `WfCore.MEM_MAX` 一处,两条认字面的源级断言按函数段取重钉,见 [w103-memremember-memwrite.md](./w103-memremember-memwrite.md)。

## 阅读约定

- **缺口编号**:`G-01…G-15` 出自资产图谱,**冻结在 15 项不再新增**;新登记的缺口一律走短名单的 `S-xx` 命名空间。判定标准文档里提议的 `G-16`(发布后→上游回路)按此规则改记为 `S-08`。
- `docs/Agent贯通落地-G1-G5.md` 里的 `G1–G5` 是该文自带的历史编号,与本目录的 `G-0x` 不是同一套,对应关系见该文与 [w5-cycle1-audit.md](./w5-cycle1-audit.md) 第 2.18 节。
- 文档描述功能本身,不写功能溯源。
- 动工前先看**最新一份收敛记录**的"剩余未合与残留",避免重做已落地的部分:当前是 [w95-integration-log.md](./w95-integration-log.md)(更早的分叉登记在 [w90-integration-log.md](./w90-integration-log.md)、[w85-integration-log.md](./w85-integration-log.md)、[w80-integration-log.md](./w80-integration-log.md)、[w75-integration-log.md](./w75-integration-log.md)、[w72-integration-log.md](./w72-integration-log.md)、[w70-integration-log.md](./w70-integration-log.md)、[w68-integration-log.md](./w68-integration-log.md)、[w67-integration-log.md](./w67-integration-log.md)、[w64-integration-log.md](./w64-integration-log.md)、[w62-integration-log.md](./w62-integration-log.md)、[w60-integration-log.md](./w60-integration-log.md)、[w57-integration-log.md](./w57-integration-log.md)、[w55-integration-log.md](./w55-integration-log.md)、[w21-integration-log.md](./w21-integration-log.md)、[w16-integration-log.md](./w16-integration-log.md)、[w13-integration-log.md](./w13-integration-log.md)、[w9-integration-log.md](./w9-integration-log.md)、[w7-integration-log.md](./w7-integration-log.md) 与 [w6-integration-log.md](./w6-integration-log.md) 第 5 节)。W13 的记录件随 W21 合入,W16 第 5.3 节记的那条悬挂索引行随之解掉。