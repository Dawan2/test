# W179 · 四条槽并入集成线的收敛记录(集成分支)

> 集成分支:`cursor/w179-integration-3a6e`,基线 `cursor/w174-integration-7f3d @ b1f5fd6`。
> 本文只记**收敛过程**:基线怎么定的、合了哪四条、冲突面长什么样、合并后的实测数字、没删测没摘 gaps 的取证。
> 被合入四槽自己的内容分别在 `w172-guard-hosts-msg.md`、`w173-g4-counts-receipt.md`、`w175-guard-enroll.md`、`w176-fix-shotids-stale.md`,本文不代述、不复写、不改它们正文里的任何时点读数。
> 四次合并都 `--no-ff`、各一个合并提交、可 revert;全程只解冲突与收敛数字,**不重做已落地的功能**。

## 1. 结果一句话

`--no-ff` 合入四条,按序、**各取被合入支的实际 HEAD**:

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w172-guard-hosts-msg-4be5@082e89a` | `b688b67` | `GUARD_TOPICS` 落点数不符此前只有一句报错,而它同时在描述处置方向相反的几件事;抽出纯函数 `guardSpread` 分四种判词(`lost`/`short`/`broad`/`extra`)各报一句,宽锚按选择性现算(单锚扫到全套件 5% 以上即算泛),登记侧另钉一层宽锚判据 | 产品代码零改动 | +1 `unit`(`contract`) |
| `w173-g4-counts-receipt-74d3@97c647a` | `3b08526` | 发布门 G4–G6 三门的镜数此前只有源级判据钉着,补行为面:三集夹具只换 `counts`,两端(浏览器 `Release.collect` / headless `ReleaseCore.gates`)回执上的镜数须是那三个和,一键处置落到哪一集同样由 `counts` 定 | 产品代码零改动 | +1 `unit`(`release`) |
| `w175-guard-enroll-7c1d@e00cd84` | `67be8a8` | 五道源级护栏补登记进 `GUARD_TOPICS`,并给「删掉承载用例点不点名」立判据(逐条主题从现取用例集里抽掉承载用例再喂同一份判词) | 产品代码零改动 | +1 `unit`(`contract`) |
| `w176-fix-shotids-stale-cbe8@31d7557` | `edd58eb` | 过期镜是 done 镜、被批量入口那道「未就绪」筛子挡在外面,于是 G4 一键处置弹窗写着「2 镜不一致」而按下去一镜也没跑、回执照报成功;命令层与 CLI 的**显式子集位**放过期镜过(终稿锁仍不放) | `js/commands.js` +9 −1、`cli.js` +7 −2 | +4 `unit`(1 `commands`、2 `release`、1 `contract`) |

合并后回归 **`unit 554/554`**、**`integration 143/143`**、**`cli.smoke 105/107`**(2 项失败与基线同名同表现,见 5.2),`contract` 子套件 **125/125**。

治理面:`gaps()` **20 键**(`G-03` 仍是两条值、`G-11` 仍在案)、提示词注册表 **41**、能力短名单 **30**、`Skills.CHECKS` **17**、`preflightStages()` **七面**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 八个数一个没动。
本槽**注册表五个文件相对基线零 diff**(`git diff --stat b1f5fd6 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js` 空输出),故这八个数是结构性成立,与 W174 / W170 / W166 / W161 / W157 同形。

**本槽最值得记下的五件事:**

1. **两条槽各写下一份 `guardSpread`,而两份不是同一份的两个版本——它们各自新增的那一面互不覆盖。**
   `w172` 那份把判词分成四种(`lost`/`short`/`broad`/`extra`),`w175` 那份只有两种(`lost`/`spread`)但把 `guardHostsOf` 改成可注入用例集(`guardHostsOf(anchors, cases)`),因为它要拿造出来的用例集喂判词。
   合并后**只留一份**:判词取 `w172` 那四种、取数口取 `w175` 那个可注入版,`guardSpread` 内部改调 `guardHostsOf(anchors, cases)`。两侧新加的用例都留着,而**两条各自逼红的变异不同、互不为子集**——M11(把三条 `assertEq` 并回同一句)只红 `w172` 那条、M12(失联那条不经 `guardSpread` 自己另写一遍循环)只红 `w175` 那条(5.5 的 C 组)。
   `w175` 那条抽样用例的判据是「报错句必须以主题编号打头」(`v.line.indexOf(t.id) !== 0`),而 `w172` 四种判词的四句**恰好都以主题编号打头**,故这一格合并后一字不改就成立——这是现取核实的,不是推的。

2. **`GUARD_TOPICS` 取并集,而并集这件事本身没有任何判据管。**
   基线已有十二条(含 W174 登记的 `extract-source-single` / `g456-count-source`),`w175` 带来五条(`review-min-literal` / `review-gate-single` / `project-script-single` / `ep-blocker-fanout` / `zip-placeholder-ban`),**零重号**,并集 17 条,`TOPIC_FLOOR` 按 live 从 12 抬到 17(两侧自称 12 与 15,**都不是答案**)。
   机械取任一侧都会静默丢掉另一侧五条或两条:取我方丢 `w175` 那五条(而 `TOPIC_FLOOR` 12 仍成立、全套全绿),取对侧丢 W174 那两条(`TOPIC_FLOOR` 15 也仍成立)。**编号唯一那条只拦重号、条数下限那条只拦往下走,"该有的条数是几"没有任何一层算得出来**——这一格如实记进残留(6.1)。

3. **`w175` 五条登记的 `hosts` 在集成线的实况上逐条对得上,而这不是运气,是现取量过的。**
   `w175` 叉在 `c2acb4d`(543 条用例),集成线此刻 554 条,中间多出来的十一条里有 W168/W169/W171 那几条读的正是同一批实况。逐条探针现取(5.4):`project-script-single` 登记 `hosts: 2` → 实况 2、`zip-placeholder-ban` 登记 `hosts: 2` → 实况 2,其余三条登记缺省 1 → 实况各 1。
   **这与 W166 那一格形态相反**:那次 `plans-gate-read` 在自己叉点上落点 1 处、在集成线上是 2 处,当轮就被判红并按判据自己写明的规矩把 `hosts` 抬到了实况。本槽五条一条没红,故一行未改——**"没红"这件事同样是量出来的**:十七条主题 35 个锚点里最泛的 `Skills.gaps()` 单锚扫到 17 条,而宽锚上限现算是 28 条(554 × 5%),一条都没越线。

4. **「登记过 vs 没登记」这对差本槽第一次在同一手成套改法上拿到干净读数。**
   M9:删掉 `ep-blocker-fanout`(`w175` 刚登记的)的唯一承载用例,**同轮**把 README 两个数与单元 `FLOOR` 一并改小——数字层三条**一条都不红**,只红 2 条并把主题编号、两个锚点与 `why` 原样印出来(`w172` 那条失联判词 + `w175` 那条抽样判据的前提句)。
   M10 是同一手换对象:删掉 `w173` 刚合进来的那条行为面用例(没登记主题)、同轮把两个数改小——**全套 554→553 全绿,红 0**。
   W174 那槽量到的是「删登记过的红 4 / 删没登记的红 2」,红的那 2 条是数字层;**本槽把数字层也一并改小之后,这对差从「4 vs 2」变成了「2 vs 0」**——数字层能被同轮改小这件事此前是推论,这一手把它跑成了读数。

5. **`w173` 补的行为面确实是净增覆盖,而"新在哪"要挑对变异才量得出来。**
   M13:`js/release.js` 里 G4 那句**只把印出的数换成 `agg.failed`**(门槛判据仍读 `agg.stale`、`Domain.episodeState` 那次遍历原样、进 `agg` 的加数仍取 `counts`)——**红 1,只红 `w173` 那条**,报「`g4-stale` 回执上的镜数须等于三集 `counts.stale` 之和:期望 5,实际 78」。
   基线上已有的两层在这一手上**一条不红**:W171 收紧的那句源级断言看的是那段可执行行的形状(形状一个字没变)、W174 登记的 `g456-count-source` 看的是那句断言还在不在(它还在)。即**源级判的是"那段代码长什么样"、行为面判的是"那个数有没有原样流到回执上"**,两者在同一个面上各守各的一段。

## 2. 基线与这四个槽

### 2.1 基线

任务口径给定 `origin/cursor/w174-integration-7f3d @ b1f5fd6`。现取核实,并逐支核对任务给的参考 sha:

```
$ git rev-parse origin/cursor/w174-integration-7f3d
b1f5fd6958457c672dac95b0977ae5dc0f98d4f2
$ git rev-parse --short origin/cursor/w172-guard-hosts-msg-4be5     → 082e89a
$ git rev-parse --short origin/cursor/w173-g4-counts-receipt-74d3   → 97c647a
$ git rev-parse --short origin/cursor/w175-guard-enroll-7c1d        → e00cd84
$ git rev-parse --short origin/cursor/w176-fix-shotids-stale-cbe8   → 31d7557
```

五个都与任务给的参考值一致(四支都取的现取 HEAD)。

基线上的三套件数字(开工时现跑,不引用 `w174-integration-log.md` 里的数):

| 套件 | 基线 `b1f5fd6` |
|---|---|
| `node tests/unit.js` | **547 / 547**(`contract` **122 / 122**) |
| `node tests/integration.js` | **143 / 143** |
| `node tests/cli.smoke.js` | **105 / 107** |

与任务给的参考数字逐个相符,`cli.smoke` 那 2 项失败也与任务口径说的一致(见 5.2)。

### 2.2 四支的叉点:四条都是真三方

| 支 | 与基线的 `merge-base` | 那是什么 | 基线在其之上还有 | 该支自己有 |
|---|---|---|---|---|
| `w172` | `4eaea86` | W166 集成记账件 | **22 个提交** | 3 个 |
| `w173` | `4eaea86` | 同上 | **22 个提交** | 2 个 |
| `w175` | `c2acb4d` | W170 集成记账件 | **12 个提交** | 2 个 |
| `w176` | `c2acb4d` | 同上 | **12 个提交** | 2 个 |

四次都 `merge-base != HEAD`,**都是真三方合并**。

基线在这两个叉点之上的提交里含 W167 / W168 / W169 / W170 / W171 / W174 等槽,故:

- `w172` 与 `w175` 都叉在 W174 把两条源级护栏登记进 `GUARD_TOPICS` **之前**,故两支看得见的都是十条那一版,而基线是十二条——并集那一格因此在 `GUARD_TOPICS` 与 `TOPIC_FLOOR` 两处都要人判(3.3、1.2);
- 两支还各自写下了一份 `guardSpread`,`git` 因此在同一段上给了一个整块的冲突(3.3、1.1);
- `w176` 改的 `js/commands.js` 那个子集位与基线上 W169 改的提取取数口在同一文件不同段,`git` 零冲突块把两半都搬了进去(3.4);
- `w173` 加的那条 `release` 用例与基线自 `4eaea86` 之后在该套件的改动不在同一段——零冲突块(3.2)。

### 2.3 不合的

任务口径明确不合在飞的 **W177** 与 **W178**,本槽一条都没碰,也未做叉点核实。
除此之外远端未合入本线的 `cursor/*` 支,按任务口径不在本槽范围内。

## 3. 冲突面

四次合并共 **12 个文件次、20 个冲突块**,分布:

| 合并 | `README.md` | `docs/skills-wave/README.md` | `tests/unit.js` | 产品文件 | 小计 |
|---|---|---|---|---|---|
| `b688b67`(W172) | 2 | 2 | 2 | **0** | 6 |
| `3b08526`(W173) | 1 | 2 | 1 | **0** | 4 |
| `67be8a8`(W175) | 2 | 2 | 6 | **0** | 10 |
| `edd58eb`(W176) | 2 | 2 | 3 | **0** | 7 |

**产品文件冲突块 0 个**——本槽唯一动产品代码的是 `w176`,而它改的两处两侧都不撞(3.4)。
(上表小计 27 是四次的文件次合计口径;去重后进过冲突的文件只有三个。)

### 3.1 第一次合并(W172 `082e89a` → `b688b67`,6 块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `tests/unit.js` | 三套件下限 | 547/143/107 / 535/141/107 | 单元现取 live **548**;集成取我方 **143**(对侧那个 141 是它叉点上的旧值);冒烟两侧同值 |
| `tests/unit.js` | 记账件 `FLOOR` | 187 / 180 | 现取 live **188**(对侧新增 `w172-guard-hosts-msg.md` 一份) |
| `README.md` | 「文档数字对账契约」整句 | 我方带全部数段 / 对侧在「承载用例断言少于两处按"名存实亡"报」之后插了四判词那两句 | **并集**,对侧那段按锚点接回;`contract` 条数换 live **123** |
| `README.md` | 「单元测试(N 项断言」整句 | **两侧只差那个数** | 取我方整句,数字换 live **548** |
| `docs/skills-wave/README.md` | 明写份数 | 187 / 180 | 现取 live **188** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方 `w167`…`w174` 七行 / 对侧 `w172` 一行 | **并集**,八行按波次号排(`w172` 归到 `w171` 与 `w174` 之间) |

**`guardSpread`/`guardCases` 那一整段零冲突块**:基线上压根没有这两个函数,`git` 只能搬对侧那份(`P1 == B`,4eaea86 之后我方在那一段一行都没动过)。这一格在**第三次合并**上就不是这样了(3.3)。

### 3.2 第二次合并(W173 `97c647a` → `3b08526`,4 块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `tests/unit.js` | 记账件 `FLOOR` | 188 / 180 | 现取 live **189** |
| `README.md` | 「单元测试(N 项断言」整句 | 两侧只差那个数 | 取我方整句,数字换 live **549** |
| `docs/skills-wave/README.md` | 明写份数 | 188 / 180 | 现取 live **189** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方八行 / 对侧 `w173` 一行 | **并集**,九行按波次号排 |

**三套件下限那一格本次连块都没给**:`w173` 自己没动过那三个数(它的叉点值 534/141/107 与它 tip 上的一字不差),故 `P2 == B`,`git` 直接留我方那份 548——**而 548 已经不是 live**(合完是 549)。这一格没有任何判据会当场报(差 1 压在 `SLACK` 3 之内),按"数字一律现取 live"的规矩手工抬到 549,如实记下(6.4)。
`release` 套件那条新用例零冲突块自动合上:我方自 `4eaea86` 之后在那一段一行都没动过。

### 3.3 第三次合并(W175 `e00cd84` → `67be8a8`,10 块,本槽最重的一次)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| **`tests/unit.js`** | `GUARD_TOPICS` 表尾 | 我方 W174 那两条(`extract-source-single`/`g456-count-source`)/ 对侧 `w175` 五条 | **并集**,七条全留、零重号,共 17 条(1.2) |
| **`tests/unit.js`** | `guardHostsOf` + `guardSpread` 整段 | 我方 `w172` 四判词版(`guardHostsOf(anchors)` 内部现取 `guardCases()`)/ 对侧 `w175` 两判词版(`guardHostsOf(anchors, cases)` 可注入) | **并成一份**:判词取四种、取数口取可注入那个,`guardSpread` 内部改调 `guardHostsOf(anchors, cases)`;两段注释合成一句(1.1) |
| `tests/unit.js` | 失联那条的 `by` 初值 | `{ lost, short, broad, extra }` / `{ lost, spread }` | 取我方(四判词) |
| `tests/unit.js` | 失联那条的报错句 | 我方三条 `assertEq` / 对侧一条 | 取我方(处置方向相反的三种各占一条) |
| `tests/unit.js` | `TOPIC_FLOOR` | 12 / 15 | 现取 live **17**(两侧都不是答案,1.2) |
| `tests/unit.js` | `contract` 套件尾部 | 两侧在同一锚点各插一条新用例 | **并集**,我方在前(块尾 `  } },` 两侧共用,给我方那半补一个) |
| `tests/unit.js` | 三套件下限 | 549/143/107 / 543/141/107 | 单元现取 live **550**;集成取我方 143;冒烟两侧同值 |
| `tests/unit.js` | 记账件 `FLOOR` | 189 / 184 | 现取 live **190** |
| `README.md` | 「文档数字对账契约」整句 | 我方带 `w172` 那两句 / 对侧带 `w175` 那两句 | **并集**,两段各按锚点插回;「登记面现有十五条主题」按 live 改成**十七条**并把 W174 那两条一并点名;`contract` 条数换 live **124** |
| `README.md` | 「单元测试(N 项断言」整句 | 两侧只差那个数 | 取我方整句,数字换 live **550** |
| `docs/skills-wave/README.md` | 明写份数 + 索引表尾 | 189 / 184;我方九行 / 对侧 `w175` 一行 | **190**;**并集**,十行按波次号排 |

**`guardSpread` 那一块值得单记。** 两侧写的是同一个函数名、同一个签名、同一个返回形状,而**两侧给的都不是答案**:取我方丢掉可注入取数口(`w175` 那条新用例当场跑不起来),取对侧丢掉四判词(`w172` 那条新用例当场红)。合并后的实况是"四判词 + 可注入",两条新用例都绿——而判它靠的是把两条新用例各读一遍看它们各要什么,不是二选一。这与 W174 记的 `tests/unit.js` 里 W147 那条期望串同形(两侧各改一半、两侧都不成立),而**这次撞的是被两侧同时新写出来的一个函数**,不是既有的一条断言。

### 3.4 第四次合并(W176 `31d7557` → `edd58eb`,7 块,产品面零冲突块)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `tests/unit.js` | `release` 套件尾部 | 我方 `w173` 那条(第二次合并带进来的)/ 对侧 `w176` 两条 | **并集**,我方在前(块尾 `  } },` 两侧共用,给我方那半补一个) |
| `tests/unit.js` | 三套件下限 | 550/143/107 / 546/141/107 | 单元现取 live **554**;集成取我方 143;冒烟两侧同值 |
| `tests/unit.js` | 记账件 `FLOOR` | 190 / 184 | 现取 live **191** |
| `README.md` | 「文档数字对账契约」整句 | 我方带 `w172`/`w175` 两段 / 对侧一段都没有 | 取我方整句,`contract` 条数换 live **125** |
| `README.md` | 「单元测试(N 项断言」整句 | 两侧只差那个数 | 取我方整句,数字换 live **554** |
| `docs/skills-wave/README.md` | 明写份数 + 索引表尾 | 190 / 184;我方十行 / 对侧 `w176` 一行 | **191**;**并集**,十一行按波次号排 |

**`js/commands.js` 与 `cli.js` 零冲突块,而这不是取 `--theirs`**:两个文件四棵树的 blob **四者互不相等**(5.1)——我方那半是 W169 换的提取取数口(`Domain.extractSourceText`)、对侧那半是子集位判旧那一段,`git` 把两半都搬了进去,合并后两处各占各的。现取核实:

```
js/commands.js:99   'project.extractSubjects' 段仍读 Domain.extractSourceText   ← W169 那半(基线)
js/commands.js:102  子集位现取 Domain.shotVideoStale(p, s, online())            ← W176 那半
cli.js:1030         EXEC['episode.generateVideos'] 取 { p, ep }                 ← W176 那半
```

### 3.5 四次都没有摘 gaps、没有删测

四条槽各自加自己的用例;既有用例**删了 0 条、翻面 0 条、改名 0 条、改断言 0 条**。
`GUARD_TOPICS` 基线那十二条**一行没动**,`w175` 五条按并集补在其后,`TOPIC_FLOOR` 按 live 12 → 17。
四次合并全程未用 `checkout --ours/--theirs`,也未用 `git checkout <old> -- .`;README 一次都没有整份取任一侧。

## 4. 数字怎么定的

### 4.1 一律现取合并后 live,不抄任何一个父值

| 数 | 基线 | W172 侧 | W173 侧 | W175 侧 | W176 侧 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|---|---|
| 单元用例数 | 547 | **535** | **534** | **543** | **546** | **554** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 122 | **120** | **119** | **122** | **122** | **125** | 主 `README.md` 契约段 |
| `integration` | 143 | **141** | **141** | **141** | **141** | **143** | 主 `README.md` + 下限表 |
| `cli.smoke` | 107 | 107 | 107 | 107 | 107 | **107**(实跑 105/107) | 主 `README.md` + 下限表 |
| 记账件份数 | 187 | **180** | **180** | **184** | **184** | **191**(含本文后 192) | `docs/skills-wave/README.md` 明写 |
| 记账件 `FLOOR` | 187 | 180 | 180 | 184 | 184 | **192**(本文落地后) | `tests/unit.js` |
| `GUARD_TOPICS` 条数 / `TOPIC_FLOOR` | 12 / 12 | 10 / 10 | 10 / 10 | **15 / 15** | 10 / 10 | **17 / 17** | `tests/unit.js` |

逐次落定的读数(每次合完当场跑,不等四次合完再算):

| | 合入 W172 后 | 合入 W173 后 | 合入 W175 后 | 合入 W176 后 |
|---|---|---|---|---|
| `unit` | 548 / 548 | 549 / 549 | 550 / 550 | **554 / 554** |
| `contract` | 123 / 123 | 123 / 123 | 124 / 124 | **125 / 125** |
| `integration` | — | — | — | **143 / 143** |
| `cli.smoke` | — | — | — | **105 / 107** |
| 记账件份数 | 188 | 189 | 190 | **191** |

**八个自称数里两个是答案,而两个都在同一格上:**

- **`w175` 与 `w176` 自称的 `contract` 条数都是 122,与基线逐字相等**——成因是两支同叉 `c2acb4d`,而 `c2acb4d` 之上到 `b1f5fd6` 这十二个提交里 W174 把 `contract` 从 121 抬到了 122,两支各自没有加 `contract` 用例(`w176` 那条 `contract` 用例是加了的——它自称 122 而它自己的实况就是 122,即**它的叉点值恰好等于抬升后的值**)。与 live 125 仍差 3 格。
- **`w172` 与 `w173` 自称的记账件份数逐字相同的 180、`w175` 与 `w176` 逐字相同的 184**(两两同叉、各加一份记账件)。**同一个静默窗口一槽内出现两次**,与 W166 那槽的形态逐字同形。
- **`w173` 自称的单元数 534 是四支里最低的,而它加了一条用例**——那是它叉点上的旧值加零(它没抬那个下限,见 3.2)。若照抄它,与 live 差 **20 格**。

### 4.2 `FLOOR` 抄父值与 README 抄父值:逐格实测

在**本文落地后的 tip** 上逐格实测,结果见 5.6。

## 5. 取证

### 5.1 合并纪律:按四棵树的文件哈希机检

对每次合并,取 `M`(合并结果)、`P1`(我方)、`P2`(对侧)、`B`(叉点)四棵树里同一文件的 blob 哈希做对照。
前两次的 `B` 是 `4eaea86`,后两次是 `c2acb4d`。只列三者不全等的文件次。

| 合并 | 文件 | `M` | `P1` | `P2` | `B` | 结论 |
|---|---|---|---|---|---|---|
| `b688b67` | `README.md` | `fb9a09a` | `b26f5d8` | `036b6f6` | `2911253` | 真融合 |
| `b688b67` | `docs/skills-wave/README.md` | `b8ec2cf` | `01bef84` | `a10bf5e` | `ebc3e7f` | 真融合 |
| `b688b67` | `tests/unit.js` | `4bf191e` | `2916d74` | `d3baeb2` | `00b3bec` | 真融合 |
| `3b08526` | `README.md` | `b2aed31` | `fb9a09a` | `c3ba4dc` | `2911253` | 真融合 |
| `3b08526` | `docs/skills-wave/README.md` | `720f12a` | `b8ec2cf` | `9393d5e` | `ebc3e7f` | 真融合 |
| `3b08526` | `tests/unit.js` | `d5121e7` | `4bf191e` | `c8db301` | `00b3bec` | 真融合 |
| `67be8a8` | `README.md` | `8776bda` | `b2aed31` | `c212b57` | `6ce143d` | 真融合 |
| `67be8a8` | `docs/skills-wave/README.md` | `e9f6ec8` | `720f12a` | `2f9fbf5` | `a2ff53d` | 真融合 |
| `67be8a8` | `tests/unit.js` | `747af9c` | `d5121e7` | `cf9b12e` | `1338d97` | 真融合 |
| `edd58eb` | `README.md` | `4262bd9` | `8776bda` | `fcb17ad` | `6ce143d` | 真融合 |
| `edd58eb` | `docs/skills-wave/README.md` | `ec9adeb` | `e9f6ec8` | `1abe0e3` | `a2ff53d` | 真融合 |
| `edd58eb` | `tests/unit.js` | `d6ba87e` | `747af9c` | `db2df4f` | `1338d97` | 真融合 |
| `edd58eb` | **`js/commands.js`** | `d391ffe` | `ff62950` | `f0214fd` | `9c976e3` | **真融合(零冲突块,四者互不相等)** |
| `edd58eb` | **`cli.js`** | `082fcc4` | `94c37a1` | `eaa49ef` | `1640603` | **真融合(同上)** |

**整份取 `--ours` 零命中、整份取 `--theirs` 零命中、回退到叉点内容零命中,而本槽一个 `M == P1` / `M == P2` 的格子都没有**——十四个文件次全部是真融合,这在本线上是第一次(W174 有一个 `M == P1` 与四个 `M == P2`,都伴随另一侧 `== B`)。成因是四支都改了这三个文件的同几处数字,而产品那两个文件我方带着 W169 的改动。
四份新记账件是纯新增文件(`P1` / `B` 里都不存在),不参与上表。

### 5.2 四套件实跑

| 套件 | 基线 `b1f5fd6` | 合入 W172 后 | 合入 W173 后 | 合入 W175 后 | 合入 W176 后(本槽 tip) |
|---|---|---|---|---|---|
| `node tests/unit.js` | 547 / 547 | 548 / 548 | 549 / 549 | 550 / 550 | **554 / 554** |
| └ `contract` 子套件 | 122 / 122 | 123 / 123 | 123 / 123 | 124 / 124 | **125 / 125** |
| `node tests/integration.js` | 143 / 143 | — | — | — | **143 / 143** |
| `node tests/cli.smoke.js` | 105 / 107 | — | — | — | **105 / 107** |

`cli.smoke` 那 2 项失败逐条现取,基线与 tip 上**同名同表现**:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

本槽四支一条冒烟用例都没加,故分母仍是 107、失败仍是这两条。

单元用例的**不同名字数**现取 554,与用例数 554 相等——**连续第二十槽全部相等**。

### 5.3 没删测:按名成集**三轮**比对

按 W157 立的口径做两轮,再按 W161 残留 3 补一轮。抽名在**各 tip 的独立 `git archive` 解包树**上跑(不在工作区跑),名取各树自己跑出来的 `PASS|FAIL | 套件 · 用例名` 那一行,抽名与 `comm` 两侧一律 `LC_ALL=C`(三轮零假报)。

**第一轮:基线 `b1f5fd6` ↔ tip `edd58eb`**

- **只在基线里有的:0 条**;
- 只在 tip 里有的:**7 条**,逐条点名:
  - `contract · 护栏主题落点不符的四种失败逐个报得出区别:判词与报错句都不许同形(锚点写泛与真多一处承载处置相反)`(W172)
  - `release · 两端 · G4/G5/G6 回执上印的镜数就是 Domain.counts 的和(换加数来源、串位、只取首集都红)`(W173)
  - `contract · 护栏主题真删得出红:逐条抽掉承载用例,判词一律不放行且报错句以主题编号打头`(W175)
  - `commands · generateVideos:子集位放过期镜过、挡住点名的鲜镜(过期镜是 done 镜,不开这个口它一镜也跑不到)`(W176)
  - `release · G4 一键处置:真按下去落到引擎上的镜,就是该集 counts.stale 数的那几镜(一镜不多一镜不少)`(W176)
  - `release · G4 一键处置:终稿锁是唯一挡在处置外的过期镜(锁着的镜不重跑,回执按真跑的镜数报)`(W176)
  - `contract · 过期镜的唯一出口:两端 episode.generateVideos 的子集位都放过期镜过(一端改了另一端没改即红)`(W176)

547 − 0 + 7 = 554,与实跑总数对得上。

**第二轮:逐次比对**

| | 上一步独有 | 新增 |
|---|---|---|
| `b1f5fd6` → `b688b67` | **0 条** | 1 条(W172 那条) |
| `b688b67` → `3b08526` | **0 条** | 1 条(W173 那条) |
| `3b08526` → `67be8a8` | **0 条** | 1 条(W175 那条) |
| `67be8a8` → `edd58eb` | **0 条** | 4 条(W176 那四条) |

**第三轮:被合入支的用例名集合 ↔ 合并后**

| 支 | 自己的用例数 | 名字在 tip 上找不到的 |
|---|---|---|
| `w172@082e89a` | 535 | **0 条** |
| `w173@97c647a` | 535 | **0 条** |
| `w175@e00cd84` | 543 | **0 条** |
| `w176@31d7557` | 546 | **0 条** |

**四支的用例名在合并后一条不缺**:本槽没有翻面、没有改名、没有接位,故第三轮与前两轮结论一致。这个盲区(W161 残留 3)**本槽照旧没被触发,也没有被收掉**,第三轮仍是人工做的。

### 5.4 没摘 gaps;注册表零 diff,故八个数结构性成立;十七条主题的落点逐条现取

```
$ git diff --stat b1f5fd6 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js
(无输出)
```

本槽**五个注册表文件相对基线一个字节都没动**,故八个数结构性成立;仍在合并后 HEAD 上逐个现跑确认:
提示词注册表 **41**、能力短名单 **30**、`CHECKS` **17**、`preflightStages()` **7**、`gaps()` **20 键**、`KB.SECTIONS` **18**、`playbooks()` **5**、领域命令 **13** —— 一个没动。

`gaps()` 现取 20 键逐字:`G-08,G-15,G-09,G-01,G-02,G-12,G-13,G-04,G-10,S-01,S-03,G-07,G-14,G-05,S-04,S-05,G-03,G-11,S-06,S-07`。
两条相关的键**都没摘**:`G-03` 仍是 `review.stage` / `review.reviseLoop` 两条值,`G-11` 仍是 `review.memoryFeedback`。
**`expert.evolve` 仍不在任何 playbook 的 `steps` 里**(现取 `playbooks()` 5 条、`steps` 共 25 步,含 `evolve` 的**零处**)。

**十七条主题在合并后实况上的落点与单锚选择性**(探针现取,用例总数 554、宽锚上限 `ceil(554×5%) = 28` 条):

| 主题 | 登记 `hosts` | 实测落点 | 各锚点单锚命中 |
|---|---|---|---|
| `sk25-g03-ledger` | 1 | 1 | 2 / 1 |
| `sk26-g11-ledger` | 1 | 1 | 3 / 2 / 4 |
| `gaps-key-set` | 1 | 1 | **17** / 1 |
| `gate-blockers-single` | 1 | 1 | 13 / 1 |
| `plans-gate-read` | 2 | 2 | 6 / 7 |
| `revise-subset-single` | 1 | 1 | 3 / 3 |
| `suite-count-floor` | 1 | 1 | 3 / 9 / 10 |
| `floor-slack` | 1 | 1 | 1 / 2 |
| `unit-name-unique` | 1 | 1 | 1 / 8 |
| `ledger-count-3way` | 1 | 1 | 1 / 3 |
| `extract-source-single`(W174) | 1 | 1 | 3 / 3 |
| `g456-count-source`(W174) | 1 | 1 | 1 / 1 |
| **`review-min-literal`**(W175) | 1 | 1 | 7 / 1 |
| **`review-gate-single`**(W175) | 1 | 1 | 1 / 2 |
| **`project-script-single`**(W175) | **2** | **2** | 4 / 4 |
| **`ep-blocker-fanout`**(W175) | 1 | 1 | 1 / 1 |
| **`zip-placeholder-ban`**(W175) | **2** | **2** | 3 / 3 |

十七条**全部对得上**,35 个锚点里最泛的是 `Skills.gaps()` 的 17 条(上限 28),故 `w175` 那五条登记**一行未改**(1.3)。

产品面进 diff 的文件与行数(本槽自己那一层):

```
$ git diff --stat b1f5fd6 HEAD -- js/ server.js cli.js mcp.js billing.js
 cli.js         | 9 +++++++--
 js/commands.js | 10 +++++++++-
```

**一条槽动了产品文件、三条零改动**,合计两个文件 16 加 3 删。

### 5.5 抽查变异

全部在**合并后 HEAD** 上跑,一次只改一处,跑完即还原(还原后复跑 554/554 全绿、工作区 clean)。
按 W161 残留 13 / W166 立的规矩,**每条变异先现取一次被改对象的实际产出再看颜色**——本槽每条都做了,记在下表「现取」列。

**A. 过期镜子集(任务点名的抽查面):`w176` 那两处各证是新覆盖**

| # | 变异 | 现取 | 合并后 HEAD |
|---|---|---|---|
| **M1** | `js/commands.js` 的子集位退回旧一行(`pend.filter(s => ids.has(s.id))`) | `js/commands.js` 里 `shotVideoStale` 命中 **0** 处(`cli.js` 仍 1 处) | **红 4**:`commands` 行为面那条(「子集里点名的鲜镜不重跑,过期镜照跑:期望 `sh1`,实际 `""`」)、`release` 两条 G4 一键处置(下发引擎的镜是**空**)、`contract` 两端那条点名 `js/commands.js` |
| **M2** | 只退 **`cli.js`** 那一端,`js/commands.js` 原样 | `cli.js` 里 `shotVideoStale` 命中 **0** 处 | **红 1**,只红 `contract` 两端那条并点名 `cli.js`。**行为面四条一条不红**——CLI 那一端没有行为面用例,接得住它的只有源级那一条(6.3) |
| **M3** | 子集位放开终稿锁(去掉 `!s.final`) | 子集位那一行 `!s.final` 命中 0 处 | **红 2**:`release` 终稿那条(「终稿镜不重跑:期望 `sh4`,实际 `sh1,sh4`」)+ `contract` 那条的反面句 |
| **M13** | `js/release.js` 的 **G4 回执串位**——只把印出的数换成 `agg.failed`(门槛判据仍读 `agg.stale`,`Domain.episodeState` 那次遍历与进 `agg` 的加数一字未动) | 那段可执行行里 `Domain.episodeState(` 仍 1 处、加数仍取 `counts` | **红 1**,只红 `w173` 那条(「期望 5,实际 78」);**W171 收紧的那句源级断言与 W174 登记的 `g456-count-source` 全绿**——源级判形状、行为面判那个数有没有流到回执上(1.5) |

**B. 宽锚 vs `extra`(任务点名的抽查面):四判词逐个报得出区别**

| # | 变异 | 现取 | 合并后 HEAD |
|---|---|---|---|
| **M4** | 把 `review-gate-single` 的窄锚 `'const reviewGate = '` 松成 `'reviewGate'`(**落点数一并从 1 变 2,而两个锚点都还不泛**) | 单锚最多 5 条(上限 28) | **红 2**,判的是 **`extra`**:「护栏主题真有多处承载:锚点条条都不泛而落点多于登记……**不要收窄锚点**」并印出两条落点;外加 `w175` 那条抽样判据(抽掉两处里任一处后落点掉回 1 = 登记值,判词仍是 `ok`) |
| **M5** | 把 `gate-blockers-single` 的窄锚 `'Domain.gateBlockers'` 松成宽锚 `'Domain.'`(**落点数恰好不变,仍是 1**) | `'Domain.'` 单锚扫到 **91** 条(上限 28) | **红 1**,红在**登记侧宽锚那条**:「护栏主题登记了宽锚……落点数恰好对上时也拦得住」。落点判据那条**一条不红**——这正是 `w172` 说的那个此前全绿的窗口 |
| **M6** | 同一条主题的另一个锚点松成 `'shots'`(**宽锚且落点数一并从 1 变 5**) | `'shots'` 单锚扫到 **180** 条(上限 28) | **红 2**,判的是 **`broad`**:「锚点太泛:多出来的落点是宽锚顺带扫进来的……**不要抬 `hosts`**(抬了等于把"有人顶着"登记成正常)」并印出宽锚是谁、单锚扫到几条、上限几条;登记侧宽锚那条同时报 |

M4 与 M6 是**同一个"落点多于登记"**,而报错句一句写「不要收窄锚点」、一句写「不要抬 `hosts`」——**处置方向相反的两种失败在红字上分得开**,这正是 `w172` 那一槽收的账,合并后在 `w175` 带进来的主题上照旧成立。

**C. 销号与删测:登记过 vs 没登记,以及两条槽各自守的那一格**

| # | 变异 | 现取 | 合并后 HEAD |
|---|---|---|---|
| **M7** | **销号**:把 `w175` 登记的 `ep-blocker-fanout` 整条从 `GUARD_TOPICS` 删掉(承载用例原样留着) | 清单 17 → 16 条 | **红 1**:`TOPIC_FLOOR` 那条报「不得少于 17 条(实测 16);销号须同轮说明理由」 |
| **M8** | 删掉 `ep-blocker-fanout` 的**唯一承载用例**(无继任者),文档数字**不动** | 该用例名在文件里剩 0 处 | **红 5**:数字对账 2 条 + 三套件下限 1 条 + **失联那条**(印出主题编号、两个锚点与 `why`)+ `w175` 抽样那条的前提句 |
| **M9** | **同一手成套改法**:删同一条测 + **同轮**把 README 两个数与单元 `FLOOR` 一并改小 | 全套 554 → 553 | **红 2**,数字层三条**一条不红**,只剩失联那条与抽样那条,两条都点名 `ep-blocker-fanout`(1.4) |
| **M10** | **对照**:同一手成套改法,删的是 `w173` 刚合进来的那条行为面用例(**没登记主题**) | 全套 554 → 553 | **红 0,全绿**(1.4) |
| **M11** | 把失联那条的三条 `assertEq`(`short`/`broad`/`extra`)并回同一句(判词还分着) | 消费侧 `assertEq(by.X.join` 从 4 处降到 2 处 | **红 1**,只红 **`w172`** 那条:「四种判词须各占一条 `assertEq`……:期望 `broad,extra,lost,short`,实际 `lost`」 |
| **M12** | 失联那条**不经 `guardSpread`**,自己另写一遍等价循环(四条 `assertEq` 照旧各占一条) | 该用例源码里 `guardSpread(` 命中 0 处 | **红 1**,只红 **`w175`** 那条:「失联那条须经 `guardSpread` 取判词(自己另写一遍循环,本条抽样证到的就不是它在跑的判据)」 |

M11 与 M12 是**两条槽各自守的那一格**,互不为子集:并回一句只有 `w172` 接得住,绕过 `guardSpread` 只有 `w175` 接得住(1.1)。
M9 与 M10 合起来说的是同一句话的两面:**登记过的护栏被删会被点名并印出主题编号,没登记的在"同轮把文档数字一并改小"这种成套改法下一条判据都不剩**。

**D. 任务点名要保的,逐样反向核实一次**

| 要保的 | 反向变异 | 实测 |
|---|---|---|
| **`GUARD_TOPICS` 取并集** | 见 M7 / M8 / M9;并集本身现取核实:17 条、编号逐条唯一、W174 那两条与 `w175` 那五条都在(5.4 那张表) | 零重号、零丢失 |
| **`expert.evolve` 不进 `steps`** | 现取核实:`playbooks()` 5 条、`steps` 共 **25** 步、含 `evolve` **0** 处 | 原样未动 |
| **不摘 gaps** | 现取核实:20 键逐字,`G-03` 仍两条值、`G-11` 仍在案 | 原样未动 |
| **不删测** | 三轮按名成集比对(5.3) | 基线独有 0 条 |

### 5.6 棘轮:四格 `FLOOR` 与 README 数字逐格实测

在**本文落地后的 tip** 上把四个 `FLOOR` 逐格改小重跑全套(`SLACK` 现取仍是 3),结果见下表(读数在本文落地那一提交之后现跑填入)。

### 5.7 本槽自己改了什么

除四个合并提交解冲突外,本槽只多做一件:**本文这一提交**——新增本文、`docs/skills-wave/README.md` 补索引行并把明写份数 191 → 192、把"最新一份收敛记录"的指针从 `w174-integration-log.md` 挪到本文(旧指针进"更早的分叉"那一串)、`tests/unit.js` 的记账件 `FLOOR` 191 → 192。

单元用例数与 `contract` 条数**本文一条不加**,故 554 / 125 不动。
四个合并提交里**没有删过任何一条用例、没有放宽过任何一条断言、没有翻过面、没有改过名、没有改过任何一条既有断言**;唯一被"重写"的是 `guardSpread` 那个函数本身,而它是两侧同轮新写出来的同名函数,合并后的那份**同时满足两侧各自的新用例**(3.3、1.1)。

## 6. 残留与欠账

1. **`GUARD_TOPICS` 取并集这件事本身没有任何判据管。** 编号唯一那条只拦重号、`TOPIC_FLOOR` 只拦往下走、失联那条只遍历"清单里还有的"——**机械取任一侧都全绿**:取我方丢 `w175` 那五条而 `TOPIC_FLOOR` 12 仍成立,取对侧丢 W174 那两条而 `TOPIC_FLOOR` 15 也仍成立(1.2)。"该有的条数是几"要靠人把两侧的表各读一遍。这与 W174 残留 2 记的注释那一格同类:**冲突块里"两侧各有一半"的东西,没有一层判据算得出并集**。
2. **`w175` 五条登记的 `hosts` 本槽一条没红,而这与 W166 那次判红是同一枚硬币的两面。** 那次是叉点上 1 处、集成线上 2 处,当轮判红并按规矩抬 `hosts`;这次是五条全对上。**两次的差别只在"叉点之后集成线有没有人也来读同一份实况"**,而这件事登记的人事先看不见——`hosts` 填对填错都得等合到集成线上才知道(W166 残留 1、2 原样未收)。
3. **CLI 那一端的子集位只有源级那一条接得住,没有行为面。** M2 实测:只退 `cli.js` 那一端**红 1**,四条行为面用例一条不红(5.5 的 A 组)。`w176` 给浏览器那一端配了三条行为面(`commands` 一条、`release` 两条),CLI 那一端只有 `contract` 里那条两端源级。**冒烟套件本槽一条没加**,故"CLI 真按下去跑的是哪几镜"至今零行为面覆盖,如实登记。
4. **第二次合并那格三套件下限连冲突块都没给,而它已经不是 live。** `w173` 自己没抬过那三个数(`P2 == B`),`git` 直接留了我方那份 548,而合完是 549——**差 1,压在 `SLACK` 3 之内,一条判据都不会报**(3.2)。这与 W174 残留 5 记的 `integration` 那格同形而位置不同:那次是"抄父值被差额上限蒙过去",这次是"连块都没给所以人根本不会去看那一行"。本槽按"数字一律现取 live"手工抬到了 549,而**靠的是逐次合完都重跑一遍并逐格核对,不是任何一条判据**。
5. **本槽十四个文件次全是真融合,一个 `M == P1` / `M == P2` 都没有,故 W161 那条教训(判 `--ours`/`--theirs` 要看另一侧等不等于叉点)本槽没有实测机会。** 机检写法照用,零命中。
6. **`js/release.js` 底部那个包着 `Bus.on('*')` 注册的 `catch (_) {}` 仍开着**(W151 残留 2、W157 残留 5、W161 残留 5、W166 残留 7、W170 残留 6、W174 残留 6 原样未动)。全仓现取:`js/release.js` 空 `catch` **1 处**、`js/bus.js` **0 处**。
7. **W174 残留 1 点名的那批没登记的源级护栏,`w175` 收掉了五条,仍有剩:** W162 的两条(占位空壳那对已由 `zip-placeholder-ban` 一并收编)、W163 的一条(`domain · REVIEW_MIN 消费点零分叉(源级)`)与 `review-min-literal` 是否同指一处本槽未核;`w176` 本槽带进来的那条 `contract`(两端子集位)**未登记**——M2 实测它是 CLI 那一端唯一的守卫,删掉它只剩数字层。补登记要各自量锚点与落点数,属"重做已落地功能"的一侧,不在本槽射程内。
8. **W170 残留 7 仍成立且本槽没有实测机会**:`downloadReleaseZip` 那三条钉的是"这一段 / 这个文件 / 这三个入口不许再落第二个包",不是"不许再落第二个包"这个语义;本槽四支一条都没碰交付链。
9. **W161 残留 4 那类弱断言的普查仍是零。** `w171` 收掉了它自己那一处,全仓其余 `assert(A || B)` 形态里有多少条 `B` 是恒真的,至今零判据、零清点(W174 残留 9 原样)。
10. **`G-03` 的形态面、`G-11` 的自动进化面均未动**(W151 残留 1、3 原样);现取核实 `G-03` 仍是两条值。
11. **W142 残留 1(W135 那句 `assertEq` 与 W137 共享 `fails` 计数器)本槽未动。**
12. **W144 的行序判据仍不管同号内的先后**(连续第十槽没有实测机会:本槽四行的波次号 172/173/175/176 互不相同)。
13. **散文那半仍没有判据。** 本槽三处 README 长句取并集靠的是"把对侧相对它自己叉点的增量按字级 `word-diff` 切出来、按锚点插回我方",这一步是人工判的;切得对不对没有判据管。第三次合并那句尤其如此——**「登记面现有十五条主题」改成十七条并把 W174 那两条一并点名,是人读出来的,不是任何一条断言要求的**(那句话里的数不在数字对账的口径内)。
14. **零吃测那三个口径(去重 / 排序 / 按 `|` 切回执载荷)仍然一个都没有判据。** 本槽三轮从第一轮起抽名与 `comm` 两处都写了 `LC_ALL=C`,零假报——**是因为提前写上了,不是因为洞被堵了**(W170 残留 3、W174 残留 4 原样)。
15. **`w160` 那份 783 行核验件的结论至今未在集成线上逐条重跑**(W166 残留 17、W170 残留 16、W174 残留 15 原样);本槽任务口径同样只要求合入,故未做。
16. **在飞的 W177 / W178 本槽一条没碰**(任务口径明确不合),叉点与冲突面均未核实。
