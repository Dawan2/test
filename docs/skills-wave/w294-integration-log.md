# W294 · 把 W293 合入 W292 尖(集成线上第十七条「快进形」)

一条槽:W293 给项目详情面补同 id 重复角标(tab 行两枚——主体库撞 id 报「位」、各集分镜撞 id
报「行」并说清摊在几集上;分集列表上撞车那几集的卡片各挂一枚报自己那一集几行),
进同一条集成线。一次 `--no-ff`,零冲突。

产品语义一个字没动;记账、索引、份数下限三处按合入纪律同轮校齐。

---

## 一、支尖与形状(全部现取 `rev-parse`,不采信任何一侧的自称)

| 位 | 现取 SHA | 与自称对不对得上 |
|---|---|---|
| 我方尖 `P1` = `origin/cursor/w292-integration-a523` | `501fe37ab449d72a77b4f7fe55f4f9cd6fc1e0c8` | W293 自称从 `501fe37` 起支,**逐字节相同** |
| 对侧 `P2` = `origin/cursor/w293-projdupbadge-57b5` | `525096e48fabd3ec26e848ac7ac447b0a27b100b` | 自称 HEAD `525096e`,**逐字节相同**,且是链尾 |
| `merge-base(P1, P2)` | `501fe37ab449d72a77b4f7fe55f4f9cd6fc1e0c8` | **恰等于 `P1`**,即 `P2..P1` 空 → **快进形**(集成线上第十七条) |
| 新支 | `cursor/w294-integration-9c41`,从 `501fe37` 拉出 | |
| merge commit `M` | `aff1c7dea2cfa5de9844dbeebde0c759c6e2d0ec`,parents `501fe37` + `525096e` | |
| `tree(M)` / `tree(P2)` | 两边都是 `c9c9d04508d49b92e9ff45dc7f64fbfe8e5a183a` | 逐字节相同 |

`P1..P2` 四条提交:`89ddb71`(项目详情面补两枚角标 + 分集卡片本集角标)、
`9f3abc3`(判据 +2 + 护栏主题 `dedupe-proj-badge-derive` + `TOPIC_FLOOR` 25 → 26)、
`584854a`(镜头那句说明在多集时也点名去哪儿收拾)、`525096e`(记账件 + 索引一行,记账件下限抬到 308)。

**快进形让整树 diff 恒空,这一格一条结论都不承载**(第十七次)。`git diff M P2` 空、
`tree(M) == tree(P2)`,两句在任何一次快进形合入上都恒真——包括「对侧一个字都没改」和
「我方把东西丢干净了」这两种正相反的情形。故取证全部压在**变异**与 **live** 两面。

产品面 `git diff P1 M` 逐文件现取 6 个文件:`js/episodes.js`(+46/−2)、`tests/unit.js`(+247/−3)、
`README.md`(+2/−1)、`docs/skills-wave/README.md`(+2/−1)、
`docs/skills-wave/w178-topic-floor-unlist.md`(+1/−0)加一份新记账件(+222/−0)。
`cli.js` / `mcp.js` / `server.js` / `index.html` / `js/roles.js` / `js/storyboard.js` /
`js/domain.js` / `js/sb-views.js` / `js/shotgroups.js` **九个文件 diff 全空**——两条 CLI 去重命令、
MCP 工具表、主体库与分镜面那两道预览 + 确认闸、那两侧的逐位/逐行卡片小标,连同
`Domain.dupIdScan` 与 `Domain.dupIdMarks` 两支派生本体,一格未动(这一屏读的就是它们)。
但"产品面不空"只证明**对侧改了东西**,仍不证明**我方没丢东西**。

---

## 二、本槽真正承载结论的那两格

### 2.1 同一手概念的变异:起点上锚点全部失配 / 合完上逐手红

基线两处都红 0(`P1` 709/709、`M` 711/711)。十二手同一概念加两手反事实,
在两棵干净 worktree 上各跑一遍(判据取 `commands` + `contract` 两套,新判据全落在 `commands`)。
每一手先报锚点命中数——**命中 0 处本身就是取证**:那段代码与那两条用例在起点那棵树上压根不存在。

| 手 | 改法 | 起点 `501fe37` | 合完 `aff1c7d` |
|---|---|---|---|
| M1 | 摘掉 tab 行主体那枚角标 | 锚点失配 | **红 2**(`commands` 69/71) |
| M2 | 两枚角标从 tab 行挪进「分集」tab 内容体 | 锚点失配 | **红 2** |
| M3 | 摘掉分集卡片那几枚本集角标 | 锚点失配 | **红 1**(70/71) |
| M4 | 主体那个数改报「共几位撞车」(含留原 id 那一位) | 锚点失配 | **红 2** |
| M5 | 项目页自己再扫一遍(不读现成那一份) | 锚点失配 | **红 1** |
| M6 | 把去重规则抄进这一屏(自己认识 `dupIdScan`) | 锚点失配 | **红 1** |
| M7 | 多集时点角标不清搜索词与页码 | 锚点失配 | **红 1** |
| M8 | 只有一集撞车时也回列表(不直接进那一集) | 锚点失配 | **红 1** |
| M9 | 分集卡片那枚角标点了不落到本集 | 锚点失配 | **红 1** |
| M10 | 角标顺手写库(替用户去重) | 锚点失配 | **红 1** |
| M11 | 镜头那个数把集数当行数报 | 锚点失配 | **红 2** |
| M12 | 两侧单位词对调(主体也数「行」) | 锚点失配 | **红 2** |
| CF-floor | `TOPIC_FLOOR` 退回 25 | 锚点失配(起点上那个字面就是 25) | **红 1**(`contract` 152/153) |
| CF1 | 只删本槽那两条新用例 | 锚点失配 | **红 4**(`commands` 69/69、`contract` 149/153) |

**十四手全红,漏网 0 手。** 三件值得单说:

- **M2 是这一槽挂点判据的那一手。** 项目详情面模板全树只有一份(`Views.projectDetail` 定义 1 处,
  调用 5 个文件),故"少挂一处入口"在这一层不成立;真会漏的维度是 **tab**——挂进某个 tab 的内容体
  就只有那一个 tab 看得见。M2 把两枚角标原样搬进「分集」tab 的内容体,行为在「分集」tab 上一格不差,
  只有逐 tab 真渲那条接得住。只渲默认 tab 的判据接不住这一手。
- **M5 / M6 是"行为面逐字节等价、只有源级接得住"那一类。** 再扫一遍表与把规则抄过来,
  渲出来的两枚角标一个字都不差(两份计算此刻恰好同结果);接住它们的是「整页各扫一次」
  与「这一段不许认识去重规则」那两句源级断言,护的全是将来——两份计算一漂,这一屏报的数
  就与用户点进那两页看到的计划对不上,"发现"这件事本身就不可信了。
- **CF-floor 现跑证明「仍写 25」当场红 1**,即棘轮那一格不是靠人自觉抬的。

### 2.2 live 探针:同一份夹具在起点 9/28、合完 28/28

探针不看用例、看真渲出来的页面:真起 `server.js`(`MV_DATA_DIR`/`MV_UPLOADS_DIR`/`MV_CONFIG`
隔离到临时目录、`env -u HUJING_SERVER -u HUJING_TOKEN`)+ 无头 Chrome + CDP + 真 `Store.pullState`
+ 真走 `#/project/<pid>` 路由 + 真点 DOM;只在 `/tmp` 跑,一个字不进仓库。

夹具有意造成三档:主体库四位里三位同 id(分属角色/场景/道具三个分类,**要改 2 位**)、
`ep1` 三行同 id(**要改 2 行**)、`ep2` 干净(**一枚不挂**)、`ep3` 两行同 id(**要改 1 行**)
——两个数都不是"撞车总数",也不是行下标顺着数下来的。

**19 格从红翻绿**,其中真正是产品面新话的那几格:

- tab 行两枚角标逐字对上:`🧹 主体 id 重复 2 位` / `🧹 镜头 id 重复 3 行(2 集)`;
- 两句 `title` 各报出代价与去处(主体那句点名「逐位计费」+「主体 id 去重」,
  镜头那句点名「逐行计费」+「镜头 id 去重」并逐集报「第一集 2 行、第三集 1 行」);
- **八个 tab 逐个真点一遍,两枚角标都在**(制片/剧本/导演/主体/分集/成片库/剧壳/切片,占 8 格);
- 分集卡片恰两枚:`ep1:🧹 id 重复 2 行`、`ep3:🧹 id 重复 1 行`,`ep2` 一枚不挂;
- 多集撞车时点镜头那枚**不猜跳某一集**(`location.hash` 仍是项目页)且**回列表时搜索词被清掉**
  ——探针先把搜索词打成「第九集」让整屏被过滤光(这一格两端同绿,是夹具前提),
  不清就是整屏一枚卡片角标都看不见;
- 点 `ep3` 那枚角标真落到 `#/project/p_w294/episode/ep3`。

另 9 格两端同绿:两侧单位词没对调、搜索过滤前提、多集不猜跳、渲这一路两棵树里 id 一个没变、
积分 100→100、任务台 0 条、主体去重仍是预览 + 确认闸、零未捕获异常。

**如实登记两处这一格说不出话的地方**(不含混说成"19 格都是新话):

1. 翻绿那 19 格里有 5 格是**级联**而不是独立结论:「点主体那枚落到主体页」「主体页逐位小标仍在」
   「回到分集列表且搜索词清掉」「点 ep3 进本集工作区」「本集工作区顶栏那道去重闸在」——
   这几句话点的对象(主体库页头那道闸、逐位小标、分集工作区顶栏那道闸)在起点上本来就有,
   起点红只是因为**探针要点的那枚角标不存在、点击当场抛错够不着后面那一屏**。
2. 「主体收拾干净后 tab 行那一枚跟着消失」在起点上是**恒真句**(压根没有那一枚),
   它只在合完那一侧承载结论。

---

## 三、live 数字(逐格现跑,两侧自称一律不抄)

W292 自称 `unit` 709、W293 自称 711;**两个自称都没盲抄**——709 是起点现跑数、711 是 live。

| 格 | 起点 `501fe37` | 合完 `aff1c7d`(live) | 备注 |
|---|---|---|---|
| `unit` 整跑 | 709/709 | **711/711** | 24 套件逐格求和 `= 711` 自洽 |
| `unit commands` | 69/69 | **71/71** | 本槽新判据两条都落这里 |
| `unit contract` | 153/153 | **153/153** | 未动 |
| `unit domain` | 46/46 | **46/46** | 未动(`dupIdScan`/`dupIdMarks` 本体一格没改) |
| `unit skills` | 104/104 | **104/104** | 未动 |
| `unit issues` | 22/22 | **22/22** | 未动 |
| `unit sb-views` | 11/11 | **11/11** | 未动 |
| `integration` | 152/152 | **152/152** | 未动,与对侧自称一致 |
| MCP `tools/list` | — | **40** | 现取,本槽没动 |
| CLI `Object.keys(CMD)` | — | **50**(bracket 26 + dotted 24) | 与 MCP 那个数分列现取,不混成一个 |
| `e2e` | — | 未跑 | 明令不跑 |

**名集多重集 `+2 −0`、`709 + 2 = 711` 自洽**(两棵干净整树各取一遍用例名双向比对),
进来的正是「项目详情面认得出存量重复 id…」与「项目详情面那两枚角标不许另抄一份规则…」两条,一条没出。

### 三之二、`cli.smoke` 单独现跑(对侧自称未跑,明令本槽必须自己跑)

单独整跑,`env -u HUJING_SERVER -u HUJING_TOKEN` 加 `MV_DATA_DIR`/`MV_UPLOADS_DIR`/`MV_CONFIG`
隔离到临时目录:**115/117**。两条失败逐条与 `master` `9adcf0ff964891dc17c352f6ae06db6ee7a9383b`
在同一 env 下现取的 **51/53** 同名同表现:

- `FAIL | 未登录 whoami → exit 3 | exit=1`
- `FAIL | llm --json mock 链路 | undefined`

两条在本支与 `master` 上逐字相同,按明令允许。本槽 `cli.js` 相对起点**逐字节未动**。

---

## 四、`TOPIC_FLOOR`、花名册与棘轮(对齐到 live)

对侧自称 26,现取核过**确是 26**,明令的「禁止仍写 25」由 CF-floor 现跑接着(退回 25 当场红 1)。

| 位 | live |
|---|---|
| `GUARD_TOPICS` 在册 | **26** 条(`dedupe-proj-badge-derive` 已在) |
| `GUARD_TOPICS_CLOSED` | **0** 条(空表是常态) |
| `TOPIC_FLOOR` | **26** |
| `w178-topic-floor-unlist.md` 花名册 | **26** 行 |
| 双向差集 | 在册 − 花名册 = 空;花名册 − 在册 = 空 |

棘轮五格与各自实况:

| 格 | `FLOOR` 字面 | live | 差额 |
|---|---|---|---|
| 单元测试 | 711 | 711 | 0 |
| 集成测试 | 152 | 152 | 0 |
| `cli.smoke` | 117 | 117 | 0 |
| 记账件份数 | **308 → 309**(本槽抬) | 309 | 0 |
| 护栏主题 | 26 | 26 | 0 |

记账件那一格本槽新添一份(本文),故 `FLOOR`、`docs/skills-wave/README.md` 那句声明份数、
索引表行数三处同轮校齐到 **309**。五格差额 **0/0/0/0/0**,`SLACK` **3** 一格没用掉。

`gaps()` **20** 键一个没剥。

---

## 五、核过的四格(明令逐条)

1. **tab 行两枚 + 分集卡片角标真在。** live 探针在真页面上逐 tab 取到两枚(8 个 tab 各一格),
   分集列表上取到 `ep1:2 行` / `ep3:1 行` 而 `ep2` 一枚不挂;源级另核 `= dupIdBadges(p)`
   全页恰 1 处、两枚角标插在 `main.innerHTML` 模板里 tab 行那一段(在各 tab 内容体分派之前)。
2. **`dupIdScan` 四处同读,一处没加。** 定义仍在 `js/domain.js:399` 一处;调用现取恰四处——
   `cli.js:727`(主体侧命令)、`cli.js:814`(镜头侧命令)、`js/roles.js:284`(主体库页面入口)、
   `js/storyboard.js:36`(分镜面入口)。`js/episodes.js` 里 `dupIdScan` 只出现在注释里、
   **可执行行 0 处**,`dupIdMarks`/`nth:`/`.plan.filter` 各 0 处(这一屏渲不出主体位与分镜行,
   没有可挂位次的对象);`Domain.dupIdMarks` 定义仍 `js/domain.js:427` 一处、消费仍恰 2 处
   (`js/roles.js:376` + `js/storyboard.js:149`)。
3. **两侧扫描整页各只调一次。** `RoleOps.dedupeScan(` 与 `SB.dedupeShotScan(` 在 `js/episodes.js`
   可执行行各恰 1 处(逐集循环在那一处之内),`renderEpisodes` 那一段自己一次都不扫,
   逐集行数由 `render` 传进来的那一份现取。
4. **产品去重语义未改。** `js/domain.js` / `js/roles.js` / `js/storyboard.js` 相对起点 diff **全空**,
   `cli.js` / `mcp.js` / `server.js` / `index.html` / `js/sb-views.js` / `js/shotgroups.js` 同样全空
   ——首位/首行留原 id、撞车位/行改发新 id、一位不删一行不删、预览 + 确认闸、纯改表零计费,
   这一整套一个字没动;本槽加的全是读它们的展示层。

---

## 六、残留(按明令一条没代修,原话逐条保留)

W293 记账件 §九六条,原话:

1. **角标只到页 / 到集,没有逐行逐位落点。** 点进「主体」页与分集工作区之后,是哪几位/哪几行
   仍靠那两页自己的卡片小标去认;`board`(分镜脚本)与 `bb`(节拍板)两档照旧一枚小标都挂不上
   (W291 残留第 1 条原样在),"跳到第 1 行"那种定位也仍没有(W289 残留第 4 条、W291 残留第 3 条)。
   明令这两件下一槽收,本槽点名不做。
2. **项目管理列表(`Views.projects` 那一屏)上仍没有角标。** 本槽收的是项目详情面;
   项目卡片那一屏还在更外一层,一个项目脏没脏得点进来才看得见。要收得先量它那张卡上还有没有位置,
   以及"全量项目逐个扫两棵树"这一下的开销值不值(项目详情面只扫当前这一个项目)。
3. **`cli.js` 的 `shots-dedupe` 注释里点名 `Domain.reviseTargets` 的那半句仍不准**
   (W287 残留第 2 条、W289 残留第 2 条、W291 残留第 2 条原样在)。明令不碰,继续记账点名。
4. **展示文案现在有三份。** 主体侧 `dupSeatTag`、镜头侧 `dupRowTag`(W291 残留第 5 条)之外,
   本槽又添了两句(tab 行那两枚角标的 `title`)。四句逐句同构、只差单位词、引用面那一段与
   "收拾走哪个入口";要合并成一份模板得先把这三处参数化。
5. **角标不随 Bus 事件实时刷新。** 项目页那三枚角标(问题/计划/交付)挂了 `'*'` 通配订阅按事件重画,
   本槽这几枚没接进去——它们只在整页重渲那一下现算。当下够用(重复 id 只由那两页的确认闸与
   整树导入改动,两条路都会重渲),但同屏另开一处写 id 的路时这几枚会陈旧一会儿。
6. **入口照旧不设闸。** 整树导入/恢复与逃生舱 `state-put` 仍能写进重复 id(明令不加闸),
   这几枚角标与那两页的入口一样,收的都是**存量**这一头。

六条现取核过仍原样成立:`js/sb-board.js` 与 `js/beatboard.js` 里 `dupRowTag`/`dupIdMarks`/`id 重复`
各 0 处;`js/projects.js` 里 `dedupeScan`/`dupIdBadges`/`id 重复` 各 0 处;`cli.js:825` 那半句注释仍在;
`dupSeatTag`(`js/roles.js:297`)与 `dupRowTag`(`js/sb-views.js:61`)加 tab 行那两句 `title` 共四句;
`js/episodes.js` 那个 `'*'` 通配订阅的回调只重画 `pissues`/`pplan`/`prelease` 三枚。

本槽另新记两格:

7. **树面那两句第十七次仍是恒真句。** `git diff M P2` 空与 `tree(M) == tree(P2)` 在任何一次
   快进形合入上都成立,承载不了"我方没丢东西",这一格靠变异与 live 两面补。
8. **live 探针那 19 格翻绿里有 5 格是级联、1 格在起点是恒真句**(§2.2 已逐条登记)。
   这几格不是"判据接住了新东西",而是"探针的走位穿过了新东西";要让它们独立说话,
   得给主体页与分集工作区各另开一条不经角标的进场路。本槽如实登记为没做的量。

---

## 七、一句话

W293 那一槽(项目详情面两枚同 id 重复角标 + 分集卡片本集角标,同读两侧现成那份扫描)
按快进形并进集成线第十七条,`--no-ff`、零冲突;整树 diff 恒空这一格不承载结论,
取证压在十四手变异(起点锚点全失配、合完逐手红 1–4、漏网 0)与 live 探针
(同一份夹具起点 9/28、合完 28/28)两面;live 数字 `unit` **711**、`commands` **71**、
`integration` **152**、`cli.smoke` **115/117**(两条失败与 `master` 同名同表现),
`TOPIC_FLOOR` 与花名册双向对齐到 **26**,棘轮五格差额全 0、`SLACK` 3 一格没用掉;
六条残留一条没代修、原话逐条保留,另新记两格。
