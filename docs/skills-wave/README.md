# docs/skills-wave · 主线 skill 层方案索引

主线口径:**剧本 → 主体 → 分集 → 分镜 → 生成 → 审片 → 成片**。
本目录放方案、判定标准、落地记账与核验件;实现落在 `js/` 与 `server.js`/`cli.js`/`mcp.js`。

## 索引

**索引表共 184 份记账件**(每份 `wNN-*.md` 各占一行,新增记账件必须同轮补行;这个份数由 `node tests/unit.js contract` 与目录实况、索引表行数三方对齐,份数只增不减)。

| 文档 | 内容 | 什么时候看 |
|---|---|---|
| [w1-pipeline-skill-map.md](./w1-pipeline-skill-map.md) | 现有 skill/专家/智能体资产清单(五层)、主线七步对照表、15 项缺口(G-01…G-15)、建议纳入与建议丢弃的 skill 概念、UMD/注册表单一来源接入方案 | 想知道"现在有什么、缺什么" |
| [w1-architecture-spec.md](./w1-architecture-spec.md) | 分层与扩展点、W2/W3/W4 可执行拆分(范围/接口契约/验收/风险)、全波通用验收标准、禁止项 | 想知道"接下来怎么改、改完怎么验" |
| [w1-inventory.md](./w1-inventory.md) | 仓库真实盘点:文件级清单、UI–CLI–server–MCP 四端加载矩阵、持久化键位 | 想查某个资产在哪一端加载、落哪个键 |
| [w1-relevance-rubric.md](./w1-relevance-rubric.md) | 相关性判定标准:R1–R4 正向维度 + C1 成本、否决项 V1–V11、三档判定与降级去向 | 判断某个能力该不该做 |
| [w1-selected-skills.md](./w1-selected-skills.md) | 入选短名单 30 条内部能力 SK-01…SK-30、波次配比、键位覆盖、新登记缺口 S-01…S-07 | 查某条能力的编号、波次与落点 |
| [w1-feishu-doc-a-extract.md](./w1-feishu-doc-a-extract.md) | 外部资料 A 的结构化提取(成片产线清单),含抓取阻塞与计数出入的如实记录 | 追溯短名单的素材来源 |
| [w1-feishu-doc-b-extract.md](./w1-feishu-doc-b-extract.md) | 外部资料 B 的结构化提取(视频能力集合 53 条,12 层分类)与许可约束 | 同上 |
| [w1-feishu-raw-notes.md](./w1-feishu-raw-notes.md) | 抓取记录与交叉核对:七条抓取路径的实测过程,供复现 | 需要重跑抓取或核对原文时 |
| [w2-kb-sections-wiring.md](./w2-kb-sections-wiring.md) | `KB.SECTIONS` 升为按键取用面、5 个消费点改按键取用、压缩块由 `DIGESTS` 同键拼装 | 改知识库条目或注入点前 |
| [w2-skills-align-30.md](./w2-skills-align-30.md) | `js/skills.js` 索引对齐短名单 30 条的落地口径与记账(pending 纪律) | 往注册表加条目前 |
| [w3-g01-expert-persona.md](./w3-g01-expert-persona.md) | G-01 雇佣专家 persona 进 `/api/wf/*`:板块雇佣 > 全局雇佣,双端唯一装配口 | 改人设注入链路前 |
| [w3-g02-memory.md](./w3-g02-memory.md) | G-02 长期记忆双端复核件(零代码槽):覆盖度取证与 5 项残留登记 | 改记忆召回/注入前 |
| [w3-g03-review-step.md](./w3-g03-review-step.md) | G-03 审片升为 `Domain.workflow` 主线一等步骤及四个消费面接通 | 改主线步骤集合前 |
| [w3-g04-headless-front.md](./w3-g04-headless-front.md) | G-04 剧本拆集下沉 `wf-core` + `/api/wf/split-episodes` + 领域命令/CLI/MCP 入口 | 改拆集或 headless 起跑链路前 |
| [w4-g05-tpl-video.md](./w4-g05-tpl-video.md) | G-05 `settings.tplVideo` 定性与接入(改在提示词成型阶段,不动生成指纹) | 改模板三件套前 |
| [w4-shot-size-glossary.md](./w4-shot-size-glossary.md) | G-07 机位词表归一:景别阶梯/运镜/角度结构表下沉 `wf-core.js`、`sizeGap` 级差、15 个消费点逐点对照、指纹影响面 | 改景别/运镜取值或其消费点前 |
| [w4-subject-ref-check.md](./w4-subject-ref-check.md) | SK-12 分镜↔主体引用完备性校验(`Skills.CHECKS` 首条实现,只报不拦) | 加校验项前 |
| [w4-sk13-consistency.md](./w4-sk13-consistency.md) | SK-13 跨镜头主体一致性校验,与 SK-12 成对闭合 S-03 | 同上 |
| [w4-film-caption-check.md](./w4-film-caption-check.md) | SK-28 成片字幕时间轴与阅读速度校验,切段口径下沉 `Domain.subtitleSegs` 与合成/SRT 同源 | 改字幕切段、烧录截断线或成片面校验前 |
| [w4-audio-meta.md](./w4-audio-meta.md) | 配音渲染清单单源:音色配置/配音文本/渲染凭据口径下沉 `Domain`,成片路径按清单取音轨 | 改配音生成或成片音轨取值前 |
| [w5-cycle1-audit.md](./w5-cycle1-audit.md) | 周期 1 逐项目独立核验报告:成熟度分档、分叉风险实测、合入次序建议 | 想知道每项做到哪一步、分叉在哪 |
| [w6-integration-log.md](./w6-integration-log.md) | 周期 1 成果收敛到集成分支的记录:冲突解法、合并后测试数字、剩余分叉 | 想知道主干现在是什么状态 |
| [w6-extract-subjects-wf.md](./w6-extract-subjects-wf.md) | 提取主体接入 `/api/wf/extract-subjects`(前段命令吃到人设与协作记忆,提示词拼装/结果规整下沉服务端) | 改提取主体链路前 |
| [w7-integration-log.md](./w7-integration-log.md) | 周期 2 第 7 波收敛记录:4 条分支的合入次序与冲突解法、记忆套件补测、剩余分叉 | 想知道第 7 波收敛成了什么样 |
| [w8-split-episodes-inject.md](./w8-split-episodes-inject.md) | 剧本拆集端点补齐人设/记忆注入(`WF_BOARD` 加「剧本」板块、`buildSplitUser` 注入位、两端同装配口) | 改拆集提示词或新增 `/api/wf/*` LLM 步前 |
| [w8-script-check.md](./w8-script-check.md) | SK-07/08/09 剧本段三条校验项,闭合 S-01 的剧本半 | 加校验项前;或想知道剧本正文怎么被判定 |
| [w9-eps-structure-check.md](./w9-eps-structure-check.md) | SK-14/SK-15 分集段两条校验项,与 W8 成对闭合 S-01(六阶段覆盖 + 付费卡点位置) | 同上;或想知道分集表与集序怎么被判定 |
| [w9-integration-log.md](./w9-integration-log.md) | 剩余分支收敛到集成分支的记录:包含性实测、并集型冲突解法、合并后测试数字、剩余未合 | 想知道主干现在是什么状态 |
| [w10-cycle2-audit.md](./w10-cycle2-audit.md) | 周期 2 集成与 skill 落地独立核验报告:分支逐条成熟度、按父提交漏合的实测佐证、「合并一律取分支 head」的方法性结论 | 想知道某条分支做到哪一步;开集成槽前 |
| [w11-preflight-film-assert.md](./w11-preflight-film-assert.md) | 就绪检查消费面并集补断言:字幕面被摘掉的两种写法实测、双端行为/结构断言分工、五种摘法转红验证 | 改 `episode.preflight` 的 `result.checks` 或新增校验面前 |
| [w12-size-gap-check.md](./w12-size-gap-check.md) | SK-18 分镜景别衔接校验(`WfCore.sizeGap` 单源落成校验项:连续同景别/两极对切/整集无递进) | 加分镜面校验项或改景别级差判据前 |
| [w13-integration-log.md](./w13-integration-log.md) | SK-14/15 分集段、SK-18 分镜段与 W11 断言收敛到集成分支的记录:五面并集冲突解法、被并集断言接住的转红点、合并后测试数字 | 想知道主干现在是什么状态 |
| [w14-review-skills-check.md](./w14-review-skills-check.md) | 审片路径接入 `Skills.check` 只读消费(报告独立字段 `checks` + 弹窗/导出展示,只报不拦)、SK-03/SK-04 的 infra 面记账诚实位对齐 | 改审片报告结构、往审片路径加校验面,或动 `pending` 记账前 |
| [w15-gen-block.md](./w15-gen-block.md) | SK-21 生成步注入面落地:提示词改写人设装配口 `WfCore.genPromptSystem`、`Skills.block('gen')` 从 0 到逐字节可对账、为什么不注生成请求构造点 | 改生成侧提示词注入或去某条目 `pending` 前 |
| [w16-integration-log.md](./w16-integration-log.md) | W12 head 补合(w13 只合到父提交的漏合)与 W14 审片消费收敛到集成分支的记录:并集型冲突解法、被登记侧反查断言接住的转红点、合并后测试数字 | 想知道主干现在是什么状态 |
| [w17-preflight-stages.md](./w17-preflight-stages.md) | 就绪检查校验面清单收成双端单源表 `Skills.preflightStages()`(冲突热点收口:新增一面只改一处),含推导规则、逐字节等价取证与变异验证 | **新增校验面前必读**;改 `episode.preflight` 的 `result.checks` 前 |
| [w18-gen-prompt-unify.md](./w18-gen-prompt-unify.md) | `gen.promptSystem` 收编两端内联:`WfCore.optimizeSystem` 与 `buildOptimizeUser` 配对、CLI 侧覆盖表显式传参、为什么修订链路不接方法论块 | 改审片修订提示词或往注册表收编内联人设前 |
| [w19-g06-inject.md](./w19-g06-inject.md) | G-06 残留两条落地:「多镜头写法」进拆镜人设 `sbSystem`、「主体参考」进主体人设 `extractSystem`、键为什么只挂一个宿主、缺省提示词变长的兼容影响 | 改拆镜/提取主体提示词,或往某一步补 KB 注入前 |
| [w20-cycle3-audit.md](./w20-cycle3-audit.md) | 周期 3(W11–W19)逐项目独立核验报告:锚定 SHA、逐支成熟度 M0–M4、独立 worktree 重跑的测试数字、阻塞与下一目标 | 想知道某条分支做到哪一步;开集成槽前 |
| [w21-integration-log.md](./w21-integration-log.md) | W15/W17/W18/W19 收敛到集成分支的记录:并集型冲突逐处解法、用例名集合比对证明没删测、包含性实测的有效期口径 | 想知道主干现在是什么状态 |
| [w22-g06-check.md](./w22-g06-check.md) | G-06 校验半落地:SK-11 主体参考纪律与 SK-13 多镜头写法的生成前置 warn、判定输入为什么必须是真实生成请求、条目五条里哪几条判得动哪几条不判、新增校验项为什么不用改消费点 | 加生成侧校验项,或想知道参考图组/提示词写法怎么被判定前 |
| [w23-readme-count-assert.md](./w23-readme-count-assert.md) | 文档数字对账契约:README 的「已落地 N 条 / N 项断言 / N 面 N 条」由代码实况反推,取数口径表、变异验证与未纳入对账的部分 | 改 `CHECKS`、加用例或改这两份 README 的数字前 |
| [w24-sk10-ai-voice.md](./w24-sk10-ai-voice.md) | SK-10 文案 AI 味的校验半落地(套话硬禁词/台词书面腔/修饰副词密度三码词法命中):三张词表的取舍口径、为什么不冒充语义审片、注入半为什么仍挂 `pending` | 加剧本面校验项、改 AI 味判据,或想知道文案怎么被词法判定 |
| [w25-integration-log.md](./w25-integration-log.md) | W22 G-06 校验半、W23 文档数字对账契约与 W24 SK-10 文案 AI 味收敛到集成分支的记录:长行文档的句级三方合并解法、合并后必须重对齐的断言逐条说明、用例名集合比对证明没删测 | 想知道主干现在是什么状态 |
| [w26-sk19-stable-lex.md](./w26-sk19-stable-lex.md) | SK-19 抽卡稳定词校验面(判定输入是 `Domain.buildVideoRequest` 装出的真实提示词):三条命中码、词表从条目正文现筛的退空行为、面表自动跟上时两端实现零改动的取证 | 加分镜面校验项,或想知道生成前置 warn 判的是哪一份提示词 |
| [w27-sk10-kb-inject.md](./w27-sk10-kb-inject.md) | SK-10 注入半落地闭合 S-02:自撰「文案AI味」条目进 `KB.SECTIONS`、校验层三张词表改现取条目正文解析(不留第二份)、注入落点为什么选剧本板块方法论清单而非拆镜人设或 `DIGESTS` | 往 KB 加条目、改 AI 味词表,或要让某条校验项的判据与注入面共用一份正文时 |
| [w28-sk22-gen-check.md](./w28-sk22-gen-check.md) | SK-22 生成凭据与确认失效校验落地(生成面从零到一):五个码各报既有机制的哪一处失效点、为什么不复述 `Domain` 已有计数、`preflightStages()` 由登记推导自动多出一面的实测(两端 preflight 实现零改动) | 加生成面校验项,或想验证「新增一面只改一处」前 |
| [w29-integration-log.md](./w29-integration-log.md) | W26 SK-19 稳定词、W27 SK-10 注入半与 W28 SK-22 生成凭据收敛到集成分支的记录:两侧同处各插一块校验实现的解法、长行文档「取一侧 + 按 word-diff 折回对侧」的合并口径、面表首次从五面变六面时两端实现零改动的实测、用例名集合比对证明没删测 | 想知道主干现在是什么状态 |
| [w30-kb-skill-cover.md](./w30-kb-skill-cover.md) | KB 条目登记面契约收紧:并集断言对索引宿主那一向为什么是盲的、`SK-01.kb` 与 `KB.SECTIONS` 双向逐条对齐 + 不重复登记 + 不得有第二个全库宿主、六条变异实测、`剧本诊断`/`场面调度` 的步条目归属为什么留后续 | 往 `KB.SECTIONS` 加条目,或改索引宿主 SK-01 的 `kb` 前 |
| [w31-sk16-playbook.md](./w31-sk16-playbook.md) | SK-16 主线前段编排补齐拆集与主体提取(playbook 2 步 → 4 步):步序为什么取 `Domain.workflow` 同源、`cmds` 为什么改由 `steps` 推出、`args` 为什么一律留空(不替调用方预授权 `overwrite`) | 改编排型条目的 `steps`/`playbook` 产出前 |
| [w32-cycle4-audit.md](./w32-cycle4-audit.md) | 周期 4(W21–W31)逐项目独立核验报告:锚定 SHA、逐支成熟度 M0–M4、独立 worktree 重跑的测试数字、阻塞与下一目标 | 想知道某条分支做到哪一步;开集成槽前 |
| [w33-next-pending-check.md](./w33-next-pending-check.md) | SK-20 镜头动态感准入校验面落地(动作幅度/运镜条数/整集镜长分布三码):判定输入为什么分两段取、与 SK-13 首尾帧插值面的归属边界、S-04 为什么不清账,附短名单里剩余 pending 面的交接清单 | 加分镜面校验项、想知道大幅动作与多运镜怎么被判定,或要接手剩下那几面 pending 时 |
| [w34-integration-log.md](./w34-integration-log.md) | W30 KB 登记面契约、W31 SK-16 前段编排、W33 SK-20 动态感准入与 W32 周期 4 核验收敛到集成分支的记录:分支自测全绿为什么不等于并入主干全绿(契约覆盖面随基线走)、`cmds` 由 `steps` 推出的零实现改动实测、面内新增项两端实现零改动的实测、长行 README 按 word-diff 折回对侧的合并口径、"每推一版就重查一次包含性"连着救回两条分支的实测 | 想知道主干现在是什么状态 |
| [w35-sk24-check.md](./w35-sk24-check.md) | SK-24 方法论维度进审片报告的校验半落地(审片面从零到一):六个码各报报告里哪一处维度没到位、为什么整份判旧的报告不产出结论、`cmds` 是条目级登记时审片路径反查断言该怎么收口、面表从六面变七面的实测 | 加审片面校验项,或想知道审片报告怎么被本地判定前 |
| [w36-infra-pending-align.md](./w36-infra-pending-align.md) | `infra` 三条(SK-03/SK-04/SK-23)的 `pending` 按实况清账:清账的判据取哪几处实况、三条各自仍欠的覆盖余量点在哪、「改 `pending` 会动 `gaps` 投影」为什么实测不成立(附退回旧记账后逐字节比对投影的用例)、`gaps` 标记为什么按关联索引口径保留 | 要动条目的 `pending`/`gaps` 记账,或想知道人设与记忆注入还差哪几步时 |
| [w37-sk29-check.md](./w37-sk29-check.md) | SK-29 交付契约门校验面落地(上游定稿契约两向三码):判定输入为什么取"看板阶段 × `Domain.workflow` 实况"两份而不另立词表、判定范围为什么窄于看板那条 toast 软闸门、S-07 清了哪一半而 G-10 为什么不清账、面内新增项两端消费实现零改动的实测、十条变异逐条转红 | 加成片面校验项、想知道定稿倒序与"定稿无产物背书"怎么被判定,或要接 G-10 发布门那一半时 |
| [w38-integration-log.md](./w38-integration-log.md) | W35 SK-24 审片方法论维度、W36 infra 面记账清账与 W37 SK-29 交付契约门收敛到集成分支的记录:被点名的分支前缀在开工时还不存在时怎么捞回(连着三次改变候选清单)、两侧对同一条断言各放宽一半各加严一半时为什么要取严格并集、三处"剩余 `pending` 快照"互相打脸时为什么把判据改钉真实约束、分支记账件里的数字为什么不随并入更新 | 想知道主干现在是什么状态 |
| [w39-review-persona-mem.md](./w39-review-persona-mem.md) | 审片侧三步(分镜评审/整集共性汇总/四维成片评审)接上人设与记忆 ctx 通道:唯一注入段装配口 `WfCore.reviewCtxNote`、三处口径各取自哪条先例、缺省逐字节不变靠哪三层钉住、各步接哪个板块与召回输入怎么定、为什么不新增 `wfPersonaNote` 调用点,附「点名断言全文查其实点不住」的实测与改法 | 改审片侧提示词、往某一步补人设/记忆注入,或要给记账件的点名断言加把手时 |
| [w40-sum-system-prompt.md](./w40-sum-system-prompt.md) | 整集共性汇总的系统人设收编两端:注册表新键 `review.sumSystem`、缺省逐字节不变靠哪两层钉住、服务端为什么必须显式传覆盖表、SK-03 的仍欠段换成剧本拆集步后点名断言怎么保持点得住 | 往注册表收编内联人设、改整集审片三步的 system 半,或要接手 SK-03 剩下那处余量时 |
| [w41-integration-log.md](./w41-integration-log.md) | W37 SK-29 判定范围自纠、W39 审片侧人设/记忆通道与 W40 共性汇总人设收编收敛到集成分支的记录:点名的分支已在基线里该怎么如实记账、复核范围为什么要含基线分支自己(基线在本槽期间前进两版)、「记账诚实位」那一句被三条分支各改一次时按段分工的解法、余量清单为什么必须跟着 `note` 实况走、四类改名逐条证明没删测 | 想知道主干现在是什么状态 |
| [w42-split-system-prompt.md](./w42-split-system-prompt.md) | 剧本拆集的系统人设收编两端:注册表新键 `split.system`、拆集步为什么不接方法论块(锚点协议只切不写)、SK-03 的仍欠段换成主体提取步后点名断言为什么改查注册表与装配口签名而不查字面(同字面在另一条链路还有一处) | 往注册表收编内联人设、改剧本拆集步的 system 半,或要接手 `EXTRACT_SYSTEM` 那处余量时 |
| [w43-sk26-memory-writeback.md](./w43-sk26-memory-writeback.md) | SK-26 回流面落地(审片/发布闭环结论按板块写回既有记忆桶):回流哪几个可判定字段、整集均分为什么有意不回流、回流键 `fb` 为什么必须原地更新、四处写入点各走哪条通道且不多一次 IO、发布留痕为什么进不了 `steps`、S-08 为什么不挂到已清 `pending` 的条目上充数 | 要动记忆写入面、往闭环收尾加回流,或接手 G-11 自进化 / S-08 决策时 |
| [w44-sk05-playbook.md](./w44-sk05-playbook.md) | SK-05 编排面落地:主线全链 playbook 由已注册命令投影(`playbook()` 从 `null` 变九步)——步序为什么取 `Domain.workflow` 同源、SK-16/SK-25/SK-30 三条与全链是分段还是并列、为什么有两条命令有意不进链、`cmds` 为什么改由 `steps` 推出、`args` 为什么一律留空,附 G-12 两个投影落点为什么仍挂账与六条变异实测 | 改编排型条目的 `steps`/`playbook` 产出前;要接 G-12 的计划步骤或 MCP 流程模板那两处时 |
| [w45-extract-system-prompt.md](./w45-extract-system-prompt.md) | 主体提取的系统人设收编两端:注册表新键 `extract.system`、这一处缺的为什么不是单源而是覆盖口、`EXTRACT_SYSTEM` 常量为什么撤掉而不留别名、为什么不能照前两槽直接两端 `Prompts.get`(方法论块会被挤出装配口)、人设句字面只剩一份怎么钉(另一条链路有同字面,断言钉的是出现次数)、SK-03 的仍欠段换成 Agent 单轮对话步 | 往注册表收编内联人设、改主体提取步的 system 半,或要接手 `buildAgentSystem` 那处余量时 |
| [w46-g12-playbook-proj.md](./w46-g12-playbook-proj.md) | G-12 一半:计划步骤(`Plans.fromWorkflow`)改由 SK-05 主线全链 playbook 投影生成——投影给命令与步序、计划层只给"待不待办"的状态取材器、取材器为什么给不出命令名、需授权或需人工挑选的状态为什么一律出导航步、九步里为什么有两步不占计划步(自省出口把"选择性不占"与"漏接"分开),另给 `mcp.js` 那个零使用的 `require` 接上第一个只读出口 `hujing_playbook`(不经 CLI、零计费、`args` 不预授权),附产出的两处实际变化与六条变异实测 | 改计划步骤推导、要给 playbook 接消费方,或要接 G-12 剩下的 MCP 流程模板那一半时 |
| [w47-integration-log.md](./w47-integration-log.md) | W42/W45 两处人设收编、W43 SK-26 结论回流与 W44 SK-05 主线全链编排收敛到集成分支的记录:折回对侧增量前为什么要先查它是不是已在非冲突区自动合进来(写重一条摘要句的实测与撤法)、两条分支各清一条 `pending` 时冲突两侧为什么都不对、自动合并的断言下限被并入抬松时怎么取严格并集、叠在别的候选分支上的那条为什么排后合、名集差集里的三条旧名逐条配对证明没删测 | 想知道主干现在是什么状态 |
| [w48-cycle5-audit.md](./w48-cycle5-audit.md) | 周期 5(W33–W47)逐项目独立核验报告:锚定 SHA、逐支成熟度 M0–M4、独立 worktree 重跑的 unit/integration/cli.smoke 数字与 README 对账(本周期 README 失真为零)、注册表口径直读实测、集成槽与功能槽的基线取点分布、16 条阻塞与下一目标次序;另点名槽号 46 跳空、`w47` 自称「整合(1/4)」只做了 1 步、`js/skills.js` 那处「仍欠」清单是唯一「机械取任一侧都会绿而内容都不完整」的合并点 | 想知道某条分支做到哪一步;开集成槽前 |
| [w49-agent-system-prompt.md](./w49-agent-system-prompt.md) | Agent 单轮对话的系统人设收编:注册表新键 `agent.system`、为什么只抽人设句而把 ops 协议留在装配口(那半是 `agentNormalize` 的解析契约,开成开关就能被改坏)、浏览器多轮三份措辞为什么不并进同一个键、这一处的「两端」为什么落在取值口而不是消费点(装配口只有服务端一个消费点)、覆盖只换人设句怎么钉成断言、SK-03 的仍欠段换成浏览器多轮三份人设 | 往注册表收编内联人设、改 Agent 单轮的 system 半,或要接手浏览器多轮那三份人设时 |
| [w50-integration-log.md](./w50-integration-log.md) | W46 G-12 计划步骤投影与 W49 Agent 单轮人设收编收敛到集成分支的记录:「每推一版重做一次包含性实测」第一次捞回的是开工时根本不存在的分支、冲突里「一侧对一侧是分叉时旧实况」为什么数字往回退的那侧一定不对、并入让**非冲突区散文**过期这一类没有断言兜底该怎么通读捞出来(两处实测)、手写的摘要句被作者原话取代时怎么撤、名集并集与主干逐条相等时为什么仍要跑比对、`master` 那 2 项冒烟失败现开工作树取证 | 想知道主干现在是什么状态 |
| [w51-browser-agent-personas.md](./w51-browser-agent-personas.md) | 浏览器多轮对话三份系统人设收编:注册表新键 `agent.panelSystem`/`agent.drawerSystem`/`agent.previsSystem`、为什么取三条独立键而不是一个带变量的键(合成即失真,拆回来还会废掉用户已写的覆盖)、分集面板那处为什么要先把 system 模板抽成装配口 `AgentCore.panelSystem`、这一处的「两端」为什么只落在取值口(三处都只有浏览器一个消费点,没有 Node 第二消费点)、缺省逐字节靠哪四层钉住(多出「覆盖不串台」那层)、8 种 ctx/模式与收编前对跑的逐字节实测 | 往注册表收编内联人设、改多轮对话的 system 半,或要给多轮接 headless 消费点时 |
| [w52-g12-mcp-mid-templates.md](./w52-g12-mcp-mid-templates.md) | G-12 另一半:主线中段(主体/分集/分镜/生成)可调用流程模板——模板为什么是「调用顺序 + 参数从哪取 + 断点」而不是第二套提示词、中段为什么必须是投影的**有序切片**(四段拼接逐字节等于整段)、`STEP_META` 登记 `null`(有意不在中段)与漏登记怎么分开、`clear` 为什么不叫 `done`、缺前置为什么进 `gaps` 并 `ready=false` 而不吞掉步骤序列、断点码怎么钉成"各端真会回的码"、工具名为什么由 MCP 注入而模板层零工具名字面(`toolOf` 现取工具表 `cmd` 字段),附与 W46 的边界与六条变异实测 | 要改中段推进的步序/参数出处/断点,给流程模板加消费方,或接 G-12 剩下的发布留痕命令化时 |
| [w53-memall-headless-seed.md](./w53-memall-headless-seed.md) | 记忆播种与板块迁移的 headless 收口:`WfCore.memSeed`/`memMigrateBoard` 两个纯函数把种子表与改名表收成一份,浏览器 `memAll` 改为委托,headless 补 `POST /api/wf/memory-seed` + CLI `memory seed|migrate` + 两个同名 MCP 工具——为什么不做成"读记忆时自动播种"(读一次写一次盘、还会背着用户改提示词字面)、板块词表为什么由调用方注入而不在 `wf-core` 里写第二份、空板/未知板名/迁移零命中为什么报错而已种过再播不报错、幂等怎么让调用方省掉写盘(浏览器逐条 `Store.save()` 收敛成一次)、与 SK-26 回流面的边界(写什么/何时写/哪个派生函数)与五条变异实测 | 改记忆种子表或板块名、要给 headless 补记忆入口,或想分清播种与回流各管哪半时 |
| [w54-issues-unreviewed-sk19.md](./w54-issues-unreviewed-sk19.md) | 问题中心补两类投影:未审分集(`no-review` / `review-stale` 两条中危,与 `Domain` 审片步同码同口径、判旧那条与发布门 G3「视为未审」对齐)与 SK-19 稳定词(`shot-stable-lexicon` 低危,warn 不抬成 G2 fail)——"已有镜头"为什么靠挂载位置保证而不是新写条件、危险级为什么选 mid(G2 由 pass 变 warn 而非 fail)、为什么不挂 `episode.smartReview`(计费动作不代按)、分镜面为什么改成一次跑完按条目分挂、两处旧断言按实况翻转的理由与干净夹具为什么要补稳定词,附五条变异实测 | 往问题中心加条目类型、改审片三态口径,或要接手 `js/issues.js` 的 UMD 化与「提醒投影表」时 |
| [w55-integration-log.md](./w55-integration-log.md) | W48 周期 5 核验件、W51 浏览器多轮三份人设与 W52 G-12 中段流程模板收敛到集成分支的记录:核验槽只提交报告不登记目录索引行(这一处有契约断言兜底,撤掉即红)、叉自 `master` 的分支为什么不能看 `git diff HEAD..<branch>` 判增量、三处 README 冲突里两侧都是各自分支上的旧实况(正确值只能由合入后 live 实测给出,380+1+8=389)、同一份文件同一主题为什么一处自动合并一处冲突、自动合并的 `js/skills.js`/`tests/unit.js` 双向比对两侧 `note` 与套件增量、名集并集与主干逐条相等的逐条配对 | 想知道主干现在是什么状态 |
| [w56-aiscript-digest-prompt.md](./w56-aiscript-digest-prompt.md) | 剧本摘要人物小传步的人设改经注册表:同字面的最后一处内联为什么复用 `extract.system` 而不新开键(字面同 / 角色同 / 产物落点同三条判据,同 `def` 开两个键实测转红 4 条)、为什么取 `Prompts.get` 而不是带方法论段的装配口(那段讲的全是生图与参考图组纪律)、这一处的「另一端」为什么是"不许长出第二端"(摘要链路无服务端对端)、W45 留的「恰好 1 处」计数路障怎么反转成零内联 + 注册表命中、`aiScriptDigest` 第一条行为面断言怎么搭(沙箱真跑、逐步截获 system/user) | 往注册表收编同字面内联、改剧本摘要四步的 system 半,或要接手摘要链路的服务端出口时 |
| [w57-integration-log.md](./w57-integration-log.md) | W54 问题中心两类投影与 W53 记忆播种/板块迁移收敛到集成分支的记录:同一张 `facts` 断言表的三行为什么必须逐行取侧(两侧都是完整三行、`--ours`/`--theirs` 跑起来都绿而内容都不完整,判据只能是合入后 `js/skills.js` 三条 `note` 的仍欠段)、冲突里四个数字为什么两侧都不能要(`cli.smoke` 两侧同为 72 却都错——各自加 8 条,合到一起才是 80,同一数字两侧相等 ≠ 合并后仍正确)、并入让非冲突区散文过期的三处怎么通读捞出(接入指南 34→36 工具与两处「仍欠」清单)、W53 那部分落点为什么另代记一份(**该槽合 `w53` 取了中间点 `09887be`,漏掉 tip `0045962` 上的 143 行记账件与索引行,「W53 没有留下自己的记账件」这个判断已随 W67 就地更正**)、名集三份 tip 逐条包含的取证 | 想知道主干现在是什么状态 |
| [w58-sk25-publish-cmd.md](./w58-sk25-publish-cmd.md) | G-12 第三个落点:发布留痕做成领域命令 `project.release`——判定与写回收进 `js/release-core.js` 双端 UMD 单源(门禁结论由调用方注入、环境差异经参数注入)、headless 七项核心门与浏览器十项门的差集为什么是有意的、空项目为什么补在 `precheck` 而不是加成第八道门(加门就是抬门)、浏览器按钮为什么必须改走命令表(绕过即红)、服务端为什么不采信客户端门禁结论、`force` 是授权位而不是万能位、这个动作为什么本来就不计费、发布留痕为什么有意不串进主线全链投影,附顺带修掉的 `exec` 参数合流 bug(MCP 只传 `--args` 时 `pid` 被抹掉)与五条变异实测 | 改发布留痕/发布门的判定与写回、给发布接新出口,或想知道 SK-25 还欠什么时 |
| [w59-issues-umd.md](./w59-issues-umd.md) | 问题中心投影核收成与 `js/domain.js` 同形的双端 UMD:环境句柄(`Media`)与前端状态桶为什么必须经参数注入才能让同一份推导跑到 Node、`Skills` 为什么传解析器而 `Domain` 直接绑(两个测试沙箱有意不加载 skills)、`collect(p)` 变 `collect(p, ctx)` 为什么不算破坏、UI 段整段移出后 `window.Issues` 全局名与成员怎么保持不变;同轮把 W17 记的**提醒投影表**抽出来(面 → 校验项 id → kind/sev/挂载级别 + 早退分支前后 `phase` 与明细写法),`phase`/`cap: null`/整面结论按面缓存/`ctx` 统一传这四处为什么是"收表不改结论"的全部难点,六处逐面消费点断言按 W17 同一手法从"源码里出现过面字面量"改钉表(判据只增不减,第一版"只此一处"漏拦的那条变异当场收紧),新出口 CLI `issues` 与 MCP `hujing_issues` 为什么只列不处置,附七条变异实测与四条残留 | 要给问题中心加/改一条方法论提醒、给它接第二个 headless 消费点,或想照这个手法把别的浏览器侧推导收成双端时 |
| [w60-integration-log.md](./w60-integration-log.md) | W56 剧本摘要人物小传步人设收编收敛到集成分支的记录:折回对侧散文时「内容对不等于字节对」(两处半角 `""` 被抄成全角 `“”`,无断言兜底、通读也看不出,按首尾锚点切段逐字节比对才捞出)、`facts` 表这一槽为什么没有取侧动作而这个结论仍要实测出来(先查对侧根本没碰 `js/skills.js`,再把三行锚点与合入后 `note` 的仍欠段逐行对一遍)、叉得早的分支为什么三个数字都比基线低却不是删测、折回的散文里每个数字都要现取复核(含 278 字那个)、被点名排除的分支怎么如实记账而不做完成度判定 | 想知道主干现在是什么状态;要折回对侧长行散文,或要接 W59 那条被排除的分支时 |
| [w61-sk26-front-writeback.md](./w61-sk26-front-writeback.md) | SK-26 回流面补齐主线前段四步(理解/分镜/拆集/提取主体各按自己那一步的板块回流既有记忆桶):四步各回流哪几个数与为什么不回流集标题/主体名/模型评分(回流内容会被下一轮照抄)、`scope` 取 `WF_BOARD` 为什么让"回流下去下一轮召回得到"成为结构保证而非约定、理解为什么是三处写入点(分镜端点内部那次理解步)、提取主体为什么只能挂命令层(端点只出候选不写 state)、`withProject` 的可选 `memFeed` 为什么必须写在里面(409 重试要按最新 state 重建 meta 桶,写外面会覆盖并发写入)、四支各自的"没有结论就不写"判据、SK-04 剩三处余量为什么是判断而不是漏做,附五条变异实测与「源级位置断言整文件搜 `wfSave` 会捞到别的端点」那处补强 | 要往闭环收尾加回流、改 `memFeedback` 的派生字段,或接手 G-11 蒸馏按板块过滤时 |
| [w62-integration-log.md](./w62-integration-log.md) | W59 问题中心投影核 UMD 双端化与提醒投影表收敛到集成分支的记录:对侧叉在 W54 并入之前又整块重写同一文件时,哪些冲突块能取侧只能靠「逐条清点对侧分叉后主干在这一块落了什么」查出来(W54 三件事两件落在自动合并区、一件落在冲突块,机械取 `theirs` 会整条消失)、往对侧新结构里补回那一面为什么只能按表补一行而不能把手写取值点粘回去(表外第二处取值点当场撞断言)、收表前后同夹具逐字节对跑怎么证明"搬完结论不变"、对侧**纯新增文件**(`git status` 记 `A`、零冲突标记、散文无断言兜底)为什么同样会带回分叉时的旧实况、`cli.smoke` 名集比对为什么必须先切掉运行期载荷(照搬 W60 手法会得 7 条假差集)、上一槽记下的 tip 为什么不能当本槽输入(W60 记 `ad135f8`,实际 tip 已推到 `97e6784`)、并入让非冲突区过期的五处(三处「六条」计数 + 工具数 36→37 + 两处先于本槽存在的分级枚举不实) | 想知道主干现在是什么状态;要合一条与主干在同一文件上分头重写的分支,或要接 W58 那条被排除的发布留痕命令化时 |
| [w63-cycle6-audit.md](./w63-cycle6-audit.md) | 周期 6(W49–W61)逐项目独立核验报告:锚定 SHA、逐支成熟度 M0–M4、独立 worktree 重跑的 unit/integration/cli.smoke 数字与 README 对账、集成线 W50→W55→W57→W60 逐次取点复查与旁支 W58/W59/W61 现状、16 条阻塞与下一目标次序;另点名 W57 合 W53 取了中间点因而漏掉一份 143 行记账件与它那行索引(且集成线三处散文写成「W53 没有留下自己的记账件」)、目录索引契约只锁一致性不锁完备性(文件与索引行一起缺仍全绿)、`facts` 表只防假清账不防假欠账 | 想知道某条分支做到哪一步;开集成槽前 |
| [w64-integration-log.md](./w64-integration-log.md) | W58 发布留痕命令化 `project.release` 收敛到集成分支的记录:「两侧在同一插入点各新增一整块」这类冲突为什么不能二选一(两个「测试 23」块,机械取侧就整块丢 13 条或 12 条断言而剩下那侧全绿,解法是两块都留 + 顺号)、"合了一条分支计数就该 +1"为什么会写错数字(对侧只把 `hujing_release` 改走 `exec` 同链路,MCP 工具数从头到尾是 37;同一槽里领域命令是真的 11→12,两个数字一动一不动只能逐个 live 现取)、对侧在一整段 10742 字里只改的那**一个字符**怎么用首尾锚点 char-diff 找出来(它落在冲突块对侧、且 `contract` 的 README 数字对账只管 unit 用例数)、把合并基也跑一遍怎么用"对侧自己加了几条 == HEAD 相对基线多了几条"证明没有断言被冲突解法吃掉、`facts` 表这一次为什么三行都不动(对侧改的是 SK-26「已回流那两个闭环走哪条通道」,不是 SK-04「回流面还差哪几步」,两句话里都出现「发布留痕」和「回流」)、对侧新模块漏进架构树这类零断言兜底的漏登记怎么通读捞出、`integration` 名集 117 与用例数 118 差 1 为什么不是丢测(对侧块里的 `项目不存在 404` 与主干拆集块同名,对侧 tip 上就有) | 想知道主干现在是什么状态;要合一条与主干在同一插入点各写一块新代码的分支,或要接 W61 那条被排除的主线前段结论回流时 |
| [w65-g11-mem-scope-filter.md](./w65-g11-mem-scope-filter.md) | G-11 三面里的一面:专家自进化的蒸馏输入按该专家的生效板块硬过滤——召回串错板块只坏这一轮上下文而蒸馏是**写死进 `persona`**(注入到该专家每条链路且无撤回口),故两侧为什么不能共用一个取样口、生效板块为什么取"雇佣事实"(`板块雇佣 > 全局雇佣`,与 `personaFor` 同一套)而不新造 `role → 板块` 映射、无 `scope` 的条目为什么一条也不收、两道闸为什么都落在扣费之前(计费口径一字未动)、"回空数组是跳过的信号而不是全部板块的简写"怎么钉、既有两条用例为什么只改夹具不改结论、W43 留的那条源级路障怎么翻转并收严成"`agentMemory` 在 `experts.js` 里恰好 1 次",附三条变异实测(第三条只坏 toast 文案而不漏全桶)与 G-11 仍欠两面的如实记账 | 要动自进化的记忆源或触发时机、要给预置专家开进化入口,或想照这个手法把别的"写死型"消费点与召回侧分开时 |
| [w66-sk11-extract-note.md](./w66-sk11-extract-note.md) | SK-10/SK-11 的「人设句入注册表待 G-13」旧账按实况改写:那句人设三槽前就已进注册表(`extract.system`),这处旧账为什么能活到第六个周期(全仓唯一提到它的是一句**断言语**而不是被断言的值,`note` 正文零断言覆盖,先只改 note 照样全绿)、SK-10 那句为什么是双重失真(本条根本没有专属人设句,注入走板块方法论通道)、「仍欠」段的新锚点怎么定(只写落在本条目自己登记键上的余量,且每处先核源码还在不在,再各配一条收编即红的源级断言)、`settings.tplImage` 为什么不算欠(用户在全局默认值里改得到,写成缺口是把已能做的事记成缺口)、`gaps` 为什么一个不摘(G-13 治的是全仓 21 处内联人设,按关联索引口径落地一面不摘标记,故 `gaps()` 键与值逐字节不变)、新用例的双向性(旧账写回来红、锚点被收编也红)与五条变异实测 | 要改 skill 条目的 `note`/`gaps` 记账、要给某条缺口的"仍欠"段换锚点,或想知道某条记账是否真有断言兜住时 |
| [w67-integration-log.md](./w67-integration-log.md) | W53 真 tip 补合与 W63 周期 6 核验件收敛到集成分支的记录:上一条集成线只合到 `w53` 的中间点因而连 143 行记账件带索引行一起漏掉,怎么用 `merge-base`/`rev-list` 把这处损失定位成"1 条 outstanding"并补成祖先、同一份 README 上两处冲突为什么判据不同(索引表取并集、摘要段取我方只把指针改对,整份取任一侧都会绿而内容不完整)、纯新增文件零冲突为什么照样要接一手(索引行不补当场红)、与实况相反的那句判断怎么就地更正而不删掉历史记录(更正追加在原句旁,读者能看到当时怎么想的与实况是什么两层)、目录索引契约从"两个集合相等"收严到"每份各有自己那行 + 相对链接不许悬空"的三条变异对照,以及这条加固覆盖不到的情况 | 想知道主干现在是什么状态;要补合一条只合到中间点的分支,或要动目录索引契约时 |
| [w68-integration-log.md](./w68-integration-log.md) | W61 主线前段四步闭环结论回流收敛到集成分支的记录:任务给的两条基线规则怎么按"前缀零匹配"落到第二条、**两侧各把同一句话改了半句**为什么必须合成一句而不能取侧(SK-26 那条 `note` 里「写入点在哪几处」与「`steps` 登记几步」,一侧写"发布已命令化"一侧写"补上前段四步",取任一侧都把另一半功能写成没做;而 `steps` 数组自己干净自动合并了,散文不手改就是数组说六步散文说两步)、**三处"两侧各自成立、合到一起才失效"的源级断言**怎么只能靠合完真跑捞出(整文件搜 `memWrite` 的那条被对侧新回流点误触发、服务端写入点 5→6、切片锚点被中间插入的 `EXEC` 撑破),判据为什么只能收窄不能放宽、W63 预判的"假欠账无声"这一次为什么**不成立**(对侧自己改了 `facts` 表锚点)而同一条 `note` 里紧挨着的另一句仍是零断言(删 406 字全绿)、四个数字两侧全错时怎么逐个 live 现取(含一个先于本槽就已漂移的 `memory` 套件条数),附七条变异实测与"合并基一起跑 + 名集双向 `comm`"的零吃测取证 | 想知道主干现在是什么状态;要合一条与主干改在同一句散文上的分支,或要接 W63 核验件与 W65 那条 `G-11` 板块过滤时 |
| [w69-persona-tplimage-prompt.md](./w69-persona-tplimage-prompt.md) | G-13 欠段收一处:`settings.tplImage` 取用点(`js/persona.js` 八维度重写文生图提示词那步)的人设进注册表新键 `persona.promptSystem`——与只差一个字的 `gen.promptSystem` 为什么不能合成一个键(产物形态/参考图口径/下游消费点都不同)、键名前缀为什么取 `persona` 而不是 `subject`、为什么只收人设句(参考模板归偏好设置面、返回 JSON 归解析契约)、`settings.tplImage` 本身为什么不算缺口(用户一直改得到,记成缺口就是把已能做的事写成欠账)、纯浏览器链路的"另一端"为什么写成"不许长出第二端"、收编解决的是可覆盖而不是可 headless、「仍内联」tripwire 怎么反转并收严成"全仓持有者恰好只有注册表一份"、SK-11 的仍欠段为什么改指别人家的余量并配反向断言,附六条变异实测与 W66 并入时的取侧判据 | 要往注册表收编别的内联人设(尤其剧本模块那几步)、要改主体八维度重写那一步,或想知道 G-13 还欠哪 20 处时 |
| [w70-integration-log.md](./w70-integration-log.md) | W65 蒸馏输入按板块过滤与 W66 记账按实况改写收敛到集成分支的记录:两条槽同叉在 `w62` tip 而基线已走过 `w64`/`w67`,故 8 处冲突的对侧文本**全是分叉那一刻的实况**、判据只能逐处问"这一块分叉后主干又落了什么"(整行取任一侧会绿而内容不完整:一次丢掉 `release-core`/`project.release`/W65 蒸馏面三段)、对侧在**非增量区**原样带回两句已被 W67 更正过的判断(W53 没留记账件 / G-12 只剩发布留痕未接)怎么靠通读认出来、用例数为什么两侧的绝对值都不能折回而两侧的增量相加恰等于合并后 live(403 → 410/404 → 417)、`memory` 套件那个数为什么有三个候选而活的是第四个、点名要保的五处怎么在合并后逐条现查、`facts` 与 `gaps` 为什么按现况一字不动 | 想知道主干现在是什么状态;要合两条分叉点相同但落点不重叠的旁支,或要接 W61/W68 那两条被排除的分支时 |
| [w71-script-four-prompts.md](./w71-script-four-prompts.md) | 剧本板块四步内联人设收编:注册表新增四条独立键 `narration.system`/`reading.system`/`concept.system`/`light.system`——四步角色(解说编剧/导演组围读会/开拍前定调导演/摄影指导)与产物落点都不同,为什么不合成带变量的一个键也不与既有导演类人设复用、注册表顺序为什么按产品流程而不是源文件行号、四份返回 JSON 字段契约为什么留在 user 半、这四步的「两端」为什么只落在取值口(纯浏览器链路,无服务端/CLI 对端)、行为面为什么没有沙箱真跑那一层(handler 在 DOM 闭包里)而配对正则怎么补上这一面、键为什么登记在 SK-03 名下而 SK-10 的 `note` 只改记账、SK-10 那句「人设句入注册表待 G-13」两层不实各是什么、仍欠段的新锚点怎么现场核过再写、「四步仍内联」的反向判据在本槽基线上根本不存在时怎么直接立正向那一侧并按全仓持有者名单收严 | 往注册表收编内联人设、改剧本页/构思页/制片页那四步的 system 半,或要接手剧本摘要三处策划人设时 |
| [w72-integration-log.md](./w72-integration-log.md) | 周期 6 之后两条并行集成线收成一条的记录:两条二选一基线规则怎么按"`merge-base --is-ancestor` 现取"落到第一条、"基线已含就不重复合"怎么用祖先关系而不是分支名判、**同一次冲突的相邻两行为什么取了不同的侧**(判据是"哪一侧描述的是合并后的实况"而不是哪一侧更新:余量句取回流面补齐后的一侧、专家段取有蒸馏板块过滤的一侧)、一条 `note` 的三句怎么两句取一侧一句取另一侧(`steps` 数组自动合成六步而散文说两步,与"读记忆不按板块过滤"那句合并后已为假)、W67 就地更正过的那句判断为什么机械取对侧会被改回去、**整行重写长行散文时半角引号被打成全角**怎么靠逐字符 `charCodeAt` 比对捞出、两侧各加一段时相对次序怎么对着套件实测排、四个数字里两个两侧都错且错法不同怎么 live 现取,附十条变异实测(含 W67 索引加固对"连人带账一起撤"只封住散文点过的那一半、零引用的记账件仍全绿)与"合并基一起跑 + 三套件名集双向 `comm` 全空"的零吃测取证 | 想知道主干现在是什么状态;要把两条并行集成线收回一条,或要合一条与主干在同一条 `note`/同一句散文上各改一半的分支时 |
| [w73-voice-director-prompts.md](./w73-voice-director-prompts.md) | G-13 欠段里配音导演那两处内联人设的收编:两处 `system` 字面逐字节相同(按口径允许共用一键)为什么仍取两条独立键(键位是持久化面、现在拆零成本以后拆要动用户数据;批量那步要顾角色间的音色区分度而单个那步没有这个约束)、契约半为什么留在调用点(音色库取值范围与返回 JSON 约定连着 `voices.includes` 与按名回填,改坏就整条退随机)、这两条的「两端」为什么只落在取值口(headless 没有"按人设推音色"这个动作)、两步零计费怎么钉、W66 留在 SK-11 那条用例上的「仍内联」判据怎么按实况翻转成"同文件恰好 1 处"、`gaps` 为什么一个不摘(G-13 基线 24 处 → 本槽后 22 处),附九条变异实测与 G-13 逐文件余量清单 | 要往注册表收编别处的内联人设、要动音色推荐两步的提示词,或想知道"允许共用一键"时该怎么判时 |
| [w74-digest-three-prompts.md](./w74-digest-three-prompts.md) | 剧本摘要链路通读/汇总/集纲三步的策划人设收编为注册表新键 `digest.planSystem`:"一个键三个取用口还是三个键"为什么必须先逐字节核对三处 `system`(`cat -A` 比对引号内字节序列,含全角句号,缩进不同只是嵌套层级)再按"字面同/角色同/产物落点同"三条判据判、同函数里的第五步人物小传为什么不能一起收(角色与落点都不同,仍听 `extract.system`,界线由"两键不同值"正向钉住与"本文件里 `extract.system` 仍恰好 1 个取用口"反向钉住)、为什么不为它包第四个装配函数(KB 里没有可接的方法论条目,接「主体参考」会让缺省从 14 字变 292 字)、取用口计数为什么钉 3 而不是写"包含"或"不得出现"(两种写法各点不住"三处只改两处"与"三口合并成一处")、变异 2 与变异 3 为什么要分开跑 | 要收编一处同字面重复的内联人设、要判"一键多口还是拆多键",或要动剧本摘要链路那四步提示词时 |
| [w75-integration-log.md](./w75-integration-log.md) | 两槽内联人设收编(W69 主体八维度重写文生图那步 / W71 剧本板块四步)并进同一条集成线的记录:注册表两处插入点相邻却不重叠因而干净合成并集、**三处「对方仍内联」反向断言只有一处出现在冲突里**(另两处压在基线的同一条 W66 用例里、两槽的 `merge-base` 都早于它并入,`git` 全程干净合上,只能靠合完真跑捞出)、两侧在同一插入点各加一块时**块尾那一行是两侧共用的**(机械两留语法当场断,`node --check` 报 `Unexpected token`)、同一句仍欠段为什么在一个槽内两次失效(前一槽点名的正是后一槽要收的)、**同形记账两侧断言锚点宽窄不同导致一边红一边全绿**(`owed.includes('js/episodes.js')` 在真句假句里都成立)、四个数字三处口径各自 live 现取、指针那行只做定点替换并按公共前后缀复核零全角引号,附十一条变异实测与三方名集双向 `comm` 全空的零吃测取证;另记 `w70` 记账件漏在集成线外(W53 那个洞的第二次发生) | 想知道主干现在是什么状态;要合两条收编面首尾相接的分支,或要接 W73/W74 与补合 `w70` 记账件时 |
| [w76-sb-board-prompts.md](./w76-sb-board-prompts.md) | 分镜脚本创作层两处内联人设收编:**"两处该一键还是两键"怎么先 grep 原文再判**(两句 def 不同,角色/产物落点/失败行为三面都不同,故两键)、为什么不并进只差前六个字的 `sb.system`、键名怎么排(前缀取分镜域 `sb.`,中段 `board` 取所属板、尾段对上两步的产物字段 `scriptBoard.scenes`/`beat.shotsDraft`)、注册表位置为什么按 tab 次序排在智能分镜之前、返回 JSON 的 `key`/`shots` 为什么必须留在 user 半不开放(② 的回填按 `场次号.节拍号` 正则定位,改一个字整轮解析失败)、**handler 藏在闭包里的两步怎么做出行为面真跑**(绑定用的是具名选择器,给个只认那两个选择器的假 `host` 就能取回 `onclick` 直接 `await`,从上游请求体上截获 `system`)、"翻转 tripwire"在基线上找不到反向那一侧时该怎么落地(逐条 grep 取证后直接立正向,并把第二条收严成全仓持有者名单),附八条变异实测(含"越界去收 `js/episode-util.js` 三处"当场红在 W56 那条行为面上、别处抄第二份字面红在全仓名单上)与 G-13 由 20 处减为 18 处的逐文件盘点 | 要把某个模块的内联人设收进注册表、要判两处人设该合一键还是分两键,或想给"handler 挂在 DOM 闭包里"的步骤补行为面用例时 |
| [w77-event-graph-prompt.md](./w77-event-graph-prompt.md) | 分集页事件图谱拆解步的人设收进注册表独立键 `graph.system`:任务给的"仍欠哪一步"前提为什么先 grep 核实再动手(基线上 W71 未合入,五处内联里只有第 1075 行属本槽,靠计费登记名/user 半锚点/产物落点三个锚点认步)、为什么是独立键而不与剧本板块其余四步合成(五步角色互不相同——拆/改写/评/定/排,合成等于改一处改掉五个角色的定位;没有同字面就谈不上复用)、JSON 契约那半为什么不开放(`events` 字段名改一个字整集拆不出事件且逐集退费)、缺省逐字节靠哪四层钉住(注册表字面 + 取值口与 user 半锚点配对 + 全仓唯一持有者 + 不冒充双端)、SK-03 与 SK-10 各记什么(键宿主在 SK-03、SK-10 只写"这一处已收编"且写在「仍欠」之前)、W66 那三处「仍内联」断言怎么按方向翻面,附十三条变异实测(含"合成复用"与"反向撞车"两个方向,以及变异 6 为什么红在另外两条断言上) | 要把某处内联人设收进注册表、要判断两处人设该不该合成一个键,或要给某条记账的"仍欠"段翻面时 |
| [w78-beatboard-prompt.md](./w78-beatboard-prompt.md) | 节拍板 AI 拆解那步的人设句收进注册表**独立键** `beat.system`:为什么是新键而不是复用既有键(字面/角色/产物落点三条判据全不成立,改取 `und.system` 当场红 2 条)、契约半为什么"半不开放"且那半到底是哪半(user 半的 5 段段名/宫格数/衔接词表/返回结构各连着 `applyAIFill` 的一个消费点,段数写成 6 段第 6 段直接被丢)、缺省逐字节靠哪三层钉住、**取用点为什么只能"按源码原文取出那一行表达式求值"而不是沙箱真跑整条链路**(`aiFillBeats` 是私有函数、只经整屏 `innerHTML` 的按钮绑定触发,DOM 归 e2e)以及这个形态钉不住什么的诚实位、新键为什么登记在 SK-14 而不是 SK-03(节拍板本来就是 SK-14 的注入落点;那一步不注生效人设也没有服务端对端,登进 SK-03 是把不实的事记成实的);另**新立全仓内联人设持有者名单断言**——G-13 的余量第一次有机器可查的判据(文件:处数,收编一处即少一处、任何文件新长出一处即多一处,双向都实测过),四类有意的例外与"旧口径 19 处 / 名单口径 27 处不是忽然多出 8 处"的口径说明,附十一条变异实测与名单判据的已知盲区 | 要收编某处模块内联人设、要判"这句人设该新开键还是复用键"、想知道 G-13 还剩哪些文件哪几处,或要动那张全仓名单的判据时 |
| [w79-editors-prompt.md](./w79-editors-prompt.md) | 漫剧编辑器「AI 生成对白」那处内联人设收编为独立键 `comic.bubbleSystem`:一整串字面里哪一段是措辞、哪一段是**解析契约**(type 词表就是 `TYPES.includes` 的判据、JSON 形状就是两种解析分支、条数就是 `slice(0,4)`),故只收人设句、契约半提成装配口常量 `BUBBLE_CONTRACT` 让断言现取;提示词说「30 字」而代码钳 60 字这处既有落差为什么一字不动(改它是行为变更不是收编);新键为什么挂 SK-03 却要在「仍欠」段写清"该步不过本条的 ctx 通道"(不注入生效人设与协作记忆,只是人设句进了表);`gaps` 为什么一个不摘;以及**全仓人设字面持有者名单**怎么立(按「以 `你是` 开头的字面」逐文件普查,注册表 `def`、专家人设库与模块内联三类混在同一张表里如实计数,兜底缺省与占位文案也计进去——例外条款越多名单越容易被绕过;新增一处与收编一处都先红),名单判据封不住什么,附九条变异实测 | 要收编某一处内联人设、要给"人设散落在哪些文件"这件事立一张查得动的名单,或想知道某处提示词的可覆盖边界该切在哪一刀时 |
| [w80-integration-log.md](./w80-integration-log.md) | 补合 `w70` 集成线真 tip(W72 当时只取到 `1e4627c`,漏掉后来那条 W70 自己的记账件与索引行——W53 那个洞的第二次发生)并把四条内联人设收编槽(W73 配音导演两步 / W74 剧本摘要三步 / W76 分镜脚本创作层两步 / W77 事件图谱拆解步)并进同一条线的记录:注册表同插入点撞车时怎么按产品流程排并集(19 → 25 条)、**反向断言是单向的且不止一条**(基线上压着 W66/W69/W71 三条"某某步仍内联",四个槽的 `merge-base` 都早于它们的现措辞,`git` 一路干净合上,合 W74 后 2 红、合 W77 后 4 红,只能靠合完真跑捞出)、翻面为什么要连记账一起翻并给新锚点配反向断言、`} },` 块尾这次怎么用"替换 `=======` 那行"固定成三步不再踩、**同名 helper 两块都留会静默互相覆盖**(`loadPersona` 后定义的赢,红的却是别人家的断言值)怎么合成一个按上下文回罐头的 helper、`note` 措辞与对侧断言锚点不合时为什么改措辞留断言、一个数字四处口径怎么各自 live 现取,附"每个 tip 相对自己 `merge-base` 的用例增量相加恰等于合并后 live(428+0+1+2+2+2=435)"与三套件名集十八次 `comm` 全空的零吃测取证,以及 G-13 由 19 处减为 11 处的逐文件盘点 | 想知道主干现在是什么状态;要补合一条只合到中间点的分支、要一次合多条收编面相邻的槽,或要接 W78/W79 那两条被排除的分支时 |
| [w81-gsettings-prompt.md](./w81-gsettings-prompt.md) | `js/gsettings.js` 剩余内联人设收编:导演设定五维生成步的人设进注册表新键 `dirset.system`——同文件三处 `你是` 命中里为什么只有一处算内联人设(另两处是输入框 `placeholder` 与正文在 `js/experts.js` 的 `FORGE_SYS`)、为什么不与只差「影视/短剧」两字的 `und.system` 复用(产物落点是全剧五维 vs 本集理解)、键前缀为什么取 `dirset` 而不是 `director`(后者已被注入面占着)、这一步比前几槽多出的那条边界(LLM 失败走 `dirFallback` 回退模板,覆盖只作用于 LLM 那一路、回退文案逐字节不跟随)、为什么本槽能有行为面真跑那一层(`window.genDirectorSetting` 本就为精细模式挂在 window 上,不必造 DOM)、**全仓内联人设持有者名单**的第三种口径:判据怎么写成机械扫描的三种形态(`system:` 值位 / 具名人设常量 / 装配函数直接 `return`)、为什么 `js/experts-data.js` 的预置专家 `persona` 与 `placeholder` 有意不在判据内、**这个数与 W71 记的 15 为什么不矛盾**(旧 grep 只数单引号直接字面,多出的几处一直都在)、本槽落点为什么钉成"`js/gsettings.js` 整条从名单消失"而不是减到 0 还挂着,附七条变异实测(含名单数少了与数多了的两向守卫) | 往注册表收编内联人设、要改导演设定生成步,或想知道 G-13 还欠哪些文件各几处时 |
| [w82-proj-shell-prompt.md](./w82-proj-shell-prompt.md) | 剧壳发行文案包内联人设收编:注册表新增独立键 `dist.copySystem`——"点名 1 处"怎么先 grep 核实再动手、键名前缀为什么取 `dist` 而不是 `shell`/文件名、后缀为什么是 `copySystem` 而不是 `system`、一条 system 串"人设句 + 现拼 KB 尾 + user 半契约"三段怎么分(只收第一段:KB 正文单源归 `KB.SECTIONS`、六字段返回契约留 user 半不开放覆盖,两种开法各配一条变异)、注册顺序为什么排在成片审片之后而不是末尾、行为面为什么仍没有沙箱真跑那一层(handler 在 DOM 闭包里)、键为什么又登记在 SK-03 名下而 SK-10/SK-11 的 `note` 一字未动、**全仓持有者名单怎么从"每槽各写一次局部扫描"收成一条按 `Prompts.list()` 现推的用例**(新增键自动进名单、`tests/` 有意不进名单、为什么不改写 W69/W71 那两条),附十一条变异实测(含"越界去收别人家余量"先红的是别人家的记账那一条) | 往注册表收编内联人设、改剧壳页发行文案包那步的 system 半,或要接手其余内联人设时 |
| [w83-proj-upload-prompt.md](./w83-proj-upload-prompt.md) | G-13 欠段再收一处:拉片建集逐段画面理解那步的人设进注册表新键 `rip.system`——键名前缀为什么取步名 `rip` 而不是文件名、为什么不与 `split.system`/`und.system` 并键(建分集的两条入口角色不同、判定输入一个是剧本文本一个是关键帧)、注册表位置为什么紧跟 `split.system` 并把"两条入口相邻"钉成断言、为什么只收人设句(段号时段的现拼提示与 `{"shot_desc":…}` 契约留在 user 半)、行为面为什么没有沙箱真跑那一层(handler 在 `openRip` 的 DOM 闭包里)而配对正则怎么补上、`js/proj-upload.js` 的内联人设计数怎么从 1 钉成 0、全仓持有者名单为什么新立一条而不是并进既有那两条,附变异实测与全仓内联人设余量清单(15 → 14 处) | 要往注册表收编别的内联人设、要改拉片建集那一步的 system 半,或想知道 G-13 还欠哪 14 处时 |
| [w84-sb-views-prompt.md](./w84-sb-views-prompt.md) | 镜头「按指令改」内联人设收编:注册表新键 `gen.editSystem`——键名前缀为什么取 `gen` 而不是 `sb`(与同层四策略优化相邻落位)、与只隔一条的 `gen.promptSystem` 为什么不能复用(那条经装配口还要接 KB 抽卡块,合成即改变两步的缺省)、"契约半"这一刀切在哪(人设句 + 改写纪律可覆盖,返回 JSON 字段名与镜头上下文摘取不开放)、handler 挂在 DOM 闭包里时行为面怎么补上沙箱真跑那一层(两层弹窗用极简选择器桩逐层驱动到 `chatJSON`)、同形的主体「按指令改」为什么有意不收并配反向断言、逐槽各写一份的持有者名单为什么收成**按注册表现取**的一条(每条 `def` 全仓扫一遍 + 反查同 `def` 双键),附九条变异实测与 G-13 余量的现场清点 | 往注册表收编内联人设、改镜头「按指令改」那步的 system 半,或要接手 `js/role-editor.js`/`js/sb-board.js` 那几处内联人设时 |
| [w85-integration-log.md](./w85-integration-log.md) | 四条内联人设收编槽(W78 节拍板拆解 / W79 漫剧气泡对白 / W81 导演设定五维 / W82 剧壳发行文案包)并进同一条线的记录:注册表四处插入点互不重叠因而四次全自动合成并集(25 → 29 条)、**同一件事三个槽各立了一张持有者名单且判据互不相同**(只数系统人设位 / 数全部 `你是` 字面含注册表 `def` 与专家库 / 三种形态且排除注册表),三张的期望串都写在各自 `merge-base` 那一刻的实况上、四次合并里每张都要按合并后 live 重算一遍(逐次取值表见 4.4)、**"全表互不相同"这类全称断言碰上别的槽有意留的同值组**怎么收窄而不是放宽(点名唯一那一组:别处再抄一份仍红、把那组合成一键也红)、`} },` 块尾为什么三次里踩到三次而第四次两侧块尾各自完整不能按经验去补、`js/skills.js` 的 `prompts` 数组三次都是真冲突(机械两留会得到两行 `],`)、仍欠段为什么必须逐句合并而不能取侧(两侧各自带着查自己那几个锚点的断言)、**索引表那行在冲突块外自动合上导致同名索引出现两行**(索引契约报得出重复但报不出措辞过期),附"每个 tip 相对自己 `merge-base` 的用例增量相加恰等于合并后 live(435+2+1+2+3=443)"与三套件名集十五次 `comm` 全空的零吃测取证(含"抽名前要先切掉回执载荷否则假报 7 条缺失"),以及 G-13 三张名单各自的余量盘点 | 想知道主干现在是什么状态;要合多条各自新立了同类全仓名单的槽,或要统一那三张持有者名单的口径时 |
| [w86-agent-ops-prompt.md](./w86-agent-ops-prompt.md) | `js/agent-ops.js` 两处内联人设收编为两条独立键(执行回执核验修复 `agent.selfFixSystem` / 会话纪要蒸馏 `agent.compactSystem`):"这个文件到底有几处内联人设"为什么要三条 grep 交叉取证再动手(`system:` 五处里两处是注释、一处是已进表的 `prearrPrompt`)、"一键还是两键"的判据为什么在**逐字节比对那一步就结束**(两句 `def` 不同即不可能共用,角色不同只是佐证;反过来 `def` 相同也不自动合并——键位是持久化面)、"人设句到哪里为止"怎么按下游解析分支切(回执的 `✕`/`⊘` 记号对着 `receipts.filter`、修复 `ops` 白名单对着 `splitOps`/`applyOps`、`≤150字` 对着 `slice(0,300)`,故契约半一律不开放覆盖)、handler 在模块闭包里但入口挂在 `window.AgentOps` 上时行为面怎么真跑截获 `system`(含 fire-and-forget 那步等微任务)、缺省逐字节靠哪四层钉住(注册表字面 + 取值口与该步锚点配对 + 行为面覆盖只换人设句 + 全仓持有者名单)、**记账件在「仍欠」前后怎么断句**(第一版把"已收编"那句写在了「仍欠」之后,切出来的仍欠段仍带 `js/agent-ops.js` 字面而当场红),附九条变异实测(含"退回内联红 3 条——两条红在别人家翻过面的路障上")与一处先于本槽就已过期的散文怎么通读捞出 | 要把某个模块的内联人设收进注册表、要判两处人设该合一键还是分两键,或要给某条记账的「仍欠」段翻面时 |
| [w87-role-editor-prompt.md](./w87-role-editor-prompt.md) | 主体编辑页「按指令改」那步的人设收进注册表独立键 `persona.editSystem`:**处数先 grep 核实再动手**(全文件恰好 1 处,`chatJSON` 调用点也只有 1 个)、类别插值那一句为什么取"一条键 + `{kind}` 变量"而不是三条键也不是写死类别(角色同、产物落点同,写死则缺省当场变——这是人设类键里第一条带变量的)、为什么不与分镜那侧同形入口合成(设定师 vs 改图师、`s.prompt` 重出主体参考图 vs 重出镜头素材,措辞也不同字面)、契约半这一刀切在哪(`{"prompt":…}` 连着回填与那次已扣的 `llm.optimize`,改坏即整轮失败;开放的恰是用户最想调的字数与保留纪律)、**逐槽各写一份的全仓持有者名单怎么更新成按注册表现取的一条**(26 条 `def` 逐条扫,新键自动进名单;反查不能照搬"全部 def 互不相同"——音色推荐那一对本就明写同字面,故写成"同 def 的键集合恰好是那一对";带变量的 `def` 另按不变量段再扫一遍)、**handler 挂在 `openModal` 闭包里且模块无出口的步骤怎么做行为面真跑**(极简 DOM 桩逐层驱动编辑页→二级弹窗→改写→应用,顺带实测"应用后真的进了 `genMainImage`"),附十三条变异实测(含"变量被写死"只红一条的如实口子、"反向撞车"红到 5 条里 3 条是既有守卫、"越界去收 `js/sb-views.js`"当场红在两条别人家的反向断言上)与 G-13 由 11 处减为 10 处的逐文件盘点 | 要把某处内联人设收进注册表、遇到人设句里带变量不知该分键还是带变量、要给"handler 挂在弹窗闭包里"的步骤补真跑用例,或要把逐槽维护的持有者名单收成一条时 |
| [w88-experts-forge-prompt.md](./w88-experts-forge-prompt.md) | 专家工坊两步内联人设收编:注册表新增两条独立键 `forge.system`(锻造器)/`forge.evolveSystem`(进化器)——任务点名 `FORGE_SYS` 时那句"gsettings 只引用常量"为什么是**落点判据**(人设的持有者是 `experts.js`,收编不该让工坊页多知道一件它不需要知道的事,故消费侧一行未改)、同一个页面上的两处为什么仍是两条键(输入/动作/产物落点/计费标签/多轮全不同——无中生有铸新专家 vs 就地改写已有专家)、`js/experts-data.js` 预置 persona 库为什么不在收编面内(那是专家数据不是提示词,越界收它当场红 9 条)、**为什么必须导出成 getter 而不是模块级常量**(照原样写 `const X = Prompts.get(…)` 会把覆盖表冻结在页面加载那一刻,用户改完提示词切回工坊 tab 吃到的还是旧值——覆盖看着生效其实没生效,比不收编更糟)、人设句与「只返回严格 JSON:」原在同一行时为什么直接相接不补 `\n`、进化器的契约半为什么是全仓最该留在外面的那一处(别处改坏是报错+退费,这一处改坏落到"无新增条款"分支**静默白扣**)、两条键为什么各归 SK-02/SK-26 而不都挂 SK-03、`G-11` 为什么要补一句"改得到提炼口径,改不出自动触发"划清界线,附十七条变异实测(含**变异 14 第一版没咬住**:锚点字面在改动前就存在于 note 第一句,与 W39 那个洞同形,须收窄到「仍欠」段内两个词)与 G-13 计数口径漏掉常量形态的补记 | 要往注册表收编内联人设(尤其"消费侧只引用一个常量"这种形态)、要判同一页面上的两处人设该合一键还是拆两键,或要接 `G-11` 自进化与 `G-09` 专家条目 `skills[]` 时 |
| [w89-plans-prompt.md](./w89-plans-prompt.md) | 制作计划生成步(项目页「计划 → 让助手规划」)的内联人设收进注册表独立键 `plan.system`:任务给的"可能已无内联"前提为什么先按 `js/plan*.js` glob 与 `grep` 核实再动手(命中一处,故是真收编而不是只写核实)、为什么不与同一个助手名下那四条对话人设合成(产物形态不同——出的是要落库并逐步执行的步骤表,不是对话回复;并成一键等于改对话口吻时顺手改掉拆步定位)、注册表位置为什么排在对话四条之后、命令白名单与 `title`/`steps` 契约为什么不开放(白名单现取命令表、契约连着步骤钳制与失败退费,改坏即整轮拆不出步骤)、缺省逐字节靠哪六层钉住(注册表字面 + 沙箱真跑整条比对 + 覆盖只换人设句 + 取值口与 user 半配对 + 全仓唯一持有者 + 不冒充双端)、**"缺省不变"与"覆盖跟得上"两条断言为什么必须都在**(变异 2 退回内联时前者反而全绿)、键为什么挂 SK-03 而 SK-05 只补一句"另一条生成路径不切主线投影",附十四条变异实测与 G-13 由 11 处减为 10 处的逐文件盘点及"计数口径不覆盖 `{role:'system',content:…}` 形态"的交接 | 要往注册表收编别处的内联人设、要动制作计划的 LLM 规划步,或想给"可直接 await 的 LLM 步"补行为面用例时 |
| [w90-integration-log.md](./w90-integration-log.md) | 四条内联人设收编槽(W83 拉片建集 / W84 镜头按指令改 / W86 Agent 两个辅助步 / W87 主体按指令改)并进同一条线的记录:注册表四处插入点里三处干净合成并集、只有一处两侧同抢表尾(29 → 34 条,块尾那行 `},` 落在冲突块之后由两侧共用,机械两留 `node --check` 当场断)、**同一条「仍欠」段在一条线里被翻了三次面**(基线点名 `js/agent-ops.js`+`js/sb-views.js`,而这条线上 W84 收掉 `sb-views`、W86 收掉 `agent-ops`、W87 收掉接替上去的 `role-editor`),每次都要同时改 `note` 的仍欠段、把反向断言改指新锚点、给刚收掉的那处补一条翻面后的反向断言,锚点为什么一律选"这条线上不会再被收掉"的那几处(最终落在 `js/experts.js` 与 `js/plans.js`)、四个槽给的三张全仓名单期望串**没有一个在合并后成立**且判据还互不相同故一律按 live 现取重写(逐次取值表见 4.4),而 W82/W84 已收成"按 `Prompts.list()` 现推"的那两条**一次没改过**——现推形态第一次拿到实测对照、`} },` 块尾四次里踩到三次而三次的块尾各不相同(用例块尾 / 表项块尾 / 顶层 helper 的函数尾,补哪一行只能逐次看,W83 那次两侧各自完整一个字都不能补)、`js/skills.js` 四次都撞在 SK-03 的 `prompts` 数组或紧跟的 `note` 上、"全表 `def` 互不相同"这类全称断言碰上音色推荐那组有意同值怎么收窄不放宽,附"每个 tip 相对自己 `merge-base` 的用例增量相加恰等于合并后 live(443+2+3+2+3=453)"与三套件名集十五次 `comm` 全空的零吃测取证,以及 G-13 三张名单各自的余量盘点与"W88 收的正是本槽给仍欠段挑的锚点"这处有意留下的路障 | 想知道主干现在是什么状态;要合多条收编面在同一条「仍欠」段上首尾相接的槽,或要接 W88 那条被排除的分支时 |
| [w91-intent-router-prompt.md](./w91-intent-router-prompt.md) | 全局抽屉意图路由那步的内联人设收编为独立键 `agent.routeSystem`:旧 `system: '你是` 口径为什么漏计它(system 不是对象字面量里的值而是局部 `const sys` 接一串模板拼接)、同文件另两句上下文框定语为什么有意不收(它们是同一个三元的两支——块尾那句引用的是按板块表与项目实况现算的数据,收一支不收另一支等于让两条路一条可覆盖一条不可)、一条 system 串四段(人设句 / 按 `AGENT_BOARDS` 现拼的板块清单 / 判据句 / `{"board","reason"}` 返回契约)怎么只切第一刀——**板块清单为什么不能进覆盖面**(板块 key 正是下一步 `find` 的匹配键,允许用户存一份过期板块表就是让路由整轮命不中)、判据句为什么也留在装配口(要开放它就得把清单做成 `{boards}` 变量走 `Prompts.fill`,把这一条从"纯人设句 + `Prompts.get`"的形状里拆出去)、**取值口的求值时机第一次被写成断言**(在 `routeIntent` 函数体内现取 vs 提成 IIFE 顶层常量把覆盖表冻在加载那一刻,与 W88 `FORGE_SYS` 同纪律,变异 8 实测)、IIFE 私有函数怎么做出行为面(按源码原文取出那段装配式在沙箱求值)以及这个形态钉不住什么的诚实位(证明不了它真被交给了 `API.chatJSON`,由 `step:'route'` 计费槽位的配对断言补)、**三张判据不同的全仓名单为什么要按各自口径分别重算**(前两张是 8→7 文件各减一处,第三张含注册表 `def` 故总数不变、只是从模块搬进 `js/prompts.js`,照前两张的经验去减总数会把它改错),附十一条变异实测(含变异 1 为什么红 6 条、变异 9/10 的两向守卫) | 要收编某处"人设句 + 现拼数据 + 判据 + 契约"四段形状的内联人设、要判某个键的取值口该写在哪一层,或要动那三张持有者名单时 |
| [w92-proj-planner-prompt.md](./w92-proj-planner-prompt.md) | 项目实验台两步内联人设收编:AI 策划对话 → `planner.chatSystem`、剧本译制 → `trans.localizeSystem`——"`def` 相同才可共用键"这条判据怎么落到两处真实字面上、**带变量的键**怎么收(`${tgt.name}`/`${tgt.lang}` 进 `vars` 走 `Prompts.fill`,登记了占位符还要正查 `def` 里真有它)、**契约半在 system 半而不在 user 半**时怎么切(「第X集」分集标记留成取值口常量 `TRANS_CONTRACT`,并新加一层"契约与解析侧同集"——把源码那条 split 正则现取出来真切两集)、**为什么两处取值口都不能提到模块顶层**(IIFE 顶层预取会把覆盖表冻在页面加载那一刻,缺省与覆盖两条路径都还是对的,只有源级看得见)、`def` 字面名单为什么拦不住带变量那处的退化(内联版是插值、字面对不上,只有源级配对断言拦得住)、三张判据不同的全仓持有者名单本槽一张不变一张减两处一张减两处又加两处的逐张重算,附十五条变异实测 | 要收编带运行期变量的内联人设、要判"契约半该留在哪一侧",或要动那三张持有者名单时 |
| [w93-video-review-prompt.md](./w93-video-review-prompt.md) | 单镜审片提示词首句内联人设收编为独立键 `review.userSystem`:任务给的三条路径(`js/review*.js` / `js/sb-review*.js` / 其余审片模块)怎么先 grep 核实再动手(前两条一个零命中一个文件不存在,那一处其实在 `js/wf-core.js` 的装配口里)、**人设不在 `system` 位而在 user 半首句**这个形态与前几槽的差别、为什么不与同一次请求里同步发出的 `review.system` 复用(一条占 system 消息位、一条是提示词首句,合掉等于同一串发两遍)、`def` 末尾的连接逗号为什么留在键内而不留在源码侧、契约半"半不开放"到底切在哪一刀(三维字段名连着 `normalizeReport` 的取值键、`severity` 连着分档、评分标准连着发布门 G3 达标线)、**这次为什么装配口必须收 `ov` 参数**(该步真双端,Node 侧没有 `window.Store`,隐式读会让 headless 侧的覆盖静默失效——收编同时解决了"可覆盖"与"headless 侧也可覆盖")、三张判据不同的全仓名单里**为什么有一张一处不改**(顶层那张的判据本就不含 `return \`你是`,注释里已列为有意的例外,按另一张的经验去改它就是把账做错),附六条变异实测(含"服务端某一处漏传覆盖表"与"收编了却把名单写回旧值"的两向守卫) | 要收编落在 user 半而非 `system` 位的内联人设、要改单镜审片那一步的提示词,或要判某个双端装配口该不该显式收覆盖表时 |
| [w94-persona-remainder-prompt.md](./w94-persona-remainder-prompt.md) | `js/persona.js` 内联人设余量的**核实件(零代码改动)**:上游盘点说这里还欠一处,先 grep 核实的结论是三个 LLM 步(八维度重写文生图 / 单个音色推荐 / 批量音色推荐)全部经 `Prompts.get` 现取、全文零内联,那处余量已随 W69/W73 并入基线而消失;文件里剩下的四类字面(三步 user 半的返回 JSON 契约、`settings.tplImage` 缺省模板、LLM 失败后的本地回退拼接、八维度输入框 `placeholder`)逐类说明为什么都在口径之外;**为什么不做空改**(动注册表就动两份 README 的数字对账、动取值口只有"不读覆盖表"与"缺省变长"两个方向、动 `note` 就是假记账);"核实"怎么写成可复查的形态——不补第四份断言拷贝,而是逐条点出基线上已经钉住这条结论的五处断言及其方向(零内联计数 / 逐步取值口配对 / 两条音色键各自的取值口 / 单句字面的全仓持有者名单 / 整串比对的全仓名单),并给出"哪种断言该翻面、哪种不必补"的判据;附 G-13 余量三张名单在该槽分叉点上的 live 现取(数字随后续各槽逐次归零,本文只留口径不留数)与"盘点表在被收编那一刻就过期"的口径 | 拿到"某处还欠一处内联人设"这类前提要动手时(先核实再动手);想知道 G-13 现在还剩哪些文件哪几处;或要写一份零代码改动的核实件而不知道该留下什么才算交账时 |
| [w95-integration-log.md](./w95-integration-log.md) | 四条内联人设收编槽(W88 专家工坊两步 / W89 制作计划生成 / W91 意图路由 / W92 项目实验台两步)并进同一条线的记录:注册表四处插入点里两处干净合成并集、两处两侧同抢表尾(34 → 40 条),**共用块尾那行 `},` 只能给一侧补**(按"每块各补一个"机械补就多一个括号,`node --check` 当场断在 `Unexpected token`)、**同一条「仍欠」段在一条线里翻了四次面**(基线点名 `js/experts.js`+`js/plans.js`,而这条线上 W88 收掉 `experts`、W89 收掉 `plans`、W91 收掉接替上去的 `agent-global`、W92 收掉最后接替的 `proj-planner`),**最后一次没有"这条线上不会再被收掉"的窄口径锚点可挑了**——W78 那张判据下的余量四个文件全在本槽收编面里、合完归零,仍欠段因此改指判据更宽那张名单还计着的那一处(单镜视频审片 `js/wf-core.js` 把人设句写在 user 半开头),而**一张名单归零时归零本身要配一条"例外仍在"的正向断言**(把 `return` 形态这个已知例外写成机器可查的,否则会出现"名单空着而余量还在"却没人报)、翻面断言用哪条正则只能逐处看(局部常量形态与 `content:` 值位都钉不住 `system:` 那条)、"已收编"那句写在「仍欠」之后当场红(W86 那个洞踩了第二次)、四个槽给的三张全仓名单期望串**没有一个在合并后成立**故一律 live 现取重写,而按 `Prompts.list()` 现推那两条**四次合并加了 6 个键一个字没改**(现推形态第二次拿到实测对照)、接进来的散文里余量数字一律删掉且**其中一句在本槽内就过期**(W88 拿意图路由举例说明常量形态漏计,而 W91 正好收掉那处),摘要长行的段界为什么要回退到句号再切,附"每个 tip 相对自己 `merge-base` 的用例增量相加恰等于合并后 live(453+2+2+2+2=461)"与三套件名集十五次 `comm` 全空的零吃测取证,以及 G-13 三张名单各自的余量盘点(A 归零、C 仍剩一处、标记一个不摘) | 想知道主干现在是什么状态;要合多条收编面在同一条「仍欠」段上首尾相接的槽、遇到某张全仓名单即将归零,或要接单镜视频审片那处 user 半人设(G-13 最后一处)时 |
| [w96-cycle7-audit.md](./w96-cycle7-audit.md) | 周期 7(集成线 `w80`→`w85`→`w90` + 旁支 W88–W95)逐项目独立核验报告:锚定 SHA、逐支成熟度 M0–M4、各支独立 worktree 重跑的 unit/integration/cli.smoke 数字与 README 对账(本周期四个数字零失真,而其中两个至今零断言、变异改错仍全绿)、注册表口径直读实测;**W63 点名的两条高危阻塞(丢记账件 `w53`、`core.memoryDual` 假欠账)本槽复核时都已被下游槽就地修掉**——六份核验报告里第一次治理债的偿还快过产生,但「把时点结论写成终局措辞」的产生机制一次未堵、本周期又发作两次(W72 把 `w70` tip 记成中间点、让一份记账件在线外挂了两个波次直到 W80 补合;W90 把已存在的 `w89-*` 记成远端零匹配,判定为记账失真无实质损失);13 次合并 13/13 取 head、`--no-ff` 13/13、用例增量相加与合并后 live 三槽逐个相等、功能槽基线 13/13 落在集成线 head 上;另记**全仓第一对同名用例**(`w84` 与 `w87` 各写一条同名的注册表持有者名单断言,W90 按并集都留下,`w90` 因此 453 条用例只有 452 个不同名字——这打掉了从 W21 起用的"用例名集合比对证明没删测",双向 `comm` 与增量相加两种口径不受重名影响)、索引完备性经 W67 加固后只封住散文点过的那一半、G-13 余量三张名单三个口径三个数(A 名单只剩 4 文件 6 处且恰好全押在四条没并线的旁支上)、G-10 发布门半边连续五个周期零开工 | 想知道某条分支做到哪一步;开集成槽前;或要判某次合并是不是真取了 head |
| [w97-g13-next-prompt.md](./w97-g13-next-prompt.md) | 单镜审片提示词首句(user 半开头)那处内联人设收编为独立键 `review.userSystem`:**"下一处该收哪一处"怎么先把 live grep 与各槽已声明的收编面逐条对表**(基线上 11 处命中里 4 类按口径排除、6 处已分属四条并行槽,剩的恰好只有一处)、**同一个落点被两条并行线各收一次时键名为什么必须对齐而不是各起一个**(键位是持久化面:异名会干净合成"两条 `def` 相同的死键",同名反而撞成一块解一次)、切刀为什么落在「只返回 JSON:」之前而把末尾连接逗号留在键内、双端单一来源的装配口为什么只能从签名收覆盖表(模块不碰 `window`,服务端两条审片分支漏传一处的表现是同一集里两种人设)、三张判据不同的全仓名单里为什么有一张**一处不改**(A 的判据本就不数 user 半,只改注释不动判据) | 要收 G-13 余量的下一处,或两条线撞在同一个收编落点上要判"该对齐还是该另起键名"时 |
| [w98-sk04-gen-compose-mem.md](./w98-sk04-gen-compose-mem.md) | SK-04/SK-26 最后那处回流余量(生成与合成两步)的核实件,**结论是否决、本槽零代码**:决定性判据为什么不是"抽不抽得出数字"而是"抽出来有没有读者"——这两步在两端都没有自己的 LLM 提示词(合成零 LLM、生成用镜头自己的 `s.prompt`),`WF_BOARD` 五个键里没有它们,W61 赖以一次接四步的那条结构保证(`scope` 取 `WF_BOARD` 故"下一轮同一步召回得到")在这里根本不成立;唯一会召回这两个板块的旁路(Agent 对话)**在同一份提示词的 user 半里已经带着 `AO.stateBlock`/`WfCore.agentStateText` 的实时逐集摘要**,回流条目会与它并排出现且更旧(附同一集 10 镜的并排实测:实时行 `共10镜/6已出片/2失败/2未生成/2待确认;下一步:处理失败镜` 完整覆盖回流行还多两项);"成片是否落盘"在成功收尾处恒为真、是常量不是结论,而合成真正有信息的那几个字段(静帧代替段数/`composedAudio` 四个数/SRT 有无)**已经逐字段落在 `ep.composed*` 回执上并有一整圈现成读者**;写下去的三处可量化负作用(成片板块召回优先段里审片条目 4 → 2 被合成流水挤掉、50 条上限下"开始挤掉用户自沉淀"的门槛由第 16 集提前到第 10 集、`生成` 板块条目会成为制片主任专家 `evolveExpert` 的蒸馏输入);另顺带核出两处记账不准并登记不改——SK-04 `note` 里"归发布门 G3/G7"指认有误(G3 是审片均分、G7 是合规敏感词且 headless 七门没有 G7,真正判素材产出的是 G4/G5/G6 加问题中心 `failed-shots`)、以及回流条目随集数增长挤掉用户自沉淀这处**已存在**问题;末节给出这条余量翻面的唯一判据与两个可能触发点 | 想接 SK-26 回流面下一步、想知道"某一步该不该回流"该按什么判,或看到 SK-04 `note` 里那句「生成与合成仍欠」想动手补齐时 |
| [w99-integration-log.md](./w99-integration-log.md) | 两条并行收编槽撞在**同一个落点、同一个键** `review.userSystem`(W93 与 W97)加一份零代码改动的核实件(W94)并进同一条线的记录:两侧的 `def`/切刀位置/装配口 `ov` 参数逐字节一致时三方合并**直接排成并集**(注册表 40 → 41 条而不是 42 条,`js/wf-core.js`/`js/review.js`/`server.js` 在第二次合并里零改动),键名不对齐则会**干净地合成两条 `def` 相同的死键**——合并不报冲突、双键也就没人报,故合完要现查四处(注册表键计数/无重键/`SK-03.prompts`/`note` 各一份)、**"增量相加"那条零吃测算法在这里不成立**(各槽增量不再互不相交:2+0+2 相加得 465 而 live 是 463,差的 2 条是两侧同名同体的那两条用例),唯一站得住的取证是名集双向对照十二次 `comm` 全空、**三张判据不同的全仓名单这次全归零**(A 与 C 因为差额里那一处被收掉而取值合流,但判据一个字未改)、**三条断言一起翻面**(SK-10/SK-11 的仍欠段加 W95 留下的那条"例外仍在"正向断言,锚点都是 `js/wf-core.js`),全归零时仍欠段只能改指口径外那一类(`js/api.js` 的两处层内兜底缺省)并配两向断言(余量还在 + 它还没进表),接进来的散文里过期的余量数删数留口径而自带"基线上"限定词的留着不动 | 想知道主干现在是什么状态;两条线撞在同一个收编落点上要判"该对齐键名还是另起一个";或某次合并里各槽的用例增量可能相交、不知道零吃测该怎么取证时 |
| [w100-sk04-gate-note.md](./w100-sk04-gate-note.md) | SK-04 仍欠段那句"素材产出的判定面归发布门 X"的门号订正:原文写 `G3/G7` 是错的——G3 判审片均分、G7 判合规敏感词且不在 headless 七门里,真正判素材产出的是 **G4 过期镜 / G5 未确认镜 / G6 失败镜**(三门都从 `Domain.episodeState` 的 counts 聚合)加问题中心 `failed-shots` 的实时判;为什么不能只把错门号删掉了事(留下正解才不用下一个人重新猜)、记账里点门号的断言为什么要**回 `js/release.js` 的门定义逐个核对**而不是在用例里手写第二份门号表(门改名或被摘掉时只有前者会红)、"G7 不在 headless 七门内"这句怎么用 `ReleaseCore.gates` 实跑出的门码钉住、既有锚点 `['生成与合成','解析向导']` 与 `gaps`/`pending` 为什么一处不动(本槽订正的是指路指错了,不是把路补上——那两步仍不回流,W98 的否决结论仍成立),附回写原句当场红的变异实测 | 要在记账或注释里点发布门门号、要动 SK-04 那条仍欠段,或想知道素材产出到底归哪几门判时 |
| [w101-mem-cap-priority.md](./w101-mem-cap-priority.md) | 记忆桶满 50 条时的淘汰优先级改为「先挤最旧的自动回流条(带 `fb`)、用户自沉淀的「记住…」留住」:为什么 `fb` 原地更新只压住"同一集反复跑"而压不住"跑二十集"(`fb` 键粒度里有集号,每集稳定占 3 条,`3 + 3×集数` 到第 16 集即 51 条开始砍数组头部,而用户手打的条目正是最早写进去那批)、两类条目的可再生性为什么不对等(自动条下一轮原样重写、用户条永久丢失且无提示)、**本次刚写下/刚更新的条目为什么必须排除在淘汰范围外**(否则全是用户条的桶里新回流唯一命中的就是自己,这次回流静默失效)、为什么不抬 50 也不给用户条留固定名额也不分桶(容量不是问题所在;两端写入面各写一份 50 字面且有源级断言钉着;分桶要连召回一起重做)、为什么用 `while` 逐条挑而不是按多出 k 条砍头 k 条(前段四步一次调用可能写四条),附"老写法回来即红"的两层变异实测与"两处用户写入面仍是裸 `slice(-50)`、用户新加一条仍可能挤掉别的用户条"这处残留的接法与代价 | 要动记忆桶的上限、淘汰顺序或两处用户写入面,或要给记忆条目加第三类优先级(如知识库种子)时 |
| [w102-ledger-assert-smoke-int.md](./w102-ledger-assert-smoke-int.md) | 补测试契约(零产品代码):`README` 里 `integration` 与 `cli.smoke` 的用例数 **进 `contract` 文档数字对账**——W23 当年把这两个数留在外面的理由(「用例不是静态数组,静态实计会得到假值」)为什么在这两个套件上不成立而在 `e2e.js` 上字面成立(行首 `report(` 56 行 vs README 63,差额来自按 tab 列表的那个循环),故对账口径取**套件自己数用例的那个口径**(每条用例一行 `report(...)`,行数即末尾那句总数),并用三层把"静态点数就是实跑总数"钉成**失败关闭**:每条 `report` 必须独立成行(塞进循环或回调当场红,即 W23 顾虑的假值现在报得出来)、末尾总数必须由套件自己的计数器算出、README 那句话删掉也算红;对账的是**用例总数**不是通过数(`cli.smoke` 那 2 项与 `master` 逐字同名的失败不影响它)。另治 `w84`/`w87` 各自独立写下的**一对同名用例**(基线 461 条只有 460 个不同名字)——重名会让本目录从 W21 起在用的「用例名集合双向比对证明没删测」把其中一条吃掉(`w85 → w90` 实测报"新增 9 条"而真实增量 10 条),故**只改其中一条用例名**(改 `w84` 那份,名里写明"与主体段那条同判据",判据逐字未动、两条都留)并新增断言:用例名集合大小恰等于用例条数、`套件 · 用例名` 标签同样逐条唯一;附六条变异实测(126→105 / 97→64 / 删措辞 / 真加一条 report / 把 report 塞进 forEach / 把重名改回去)与"这三种改法在基线上一次都不红"的复现取证 | 要改 README 里的测试条数、要往 `integration.js`/`cli.smoke.js` 加删用例、要把 `e2e.js` 也收进对账,或新写用例撞了名时 |
| [w103-memremember-memwrite.md](./w103-memremember-memwrite.md) | 用户手打那半个记忆写入面(`js/agent.js` `memRemember` / `cli.js` `memory add`)从裸 `slice(-50)` 接进 `WfCore.memWrite`:为什么 W101 落地之后残留的形态更别扭(**自动条写入时绕开用户条,用户条写入时反而只砍用户条**——`memWrite` 留下来的头部正是那些受保护的用户条)、为什么是"接进去"而不是在两个写入面各复制一份优先级(`memWrite` 对无 `fb` 的条目本来就走追加,接入不加参数不加分支;复制就是把同一淘汰口径写成三份且其中两份没断言)、"用户条挤用户条"为什么只缩范围不彻底解决(桶里一条自动条都没有那一路是容量不够而非顺序选错,要答"用户偏好按什么排序丢"得引新判据)、**两条认字面的源级断言怎么翻**(整文件搜 `slice(-50)` 在 `js/agent.js` 改后仍有 8 处 `agentChat` 命中会恒真失效,故改按函数段取并加"段内不得再自己截尾"的反向条件,判据收窄不放宽)、CLI 那一层为什么落在 `cli.smoke` 而不是 `integration`(淘汰发生在那次 `PUT` 之前,纯 HTTP 复现不出来),附两层变异实测(各自点名"少了哪一条用户条") | 要动记忆桶的写入面、上限口径或淘汰顺序,或要给记忆条目加第三类优先级(知识库种子)时 |
| [w104-integration-log.md](./w104-integration-log.md) | 一份核验件(W96 周期 7)、一份否决件(W98 SK-04 生成/合成两步不回流)与两条记账订正(W100 门号 / W101 满桶淘汰优先级)并进同一条集成线的记录:四次合并 4/4 取 head、`--no-ff` 4/4,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**(周期 7 以来第一个不推注册表条数的集成槽);**核验槽那份报告在自己分支上红的位置恰好把第二个洞挡住了**——核验槽按惯例只提交报告不登记索引行,那条正向 assert 一直红着,同一个用例里排在它之后的「相对链接不许悬空」就从来没跑到过,而报告正文有两处讲判据用的 `](./〈文件名〉.md)` 示意写法正是那半会认成悬空的形状,补上索引行让前一条转绿的同一刻这两处第一次可达并当场红(解法取"改文档占位形状"而不是"放宽断言正则":那条断言是这个洞唯一的守卫);**同一次冲突的相邻三段取了不同的侧**(SK-26 回流面那段:「原地更新故反复闭环不刷满 50 条上限」取对侧——那正是 W101 推翻的立论,`fb` 原地更新压住"同一个键被反复写"却压不住"键本身在变多",`3 + 3×集数` 到第 16 集即 51 条;段尾两侧各追加一句互不相干的新话则按并集都留、按槽号排序);同一行里「仍欠段点名的余量」取我方而 `memWrite` 那半取对侧(前者对侧带的是 `w95` 叉点旧实况、那处已随 W93/W97 在 W99 收编,后者「尾部截 50 条」是被改掉的实现);非冲突区过期一处(「一分钟摘要」里现在时的 `G3/G7`,新用例只查 `js/skills.js` 的 `note` 故删掉它也全绿)而四份记账件里那几处 `G3/G7` **引文一处不动**;**零吃测两种口径都跑**——增量相加这次成立(463+0+0+1+1=465)但成立条件"各槽增量互不相交"是现查出来的而不是默认的(W99 正是在这一步翻的车),名集双向对照十五次 `comm` 全空且**抽名前必须按 `\|` 切掉回执载荷**(W96 那两条用例在自己分支上是红的、带一整串期望/实际,合并后转绿,按整行比会假报 2 条消失);数字 live 现取(unit 两侧给的 462/462 与我方 463 全不成立,合 W100 后 464、合 W101 后 465;integration 那个 130 在非冲突区自动合上且恰好对,而这个数至今零断言故仍现跑复核一次);点名要保的四处逐条现查(`review.userSystem` 仍单键无死键、FORGE 仍是 getter、回流仍六步、三张名单仍 live 且本槽一个字不用重算) | 想知道主干现在是什么状态;要合几条只改记账/只加核验件的槽;或某个契约用例里串了多段 assert、想知道"合并后前段转绿会让后段第一次可达"这类怎么捞时 |
| [w105-index-completeness.md](./w105-index-completeness.md) | 目录索引契约补完备性那一层:**"文件连同索引行一起消失"为什么此前全绿**(按份点名只遍历目录里还在的文件,一起删则一份都不遍历,只剩散文 markdown 链接兜着,而记账件里有相当一批只有索引行这一处链接)、第三份记录该立在哪里(README 明写份数 + 用例里的下限,两个都不随目录与索引表一起消失)、为什么不用"波次号连续"当判据(W96/W98 是如实登记的未合槽,连续性判据当场误报)、下限为什么写成 `>=` 而不是 `==`、四条变异实测(只删行 / 文件加行一起删 / 三处一起改小 / 加文件不补行) | 要删或搬 `docs/skills-wave/` 下的记账件、要给"漏合导致记账件整份丢失"补守卫,或想知道索引契约现在拦得住哪几种删法时 |
| [w107-integration-log.md](./w107-integration-log.md) | 两条测试契约槽(W102 集成/冒烟用例数进对账 + 单元用例名全局唯一 / W105 目录索引完备性)与一条记忆写入面收编(W103 `memRemember`/`memory add` 接进 `WfCore.memWrite`)并进同一条集成线的记录:三次合并 3/3 取 head、`--no-ff` 3/3,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**;**目录索引表那处冲突两侧不是并集**——对侧带的是它自己那条线上的 `w101` 行(它叉在 `w101` tip 上),而 `w101` 早随 W104 并入我方表,机械取并集就得到两行同名索引,而三条索引断言**没有一条报得出"重复行"**(份数那条会红在"份数对不上"上,指向不准),解法是取我方后把新行单独按波次号插;**同一句散文被三条线各改一半**(README「文档数字对账契约」那句:我方带 W102 的两条、对侧带 W105 的完备性那一层,而句首"`contract` N 条断言"三边给的 五/七/六 **没有一个在合并后成立**,只能逐句合成再把条数 live 现取为八),同一个括号里"靠散文点名兜住"那半为什么取对侧(W105 做的正是把它换成不借道散文的判据,取我方即把它的立论写成假的);README 那条 unit 长行三次都取我方(对侧带的是叉点旧实况:`memWrite`「尾部截 50 条」会把 W101 满桶淘汰冲掉、仍欠段点名的余量已随 W93/W97 在 W99 收编),而对侧在同一行里的真增量按公共前后缀**定点替换**折回(不整行重写,W60/W72 那处全角引号的坑);**名集双向 `comm` 与"增量相加"给出两个不同的数而两个都对**——`w102` 名集增量是 `+3 / −1` 而条数增量是 `+2`,那条 `−1` 是它改了 `w84`/`w87` 那对同名用例中的一条、**重名在集合口径下互相抵消**(W96 点名过的盲区),合并后是"改名的一份 + 未改名的一份"而不是第三条同名,`unit` 的条数与不同名字数**第一次相等**(469/469,基线是 465 条 465 名里只有 464 个不同名);索引份数与用例里的下限按合入后 live 上调,W105 那条断言合进来的当轮就接住一次(明写份数还是叉点的旧值);另记这条集成线**第一次做到远端槽分支零 outstanding**(126/126 全成祖先)与索引表行序至今无断言兜底 | 想知道主干现在是什么状态;要合一条与主干在同一张索引表上各带一段的分支(留神并集会出重复行)、遇到同一句散文被三条线各改一半,或某次合并里名集增量与条数增量对不上时 |
| [w108-parse-wizard-cmd.md](./w108-parse-wizard-cmd.md) | SK-04 那条"浏览器剧本解析向导另有入库路径"的余量真收掉:向导 Step1(`js/director.js`)原先自己调提取、自己造主体条目、最后一行 `p.subjects = subjects` **整表覆盖**——W61 记的"另一套合并口径"其实低估了差距,那条路**根本不合并**,同一个用户点两个按钮(解析剧本 vs 命令面板/Agent 的提取主体),一个会把已生成的主体参考图连带抹掉、一个不会;现整段改经 `Commands.execute('project.extractSubjects')`,进度/ETA/断点重试那套 UI 一行未改。收之前先把两件真会丢的东西做成命令参数位(否则就是拿口径统一去换用户看得见的功能):`model`(上传弹窗那个模型选择器不能变装饰)与 `local`(重试仍失败时回退本地启发式,与拆集的 `local` 同义:强制本地、零 LLM、零计费);两位都只在浏览器语境成立(服务端模型取账号默认设置、本地启发式词典只在浏览器),故 `cli.js` **如实 `blocked browser-only`** 而不是收下来静默忽略(注册表里 `noImage` 标 "(CLI)" 是同一条口径的先例)。唯一的行为退让写在正文里而不藏在 diff 里:LLM 某一类返回空时不再用本地词典命中补那一类(那是给主体库塞噪音,且会一路带到分镜主体引用与发布门 G9);`st.subjects` 的取材口径改成"库里缺参考图的那些"(与 `subject.generateImage` 默认/G9/问题中心同判据),Step1 文案由"已识别 a 角色 · b 场景 · c 道具"换成新增/已有/库存三个数(整表覆盖时那一个数恰好当两个用,合并后不再相等)。另附:向导不再自己登记重复的「剧本解析」任务、新建主体的生图模型默认搬进命令层(与同行 `prompt` 读 `tplImage` 同性质)、`types` 形参去掉(全量提取口径归命令层);**分集那一半核实为"已经共用单源、不收"**(向导与命令层同调 `EpisodeUtil.splitCore`,是一份实现两个入口,不是两份实现)并顺带登记出一个真断点——「仅进行分集」按钮传的正文**不写进 `p.script`**,收口会当场 `blocked no-script`,而让它写进去翻的是 `Domain.workflow` 的剧本步(`no-script` → `done`)——W108 原记的"会翻转发布门 G1"是误记,`g1-workflow` 逐集判 `ep.content`、一个字不读 `p.script`,判据由 W110 按 live 代码订正;`js/director.js` 由此有了第一条自动化断言(`loadDirector()` 沙箱:命令层与入库口径真跑,`bgDock.close()` 必须触发 `onCancel`、审核窗必须真点确认,否则测的不是这段代码),附六条变异实测与"整份回退是无效判据"的说明 | 要往 `p.subjects` 加新的写入入口、要给领域命令加只在一端成立的参数位、要动 SK-04 的仍欠段,或想知道"UI 向导收口到命令层"该先把哪几件独有语义变成参数时 |
| [w109-integration-log.md](./w109-integration-log.md) | 一条 UI 向导收口槽(W108 剧本解析向导的主体入库改经 `Commands.execute('project.extractSubjects')`)并进同一条集成线的记录:一次合并取 head、`--no-ff`,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**,而这是本条线自周期 7 以来第一个真改产品行为的合入(动的是浏览器一条完整用户路径,不再整表覆盖 `p.subjects`);被合入支叉在 `w104` 上、看不见 W107 那条线落的三件事,故**两份 README 的三处冲突全是"对侧带着叉点旧实况"**——unit 长行第三次取我方(对侧 memory 段那句「120 字·50 条口径一致」正是 W103 改掉的写法,取对侧即把 W103 冲掉),对侧的真增量是一整段新话故按段界纯插入(比 W107 那次替换段落中间一截容易);`cli.smoke` 那句的条数三边给 97 / 101 / 98 **一个都不成立**(live 是 102),归属标注跟着最后一次扩它的槽走;**索引表这次现查出来是真并集**——与 W107 那次"机械并集会出两行同名 `w101`"用的是同一条判据(「这一块分叉之后主干又落了什么」)而答案相反,故那条记录只能当"必须现查"的理由、不能当结论套用;**两侧各改同一个注册表里的两条 `note` 而 `git` 自动合上**(SK-04 仍欠段 vs SK-26 回流面隔着三百多行),"合上了"与"合对了"是两回事——`w108` 交接点名过「仍欠段里不许把 `解析向导` 留回去」,靠它自带的正反两条断言复核而不是通读;**零吃测的比对脚本自己踩了一次重名盲区**:期望集去重而 live 侧不去重,`integration` 当场假报一条 `项目不存在 404` 是"live 多出来的"(那正是 130 条只有 129 个名字的那一对),改成两侧都不去重的多重集比对后三套件双向全空——W96 记的是重名会让比对**吃掉**一条,这是同一盲区的另一面:**去重口径不一致会凭空多报一条**;数字 live 现取(unit 469 → 471、`cli.smoke` 101 → 102、索引份数 125 → 127 含本槽记账件,下限棘轮同步上调),接进来的散文里**一处过期数都没有**(这条线上第一次) | 想知道主干现在是什么状态;要合一条叉在更早集成点上的槽(留神对侧整行带的是旧实况)、遇到两侧改同一文件不同段而自动合上,或零吃测的双向比对报出对不上的条数时 |
| [w110-split-only-script.md](./w110-split-only-script.md) | 上传弹窗「仅进行分集」按钮把原文补写进 `p.script`(W108 交接第 5 条):改前那条路只把正文传给 `doSplit`,拆出来的分集正文有了而项目剧本恒空——主线剧本步一直报"未上传剧本"、`project.splitEpisodes`/`project.extractSubjects` 两条命令一律 `blocked no-script`、计划步推不出、AI 策划读到空原文,而原文就在用户刚才那个输入框里。写入落在 `doSplitRun` 的成功路径(不是按钮回调:另四个调用点传的本就是 `p.script`,写这里对它们是幂等空转;走 `after` 回调则会排在 `Views.projectDetail` 之后,项目页当场重渲一次仍显示"未上传剧本")、落在 `splitCore` 成功之后(守卫抛错已提前 return,不造"有剧本、零分集"这第二种自相矛盾;空原文更早一层被按钮拦下)。**顺带订正一处判据标签**:W108 把它记成"会翻转发布门 G1(有剧本)",实况是 G1(`g1-workflow`)逐集调 `Domain.episodeState` 判 `ep.content`,`p.script` 一个字不读——同一夹具只改 `script` 一个字段,两次 `Release.collect` 的 G1 都 `pass` 且 `fails`/`overall` 全等,真正报"未上传剧本"的是 `Domain.workflow` 的剧本步(另附随写入变化但都不改门禁结论的两处:G7 合规扫的项目级文本现含整本原文、问题中心剧本面低危提醒开始出);`js/proj-upload.js` 由此有了第一个行为面沙箱 `loadProjUpload()`(弹窗真跑到 `splitCore`,只桩 DOM/任务条/重渲,重渲桩顺手记下那一刻的 `p.script` 取值),三条变异各红一条,含"ordering 断言第一版写成恒真句、跑变异没红"的实测 | 要给 `p.script` 加写入点、要判"写在动作成功前还是成功后"、要接 W108 交接第 5 条把 `doSplitRun` 收口到命令层,或在记账/注释里要点发布门 G1 的判据时 |
| [w112-integration-log.md](./w112-integration-log.md) | 一条主线断点收口槽(W110「仅进行分集」把原文落进 `p.script`)并进同一条集成线的记录:一次合并取 head、`--no-ff`,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**,发布门更是一行未改;被合入支叉在 `w108` tip 上而基线在它之后还有三个提交,故 `merge-base` 恰好落在 W108 的 tip、仍是真三方合并,**冲突面正好就是那两份 README、一处不多**——unit 长行第四次取我方骨架(对侧整行带的是 `w108` 叉点旧实况,memory 段那句「120 字·50 条口径一致」正是 W103 改掉的写法),对侧的真增量按段界**纯插入**而不重写整行(W60/W72 那处全角引号的坑就出在整行重写上),索引表现查为真并集(两侧各带 `w109`/`w110` 一行);**合进来的槽自带一处判据订正,而订正对象是本条线自己上一份记账件的交接条**——W108 把「仅进行分集」写 `p.script` 记成"会翻转发布门 G1"、W109 交接原样传了一轮,而 `g1-workflow` 逐集判 `ep.content`、一个字不读 `p.script`,真正翻的是 `Domain.workflow` 的剧本步(`no-script` → `done`),故本槽在合提交之外多做一件事:**现查这条误记还留在 live 面的哪几处**(`js/skills.js` 零命中、主 README 由对侧自己改对并自动合上、唯一残留是索引表里 `w108` 那行,只改这一处),而两份记账件正文按存档口径一字不动——**"判据记错"与"数字过期"不是同一件事,前者要改到 live 面为止,后者只删数留口径**;**W109 记的"去重口径不一致会凭空多报一条"这处盲区本槽照它的判据主动撞了一次并复现成功**(期望集 `sort -u`、live 不去重,`integration` 照样假报 `项目不存在 404`),盲区因此从一次性观察变成两次可复现的实测;不抬门另有三面取证(`js/release.js`/`js/release-core.js` 零改动、`cli.smoke` 那条 `fail=4,warn=1` 与 `master` 逐字相等、W110 那条正反用例合并后为绿);数字 live 现取(unit 471 → 474,`integration` 130 与 `cli.smoke` 102 没动但都复核过,索引份数 127 → 129 含本槽记账件,下限棘轮同步上调) | 想知道主干现在是什么状态;要合一条其判据订正会波及本线既有记账件的槽(留神"改到哪一层为止")、想照抄"取我方骨架 + 按段界纯插入"这一手,或要复核零吃测取证里那两侧的去重口径时 |
| [w113-cycle8-audit.md](./w113-cycle8-audit.md) | **周期 8 独立核验件**(核验面:集成线 `w99 → w104 → w107 → w109`,旁支 W93–W98、W100–W105、W108,以及已推未并线的 W110;全部数字在各支独立 `git worktree` 上重跑、不采信被核分支自述):**周期 7 那六条高危阻塞里四条闭合、一条部分闭合,是七份核验报告里最高的一次**,而闭合路径本身是新的——前七个周期的定式是「药方靠下一个碰到它的人顺手做」,本周期 W102 与 W105 两个槽**全部产出就是把 W96 点名的两个洞做成断言**(各自零产品改动),故报告给 W96 那条「落点要落在下一个人一定会经过的地方」加了一层:那是**让药方自己生效**的条件,而**专门排一个只产出断言的槽**是**让药方在没人经过时也生效**的条件(三条未动的阻塞——主干策略、集成线 head 指针、`assert*` 口径——共同点是"要改的不是任何一个槽的产出物");十九路变异逐条实测把三条契约的**确切边界**量了出来,其中两处残留最要紧:**W102 那条静态点数只封住单行形态**(变异 19 把一条 `report(` 用多行 `forEach` 包起来,`report(` 仍独立成行故行数与调用数都不变、单测全绿,而 `integration` 实跑从 130 变 131——用例源码那句注释声称这一路会红,实测只对单行成立),以及**三个用例数一条棘轮都没有**(变异 7:真删一条 `report(` 并把 README 同步改小,全绿;而记账件份数那条有 `FLOOR` 挡着,变异 12/13/14 要静默删一份得**同时改四处**、第四处在用例源码的 `FLOOR` 字面上);另实测 `cli.smoke` **不是并行安全的**(四支并发跑得到 81/97、88/97、94/101、83/102,严格串行后稳定,成因是共用同一套临时端口与配置目录,而两份 README 搜「并行不安全 / 必须串行」零命中)——**批量复核者不串行就会误报回归**;基线纪律面点出本周期最要紧的一处实质缺失:**W110 叉在功能槽 `w108` head 上,故它的树里没有 W107 那一整波**(`is-ancestor` 对 W107/W102/W105 三处逐条实测为假),那四条新契约一条都没有、仍带着 W102 已清掉的那对同名用例 | 想知道周期 8 这一批(W99–W110)每支的实测数字与成熟度、某条契约到底封住了什么没封住什么(尤其"静态点数 vs 实跑条数"那处边界),或要判"专门排一个只产出断言的槽"值不值得时 |
| [w114-int-dup-casename.md](./w114-int-dup-casename.md) | `tests/integration.js` 那一对逐字同名的用例名 `项目不存在 404`(拆集 `/api/wf/split-episodes` 与发布留痕 `/api/wf/release` 各一条)拆开:**只改发布留痕那一条**为 `wf/release 项目不存在 404`(判据逐字未动、两条都留),端点前缀取自同文件既有的 `wf/extract-subjects 项目不存在 404`;为什么这处重名值得单开一槽治而不是继续在比对脚本里绕——它坏的是本目录从 W21 起用的「用例名成集双向比对证明没删测」这件取证工具(集合口径会把一对重名里的一条吃掉,删一条真用例与少数出一条重名互相抵消),`w109`/`w112` 两条集成线各在这里被同一盲区的**另一面**绊过一次(两侧去重口径不一致时凭空多报一条),但都只治了脚本口径没治重名本身;同槽补一条与 W102 `unit` 唯一性同形的断言:`integration`/`cli.smoke` 两个套件的用例名集合大小恰等于 `report(...)` 登记行数,并**先钉住"每行的名字都是就地写死的字面"**(拼出来的名字静态取不到,后两层的名集口径当场失真),`e2e.js` 为什么仍不收(那处 tab 循环让行首点数不代表实跑条数,接进来第 1 层当场红),附四条变异实测(含"整行删掉一条 report 红的是 W102 那条数字对账而不是本条"这处如实分工) | 要往 `integration.js`/`cli.smoke.js` 加删用例、新写用例撞了名,或某次收敛槽的零吃测比对报出对不上的条数时 |
| [w115-g13-next-prompt.md](./w115-g13-next-prompt.md) | `G-13` 下一处内联人设的**现取核实件(零代码槽)**:在 `w112` HEAD 上按"仍内联 + 属于发给模型的人设句 + 排除注册表/预置专家库/placeholder/API 层兜底/装配协议半"这一口径逐条重扫,结论是**合格余量为 0**,故一行代码未改——余量为零时新开一个键就是把"没有余量"伪装成一次收编;核实分两刀,第一刀是 `你是` 字面全仓普查(窄口径三处、宽口径再多四处,逐处写明落在哪一类排除里),第二刀从消费侧切——把全仓 43 个浏览器 LLM 调用点 + 服务端 `wfLLM` 11 处 + CLI 1 处的 `system` 半摊开,逐键列出取值口,并做两向反查(**40 条人设键无孤儿**、**全仓没有不带 `system` 的 LLM 请求**),后一条顺带把 `js/api.js` 那两处兜底的性质钉实为"当前零调用方依赖的层内缺省";边界上三处"看起来像但不合格"的各有一节交代(`js/agent-global.js` 两句上下文框定语为什么归装配面、`FORGE_CONTRACT` 里那句为什么是字段说明不是人设、placeholder 为什么单列一类);三张持有者名单逐张 live 重算,A 与 C 空集、B 只剩排除类,**三张都不用改**;`G-13` 仍开着与余量归零不矛盾(摘标记判据看的是含 API 层兜底的那个集合),并把"那句兜底要不要进注册表"如实登记为一件未决的产品口径 | 想知道 `G-13` 这条线还剩什么、要不要再开一个收编槽;或想照抄"字面普查 + 消费侧摊开 + 两向反查"这套余量核实法,以及零余量时该交付什么 |
| [w117-integration-log.md](./w117-integration-log.md) | 一条测试契约槽(W114 拆开 `integration` 那对逐字同名的 `项目不存在 404` 并给 `integration`/`cli.smoke` 补名唯一断言)与一份零代码核实件(W115 G-13 下一处内联人设现取核实,**合格余量为 0**)并进同一条集成线的记录:两次合并 2/2 取 head、`--no-ff` 2/2,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**,`js/` 与四个 Node 端一个文件都没进 diff;**两条槽都叉在基线 tip 上,故第一次合并 `merge-base` == HEAD、本可快进**——`--no-ff` 合出来的是「树等于对侧树」的合并提交而**不是三方合并**,前几槽那句「是真三方合并」这一次如实不照抄(第二次才是真三方,冲突面因此正好一处);**最值得记的一处是两侧把同一个统计量改成了同一个值故 `git` 连冲突都不报,而那个值合并后不成立**(明写份数两侧都算「基线 129 + 我这一份」写成 130,合完实际 131),与 W57 那条「同一数字两侧相等不等于合并后仍正确」同形却更隐蔽——W57 那次两侧是两个不同的错数、进了冲突块有人判,这次是自动合上,接住它的是 W105 的三方对齐而不是通读(附退回 130 当场红的实测);索引表现查为真并集(两侧各带 `w114`/`w115` 一行,判据同 W107 而答案相反);**`integration` 那对同名用例合进来之后就没了**——W64 第一次记下它、W96 点名它打掉了「按名成集比对证明没删测」这件取证工具、W109/W112 各被同一盲区的另一面绊过一次(两侧去重口径不一致时凭空多报一条),本槽合完是 **130 条 / 130 个不同名字**,照 W112 那条判据(期望集 `sort -u`、live 不去重)再撞一次**撞不出来了**,三套件的条数与不同名字数第一次全部相等;`w115` 那份核实件按任务口径**在合并后的 HEAD 上把它的现取命令重跑一遍复核**(窄口径三处/宽口径四处的行号逐个相同、43 个浏览器调用点与 `wfLLM` 11 处各自减掉定义与注释才对得上、40 条人设键零孤儿、`review.userSystem` 仍在 `js/wf-core.js:662`),故它索引行里那四个数是核实为真才留下的而不是「看着不会过期」就留;数字 live 现取(unit 474 → 475,`integration` 130 与 `cli.smoke` 102 没动但都复核过,索引份数 129 → 132 含本槽记账件,下限棘轮同步上调),`w113-cycle8-audit-*` 远端两遍现取零匹配故如实跳过并记账 | 想知道主干现在是什么状态;要合两条都叉在基线 tip 上的旁支(留神第一次合并不是三方合并)、遇到两条槽各自更新同一个统计量而冲突块里看不见它,或想知道那处重名盲区还在不在时 |
| [w119-integration-log.md](./w119-integration-log.md) | 一份周期 8 独立核验件(W113,核验面止于 `w109`)并入集成线的记录:一次合并取 head、`--no-ff`,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**,`js/` 与四个 Node 端一个文件都没进 diff,`tests/` 下唯一改动是 `FLOOR` 那个字面;**本槽是"真三方合并而冲突块为 0"的样本**(被合入支叉在 `w109` tip、落后基线 14 个提交,却只新增一个基线上不存在的文件),而**要解的东西一件不少**——合上那一刻单测红三条,因为 W105 那层三方对齐耦合的是「目录实况 / 索引表 / README 明写份数」而**三份里只有一份在对侧 diff 里**:`git` 看得见文件内容的分歧,看不见"合上之后契约还成不成立",故前几槽「有冲突块要判哪一侧」的记法这次给出另一端的反例(三条红与核验件结尾那段「未改目录索引的声明」预告的逐条相符,核验槽只交报告、索引行由集成槽补是 W104 立的分工);**第二处是那条占位链接口径要照字面执行**——报告在转述 W104 修 W96 那件事时把当年的 ASCII 占位原样抄了进来、只包了一对反引号,并据此声明「本文件全程用反引号写文件名故不会再冒出第四条红」,而判据是跑在纯文本上的正则、**反引号不参与匹配**,补上索引行让前一条 assert 转绿的同一刻它第一次可达并当场红(解法仍取"改占位形状"而不是"放宽正则",附正反两向变异复核;这条断言的这一面至此实测四次,这次撞上它的是一份**专门声明自己避开了它**的文档);**第三处是把核验件的阻塞按原判据在合并后的 HEAD 上逐条重跑**(它在 `w109` 上量的,而基线已多走 W110/W112/W114/W115/W117 三波):阻塞 5(用例名唯一性只覆盖 `unit`)已由 W114 结掉,而**阻塞 4 与 6 亲手复现、一字不差**——尤其阻塞 4,**W114 后补的名唯一断言接不住它**,因为多行包裹后那条用例跑两遍且两遍同名,"名集大小"与"静态行数"都仍是 130、两边相等故绿,只有实跑变 131,**这是 W96/W109/W112/W114 一路追的那处集合去重盲区的又一面**;另记一条要紧的反向发现:**阻塞 12 的 `grep` 判据被本槽自己写的那行索引打掉了**(如实转述那条发现即让「两份 README 搜『并行不安全 / 必须串行』零命中」变成非零,而运行说明里仍然一句都没有),下一个人复核它要查"运行说明里有没有这句话"而不是"全文有没有出现这几个词";数字 live 现取(三套件 475/130/100-102 三个数与基线逐字相等但仍逐套件跑过,索引份数 132 → 134 含本槽记账件,下限棘轮同步上调,**份数与 `FLOOR` 按合入后 live 取值而不是核验件交接指令里那个 128**——那是它分叉时点的数,照字面执行会同时踩两条断言);**并记下这条集成线连续四次零 outstanding 的记录到本槽为止**:记账落地前 `fetch --prune` 现取出一条未合的 `w120-report-count-multiline-ea89`,按提交题目与 diff 面看正是冲着阻塞 4 去的那个槽,任务口径只合 `w113` 故如实登记不合 | 想知道主干现在是什么状态;要合一条"零冲突块但会打破三方对齐契约"的槽、要照抄"把核验件的阻塞在合并后 HEAD 上逐条重跑"这一手,或要复核某条断言是不是真接得住它声称接得住的那一路时 |
| [w120-report-count-multiline.md](./w120-report-count-multiline.md) | W102 用例数对账那条静态点数的**另一半**:它数行首 `report(` 的行数当实跑条数,靠「每条 report 独立成行」(`calls === lines`)把两者钉在一起,而**那句注释声称会红的路只对单行成立**——把同一条 `report(` 拆成三行包进 `forEach`(`forEach` 一行、`report(` 仍独立成行、`});` 一行),行首行数/调用数/名字面数/不同名数**四个静态口径纹丝不动**而 `integration` 实跑 130 → 131、单测全绿(W113 周期 8 报告阻塞 4 记的就是这条,本槽在 `w117` head 上逐个数复核为真);收严取 W113 给的第二条路(留在纯静态、把判据从「行首 `report(`」换成「所在块链不在循环/回调体内」),而不是第一条(读回套件运行结果——那份文件本身成了新的可造假凭据,且让单测不再自足):逐字符配对括号还原每条 `report(...)` 的**外层块链**,链上任一层命中 `.forEach(`/`.map(`/`.filter(` 等 11 个迭代方法或 `for (`/`while (`/`do {` 即红;**为什么不按缩进往上找块头**(那样写当轮也绿,但把包裹层写成同缩进就整层跳过、判绿,见变异 2)、**抹码那道自检为什么是硬判据**(注释/字符串/模板串/正则先整段抹成等长空白才配得准括号,而这类手写扫描失准的方向恰好与放行方向一致——少配一对括号就是块链变短、变短就更容易判绿,故配不平当场报错而不是拿半截栈静默放行);附四条变异实测(多行包裹红、不缩进的 `for` 也红、**同一变异下其余所有断言全绿**这条正取证、以及实跑 131 与静态 130 的对照)与两个套件 232 条 `report(` 零误报的现测;同节点名 W113 同类残留仍开着的那条(真删一条 `report(` 且 README 同步改小仍全绿——三个用例数一条棘轮都没有)并写明为什么不顺手合做 | 要往 `integration.js`/`cli.smoke.js` 加用例而想循环着写、被这条断言拦住时(正解是拆成逐条独立登记,不是删测也不是改窄词表);或要写「某段代码落在什么结构里」这类静态判据时(`blankNonCode`/`reportBlockHeads` 是仓里第一份按 JS 词法抹非代码段的工具,配平自检不可省) |
| [w121-suite-count-ratchet.md](./w121-suite-count-ratchet.md) | `unit`/`integration`/`cli.smoke` 三个套件的用例数补「只增不减」棘轮:W102 立的对账钉的是"实测与文档相等",而相等这件事**两边一起改小照样成立**——真删掉一行 `report(...)` 再把 README 那个数字改小,既有断言一条不报(基线实测 475/475 全绿),于是"没人删测"在三个套件上至今没有机器判据、只靠评审看 diff;本槽新增一条用例给三个套件各钉一个下限(FLOOR 取本槽合入时的 live 条数:unit **476**、integration **130**、cli.smoke **102**),**实测数与 README 明写数分开各钉一次**(少一条用例红在前一句,只把文档改小红在后一句,那句话被整段删掉同样算红),故报错本身就说明被改小的是哪一侧;有意**不并进** W102 那条、也不与「每条用例须是独立一行的 `report(...)`」混成一句——后者报的是登记形态被破坏、静态点数代表不了实跑条数,本条报的是用例被删了,两种病灶的处置完全不同,合成一条会让报错文案必须同时描述两件事;`tests/e2e.js` 仍在对账与下限之外(tab 循环让行首点数 56 ≠ 实跑 63,给假值设下限没有意义),记账件份数那条既有下限按它自己的规矩 132 → 133;附五条变异实测(含"删测且把文档一并改小"这一条在基线上全绿、在本槽红,以及"只增仍绿"的反向验证) | 要往三个套件加删用例(新增用例的槽有义务把 FLOOR 抬到当轮实况)、想知道删测为什么现在会红以及红在哪一句,或要照抄"相等之外再立一层下限"这套双侧改小的防法时 |
| [w122-integration-log.md](./w122-integration-log.md) | 一条测试契约收严槽(W120 把 `report(...)` 静态点数从「行首独立成行」收严到「外层块链不在循环/迭代回调里」)并进同一条集成线的记录:一次合并取 head、`--no-ff`,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**,`js/` 与四个 Node 端一个文件都没进 diff,`tests/` 下本槽自己只改了 `FLOOR` 那个字面;**「明写份数」这个统计量在本条集成线上至此攒够三种样本而本槽是第三种**——W117 是两侧写成同一个错值故 `git` 连冲突都不报、W119 是只有一侧在 diff 里同样零冲突块、本槽是**两侧值不同(134 / 133)故终于进了冲突块有人判,而两侧给的都不是答案**(正解是合入后的目录实况 135):三次连起来说的是同一句话——这个数是「合并后的实况」不是「某一侧的主张」,既不能靠自动合并也不能在冲突块里二选一,只能合完现取;**W113 周期 8 报告的阻塞 4 到本槽为止闭合,而闭合是在合并后的 HEAD 上亲手复现验收的**(照 W119 那次的原样重做多行 `forEach` 包裹:静态行首点数仍 130、实跑仍 131,而 `contract` 这次红了 99/100 且**红的只有新加那一条、其余 99 条全绿**——正取证说明它接的正是自己声称的那一路,没靠连坐别的断言充数),这是七份周期核验件里第一条被「专门排一个只产出断言的槽」结掉的高危阻塞、闭合动作分在 W113 量边界 / W120 落断言 / 本槽验收三处;**同一节另记两条本槽实测走得通的边界并且都不改判据**——把 `report(...)` 包进箭头函数 helper 再调用两次(块链那层不是循环)、以及 `for await (const v of arr)`(词表正则要求 `for` 与 `(` 相邻,中间隔个 `await` 就溜过去),两条都静态 130 / 实跑 131 / `contract` 100/100 全绿,写清后者不是「漏了一种语法糖」而是**按词表匹配块头字面这类判据的固有面**(词表列举得再全,写法比词表多),要收得换判据类别而不是往词表里再塞词,集成槽不新立口径故如实交接;主 `README.md` 那两处自动合上的数字(契约段九条→十条、单元 475→476)**合并后现跑对过才留**,并写明第二处正是 W117 那个坑的形状而这次没塌不是靠运气——对侧算的「叉点 475 + 我这一条」恰好成立只因基线那 4 个提交一条用例都没加,真加过就会静默偏低一格而接住它的是 W102 那条**相等**判据;数字 live 现取(unit 475 → 476、`integration` 130 与 `cli.smoke` 102 没动但都复核过,三套件条数与不同名字数**连续第三槽全部相等**,索引份数 134 → 136 含本槽记账件,下限棘轮同步上调);W119 登记的那条 outstanding 由本槽结掉而同一刻又现取出一条 `w121-suite-count-ratchet-aa65`(冲的是同一份核验件开的另一张药方阻塞 6,与 `w120` 叉在同一个提交上,故下一个集成槽会撞上一模一样的份数冲突形态) | 想知道主干现在是什么状态;要合一条与本线在同一个统计量上给出不同值的槽(留神答案不在冲突块的任何一侧)、要照抄「把被合入支声称结掉的阻塞在合并后 HEAD 上亲手复现验收 + 顺带量它的边界」这一手,或想知道 `report(...)` 复跑这条线还剩哪些走得通的写法时 |
| [w123-integration-log.md](./w123-integration-log.md) | 一条测试契约收严槽(W121 给 `unit`/`integration`/`cli.smoke` 三个套件的用例数各钉一条「只增不减」下限)并进同一条集成线的记录:一次合并取 head、`--no-ff`,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**,`js/` 与四个 Node 端一个文件都没进 diff,`tests/` 下本槽自己只改了两个 `FLOOR` 字面与一处 `476 → 477`;**冲突块比前几槽多一类**——两侧各自往契约段的同一位置插了一条新用例(我方 W120 循环禁令、对侧 W121 三套件下限),判据是**两条都留、不并条**(一条报"用例被删了"、一条报"登记形态被破坏",病灶不同、报错文案不许合成一句),本槽实测这一点成立:删测那一路只红下限、多行 `forEach` 包裹那一路只红循环禁令,两条各红一次含义不混;**真正险的一处不在冲突块里**——主 `README.md`「单元测试(N 项断言)」两侧从同一叉点 475 各加自己那一条、都写成 476,值相同故 `git` 零冲突块自动合上,而合入后 live 是 **477**,与 W117 那个坑逐字同形(「两条槽各加一条而那个数记的是总数」只要叉点相同就必然写出同一个错值),这次接住它的是 W102 那条**相等**判据而不是通读(退回 476 当场红两条);**W113 周期 8 报告的阻塞 6 到本槽为止闭合且是在合并后 HEAD 上亲手复现验收的**(照原判据真删一行 `report(` 并把 README 130 改小到 129:基线 `contract` **100/100 全绿**、合并后 **100/101** 且红的只有新合入的下限那条),至此那份核验件开的两张药方都已落地并各自验收(阻塞 4 归 W120、阻塞 6 归 W121);**同一节另记一条要紧的性质判断**——三条断言(W102 相等 / W121 下限 / W120 循环禁令)里前两条数的是同一个「行首 `report(` 静态行数」,故**多一层下限不等于多一个取数口**,W122 量过的 `for await (` 那条边界本槽重测**照样走得通**(静态 130 / 实跑 131 / `contract` 101/101 全绿),阻塞 4 的实质仍未关死、要收得换判据类别而不是再叠第四层下限;另如实登记「`contract` N 条断言」这个数**至今零机器判据**(改成"九条"全绿)且它每加一条契约断言就要动一次;数字 live 现取(unit 476 → 477、`integration` 130 与 `cli.smoke` 102 没动但都复核过,三套件条数与不同名字数**连续第四槽全部相等**,索引份数 136 → 138 含本槽记账件,三个套件的 `FLOOR` 与记账件 `FLOOR` 一并按合入后 live 取而不照抄对侧写死的 476/130/102) | 想知道主干现在是什么状态;要合一条与本线在同一处用例表落笔的槽(留神两条新用例都得留、别并条)、要照抄「两侧同值故自动合上而合入后不成立」那一类数字的查法,或想知道一条新断言到底给既有那层加了什么、没加什么时 |
| [w124-report-wrap-helpers.md](./w124-report-wrap-helpers.md) | W120 循环禁令仍放行的**两路**(W122 现测出来、集成槽不新立口径故如实交接的那两条)各做成一条断言,**旧条一个字未动**:一路是把 `report(...)` 包进箭头 helper 再从两处调用——块链那层不是循环故循环词表点不到,而**不带花括号的单表达式箭头体连块链都不留痕**(`const emit = (…) =>` 换行接 `report(…);`,`report(` 出现那一刻栈上除 `async function main() {` 一个多余括号都没有),故新条一分两点判:块链上不许出现 `main` 以外的函数体(箭头体/`function` 体/`名字(参数) {` 方法体,控制结构按关键词表排除,**`await` 是有意排除的**——`for await (…) {` 恰好也长成"名字(参数) {"的样子,不排就会与另一条串味),以及 `report(...)` 必须是**裸表达式语句**(给 `reportBlockHeads` 多返回一个 `pre`:往前到最近一处语句边界之间的抹码后原文,除空白外有任何东西即红,顺带拦住 `const r = report(…)`/`return report(…)`/`if (x) report(…)`);另一路是 `for await (const v of arr)`——词表正则要求 `for` 与 `(` 相邻,隔个 `await` 就点不到,而**这不是漏收一种语法糖,是按词表匹配字面这类判据的固有面**(词表列举得再全,写法比词表多),故新条二不补词、改判「块头里有 `for` 却不是那个紧邻形状」,凡 `for` 引出的块要么被旧条点到、要么红在这里,中间不留缝(与 W120 交接第 2 条给的"按同形补词、不要改成禁函数体"处置**相反**,两次的判断依据都摆在 6.1 节);**三条断言的失败含义分开**(旧条=套着循环、新条一=被包成可复用的函数、新条二=词表点不到的 `for`),迭代回调那处交集**有意双报**并写明为什么不切掉(切掉就得让一条去引用另一条的词表);四条变异实测:两条新断言**互不连坐**(变异 1 只红新条一的裸语句那点、变异 2 只红新条二)、**旧条在三条变异下一次都没红过**(正是本槽存在的理由)、多行 `forEach` 对照组按分工红两条;两个套件 232 条 `report(...)` 零误报,静态四口径 130 而实跑 131 那处失真到此关掉 | 要往 `integration.js`/`cli.smoke.js` 加用例而想抽个 helper 复用登记行、被这三条里任一条拦住时(正解是拆成逐条独立登记,不是删测也不是改窄词表);或要写"某处调用落在什么结构里"这类静态判据时(块链看得见带括号的包裹层、`pre` 看得见不带括号的那半,两个一起用才盖得全) |
| [w125-integration-log.md](./w125-integration-log.md) | 一条测试契约收严槽(W124 给 W120 循环禁令仍放行的两路各做一条断言:`report(...)` 不许包进 `main` 以外的函数体、必须是裸表达式语句;`for` 与 `(` 不相邻的迭代体另判一条)并进同一条集成线的记录:一次合并取 head、`--no-ff`,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**,`js/` 与四个 Node 端一个文件都没进 diff,`tests/` 下本槽自己只改了一个 `FLOOR` 字面与一处 `477 → 479`;**W123 交接的残留 2 到本槽闭合,而闭合走的正是那条残留自己开的药方**——它写的是"要收得换判据类别,不是再往同一个数上叠第四层下限",W124 换的两条判据(看 `report(...)` 落在什么块里、前面还有没有代码 / 判"块头里有 `for` 却不是那个紧邻形状")本槽在合并后 HEAD 上把 W122、W123 连续两槽量成"走得通、全绿"的那两条边界原样重跑,**现在各红一条**,这是这条线上第一次由"上一槽写下的处置意见"直接指出下一槽该落什么断言、并在下下槽验收成功的完整闭环;**四条变异同时记下四个静态口径(行首点数/调用数/名字面数/不同名数)全程 130 一个不动**,由此直接读出三件事——带花括号 helper 只红新条一的函数体那点、不带花括号的单表达式箭头 helper 只红它的裸语句那点、`for await` 只红新条二,**旧的循环禁令在这三条变异下一次都没红过**(正是 W124 存在的理由),多行 `forEach` 对照组按 W124 写明的分工有意双报;另复核 W121 的三套件下限仍接得住"删测且把文档一并改小"(只红下限那一条),三层判据分工一条不缺;**主 `README.md` 同一段散文里的两个数这次同时踩到同一个形态**——「单元测试(N 项断言)」我方 477 / 对侧 478、「`contract` N 条断言」我方十一 / 对侧十二,**两侧值不同故都进了冲突块而两侧给的仍都不是答案**(合入后 479 与十三),前者有 W102 的相等判据兜底、后者至今零机器判据故是逐条点着实跑清单数出来的;数字 live 现取(unit 477 → 479、`integration` 130 与 `cli.smoke` 102 没动但都复核过,三套件条数与不同名字数**连续第五槽全部相等**,索引份数 138 → 140 含本槽记账件,下限棘轮同步上调);W123 登记的"零 outstanding"到本刻已翻转——现取出一条 `w126-contract-count-assert-2556`,冲的是「`contract` N 条断言」那条残留而给的口径与本槽写下的不同(它改成取整套件条数并改写那句散文),故下一个集成槽撞上的不是数字二选一而是**同一句话两种口径** | 想知道主干现在是什么状态;要照抄「把上一槽写下的处置意见落成断言、再在合并后 HEAD 上把原先量成全绿的边界逐条重跑验收」这一手、要合一条与本线在同一段散文里两个数都给出不同值的槽,或想知道 `report(...)` 复跑这条线现在还剩哪一类写法时 |
| [w126-contract-count-assert.md](./w126-contract-count-assert.md) | `README.md`「文档数字对账契约」那段**自报的断言条数**接进对账:段里点名的每个数字都有判据接着,唯独开头那个 N 是纯手写——W123 核验实测过「十一条」改成「九条」仍 101/101 全绿,而这个数每加一条契约断言就要动一次,还是三条集成线在同一句散文上反复撞车的那个字(W107 三边给「五/七/六」没有一个在合并后成立、W122 记过一次「九→十」并注明"人工核对为真");本槽给它立判据的同时**换了口径**——从"这段里做数字对账的那几条"(一个人挑的子集)改成 `SUITES.contract` 长度即整套件实跑条数,理由是前者**没有机器边界**:按名字前缀圈会漏掉段里点名却不带前缀的三条(循环禁令/三套件棘轮/单元名唯一),为它们改名等于给"按名成集比对证明没删测"那件取证工具制造一次全仓改名;按首尾标记注释圈则把病灶换个地方漂(漏在标记外的新条永远数不到)。README 那句同步改成写明口径的形态(「含下面这些数字对账在内的全部契约用例」这半句有意写死,免得下一个人照旧按旧口径去点数),沿用 `assertDocNum` 的两层判据故**改写或删掉那句话同样算红**;另记正则里那对括号写成 `[(（]` 半全角都收、全角那半在源码里写成 `\uFF08` 转义的原因(直接写字面被规范成半角过一次,当场表现为「找不到该数字表述」的假红);附五条变异实测(改小/改大/改写三个方向各红一条且基线上全不红,真加一条与真删一条各自惊动"套件条数"与"单元总数"两个口径、报错不混) | 要往 `contract` 套件加减用例(那句话得跟着改)、想知道"给一类断言的条数立判据"为什么难以及两条路各自的代价,或集成时撞上契约段那句与「单元测试(N 项断言」这两个高发数字时 |
| [w127-integration-log.md](./w127-integration-log.md) | 一条测试契约收严槽(W126 给 `README.md` 契约段**自报的断言条数**立机器判据,并把口径换成 `SUITES.contract` 长度即整套件实跑条数)并进同一条集成线的记录:一次合并取 head、`--no-ff`,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**,`js/` 与四个 Node 端一个文件都没进 diff,`tests/` 下本槽自己只改了两个 `FLOOR` 字面;**本线第一次撞上「两侧改的是同一句话、给的是两种口径」**——前六槽在数字上撞过三种形态(两侧同值故零冲突块 / 只有一侧在 diff 里 / 两侧不同值进冲突块),三种的正解都是"取合并后的实况",而本槽这一处**不是数字二选一能解的**:我方 W125 沿旧口径把那句里的条数手改成「十三条」,对侧把判据口径从"这段里做数字对账的那几条"(人挑的子集、没有机器边界)换成整套件条数并改写了那句散文,判据是**取对侧口径 + 一个字不删地保留我方那段散文**(理由不是"对侧更新",是口径之争里只有一侧有断言接着;对侧叉在 `w123` 上故看不见 W124 那两条新断言,它保留的段中散文是叉点那版),条数不取两侧任一值(十三 / 102)而现取 live **104**;**W125 交接的残留 3 由此闭合,但闭合带一条本槽实测出来的附注**——变异 5 真把计数那条用例**自己**删掉时它一声不吭(已不在套件里),接住那一路的是"单元总数"与"三套件下限",故准确说法是「有判据的是"这个数与套件实况不符"这一路,"判据本身被摘掉"那一路仍由别的断言兜着」,这是本线第一次量出**判据自身被摘掉**这种形状;六条变异记全三个方向(改小/改大/改写各红一条,第三条走的是 `assertDocNum` 第一层"找不到该数字表述"故与写错数可分)与真加/真删各惊动两个口径且报错不混,另把 W124 两条与 W121 那条在合并后 HEAD 上原样重跑(helper 两点、`for await`、删测+改小文档各只红自己那条,旧循环禁令一次没红),**五层判据互不顶替**;另如实登记条数对上之后仍零判据的那一层(段里逐条描述的内容与套件那 104 条除条数外没有任何对齐判据,加一条无关用例再同步改数即全绿);数字 live 现取(unit 479 → 480、`contract` 103 → 104、`integration` 130 与 `cli.smoke` 102 没动但都复核过,三套件条数与不同名字数**连续第六槽全部相等**,索引份数 138 → 142 含本槽记账件,两个 `FLOOR` 一并按合入后 live 取);本刻远端零未合而按前例只记成时点读数 | 想知道主干现在是什么状态;要合一条与本线**在同一句散文上给出不同口径**(而不只是不同数字)的槽、要照抄「口径之争按"哪一侧有机器判据"判、再把与口径无关的散文原样嵌回」这一手,或想知道一条"数自己所在套件"的断言接得住什么、接不住什么时 |
| [w128-g11-preset-evolve.md](./w128-g11-preset-evolve.md) | G-11 第三面:预置专家补上人手进化入口——先核实这处是"有意锁死"还是"漏入口"(源码那句注释只描述实况不给理由、上游两份记账都把它当未做且写好了接法、零件全是现成的,故判漏入口),预置专家为什么不能原地进化(注册表是模块级常量存不住 + 服务端 `require` 同一份故两端会漂移),落点自定义副本的四个决定(`from` 记派生源 / 同一预置只派生一份 / 深拷贝防共用 `dims`·`tpl` 引用 / 名字带派生源名),`expertBoards` 多认一个 `from` 为什么是沿用而不是开口子(四种回空情形逐个钉),副本为什么卡在两道闸之后、扣费之前落库,判"是不是预置"为什么用注册表反查而不用 `e.custom`,以及"数出现次数"怎么补上第一版源级断言的漏(只查"出现过 `data-pevolve`"时,把风格导演卡的按钮整个删掉全套 486 条一条不红);G-11 三面记账更新为"只剩人手点一面" | 要给预置专家类资产开写入口(落副本这一手)、要动专家自进化的落点或计费口径,或想知道 G-11 还欠哪一面时 |
| [w129-integration-log.md](./w129-integration-log.md) | 一条预置专家进化入口槽(W128 给预置专家补人手进化入口,条款落自定义副本、副本记 `from`=派生源,`WfCore.expertBoards` 多认一个 `from`)并进同一条集成线的记录:一次合并取 head、`--no-ff`,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**;**本线第一次出现「合并提交自己一个字都不用改」的槽,而这是分叉点位置直接决定的**——`w128` 叉在基线 tip 上(`merge-base == HEAD`),故它算的每一个数都是在本线最终树上算的(明写份数 143、主 README 的 `单元测试(486` 都恰是合并后 live),前十来槽反复踩的那三种数字形态(两侧同值故零冲突块 / 只有一侧在 diff 里 / 两侧不同值进冲突块)**一个都不成立**,冲突块为 0 且合上那一刻单测直接全绿,与 W119 那次「真三方合并而冲突块为 0 却当场红三条」**不是同一回事**,故前几槽那句「是真三方合并」本槽一次不照抄;**零冲突零红不等于零欠账**——本槽量出的是「下限棘轮松了 6 格而没有任何断言报得出来」:W121 立三套件下限时写明"新增用例的槽有义务把 `FLOOR` 抬到当轮实况",而 `w128` 加了 6 条用例、抬了记账件那条(142 → 143)却把单元测试那条留在 480(live 486),它**不红**因为判据是 `live >= floor`、**下限变松是静默的**(W105 当年写成 `>=` 而不是 `==` 有明写的理由:`==` 会让每加一条用例都当场红,等于再造一个要人工同步的数),故"该抬没抬"这一路至今零机器判据、只靠人记得,本槽照 live 抬到 486 并如实交接;**`js/` 第一次在连着五槽「一个文件都没进 diff」之后重新进 diff**(`experts.js`/`gsettings.js`/`wf-core.js`/`skills.js` 四个文件,动的是浏览器一条完整用户路径),而 `server.js`/`cli.js`/`mcp.js`/`billing.js` **仍是零命中且这是设计不是巧合**(预置注册表是双端共享静态数据,落副本这一手正是为了不让两端漂移);十二条变异分两组——合进来的四条各自点名(只删两处预置卡中的一处只红计数那一句而其余 485 条全绿、副本不记 `from` 红两条、`expertBoards` 退回只认自己 id 红两条、副本落库提到闸前红两条,报错不混),任务点名要保的三层在合并后 HEAD 上原样重跑(契约段自报条数改小与改写各红一条且第二条走「找不到该数字表述」那一层、`report(...)` 三层包装禁令按 W124 写明的分工各红各的且静态四口径全程 130 不动、三套件下限接住"删测且把文档一并改小"只红下限那一条、记账件连文件带索引行一起删红两条、SK-26 仍欠段写回「只对自定义专家开放」当场红——最后一条是"note 用的是 W128 分面现况(人手点仍欠)"的机器复核);另记一条方法性的诚实位——**叉点与基线重合时双向 `comm` 能排除的东西反而比往常少**(并集运算退化成对侧全集,两向为空是必然的,真正起作用的是"对侧增量 −0"那一半);数字 live 现取(unit 480 → 486、`contract` 104 与 `integration` 130 与 `cli.smoke` 102 没动但都复核过,三套件条数与不同名字数**连续第七槽全部相等**,索引份数 142 → 144 含本槽记账件,单元测试 `FLOOR` 480 → 486 与记账件 `FLOOR` 143 → 144 一并按合入后 live 取);本刻远端零未合而按前例只记成时点读数 | 想知道主干现在是什么状态;要合一条**叉在基线 tip 上**的槽(留神这次对侧算的数就是答案,而换个叉得早的槽同一个人写同一个数照样会错)、想知道下限棘轮拦得住什么拦不住什么,或要照抄「把任务点名要保的判据在合并后 HEAD 上逐条重跑验收」这一手时 |
| [w130-cycle9-audit.md](./w130-cycle9-audit.md) | **周期 9 独立核验件**(核验面:集成线 `w117 → w119 → w122 → w123 → w125 → w127 → w129`,旁支 W113–W115、W120、W121、W124、W126、W128;十九支各开一个 `git worktree` 现跑,`integration`/`cli.smoke` 串行):**合入纪律面零违规**(整文件取 ours/theirs 24 次检查零命中、文件被回退到 merge-base 内容零命中、远端未合入分支 0 条),主线七步在十三态走查下逐态自洽;而本轮量出的两处都落在**判据的方向**上——五层测试契约(README 精确对账 / 契约段自数 / 登记形态 / 包装禁令 / 下限棘轮)全部在防「一处静态登记跑出多条、数字被改小」,**反方向一条判据都没有**:**洞一**是 W129 交接的下限棘轮「该抬没抬」零判据,本槽做成两步实证——插一条探针用例只红 L1/L2 两条精确对账(`FLOOR` 那条判的是 `live >= floor` 故不红),把 README 抬到 487/105 后全绿而余量变成 1,**随后在这一格上真删掉一条既有用例并把 README 改回 486,仍是 486/486 全绿**(棘轮把刚攒的那一格原样还给了删测,故「`FLOOR` 落后 live 多少条」就等价于「预先批准删掉多少条真用例」;当前四条棘轮余量都是 0,是**下一次加测就打开**的口而不是现在张着的口);**洞二**是静态点数只守住「静态 < 实跑」一向——把一条 `report(...)` 搬进 `if (环境变量) { … }` 的花括号里,`contract` **104/104 全绿**而 `node tests/integration.js` 实跑 **129/129**、README 写着 130,L1 对的是静态点数、L3 三个口径全不变、L4 的 `CTRL` 词表按设计排除 `if`(排除本身是对的)、L5 看的也是静态点数,**五层无一接得住**,这是本目录第一次量出 README 与实跑数可以长期不等而全绿(同一条改成同一行前缀写法则三层同时报红,故这一路是块写法独有的);主线面最实质的一处不在测试上:**问题中心在项目还没有分集时一条都不报**——空项目 / 只有剧本 / 只有主体三态 `Issues.collect` 实测 0 条、`Issues.count` 为 0,而 `Domain.workflow` 同态给得出 `no-script`/`no-subjects`/`no-eps` 与推荐动作(根因是 `collect` 主体是 `episodes.forEach`,主线前三步循环体一次都不进),另量出同一断点两个码(`Domain` 的 `subjects-no-image` 对 `Issues` 的 `subject-no-image`)且没有任何断言钉住两侧码集的包含关系;发布门另有两处——**G1 未过门时回执恒印 `undefined`**(收集侧 push 的是 `action`、渲染侧取的是 `b.label`,实测 `第1集(ready:undefined)`,真实 bug 但只坏 `info` 串、不改判据与计数),以及 **`gates()` 漏注入 `opts.Domain` 时 G1 从 `fail` 降成 `warn`**(`fails` 1→0、原因串换成「Domain 异常」,发布仍不放行故不是门禁绕过,是门禁回执失真,且零断言);缺口面逐条现取:G-11 的 `forge.evolveSystem` 已在注册表而 `cli.js`/`server.js`/`mcp.js`/`cmd-registry`/`commands` **五处零 `evolve`**(欠的不止自动触发,headless 侧连手动都没有),G-13 剩的那句 `'你是专业助手。'` 在 41 个调用点逐个显式给 `system` 的前提下**实测不可达**(收编它不改任何一条实跑提示词),G-03 的低分镜子集**在 `js/issues.js` 里已经算出来了**(`lows`)却只落进给人看的 `detail`、而隔二十行的 `failed-shots` 走同一条路出的是 `shotIds`+`cmd`(故编排层缺的那一环比记账读起来的浅,真正缺的只剩收敛次数登记口径),SK-04 否决维持(门号对照与 `WF_BOARD` 无键两条判据复核成立);另有一条本目录第一次量到的:**SK-25 的「仍欠(G-03)」整段抹掉 486/486 全绿**,而同一手法在 SK-26 上当场红两条——八条「仍欠」段里只有它零断言,G-03 目前只剩 `gaps()` 那个键在记;基线纪律面如实记两条读数——「feature 从最新 integration HEAD 叉」执行率 **2/8**(其余落后 3–14 个提交,W113 落后 14),未酿成事故靠的是集成槽每次「份数/`FLOOR`/条数按合入后 live 重取」的**人工补偿**而不是 feature 侧遵守,以及**核验槽的分支 tip 必然是红的**(`w113-cycle8-audit-b7d1` 自身实测 468/471,三条 FAIL 全在索引完备性上,由集成槽事后补行——本槽因此把索引行、明写份数与记账件 `FLOOR` 写进同一个提交);相对 `master`(仍 `9adcf0f`、零提交)已 425 提交 / 199 文件 / +45686 −937 | 想知道周期 9 这一批每支的实测数字、五层测试契约各自封住了什么而**没封住哪个方向**(尤其「README 数字与实跑数第一次可以长期不等而全绿」那一路)、要判某条缺口的「仍欠」文字有没有断言护着,或要照抄「按四棵树的文件哈希机检 `--ours` 与整树回退」这一手时 |
| [w131-g03-shotids.md](./w131-g03-shotids.md) | G-03 一面:审片修订闭环的**重抽面**改由编排层派生——「该重抽哪几镜」收进 `Domain.reviseTargets`(达标线取 `REVIEW_MIN`、报告判旧回空且与发布门 G3「视为未审」同口径、与当前分镜表取交集、定稿镜不重抽、`order` 取分镜表实位),`WfCore.reviseSubset` 在其上按 `reportId` 回取报告补逐镜修正意见;`cli.js` 的 `episode.produce` 闭环每轮**现取实况**派生 `shotIds`,不再摘上一步回执里那份会与分镜表漂移的 `lowShots`(先在源码上核实过"名单一轮一轮传下去、全程没按该集实况重算过一次"),服务端 `/api/wf/smart-review` 回执、助手工作台摘要与审片完成卡、问题中心 `low-review` 四处消费点同读这一份、五个文件零 `score < 7`;顺手修掉助手摘要在报告判旧时仍会回一句「全部达标」的谎(判旧不出重抽面,那就没资格替这一集背书);**`G-03` 不摘键**(落地一面不摘键,`gaps()` 仍 20 键、值仍两条),SK-25 的 `note` 改成分面写并新点出一条基线没写的欠账——浏览器 `autoSmartReview` 是**逐镜重试**而 CLI 是**整集子集分轮**,两端闭环结构上就不同构,本槽的派生只接得住 CLI 那一侧;另记一条方法性的位——恒等断言的**夹具选错等于没写**(第一版"三镜都在都没定稿镜序一致"的夹具上,照 `perShot` 自筛一遍与真派生逐字相同、变异不红,改成"调序 + 一镜定稿 + 一镜已删"才当场红,并另加源级断言接住"两份实现恰好同值"那一路);数字 live 现取(unit 486 → 491、`contract` 104 → 106、`integration` 130 与 `cli.smoke` 100/102 未动,两个 `FLOOR` 与明写份数一并按 live 抬) | 要动审片修订闭环的重抽面或 `shotIds` 子集口径、要给 `Domain.reviseTargets` 加消费点(别在调用侧补条件)、想知道 `G-03` 还剩哪两面,或要照抄「比对两份实现是否同源时怎么挑夹具」这一手时 |
| [w132-floor-lag.md](./w132-floor-lag.md) | 棘轮下限「该抬没抬」补机器判据(零产品代码):四个 `FLOOR` 字面(三套件用例数表里的单元/集成/CLI 冒烟三格 + 记账件份数一格)判的都是 `live >= floor`,这个方向只拦"往下走"、拦不住**下限自己停在旧水位**——加了用例不抬 FLOOR 全绿(某槽一次松 6 格),把 FLOOR 改小而一条用例没删同样全绿,而两者落到同一个状态:下限守的水位低于实况,那段差额里的用例删起来只剩文档数字一道守卫,棘轮在这几条上等于不存在;判据**不取 `live === floor`**(那正是 W105 拒过的路:每加一条用例都得同轮改这个字面,等于再造一个要人工同步的数),换的是判据类别——**差额本身进对账,对的是上限而不是等号**:`live - floor <= SLACK`(现取 3),差额由此有了可说出口的含义即"能被静默删掉的条数上限",今天无界、本条钉成 3;两种病灶归同一条是因为判据取的是状态而不是动作,失败含义仍只有一个;四个 FLOOR 字面**现取自 `tests/unit.js` 自己的源码**(按 `['套件名', 数字,` 取三格、按 `const FLOOR = 数字;` 取一格),**取不到即红**,改名/挪窝/搬走判据都不会让本条静静退化成恒真句,而 live 那半各按被盯着的那两条用例的既有口径现算、不另立第二种算法;六条变异实测(下限改小 4 格 / 记账件下限改小 5 格 / 真加 4 条用例并按相等判据同步 README 而不抬 FLOOR——三条各红一条且既有相等对账全绿,而**同样三条在基线上一条都不红**;只加 3 条则全绿即常规加测槽不必改第二处字面;`const FLOOR` 改名红在"取数口径失效";抬过头归 W121 既有那条,本条不重复报) | 要加删测试用例、要动任何一个 `FLOOR` 字面或新立棘轮下限、想知道"下限落后实况"这一路现在拦得住几格,或要照抄「把两种动作归到同一个状态判据 + 判据自己的取数口不许静默失效」这一手时 |
| [w133-integration-log.md](./w133-integration-log.md) | 两条槽(W131 重抽面由编排层派生、W132 棘轮下限差额钉上限)并进同一条集成线的记录:两次 `--no-ff` 各取被合入支实际 head,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**(`G-03` 不摘键、两条值一字未动);**两支都叉在基线 tip 上而彼此不知道对方存在**,故第一次合并 `merge-base == HEAD`(零冲突块)、第二次才是真三方(3 文件 4 块),W129 那句「从头到尾没有三方合并这回事」本槽不成立;**本线第一次量出「两侧同值故零冲突块」这个形态是错的**——两边各加一份记账件、各自只看得见自己那份,于是都把明写份数从 144 改成 145 且逐字相同,`git` 判为"两边做了同一件事"零冲突块合上,而合并后目录实况是 146,**这一处恰恰是本槽唯一一处 `git` 不请你过目、只有断言(W105 三方对齐)拦得住的改动**,前十来槽把"两侧同值"当良性的等价关系由此证伪;五个撞车的数一律现取合并后 live 不抄任一父分支(`contract` 106/105 → **107**、单元 491/487 → **492**、明写份数与记账件 `FLOOR` 145/145 → **146**,本文落地后再抬到 147),而**这五处分两类**:README 那三处抄父分支必被逮住(变异 8/9 实测),两个 `FLOOR` 字面**抄了也全绿**(变异 12/13:差 1 ≤ W132 定的 SLACK 3),故 W132 那条新判据**接得住它自己带来的旧值 487、接不住 W131 带来的 491**,"禁止抄父分支"在这两处是纯人工纪律;索引表两行是**并集**不是二选一(变异 7 只摘一行即按份点名报出);契约段那句散文两侧**不是口径之争而是纯插入**(W131 只改数、W132 零删除地插了一段),分辨点是"差异里有没有删除",与 W127 那处形似而处置不同;十三条变异分三组(W132 差额上限三向各红一条含"取数口失效"、W131 派生内容两条报错不混、任务点名三层原样重跑),另量出一条欠账——W131 那条"不摘回执名单"的负向断言**钉的是 `rv.result...lowShots` 字面**,把接收变量改名成 `rv2` 再摘回执全套 492 条一条不红,而正向兜底只要求首轮那次派生调用在场,故也接不住;数字 live 现取(unit 486 → 492、`contract` 104 → 107、`integration` 130 与 `cli.smoke` 100/102 未动但都复核过,三套件条数与不同名字数**连续第八槽全部相等**,索引份数 144 → 147 含本文);**本刻远端零未合但明知周期 9 审计 W130 在飞**(现取核实它还没推分支),故这个零是"还没到"而不是"收敛完了" | 想知道主干现在是什么状态;要合**两条叉在同一个基线 tip 上、又都改了同一批数字**的槽(留神那个零冲突块的同值处)、想知道棘轮差额上限拦得住几格与"抄哪个父分支会被逮住",或要照抄「按'差异里有没有删除'分辨口径之争与纯插入」「把负向断言当字面级判据来变异」这两手时 |
| [w134-integration-log.md](./w134-integration-log.md) | 一条周期 9 独立核验槽(W130 十九支各开 worktree 现跑,零产品代码、零判据改动)并进同一条集成线的记录:一次合并取 head、`--no-ff`,注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks`/`pending` **七个数一个没动**且这次是在**叉点、对侧 tip、合并后 HEAD 三棵树各跑一遍**对出来的,`js/` 与四个 Node 端一个文件都没进 diff;对侧叉在更旧的 `w129 @ 2f6880c` 上而我方已走出七个提交,故是真三方合并、3 个冲突块(明写份数 147/145、索引表尾相邻插入、记账件 `FLOOR` 147/145);**W132 那条差额上限第一次出现"接住一侧、放过另一侧"**——合并后记账件 live 149,抄我方父值 147 差 2 格**全绿**(变异 M3)、抄对侧父值 145 差 4 格**当场红**(变异 M4),而 W133 那次两个父值同为 145、live 146、差 1 故**两侧都放过**,由此把 W133 残留 2 说得更准:**这条判据逮不逮得住取决于 `live − 父值` 有没有越过 SLACK 3,与"抄没抄"无关,叉得越早的槽抄它的 `FLOOR` 反而越容易被逮住**,故"纯人工纪律"这句话仍成立、只是会偶尔被响一次;**W133 记的「两侧同值故零冲突块」本槽不成立而不成立的原因就是分叉点位置**(两侧份数 147/145 不同值故进冲突块、`git` 请你过目),前十来槽归纳的三种数字形态由此读成**分叉距离的三个取值**而不是三种病;**「核验槽的分支 tip 必然是红的」由 W130 自己证伪**——它正文写下这一条并因此把索引行、明写份数与 `FLOOR` 写进同一个提交,本槽现跑核实 `89ee9b7` 自测 486/486 全绿而 `w113-cycle8-audit-b7d1` 仍 468/471,集成槽这次因此不用事后补索引行;索引四行取并集并按波次号把 `w130` 排回 `w129` 与 `w131` 之间(行序仍无断言,人工排的第三次),两个撞车的数一律现取合并后 live(148,本文落地后 149),合并纪律按四棵树的文件哈希机检(两个冲突文件都"三者皆不等"即真融合,整份取 ours/theirs 与回退到叉点内容各零命中);十二条变异分四组(并集索引两向 + 两个父值各自试抄 / W132 差额上限三向含"取数口失效" / W131 派生两条原样重跑 / 摘 `gaps` 与 README 契约段各一条),另量出一条欠账——**摘 `G-03` 确实红 11 条,但没有一条在说"键少了"**,全是源级人设名单与记账对齐**顺带**报出来的,`gaps()` 键集本身零直接判据;接进来的核验件正文一字不改(含它写的 486/145/425 提交那些时点读数),只在本文标注两处语境已变的结论;数字 live 现取(unit 492、`contract` 107、`integration` 130、`cli.smoke` 100/102 **四套件相对基线一个数都没动**,三套件条数与不同名字数**连续第九槽全部相等**,索引份数 147 → 149 含本文);远端本刻零未合,而这个零第一次是"上一槽点名在飞的那条已经落地" | 想知道主干现在是什么状态;要合一条**叉得比基线早好几个提交的核验槽**(留神撞车形态与分叉距离直接相关)、想知道棘轮差额上限在什么条件下才逮得住抄父分支、要判"摘 gaps / 摘仍欠段"到底是谁在拦,或要照抄「按四棵树的文件哈希机检 `--ours` 与整树回退」「把上一槽写下的结论用现跑数据校准而不是照抄」这两手时 |
| [w135-g1-receipt-label.md](./w135-g1-receipt-label.md) | 发布门 G1 未过门回执的两处失真(`js/release-core.js`,门禁判据一字未动):**收集侧把每集推荐动作按 `action` 存、渲染侧读 `b.label`**,三元的条件读对了而取值读错,于是凡要显示推荐动作的那一集一律稳定印成 `集名(状态:undefined)`——浏览器 `js/release.js` 同一段存读都是 `label` 故只坏 headless 这一份,本槽把 headless 的键名改成 `label` 而不是把渲染改成 `b.action`(两端此后逐字同形);坏的只有 `info` 那一句(`status`/`fails`/`warns`/`overall` 改前改后逐字相同),消费点是 `release-check` 的 JSON、MCP `hujing_release_check` 与 `/api/wf/release` 的成功回执,而 `brief()` 不摘 `info`,故跑得最多的 `exec project.release` 那条路恰好看不见它——**这正是它能一直活着的原因**;第二处是**漏注入 `opts.Domain` 时 G1 从 fail 降成 warn、`fails` 1→0**(逐集调用当场 `TypeError`、被 G1 自己的 `catch` 兜住),两种 `overall` 都不在 `PASS_OVERALL` 里故门没被绕过、失真的是"未过门项 0 条"这句假话,处置是在 G1 开头显式判一次注入按 fail 记并点名缺什么、`catch` 那条(Domain 自身抛错)一字未动,**为什么不算抬门**分三条写明(判据没动 / 改的是"判不出来"这一格而它此前在声称一件没发生的事 / 浏览器那半的 warn 判的是"页面模块没加载"、与"调用方漏传参数"不是同一件事,故不动浏览器);三条变异各红一条且报错不混,回执那条另把两端同形一并钉住(此前 G1 的 `info` 零判据,靠人眼看而印出来的恰是 `undefined`);**如实登记未动的一面**——G4/G5/G6 聚合那段的 `catch (_) {}` 漏注入时静静报 0 镜全 pass,是同类失真的另一面,建议与 G1 共用一次注入判定而不是三门各补一格;数字 live 现取(unit 492 → 494,`integration` 130 与 `cli.smoke` 102 未动,单元 `FLOOR` 与明写数、记账件份数 147 → 148 一并按 live 抬) | 要动发布门回执的字面或 blocker 结构、要给 `gates()` 加调用点(留神 `Domain` 是必注入位)、想知道"两端各写一份渲染"这类失真怎么用一条同形断言钉住,或要收 G4/G5/G6 那半时 |
| [w136-sk25-owe-guard.md](./w136-sk25-owe-guard.md) | 给 SK-25 的「仍欠(G-03)」段补护栏(零产品代码,`js/skills.js` 一个字未动):周期 9 审计报的「抹掉 SK-25 那段全绿、同手法抹 SK-26 红两条」先在基线上重量一遍并**把范围铺开到全部八条「仍欠」段**——逐段只删带 `仍欠` 那一行各跑一次全套,SK-02 红 1 / SK-03 红 6 / SK-04 红 2 / SK-10 红 1 / SK-11 红 1 / SK-23 红 1 / SK-26 红 2,**只有 SK-25 红 0**,故审计点名属实且它是唯一一条,其余七条各有自己形态的护栏(收编键不许挤进仍欠段 / 仍欠段点名的文件此刻确实还有内联人设 / `infra` 三条 `pending` 记账)一律不重复造;漏的成因有两层——W131 落 `G-03` 重抽面时新加的两条用例钉的**全是已落地那半**(落地面有判据、欠账面靠自觉),叠上 `G-03` 的关联索引里有**两条** skill 故既有那句「`G-03` 关联索引应仍在」在 SK-25 单独摘键时只是从两条变一条、长度仍非零而不红;补的一条对照 SK-26 现有护栏的写法立**两向**——前半钉段与段里的锚点(`仍欠(G-03)` 在场、两个余面各要"名词 + 源码标识符"成对的两个锚点、已交账那半不许挤回欠账段、`note` 不许另起一句宣布 `G-03` 清账、`gaps` 与关联索引不摘键),后半**反向逐处对照源码**钉住点名的余量此刻确实还在(两端各就地钳一份 `maxRetry` 缺省而 `domain.js`/`wf-core.js` **按名字(大小写不论)与按形状(1-5 那道钳位)两向都零命中**,并以同一份 `domain.js` 里 `D.REVIEW_MIN = 7` 作对照项——"有登记口径"与"没有"并排放着这一面才算量得准;形态那一面取的是**循环的嵌套方向**即 `for (const s of targets)` 在 `for (let attempt` 之前、浏览器一处不引 `revise*` 派生,而不是两条循环各自在不在);九条变异各红一条且报错句互不相同(段不在 / 锚点缺 / 键被摘 / 已落地挤进欠账 / 源码真收编了记账没跟上,末一类按驼峰改名与整个换名两路各测一次,它们正是"欠账还上了要同轮改记账"的提醒);另记一条方法性的位——**变异体里顺手写的注释会替被测断言把活干了**:「`domain.js` 真加一份 `D.reviseMaxRetry`」这条第一次带着一句写小写 `maxRetry` 的注释故看着是红的,去掉注释再跑全套才转绿,暴露出原判据 `/maxRetry/g` 区分大小写、根本没命中驼峰改写(由此补出名字/形状两向);数字 live 现取(unit 492 → 493、`contract` 107 → 108、`integration` 130 未动,单元 `FLOOR` 与记账件 `FLOOR` 及明写份数一并按 live 抬) | 要收编 `G-03` 剩下那两面(收敛次数登记口径 / 两端闭环形态)、想知道注册表八条「仍欠」段各自被什么护着,或要照抄「给一句欠账记账立双向判据:段里锚点成对 + 反向对照源码」这一手时 |
| [w137-gate-catch.md](./w137-gate-catch.md) | 发布门 G4 过期镜 / G5 未确认镜 / G6 失败镜的聚合外面兜着一个 `catch (_) {}`:三门是**同一次遍历**的三个投影,遍历失手时它们拿 `agg` 的初值照常报 `pass`「0 镜」——漏注入 `opts.Domain` 时一集都没遍历却宣称"一镜不缺",遍历到第二集才抛时更坏(G4 拿第一集那半截数报「2 镜 fail」、G5/G6 报「0 镜 pass」,三个数没有一个是全量算出来的,而回执上看不出任何异常痕迹);处置是三门**共用一个 `aggErr`**(W135 交接里点名的坑:分开补会出现"G4 说判不出来、G5 说 0 镜"这种自相矛盾的回执),缺注入按未过门 `fail`、`Domain` 自身抛错按 `warn`,两级与 G1 那条 `catch` 对齐,`fails`/`warns` 跟着记(只改 `status` 不计数的话 `overall`/`score`/`brief().blockers` 还是按"没有未过门项"算),失手时 `info` 不再印那个算了半截的镜数;**判据一个字没动**(注入齐全时仍"计数非零即 fail",新用例第一段正面钉住三门在齐备夹具仍 `pass`),既有三个调用点都显式注入 `Domain` 故零行为变化;四条变异各自能红且报错不混(退回基线形态红 2 条、缺注入那格降 `warn` 只红 1 条、只补 G4 不补 G5/G6 两条都点名 `g5-unconfirmed`、去掉计数红在 `fails` 0 与 `warns` 2 上);两条用例特意**不断言 `fails` 绝对值**,故 W135(G1 那半)合入前后同一份断言都成立;数字 live 现取(unit 492 → 494,`integration` 130 与 `cli.smoke` 100/102 未动但都复核过——两项失败在改动前基线现跑同样存在,索引份数 147 → 148 含本文) | 要收"空 catch 让门禁回执失真"这一类残留、或要照抄「同一次遍历的多个投影必须共用一个失手结论」「两种失手记两级(接线错 fail / 运行时异常 warn)」「断言避开会被并行槽改动的绝对计数」这三手时 |
| [w138-issues-empty.md](./w138-issues-empty.md) | 问题中心在**无分集**时一条问题都收不出来(空项目/只有剧本/只有主体实测 0 条):`Issues.collect` 的状态类判定全在 `(p.episodes||[]).forEach` 的循环体里,循环体不进就没有结论,而同一个项目交给 `Domain.workflow`,`script`/`subjects`/`eps` 三步一条不差地给得出 `no-script`/`no-subjects`/`no-eps` 与推荐动作——**同一个项目两个面结论相反**(流程条说主线断在剧本,问题中心说"🎉 主线畅通",CLI `hujing issues` 与 MCP 同读这份投影故回 `total: 0`),基线上这个 0 还被一条 `空项目零条` 的用例钉着;另有一处**码名分裂**(问题中心 `subject-no-image` vs 流程条与 `flow-tpl` 的 `subjects-no-image`,两侧谁都没错、只是没有一处判据说它们必须同词,按码筛的消费方挑错一个就静默筛空、看起来与"这项没问题"一模一样)。落法不在问题中心补 `if`(那是把 `p.script || p.extractDone` 这类判据写第三份、必漏 `extractDone` 那一半),而在两侧共同的上游:新增 `Domain.gateBlockers(p)` 收下三步的项目级阻塞项(判定输入全是项目对象本身,与逐集推导无关,故能整块抽出而不牵动 `epStates`;四行逐字取自 `workflow` 原来的内联表达式含库空/缺图那处 `else if` 互斥,只多 `step`/`count` 两列),`workflow` 改读它且 blockers 产出形状逐字节不变;问题中心新增与 `REMINDERS` 同形的前置门槛投影表 + `Issues.gates()` 只读投影,**键就是 `Domain` 的码**、表里只定危险级/落点/一行说明、标题原样取 `Domain`、表外的码一律不投;四条一律走导航不挂命令(前置三步没有一个是能替用户按的计费动作);**危险级是本槽唯一一处不能照搬上游的取值**——除主体缺图沿用中危外三条门槛态一律低危,因为发布门 G2 只数高/中危而这三件各有自己的门(G1 逐集判 `ep.content`、G3 判有无分集、G9 判缺图数),抬成中危等于让 G2 转而去读 `p.script`,实测把 `no-script` 抬一格会让 release 与 split 套件**连红 12 条**(那些夹具都是没写 `p.script` 的成片项目);码名收敛为 `subjects-no-image`(全仓核实旧码只在投影核与用例两处、无任何分支按它取值,故不带兼容层);断言 4 条含**两侧码集双向相等**(只钉一向拦不住另一向)与源级单源(三码在 `domain.js` 各只一处字面、`no-script` 恰三处),六条变异逐条红(退回基线行为红 6、码名分裂红 5、`workflow` 退回内联"不改行为只抄第二份判据"红 1 且只有源级判据接得住、危险级抬一格红 12、`gateBlockers` 少一档连 `flow` 套件既有那条一起红即证下游确同读一份、`GATES` 表少一行红 3);数字 live 现取(unit 492 → 496、`contract` 107 → 108、`integration` 130 与 `cli.smoke` 100/102 复跑未变,两个 `FLOOR` 与明写份数按 live 抬) | 要改问题中心收哪些条目、要动 `Domain.workflow` 前三步的阻塞码或危险级、想知道"前置断点为什么一律低危"与发布门 G2 的边界,或要照抄「把两个面共同的上游抽成一份、消费侧只按码投影 + 两侧码集双向钉恒等」这一手时 |
| [w139-integration-log.md](./w139-integration-log.md) | 两条槽(W135 发布门 G1 未过门回执两处失真、W136 给 SK-25「仍欠(G-03)」段补双向护栏)并进同一条集成线的记录:两次 `--no-ff` 各取被合入支实际 head,两支的 `merge-base` 都是更旧的 `w133` 而基线已走出两个提交,故**两次都是真三方**(5 文件次 8 冲突块,`js/` 零冲突块),注册表 41 条与 `CHECKS`/面表/`gaps`/`KB`/`playbooks` **七个数一个没动**且这次是**结构性成立**——五个注册表文件相对基线零 diff,不靠现跑对数;**本线第一次量出「只有一侧在 diff 里」这一形态靠的是运气而不是 `git` 判得对**:第一次合并时主 `README.md` 我方一字未动,`git` 原样搬进 W135 那句(含它自己算的「单元测试(494」)且**连冲突块都不给**,它这次对的唯一原因是我方那两个提交(W130 审计合入 + W134 记账)**一条用例都没加**故 492+2 恰等于 live 494,同一个静默窗口本槽踩了两次(契约段条数 107→108 同理),两次都恰好对而**换个基线就会静默错**,前几槽把这一形态当良性由此读准:它是三种数字形态里**唯一连 `git` 都不提醒**的一种;W132 那条差额上限本槽**两侧父值全部放过**且其中一侧压在边界上(记账件 live 151,逐格实测 150 差 1 / 149 差 2 / **148 差 3 恰等于 SLACK** 全绿,147 差 4 才红;单元那格 494/493/492 三值全绿),三槽连读(W133 差 1 两侧都放过、W134 差 2 与差 4 放过一侧逮住一侧、本槽两侧都放过含一次压线)得出**它响不响只由 `live − 父值` 与 SLACK 的大小关系决定,与叉点与"抄没抄"都无关**,是概率事件不是纪律;主 `README.md` 那一块**不是数字二选一**——逐 opcode 比对出两处差异(一个数 + W135 零删除插进去的一句),按 W133「差异里有没有删除」判为纯插入,故取我方整句只换数字、对侧那句一字不删;索引三行取并集按波次号排(变异只摘一行即按份点名报出,红 3 条);七组变异各红各的(W135 两处失真退回各红 1、抹 SK-25 仍欠段红 1 **而同一手在基线上 492/492 全绿**故 W136 的护栏确是新覆盖、两个 `FLOOR` 抄父值四格与三格扫描、README 三处抄父值各红 2/1 条、索引摘行红 3 条),另反向抽查要保的 `Domain.reviseTargets` 派生(退回调用侧自筛 `score < 7` 红 2 条且含义不混);没删测按名成集双向比对(基线独有 0 条、新增恰 3 条并逐条点名);数字 live 现取(unit 492 → **495**、`contract` 107 → **108**、`integration` 130 与 `cli.smoke` 100/102 未动但都复核过,三套件条数与不同名字数**连续第十槽全部相等**,索引份数 149 → 152 含本文,两个 `FLOOR` 一并按 live 抬);**本刻远端未合 2 条**(W137 gate catch、W138 issues empty,都叉自更旧的 `w133`),这个 2 是"任务明确要求留着"而不是"还没到",且 W137 收的正是 W135 自己登记的 G4/G5/G6 那半、下一槽会与 W135 改过的同一段落撞车 | 想知道主干现在是什么状态;要合**两条叉自同一个更旧提交、又都改了同一批数字**的槽、想知道「只有一侧在 diff 里」这一形态什么时候会静默错、想知道棘轮差额上限在什么条件下才逮得住抄父分支,或要照抄「按逐 opcode 比对分辨纯插入与口径之争」「把要保的功能反向变异一次当验收」这两手时 |
| [w140-gaps-keys.md](./w140-gaps-keys.md) | `Skills.gaps()` 键集补直接判据(零产品代码):W134 交接的「摘 `G-03` 红 11 条却没有一条在说键少了」本槽收它——现跑复核那 11 条的实际判据(九条是源级人设用例收尾顺带写的 `Object.keys(gaps()).length === 20`、一条判条目字段、一条早退在 `validate()` 上连自己那两句键集断言都没跑到),并用三条变异把「基数不是键集」量成读数:`G-03` → `G-99` 基数仍 20 时九条全绿、**挑一个没有条目级断言护着的编号改名(`S-06` → `S-99`)时 `unit` 492/492 与 `integration` 130/130 全绿**、摘 `S-06` 键也只报得出「键数 19」而说不出少的是谁;新增 1 条 `contract` 用例把键集钉成四层——逐字点名 20 键字面、只收缺口图谱(`w1-pipeline-skill-map.md` 的 `G-xx` 表 + `w1-selected-skills.md` 的 `S-xx` 表)登记过的编号、**反向对账**(图谱有而键集没有的必须在用例里明写闭合/待登记理由,现为 `G-06`/`S-02`/`S-08`,已声明闭合的又冒回键集同样红)、**落地一面不摘键**(`G-03`/`G-11`/`G-13` 三条各有一面已落地、关联能力早无 `pending` 而键仍在案,单独点名);三种恒真句(键集等于 `REG` 并集 / 每键至少一条值 / 值都是条目 id)对着 `gaps()` 实现逐句筛掉没进用例;八条变异实测(M3 从 0 红变 1 红且那 1 红就写着键集哪里不对;M5 只改字面被第二层接住、M6 改成 19 键被第三层接住、M8 图谱读空时报「取数口失效」不许退化成恒真;**M7 如实登记为有意留的人工出口**——三处一起改并写下闭合理由才转绿);`js/skills.js` 一字未动,不清 `G-11`/`G-03`/`G-13`,数字 live 现取(unit 492 → 493、`contract` 107 → 108,单元与记账件两个 `FLOOR` 一并按 live 抬) | 要摘/改/新登记缺口编号(先看第 6 节那三处怎么改)、想知道「键数对得上」为什么不等于「键集没变」、要判某条 gaps 相关断言到底在判条目字段还是投影键集,或要照抄「把投影的键集接到两份不在代码里的既有记录上,让静默改动变成一处要写理由的显式声明」这一手时 |
| [w141-report-if.md](./w141-report-if.md) | 静态点数的**反方向**补判据(零产品代码):`integration.js`/`cli.smoke.js` 的用例数按行首 `report(` 静态点数对账,围着它的五层判据(L1 行首点数==调用数==README 数 / L2 契约段自数 / L3 块链禁循环 / L4 禁 helper+须裸表达式语句 / L5 下限棘轮与差额上限)封的方向**清一色是"一处静态登记点跑出多条"即 静态 < 实跑**,反方向零判据——把一条 `report(...)` 原样搬进 `if (环境变量) { … }` 的花括号里,四个静态口径(行首点数/调用数/名字面数/不同名数)一个不变而分支不成立时实跑少一条,基线 `42b2798` 上现跑实测 **`unit` 492/492 全绿而 `integration` 129/129、README 写着 130**(本目录第一次由这一路量出"README 数字与实跑数可以长期不等而全绿"的是周期 9 核验件,本槽收它);同一条改成同一行前缀写法反倒三层同时红,故漏在外面的只有**带花括号**这一形状;判据**不列 `if`/`switch`/`try` 词表**(`for await` 那条已经上过"词表列不全"这一课,而条件分支比循环更宽),反过来立**白名单**:登记点的块链上只许出现"进了 `main` 就一定执行"的块即 `main` 函数体与就地开的裸 `{ }` 分节块,新写法默认落在红的一侧(`switch`/`try` 从没进过任何词表也各红一条);**不是"禁止一切 `if`"**——只看那条 `report` 的块链,两个套件里其余合法分支一处不碰;归口做得**窄**(块头写着 `for`/`while`/`do`/`function` 或以 `=> {` 收尾的留给 L3/L4 报,漏归口只多红一条、归口过宽才开洞),`{` 之外的括号层跳过(登记点落进表达式归 L4 裸语句那点,不跳过多行 `forEach` 会连坐三条);归口判词**先抹掉块头里的字符串字面量**——`if (mode === 'do-it') {` 里的 `do` 会让归口误判成"归上面报"而上面一个也点不到,拆掉这一步同一条变异全绿(顺带量到既有词表判据自己也在读字符串:`'for-real'` 会让 `for await` 那条误报、`'function'` 会让函数体那条误报);八条变异实测(带花括号的 `if`/`switch`/`try`/字符串诓归口四条各只红本条,同一行前缀/多行 `forEach`/箭头 helper/`for await` 四条各红各的而**本条一次没报**,分工不串味) | 要往 `integration.js`/`cli.smoke.js` 加用例而被这四条包装禁令里任一条拦住时(正解是拆成逐条无条件登记,不是删测也不是改窄判据)、想知道"静态点数代表实跑条数"这件事今天还剩哪一路没接住(`main` 里提前 `return`/`throw` 那一路仍开着,见本文交接 1),或要照抄「词表判据收不住就反过来立白名单 + 把别人辖区窄归口」这一手时 |
| [w142-integration-log.md](./w142-integration-log.md) | 两条槽(W137 发布门 G4/G5/G6 聚合空 `catch`、W138 无分集时问题中心也收得出前置断点)并进同一条集成线的记录:两次 `--no-ff` 各取被合入支实际 head,两支都叉自更旧的 `w133` 而基线已走出四个提交,故**两次都是真三方**(6 文件次 9 冲突块,`js/` 零冲突块),七个数一个没动且五个注册表文件相对基线零 diff;**任务口径预告的那处"真撞"撞的不是代码而是判据**——W135(已在基线里)与 W137 改的是 `gates()` 同一个函数,但两处相隔九行未改的 G3 段,`git` 把两侧 hunk 各自搬进去**零冲突块**合上(四棵树哈希互不相等,是真融合),真红的是 W135 那条用例里的 `assertEq(bare.fails, withD.fails)`:它写在"漏注入时只有 G1 多一门 fail"的前提上,W137 让 G4–G6 在同一情形各多一门,同一夹具当场 `bare 4 / withD 2`,**代码合得上、判据合不上而两者不在同一个文件里,`git` 对后者零提示**,处置是把那三门从两侧计数里各自剔出去再对账(那三门另有 W137 自己的用例守着)并变异复核改写没削弱它(把 G1 缺注入改成记 fail 不计数,仍红在「期望 1,实际 0」);**W132 那条差额上限本槽第一次真的逮住人**——合并后 live 单元 501 / 记账件 154,单元那格三个父值 497/496/495 差 4/5/6 **全红**、记账件那格对侧的 148 差 6 红而我方的 152 差 2 绿,四槽连读(W133 全放过、W134 放过一逮住一、W139 全放过含一次压线、本槽单元三值全逮住)得出**判据没变、变的是并槽体量**:一次合进 6 条用例 2 份记账件才让 `live − 父值` 整体越过 SLACK 3,**小槽仍必然被放过**;**W139 残留里「数字的静默窗口今天没有任何判据」本槽实测不成立**——契约段条数三侧同值 108 而 live 109(本线第三次踩这个窗口、第一次踩错),`git` 零冲突块放行但精确对账当场红,逐处变异过后该收窄成「**数字有判据、散文没有**」:W138 在主 README 把问题中心那整段改写近千字而我方一字未动,`git` 整段取对侧且没有任何一条用例读那段话;两处主 README 同行冲突**形似而处置相反**(W137 那次对侧是纯插入故取我方整句只换数字,W138 那次对侧删了「,空项目零条」四个字属口径之争故取对侧正文改动同时留住 W135 那句);十八组变异各红各的(W137 两条退回/只补 G4/降 warn、W135 两处失真退回、W138 摘门槛表一条与**危险级由 low 抬到 mid 红 12 条**与流程条再写一份码各自打在双向码集那条的正反两向、两个 `FLOOR` 与三处 README 数字抄父值、索引摘行红 3 条),另反向抽查 `reviseTargets` 派生、`SLACK` 字面与 SK-25 护栏三样都还有判据护着;没删测按名成集双向比对(基线独有 0 条、新增恰 6 条并逐条点名);数字 live 现取(unit 495 → **501**、`contract` 108 → **109**、`integration` 130 与 `cli.smoke` 100/102 未动但都复核过且 2 项失败在 `master` 独立 worktree 上同跑同名同表现,三套件条数与不同名字数**连续第十一槽全部相等**,索引份数 152 → 155 含本文);**本刻远端未合 2 条**(W140 gaps keys、W141 report if),这个 2 是"任务明确要求留着"而不是"还没到" | 想知道主干现在是什么状态;要合**改同一个函数但不在同一段**的两条槽(留神代码零冲突块而判据当场红那一路)、想知道棘轮差额上限在什么体量的并槽下才逮得住抄父分支、想知道合并静默窗口对数字与对散文的守卫差在哪,或要照抄「两条判据真撞时怎么各留各的门再变异复核没削弱」「按'差异里有没有删除'把同一行的两次冲突判成相反处置」这两手时 |
| [w143-evolve-headless.md](./w143-evolve-headless.md) | G-11 的 headless 面:专家自进化补出**人手、无头**入口。先核实基线上 `cli.js`/`server.js`/`mcp.js`/`cmd-registry`/`commands` 五处确实零 `evolve`(唯一实现是 `js/experts.js` 的 `evolveExpert`,四个触发点全在浏览器;`forge.evolveSystem` 虽在注册表却只有浏览器一个消费点,W88 原话「收编解决的是可覆盖不是可 headless」),判为补出口而非停工;**先下沉再开口**——蒸馏结果是写死进 `persona` 且无撤回口,故落点判定 / 提示词两半 / 条款规整 / 落 `persona` 四步一并进 `js/wf-core.js` 双端单源(`evolveTarget`/`evolveSystem`+`buildEvolveUser`/`evolveClauses`/`evolveApply`,预置副本造形 `presetCopyOf` 同下沉),两端一字不抄、环境差异(`Store.save`/五件套/toast vs `wfLLM`/`wfSave`)留在各端调用点;出口是领域命令 `expert.evolve`(`needs` 为空——四端唯一一条项目外命令,不吃 `pid` 故也不推 `next`),浏览器走 `Experts.evolveExpert`(改回结构化 `{ok,code,...}` 八码,四个按钮行为一字未改)、CLI `exec` 与 MCP `hujing_expert_evolve` 走服务端 `/api/wf/evolve-expert`(两道闸仍在 `wfLLM` 之前 400 拦下、覆盖表显式传、预置专家同样落自定义副本、无新增条款 `changed:false` 且副本不入库不写盘);CLI 那侧另修一处**非平凡**的:`api()` 的 exit 映射把 400 与"连不上服务端"并成同一个 1,故给 `CliError` 挂原始 `status`,只有 400 走 `blocked` 而 404 原样抛;**记账 `cmds` +1 而 `steps` 一条不加**(`steps` 是 playbook 投影的来源,编排步序里出现"进化"就等于把自动蒸馏写成口径),两条断言分别钉住不在 `steps` 与不在投影里;**`G-11` 未摘键**——四面里"只有浏览器一个出口"这面落地,"自动进化"仍欠且本槽反把人手出口从一个变成四个;七条变异各红各的(条款上限 / 副本复用 / 退回内联提示词 / `steps` 掺进进化 / 服务端删闸 / 删"无新增不写盘" / CLI 删 400 分支),末两条**只有起真实服务与真实子进程的套件抓得住**,故本槽三个套件同时加测(unit +3、integration +11、cli.smoke +5);数字 live 现取(unit 495 → 498、`integration` 130 → 141、`cli.smoke` 102 → 107、领域命令 12 → 13、MCP 工具数按 live 订正 35 → 38,三套件 `FLOOR`、记账件份数 152 → 153 与明写数一并按 live 抬) | 要给一个"只在浏览器里有"的动作补 headless 出口(先判该不该下沉:结果落在哪、有没有撤回口)、要加一条**项目外**的领域命令、想知道 CLI 怎么把服务端的前置拦截与"连不上"分开,或要照抄「命令进 `cmds` 但有意不进 `steps`」这一手时 |
| [w144-index-order.md](./w144-index-order.md) | 目录索引表的**行序**补机器判据(零产品代码,`js/` 一行未改):既有三条索引契约钉的全是"哪些行在场"——集合双向相等、按份点名、份数三方对齐加下限,**一行也不管"排在哪"**,故整行搬到表尾时四条一起全绿,而"合入新记账件要插回它那个波次的位置"已经人工排过三次(W134 把 `w130` 排回 `w129` 与 `w131` 之间那次写明"行序仍无断言");本槽把次序立成判据——**波次号沿表往下不许倒退**(同一号下多份记账件的先后不管,`w1-*` 那八行照旧任意排),判据有意不取「波次号连续」(W105 §2.2 已否掉:W96/W98 这类如实登记的未合号本就空着,连续性会把如实记录判成红)、也不取"按 git 时间或文件名字典序"(`w9` 与 `w10` 的字典序恰好是反的,按名排等于要求乱序);本条**自足不借道**并自带取数口守卫——取到的 wNN 行数须等于目录里 `wNN-*.md` 的份数,表被挪走或那条行首正则被改坏时先红在取数口那一句,不许静默退化成恒真;六条变异实测(把 `w130` 整行搬到表尾 / 把 `w139` 整行搬到表首 / 整表逆序 —— 三条**在基线上一条不红**而本槽各红 1;同号内两行对调两边都全绿,如实登记为不判的那一面;文件连同索引行一起删仍红 2 条即既有完备性一处没丢、本条的取数口另报一条;只删索引行红 4 条);数字 live 现取(unit 495 → 496、`contract` 108 → 109、`integration` 130 未动,单元 `FLOOR` 与记账件 `FLOOR` 及明写份数一并按 live 抬) | 要往目录索引表加行(插回你那个波次的位置,别追加到表尾)、合入时取并集排行,或想知道索引契约现在钉住了哪四件事、哪一件仍没钉 |
| [w145-browser-gate-catch.md](./w145-browser-gate-catch.md) | 浏览器发布门 G4/G5/G6 那段 `catch (_) {}` (`js/release.js`,门禁判据与通过线一字未动):三门的镜次计数出自**同一次** `Domain.episodeState` 遍历,外面兜着一个空 catch,于是 `agg` 停在初值的两种成因(`Domain` 没加载走 `if` 假分支 / 遍历抛错被吞)与"真的查过真的是 0"在三门看来完全一样,照常印「0 镜 → 通过」并各挂一个绿勾——那句话宣称"过期/未确认/失败镜都查过了,一镜不缺"而实际一镜未查,**半途抛出更坏**(实测两集夹具:第一集 `{stale:2,unconfirmed:1,failed:3}`、第二集抛错,改前三门报 `fail` 2/1/3 镜,三个数没有一个是全量口径而弹窗上与真结论长得一模一样);两个场景改前的 `overall` 都不是 `pass`/`cond-pass`(G1 与三门读同一个 `episodeState`,`Domain` 缺席时 G1 自己先 `warn`,加常在的 G10 那条 `warns` 至少 2),**故门没被绕过、失真的是回执**,消费面是弹窗门禁列表的 ✅/「通过」、tab 角标的 `blockers`(场景 A 少报 3 项)与一键处置的 `fix.shotIds` 子集;处置是把"判不出来"从初值里分出来——缺模块与遍历抛错各点名一句、三门写在**同一个分支**里(`agg` 本就是一次遍历的产物,各补一格会出现「G4 说判不出来、G5 说 0 镜」这种自相矛盾的回执)、判不出来时不挂 `fix`(要处置的那批镜头正是没算出来的那批),两句文案逐字取自同文件 G1 那两条分支;**浏览器记 `warn` 而 headless 记 `fail` 是有意的**——前者判 `typeof Domain` 说的是"页面模块没加载"(同文件 G1/G2/G7/G8 缺模块一律 `warn`,文件头写明这条降级纪律),后者判 `opts.Domain` 说的是"调用方接线错",两端因此不同值并各有用例钉着;四条变异**分工可辨**(整段退回红 2 条、缺模块那格改 `fail` 红 2 条、只退遍历 `catch` 红第二条、只删缺模块守卫红第一条且报错文案不混);如实登记未收的两处——`buildReleaseZip` 抓分镜文件那处空 catch(失手少几个文件而 `summary` 不提)与底部 `Bus.on('*')` 那处,`gaps` 一个不摘、`js/release.js` 对 `Skills` 照旧零引用;数字 live 现取(unit 495 → 497、`integration` 130 与 `cli.smoke` 100/102 未动但都复核过,单元 `FLOOR` 与主 README 明写数、记账件份数 152 → 153 一并按 live 抬) | 要动发布门 G4/G5/G6 的聚合或那三门的回执、想知道"同一段代码两端有意不同值"该怎么划界并各自钉住,或要照抄「把判不出来从初值里分出来、并用单边变异证明两条用例各守一格」这一手时 |
| [w146-integration-log.md](./w146-integration-log.md) | 三条**零产品代码的判据槽**(W140 `gaps()` 键集直接判据、W141 `report(...)` 不许坐在会跳过的条件块里、W144 索引表行序按波次号递增)并进同一条集成线的记录:三次 `--no-ff` 各取被合入支实际 HEAD,三支的 `merge-base` 两两都是更旧的 `42b2798`(W144 那支叉在 `2851d97`),故**三次都是真三方**(每次 3 文件 6 块、共 18 块),而 `js/`/`server.js`/`cli.js`/`mcp.js` **一个文件都没进 diff**——这是本线第一次一整槽只加判据,七个数因此是结构性成立;三个自称数 493/493/496 **全部低于基线的 501**,合并后 live 是 504;**W142 残留 6「索引表行序仍无断言」本槽由 W144 收掉,且它回溯覆盖整张表**(变异把 `w130` 那行搬到表尾当场红并点名前后两份,而同一手在基线上 501/501 全绿),顺带把"只删一行索引"的报错从 3 条抬到 **4 条**(多的那条是 W144 自带的取数口守卫);**W142 残留 2「散文那半 git 不提醒且没人接」本槽只对了一半**——本槽两处真散文改动(W141 的条件块白名单一层、W144 的行序判据一句)**都进了冲突块、全槽零处静默取对侧**,因为那句散文与 `contract` 条数**同行**而那个条数每槽必改,故准确形状是「散文仍无判据,但只有与每槽必改的数字不同行时 `git` 才不提醒」;契约段那一块**本线第一次两侧都是纯插入**(相对同一叉点双向零删除),二选一会丢掉一整段故取**并集**并按对侧那段的锚点插回;`FLOOR` 抄父值两格形状完全一样——三个被合入支的父值(差 5–11)全部逮住、**基线自己那个恰好压在 SLACK 边界(差 3)上被放过**,五槽连读得出"抄叉得早的支从来都响,抄基线那个响不响由并槽体量决定";没删测按名成集双向比对(基线独有 **0 条**、新增恰 **3 条**并逐条点名),反向抽查任务点名要保的五样(`gateBlockers`+`Issues.gates`、`release-core` G1 与 G4–G6 `aggErr`、`reviseTargets` 派生、`FLOOR` 的 `SLACK` 仍是 3、SK-25 护栏)逐样变异一次各红各的;数字 live 现取(unit 501 → **504**、`contract` 109 → **112**、`integration` 130 与 `cli.smoke` 100/102 未动但都复核过,不同名字数与用例数**连续第十二槽相等**,索引份数 155 → 159 含本文);**本刻远端未合 2 条**(`w143` evolve headless、`w145` browser gate catch),这个 2 是任务明确要求留着 | 想知道主干现在是什么状态;要合**三条只加判据、彼此互不相干而叉点不同**的槽、要判"两侧都往同一句散文里插东西"该取并集还是二选一、想知道棘轮差额上限现在把哪一类父值分在哪一层,或要照抄「把并集索引行按波次号归位并让新合进来的行序判据回溯复核整张表」这一手时 |
| [w147-plans-gates.md](./w147-plans-gates.md) | 计划层还留着**全仓第三份**前置门槛判定:`js/plans.js` 的 `TODO_OF` 里 `project.extractSubjects`/`subject.generateImage`/`project.splitEpisodes` 三个取材器各自内联判了一遍「剧本在不在、主体库空不空、缺几张图、有没有分集」——主体那两半与 `Domain.gateBlockers` 恰好同结论(缺图算式逐字相同、库空时 `noImg` 自然为 0 与那处 `else if` 互斥等效),**剧本那半不同口径**(计划层 `String(p.script||'').trim()` vs 门槛派生 `p.script || p.extractDone`),后果不是文案不对而是**整份计划推不出来**:提取过主体但没存整本原文的老项目,流程条画着「剧本 ✓ 主体 ✗ 分集 ✗」、问题中心列着两条低危,点「📋 按主线生成」却弹 `当前主线无待推进事项,无需计划`(六夹具在基线上现跑逐格列出,另有一格更隐蔽:老项目 + 主体缺图时计划出得来但**少了拆集那一步**,补完图就没有下一步了);**收口的不是形状而是判据**——W138 交接里那句顾虑仍成立(计划步答「该跑哪条命令」、阻塞项答「这一步卡在哪」,`label` 一个是动作词一个是断点词),故不把阻塞项直接当计划步用,而是 `gateMap` 一处把 `Domain.gateBlockers` 收成码 → 阻塞项的表、三个取材器**按码取材**、码 → 步骤文案的映射显式留在计划层;三处细节各有理由(提取与拆集**各判两码**是投影步序的前置关系而非把门槛判两遍、缺图数取阻塞项自带的 `count` 不重数、`gates` 经 ctx 注入且每次现取不缓存以支持「按主线重建」);行为变化**只有 `extractDone && !script` 这一类老项目那一格**(其余夹具逐字节不变,既有 9 条 plans 用例一条未改),且如实登记它往下走会遇到什么——`project.extractSubjects` 自带"拼各集正文"的回退故真跑得动,`project.splitEpisodes` 只认 `p.script` 故会回 `blocked / no-script` 并被 `execStep` 记成 `failed` 带上「请先上传剧本」,**这仍比基线的"无待推进事项"好**(把假话换成指向该做那件事的真话),而"在计划层提前避开这一步"就是同一个病换地方犯;加测 3 条里**主判据是桩**——桩替换 `Domain.gateBlockers` 而项目对象一字不改(回空 → 一步不出、只回 `subjects-no-image` 且 `count: 7` 而项目真实只缺 1 张 → 文案写 7、回全部三码 → 提取/拆集都不出),**内联拷贝拿不到桩**故退回任何一份内联判定当场红,另有源级一条兜底(三处判据字面零命中 + 按码取材恰 5 处 + 每个码都在实际产出码集里);四组变异各红各的(整体退回内联红 3、只退补图那半红 2 且它在真实夹具上同值只有桩看得出、码名分裂回 `subject-no-image` 红 5 且其中 4 条是既有用例——静默筛空确实与"这步没待办"一模一样、拆集步丢掉剧本前置红 4 含既有那条「无待办即无计划」);数字 live 现取(unit 501 → **504**、`contract` 109 → **110**、`integration` 130 与 `cli.smoke` 100/102 复跑未变,两个 `FLOOR` 与明写份数按 live 抬) | 要动计划层的项目级待办、要给 `Domain.gateBlockers` 加一档阻塞码(留神它现在有**三个消费方**且三张投影表形状各不相同、各自决定投不投)、想知道"提取过主体但没存整本原文"这类老项目在各面上的口径差,或要照抄「同一件事的第三份判据怎么收:收判据不收形状,映射显式写在消费层」「用桩替换上游派生来证明消费方真的读了它(内联拷贝拿不到桩)」这两手时 |
| [w148-integration-log.md](./w148-integration-log.md) | 两条槽(W145 浏览器发布门 G4/G5/G6 聚合空 `catch`、W147 计划层项目级前置三步的待办判定收口 `Domain.gateBlockers`)并进同一条集成线的记录:两次 `--no-ff` 各取被合入支实际 head,`w145` 叉在更旧的 `w139` 而基线已走出 **14 个提交**、`w147` 叉在 `w142` 而基线已走出 **7 个**,故**两次都是真三方**(6 文件次 11 冲突块,两个产品文件 `js/release.js`/`js/plans.js` **零冲突块**、五条新用例各自零冲突块插入),七个数一个没动且五个注册表文件相对基线零 diff;**W146 交接的那句预告一句对一句错**——「与 `js/release.js` 撞不上」对(W137 那半在 `js/release-core.js`,是另一个文件),「与 W137 落地的两条用例撞得上」**错**(两端的用例分处两个夹具、各自只断言自己那三门,合完首跑就 509/509),由此把 W142 那条经验收窄成**「代码合得上、判据合不上」只发生在有一条断言同时读两边产出的时候**(W135 那条 `assertEq(bare.fails, withD.fails)` 把全部门的 fail 聚成一个数,故它才撞),与"改的是不是同一件事"无关;**"两侧同值"这个静默窗口两次合并各出现一次而成因相反**——第一次是契约段条数(叉点 108 / 对侧 108 / 我方 112,`git` 连冲突块都不给直接取我方),这次它对**不是运气**,W145 的两条用例落在 `release` 套件故 `contract` 条数结构上就不会变(与 W139 那次"恰好加了两条"两回事),第二次是单元下限(对侧写的 504 与基线 live 逐字相同),而它进了冲突块,因为我方那一格已被第一次合并抬到 506;两处主 README 单元整句**形似而处置不同**:第一次两侧都带内容(我方带 W138 那处含删除的口径之争改写、对侧带 W145 零删除的插入)故取并集,第二次契约段对侧只改数字故取我方整句、而单元那句两侧都是纯插入故仍取并集——**并如实登记一处操作失误**:第二次我按上一块的形态外推、先做成了"取我方只换数",机检才发现对侧那段插入被丢掉并当场补回,故口径写死成**每块都先机检两侧相对叉点的差异形态,不靠上一块外推**;W132 那条差额上限本槽**单元那格连基线父值一起逮住**(live 509,504 差 5 与 497 差 12 各红 1)而**记账件那格的基线父值仍压在边界上放过**(live 162,159 差 3 全绿、158 差 4 才红,156/153 各红 1),六槽连读得出**"抄基线自己那个父值"响不响只由本槽加了几条决定**(本槽加 5 条用例故单元那格响,只加 1 份记账件故记账件那格不响);七组变异各红各的(W145 缺模块守卫退回红 1 **而同一手在基线上 504/504 全绿**故浏览器那半确是新覆盖、W147 退回内联红 3 与码名分裂红 6 且其中 4 条是既有用例、`gaps()` 改名红 1、`report(...)` 搬进 `if` 红 3、索引整行挪位红 1、门槛表摘 `no-script` 红 3、`release-core` `aggErr` 两格**各守一格**各红 1),索引摘行红 4 条;没删测按名成集双向比对(基线独有 0 条、新增恰 5 条并逐条点名);数字 live 现取(unit 504 → **509**、`contract` 112 → **113**、`integration` 130 与 `cli.smoke` 100/102 未动但都复核过且 2 项失败在 `master` 独立 worktree 上同跑同名同表现,三套件条数与不同名字数**连续第十三槽全部相等**,索引份数 159 → 162 含本文,两个 `FLOOR` 一并按 live 抬);**本刻远端未合 1 条**(`w143` evolve headless `@8c657bf`),这个 1 是"任务明确要求留着(仍在飞)"而不是"还没到" | 想知道主干现在是什么状态;要合**同一件事的两端**(留神它们撞不撞只看有没有断言同时读两边)、想知道"两侧同值"这个静默窗口什么时候是运气什么时候是结构、想知道棘轮差额上限在什么条件下连基线父值一起逮得住,或要照抄「同一次合并里的两块冲突不许按形态外推、每块都先机检」这一手时 |
| [w149-release-zip-catch.md](./w149-release-zip-catch.md) | 交付包打包吞掉抓分镜失败(W145 文末登记的两处空 `catch` 里**有产品危害**的那一处):`buildReleaseZip` 抓分镜文件那段兜着 `catch (_) {}`,`Exporter` 抛错时这一集的整个 `storyboard/` **一个文件都不进包**(兜底 CSV 长在 `else` 分支上,失败这一路够不着),而回执四格里没有任何一格记这件事、下载提示照印「交付包已下载:N 个文件」`success`——**缺分镜的包与齐全的包在用户眼里一模一样,发出去对方拆包才发现**,而同一函数里紧挨着的成片 mp4 那一路失败是进 `skipped`、进包内 `README.txt`、进下载提示的;处置取与 mp4 同纪律(**照样出包,但包里缺什么回执上就写着什么**,不抛出去连已抓到的 mp4/SRT 一起丢):兜底从 `else` 挪成 `if (!storyboardOK)` 并抽出 `fallbackStoryboard()` 让两条路共用、失手另记一格 `storyboardFailed`(不并进 `videosSkipped`——混一起就分不出该重合成还是该查 `Exporter`)并同时进返回值/包内 `README.txt` 新增一栏/`downloadReleaseZip` 另出的 `error` 提示、清单先 `map` 整批算完再入列(原写法边遍历边 `push`,元素读崩时会留下"半截 shim 产物 + 一份兜底 CSV"混着的目录)、`list` 不是数组按同一格记(`shim` 回 `undefined` 时原写法在 `forEach` 上抛 `TypeError`,被同一个空 `catch` 吞掉、后果一样);**第二处 `Bus.on('*')` 的 `catch` 一个字没动并写明三条理由**(只兜"注册订阅"这一下而回调异常由 `emit` 自己隔离、`Bus.on` 是全函数故只有 `bus.js` 没加载才走得到属页面降级、`release.dirty` 全树零订阅者且角标每次重绘现取),**但审它时量出它兜着的那段在自喂自**——转发时把整条源事件当 payload 递出去,而 `emit` 里 `Object.assign({ name }, payload)` 让 `payload.name` 顶掉事件名,通配订阅按名字判来源便认定这是条主线事件再转一次,基线实测**一条 `shots.batchDone` 转 1695 次直到爆栈**、爆栈的 `RangeError` 又正好被订阅者隔离 `catch` 吃掉一声不吭,50 格事件留痕被同一条冲成 1 种,而那份留痕正是 Agent「最近发生了什么」的取数口(`Bus.recent(12, pid)`),故助手会照着念一件被复制 50 遍的事、说不出别的——**这一处收掉**,`js/bus.js` 一行(事件名由 `emit` 定、`payload` 顶不掉,根因;全树扫过无第二处 payload 带 `name`)+ `js/release.js` 一行(源事件名走 `src` 不占 `name`)各自独立成判据;`js/release-core.js` 与 G4–G6(W145 的活)一行未碰,发布门 G1–G10 与打包拦截口径一字未动,打包零计费故无退费一说;七条变异各红各的(整段退回红 2、只补记不补兜底红 1 且点名 `storyboard/` 空、去掉下载提示只红下载那条、静默替换红 2、`bus.js` 单退只红 `bus` 套件、`release.js` 单退只红 `src` 恒 `undefined`、两处齐退当场量出自喂自),正面段(抓取正常仍走 `Exporter` 清单/不报失败/不多噪音提示)七次一次没红;数字 live 现取(unit 504 → **508**,`release` 19 → 22、`bus` 4 → 5,单元 `FLOOR` 与记账件 `FLOOR` 及明写份数一并按 live 抬;`integration` 130 未动) | 要改交付包打包(先看第 2.1 节"缺什么报什么、不拦交付"这条既有纪律)、要往 `Bus` 加转发型订阅(转发别人的事件时别把整条 `ev` 当 payload)、想知道"空 `catch` 无害"该怎么举证到可以不动它,或要照抄「审一处 `catch` 时顺手把它兜着的那段真跑一遍」这一手时 |
| [w150-revise-maxretry.md](./w150-revise-maxretry.md) | `G-03`/SK-25 两个余面里的**收敛次数**那一面:「复审不达标还能重来几轮」收进 `Domain.reviseRetryLimit` 双端单源(整数轮次、区间 `REVISE_RETRY_MIN..MAX` = 1-5、缺省 2,候选值按优先级择先——变参而不是固定两个入参,因为三个消费点的候选链不一样长:CLI 只有命令入参、浏览器闭环只有分集配置、命令层是入参先于配置,"谁先谁后"留在调用处一眼看得见而"读不出来算什么"只在 domain 一处定义;上下限的**理由**也写进注释——上限存在是因为每轮都真扣费,下限 1 是至少给一次改正机会,**0 轮等于关掉修订闭环、那由 `smartReview` 开关表达**,这句是本槽新定的口径,此前 `\|\| 2` 只是把 0 当"没填"),收之前先量出同一道钳位实际写在**三处**而不是记账里写的两处(`cli.js`/`js/produce.js`/`js/commands.js`,另有三处同区间/缺省的非钳位写法在两个 UI 面板与 `defaultSBConfig`),四个消费点一律改现取、参数配置面板的次数选项由区间派生;逐值比对新老两算法**只有小数不同**且那一处是有意改——`maxRetry=1.9` 时浏览器闭环的重试循环在 `attempt=2` 收尾而"转人工"只在 `attempt === maxRetry` 那一轮走得到,于是该镜既不达标也不进 `manual` 计数,而质量闸门判的正是 `manual > 0`,**小数轮次能让带病成片放行**(小数经 `sanitizeCmdArgs` 的 `+v` 不取整进得来),截整后这一路不复存在;选 `domain.js` 而非 `wf-core.js` 的判据与 W131 同(领域推导不是提示词装配),落点紧挨修订闭环另两个口径 `REVIEW_MIN` 与 `reviseTargets`;**W136 那条护栏两向翻面重写而不是删**——它后半的反向断言本就是为"哪天真收编了"设的,故收敛面那几句从"两端各有钳位、单源层零命中"翻成"钳位只在 domain 一处、四个消费点各自 `Math.min(5,` 与 `maxRetry \|\| 数字` 都归零且都现取",前半另加正向要求(已落地那半须点名"名词 + 源码标识符"成对)、`reviseRetryLimit` 进"不许挤回仍欠段"名单,形态面那四句一字未改;另加一条 UI 子集判据(跑批模板只放低三档是 UI 选择,判据是档位须落在区间内且给得出缺省档,不是必须等于全区间);十五条变异含**一条有意不该红的对照**(仍欠段换个说法但两锚点都保住 → 绿:护栏钉的是记账与源码一致,不是那段话一个字不许动);`G-03` **不摘键**(仍 20 键、值仍两条),SK-25 的仍欠段只剩形态面(浏览器逐镜循环 vs CLI 整集轮次——同一份次数口径在两端数的不是同一件事,要合成一份得先定进度面板语义与逐镜计费节奏,属产品口径);数字 live 现取(unit 504 → 508、`contract` 112 未动因那条护栏是原地重写、`integration` 130 未动,两个 `FLOOR` 与明写份数一并按 live 抬) | 要改审片修订闭环的重抽轮数/缺省值/取值区间、要给 `Domain.reviseRetryLimit` 加消费点(别在调用侧再兜一层 `\|\| 2`)、想知道 `G-03` 还剩哪一面与它为什么卡在产品口径上,或要照抄「把反向护栏翻面重写而不是删掉」「变异集里放一条有意不该红的对照」这两手时 |
| [w151-integration-log.md](./w151-integration-log.md) | 三条槽(W143 专家自进化补 headless 人手出口、W149 交付包抓分镜失败留痕 + Bus 通配转发自喂自、W150 复审收敛次数收进 `Domain.reviseRetryLimit`)并进同一条集成线的记录:三次 `--no-ff` 各取被合入支实际 HEAD,`w143` 叉在更旧的 `w139` 而基线已走出 **21 个提交**、`w149`/`w150` 都叉在 `w146` 各走出 **7 个**,故**三次都是真三方**(9 文件次 15 冲突块,十三个产品文件**零冲突块**、三套件新增用例各自零冲突块插入);**任务口径预告的「evolve vs maxRetry 真撞」实测零冲突块也零判据冲突**——两支共改 `js/skills.js`/`js/commands.js`/`cli.js` 三个文件却处处是「同一文件的不同段」(SK-26 与 SK-25 两条记账条目相隔十余行、`reg` 段尾与 `episode.produce` 钳位那一行、`EXEC` 新增与 `maxRetry` 取值),四棵树哈希互不相等是真融合,而按 W148 的分辨法复核判据面**没有一条断言同时读两边产出**,故合完首跑就 520/520,预告点名的 `domain`/`wf-core` 更是两支各只有一侧在 diff 里;**「两侧同值」这个静默窗口本线第一次出现在两个被合入支之间而不是我方与对侧之间**(`w149` 与 `w150` 叉点相同、各加 4 条用例各加 1 份记账件,自称数逐字相同的 508/160),它们没能静默只因我方那一格已被前一次合并抬走,而**若只合其中一条那个 508 与基线 504 差 4、离 SLACK 3 只一格**;相反形态同时出现——`w143` 自称的 `integration 141` 与 `cli.smoke 107` **恰好等于三次合并后的 live**,理由是那两个套件全槽只有它一支在改,**自称数第一次成了答案而它是结构事实不是运气**;三处主 README 单元整句**形态各异处置各异且每块都先机检不按上一块外推**(第一次两侧都带内容取并集、第二次两侧都是纯插入且**对侧那段与我方 W145 那段锚点逐字相同**故本线第一次做**同锚点并集**、第三次对侧只改数字故取我方整句);`js/release.js` 是本槽唯一一个两侧都改过的产品文件(`w149` 叉在 W145 落地之前),两半分处 `gates()` 的 `aggErr` 与 `buildReleaseZip` 的抓分镜,`git` 双向搬进去**零冲突块**;**本槽不是注册表零 diff 槽**(`js/cmd-registry.js` +7 领域命令 12 → 13、`js/skills.js` +31 −8),故七个数是现跑对出来的而非结构性成立,`gaps()` 20 键与 `G-03`/`G-11` 两键一个没摘,**`expert.evolve` 进 `cmds` 不进任何 playbook 的 `steps`**(变异掺进去即红,报「编排步序里出现进化=自动蒸馏被写成口径」);没删测按名成集双向比对得出**基线独有 1 条**——那不是删测而是 W150 把 W136 立的 SK-25 护栏按新实况**翻面重写并改名**(两向仍在、覆盖面从两个欠账面变成「一面已落地含四处消费点源码形态 + 一面仍欠」),**本线第一次有既有用例改名,由此暴露按名比对把「翻面」与「真删」报成同一形态、判定没被削弱这一步至今零机器判据**;十七组变异各红各的(evolve 四条含一条只有真实服务抓得住、bus 与交付包两条**在基线上同一手全绿**故确是新覆盖、maxRetry 两条、任务点名要保的七样逐样反向一次);数字 live 现取(unit 509 → **520**、`integration` 130 → **141**、`cli.smoke` 102 → **107**(实跑 105/107,2 项失败与 `master` 同名同表现且**分母涨了 5 而失败项没变**)、`contract` **四侧与 live 五值全同 113**、领域命令 12 → 13,索引份数 162 → 166 含本文,四个 `FLOOR` 一并按 live 抬) | 想知道主干现在是什么状态;要合**共改同一批文件但各占一段**的槽(先按「有没有断言同时读两边产出」判撞不撞,再看代码)、想知道「两侧同值」这个窗口出现在被合入支之间时会怎样、要判同一行的三次冲突为什么处置各不相同,或要照抄「同锚点并集」「既有用例翻面重写时怎么人工核对它没被削弱」这两手时 |
| [w152-empty-zip.md](./w152-empty-zip.md) | 打包交付一次给用户两个 zip、先落地那个是空的:`js/release.js` 的 `downloadReleaseZip` 在真下载之前先 `ZipUtil.download(name, [{name:'PLACEHOLDER', data:''}])` 落了一份 120 字节的占位包,而**紧跟的下一行注释就写着「正确写法:用 `URL.createObjectURL`,避免上一行占位生成额外空 zip」**——注释描述的那件事没做完,是一次改到一半的编辑(新写法补上了、旧写法没删),坏在 `ZipUtil.download(filename, files)` 不是「准备下载」而是 `create → Blob → a.click()` 一整条下载,故那一行落的是一个已经到硬盘上的完整 zip(里面只有一个空条目 `PLACEHOLDER`);用户侧看到的是同名同后缀同一秒落地的两个文件、真包被浏览器加了 `(1)`,而 toast 只报真包那份口径(「7 个文件,1/1 集成片」)**一个字没提旁边那个空的**,挑错了也没有任何提示;处置只删那一行并把注释换成「为什么不在这里调 `ZipUtil.download`」(`buildReleaseZip` 与两条 toast 一字未动,`files`/`videosOK`/`stale`/`size` 口径照旧);两条用例共用新装配 `loadReleaseZip(urlFail)`——在既有 `loadRelease()` 上按 `index.html` 的方式把**真的** `js/ziputil.js` 装进沙箱、注入记录用的 `Blob`/`URL`/`createElement`,断言逐条读 local file header 拿条目名(STORE 无压缩故不必解压),第一条钉「只落一个文件 + 落地 bytes 就是 `buildReleaseZip` 那份 + 7 条清单逐条相等 + toast 同口径」、第二条钉兜底那条路(`urlFail` 让第 N 次 `createObjectURL` 抛错)仍只落一个且包里只有那张「请重新打包」的说明;四条变异实测(退回基线红 2 条且第二条报的是**整个调用抛出去**——占位那次下载先撞上失败而它不在 `try` 里,即基线在这一路上按钮会报「打包失败」而包早就打好了;掏空兜底 `catch`、把兜底包换成 `PLACEHOLDER`、把 blob 里的 `r.bytes` 换成另打的空包,后三条各红一条且分工不混);**兜底那条 `catch` 有意未改、只把现有行为钉住**并如实登记两处仍欠(它走的是同一套 API 故真不可用时同样不可用;它给的是一个能打开的 zip 而不是一句报错,与「失败如实报错不用占位冒充」同类),`_buildMaterialShim` 那处空 `catch` 与 W149 的分镜失败留痕(本 HEAD 全仓零命中)一律不碰;数字 live 现取(unit 509 → 511,`integration` 130 未动,单元 `FLOOR` 与明写数、记账件份数 162 → 163 一并按 live 抬) | 要动交付 ZIP 的下载落地路径或兜底行为(留神那条用例钉的是现有行为,真改成报错要同轮改它)、要往打包清单加条目(第一条用例那 7 条期望值要同轮改)、想知道 `ZipUtil.download` 与 `ZipUtil.create` 的分工,或要照抄「把真 ziputil 装进沙箱、按 zip 字节数清用户到手几个文件」这套取证时 |
| [w153-gate-fanout.md](./w153-gate-fanout.md) | 门槛派生 `Domain.gateBlockers` 有**三个消费方**且取法各不相同(流程条 `workflow` 按 `step` 取、问题中心 `Issues.GATES` 与计划层 `TODO_OF` 按码取),三处都是"表外的码一律不投"而且**不投是静默的**;W138/W147 钉的都是同一向「消费方用的码必须是派生会回的码」,**反向那向向零**——给派生加一档 `eps-no-content` 在基线 `3bbaac1` 上实测 **509/509 全绿**,而现跑佐证这一档真回得出来:流程条因沿用 `eps` 这一步画得出来、**问题中心的项目级条目却是空的**(`GATES` 查不到即 `return`)、计划层一步不出,用户看到的与"这项没问题"一模一样(W147 交接第 1 条预告过这一格并写明"真要点名得同轮加一句");W138 那条"两侧码集双向相等"钉不住反向,因为它的枚举面是**五个夹具摊出来的码**——新码只要没被夹具触发,包含关系两向都照样成立,**判据的枚举面必须来自派生自己**;落法两步:阻塞码收进登记表 `GATE` 且 `gateBlockers` 函数体**不再出现码字面**(想绕开表直接写码撞源级断言,四码在 `js/domain.js` 里仍各只一处字面故 W138 的单源那条全程绿)、`D.gateCodes()` 把码全集报出来当枚举面,契约用例**逐码把派生打桩成只回该码**再看两个消费方的实际产出(打桩而不是造夹具:新增的码本来就没有夹具,人漏投时多半也想不到写它);接不住的码要在消费方**显式写下理由**(`Plans.gateSkips()` 现只有 `no-script`:没有能替用户写整本剧本的命令,与 `TODO_OF` 登记 `null` 同一纪律),问题中心那侧**有意不设白名单**(它是断点的唯一清单面,CLI/MCP 同读);顺带补了第三个消费方那一格——派生给得出的 `step` 集合必须等于 `workflow` 里 `gateOf` 取的 `step` 集合(新增一档带新 `step` 时流程条一条都画不出来,同一个病);八组变异各红各的(基线对照全绿、两消费方各漏投时**报错点名换人**、`GATES` 摘一行红 4、计划层取材器不读一码红 4、绕开登记表写码字面红 1、白名单过期红 1、新 `step` 红 1、副本纪律回表本身各红 1),其中**副本那两条头一版没红**:比"取两次的长度"时,回的若是表本身则先前捞出的引用被同一次污染一起改、比对成了自己等于自己,改成**污染前先存字面快照**才接得住——取表给副本这条纪律的判据自己也会犯同一类自指错误;数字 live 现取(unit 509 → **510**、`contract` 113 → **114**、`integration` 130 与 `cli.smoke` 100/102 复跑未变,两个 `FLOOR` 与明写份数按 live 抬) | 要给 `Domain.gateBlockers` 加一档阻塞码(交接第 1 条写了三处消费方逐处该改什么、什么时候才绿)、想知道"某一态在问题中心/计划层静默消失"这类缺陷怎么在没有夹具的前提下钉住,或要照抄「把枚举面收进登记表让判据不去猜实现」「逐码打桩看消费方实际产出」「不投的码要写下理由、白名单过期反过来红」「副本纪律的判据要用字面快照防自指」这几手时 |
| [w154-zip-fallback.md](./w154-zip-fallback.md) | 交付包下载的兜底 `catch`(`js/release.js` 的 `downloadReleaseZip`,打包结果 `buildReleaseZip` 一字未动):落盘那步失败时兜底改调 `ZipUtil.download`,而它就是**同一套** `Blob` / `URL.createObjectURL` / `a.click()`——主路径落不下来它同样落不下来;偶尔落得下来时给的是一个名字仍叫「交付包_*.zip」、里面只装一句「空下载兜底:请重新打包」(条目名还冒充 `project_meta.json`)的空壳,函数照常往下走把「交付包已下载:N 个文件」这条绿色成功提示发出去,**用户手里是空壳而平台说交付成功**;现改为如实抛出带原因与两条出路的错误,后面两条 toast 与 `return r` 一并不再执行,唯一调用点(交付弹窗「📦 打包交付 ZIP」按钮)本就有的 `catch` 把它播报成「打包失败:…」并恢复按钮可重试——失败播报通道本来就在,缺的只是让失败走到它面前;另说明为何不换一种"真能用"的兜底(`data:` URL 撑不住几十 MB 的 mp4、`window.open` 拦得更狠、暂存重试是另一个功能);同轮删掉 `downloadReleaseZip` 开头那行改到一半留下的 `PLACEHOLDER` 空 zip 占位调用(与 W152 逐字同 diff,合入按同一处删除收敛,那一面的记账在 W152);`_buildMaterialShim` 空 catch、W149 的 `storyboardFailed` 与任何 `gaps` 一律未动。两条用例共用新装配 `loadReleaseZip(urlFail)`(真 `js/ziputil.js` 进沙箱 + `Blob`/`URL`/`createElement` 三个最小桩,`__clicks` 记下用户到手了什么):一条钉模块出口层(必须抛、报错点名是下载这一步并带原因、到手 0 个文件、不许再报「已下载」),一条**真跑交付检查弹窗**驱动打包按钮 `onclick`(toast 以「打包失败:」开头、同屏无「已下载」、到手 0 个文件、按钮恢复可点),五组变异各红各的(退回基线红 2、静默吞掉红 2、抛错不带原因红 1、又抛错又落一份空壳红 2 且都红在文件数、只 toast 不抛红 2 且失败与成功同屏) | 想收"兜底与主路径是同一条路"这类**假兜底**、想知道什么时候该让失败如实抛而不是塞一个能打开的空壳,或要照抄「`U.openModal` 桩 + `querySelector` 记忆化节点」在单测里真跑弹窗按钮这套装配时 |
| [w155-ep-blockers-fanout.md](./w155-ep-blockers-fanout.md) | 项目级前置门槛那面已经收成单源并逐码钉住扇出,**分集级** `Domain.episodeState` 的 8 档阻塞码还散落成字面,而它同样有两个按码分工的消费方,且**两处的形状都不是查表**:问题中心 `Issues.collect` 是**逐集循环里的分支**(每档一支 `if`,各带自己的明细、处置命令与早退),中段流程模板 `js/flow-tpl.js` 是各步的 `stop` 断点登记——两处都"表外的码一律不投"且**不投是静默的**,派生新加一档时谁没跟上都不会红(问题中心收不出条目 = 那一态在清单上凭空消失,模板缺一档 = 调用方跑砸在这一码上只能自己猜),既有判据只钉了「消费方按码筛的码必须是派生真会回的码」这一向,反向那格向零;**分集级比项目级更该钉**的地方在于漏一码是"循环体里少一支 `if`",与"这一态恰好没摊到"在夹具上长得一模一样;落法三处:`js/domain.js` 出码只走 `EPB` 登记表(函数体零码字面,`D.workflow` 里那几档**同名的项目级聚合**一并改读同一份——`label` 是聚合口径故留在各步,码名不在一个文件里写第二遍;落地后每码恰一处字面,`no-script` 仍两处因两级判定输入不同)并由 `D.epBlockerCodes()` 报出码全集,`js/issues.js` 把危险级收成表、逐集分支按表取值并给接不住的码写下理由(`epBlockers()` / `epSkips()`),`js/flow-tpl.js` 登记不进中段的两档 + 理由(`stopSkips()`,与该文件既有"投影步登记 `null` = 有意不在中段"同一纪律);**有意没把分支收成查表**——分集条目还带明细/处置/早退,硬收就是把判据挪进表里换地方写同一份,可枚举的只有码与危险级,漏没漏投由契约用例拿真实夹具逐码看产出;**行为零变化**(判定条件与全部文案一字未动,`js/release.js`/`js/release-core.js` 零 diff,G1 仍按 `status` 判、没被引去读 `p.script`);加测 1 条:枚举面逐码拿一份**真会触发该码**的夹具(先断言 `episodeState` 确实报出它,夹具失效即红)、再看两个消费方的实际产出与危险级是否取自登记表,白名单需写明理由且三张表都验"现生成副本";七组变异各红各的,**主判据是 M2**(模板断点摘掉 `stale-shots` 只红本槽这一条,既有 7 条 `flow-tpl` 用例全绿,证明反向那格此前确实向零),M1(问题中心摘掉一支)另撞红 3 条既有用例说明**已摊到的那几档**撞得到既有夹具、撞不到的正是"新加一档"(M3)那种真实形态;数字 live 现取(unit 509 → **510**、`contract` 113 → **114**,`integration` 130 与 `cli.smoke` 100/102 复跑未变,两个 `FLOOR` 与明写份数按 live 抬) | 要给 `Domain.episodeState` 加一档阻塞码(义务:两个消费方各自投影或写下不投的理由,并同轮补一份触发夹具)、要动问题中心的逐集循环或中段模板的断点登记、想知道分集级阻塞码有哪些消费方(含一组**只读 `label` 不读码**、对新增档天然免疫故本槽没立判据的面),或要照抄「逐集循环那种分支形状怎么钉扇出:只收可枚举的部分,漏没漏投拿真实夹具逐码验产出」「接不住的码要写下理由,白名单之外一律红」这两手时 |
| [w156-material-shim-catch.md](./w156-material-shim-catch.md) | 交付包打包链上 `_buildMaterialShim` 那段 `catch (_) {}`(`js/release.js` `buildReleaseZip`,发布门 G1–G10 一字未动):`Exporter` 抛错时这一集的整个 `storyboard/` 目录一个文件都不进包——兜底 CSV 挂在 `else` 分支上,**抓取失败与抓取成功走同一个分支**故兜底根本够不着——而回执与下载提示照报「交付包已下载:N 个文件」;**坏的不是"包落不落得下来"而是"包里少了几集分镜表"**,`r.files` 按 `files.length` **现数**,少了几集那个数照样如实(成片那一路有 `videosOK/分集数` 这个分母可对,分镜这一路没有任何东西对着分集数),故本槽判据**有意不落在 `r.files` 上**而落在「每集都得有分镜进包」与失手计数上(三集夹具第二集抛错,改前 `r.files`=8、有分镜的集数 2/3、`storyboardFailed` 是 `undefined`、下载提示只有那条「8 个文件」;改后 9、3/3、按集点名、另起一条 error toast);处置是往 `summary` 加一类 `storyboardFailed` 而**不抛错中断整包**(缺一集分镜不该拿另外两集的成片/SRT/元 JSON 陪葬,与同函数成片那一路记 `summary.skipped` 同纪律)、兜底从 `else` 挪出来改判 `storyboardOK` 让失手这一路也够得着、`files` 先整批算完再入列(原来边转边 `push`,清单第 k 个读崩时前 k-1 个已进包会与兜底那份混着)、`Array.isArray` 守卫让回执说得出"清单不是数组"而不是把 `.map of undefined` 原样甩出来;回报走三处**与 `videosSkipped`/`stale` 同一套口径**(返回值 + 下载 toast + 包内 `README.txt` 新增一节);六种变异各红各的且**分工可辨**(整段退回红 3、只记不回退红 2、去掉非数组守卫红 1、去掉下载 toast 红 1、去掉包内 README 那节红 1、改成 `throw` 中断整包红 3),其中**守卫那条最初写成了摆设**——第一版只断言 `storyboardFailed.length`,而 shim 回 `undefined` 时不带守卫也被 `catch` 兜住条数照样对、变异全绿,改成断言"回执得说清是清单不是数组"才转红,**加守卫不等于有判据,判据得落在它多产出的那点信息上**;与尚未合入的 `w149` 同口径(`js/release.js` 那半两份 diff 除 hunk 行号外**逐字节相同**,字段名/文案/`fallbackStoryboard` 一律照它免得冒出两套计数),但**有意不带上那支的 `Bus.on('*')` 那半**(事件名转发是另一件事,一并塞进来会让合并时分不清哪块是哪槽的),如实登记为仍欠;G4–G6、下载失败那一路的空下载兜底(另有源级断言钉着别被顺手抹平)与 `gaps` 一律未动,`js/release.js` 对 `Skills` 照旧零引用;数字 live 现取(unit 509 → **513**、`integration` 130 与 `cli.smoke` 100/102 未动但都复核过,单元 `FLOOR` 与主 README 明写数、记账件份数 162 → 163 一并按 live 抬) | 要动交付 ZIP 的打包清单或它的回执/下载提示、要合 `w149`(留神 `js/release.js` 那半逐字节相同故不会真冲突,`tests/unit.js` 会撞而本槽是超集)、想知道"回执上那个数是对的却什么也证明不了"该怎么立判据,或要照抄「吞掉的失手改成加一类计数而不是抛错中断整批」「用变异证明一道守卫不是摆设、不红就把断言改到它多产出的那点信息上」这两手时 |
| [w157-integration-log.md](./w157-integration-log.md) | 三条槽(W152 删 `downloadReleaseZip` 开头那行占位空 zip、W153 门槛阻塞码收进 `Domain.GATE` 登记表 + `gateCodes()` 报全集 + 计划层 `Plans.gateSkips()` 显式登记不投的码与理由、W154 交付包下载落地失败如实抛错不再兜底落空壳)并进同一条集成线的记录:三次 `--no-ff` 各取被合入支实际 HEAD,三支都叉在 `w148 @3bbaac1` 而基线已走出 **13 个提交**、**三支两两的 `merge-base` 也都是 `3bbaac1`**(即彼此看不见),故三次都是真三方(11 文件次 17 冲突块,四个产品文件次 `js/release.js` ×2 / `js/domain.js` / `js/plans.js` **零冲突块**);**本线第一次遇到两支对同一行给出逐字节相同的 hunk**(W152 与 W154 都删占位那一行,两份 patch 除 blob 哈希行外 `diff` 无输出),第三次合并时 `git` 判成两侧同向、既不给冲突块也不把占位加回来——**这一处是结构上守住的不是人手守住的,而它守得住的前提是两支 diff 逐字相同,差一个空格就会变成冲突块**;任务口径预告的「W150 `reviseRetryLimit` vs W153 `GATE` 在 `js/domain.js` 真撞」实测两段相隔五十余行、零冲突块且**两侧都留**(按 W148 分辨法复核判据面,两侧连夹具都不共用);**本槽真正撞上的是判据不是代码**——W152 那条钉「兜底重打仍只落一个」的用例与 W154 那条钉「落地失败如实抛错」的用例**同夹具 `loadReleaseZip(1)`、同判定点、结论相反**,按任务口径取抛错那侧,故 W152 那条**按新实况翻面**由 W154 那条接位,逐面核对四个断言里两个随被删的行为一起消失、两个有新家(`r.files` 那一面在失败路径上净减少,如实登记);**另有一条既有用例被行为改动打穿而冲突块完全看不见**——W149 的 `抓分镜失手在下载回执上看得见` 原用 `loadRelease()` 装配,基线上跑得通只因落地失败被那个兜底 `catch` 吞掉,W154 改成抛错后它当场炸在 `Blob is not defined`,处置是换成带下载桩的 `loadReleaseZip()` 而**断言一字未改**(变异复核报错句与换装配前逐字相同),由此把 W148 那条经验补上一种新形态:**一侧的行为改动让另一侧用例的装配前提不成立,不需要任何断言读两边**;没删测按名成集**做两轮**(首尾一轮报基线独有 0 条、新增 4 条,**逐次那一轮才看得见翻面的那 1 条**——翻面发生在两次合并之间时首尾比对连"独有 N 条"都不报,这是 W151 那条盲区的更隐蔽变体);注册表五个文件**相对基线零 diff**故八个数结构性成立(`gaps()` 20 键、提示词 41、短名单 30、`CHECKS` 17、`preflightStages` 7、`KB.SECTIONS` 18、`playbooks` 5、领域命令 13),`G-03`/`G-11` 未摘、`expert.evolve` 仍不进 `steps`(变异掺进去红 3);七组变异各红各的(退回兜底红 2 且报错句里直接印出那条假成功 toast、占位加回去红 2、`GATE` 新增一档码而消费方不跟红 1、`gateSkips` 清空红 1、`storyboardFailed` 退回静默红 2、`REVISE_RETRY_DEFAULT` 2→3 红 2、`expert.evolve` 掺进 `steps` 红 3);数字 live 现取(unit 520 → **524**、`contract` 113 → **114**、`integration` **141** 与 `cli.smoke` **105/107** 未动但都复核过且 2 项失败在 `master` 独立 worktree 上同跑同名同表现,不同名字数与用例数**连续第十五槽相等**,索引份数 166 → 170 含本文,四个 `FLOOR` 一并按 live 抬;**三支自称的记账件份数逐字相同的 163 且都不是答案**,三支自称的单元数 511/510/511 也都不是);**本刻远端未合 2 条**(在飞的 W155、W156),这个 2 是任务明确要求留着 | 想知道主干现在是什么状态;要合**两支对同一行做同一处删除**的槽(先比两份 patch 是不是逐字相同,再判会不会给冲突块)、要合**两条用例同夹具而结论相反**的两支、想知道"零冲突块 + 两半代码都在"为什么仍可能打穿既有用例,或要照抄「按名成集比对做两轮:首尾一轮 + 逐次一轮」「既有用例被行为改动打穿时只换装配不动断言并用反向变异复核没削弱」这两手时 |
| [w158-rename-vs-delete.md](./w158-rename-vs-delete.md) | `w151` 残留 10 的收口(零产品代码):**「同主题翻面重写并改名」与「真删掉一条护栏用例」此前零机器判据可分**——先在基线上把"分不出来"量成两组逐字相同的读数(改名 + 另加 1 条 vs 真删 + 另加 2 条,两次都是**按名成集 `基线独有 1 条 / 新增 2 条`、两次都 521/521 全绿**;此前只说"报成同一形态",本槽落到了同一对数上),再证既有那三条数字判据管的是别的事(删掉它、把 README 两个数与三套件下限一并改小 → **519/519 全绿**);判据**有意不做成禁止改名**(W150 那次改写覆盖面反而变大,禁改名等于把"记账随实况改写"与"不许删测"两条纪律对立起来),改钉**主题还有没有承载它的用例**:`GUARD_TOPICS` 逐条登记那道护栏读的**实况取数口**(注册表键 / 源码标识符 / 文件路径片段,故翻面重写会换措辞但不会换它读的是哪份实况),按锚点在**全部套件的用例运行时源码**上找落点,用例名一个字不参与判定;`hosts` 取**恰等于**而不是"至少一条"(松成"至少"时锚点写泛没有代价,那条真被删掉时会有别的用例替它顶着,实测松一个锚点即红并列出是哪两条);清单自身另有一条守着(条数只增不减、编号唯一、每条至少两个锚点、承载用例断言少于两处按"名存实亡"报),取锚点前把注释整段抹掉而字面量原样留着(`blankNonCode` 收 `keepText` 位,+4 行、既有调用点行为逐字节不变;W136 记过"注释替断言把活干了"那次假红,同一形状在这里就是"删光断言、锚点留注释"照旧全绿),两条口径各有一句自检断言钉着不许静默退化;十条主题收的是记账/欠账类与单源类护栏,含**取证工具自己那一层**(名集唯一、三套件下限、`FLOOR` 差额、记账件三方对齐——它们此前只靠彼此的数字互相兜);十组变异各红各的,含**两条有意不该红的对照**(只改名、翻面重写各 522/522 全绿)与**一条能力边界实测**(删掉没登记进清单的用例,本槽两条一条不红);另有一处**真实语料回放**:把这张清单原样搬到 W150 那次改名之前的 `23b7a60` 上,十条主题全部找得到落点、`sk25` 落在**改名前的旧名字**上、两条新用例都绿——即对那次真实翻面不误报,而对真删掉会报;残留量化:静默删掉一条被登记的护栏从"改 3 处数字"抬到"改 5 处且最后两处落在带 `why` 的表里"(**抬价不是封死**,第 ⑤ 步仍全绿),销号与删测可以分两槽做而中间那槽是全绿的(故审 `GUARD_TOPICS` 的 diff 要与审删测同一标准);数字 live 现取(unit 520 → **522**、`contract` 113 → **115**、`integration` 141 与 `cli.smoke` 105/107 未动,索引份数 166 → 167 含本文,两个 `FLOOR` 按 live 抬) | 要删/改名/翻面重写一条既有护栏用例、收敛槽做完按名成集比对发现"基线独有 N 条"要判是不是删测、想给某道护栏登记进 `GUARD_TOPICS`(或看清单里某条为什么在),或要照抄「把'分不出来'先量成两组相同读数再动手」「判据回放到历史上那次真实改动上验不误报」这两手时 |
| [w159-report-early-return.md](./w159-report-early-return.md) | 静态 > 实跑 的**最后一路**(零产品代码):W141 交接第 1 条点名的那一路——登记点在 `main` 里、前面有一句提前 `return`/`throw`,后面的 `report(...)` 一条都不跑,而它们的**块链上一层条件块都没有**,W120/W124/W141 那四层块链判据逐层点不到;基线 `31d7aeb` 上现跑量出:两条登记点之间插一句 `if (env) return;` 后静态四个口径仍是 141、`unit` 全绿,而实跑只剩 **3 条 PASS、末尾那句总数根本不打印、退出码仍是 0**——比 W141 那一路更糟的一点是**它连"跑完打印了一个总数"都没有**,今天没有任何一处看得见;故换判据类别:不看登记点的块链,改看 `main` 自己那一层的控制流(新增模块级工具 `mainOwnFlow` 在抹码骨架上定位 `main` 函数体、逐字符走到收尾括号、把嵌套函数体整段略过,返回 `main` **直属**的退出点与登记点),三种退出形式各有各的位置口径——`return` **一律不许**(唯一一种静默收场:后半截不跑、总数不打印、退出码 0,要中断就 `throw` 让 `main().catch` 记成失败)、`process.exit(` 只许在**最后一条**登记点之后(`process.exit(0)` 摆在就绪前置里同样是静默归零,故比 `throw` 严一格)、`throw` 不许夹在**第一条与最后一条**之间;两个套件今天的合法形状恰好各在两头(就绪等待失败 `throw`、冒烟末尾按 FAIL 数 `process.exit`)一处不用改,**静默截断那一路整条封死,漏在外面的只剩"前置里 `throw`"而它退出码非 0 当场看得出来**;**不禁一切 early return**——判据只认 `main` 直属那一层,helper 与回调里的 `return` 一处不碰(变异 5 就地写一个带两处 `return` 的箭头 helper 全绿);函数体判定有意取**保守侧**(认不出来就当普通块:认错只多报一条当场红,误当函数体才会静默放行);九条变异各红各的(`return` 单行/块写法、中途 `process.exit(`/`throw`、前置 `process.exit(0)`、裸 `{ }` 分节块里的早退、`cli.smoke` 同形、**把一条登记点挪到就绪 `throw` 之前会让那句合法 `throw` 转红**——放行的是位置不是那一行),归口两条反向对照:W141 的条件块变异与 W120/W124 的 `forEach` 变异下**本条零命中**;数字 live 现取(unit 520 → **521**、`contract` 113 → **114**、`integration` **141/141** 现跑、`cli.smoke` 静态 107 零命中,`unit` 与记账件份数两个 `FLOOR` 按 live 抬到 521/167,四格差额全 0) | 要写"某段代码在 `main` 自己那一层还是在嵌套函数里"这类静态判据(直接用 `mainOwnFlow`)、想知道静态点数这一串判据现在还漏什么、或要判"提前退出"该按哪种形式区分静默与大声时 |
| [w160-cycle10-audit.md](./w160-cycle10-audit.md) | **周期 10 独立核验**(`w130-cycle9-audit` 落地之后到 `w151-integration-2bc5` @ `31d7aeb` 为止,十五次合入 + 七支集成槽):周期 9 那十一条阻塞**逐条结账**——A2/B1/B2/C1/C2/D1/E1/F2 八条已收(分别由 W141/W138+W147/W138/W135/W135+W137+W145/W136+W150/W143/W130 自己收掉),A1 由 W132 从「无界差额」钉成 `SLACK = 3`,D2/E2/F1 仍开且 F1 从 2/8 恶化到 **1/15**;判据层五层长到**八层**(新增 L5 条件块白名单、L7 棘轮差额上限、L8 索引行序),而**十个注册表口径整周期只动了一个**(领域命令 12 → 13),故这是治理层自我加固连着的第二轮。本槽新量出三个口,方向同一个——**判据钉住了产出物长什么样,没钉住这个数/这个值从哪儿来**:① `main` 里一条 early `return` 让静态点数 141 / 实跑 130 / 退出码 0 / 八层全绿(W141 封的是块链形状,封不住「块一层都不开」的跳过;唯一痕迹是末尾那行 `=====` 不见了,只有人眼看得见,周期 9 §8-1 那条「起子进程读回实跑总数」至今没人做,W159 与发布门扇出**在飞**);② `Domain.REVIEW_MIN` 的「单源」实为**两处各被冻在 7**——把达标线整体挪到 8(常量 + 两条钉住 7 的断言同轮改)、`js/produce.js:272` 那份字面 7 留在原地 → **520/520 全绿**,而该处会把 7 分镜判达标并替用户 `s.confirm = true`,与同一集 `episodeState` 的 `needs_human`、发布门 G3 的 fail 直接矛盾(反向对照:只改 `produce.js` 那个 7 → 513/520 红 7 条,管它的是一批把 7 烤进夹具的行为用例,而那批在前一个变异里一条都不红);③ 前置断点「表外码」守卫**按夹具射程不按机制**——给 `gateBlockers` 加一档、触发条件落在五个手写夹具射程外 → **520/520 全绿**,码在流程条上照常出面而问题中心静默丢掉(射程内则当场红 3 条,故判据没坏、取数口太窄);另量出 `FLOOR` 余量 3 的确切代价(两步实测:加 3 条探针 + README 同步 → 116/116 全绿;在那 3 格上真删 3 条既有用例 3828 字符 + README 改回 → **520/520 全绿**)。主线十三态走查逐态自洽,**前置三步四态三方码集相等**(`gateBlockers` == 流程条 == 问题中心),断点码差集只剩入参保护那条 `no-episode`;十五次合并**三禁令零违规**(45 次整文件取边检查零命中、文件回退零命中),但**逐支复跑的取证对象被删了**——十五支功能分支的远端引用已不在,「分支漏合」那条判据成了空判,本槽改挂**第二父 SHA** 才复跑出那张逐支表;数字 live 现跑(unit 520/520、`contract` 113/113、`integration` 141/141、`cli.smoke` 105/107 两项与 `master` 同名,四条棘轮余量全 0;`master` 9adcf0f 零新提交、ahead 477、223 文件 +52655/−1021,积压十个周期) | 想知道周期 9 那十一条各自结账没有、要给「静态点数 vs 实跑数」补第一条比对、要收「常量单源却没人读」这一类第二份字面、要把按夹具射程立的包含关系判据改成按源码取数,或要照抄「远端分支被删后改挂第二父 SHA 复跑」「变异集里放一条反向对照证明判据没坏只是取数口窄」这两手时 |
| [w161-integration-log.md](./w161-integration-log.md) | 三条槽(W155 分集阻塞码收进 `Domain.EPB` 登记表 + `epBlockerCodes()` 报全集、问题中心与中段流程模板的扇出成契约,W156 交付 ZIP 抓分镜失手回报的测试面,W159 `main` 中途收场那一路补判据)并进同一条集成线的记录:三次 `--no-ff` 各取被合入支实际 HEAD,`w155`/`w156` 都叉在 `w148 @3bbaac1` 而基线已走出 **22 个提交**、`w159` 叉在 `w151 @31d7aeb` 走出 **9 个**,故三次都是真三方(10 文件次 21 冲突块);**连着四槽零产品冲突块之后第一次有产品文件真的给出冲突块**——`js/domain.js` 里我方 W150 的 `reviseRetryLimit` 与对侧 W155 的 `EPB` 表**插在同一个锚点上**(两侧都是零删除的纯插入)故取并集,而同一次合并里对侧把 `episodeState`/`workflow` 函数体里的码字面换读 `EPB.*` 那十几行**没进冲突块**(我方相对叉点在那些行上零 diff),即"插在同一锚点给块、改的是不同行不给"两种形态在一次合并里同时出现,机检结果相对我方的增删行逐字等于对侧相对叉点的增删行、W150/W153/W155 三侧都留;**一支自称与已合入支逐字节相同的产品改动实测确实相同**(W156 的 `js/release.js` 与基线里 W149 那次除 hunk 头外 `diff` 无输出),形态与 W157 那次"两支同删一行"相反——这次是**对侧那半已整份在我方里**,故 `git` 把文件收敛到我方原样而对侧未改的两段(占位空 zip、落地失败兜底)照旧留在我方 W152/W154 的删后/抛错后形态,`M == P1` 与整份取 `--ours` 在哈希上分不出来、靠"对侧加减 45 行逐行核对"分开,由此**本槽合进来一整槽产品改动而产品面对它零 diff**;**判据冲突这次是翻面而不是接位**——W156 那条源级钉住兜底还在的用例与 W154 结论相反,但它捆着一个全仓没有第二处在管的面(`ZipUtil.create` 崩了时 `buildReleaseZip` 照旧抛出),故不删不接位而改名翻面:源级那格从"兜底仍在"翻成"不许回来 + 落地失败得抛带原因的错误",**顺带把 W157 残留 1 收掉一半**(那一行"不许回来"此前只靠现取 `grep`);没删测按名成集两轮比对**首尾与逐次全为 0**,而本槽确实翻了一条——它在落地当轮就被翻、从未以原名进过集成线,是 W157 残留 3 那个盲区更彻底的变体,只有"被合入支名集 ↔ 合并后"这第三种比法看得见(人工);注册表五文件相对基线零 diff 故八个数结构性成立(`gaps()` 20 键、提示词 41、短名单 30、`CHECKS` 17、`preflightStages` 7、`KB.SECTIONS` 18、`playbooks` 5、领域命令 13),`G-03`/`G-11` 未摘、`expert.evolve` 仍不进 `steps`;十组变异各红各的,并如实登记**一次变异施加失手**(码插到单行 `const GATE = {…};` 之后成了合法的标签语句,全套全绿,与"判据没接住"在跑分上一模一样);数字 live 现取(unit 524 → **528**、`contract` 114 → **116**、`integration` **141** 与 `cli.smoke` **105/107** 未动但都复核过,不同名字数与用例数**连续第十六槽相等**,索引份数 170 → 174 含本文,四个 `FLOOR` 一并按 live 抬;**`w155` 与 `w156` 自称的记账件份数逐字相同的 163 且与 W157 那三支的 163 也相同,连着两槽落在同一个数值上**,而 `w159` 自称的 `integration 141`/`cli.smoke 107` 恰等于 live 是结构事实);**本刻远端未合 2 条**(在飞的 W158、W160 周期 10 审计),这个 2 是任务明确要求留着 | 想知道主干现在是什么状态;要合**两侧插在同一个锚点上**的产品改动、要合一支**自称与已合入支逐字节相同**的槽(先比 patch 再判会不会给冲突块,并留神 `M == P1` 不等于 `--ours`)、要处置一条**与已合入支结论相反却捆着无人接管面**的用例,或要照抄「翻面而不是接位:被推翻那格反向立住、没人接那格原样留」「按名比对的第三种比法:被合入支名集 ↔ 合并后」「变异先现取被改对象的实际产出再看颜色」这几手时 |
| [w162-no-placeholder-zip.md](./w162-no-placeholder-zip.md) | 交付包占位空壳这个**字面**不许回来(W157 残留 1 的正面收口,**产品代码零改动、只补判据**):`downloadReleaseZip` 那两处占位——打包前先 `ZipUtil.download` 一份只装 `PLACEHOLDER` 空条目的 zip、落地失败时兜底 `ZipUtil.download` 一份只写着「请重新打包」的空壳——早已删干净,但删干净这件事**没有任何一条判据钉着**:W152 与 W154 两支对同一行给出逐字节相同的 hunk,`git` 判成两侧同向自动收敛,**是结构上守住的不是人手守住的**,而它守得住的前提正是两支 diff 逐字相同;最接近的既有守卫是那两条行为面用例,钉的却是「用户到手几个文件」与「报错带不带原因」,**不是「占位这个字面不许出现」**(开工时现取核实:`js/` 侧 `PLACEHOLDER` 零命中,`tests/` 侧两处全在注释与报错句里,一条断言都没以这个字面为判定对象,故不是「字面已钉」)。补两条各守一面:**源级**——`js/release.js` 的可执行行零 `ZipUtil.download`、零 `PLACEHOLDER`、零「请重新打包」/「空下载兜底」,反面钉住真形态还在(落地动作全函数只此一次 `a.click()`、落的是 `buildReleaseZip` 那份 bytes、失败仍以 `throw` 收尾)并把 `ZipUtil.download` 的调用方名单钉在 `exporter.js` 一家;「可执行行」按**行首形态**判(整行注释与空行豁免)而不是「先剥注释再扫」——过度剥离是漏判、比误判危险,故 `'http://x'; ZipUtil.download(…)` 这类骗不过它,同时讲占位历史那几段注释原样留着;`PLACEHOLDER` 那格**区分大小写**是实测定下来的(第一版写 `/i` 时被发布说明输入框的 `placeholder=` 属性逮住);**运行期**——桩打在 `ZipUtil.download` 函数对象上(`const Z = ZipUtil; Z['down'+'load'](…)` 这类别名写法源级扫不出来),成功 / 落地失败 / 交付弹窗打包按钮**三个入口**一次都不许叫到它,逐份落地文件按 zip 解出条目对着打包清单点,条目数 0/1 的空壳当场红。**判据只扫 `js/`**,`tests/` 里 zip 夹具的假文件名一概不管——测试要造得出空壳才验得了「空壳当场红」,禁掉夹具等于把验证手段一起禁了;W154 的抛错失败路径与 W149 的 `storyboardFailed` 一行未动(`git diff -- js/ server.js cli.js mcp.js` 空输出)。七条变异各红各的,**M3 与 M4 是这两条各自的存在理由**:占位落在 `opts.warmup` 这种现有用例走不到的分支上时三条既有用例全绿而**只红源级那条**(净增覆盖就是这一格),别名写法时**源级如实扫不出来而运行期那条逮住**;另有占位原样加回红 4、兜底空壳换个文件名加回 `catch` 红 4、别的模块新开一个 `ZipUtil.download` 调用点红 1、`downloadReleaseZip` 改名红 1(取数口失效先红,不留成恒真)、落地改成就地打一份单条目 zip 红 3(含「到手的这份只有 1 个条目,是空壳」);数字 live 现取(unit 524 → **526**,`release` 27 → 29、`contract` **114** 与 `integration` **141** 未动,不同名字数与用例数相等,单元 `FLOOR` 与记账件 `FLOOR` 及两处 README 明写数按 live 抬),棘轮逐格实测**单元 `FLOOR` 那格被基线父值 524 蒙过去了**(差 2 压在 `SLACK` 3 之内,成因是本槽只加 2 条)而 README 两格差 1 就红,如实登记进残留 | 要给「某个字面 / 某处写法不许回来」立判据(先分清行为面钉的到底是不是那件事)、想知道源级行扫与运行期打桩各自逮得住什么逮不住什么、要照抄「按行首形态判可执行行而不是先剥注释」「用一条现有用例走不到的分支做变异来证明净增覆盖」这两手,或要改交付包落地口时 |
| [w163-review-min-literal.md](./w163-review-min-literal.md) | `Domain.REVIEW_MIN` 的注释写着「分集状态与主线审片步骤共用的唯一常量」,而智能审片闭环 `js/produce.js` 里冻着 `if (r.score >= 7) { … s.confirm = true; }` 第二份字面:**两份数恰好相等故一条判据都量不出来**——在基线 `8507194` 上只把常量挪到 8(连同两句钉住 7 的断言同轮改)实测 `unit` **524/524**、`integration` **141/141** 全绿,而闭环仍按 7 判达标并替用户确认,`counts.unconfirmed` 因此归零、`Domain.episodeState` 落到均分那一支上按新线判 `needs_human`、发布门 G3 同时 fail——**「系统替你确认了」与「该转人工」在同一集上同时成立**,用户只看得到「✅ 7.0 分,达标(已自动确认)」;摊开数同一条线实际写在**九处**(确认闸 / 闭环开场文案 / 跑批中心整集均分标 / `js/review.js` 的重抽入口与整集报告三处 / `js/sb-views.js` 状态条与版本区 / `js/agent-ops.js` 对话流两处文案 / `js/wf-core.js` 的 `W.MEM_LOW_SCORE`),其中 `MEM_LOW_SCORE` 最值得单说——它不是裸数是**登记过的常量**且注释写明「与审片报告重抽入口、发布门 G3 默认阈值同数」,**把"我知道我在抄"写进注释并不能让两份数在挪动时一起走**,故删掉而不是改成别名(留别名等于留第二个可被就地改写的名字);九处一律改现取,文案按 `Domain.REVIEW_MIN.toFixed(1)` 现算;**两处有意不收编并写明理由**——发布门 `releaseMinReviewScore` 是用户可调严的偏好设置(领域常量不读 Store,自 W3 起有意两份)、审片提示词里「≥8.5 优秀,7~8.5 良好,<7 需返工」是给模型读的打分刻度且**没有一处代码读它**(只把 `<7` 换成变量会让三档读成自相矛盾的刻度),直方图的 `8.5`/`8` 同理是评分档不是达标线;判据两层——行为面**不写死数字**(压线那一分现取 `Domain.REVIEW_MIN`,再在运行期把沙箱常量挪一格看闭环跟不跟),自相矛盾态单立谓词「全镜都被闭环确认掉 ∧ 分集状态判 `needs_human`」而不是简单断言 `status !== 'needs_human'`(不达标那一路本就该停在 `needs_review`,后者会把正常态判成异常);源级那条**分两档**——严格档(produce/sb-views/agent-ops 三份里 `score`/`avg` 不许跟裸数字比、达标线文案不许写死数字)与分档档(review/batchops/wf-core 另有 `8.5`/`8` 故只禁达标线那个数,而这个数**现取常量拼进正则**、判据自己不当第十份字面,`(?!\.\d)` 那道否定预查是必需的——`\b` 在数字后遇上 `8.5` 的 `.` 照样成立,不挡一下达标线挪到 8 就会把「≥8.5 优秀」误报成分叉),外加与 W136/W150 同形的**反向两句**钉住那两处有意不收编的此刻确实还各是各的;既有 `assert(dom.includes('D.REVIEW_MIN = 7;'))` 改成现取常量拼串(它钉的是"仍登记在 Domain 一处"、与值是几无关,写死 7 只会造出第二个要人工同步的数),而 `assertEq(sb.Domain.REVIEW_MIN, 7)` **留着**——挪达标线自此只动**两处**;八条变异各红各的且**含一条与 §1.2 逐字同手的对照项**——常量 7→8 只改那一句产品口径断言,基线上这一手 524/524 全绿而闭环停在 7,现在同一手**红 2 而九个消费点一个没红**:红的是按旧线挑的测试数据(`sh0: [5, 7.5]` 那个夹具,**有意不改**——它同时如实记着"今天 7.5 分算过",挪线时该由挪线的人重挑)与审片报告 `score >= 8` 色阶**撞了号**(报红是对的:两条线同数时该当场重判那个色阶算哪一档),即"挪一格"要动的地方从九处消费点变成常量 + 一句产品口径断言,余下两类都会当场报红点名不再静默分叉;判据侧那份字面同轮也收了——`agent-ops` 套件钉低分 chip 文案的 `/低于 7 分/` 改成现取常量拼串(文案已派生而判据写死会让常量挪动时**假红**:红的不是文案错了而是判据没跟上);`loadSbViews()` 同轮补 `loadFile(sb, 'domain.js')`(状态条现取常量后沙箱缺 `Domain` 会直接炸);**不抬发布门**(`js/release.js`/`js/release-core.js` 一字未动、没让任何门去看 `p.script`)、**不摘 `gaps`**(仍 20 键,`G-03` 与 SK-25 的 `pending`/`note` 一个字没改,仍欠的形态面照旧欠着)、提示词字节零变化、`server.js`/`cli.js` 扫下来无同类字面故两端零改动;数字 live 现取(unit 524 → **526**、`contract` 114 与 `integration` 141、`cli.smoke` 107 未动,`README.md` 两处写死的「达标线 7.0」同轮改成常量名——散文里的第十份字面同样会分叉,而它还是用户读到的那一份;两个 `FLOOR` 与明写份数按 live 抬) | 要挪审片达标线(改常量 + 那一句产品口径断言,两处;若源级条报「review.js 不得再按达标数 8 自判一遍」那是撞上了直方图的 `>= 8` 色阶,该当场重判它算哪一档而不是放宽断言)、要给 `Domain.REVIEW_MIN` 加消费点(直接读,别在调用侧比数字;属"达标判定与确认闸"那一圈的新文件加进源级那条的 `STRICT` 表)、要收编发布门那份可配阈值或提示词三档(先改记账再改反向那两句),或要照抄「判据的数现取常量拼进正则、判据自己不当第 N 份字面」「变异集里放一条与病灶实证逐字同手的对照项来区分两种全绿」「自相矛盾态单立谓词而不是断言某个状态不出现」这三手时 |
| [w164-low-review-shotids.md](./w164-low-review-shotids.md) | 问题中心的 `low-review` 自 W131 起就走 `Domain.reviseTargets` 算出了重抽面,却只把它拼成给人看的 `detail`(`低分镜:1镜4分、3镜3分`),`shotIds` 恒为空——编排层(CLI `issues` / MCP `hujing_issues` / 助手工作台)要拿这几镜就得照文案回抠(抠到的还是**报告里记的旧位**,分镜表调过序就抠错镜)或自己再筛一遍 `score < 7`,第二份名单就是这么来的;本槽让这条把子集一并投出来,**取 `Domain.reviseShotIds` 本身**而不在调用侧把它的函数体(`lows.map(x => x.shotId)`)抄一遍——值一样也是第二份投影,口径改了它不跟着走(变异实测:抄一份全绿于行为面、只红在取数口那条);**空重抽面不出这个字段**而不是出一个 `[]`,理由不在本层——命令层与 CLI 的子集位都是 `Array.isArray(args.shotIds) && args.shotIds.length ? 子集 : 整集`,空数组在那儿等于"整集",带一个空 `shotIds` 会把只该修几镜的活儿放大成整集重跑(三种走得到:报告只有 `avg`、低分镜全定稿、低分镜全被删);**仍不挂 `cmd`**(三条现成纪律同向:SK-25 第二步原话是「按审片问题修订提示词后只重跑低分镜」故提示词不动按下去只是再抽一次卡、`stale-shots` 同样算得出子集而有意不挂命令、审片侧计费动作问题中心自 W54 起一律不代按),`shotIds` 是给人挑的清单不是替人按下处置,这个决定由用例反向钉住(挂上 `cmd` 即红);产品代码只动 `js/issues.js` 一处 `if` 加一行,`issues-ui.js` 的 `fixIssue` 与 `cli.js` 的 `issues` 投影本来就在透传这个字段故两侧一字未改;另量出一处判据缺陷并当场收掉——`assert(/写法/.test(src))` 这种"某写法在这个文件里出现过"的断言,在同一写法有多处时(`js/commands.js` 的子集位有两处)拦不住其中一处被改,改成逐处 `matchAll` 各判一次并把 `cli.js` 那处一并纳入;数字 live 现取(unit 524 → **526**、`contract` 114 → **115**、`integration` **141** 未动但实跑复核过,两个 `FLOOR` 与明写份数一并按 live 抬) | 要动问题中心条目的 `shotIds`/`cmd` 形状(尤其想把某条导航类改成一键处置)、要给 `Domain.reviseTargets` 加第六个消费点、想知道"为什么低分审片不给一键重生成",或要照抄「值一样也算第二份投影」「只判某写法出现过的断言在多处同形时拦不住」这两条时 |
| [w165-review-issues-align.md](./w165-review-issues-align.md) | `workflow.review` 与问题中心的**分集级审片门槛**不同(W130 周期九走查 3.3 节登记、当年判成"展示噪音"的那一条):开工现取核实,噪音之外还有三格**同一集同一份报告一边说卡一边说通**——缺正文 / 未拆镜 / 分镜表判旧的集手上留着一份均分 5 的旧报告时,流程条按 `low-review` 报「1 集审片均分低于 7」(那份审的根本不是当前分镜表),而分集状态与问题中心一致地说"该去拆镜";根因是门槛在 `episodeState`(靠 `no-script`/`counts.total`/`shotsStale` 三处早退隐式表达)与问题中心(同三关早退)是一套、在 `D.workflow` 的 `review` 步压根没有,后者只判了"报告判旧没有"。落点在两侧共同的上游:`episodeState` 归一个 `reviewGate` 出来(`unready` / `no-review` / `review-stale` / `low-review` / `pass`,后三档的码字面就是 review 步阻塞码与问题中心 kind,三关逐字取自本函数原有的表达式、一条判据没新写),`review` 步逐档只数它、项目级推荐动作按它挑集、问题中心三态按它取——`Domain.REVIEW_MIN` 与 `st.reviewStale` 在 `js/issues.js` 归零,三态互斥不再靠 `!st.reviewStale` 守卫而是由档位只能取一个值保证;两侧的**条目形态一字未改**(流程条仍出项目级三档集数、问题中心仍逐集出条目),统一的只有"哪些集该进这三档",故 W138 交接第 1 条那个"项目级汇总与逐集条目谁承接"的问题不在本槽射程内;另记为什么对齐方向不是二选一(分集状态与计划层这两个第三方本就与问题中心同门槛,四个面里三个已同一道门)、为什么发布门 G2/G3 不改判,以及一处如实登记的观感变化(主线真卡在审片而某集不可审时推荐动作由「整集审片:<该集>」变 `null`)。三条用例:domain 那条逐关钉门槛与 `review` 步零阻塞项、issues 那条七集一个项目**双向相等**(多报一条也红)并钉住三档集数只数可审集、contract 那条钉源级单源;七组变异各红各的(退回基线红 3、判据抄回第二份**行为完全一致**只有源级那条接得住红 1、三关各去一关各红 2、推荐动作退回旧筛法红 1、**朝另一个方向对齐**(问题中心也报不可审集)红 2 且撞红 W54 那条既有断言) | 要判"这一集当下能不能审"(第五个消费面别再按 `reviewAvg` 比一遍)、想收这类**同一个断点两侧各设一道门**的账、或要接手 `js/pipeline.js` 的 `unRvEp()`(第四份同口径取数,本槽有意没碰,理由与改法记在交接第 2 条)时 |
| [w166-integration-log.md](./w166-integration-log.md) | 四条槽(W158 护栏主题清单 `GUARD_TOPICS` 给「这道护栏还在不在」一个不随用例名走的落点、W160 周期 10 独立核验件、W162 占位空壳字面源级 + 运行期两条判据、W164 `low-review` 带上派生 `shotIds` 子集)并进同一条集成线的记录:四次合并 4/4 取 head、`--no-ff` 4/4、**4/4 都是真三方**,注册表五个文件相对基线零 diff 故八个治理数结构性成立,产品面只有 `js/issues.js` 一个文件 13 加 2 删;**一条新护栏在落地当轮就被集成线的实况判红而它红得对**——`plans-gate-read` 那条在自己叉点上落点 1 处、在集成线上是 2 处(基线里 W153 那条逐码扇出读的是同一份实况),处置按判据自己写明的规矩把 `hosts` 抬到实况,**那是收严不是放宽**(缺省 1 在集成线上压根跑不起来,抬到 2 之后落点掉回 1 也会红),但「锚点太泛」与「这道护栏真有两个承载」在报错句里逐字同形、分开靠人读那两条用例在判什么;**源级判据与运行期判据的分界这次被实测量了出来**——变异 M3′ 在成功路径插一行 `const Z = ZipUtil; Z.download(…)` 且不带占位字面,现取 `js/release.js` 可执行行里 `ZipUtil.download` 命中 0 处,实测只红 2 条(行为面 + W162 运行期),**基线里 W156 翻面那条与 W162 源级那条全绿**,故三条判据各守各的一段、没有一条是另一条的子集(反过来 M2 退回兜底空壳红 5 条,三条都在里头);`js/issues.js` 两侧都改过而 `git` 零冲突块自动合上,**「零冲突块」与「两侧都落地」是两回事**——靠四棵树 blob(M 与 P1/P2/B 三者都不等)加逐面现取分开,变异 M6 给 `EPB` 新增一档反向再证 W155 那半确实还在跑;**四条槽里有两对自称数逐字相同**(`w158`/`w160` 份数都是 167、`w162`/`w164` 份数都是 171 且单元数都是 526,成因是两两同叉点各加一份记账件),同一个静默窗口一槽内出现两次而两次都没能静默;按名成集**三轮**比对(首尾 / 逐次 / 被合入支名集 ↔ 合并后)全部零丢失,而第三轮抽名时 JS `Array.sort()` 与 `comm` 的排序口径不一致**假报了 2 条**(同两个名字同时出现在两侧),这是 W109「去重口径不一致会凭空多报一条」的另一面;另记一条变异施加失手的**新形态**——脚本锚点不存在故抛异常、文件一字未改而全套全绿,与 W161 那次「改上去了但没生效」成因不同而跑分同形;数字 live 现取(unit 528 → **534**、`contract` 116 → **119**、`integration` 141 与 `cli.smoke` 105/107 未动但都复核过,索引份数 174 → 179 含本文,四格下限棘轮同步上调) | 想知道主干现在是什么状态;要合一条其护栏登记会被集成线实况判红的槽(留神抬 `hosts` 与收窄锚点的报错句同形)、想知道源级判据到底封不封得住别名写法,或要复核零吃测那三个口径(去重 / 排序 / 切回执载荷)时 |
| [w167-extractdone-split.md](./w167-extractdone-split.md) | 拆集的切分输入收进 `Domain.projectScript`(项目整本原文这一份下游输入),W161 残留第 9 条就此闭环:门槛派生 `gateBlockers` 答的是「剧本这一步走过没有」(`extractDone` 也算走过),拆集要的是「整本原文读不读得到」——提取过主体却没存原文的老项目上两问结论相反,而计划层只看得见第一问,于是推出一条注定 `blocked / no-script` 的拆集命令步;**先把"提取产物能不能当拆集输入"逐项核死再落地**(主体库是名字与提示词、`scriptMeta` 是 ≤220 字梗概与 4–6 句大纲、`p.episodes[].content` 那条回退在"无分集"这一态上恒为空——而无分集正是拆集步的出场条件,全仓也没有第二处存过整本原文的副本),结论是**不能**,故首选落法(拿提取产物继续跑)一律不做,改把第二问的判据从拆集命令入口收进 `Domain.projectScript` 一处、命令层与计划层同读:命令层取值逐字节不变(这类老项目**仍然 blocked**),计划层按同一份派生分两态——原文在库出可执行的拆集命令步,原文缺位出「补上剧本原文」的导航步(步照出、位置照旧、`key` 仍是 `split`,既不推注定失败的命令、也不静默把这一步藏掉);`gateBlockers` 判据与产出形状一个字未动,发布门 G1–G10 一字未改;四组变异各红各的,其中 **M3「假装打通」**(把派生改成拿 `extractDone` 冒充原文)红 4 条、**M4「静默避开」**(无原文时 `return null`)红 2 条,两条底线各有夹具接住 | 要动计划层与命令层之间"同一件事两个面结论对不对得上"这类口径题,或要照抄「先核死前提再决定落哪条法」「两问不许互相冒充:进度归进度、输入归输入」「不推注定失败的步 ≠ 把步藏掉」这几手时 |
| [w170-integration-log.md](./w170-integration-log.md) | 三条槽(W163 审片达标线九处现取 `Domain.REVIEW_MIN`、W165 分集级审片门槛收成 `episodeState().reviewGate`、W167 `Domain.projectScript` 立成整本原文的单源并让计划层在原文缺位时出导航步)并进同一条集成线的记录:三次合并 3/3 取 head、`--no-ff` 3/3、**3/3 都是真三方**,注册表五个文件相对基线零 diff 故八个治理数结构性成立,产品面十个文件 68 加 40 删;**产品文件冲突块回来了且两个都在同一次合并里**——`js/domain.js` 那块是两侧改同三行(我方 W155 把码字面换成 `EPB.*` 登记表取值、对侧提 `hasScript` 供 `reviewGate` 复用)取并集,`js/issues.js` 那块两侧改的其实不是同一行(对侧只重写段头注释,`git` 把紧邻上一行也划进块内,而那一行我方是 `sev: EPB['unconfirmed']`、对侧是 `sev: 'low'`),**机械取对侧行为一模一样、`git` 不报、单测全绿,只会在下次改 `EPB` 档位时才悄悄分叉**,而同一文件相邻一段的 W164 `shotIds` 三行零冲突块自动留住——"给冲突块"与"两侧都落地"在同一个文件的相邻两段上同时出现正反两例;**「两槽真撞、两侧都留」这次被实测量了出来**——W163 记的那一手(常量 7→8 + 同轮改那句产品口径断言)在集成线上逐字复跑仍是**红 2**、九个消费点一个没红,而 W165 新开的两个 `reviewGate` 消费面**也一个没红**(它们读的正是同一份常量),即两槽收的是同一条线的两段(W163 收「值」、W165 收「档位」),合起来「挪达标线只动两处」这句话仍然成立;只挪常量不改那句断言则红 3,多出来的正是 W163 有意留着的那句;**「值一样也是第二份投影」第二次被实测**——把 `st.reviewGate === 'low-review'` 换回等价的 `reviewAvg` 三段式,行为面与双向相等那条全绿、**只有源级那一条红**(与 W164 的 M5 同形,双向相等那条看着最像能接住实际接不住,因为这一改把两侧一起改了);任务点名的四样各反向变异一次全部红(`EPB` 扇出红 1、`shotIds` 空面闸红 1、PLACEHOLDER 禁令红 6、`GUARD_TOPICS` 删护栏红 4、`evolve` 进 `steps` 红 3),另有一条**「不要把 `extractDone` 当原文」的正面变异红 4**;**另量出一个差并登记进残留**——本槽三支新加的三条源级护栏一条都没进 `GUARD_TOPICS`,删掉登记过的那条红 4 并点名主题编号、删掉本槽新加的那条只红 3(全是数字层),而数字层拦不住「删一条 + 同轮把 README 与 `FLOOR` 一起改小」;三支自称的记账件份数里 `w163`/`w165` 逐字相同的 171(同叉 `8507194` 各加一份),**静默窗口连着第三槽出现**,而记账件 `FLOOR` 这一格三次合并一次都没动过(三支谁都没加记账件),直到本文落地才一次抬到位;按名成集三轮比对全部零丢失,而第一轮**假报 8 条**——抽名脚本里写了 `LC_ALL=C sort` 而调 `comm` 那一层没写,是 W166 残留 4 的第二种踩法;数字 live 现取(unit 534 → **542**、`contract` 119 → **121**、`integration` 141 与 `cli.smoke` 105/107 未动但都复核过,索引份数 179 → 183 含本文,四格下限棘轮同步上调,记账件那格差 4 压着 `SLACK` 上沿过线) | 想知道主干现在是什么状态;要合两支**改同一函数同几行**的产品改动(留神冲突块会把紧邻的无关行一并划进来,机械取一侧可能静默退回被收编过的字面)、想知道两条冲上同一条业务线的槽合起来会不会互相削弱、要复核零吃测那三个口径(去重 / 排序 / 切回执载荷),或要照抄「判 `--ours`/`--theirs` 要看另一侧等不等于叉点而不是看 `M` 等于谁」「用一条同手法但换对象的对照变异量出护栏登记面的缺口」这两手时 |
| [w178-topic-floor-unlist.md](./w178-topic-floor-unlist.md) | 护栏主题的**销号从此非留痕不可**:`GUARD_TOPICS` 的下限此前只是个可改的数字,把 `TOPIC_FLOOR` 改小而一条主题都不删、连同下限一起把某条主题整段删掉(完整的销号动作)两种改法实测各红 0——即撤掉一道护栏可以一个字都不留。本槽把"哪些编号登记过"从数字换成**逐号点名的花名册**(写在记账件里、与源码分开,取空即红),并开出只能显式落笔的销号台账 `GUARD_TOPICS_CLOSED`(锚点原样搬过来、闭合理由不许空也不许照抄 `why`、锚点仍点得到承载用例时须写明接手编号且接手方须在册);`TOPIC_FLOOR` 不得低于花名册条数,护栏主题这一格并进既有 `SLACK` 棘轮成第五格(live 取在册 + 销号的总数,故销号不会把这个数带下去)。判词抽成纯函数、拿造出来的四元组逐路喂它(真删一条登记要改文件,单测做不到),另拿真清单抽掉最后一条再喂一次证明对实况同样咬得住;**合法增主题一条不红**(花名册没有的号是允许的)。静默销号的成本从"改一个字面"抬到"四处齐改"(实测四处齐改仍红 0,如实登记),另两条残留:花名册只覆盖写进它的号、闭合理由的内容没有判据 | 要撤掉/改写一道已登记的护栏主题,或要照抄「把一个可改的数字换成逐号点名的清单」「给'放松'开一条必须写下理由的显式出口,而不是一禁了之」这两手时 |

## 一分钟摘要(周期 2 收敛后)

- 资产不缺,缺索引:知识在 `js/knowledge.js`(18 条目)、提示词在 `js/prompts.js`(41 条)、人设在 `js/experts-data.js`(16 专家)、编排在 `js/cmd-registry.js`,层与层之间的按主线步骤索引由 `js/skills.js`(30 条内部能力)承担。**KB 条目的登记面已收成契约**:`KB.SECTIONS` 与索引宿主 SK-01 的 `kb` 双向逐条对齐、宿主内不重复登记、不得出现第二个全库宿主,新增条目漏登即红并点名缺哪个键(见 [w30-kb-skill-cover.md](./w30-kb-skill-cover.md))。
- 主线七步在代码里齐了:`Domain.workflow` 已含"审片"步(G-03),`js/skills.js` 的 `STAGES` 七步全部 `wfStep: true`。**G-03 的修订闭环那一半随 W131 落了一面**:「该重抽哪几镜」收进 `Domain.reviseTargets` 双端单源(达标线、报告判旧、与分镜表取交集、定稿不重抽、实位序五条判据一处),`WfCore.reviseSubset` 在其上补逐镜修正意见,CLI 闭环每轮现取实况派生 `shotIds` 而不再摘回执名单,服务端回执/助手摘要/审片完成卡/问题中心四处同读这一份;`G-03` **未摘键**——收敛次数无登记口径、两端闭环形态不同构两面仍欠,见 `w131-g03-shotids.md`;这两句欠账随 W136 补上双向护栏(段与锚点在不在、点名的余量对照源码此刻确实还在),抹段、写成已清、摘键与"真收编了不改记账"各红一条,见 `w136-sk25-owe-guard.md`。**收敛次数那一面随 W150 落地**:取值口径(整数轮次、1-5、缺省 2、候选值按优先级择先)收进 `Domain.reviseRetryLimit`,原先各自钳一份的三处(`cli.js`/`js/produce.js`/`js/commands.js`)与两个 UI 面板一律现取,小数轮次改为截整(此前 `maxRetry=1.9` 会让浏览器闭环既不达标也不登记待人工,质量闸门按 `manual > 0` 判就放行了);W136 那条护栏两向翻面重写而不是删,`G-03` 仍不摘键、仍欠的只剩形态那一面,见 `w150-revise-maxretry.md`。
- 贯通缺口已收口的部分:专家人设(G-01)与协作记忆(G-02 由 agent-flow 覆盖)进 `/api/wf/*`,CLI/MCP 同链路吃到;剧本拆集(G-04)补上机读入口,headless 可从"一份整部剧本"起跑,其 LLM 步也已接入同一注入链(见 [w8-split-episodes-inject.md](./w8-split-episodes-inject.md))。
- 空挂已清:`settings.tplVideo`(G-05)与 `KB.SECTIONS`(G-15/G-08 的 KB 侧)都有了消费方,并有断言防回退。生成步的注入面亦已出块——`Skills.block('gen')` 逐字节等于提示词改写人设 `WfCore.genPromptSystem` 的方法论段(抽卡公式+抽卡军规),SK-21 的 `pending` 随之清空(见 [w15-gen-block.md](./w15-gen-block.md))。G-06 的**注入半到此闭合**:残留两条也已进主线提示词构造点——「多镜头写法」进拆镜人设 `WfCore.sbSystem`、「主体参考」进主体提取人设 `WfCore.extractSystem`,两处缺省提示词随之变长,拼块逐字节对账与兼容影响见 [w19-g06-inject.md](./w19-g06-inject.md)。**G-06 的校验半随后也闭上**:主体参考纪律(SK-11 `subjects.genRefDiscipline`:参考人物超上限 / 有图被参考图组上限挤出 / 三视图当视频参考)与多镜头写法(SK-13 `subjects.multiShotPrompt`:图生视频缺一致性声明 / 首尾帧写大幅动作 / 一镜切太碎)以生成前 warn 落地,判定输入是 `Domain.buildVideoRequest` 那份真实请求,经就绪检查双端消费、只报不拦,`Skills.gaps()['G-06']` 随之归空(见 [w22-g06-check.md](./w22-g06-check.md))。该人设键的取值口已收编到底:审片一键优化与 CLI 修订重抽改经 `WfCore.optimizeSystem` 只取人设句(缺省逐字节不变),覆盖两端一并跟随(见 [w18-gen-prompt-unify.md](./w18-gen-prompt-unify.md))。
- 校验宿主七面齐了:剧本面(S-01,SK-07/08/09;文案 AI 味 SK-10 的校验半随 W24 落地,见 [w24-sk10-ai-voice.md](./w24-sk10-ai-voice.md),注入半随 W27 闭合 S-02——自撰条目进 `KB.SECTIONS` 后校验层的三张词表改现取条目正文,注入与校验共用一份判据,见 [w27-sk10-kb-inject.md](./w27-sk10-kb-inject.md))、主体面(S-03,SK-11/12/13 共四条)、分集面(SK-14/15)、分镜面(SK-18 景别衔接,级差取 `WfCore.sizeGap` 词表单源;SK-19 抽卡稳定词,判定输入是 `Domain.buildVideoRequest` 装出的真实提示词,见 [w26-sk19-stable-lex.md](./w26-sk19-stable-lex.md);SK-20 镜头动态感准入,动作幅度判动作描述那段、运镜条数判装好的真实提示词、镜长分布取该请求的 `duration`,见 [w33-next-pending-check.md](./w33-next-pending-check.md))、生成面(S-05,SK-22 生成凭据与确认失效,判旧与就绪取 `Domain.shotVideoStale/shotVideoReady` 单源,见 [w28-sk22-gen-check.md](./w28-sk22-gen-check.md))、审片面(G-10 的第二半起步,SK-24 方法论维度进审片报告:判定输入是已成型的报告,四维维度名取 `WfCore.normalizeCut`、整份判旧取 `Domain.reviewStaleByScript` 单源,见 [w35-sk24-check.md](./w35-sk24-check.md))、成片面(S-06,SK-28 字幕质检;SK-29 上游定稿契约,板块链序取 `Skills.STAGES` 主线七步、产物实况取 `Domain.workflow` 同键步骤,见 [w37-sk29-check.md](./w37-sk29-check.md))共十七条 `Skills.CHECKS` 校验项,剧本面与分集面成对闭合 S-01、主体面另接住 G-06 校验半;两端就绪检查按主线步序同挂 `result.checks`,问题中心同挂低危提醒——纯本地零 LLM 零计费,只报不拦。**问题中心那侧的七条低危提醒也收成了一张单源表** `Issues.reminders()`(面 → 校验项 id → `kind`/`sev`/挂载级别 + 早退分支前后与明细写法),`collect` 只按表跑、模块里只剩一处校验项取值点;同轮 `js/issues.js` 收成与 `js/domain.js` 同形的双端 UMD(项目树与 `online` 经参数注入、不碰 `window`),浏览器 UI 段移到薄封装 `js/issues-ui.js` 而全局名与成员不变,headless 侧新出 CLI `issues` 与 MCP `hujing_issues`(只列不处置、零计费),见 [w59-issues-umd.md](./w59-issues-umd.md)。**面清单现为双端单源表 `Skills.preflightStages()`**,由注册表现推(校验面已落地 + 登记 `episode.preflight` 消费点),两端只读该表 `concat`,**新增一面只改一处**(在条目上登记 `checks` 实现与 `cmds`),见 [w17-preflight-stages.md](./w17-preflight-stages.md);W22 的两条、W24 与 W26、W33 的各一条都落在已在表内的面上,故两端消费实现一行未改;W28 落地的是表里原本没有的 `gen` 面、W35 落地的是 `review` 面,两端消费实现同样一行未改——面表由登记推导,自动从五面变六面再变七面(主线七步至此全部在表内);W37 的 SK-29 落在已在表内的 `film` 面上,两端消费实现同样一行未改。七面并集与步序由行为断言(浏览器端真跑命令看回执)+ 面表源级断言(表与登记侧双向对齐 + 两端只读该表且写法逐字节相同 + 段内不得写死面名)锁死,断言分工的由来见 [w11-preflight-film-assert.md](./w11-preflight-film-assert.md);新增一面漏接或单端退回写死清单时这些断言先红,W13 收敛 SK-18 与 W17 收表时均实测接住过。
- 校验结论已进审片路径(G-10 的第一半):审片报告按镜只读消费剧本面与主体面,命中挂报告独立字段 `checks` 并在弹窗/导出各列一区——不并入 `issues`、不参与评分与达标线、不改发布门计数与计费动作,见 [w14-review-skills-check.md](./w14-review-skills-check.md)。**反向那一半也起步了**:审片报告本身成了判定输入——SK-24 的校验面(`review.methodDim`)逐集看报告里那几个方法论维度到位没有,经就绪检查双端消费、只报不拦,见 [w35-sk24-check.md](./w35-sk24-check.md);报告好坏优劣的语义判断仍归 LLM 审片。**发布门那一半仍未清**:SK-29 的**校验半随 W37 落地**(上游定稿契约的两向判定,只报不拦、不动 `js/release.js` 的 G1–G10 与 overall 计数,见 [w37-sk29-check.md](./w37-sk29-check.md)),方法论门挂进发布门的产品口径未定,G-10 因此仍挂账。
- 记账诚实位:`SK-03`/`SK-04`/`SK-23` 的 `infra` 面 `pending` 已按实况清空(三处出口 G-01/G-02/G-03 都已落地),三条 `note` 改为逐处点名各自仍欠的**覆盖余量**(四处装配口的 ops 协议半仍不开放覆盖、生成与合成两步没有可判定的结构化结论可回流记忆(主线其余六步的结论已回流)、审片报告好坏优劣的语义面仍待 G-10),并由断言钉在三处出口的实况上;缺口标记按**关联索引**口径保留——`gaps()` 只投影 `gaps` 字段、不看 `pending`,故「改 `pending` 会动 `Skills.gaps()` 投影」这条早先的顾虑**实测不成立**(有用例把三条退回后逐字节比对投影钉住),清账的行为面为零,见 [w36-infra-pending-align.md](./w36-infra-pending-align.md)。**W36 点名的第一处余量随 W39 补掉**:审片侧三步(分镜评审 / 整集共性汇总 / 四维成片评审)已接上人设与记忆通道,三步的 user 模板同经唯一注入段装配口 `WfCore.reviewCtxNote`,分镜评审随分镜板块 ctx、集级两步随成片板块 ctx 并按集标题召回,未雇佣专家且无沉淀记忆时输出逐字节不变,服务端不新增 `wfPersonaNote` 调用点,见 [w39-review-persona-mem.md](./w39-review-persona-mem.md)。**W39 点名的那处余量随 W40 收掉**:共性汇总步的人设句进注册表新键 `review.sumSystem`(`def` 与原内联字面逐字节相同),浏览器与服务端同键取值、服务端显式传 `settings.promptOverrides`,整集审片三个 LLM 步的 system 半至此全部可被用户覆盖;SK-03 的仍欠段随之换成剧本拆集步的内联人设,见 [w40-sum-system-prompt.md](./w40-sum-system-prompt.md)。**W40 点名的那处余量随 W42 收掉**:剧本拆集步的人设句进注册表新键 `split.system`(`def` 与原内联字面逐字节相同),浏览器 `js/episode-util.js` 与服务端 `/api/wf/split-episodes` 同键取值、服务端显式传 `settings.promptOverrides`;SK-03 的仍欠段换成主体提取步的人设句(`wf-core` 常量 `EXTRACT_SYSTEM`,双端同源但覆盖不到,故 `WfCore.extractSystem` 不收覆盖表参数),点名断言随之改为查注册表与装配口签名——那句字面在 `aiScriptDigest` 人物小传步还有一处同字面内联,查字面点不住,见 [w42-split-system-prompt.md](./w42-split-system-prompt.md)。**W42 点名的那处余量随 W45 收掉**:主体提取的人设句进注册表新键 `extract.system`(`def` 与原 `wf-core` 常量字面逐字节相同,常量随之撤掉),装配口 `WfCore.extractSystem(ov)` 开始收覆盖表参数、方法论段仍是 `KB.pick('主体参考')` 整条注入且不随人设覆盖变动,服务端显式传 `settings.promptOverrides`——`/api/wf/*` 五条工作流的 system 半至此**全部可被用户覆盖**;SK-03 的仍欠段换成 Agent 单轮对话步(人设句与 ops 协议同写在 `WfCore.buildAgentSystem` 一个模板串里,双端各一份措辞),见 [w45-extract-system-prompt.md](./w45-extract-system-prompt.md)。**W45 点名的那处余量随 W49 收掉**:Agent 单轮的人设句进注册表新键 `agent.system`(`def` 与原模板串开头字面逐字节相同),装配口 `WfCore.buildAgentSystem(ctx, ov)` 开始收覆盖表参数、服务端 `/api/wf/agent` 显式传 `settings.promptOverrides`——`/api/wf/*` 六个 LLM 端点的人设句至此**全部在注册表内**;**只收人设句是有意的**:其后的 ops 协议 / 命令白名单 / 返回 JSON 约定是 `agentNormalize` 的解析契约,做成可覆盖变量就能被用户一改改坏,故仍由 `wf-core` 拼,并由「覆盖只换人设句、协议半逐字节不变」一条断言钉住;SK-03 的仍欠段随之换成浏览器多轮那三份人设(分集面板 / 全局抽屉 / 预排模式,三种运行模式各一份措辞,不并进同一个键),见 [w49-agent-system-prompt.md](./w49-agent-system-prompt.md)。**W49 点名的那三处余量随 W51 收掉**:三份人设各成一条独立键(`agent.panelSystem` / `agent.drawerSystem` / `agent.previsSystem`,`def` 与三处模板串开头字面逐字节相同),装配口分别是 `AgentCore.panelSystem`(面板那处收编前没有装配口,system 模板夹在 `sendInner` 闭包里,本槽原样抽出)、`AgentG.buildGlobalPrompt` 与 `agent-ops.js` 的 `prearrPrompt`,浏览器隐式读 `settings.promptOverrides`——**单轮与多轮的人设句至此全部在注册表内**,且「覆盖只换对应那一份、另两份逐字节不动」有断言钉住;三条键**只有浏览器一个消费点**(headless 多轮不存在),故其「两端」如实记成落在取值口而非两个消费点;SK-03 的仍欠段随之换成四处协议半有意不开放覆盖 + 这条取值口口径,见 [w51-browser-agent-personas.md](./w51-browser-agent-personas.md)。**SK-23 点名的那处余量随 W54 收掉**:问题中心补上未审(`no-review`)与记录判旧(`review-stale`)两条中危投影,与 `Domain` 审片步的 `blockers` 同码同口径、判旧那条与发布门 G3「视为未审」对齐,三态互斥同集只报一条;判据一行不新写(现取 `Domain.episodeState`),挂载位置在未拆镜/分镜判旧的早退分支之后即"该集已有镜头"。同槽把 SK-19 稳定词面接进问题中心(`shot-stable-lexicon` 低危,warn 不抬成 G2 fail),W26 残留的第一条随之清账;SK-23 的仍欠段只剩审片报告好坏优劣的语义面(G-10),见 [w54-issues-unreviewed-sk19.md](./w54-issues-unreviewed-sk19.md)。**SK-04 点名的那处余量随 W53 收掉**:记忆补种与旧板块名迁移下沉 `WfCore.memSeed`/`memMigrateBoard` 双端单源,浏览器 `memAll` 与 headless 三个入口(`/api/wf/memory-seed` + CLI `memory seed|migrate` + MCP 同名工具)吃同一份种子表与迁移表,空板/未知板名如实报错不静默空成功,播种保持显式动作(读记忆时不自动跑,免得读一次写一次盘);SK-04 的仍欠段随之只剩自动沉淀那一半——理解/分镜/拆集/提取主体几步的结论仍不回流(回流面本身归 SK-26);**那四步随后随 W61 补齐**,SK-04 的仍欠段现只剩生成与合成两步(没有可判定的结构化结论可回流,素材产出的判定面归发布门 G4 过期镜 / G5 未确认镜 / G6 失败镜与问题中心 `failed-shots`——原先记的 `G3/G7` 是指错了门,G3 判审片均分、G7 判合规敏感词且不在 headless 七门内,随 W100 订正,见 [w100-sk04-gate-note.md](./w100-sk04-gate-note.md);这两步要不要回流本身经 W98 核实后**否决**,见 [w98-sk04-gen-compose-mem.md](./w98-sk04-gen-compose-mem.md))。见 [w53-memall-headless-seed.md](./w53-memall-headless-seed.md)——这份记账件一直在该槽的 tip 上,W57 当时只合到它的中间点,故连文件带索引行一起漏在集成线之外,随 W67 补合(见 [w67-integration-log.md](./w67-integration-log.md))。**注册表之外的同字面内联也清了一处**:剧本摘要全文通读(`EpisodeUtil.aiScriptDigest`)第五步「人物小传」原先写死了与 `extract.system` 逐字节相同的那句人设,现改经 `Prompts.get('extract.system')` 同键取值——字面同 / 角色同(都是"剧本分析助手"从剧本里认人)/ 产物落点同(都往 `p.subjects` 写 `character` 条目并过同一道可信性校验),故复用键而非新开,注册表条数不变;**只取人设句不接方法论段**(「主体参考」那 278 字讲的全是生图与参考图组纪律,该步不生图),缺省四步 `system` 逐字节不变、覆盖时提取步与小传步一并跟随;W45 为它留的「该字面在 `js/episode-util.js` 恰好 1 处」计数路障随之反转为零内联 + 注册表命中恰好一条,`aiScriptDigest` 由此有了第一条行为面断言(沙箱真跑、逐步截获 `system`/`user`),见 [w56-aiscript-digest-prompt.md](./w56-aiscript-digest-prompt.md)。W39 另修掉一个洞:记账件的点名断言原先**全文查 `note`**,余量补完后把锚点留在"已落地"那半即可蒙过去(实测把仍欠段换成「仍欠:无」仍全绿),现已收紧为只认「仍欠」之后那段。`check` 面随 W35/W37 全部落地,编排面剩下的两条随 W43(`SK-26` 回流面)与 W44(`SK-05` 主线全链投影)相继落地,**短名单至此已无 `pending`**;`G-11`(自进化)与 `G-12`(投影落点)按关联索引口径仍挂在各自条目上——`G-12` 的三个落点已全部接上——计划步骤随 W46、MCP 中段流程模板随 W52、发布留痕的命令化出口随 W58(判定与写回收进 `js/release-core.js` 双端单源,出口是领域命令 `project.release`);标记按关联索引口径仍挂在 SK-05/SK-25 上,SK-25 自己仍欠 `G-03`(修订循环的 `shotIds` 子集不由编排层推导)。**W56/W60 登记的那处基线记账不实随 W66 清掉**:SK-10/SK-11 的 `note` 原先都写着「人设句入注册表待 G-13」,而那句人设(`extract.system`)自 W45 就在注册表里、装配口也收覆盖表参数——两条改为写明人设句已在表,「仍欠」段各自改指真正还在的那一处(SK-11 是 `tplImage` 取用点即 `js/persona.js` 八维度重写步那份内联人设,SK-10 则先写明本条没有专属人设句、仍欠的是剧本模块解说体改写/导演阐述/光影总控/剧本围读四处内联),两处锚点各配一条"收编即红"的源级断言;SK-11 另按 SK-17/SK-21 同口径补登记注入落点键 `prompts: ['extract.system']`。`gaps` 一字未动(G-13 治的是全仓 21 处内联人设,未闭合),故 `Skills.gaps()` 的键数与每个键的值逐字节不变——W56/W60 顾虑的"改 note 会动投影"实测不成立,成因是被误当成期望值的其实是一句断言语,见 [w66-sk11-extract-note.md](./w66-sk11-extract-note.md)。**G-13 欠段又收一处**:SK-11 另一登记键 `settings.tplImage` 的取用点——`js/persona.js` 八维度重写文生图提示词那步——人设句进注册表新键 `persona.promptSystem`(`def` 与原内联字面逐字节相同),与只差一个字的 `gen.promptSystem` 有意不合并(一个出主体立绘、一个出镜头视频,参考图口径与下游消费点都不同);只收人设句(参考模板仍取 `settings.tplImage`、返回 JSON 约定仍由该步装配口拼),该步是纯浏览器链路故收编解决的是"可覆盖"而不是"可 headless";`settings.tplImage` 本身不记成缺口(用户一直改得到),SK-11 的 `note` 随之改写、仍欠段当时改指剧本板块那四步并配反向断言,`gaps` 与 `Skills.gaps()` 逐字节不变——**G-13 缺口仍开着**,见 [w69-persona-tplimage-prompt.md](./w69-persona-tplimage-prompt.md)。**那四步随后随 W71 收编**:旁白解说体改写 `narration.system`/剧本围读 `reading.system`/构思导演阐述 `concept.system`/全剧光影总控 `light.system` 各成一条独立键(四步角色分别是解说编剧、导演组围读会、开拍前定调导演、摄影指导,措辞与产物落点都不同,不合成带变量的一个键,也不与既有导演类人设复用),取值口都在 `js/episodes.js` 经 `Prompts.get` 隐式读覆盖表、`def` 与收编前的内联字面逐字节相同,同样是纯浏览器链路故收编解决的仍是可覆盖而非可 headless,返回 JSON 字段契约留在各自 user 半不开放覆盖;四键登记在 SK-03 名下,SK-10 本条没有专属人设句故只改记账,SK-10 与 SK-11 的仍欠段一并改指真正还在的那几处——`js/episodes.js` 事件图谱拆解步与 `js/episode-util.js` 剧本摘要的通读/汇总/集纲三步;`gaps` 仍一个不摘(**G-13 缺口仍开着,全仓内联人设现存 15 处**),见 [w71-script-four-prompts.md](./w71-script-four-prompts.md)。**W56 交接第 1 条点名的那三处随 W74 收掉**:剧本摘要链路(`EpisodeUtil.aiScriptDigest`)的分块通读/汇总/集纲三步写着同一句策划人设,先按 `cat -A` 逐字节核对三处 `system` 确认是同一句话的三份拷贝(不是三份措辞),再按"字面同/角色同/产物落点同"三条判据定为**一键三口**——注册表新键 `digest.planSystem`(`def` 与三处内联字面逐字节相同),三步各经 `Prompts.get` 取值,缺省四步输出逐字节不变;同函数第五步人物小传是另一个角色(剧本分析助手把人物落进主体库),仍听 `extract.system` 不合并,界线由"两键不同值"与"本文件里 `extract.system` 仍恰好 1 个取用口"两条正反断言钉住。SK-03 只补一个 `prompts` 键与一句 `note`,`gaps`/`pending` 一字未动(按同一口径实测,全仓内联人设自 W66 记的 21 处降到 17 处,G-13 仍开着),见 [w74-digest-three-prompts.md](./w74-digest-three-prompts.md)。**同口径又收两处半随 W76/W77 落地**:分镜脚本创作层两步(场次节拍拆解 `sb.boardSceneSystem` 是编剧、文字分镜拆解 `sb.boardDraftSystem` 是分镜师,措辞与产物落点都不同故两键不合并)在 `js/sb-board.js` 同经 `Prompts.get` 取值,注册表位置按 tab 次序排在智能分镜之前,见 [w76-sb-board-prompts.md](./w76-sb-board-prompts.md);分集页事件图谱拆解步的人设收进独立键 `graph.system`(角色是"剧本结构分析师",与剧本板块其余几步不同,不合成一个键),取值口在 `js/episodes.js` 逐集拆解那步就地经 `Prompts.get`,返回 JSON 的 `events` 字段契约仍留在该步 user 半,见 [w77-event-graph-prompt.md](./w77-event-graph-prompt.md)。**剧本模块两个文件(`js/episodes.js` / `js/episode-util.js`)至此内联人设归零**,SK-10 与 SK-11 的仍欠段随之改指真正还在的那几处(`js/agent-ops.js` 的执行核验器与会话纪要整理器、`js/sb-views.js` 的分镜改图专家),两处各配一条"收编即红"的源级反向断言;`gaps` 仍一个不摘(**G-13 缺口仍开着**),收敛过程见 [w80-integration-log.md](./w80-integration-log.md)。**W66 点名的"余量只在散文里有数字"随 W78 补上判据**:节拍板 AI 拆解步的人设句收进注册表独立键 `beat.system`(`def` 与原内联字面逐字节相同,其后 KB「六阶段结构/打脸四步」方法论段按键原样接,user 半的 5 段式返回 JSON 约定有意不开放——那半是 `applyAIFill` 的解析契约),新键登记在 SK-14(节拍板本就是它的注入落点,而该步不注生效人设、没有服务端对端,故 SK-03 一行未动);同槽**新立全仓内联人设持有者名单断言**(文件:处数,收编一处即少一处、任何文件新长出一处即多一处),G-13 的余量从此机器可查而不是每槽各写一个数字,`gaps` 按关联索引口径照旧一个不摘,见 [w78-beatboard-prompt.md](./w78-beatboard-prompt.md)。**注册表之外的内联人设又收掉一处随 W79**:漫剧编辑器「AI 生成对白」那句人设进注册表新键 `comic.bubbleSystem`(`def` 与原字面开头逐字节相同),取值改经 `Prompts.get`;**只收人设句同样是有意的**——同一串里的气泡条数 / 返回 JSON 形状 / `type` 词表是那一步的解析判据(`TYPES.includes` 归类、两种解析分支、`slice(0,4)`),故提成装配口常量 `BUBBLE_CONTRACT` 不开放覆盖,并由「覆盖只换人设句」与「契约半的 type 词表与解析侧 `TYPES` 同集」两条断言钉住;新键挂在 SK-03 名下,但「仍欠」段写清它**不过那条 ctx 通道**(编辑器工具步不注入生效人设与协作记忆,只是人设句进了表)。同槽另给 G-13 立了第二张口径的**全仓人设字面持有者名单**——按「以 `你是` 开头的字面」逐文件普查,注册表 `def`、专家人设库与其余模块内联混在一张表里如实计数(`js/api.js` 两处兜底缺省与 `js/gsettings.js` 一处占位文案也计进去,例外条款越多名单越容易被绕过),与 W78 那张只数系统人设位的名单口径不同、两张各自为政不互相折算,见 [w79-editors-prompt.md](./w79-editors-prompt.md)。**W76 点名 `js/gsettings.js` 的那一处随 W81 收掉**:导演设定五维(光影/色调/情感氛围/服化道审美/表演气质)的 AI 生成步人设进注册表新键 `dirset.system`(`def` 与原内联字面逐字节相同),取值口在 `js/gsettings.js` 的 `genDirectorSetting` 经 `Prompts.get` 隐式读覆盖表;**不与只差「影视/短剧」两字的 `und.system` 复用**——一个给全剧定一次风格五维、一个逐集读本,产物落点(`settings.directorSetting` / `ep.understanding`)与消费面都不同,合成一个键就是让用户改一次改掉两条链路。同为纯浏览器链路故收编解决的仍是可覆盖而非可 headless,五维返回 JSON 的字段契约与剧本前段摘取留在 user 半不开放覆盖;这一步比前几槽多一条边界——LLM 失败走 `dirFallback` 按风格回退,**覆盖只作用于 LLM 那一路、回退文案逐字节不跟随**,并专配一条断言钉住。同槽另立第三种口径的全仓内联人设持有者名单(判据收成机械扫描的三种形态:`system:` 值位 / 具名人设常量 / 装配函数直接 `return`,`js/experts-data.js` 的预置专家 `persona` 与输入框 `placeholder` 有意不在判据内),本槽落点钉成"`js/gsettings.js` 整条从名单消失",`gaps` 仍一字未动(**G-13 缺口仍开着**),见 [w81-gsettings-prompt.md](./w81-gsettings-prompt.md)。**同口径再收一处随 W82**:剧壳页发行文案包那步的人设句进注册表新键 `dist.copySystem`(`def` 与原内联字面逐字节相同),其后按键现拼的 KB「钩子六型+付费卡点」仍归 `KB.SECTIONS` 单源、六字段返回契约留在 user 半不开放覆盖;同槽把全仓持有者名单再往前收了一步——由「每槽各写一次局部扫描」改成按 `Prompts.list()` 现推的一条用例(新增键自动进名单),`gaps` 照旧一个不摘,见 [w82-proj-shell-prompt.md](./w82-proj-shell-prompt.md)。**W83 再收一处**:拉片建集(参考视频 → 场景切段 → 逐段画面理解)那步的人设进注册表新键 `rip.system`(`def` 与 `js/proj-upload.js` 原内联字面逐字节相同),登记位置紧跟 `split.system`——两者是建分集的两条入口,但角色与判定输入都不同故不并键;同样只收人设句(段号时段的现拼提示与返回 `{"shot_desc":…}` 的字段契约仍留在该步 user 半),纯浏览器链路故收编解决的仍是可覆盖而非可 headless,键登记在 SK-03 名下并新立一条全仓持有者名单;`gaps` 与 `Skills.gaps()` 逐字节不变(**G-13 缺口仍开着**),见 [w83-proj-upload-prompt.md](./w83-proj-upload-prompt.md)。**G-13 欠段再收一处**:镜头「按指令改」那步(`js/sb-views.js` 的 `openCommentGen`)的人设进注册表新键 `gen.editSystem`(`def` 与收编前的内联字面逐字节相同),与同层四策略优化的 `gen.promptSystem` 有意不合并——那条经装配口 `WfCore.genPromptSystem` 还要接 KB 抽卡公式+抽卡军规,复用即把两步的缺省一起改掉;收的是人设句连同改写纪律(写坏只是效果差),镜头上下文摘取与返回 JSON 契约仍留在该步 user 半不开放覆盖;该步同为纯浏览器链路,收编解决的仍是可覆盖而非可 headless;键登记在 SK-03 名下,同形的主体「按指令改」(`js/role-editor.js`,角色与产物落点都不同)有意不在本槽收编并配了反向断言;同轮把逐槽各写一份的"这一句只剩注册表一份"改成**按注册表现取的全仓持有者名单**(每条 `def` 全仓扫一遍,持有者必须恰好是 `js/prompts.js`,下一槽新收的键自动进名单),`gaps` 与 `Skills.gaps()` 逐字节不变(**G-13 缺口仍开着**),见 [w84-sb-views-prompt.md](./w84-sb-views-prompt.md)。**`js/agent-ops.js` 那两处随 W86 收掉**:Agent 对话闭环的两个辅助步——执行回执核验修复(`AgentOps.selfFixRound`,`step:'fix'`)与会话纪要蒸馏(`AgentOps.compactChat`,`step:'cmp'`)——各成一条独立键 `agent.selfFixSystem`/`agent.compactSystem`,两句 `def` 逐字节不同故判据在比对那一步就结束(不可能共用一键),取值口都在 `js/agent-ops.js` 就地经 `Prompts.get` 隐式读覆盖表、`def` 与收编前的内联字面逐字节相同;同样只收人设句——回执的 `✕`/`⊘` 记号、修复 `ops` 白名单、蒸馏字数与保留项、返回 JSON 约定都对着下游解析分支,仍写在各自调用点不开放覆盖;两步只有浏览器一个消费点(headless 没有自修复轮与对话纪要这两个动作),收编解决的是可覆盖不是可 headless,计费仍走所属那条消息的辅助步槽位不另扣费。SK-10 与 SK-11 的仍欠段随之只剩 `js/role-editor.js` 的角色设定师(仍配"收编即红"的源级反向断言),`gaps` 仍一个不摘(**G-13 缺口仍开着**),见 [w86-agent-ops-prompt.md](./w86-agent-ops-prompt.md)。**主体域随 W87 内联归零**:主体编辑页「按指令改」那步的人设收进独立键 `persona.editSystem`——类别是插值进去的,故取"一条键 + `{kind}` 变量"(角色同、产物落点同,分三键等于同一句话改三遍;写死类别则缺省当场变),是人设类键里第一条带变量的;与分镜那侧同形入口有意不合成(设定师出主体参考图、改图师出镜头素材,措辞也不同字面),契约半仍留在该步 user 半不开放覆盖。同槽把逐槽各写一份的全仓持有者名单更新成**按注册表现取的一条**(逐条扫每条 `def`,下一槽新收的键自动进名单;同 `def` 反查钉在音色推荐那一对上),既有那几份逐槽写的一条不删;`gaps` 仍一个不摘(**G-13 缺口仍开着**),见 [w87-role-editor-prompt.md](./w87-role-editor-prompt.md)。**专家工坊那两处随 W88 收掉**:锻造器 `forge.system`(从一句描述铸出一位新专家)与进化器 `forge.evolveSystem`(把已有专家的使用记录蒸馏成进化条款)各成一条独立键——同在「🧪 专家工坊」页上仍不合并,因为输入/动作/产物落点/计费标签/多轮全不同(无中生有 vs 就地改写);取值口都留在 `js/experts.js`,锻造器那处**导出成 getter**(照原样写成模块级 `const` 会把覆盖表冻结在页面加载那一刻,用户改完提示词切回工坊 tab 吃到的还是旧值),`js/gsettings.js` 工坊页因此一行未改、仍只引用 `Experts.FORGE_SYS` 这一个常量;`js/experts-data.js` 的 16 位预置专家 `persona` **不在收编面内**(那是专家数据不是提示词);两条键分别登记在 SK-02(专家条目的铸造口,并写明工坊那份字段面同样没有 `skills[]` 即 `G-09` 的另一头)与 SK-26(`evolveExpert` 的记账宿主)名下,**`G-11` 如实保留**并补一句划界——改得到提炼口径,改不出自动触发;`gaps` 仍一个不摘(**G-13 缺口仍开着**;另**补记 `system: '你是` 那个计数口径漏计的一类**:局部常量形态(`const X = \`你是…\``)不匹配该口径,`FORGE_SYS` 与意图路由那处都属这一类),见 [w88-experts-forge-prompt.md](./w88-experts-forge-prompt.md)。**W89 点名 `js/proj-planner.js` 的那两处随 W92 收掉**:项目实验台的 AI 策划对话与剧本译制各成一键(`planner.chatSystem` / `trans.localizeSystem`,两处 `def` 字面不同故不共用),策划那步只收人设句、其后的「当前项目信息:」项目实况(项目名称/风格影调/目标市场/剧本前段/分集列表)仍由取值口现拼;译制那步是**第一处带运行期变量的收编**——`${tgt.name}`/`${tgt.lang}` 登记成 `vars: ['{market}', '{lang}']` 走 `Prompts.fill`,并正查 `def` 里真有这两个占位符(登记了填不到是静默失效);它的**契约半写在 system 半而不在 user 半**,「保留『第X集』分集标记」那一条留成取值口常量 `TRANS_CONTRACT` 不开放覆盖(它就是「应用译制结果」按标记拆分回写分集的判据),并新加一层"契约与解析侧同集"——把源码那条 split 正则现取出来真切两集,拦住"契约文字还在、解析侧那一支被删了"的单侧漂移。另钉住**两处取值口都不许提到模块顶层**(IIFE 顶层预取会把覆盖表冻在页面加载那一刻,缺省与覆盖两条路径看起来都对,只有源级看得见);三张判据不同的全仓名单本槽逐张按 live 重算(按注册表 `def` 现推那张期望值不变,逐文件手写处数那两张各自减一批加一批),`gaps` 照旧一个不摘,见 [w92-proj-planner-prompt.md](./w92-proj-planner-prompt.md)。
- 人设/记忆注入面覆盖五条工作流:理解、分镜、审片、提取主体、剧本拆集(`WfCore.WF_BOARD` 五键单源,服务端唯一装配口 `wfPersonaNote` 由契约断言锁死调用点数);工作流内部的审片侧三步随 W39 一并接上,ctx 各随所属工作流的板块。
- 词表分叉已收口:景别/运镜/视角/角度四张词表的单一来源在 `js/wf-core.js`,`camera.js`/`review.js`/`sb-io.js`/`agent.js` 全派生(G-07,见 `w4-shot-size-glossary.md`)。
- 前段命令全过 wf 通道:剧本拆集(G-04)与提取主体都有服务端端点,人设与协作记忆按板块注入,headless 与浏览器同口径。**编排层也已跟上**:`Skills.playbook('eps.frontPipeline')`(SK-16)从集内两步补成前段四步(提取主体 → 剧本拆集 → 本集理解 → 智能分镜,步序与 `Domain.workflow` 同源),条目 `cmds` 改由 `steps` 推出、四步 `args` 一律留空不预授权,见 [w31-sk16-playbook.md](./w31-sk16-playbook.md)。**编排层随后补上了全链那一条**:SK-05 的 `steps` 从空变成主线端到端九步(提取主体 → 主体生图 → 剧本拆集 → 本集理解 → 智能分镜 → 就绪检查 → 批量生成 → 智能审片 → 合成成片,步序同取 `Domain.workflow` 主线步序),`Skills.playbook('core.playbookProjection')` 从 `null` 变成九步,SK-16 前段四步是它的有序子序列(分叉即红);`shot.generateVideo`(断点补拍)与 `episode.produce`(三步聚合)有意不进链、各由别的条目登记;`args` 同样一律留空,`gaps()`/面表/拼块逐字节不变,见 [w44-sk05-playbook.md](./w44-sk05-playbook.md)。**那张步骤表随 W46 有了第一个消费方**:`Plans.fromWorkflow` 的命令名与步序改由该投影现取(计划层只留一张按命令名索引的状态取材器,回答"这一步当下待不待办",命令名由投影盖写故取材器给不出第二条链),项目级步按投影步序排在前、集级步逐集取首个待办步,需授权或需人工挑选的状态(重拆覆盖 / 过期镜子集 / 确认闸 / 补剧本)一律出导航步且不预设任何 `args`,本集理解与就绪检查按理由登记为不占计划步(自省出口 `Plans.projection()` 把"选择性不占"与"漏接"分开);`mcp.js` 那个零使用的 `require('./js/skills.js')` 同轮接上第一个只读出口 `hujing_playbook`(playbook 步骤表 + 就绪检查各面校验项,不经 CLI、不打服务端、零计费、未登录也答得出)。见 [w46-g12-playbook-proj.md](./w46-g12-playbook-proj.md)。**那张步骤表的第二个消费方随 W52 落地**:同一份投影切出中段(主体/分集/分镜/生成)做成可调用的流程模板 `js/flow-tpl.js`——步序与命令名取投影、参数面与用法串取 `cmd-registry`、待办与缺前置取 `Domain.workflow`,本层只登记注册表答不出的三件事(这一步落在中段哪一主线步、待办由哪几个阻塞码判、跑砸了断在哪一码),四段分开取拼接逐字节等于整段、缺前置一律进 `gaps` 并 `ready=false` 而不吞掉步骤序列、授权位与子集位一律写明要用户明示;出口是 CLI `flow-template` 与 MCP 工具 `hujing_flow_template`(CLI 薄包装)加提示模板 `hujing_mid_pipeline`(正文由同一份模板渲染,工具名经 `toolOf` 从工具表 `cmd` 字段现取,模板层零工具名字面)。见 [w52-g12-mcp-mid-templates.md](./w52-g12-mcp-mid-templates.md)。**G-12 的第三个落点随 W58 补上**:发布留痕做成领域命令 `project.release`——准入判定(空项目 / 缺门禁结论 / 未过门各给明确错误码)与写回(打版本号 + 追加一条 `p.releases` + 内容摘要)收进 `js/release-core.js` 双端 UMD 单源,浏览器 `Release.stampRelease`、服务端 `POST /api/wf/release`(CLI `exec project.release` / `release` 与 MCP `hujing_release` 同链路)调同一个 `stamp()`,环境差异(时钟/用户名/版本号推进器/落库)经参数注入;浏览器交付检查的「打版本」按钮改走命令表(绕过即红),`force` 是"未过门强打"的授权位而不是万能位(空项目带 `force` 仍拒),门禁判据、fail/warn 计数与 overall 四级口径一字未动(G10 仍是 warn,不抬门);这个动作本来就零 LLM 零上游零计费,两端 `meter:false` 且都不进 `Tasks.run`。见 [w58-sk25-publish-cmd.md](./w58-sk25-publish-cmd.md)。
- `WfCore.memRecall/memBlock` 有了直接单测(memory 套件现 28 条,末 6 条覆盖 W53 的播种/迁移面,另 7 条覆盖 W61 的前段四步回流面、2 条覆盖 W65 的蒸馏板块过滤面),不再是"实现存在但零断言"的模块。
- 记忆的**写入面不再全靠人打字**(SK-26 回流面):整集审片与发布留痕两个闭环收尾,把该轮的**可判定**结论(待返工镜数、共性问题类型、四维最弱维、发布门状态与未过门项)按板块写回既有 `state.agentMemory`,派生只此一份 `WfCore.memFeedback/memWrite`(记忆数组经参数注入),四处写入点各走自己那端既有的落盘通道、不多一次 IO;按回流键原地更新故反复闭环只留最新一条;整集均分**有意不回流**——成片板块的记忆会被下一轮逐镜审片召回,回灌分数等于给打分设锚点。"回流专家"的自动那一半由此闭合(条目带板块 `scope`,下一轮同板块提示词按 `memBlock` 召回即吃到),蒸馏进 `persona` 仍要人点自进化(`G-11`),见 [w43-sk26-memory-writeback.md](./w43-sk26-memory-writeback.md);发布留痕的命令化出口随 W58 接上后,那一处写入点从「CLI 自己拼 meta 桶随 `PUT /api/state` 写回」改成服务端 `/api/wf/release` 随同一次落盘写回,回流内容与原地更新口径一字未改。**W61 把回流面从这两处补到主线六处**:前段四步(本集理解 → 六维产出数与缺的维名归导演板块、智能分镜 → 镜数/预估总时长与缺提示词·未挂主体两处缺口归分镜板块、剧本拆集 → 集数/切分模式/最长集与超长集数归剧本板块、提取主体 → 本轮新增·已有位数与库存·缺参考图归主体板块)各在成功且**可判定**时回流一条,浏览器与 headless 各一套写入点、都走同一份 UMD 派生并挂在原本那次落盘之前;`scope` 一律取 `WF_BOARD` 单源——那正是各步提示词召回记忆用的同一个键,故"回流下去下一轮同一步吃得到"是结构上保证的。失败路径(理解回退模板、拆镜回退本地、零产出、LLM 报错)一律不写;回流内容有意只取"做到什么程度、还差什么",不取集标题/主体名/模型评分(那些会被下一轮照抄)。SK-26 至此不再有"某步不回流"的欠账,`G-11`/`G-02` 两条仍开,见 [w61-sk26-front-writeback.md](./w61-sk26-front-writeback.md)。**W43 点名的 `G-11` 三面里,"读记忆不按板块过滤"那一面随 W65 收掉**:`evolveExpert` 的蒸馏输入改经 `WfCore.expertBoards`(生效板块 = 板块雇佣 > 全局雇佣,判据与 `personaFor` 同一套,专家/项目树/雇佣状态/板块词表全经参数注入)+ `WfCore.memForBoards`(只收 `scope` 命中的条目,无 `scope` 的手工沉淀不收,按 `text` 去重)硬过滤——**召回串错板块只坏这一轮的上下文,蒸馏是写死进 `persona` 且没有撤回口**,故蒸馏侧不补召、取不到就回空数组让调用方跳过;板块或条目取不到的两道闸都落在 `Tasks.start`/`U.charge` 之前(不登记不扣费,计费口径一字未动),文案点名是哪个板块;`system` 与 `user` 两侧都点明生效板块,断言钉的是别的板块正文在**两侧都不得出现**。人手触发点原样保留(三个调用点一个没动),**`G-11` 因此没有清零**——蒸馏仍是人手动作、且当时仍只对自定义专家开放,两面照旧记在 SK-26 的 `note` 里,见 [w65-g11-mem-scope-filter.md](./w65-g11-mem-scope-filter.md)。**其中"只对自定义专家开放"那一面随 W128 收掉**:先核实这处是不是产品有意锁死(源码那句「预置 EXPERTS 不挂此按钮」只描述实况不给理由,W43/W65 两份交接都把它记成待接并写好了接法),判为漏入口后给专家雇佣页两类预置卡各挂一个「🧠 进化」,走同一个 `evolveExpert`、同一份板块过滤、同一份计费口径;**条款落到该预置专家的自定义副本**——预置库是 `js/experts-data.js` 的模块级常量,原地追加既存不进 `Store` 也会让服务端 `require` 的那份与浏览器漂移,副本记 `from`=派生源(同一预置只派生一份、深拷贝以免与注册表共用 `dims`/`tpl` 引用),`WfCore.expertBoards` 多认一个 `from` 故副本自身还没被雇佣时生效板块仍按派生源算,副本落库卡在两道闸之后、扣费之前(闸没过不留空副本);`G-11` 至此**只剩"自动进化"一面**,人手点仍如实记在 SK-26 的 `note` 里,见 [w128-g11-preset-evolve.md](./w128-g11-preset-evolve.md)。**SK-04 那句「生成与合成两步仍欠」随 W98 核过一遍,结论是维持不做**:决定性判据不是"抽不抽得出可判定数字"(抽得出:成功镜数/失败镜数/拼接段数/`composedAudio` 四个数),而是"抽出来有没有读者"——这两步在两端都没有自己的 LLM 提示词、`WF_BOARD` 里也没有它们的键,W61 那条"`scope` 取 `WF_BOARD` 故下一轮同一步召回得到"的结构保证在这里不成立;唯一会召回这两个板块的旁路(Agent 对话)在**同一份提示词**里已经带着 `AO.stateBlock`/`WfCore.agentStateText` 的实时逐集摘要,回流条目只会是它旁边更旧的那份,另有三处可量化负作用(成片板块召回优先段被稀释、50 条上限的挤出门槛由第 16 集提前到第 10 集、`生成` 板块条目会进专家蒸馏),见 [w98-sk04-gen-compose-mem.md](./w98-sk04-gen-compose-mem.md)。**W43 那句"反复闭环不刷满 50 条上限挤掉用户偏好"随 W101 补上第二层**:`fb` 原地更新压住的是"同一个键被反复写",压不住"键本身在变多"——`fb` 键粒度里有集号(`und`/`sb`/`review` 每集各一条),`3 + 3×集数` 到第 16 集即 51 条,原先按 `slice(-MEM_MAX)` 砍数组头部,而用户手打的「记住…」正是最早写进去那批。现改为满桶按优先级淘汰:先挤最旧的自动回流条,用户自沉淀的留住,本次刚写下/刚更新的条目不参与淘汰,桶里没有自动条时退回先进先出(空桶与全用户条两种情形与改前逐字相同);`MEM_MAX = 50`、计费、发布门与六处写入点一字未动,改动全在 `memWrite` 那一个纯函数里,见 [w101-mem-cap-priority.md](./w101-mem-cap-priority.md)。**W103 把用户手打那一半的写入面也接进同一个 `memWrite`**:`js/agent.js` 的 `memRemember` 与 `cli.js` 的 `memory add` 此前各写一份裸 `slice(-50)`,而 W101 之后桶头部留下的正是那些受保护的用户条——于是形成"自动条写入时绕开用户条、用户条写入时只砍用户条"的别扭形态,两条用户偏好里先写的那条会被后写的顶掉,而桶里同时躺着几十条随时可再生的回流统计。两处各删一行截断改成一行 `memWrite`(无 `fb` 的条目本来就走追加,优先级顺带生效),`MEM_MAX`、120 字截断、落盘时机与七个调用点一字未动;50 这个数自此只在 `WfCore.MEM_MAX` 一处,两条认字面的源级断言按函数段取重钉,见 [w103-memremember-memwrite.md](./w103-memremember-memwrite.md)。**W61 留在浏览器侧的那条绕过回流的入库路径随 W108 收掉**:剧本解析向导 Step1 此前自己调提取、自己造主体条目、最后整表覆盖 `p.subjects`(不只是"另一套合并口径",是根本不合并——同名同类的老主体连已生成的参考图一起被抹掉),现整段改经 `Commands.execute('project.extractSubjects')`,与 CLI `exec` 同一份合并口径与同一处回流点;收之前先把向导独有的两件语义做成命令参数位(`model` 透传上传弹窗选的文本模型、`local` 强制本地启发式零 LLM 零计费),两位只在浏览器语境成立故 CLI 如实 `blocked browser-only` 而不是静默忽略,见 [w108-parse-wizard-cmd.md](./w108-parse-wizard-cmd.md)。

## 阅读约定

- **缺口编号**:`G-01…G-15` 出自资产图谱,**冻结在 15 项不再新增**;新登记的缺口一律走短名单的 `S-xx` 命名空间。判定标准文档里提议的 `G-16`(发布后→上游回路)按此规则改记为 `S-08`。
- `docs/Agent贯通落地-G1-G5.md` 里的 `G1–G5` 是该文自带的历史编号,与本目录的 `G-0x` 不是同一套,对应关系见该文与 [w5-cycle1-audit.md](./w5-cycle1-audit.md) 第 2.18 节。
- 文档描述功能本身,不写功能溯源。
- 动工前先看**最新一份收敛记录**的"剩余未合与残留",避免重做已落地的部分:当前是 [w170-integration-log.md](./w170-integration-log.md)(更早的分叉登记在 [w166-integration-log.md](./w166-integration-log.md)、[w161-integration-log.md](./w161-integration-log.md)、[w157-integration-log.md](./w157-integration-log.md)、[w151-integration-log.md](./w151-integration-log.md)、[w148-integration-log.md](./w148-integration-log.md)、[w146-integration-log.md](./w146-integration-log.md)、[w142-integration-log.md](./w142-integration-log.md)、[w139-integration-log.md](./w139-integration-log.md)、[w134-integration-log.md](./w134-integration-log.md)、[w133-integration-log.md](./w133-integration-log.md)、[w129-integration-log.md](./w129-integration-log.md)、[w127-integration-log.md](./w127-integration-log.md)、[w125-integration-log.md](./w125-integration-log.md)、[w123-integration-log.md](./w123-integration-log.md)、[w122-integration-log.md](./w122-integration-log.md)、[w119-integration-log.md](./w119-integration-log.md)、[w117-integration-log.md](./w117-integration-log.md)、[w112-integration-log.md](./w112-integration-log.md)、[w109-integration-log.md](./w109-integration-log.md)、[w107-integration-log.md](./w107-integration-log.md)、[w104-integration-log.md](./w104-integration-log.md)、[w99-integration-log.md](./w99-integration-log.md)、[w95-integration-log.md](./w95-integration-log.md)、[w90-integration-log.md](./w90-integration-log.md)、[w85-integration-log.md](./w85-integration-log.md)、[w80-integration-log.md](./w80-integration-log.md)、[w75-integration-log.md](./w75-integration-log.md)、[w72-integration-log.md](./w72-integration-log.md)、[w70-integration-log.md](./w70-integration-log.md)、[w68-integration-log.md](./w68-integration-log.md)、[w67-integration-log.md](./w67-integration-log.md)、[w64-integration-log.md](./w64-integration-log.md)、[w62-integration-log.md](./w62-integration-log.md)、[w60-integration-log.md](./w60-integration-log.md)、[w57-integration-log.md](./w57-integration-log.md)、[w55-integration-log.md](./w55-integration-log.md)、[w21-integration-log.md](./w21-integration-log.md)、[w16-integration-log.md](./w16-integration-log.md)、[w13-integration-log.md](./w13-integration-log.md)、[w9-integration-log.md](./w9-integration-log.md)、[w7-integration-log.md](./w7-integration-log.md) 与 [w6-integration-log.md](./w6-integration-log.md) 第 5 节)。W13 的记录件随 W21 合入,W16 第 5.3 节记的那条悬挂索引行随之解掉。