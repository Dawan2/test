# W276 集成日志:W275 问题中心明细镜号并进 W274 尖(集成线上第八条「快进形」)

我方尖:`origin/cursor/w274-integration-a6d1` **现取** `928c83c`(先 `fetch` 再 `rev-parse`)。
对侧:`origin/cursor/w275-issues-shotno-857e` **现取** `8a8b08c`。
W275 自称从 w274 尖 `928c83c` 起支、HEAD 自称 `8a8b08c`,两处**以 `rev-parse` 为准**核过,与自称逐字节相同。
新支 `cursor/w276-integration-3f7c` 从 w274 现取 HEAD 拉出,`checkout -b` 后立刻 `rev-parse`,与 `928c83c` 相同。

**结论先写:一次 `--no-ff`,零冲突。合完 live 跑出 `unit` 695 / `contract` 153 / `skills` 104 /
`issues` 22 / `integration` 152 / `cli.smoke` 115-117,`gaps()` 20 键一个没剥,W275 十条残留一条没代修。
本槽是集成线上第八条快进形,`tree(M)` 与 `tree(P2)` 逐字节相同、`P1→M` 与 `B→P2` 的 numstat 逐行相同——
这两格照旧**恒真**,一格结论都不承载,故本文的证据全在变异与 live 上。
本槽新量出来的两格是:①「抄回旧数字」这一反事实在本槽只**红 1** 而不是 W274 那次的**红 2**,原因是
W275 有意没抬单元那格 `FLOOR`、缓冲格把第二层吃掉了(§五);②由此往下走一步实测出来的
**M12:真删掉本槽新增的行为面那一条用例、README 按 live 写成 694,`unit` 694/694 全绿**——
那两条新用例此刻整整落在「可静默删掉」的窗口里(§四 M12/M13)。**

## 一、拓扑(现取,不抄自称)

| 位 | 现取 SHA |
|---|---|
| `B` 合并基(`merge-base P1 P2`) | `928c83ca016ee26b89d922cc179e60712629f4cb` |
| `P1` 我方尖(w274) | `928c83ca016ee26b89d922cc179e60712629f4cb` |
| `P2` 对侧尖(w275) | `8a8b08c549ce8d15edf352c3948ded448eb4f51f` |
| `M` merge commit | `8e21a99fba8040a7b6259e85fe2204a0912bfd68` |

`git merge-base --is-ancestor P1 P2` 成立,`B` 与 `P1` **提交级相等**,即典型「快进形」——
本可 `--ff` 掉,仍按纪律走 `--no-ff` 留住合点。`P1..P2` 四条提交:

| 现取 SHA | 是什么 |
|---|---|
| `4b295389…` | 产品:失败镜/过期镜明细两行改走 `Domain.shotNo` |
| `617123e…` | 加测两条(源级 + 行为面)并按 live 同步 README 数字 |
| `67bc030…` | 记账 + 索引一行 |
| `8a8b08c549ce8d15edf352c3948ded448eb4f51f` | 记账补 `cli.smoke` 现取 115/117 |

「自称尖是不是链尾」这一格现取核过:`8a8b08c` 就是链尾。

树面:`tree(P1)` `6c1a892`、`tree(P2)` = `tree(M)` = `313fc15`(逐字节相同);
`P1→M` 与 `B→P2` 的 `--numstat` 逐行相同(五个文件)。
**这两格在快进形下是恒真的**,写在这儿只是留档,不当证据用。

## 二、并集面(README / GUARD / roster / 索引)

快进形下 `P2` 的文档本就含着 `P1` 的全部内容,故并集不靠手工拼:
`docs/skills-wave/README.md` 索引表里 w274 那行与 w275 那行**同时在册**,现取核过。
数字一律 live,**W274 自称的 693 一个字没抄**(反事实见 §五):

| 面 | `P1` 基线现取 | `M` 合完现取 | 差 |
|---|---|---|---|
| `unit` | 693 | **695** | +2(全部来自对侧) |
| `contract` | 152 | **153** | +1 |
| `skills` | 104 | **104** | 0 |
| `issues` | 21 | **22** | +1 |
| `integration` | 152 | **152** | 0 |
| `cli.smoke` | 115/117 | **115/117** | 0 |
| 记账件 | 289 | **291**(w275 一份 + 本文) | +2 |

GUARD / roster 现取四者对齐、本槽与对侧都没登记新护栏主题:
`GUARD_TOPICS` **19** / `GUARD_TOPICS_CLOSED` **0** / `TOPIC_FLOOR` **19** /
花名册(`w178-topic-floor-unlist.md` 的花名册节,按判据同一条正则现取)**19**。
`gaps()` 键集现算 **20** 键(`G-08 G-15 G-09 G-01 G-02 G-12 G-13 G-04 G-10 S-01 S-03 G-07 G-14 G-05 S-04 S-05 G-03 G-11 S-06 S-07`),**一个没剥**。

棘轮与差额:`['单元测试', 693]`、`['集成测试', 152]`、`['CLI 冒烟', 117]`、`TOPIC_FLOOR` 19 均未动
(**单元那格是对侧有意没抬的**,本槽照旧不替它抬,见 §四 M12 与 §六 11);
记账件 `const FLOOR` 289 → **291**(w275 一份 + 本文,按集成槽惯例抬到 live),
`docs/skills-wave/README.md` 明写份数 290 → **291**、索引补本文一行(按波次号插在 w275 之后)。
五格差额合完现取:单元 **2**、集成 0、CLI 冒烟 0、记账件 **0**、护栏主题 0,`SLACK` **3** 未越界。

## 三、快进形的空集不能当没丢东西

`git diff P2 M` 是空集——这在快进形下是**定义使然**,它证明的只是"合入没引入新东西",
证不了"对侧那两处语义在合完的树上还活着、判据还咬得住"。故取证换成四类:
**逐处变异**、**边界反向变异**、**名集对账**、**反事实**。

### 产品语义现取核过(§合入纪律 4)

```
js/issues.js:210  detail: fs.map(s => `镜头${Domain.shotNo(ep.shots, s)}:…
js/issues.js:224  detail: `镜头 ${ss.map(s => Domain.shotNo(ep.shots, s)).slice(0, 8)…
```

- `js/issues.js` 全文 `.order + 1` 现取 **0 处**(`\.order\s*\+\s*1` 与 `\.order \+ 1` 两种写法都扫过);
- 低分镜明细仍是 `lows.map(x => x.order …)`,而 `lows` 来自 `Domain.reviseTargets`,
  `js/domain.js:704` 现取仍是 `order: t.i + 1`——**第二份对位一份都没造**;
- skills 方法论提醒那五行(`js/issues.js:46 / 66 / 75 / 78 / 80`)现取仍读上游 `h.order`,**一个字没改**;
- `js/` 相对 `P1` 的逐字节改动只有 `js/issues.js` 一个文件(`5 insertions, 2 deletions`);
- 残留锚点现取:`js/batchops.js` `.order + 1` **17 行**、`js/produce.js` **0 处**且 `:279` 那行仍走
  `Domain.shotNo(ep.shots, s) || '?'`。

### 名集多重集对账

`P1` 用例名 **693** 条、`M` **695** 条,多重集差 **+2 −0**,`693 + 2 = 695` 自洽。新增的两条逐名点出来是:

```
collect:失败镜/过期镜明细的镜号按分镜表实位出(order 字段漂了也不跟着漂)
问题中心展示镜号(源级):失败镜/过期镜明细取 Domain.shotNo,提醒投影行与低分镜明细照旧读各自上游派生的号
```

### 三棵树关键词点数

十三个关键词逐树现算,`M` 与 `P2` **逐格相等**且无一格掉到 `P1` 之下。
变的几格全是对侧净增:`shotNo` 163→189、`reviseTargets` 188→196、`staleShotSplit` 77→80、
`failed-shots` 71→75、`stale-shots` 52→54、`rowIndexOf` 75→76、`gaps()` 633→636、
`TOPIC_FLOOR` 270→271、`GUARD_TOPICS` 308→309、`runBatchOp` 26→27;
`low-review` 108、`bgDock` 36、`autoSmartReview` 151 一格没动。

## 四、变异(逐手在本支上做、跑完 `git checkout` 还原,`git status --porcelain` 复核 clean)

| 手 | 改法 | `contract` | `issues` | `skills` | 报的是 |
|---|---|---|---|---|---|
| M0 | 不变异 | 0 | 0 | 0 | 基线 |
| M1 | 只把**失败镜**那处退回 `s.order + 1` | 1 | 1 | 0 | 「失败镜明细应按实位出号(读 order 字段时是 6 与 1…)」 |
| M2 | 只把**过期镜**那处退回 `s.order + 1` | 1 | 1 | 0 | 「过期镜明细应按实位出号(读 order 字段时是 6、1)」——与 M1 报错句**不同形** |
| M3 | 两处**一起**退 | 1 | 1 | 0 | 与 M1 逐字相同(见下) |
| M4 | 越界:把**低分镜明细**改成走 `Domain.shotNo` | 1 | 0 | 0 | 「低分镜明细的号应照旧取 `Domain.reviseTargets`…」 |
| M5 | 越界:把 **`lexLine` 提醒行**改成走 `Domain.shotNo` | 1 | 1 | 0 | 「提示词稳定词提醒的一行明细的镜号应照旧…」 |
| M6 | 只加不退:另处新增一个 `.order + 1`,两行原样不动 | 1 | 0 | 0 | 「`js/issues.js` 不许再按 order 字段出镜号:`if (c.unconfirmed …`」(原样抄出犯规那一行) |
| M7 | 取法名字还在,对位偷换成**按 id 取首行** | 1 | 1 | 0 | 「…按 id 取首行时是 2 与 1」——行为面夹具第三行与首行同 id,当场分得开 |
| M8 | 两处都**加上** `|| '?'` 兜底 | **0** | **0** | **0** | 一条不红(对侧 §六 8 已记,本槽复量成立) |
| M9 | 跨支反向:退掉 W272 已落的 `js/sb-views.js:1243` 更多工具页头 | 2 | 0 | — | 红的是「分镜面展示镜号(源级)」+「分镜面弹窗页头镜号按实位」,**本槽两条判据一条不红** |

M1/M2 的报错句**不同形**、M4/M5 两手越界**报错不混**,四半各有各的判词。
M6 证明「整份文件 `.order + 1` 恰 0 处」这一档不是靠 M1 那句锚顺带兑现的。

### M3:复合判据同时坏两半时只报第一半(复量对侧 §五 M3,成立)

两处一起退时 `issues` 报的是**「失败镜明细应按实位出号」**,与只退失败镜那一处逐字同形——
`assert` 抛在前一句上、一条用例只报一句,读报错分不出坏的是一处还是两处。
每一半都单独量过(M1/M2 各自可分),这是**已知定价不是判据失效**,对侧已按这个口径记在残留 10 上,本槽原话保留。

### M9:连续第五次读成「分家」

M9 与 W271 §五 M1 / W272 §三 / W273 §五 M4 / W274 §四 M4 形状相同:退掉一处**别的面**已落的镜号,
本槽两条判据一条不红,只红那一面自己的两条。镜号这条线四个展示面
(审片 `js/review.js` / 分镜 `js/sb-*.js` / 生成 `js/produce.js` / 问题中心 `js/issues.js`)的判据
**各管各的文件、不叠着**——看的是对侧改动落在**哪个文件**,不是红几条。

### M12 / M13:那两条新用例此刻整整落在「可静默删掉」的窗口里(本槽新量)

`['单元测试', 693]` 这格下限是对侧有意没抬的,合完 live 695,差额 **2**,而 `SLACK` 是 3,故:

- **M12**:真删掉本槽新增的**行为面**那一条(`issues` 套件那条),`README.md` 的单元数按 live 改成 694
  → **`unit` 694/694 全绿,一条不红**。三方对齐那几句(实测 == 文档)成立、`live >= floor`
  (694 >= 693)也成立、差额从 2 降到 1 仍在缓冲格内,五层测试契约无一接得住。
- **M13**:在 M12 之上再做一次 M7(对位偷换成按 id 取首行)→ **红 1**,红的是**源级**那条
  (「失败镜明细的镜号应取 `Domain.shotNo`…」)。即行为面那条被删掉之后,这道护栏没有裸奔,
  源级那一层还咬得住取法本身;M12 真正丢掉的是**「同 id 第二行报自己那一行的号」这一格的行为面证据**
  (源级只认得出 `Domain.shotNo(ep.shots, s)` 这个字面,认不出对位对不对)。

这一格是 W130/W132 起就写明的「合入期减压阀」在本槽上的具体落点,不是新病;
**新的是把它量成了读数**:缓冲不是抽象的两格,它此刻就等于对侧刚加的那两条用例。
按明令(不代修残留、不改 W275 产品语义)本槽**不替对侧抬单元那格下限**,只把它记在残留 11 上。

## 五、反事实:抄回旧数字

| 手 | 改法 | 红几条 | 报的是 |
|---|---|---|---|
| CF1 | 把 W274 自称的 693 抄回 `README.md` 单元那处 | **1** | 「单元测试用例数:`README.md` 与实测不符(实测 695,文档 693)」 |
| CF2 | CF1 之上再真删掉本槽新增的两条用例(live 回到 693) | **1** | 只剩「contract 套件断言条数:与实测不符(实测 152,文档 153)」——单元那三层全绿 |

**CF1 与 W274 §五 CF1 的读数不同**(那次是红 2):W274 那槽对侧把 `['单元测试', 693]` 抬到了 live,
故抄回旧数字会同时踩到「文档低于下限」那一层;本槽对侧没抬,下限停在 693,抄回来的正好等于下限,
第二层被缓冲格吃掉了。**这条判据逮不逮得住取决于 `live − 下限字面` 有没有越过缓冲**,与"抄没抄"无关
——与 W134 归纳的那句是同一件事,本槽是它在「对侧没抬下限」这一形态上的读数。
CF2 更进一步:两条用例真删掉、文档一起改小,单元那三层(精确对账 / 下限 / 差额)**一条都不响**,
唯一响的是 `contract` 那格,而它响只是因为被删的两条里恰好有一条落在 `contract` 套件里
——若两条都落在 `issues` 套件,CF2 会是全绿。

## 六、残留(按明令一条没代修,前十条原话逐条保留)

1. **产线邻面 `js/batchops.js` 那 17 行仍读 `order`**(W273 有意整族留下、W274 §六 1 复核):落库文本 8 行、
   瞬时串 8 行、导出 TXT 正文 1 行。与 W271 留下的分镜面 38 行同族,收的时候要一起定
   "进度坞行与它同行的扣费摘要按什么切开";另记那处判词口子——一键审片确认弹窗正文那句 `'视频 #N·M'`
   是 `U.openModal` 的 `body` 不是 toast,按 W271 的分类更像持久 chrome。**产品口径未定,本槽不定。**
   (本槽现取复核:`js/batchops.js` 的 `.order + 1` 仍是 **17 行**,相对 `P1` 逐字节未动。)
2. **两份待确认清单行只有一份有行为面判据**(W271 §五 M2 量出、W272/W273/W274 逐槽复核):
   `js/sb-batch.js:154`(`runBatchOp` 出片前置检查)只有源级那一句锚着。本槽一条测都没加。
3. **「按指令改」页头(`js/sb-views.js:1096`)只有源级判据护着**(W272 §六 7 起在册):照旧开着。
4. **`js/skills.js` 十余处 `(+s.order || 0) + 1`、`Domain.subtitleSegs` 的镜号,与 `js/wf-core.js` /
   `js/sb-llm.js` 里的镜号**:回执与提示词口径,按明令未碰。**问题中心 §二 的第 4–8 条就挂在这一族上**——
   本槽收完之后,问题中心一个条目里仍可能同时出现两种号:状态类条目(失败/过期)报实位号,
   方法论提醒条目报上游那个 order 号。**同屏两号这件事本身没收干净,如实记下**;
   收它要先定"skills 命中里的号跟不跟着展示面走"(那份号同时喂着 CLI/MCP 回执与 LLM 提示词),
   属产品口径不属寻址,本槽不定、也一条测都没加。
5. **审片面任务名与扣费摘要那 4 行**(W269 §六 1 起在册):`js/review.js` 未碰,判据照旧钉着。
6. **浏览器闭环 `common` / `cut` 空壳、`openEpisodeReview` 整表与 generating 过滤、同 id 多行源头、
   库里 `order` 字段本身会漂**:一律未碰。
7. **邻面落库文本那一族只有源级判据护着**(W273 §五 M2/M3 量出、W274 §六 8 在册):本槽同样没补。
8. **生成面那行的 `|| '?'` 兜底半边两层都没有判据**(W274 §六 9 新记):照旧开着。
   **本槽这两处连兜底本身都没写**(§三 写明了理由:入参恒在表内、不可达),
   M8b 量出加不加都不红——即"将来改成从别处拿镜对象"这一步没有任何守卫。同族的兜底还有十几处
   (`js/sb-views.js` 七处、`js/sb-gen.js` 三处、`js/sb-batch.js` 两处、`js/review.js` 四处),
   本槽只量了自己这一处、没顺手去量别的面。
   (本槽 M8 复量成立:两处都补上 `|| '?'`,三个套件各红 0。)
9. **(W275 记)过期镜那一堆的入选是按 id 取的集合**:`js/issues.js:219-220` 拿
   `Domain.staleShotSplit` 回的**id 列表**建 `Set` 再筛 `ep.shots`,故同 id 多行时只要有一行过期,
   同 id 的**鲜镜也会被算进明细**(行为面用例的夹具里量到过,故那一组有意用了三个不同 id,
   并在注释里写明"不在本条量")。这是同 id 多行源头(W226 起在册)在这一面上的一个落点,
   不是本槽改出来的、也不属镜号取法,**按明令(同 id 源头不碰)留着,一条测都没加。**
10. **(W275 记)复合判据同时坏几半时只报第一半**(W275 §五 M3):每一半都有自己的判词(逐半单独量过),
    但报错句读不出坏了几处。已知定价,记下来免得下一槽把 M3 那种"报错句与 M1 逐字相同"误读成判据失效。
    (本槽 §四 M3 复量成立。)
11. **(本槽新记)`['单元测试', 693]` 那格下限落后 live 两格,而落后的正好就是本槽合进来的两条用例**
    (§四 M12/M13、§五 CF2 三处量到):删掉行为面那条并把 `README.md` 改成 694 → **全绿**;
    两条一起删并把文档改回 693 → 单元那三层仍**全绿**,只剩 `contract` 那格因为被删的一条恰在
    `contract` 套件里而响。按常规加测槽的口径这格缓冲是有意留的,不算毛病;
    **本槽照明令不替对侧抬这个字面**(那属改对侧的记账,不属合入),只把"缓冲此刻具体等于哪两条"写下来
    ——下一槽若在这一面加测,抬不抬这格就是个明面的选择而不是默认继承。
    另记:记账件那格本槽已按集成槽惯例抬到 live(289 → 291),差额回到 0。

## 七、跑了什么(数字全部 live 现取)

- `node tests/unit.js` **695/695**(`P1` 基线 `928c83c` 上现跑 **693**);
- `node tests/unit.js contract` **153/153**(基线现跑 152);
- `node tests/unit.js skills` **104/104**(基线现跑 104,**未动**);
- `node tests/unit.js issues` **22/22**(基线现跑 21);
- `node tests/integration.js` **152/152**(`env -u HUJING_SERVER` 加 `MV_DATA_DIR`/`MV_UPLOADS_DIR`/`MV_CONFIG`
  指向临时目录隔离;未加测,服务端与 CLI 一个字没动,全绿本身是"没搬坏"的判据);
- `node tests/cli.smoke.js` **单独整跑**、同一套 `env -u HUJING_SERVER` 与 `MV_*` 隔离:**115/117**,
  两条失败现取:

  ```
  FAIL | 未登录 whoami → exit 3 | exit=1
  FAIL | llm --json mock 链路 | undefined
  ```

  同 env 下在 `master` `9adcf0f` 的独立 `git worktree` 上现跑 **51/53**,两条失败**同名同表现**,
  逐字相同(按明令允许);
- 不跑 `tests/e2e.js`(按明令);
- `node --check`:`js/issues.js`、`tests/unit.js` 均过。

计费面一处未触及(`js/issues.js` 源级本就封死 `Commands`/计费引用,判据在册)。

## 八、交接

我方尖:见本支 `git rev-parse HEAD`(现取,不在此写死自称)。合点 `8e21a99`,基线 `928c83c`。
本槽动过的文件只有三个:`docs/skills-wave/README.md`(明写份数 290→291 + 索引一行)、
`tests/unit.js`(记账件 `const FLOOR` 289→291)与本文。
**`js/` 下逐字节未动**,`README.md` 未动(对侧已按 live 写到 695/153)。
未开 PR、未合 master、未开第三条功能支。

下一槽若接镜号这条线:四个展示面已收完,**剩下的都在 §六 4 那一族**(skills 命中 / 回执 / 提示词),
要先定产品口径再动手。若在问题中心这一面加测,先看 §六 11——缓冲此刻就等于对侧那两条。
