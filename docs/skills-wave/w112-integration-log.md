# W112 · 一条主线断点收口槽并入集成线的收敛记录(集成分支)

> 集成分支:`cursor/w112-integration-5d79`,基线 `cursor/w109-integration-3f7a @ 4f977aa`。
> 本文只记**收敛过程**:基线怎么定的、合了哪一条、每处冲突怎么解、合并后的实测数字、没删测的取证。
> 槽本身的内容说明在 [w110-split-only-script.md](./w110-split-only-script.md),本文不代述、不复写。
> 一次合并 `--no-ff`、一个合并提交、可单独 revert;全程只解冲突与收敛双口径,**不重做已落地的功能**。

## 1. 结果一句话

`--no-ff` 合入一条槽,**取被合入支的 head、是真三方合并**:

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w110-split-only-script-a91c@902d6d0` | `7caa286` | 上传弹窗「仅进行分集」拆集成功后把原文落进 `p.script`(主线上一处已登记的真断点),并按 live 代码订正「这会翻转发布门 G1」那处误记 | `js/proj-upload.js` 一处写入 | unit +3 |

合并后回归 **`unit 474/474`**、**`integration 130/130`**、**`cli.smoke 100/102`**(2 项失败与 `master` 同名同表现,见 5.2)。

治理面零变动:注册表 41 条、`CHECKS` 17 条、`preflightStages()` 七面、`gaps()` 20 键、`KB.SECTIONS` 18 条、`playbooks()` 5 条、短名单 30 条 0 `pending`——**七个数一个没动**(见 5.4);发布门更是一行未改(见 5.5)。

**本槽最值得记下的三件事:**

1. **合进来的槽自带一处判据订正,而订正对象是本条集成线自己上一份记账件的交接条。**
   [w109-integration-log.md](./w109-integration-log.md) 6.3 第 2 条把这个断点记成
   「接它要么让那一支写 `p.script`(**会翻转 G1 结论**,属产品口径需明示)」,
   源头是 [w108-parse-wizard-cmd.md](./w108-parse-wizard-cmd.md) 的交接第 5 条。
   `w110` 按 live 代码逐条核过:`g1-workflow` 逐集调 `Domain.episodeState` 判 `ep.content`,
   **`p.script` 一个字都不读**,真正报"未上传剧本"的是 `Domain.workflow` 的剧本步。
   于是本槽在"把提交合上"之外多了一件事:**把这条误记从 live 面上清干净**。
   清哪一层不是凭感觉——三处现查、只有一处要改(见 4.3)。
2. **两处冲突同形于 W109,而"取我方骨架 + 按段界折回对侧那段"这一手第四次用上。**
   `README.md` 那条 unit 长行,对侧整行带的是 `w108` 叉点的旧实况
   (memory 段那句「120 字·50 条口径一致」正是 W103 改掉的写法),整块取我方;
   对侧的真增量是 split 套件里一整段新话,按公共前后缀纯插入。判据仍是 W72 那条:
   **哪一侧描述的是合并后的实况,不是哪一侧更新。**
3. **W109 记的"去重口径不一致会凭空多报一条"这处盲区,本槽有意复现了一次,复现成功。**
   W109 是踩到的,本槽是**照它写的判据去撞**:期望集用 `sort -u`、live 侧不去重,
   `integration` 照样假报一条 `项目不存在 404`。这条盲区因此从"一次性观察"变成两次可复现的实测,
   而三套件的正式取证一律用两侧都不去重的多重集(见 5.3)。

## 2. 基线与这个槽

### 2.1 基线

任务口径给定 `origin/cursor/w109-integration-3f7a` 的 HEAD,参考值 `4f977aa`。现取核实:

```
$ git rev-parse --short origin/cursor/w109-integration-3f7a
4f977aa
```

**与任务给的参考值一致**,不需要在"参考值"与"live head"之间做取舍。
被合入支同样现取(任务给的是"实际 HEAD"、没给参考值):

```
$ git rev-parse --short origin/cursor/w110-split-only-script-a91c
902d6d0
```

基线上的三套件数字(开工时现跑,不引用 `w109-integration-log.md` 里的数):

| 套件 | 基线 `4f977aa` |
|---|---|
| `node tests/unit.js` | **471 / 471** |
| `node tests/integration.js` | **130 / 130** |
| `node tests/cli.smoke.js` | **100 / 102**(2 项与 `master` 同名同表现) |

三个数与 `w109-integration-log.md` 第 5.1 节记的末行逐字相等,故那份记账件在这一点上没有失真——
**但这是现跑对出来的,不是引用来的**。

### 2.2 分叉点:恰好落在 W108 的 tip 上

| 槽 | tip | `merge-base` 与 HEAD | 叉在哪条线上 |
|---|---|---|---|
| `w110-split-only-script-a91c` | `902d6d0` | `51fdec7` | `w108-parse-wizard-cmd-7b34` tip |

`w110` 叉自 W108,而 W109 已把 W108 整条合进来,所以 `merge-base` **恰好等于 W108 的 tip**。
基线在它之后还有三个提交(W109 的合并提交与两份记账件改动),故这次仍是真三方合并、没有退化成快进。

这个分叉点比 W109 那次窄得多:对侧看不见的只有 W109 那一次合并本身带来的收敛结果
(两份 README 的三处双口径与 `docs/skills-wave/README.md` 的索引/份数),
W102–W108 那些东西两侧都有。**冲突面因此正好就是那两份 README,一处不多。**

### 2.3 只合这一条

任务口径明写只合 `w110`、不碰其它槽,本槽不越界。合完现取,`origin/cursor/*` **130 条全部已是 HEAD 的祖先**(见 6.2)。

## 3. 这次合并的冲突面

| 冲突文件 | 冲突块数 | 两侧都改而自动合上的文件 | 只有对侧改的文件 |
|---|---|---|---|
| `README.md` | 1 | `tests/unit.js` | `js/proj-upload.js` / `docs/skills-wave/w110-split-only-script.md` |
| `docs/skills-wave/README.md` | 1 | | |

**代码面一次冲突都没有**:对侧的代码改动只有 `js/proj-upload.js` 里的一行写入,我方一字未动那个文件。
`tests/unit.js` 两侧都改而自动合上——我方那 5 条落在 `contract`/`memory` 套件,
对侧那 3 条落在 `split` 套件,位置不相邻,故三方合并直接排成并集(4.4 复核)。

`README.md` 里对侧还改了「剧本上传」一节那一句(第 596 行,写明两个按钮都会把原文落进剧本板块),
我方那一侧一字未动,**自动合上且内容就是合并后的实况**,不进冲突块也不需要回改。

## 4. 逐处怎么解

### 4.1 README 的 unit 长行:第四次取我方,对侧的真增量按段界纯插入

冲突块两侧都是完整的一整行(合并后 10893 字)。逐字符对出来的差额只有四处,归成两类:

| 差处 | 我方 | 对侧 | 取 |
|---|---|---|---|
| 行首条数 | `471 项断言` | `470 项断言` | 都不取,按合入后 live 现取 **474** |
| `split` 套件段尾 | 无 | `;**「仅进行分集」行为面**:…补写入不动发布门——G1 逐集读 `Domain.episodeState` 判 `ep.content`…` | **取对侧**(纯插入) |
| `memory` 套件段中三处 | 「120 字截断同口径、上限与满桶淘汰一律经 `memWrite`…行为面真跑浏览器 `memRemember`:…」 | 「120 字·50 条口径一致」 | **取我方** |

**取对侧那半就是把 W103 整条冲掉**(「120 字·50 条口径一致」正是 W103 改掉的写法,
且 W109 已在同一处第三次判过这件事),故整块取我方骨架。
对侧独占的那段是 split 套件的一整段新话,按公共前后缀定点插在
`…不各抄切分算法` 与 `)、\`wf-core.js\` 协作记忆` 这对段界之间——**纯插入,不重写整行**
(W60/W72 那处全角引号的坑就出在整行重写上)。

行首那个数按合入后 live 现取:`471 → 474`。`cli.smoke` 那句的 102 与 `integration` 那句的 130
本槽两侧都没动、也没有变化,不需要改;`contract` 那句"八条断言"同理不动(本槽没有新增文档数字对账断言)。

### 4.2 目录索引表:两侧各带自己那一行,现查为真并集

冲突块两侧:

```
<<<<<<< HEAD
| [w109-integration-log.md](./w109-integration-log.md) | … |
=======
| [w110-split-only-script.md](./w110-split-only-script.md) | … |
>>>>>>>
```

各一行、文件名不同,**取并集成立**,按波次号排成 `w109 → w110`。

这一步照 W109 第 4.3 节交待的做法**现查**而不是套结论:逐行比对两侧的文件名集合、确认无交集之后才取的并集。
W107 那次同一张表现查出来的答案是相反的(对侧带的 `w101` 行早已在我方表里,机械并集会得到两行同名索引,
而三条索引断言一条都报不出"重复行"),**判据同一条、答案未必同**。

### 4.3 「会翻转 G1」这处误记:三处现查,只有一处要改

`w110` 第 3 节按 live 代码把判据订正了。本槽要决定的是:这条误记还留在 live 面的哪几处、改哪几处。
现查三处:

| 现查处 | 结果 | 处理 |
|---|---|---|
| `js/skills.js` 的 SK 条目 `note` | **零命中**——这句话从来没进过注册表(`G1` 在 `js/skills.js` 里只出现在"产物实况现取 `Domain.workflow`,与流程条、发布门 G1 同一份推导"与"不改发布门 G1–G10 的 fail/warn 计数"两类表述里,都成立) | 不动 |
| `README.md` | 第 596 行由对侧自己改成了实况(「此后主线剧本步、`project.splitEpisodes`/`project.extractSubjects` 与 AI 策划等读 `p.script` 的下游一律不再把已拆集的项目当成无剧本」),自动合上 | 不动 |
| `docs/skills-wave/README.md` 的 `w108` 索引行 | **唯一残留**:「收口会当场 `blocked no-script`,而让它写进去**会翻转发布门 G1**」 | **改**:翻的是 `Domain.workflow` 的剧本步(`no-script` → `done`),不是 G1;并点名这是 W108 的误记、由 W110 按 live 代码订正 |

改索引行而不改记账件正文,是这条线一贯的分工:**索引表是 live 的导航面**
(读它的人是拿它当"现在是什么情况"用的),而 `w108-parse-wizard-cmd.md`/`w109-integration-log.md`
两份记账件是"那一轮当时怎么判的"的存档,按 W38 口径**一字不动**——
`w110` 自己第 5 节交接第 2 条已经把订正挂在可检索的位置上,存档里的旧判据有它兜着,不会再误导人。

**这一处不是靠通读兜的**:`w110` 自带的那条用例(同一夹具只改 `script` 一个字段,两次 `Release.collect`
的 `g1-workflow` 都 `pass`、`fails`/`overall` 全等,而 `Domain.workflow` 的剧本步两次结论相反)
在合并后真跑确认是绿的;它还有一条反证——去掉那行写入,G1 那条**仍然绿**(见 `w110` 第 4 节变异表)。

### 4.4 自动合上的 `tests/unit.js`,按它自己的判据复核

两侧新增用例落在不同套件,三方合并排成并集。要复核的是两条既有契约会不会被并集本身弄红:

- **用例名全局唯一(W102 那条)**:对侧新增的 3 条 `split · 仅…` 与我方那 5 条无一同名,
  合并后 `unit` 仍是 **474 条 / 474 个不同名字**(条数与名字数相等)。
- **`w102` 那对被拆开的重名**(任务点名要保的一处)现查仍是"改名的一份 + 未改名的一份":

```
$ grep -c '注册表全仓持有者名单' live/unit.names           → 2
$ grep -c '注册表全仓持有者名单:每条 def' live/unit.names  → 1
```

没有合出第三条同名的。`integration` 仍是 130 条 / 129 个名字(那对同名用例照旧,见 5.3)。

### 4.5 索引份数 N 与用例里的下限:按合入后 live 上调

W105 立的这层判据有三份记录要同时对上:目录实况、`docs/skills-wave/README.md` 明写的份数、索引表行数。

```
基线 4f977aa:127 份   合 w110 后:128 份   加本槽记账件后:129 份
```

合并提交里明写份数已随并入抬到 **128**(三方对齐是硬判据,不抬当场红);
本槽记账件落地后目录是 **129**,明写份数与用例里的下限 `FLOOR` 一并抬到 **129**。
**下限写成 `>=` 而不是 `==`** 的理由见 [w105-index-completeness.md](./w105-index-completeness.md) §2.1,
本槽照它交待的做法(「新开一槽记账时把下限抬到当轮实况」)把棘轮往上推一格。
「最新一份收敛记录」那个指针同步改成本文,`w109` 挪进"更早的分叉登记"那串的队首。

### 4.6 接进来的散文里的数与现在时表述,逐处现查

| 出处 | 原文 | 处理 |
|---|---|---|
| `w110` 索引行 | 全文无测试用例数、无份数 | **无需处理**(这一槽的索引行没写会过期的余量数) |
| `w110-split-only-script.md` 正文 | `467 → 470` / 基线 `w108`(`51fdec7`) | **不动**(W38 定的:分支记账件里的数字不随并入更新;且它自带"基线"限定) |
| `w108` 索引行 | 「让它写进去会翻转发布门 G1」 | **改**——但这处不是"过期的余量数",是**判据本身记错了**,理由与边界见 4.3 |

口径沿用 W95/W99/W107/W109:**接进来的散文里的余量数字一律删数留口径;自带"本槽/基线上"这类限定词的留着不动。**
本槽的余量数字实际一处都没删(连续第二槽),但多出 4.3 那一类——
**"判据记错"与"数字过期"不是同一件事,前者要改到 live 面为止,后者只删数留口径。**

## 5. 实测与取证

### 5.1 三套件数字

合并后现跑,不引用分支自测数:

| 时点 | `unit` | `integration` | `cli.smoke` |
|---|---|---|---|
| 基线 `4f977aa` | 471 / 471 | 130 / 130 | 100 / 102 |
| 合 `w110` 后 `7caa286` | **474 / 474** | 130 / 130 | 100 / 102 |

README 里跟着 live 改的两个数:`unit 471 → 474`、`docs/skills-wave/README.md` 明写份数 `127 → 129`(含本槽记账件)。
`cli.smoke` 的 102 与 `integration` 的 130 没动,但两个都现跑复核过一遍
(`w102` 那条断言会拿它们去和 README 对账,写错即红)。

`node --check` 过:`js/proj-upload.js`、`js/skills.js`、`js/domain.js`、`js/release.js`、`js/release-core.js`、`tests/unit.js`。

### 5.2 `cli.smoke` 那 2 项失败:与 `master` 同名同表现

`master` 现开工作树实跑:

```
$ git worktree add --detach /tmp/wt/master origin/master && cd /tmp/wt/master && node tests/cli.smoke.js
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
==== CLI 冒烟:51/53 通过 ====
```

本槽 HEAD 上失败的是**同样两条、同样表现**(`exit=1` / `undefined`),差别只在总数(53 → 102)。
按既有口径记成"与 `master` 同名同表现的既存失败",本槽不修、也不算回归。

### 5.3 零吃测:多重集双向对照全空,并把 W109 那处去重盲区复现一次

**名集口径**:`w110` 相对自己 `merge-base`(`51fdec7`)取增量,与基线取并集,与合并后 live 逐条双向 `comm`
(抽名前按 `|` 切掉回执载荷,否则 `cli.smoke`/`integration` 那些带期望/实际串的行会假报差集——W104 记过这一处;
两侧一律**不去重**——W109 记过这一处)。

| 套件 | `w110` 增量 | 与基线增量相交 | 期望集 \ live | live \ 期望集 |
|---|---|---|---|---|
| `unit` | +3 / −0 | **0** | **0** | **0** |
| `integration` | +0 / −0 | **0** | **0** | **0** |
| `cli.smoke` | +0 / −0 | **0** | **0** | **0** |

`w110` 的三条增量全在 `split` 套件,与基线自己那批增量(`contract` 4 条改 1 条、`memory` 1 条、`cli.smoke` 4 条)
**现查为零相交**(不是默认的——W99 正是在这一步翻的车),故"增量相加"这条算法这一次成立:

```
unit 471 + 3 = 474   ← 与 live 逐字相等
```

**W109 记的那处去重盲区,本槽有意撞了一次,复现成功。** 把期望集换成 `sort -u`(去重)、live 侧仍不去重:

```
$ sort -u 基线.int 增量.int > 期望_u.int; comm -13 期望_u.int live.int
项目不存在 404
```

一模一样地假报一条。这不是新增用例,是 W64 记过、W96 点名过的那对同名用例——
`integration` 130 条只有 129 个不同名字,去重把期望侧压成一份、live 侧仍是两份,差集就多出一行。
W109 是踩到之后才判出来的,本槽是**照它写的判据主动去撞**:
盲区因此从一次性观察变成两次可复现的实测,而正式取证一律用两侧都不去重的多重集。

三套件的重名实况一并现取,免得下一槽再猜:

```
unit       474 条 / 474 个不同名字   ← 相等
integration 130 条 / 129 个不同名字  ← 那对同名仍在
cli.smoke   102 条 / 102 个不同名字  ← 相等
```

### 5.4 能力面:七个数一个没动

| 口径 | 基线 | 合并后 |
|---|---|---|
| `Prompts.list()` 注册表条数 | 41 | **41** |
| `Skills.CHECKS` 校验项 | 17 | **17** |
| `Skills.preflightStages()` 面数 | 7 | **7** |
| `Skills.gaps()` 键数 | 20 | **20** |
| `KB.SECTIONS` 条目 | 18 | **18** |
| `Skills.playbooks()` | 5 | **5** |
| 短名单条数 / 剩余 `pending` | 30 / 0 | **30 / 0** |

注册表无重键、`review.userSystem` 仍是单键(现取两项都成立)。
**行为面的变更集中在浏览器一条路径上**:「仅进行分集」拆集成功后原文落进剧本板块,
此后主线剧本步、两条命令的 `no-script` 闸门与计划步推导都不再把已拆集的项目当成无剧本。
判据不在本文——见 [w110-split-only-script.md](./w110-split-only-script.md) 第 3、4 节的对照表与变异实测。

### 5.5 门没抬:三面取证

任务口径与 `w110` 自己的"不做"清单都写明不抬也不降发布门。三面现查:

1. **源码面**:合并的 diff 里 `js/release.js` / `js/release-core.js` **一行未改**
   (`git diff --stat 4f977aa..HEAD` 只有 `README.md` / `docs/skills-wave/README.md` /
   `docs/skills-wave/w110-split-only-script.md` / `js/proj-upload.js` / `tests/unit.js` 五个文件)。
2. **行为面**:`cli.smoke` 那条基线项目发布用例的回执与 `master` 逐字相等——
   `发布门未通过(fail=4,warn=1)`,fail/warn 计数与 overall 口径都没动。
3. **判据面**:`w110` 那条正反用例合并后真跑为绿(同一夹具只改 `script`,G1 两次同为 `pass`、
   `fails`/`overall` 全等;而主线剧本步两次结论相反)。这条同时也是 4.3 那处订正的守卫——
   谁为了"让 G1 认剧本"去动门禁判据,它当场红。

顺带记两处**确实**随这次写入变化、但都不改门禁结论的面(`w110` 第 3 节已列,本槽合并后复核成立):
G7 合规扫的项目级文本现含整本原文(G7 是 warn 门,命中经 HumanReview 复核)、
问题中心剧本面的低危提醒开始对整本原文出(低危不进 G2 的高/中危计数)。

## 6. 剩余未合与残留

### 6.1 相对 `master`

`origin/master..HEAD` 共 **390** 个提交尚未回主干(与前几槽同口径,本槽不做合入 `master` 的动作)。

### 6.2 远端槽分支:合并那一刻 130 / 130

逐条现取,合并提交推上去之后跑的第一遍:

```
$ for b in $(git for-each-ref --format='%(refname:short)' refs/remotes/origin/cursor); do
    git merge-base --is-ancestor $b HEAD || echo "未合: $b"; done
祖先 130 / 未合 0
```

**这条集成线连续第三次做到零 outstanding**(W107 第一次 126/126、W109 合并那一刻第二次 128/128)。
但 W109 第 6.2 节的教训照抄不误:**"零 outstanding"从来只是某一刻的读数,不是一个可以引用的状态**——
W109 就是在记账件落地前又 `fetch` 出一条 `w110`(也就是本槽合的这一条)。
下一个集成槽照旧要现取一遍,不引用本节。

每份记账件都在目录里、每份都有自己那行索引、份数三方对齐(合入 `w110` 后 128 份,加本文 129 份)。

### 6.3 本槽留下的残留与交接

1. **W109 那三条残留一条没治,原样往下传**:索引表行序无断言兜底(按波次号排是人工口径)、
   索引表重复行报得出但指向不准、`integration` 那对同名用例仍在(130 条 / 129 个名字)。
   第三条本槽在 5.3 又加了一笔实测——它现在有两次可复现的记录,两个方向(吃掉一条 / 多报一条)都验过。
2. **`w110` 交接的四条原样有效**,其中两条对下一个动这条链的槽最相关:
   - **W108 交接第 5 条只解了一半**:「仅进行分集」现在写 `p.script` 了,但 `doSplitRun` 还没收口到
     `Commands.execute('project.splitEpisodes')`。要收得先想清楚写入与调用的先后
     (命令按 `pid` 从 `p.script` 取正文),并且别把 UI 侧的覆盖确认弹窗与在飞拦截 toast 变成静默 `blocked`。
   - **`p.script` 现在有三个写入点**(上传弹窗「解析剧本」、`js/proj-upload.js` 的「仅进行分集」、
     `js/director.js` 精细模式,另加 CLI `project-script`)。再加写入点前先判"写在动作成功前还是成功后"。
3. **发布门 G1 的判据不要再按"有剧本"记**。这一处 W108 记错、W109 交接原样往下传了一轮、W110 按 live 代码订正、
   本槽把 live 导航面上的最后一处改掉(4.3)。判据现在有正反两条用例钉着,改门禁判据会当场红。
   两份记账件正文里那两处旧措辞按存档口径留着不动——**读记账件正文时要连它的时点一起读**。
4. **W108 交接的那条源级断言仍只钉着 `js/director.js` 一个文件**,别的文件新长出直写 `p.subjects` 不会被它报出来;
   要扩得把断言的文件名做成清单(与 G-13 的全仓持有者名单同形)。
5. **`js/proj-upload.js` 的行为面沙箱 `loadProjUpload()` 只覆盖了拆集那条链**,
   主体确认弹窗与拉片建集两条链仍零行为断言(`openRip` 要接得先给 `Media`/`FF` 两侧做桩)。
6. **`cli.smoke` 那 2 项与 `master` 同名的失败**照旧未修(见 5.2),不属本槽范围。
7. **G-13 缺口仍开着**,三张全仓名单本槽一个字未改(零收编、注册表零变动)。
