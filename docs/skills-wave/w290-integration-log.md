# W290 · 把 W289 合入 W288 尖(集成线上第十五条「快进形」)

一条槽:W289 给主体库卡片补同 id 重复标记(撞车的每一位在名字旁报出「第几位 / 共几位」),
并把位次那一步派生下沉 `Domain.dupIdMarks`,进同一条集成线。一次 `--no-ff`,零冲突。

产品语义一个字没动;记账、索引、份数下限三处按合入纪律同轮校齐。

---

## 一、支尖与形状(全部现取 `rev-parse`,不采信任何一侧的自称)

| 位 | 现取 SHA | 与自称对不对得上 |
|---|---|---|
| 我方尖 `P1` = `origin/cursor/w288-integration-4291` | `f9a6bfa11591b307f1c802f564f37d2a8f8fb41b` | W289 自称从 `f9a6bfa` 起支,**逐字节相同** |
| 对侧 `P2` = `origin/cursor/w289-dupcard-7c31` | `aaf2b7f8f08c46ad1aa1993eb3f0874813ffe40a` | 自称 HEAD `aaf2b7f`,**逐字节相同**,且是链尾 |
| `merge-base(P1, P2)` | `f9a6bfa` | **恰等于 `P1`**,即 `P2..P1` 空 → **快进形**(集成线上第十五条) |
| 新支 | `cursor/w290-integration-2ade`,从 `f9a6bfa` 拉出 | |
| merge commit `M` | `53493a8a3b586d601a77f2c19ad305dcda6d38b6`,parents `f9a6bfa` + `aaf2b7f` | |
| `tree(M)` / `tree(P2)` | 两边都是 `64c54f31b2cba85082233714078385336244533e` | 逐字节相同 |

`P1..P2` 两条提交:`3886129`(主体库卡片补标记 + `Domain.dupIdMarks` 下沉 + 判据 +2 + 护栏登记)、
`aaf2b7f`(记账件 + 索引一行,记账件下限抬到 live 304)。

**快进形让整树 diff 恒空,这一格一条结论都不承载**(第十五次)。`git diff M P2` 空、
`tree(M) == tree(P2)`,两句在任何一次快进形合入上都恒真——包括「对侧一个字都没改」和
「我方把东西丢干净了」这两种正相反的情形。故取证全部压在**变异**与 **live** 两面。

产品面 `git diff P1 M` 逐文件现取:`js/domain.js`(+23/−0)、`js/roles.js`(+29/−3)、
`tests/unit.js`(+134/−2)、`README.md`(+2/−2)加两份文档 + 一份新记账件,合计 7 个文件。
`cli.js` / `mcp.js` / `server.js` / `index.html` / `js/storyboard.js` / `js/sb-views.js`
**六个文件 diff 全空**——两条 CLI 命令、MCP 工具表、分镜面那条顶栏入口、按 id 删那两处一格未动。
但"产品面不空"只证明**对侧改了东西**,仍不证明**我方没丢东西**。

---

## 二、本槽真正承载结论的那两格

### 2.1 同一手变异,起点红几条 / 合完红几条

六手同一概念的变异,在起点 `f9a6bfa` 的干净 worktree 与合完 `53493a8` 上各整跑一遍
`node tests/unit.js`(每手跑完把文件原样写回):

| # | 变异 | 起点 `f9a6bfa` | 合完 `53493a8` |
|---|---|---|---|
| 基线 | 不改 | `705/705 PASS, 0 FAIL` | `707/707 PASS, 0 FAIL` |
| M1 | `dupIdMarks` 里 `nth` 从 2 起数(位次偏一格) | **锚点失配(命中 0 处)** | **红 2** |
| M2 | 位次按本分类 tab 取(`list.indexOf` 而非 `p.subjects.indexOf`) | **锚点失配** | **红 1** |
| M3 | 为小标再扫一遍主体库(页面第二份扫描) | **锚点失配** | **红 1** |
| M4 | 只标要改的那几位(留原 id 那一位不标) | **锚点失配** | **红 1** |
| M5 | 位次算法抄一份到页面上(不读 `Domain.dupIdMarks`) | **锚点失配** | **红 1** |
| CF1 | `TOPIC_FLOOR` 24 → 23(花名册 24 行) | **锚点失配**(起点上那个字面是 23) | **红 1** |

**六手在起点上锚点全部失配、在合完上逐手红**:失配这一格本身就是取证——那段代码在起点那棵树上
压根不存在,`TOPIC_FLOOR = 24` 这个字面也不存在(起点是 23),故「合进来的东西是新的」
不靠任何一侧的文档口径。这是快进形这一槽里说得出话的第一格。

CF1 现跑红 1 即明令那句「禁止仍写 23」有判据接着:下限 23 低于花名册的 24 行,当场红。

### 2.2 live 探针:同一份夹具在起点 9/13、合完 13/13

第二格换判据类别——不看用例、看**渲出来的页面**。探针真起服务(`MV_DATA_DIR`/`MV_UPLOADS_DIR`/
`MV_CONFIG` 隔离到临时目录)+ 无头 Chrome + CDP,真 `Store.pullState()` 拉状态、真渲主体库页
(`#/project/<pid>/roles`),探针只在 `/tmp` 跑、不进仓库。夹具有意造两格:

- 同 id 三位 `sj_dup`,落位是**第 0 位(角色)/ 第 2 位(角色)/ 第 3 位(场景)**——第三位跨到另一个分类 tab;
- 中间夹一位不撞车的 `sj_solo`(角色)。

| 探针格 | 起点 `f9a6bfa` | 合完 `53493a8` |
|---|---|---|
| 主体库页真渲出来(角色 tab 三张卡) | ✔ | ✔ |
| 页头入口报「🧹 主体 id 去重(2)」(首位不算) | ✔ | ✔ |
| **角色 tab 两枚小标 `第 1/3` + `第 2/3`** | ✘ 取到 `[]` | ✔ |
| **悬停那句说明报出共几位 / 哪个 id / 这一位的去向** | ✘ | ✔ |
| 不撞车那张卡零小标 | ✔ | ✔ |
| **场景 tab 那一位标「第 3/3 位」(共几位按全库算)** | ✘ 取到 `[]` | ✔ |
| 切 tab 后页头那个数照旧是全库口径的 2 | ✔ | ✔ |
| 渲两遍主体库一个字段没被改写 | ✔ | ✔ |
| 零任务零计费(任务台 0 条、积分 100 → 100) | ✔ | ✔ |
| `dedupeScan(p.subjects)` 源级恰 1 处 | ✔ | ✔ |
| **位次只从 `Domain.dupIdMarks` 派生(页面零第二份位次算法)** | ✘ | ✔ |
| 页面渲染全程零未捕获异常 | ✔ | ✔ |
| **合计** | **9/13** | **13/13** |

**四格从红翻绿**,而它们全是产品面的话(卡片上有没有那枚小标、跨分类那一位标的是第几位)。
这一格与 §2.1 分工不同:变异证明「这段代码是新的、且它有判据护着」,探针证明
「这个功能在真页面上是真的渲得出来」——整树 diff 空集在这两格面前都说不上话。

**场景 tab 那一格是有意造的**:按本 tab 数的话跨分类那一位会标成「第 1/1 位」,
而角色 tab 上那两枚恰好仍对——夹具不跨分类的话 M2 这手量不出来。

---

## 三、live 数字(逐格现跑,两侧自称一律不抄)

| 项 | W288 自称 | W289 自称 | **本槽 live** | 说明 |
|---|---|---|---|---|
| `unit` 总数 | 705 | 707 | **707** | `707/707 PASS, 0 FAIL`;24 套件逐格求和 `= 707` 自洽。**W288 自称的 705 是起点数,没抄** |
| `unit · commands` | 67 | 68 | **68** | +1(行为面与源级并在同一条) |
| `unit · domain` | 45 | 46 | **46** | +1(`dupIdMarks` 纯函数面) |
| `unit · contract` | 153 | 153 | **153** | 未动(新登记护栏主题只改 `GUARD_TOPICS` 那张表) |
| `unit · skills` | 104 | — | **104** | 未动 |
| `unit · issues` | 22 | — | **22** | 未动 |
| `integration` | 152 | 152 | **152** | `152/152 PASS, 0 FAIL` |
| `cli.smoke` | 115/117 | 115/117 | **115/117** | 见下 |
| MCP `tools/list` | 40 | 40 | **40** | 运行期 `tools/list` 现取,不数源码字面 |
| CLI `Object.keys(CMD)` | 50 | — | **50** | bracket **26** + dotted **24**,两种写法之和 |
| `Domain.dupIdScan` 同读处 | 4 | 4 | **4** | `cli.js:727`、`cli.js:814`、`js/roles.js:284`、`js/storyboard.js:36`——本槽一处没加 |
| `Domain.dupIdMarks` 消费处 | 0 | 1 | **1** | `js/roles.js:376`(定义在 `js/domain.js:427`) |
| `gaps()` 键数 | 20 | 20 | **20** | 一条没剥(`skill 索引缺口键集:gaps() 20 键逐个点名` 那条现跑绿) |

**名集多重集 `+2 −0`、`705 + 2 = 707` 自洽**(两棵干净整树各取一遍用例名、排序后双向比对):
进来的正是那两条新判据、**一条没出**。

- `commands · 主体库卡片自己认得出撞了 id:撞车的每一位在名字旁挂一枚「第几位 / 共几位」小标(共几位按全库算),位次只读同一份扫描派生、一个字不写库`
- `domain · dupIdMarks:同 id 那几处各自的位次只从 dupIdScan 的结果派生——首处算第 1、计划里第 k 条算第 k+1,单位词不进这一层`

`cli.smoke` **单独整跑**,`env -u HUJING_SERVER` 加 `MV_DATA_DIR` / `MV_UPLOADS_DIR` / `MV_CONFIG`
隔离,**115/117**;两条失败与 `master`(`9adcf0ff964891dc17c352f6ae06db6ee7a9383b`)在同 env 下
现取的 **51/53** 同名同表现,明令允许:

- `FAIL | 未登录 whoami → exit 3 | exit=1`
- `FAIL | llm --json mock 链路 | undefined`

`node --check` 逐个跑过:`js/domain.js` / `js/roles.js` / `tests/unit.js` 全 OK。
**按明令不跑 `tests/e2e.js`。**

---

## 四、`TOPIC_FLOOR`、花名册与棘轮(对齐到 live)

| 格 | live | `FLOOR` 字面 | 差额 |
|---|---|---|---|
| 单元测试 | 707 | 707 | 0 |
| 集成测试 | 152 | 152 | 0 |
| CLI 冒烟 | 117 | 117 | 0 |
| 记账件份数 | **305**(本文进来那一份) | 304 → **305**(本槽抬) | 0 |
| 护栏主题 | 25(在册 24 + 销号 1) | 24 | 1 |

`SLACK` **3**,五格差额 `0/0/0/0/1`,**一格都没超**。

**`TOPIC_FLOOR` 与花名册对齐到 24**(明令:对侧自称 24,禁止仍写 23):
`GUARD_TOPICS` 在册 **24** 条 / `GUARD_TOPICS_CLOSED` **1** 条 / `TOPIC_FLOOR` **24** /
花名册 **24** 行,四者对账。W285 的 `dedupe-ui-rule-single`、W287 的 `dedupe-shots-ui-single`
与 W289 新立的 `dedupe-card-mark-derive` **三条都在**,不是三选一。
CF1 现跑证明「仍写 23」当场红 1。

**真并集逐处现取核过**(快进形让并集自动成立,但不采信"自动"这两个字):

- `README.md` 第 **299** 行「🧹 主体 id 去重」那段现在带着卡片小标那半句(`tag yellow` 同形、
  两处同读一份扫描、共几位按全库算、`Domain.dupIdMarks` 点名),第 **300** 行「🧹 镜头 id 去重」
  那段(W287)**原样都在**;第 **618** 行单元数写 **707**、第 **620** 行集成写 **152**、
  第 **624** 行冒烟写 **117**,三处与 live 相等。
- 花名册末四行:`landed-seat-order`(W281)/ `dedupe-rule-single`(W283)/
  `dedupe-ui-rule-single`(W285)/ `dedupe-shots-ui-single`(W287)/ `dedupe-card-mark-derive`(W289),
  在册集合与花名册集合双向差集都空。
- `docs/skills-wave/README.md` 索引表:`w287` / `w288` / `w289` 三行都在且按波次号递增,
  本文那一行插在 `w289` 之后;`索引表共 N 份记账件` 那句同轮改到 **305**,与目录实况、
  索引表行数三方对齐。

---

## 五、核过的四格(明令逐条)

| 明令那一格 | 现取核法 | 结果 |
|---|---|---|
| **核卡片标记** | 探针在真页面上取到 `🧹 id 重复 第 1/3 位` / `第 2/3 位`(角色 tab)与 `第 3/3 位`(场景 tab),悬停那句说明报出共 3 位共用 `sj_dup`、这一位的去向;不撞车那张卡零小标 | ✔ 见 §2.2 |
| **`dupIdMarks`** | 定义在 `js/domain.js:427`,消费恰 1 处(`js/roles.js:376`);页面侧零 `nth:` 字面、零 `.plan.filter`,位次不在页面上再算一遍 | ✔ |
| **`dupIdScan` 四处同读** | `cli.js:727`(主体命令)、`cli.js:814`(镜头命令)、`js/roles.js:284`(主体库入口)、`js/storyboard.js:36`(分镜面入口) | ✔ 四处,本槽一处没加 |
| **分镜卡片仍无标记** | `js/storyboard.js` 与 `js/sb-views.js` 现取 `dupIdMarks` / `id 重复` 各 **0** 处 | ✔ 明令下一槽,本槽不代做 |

明令没碰而现取核过一遍的:去重写库语义(`openDedupe` 的扫描/重算/落库三句一字未改)、
删除语义(`js/roles.js` 按 id 删那一处一字未改)、`unique` 口径(`js/domain.js` 那一行未动)、
`cli.js` 两条命令、`lastReview` 合入、入口不加闸、`state-put` 不加闸、`gaps()` 不剥、SK-04。
探针另量过:渲两遍主体库 `p.subjects` 逐位 `id/name` 逐字节原样,任务台 0 条、积分 100 → 100
一分没动——**卡片小标是纯展示**这句是渲出来量出来的,不是读代码读来的。

---

## 六、残留(按明令一条没代修,原话逐条保留)

W289 那五条,原话照搬:

1. **分镜卡片还没有同 id 重复标记。** 明令点名下一槽。形状不能照抄本槽:分镜侧的单位词是「行」,
   而卡片在四视图 + 剪辑台五档里各有各的渲法(缩略图卡 / 节拍格 / 镜头组格),
   「挂在哪一处才五档都看得见」得像 W287 挂顶栏那样先量一遍;`Domain.dupIdMarks` 收下
   `dedupeShotScan` 换过单位词那份结果照样读得动(本槽判据已把这一格喂过),要加的只有展示那一层。
2. **`cli.js` 的 `shots-dedupe` 注释里点名 `Domain.reviseTargets` 的那半句仍不准**(W287 残留第 1 条原样在)。
   明令不碰,继续记账点名。
3. **发现存量重复仍得先走到主体库那一页。** 项目详情面与项目卡片上没有对应的角标,
   页头那个按钮与本槽这几枚小标都只在主体库(独立页与项目详情嵌入版)渲出来;
   整树导入/恢复那几条路照旧能写进重复 id(明令不给入口加闸),这仍是**存量**出口。
4. **小标只报位次,不报"这一位与哪一位撞了"的跳转。** 用户读到「第 2/3 位」之后想看第 1 位长什么样,
   仍得自己在同一 tab(或别的 tab)里翻——跨分类那一位尤其翻不到,小标那句说明只提了一句
   「可能分散在不同分类下」。要收得给小标挂上按序号定位的跳转(与分集页 `__roleFocus` 那条路同形)。
5. **主体侧与镜头侧的展示文案仍各写一份**(W287 残留第 5 条原样在)。本槽又添了一句
   (卡片小标那句说明),要合并成一份模板得先把单位词与引用面交代那一段参数化。

W287 那五条未收项,原话照搬:

1. **`cli.js` 的 `shots-dedupe` 注释里点名 `Domain.reviseTargets` 的那半句已经不准了。**
   它仍写着 `lastReview.perShot[].shotId` / `ep.uiSel` / `Domain.reviseTargets`「一律按 `find` 首行语义解析…
   去重前后落到的是同一行」;而 W263/W267 把行对位改成序数口径之后,后两者是经 `Domain.reviewRows`
   按序数落行、**去重后才退回首行**。就**行为**而言两端一致(本槽行为面逐格量过),不准的只是那句注释。
   本槽没改它:明令只做一条产品缺口,且它属于「审片合入那一问」的交代面。改的时候连 §2.2 那张表一起搬。
2. **旧报告回位仍只有「重跑一次整集审片」一条路。** 本槽把「会塌几条」现算现报进预览、并把回位办法写在同一格里,
   但那是让用户自己再花一次审片的钱。真正的收法是让 `perShot` 的行对位不再靠序数(例如落库时按行写一个稳定的行键),
   那是改审片合入,明令不在本槽。
3. **「同 id 多行」在页面上仍只有这一个出口,没有入口侧的闸。** 整树导入/恢复那几条路照旧能写进重复 id
   (明令不给 `state-put` 加闸),这条入口是**存量**出口;发现它仍靠打开分集那一下顶栏上多出来的按钮,
   项目详情面与分集卡片上没有对应的角标。
4. **一位不删的那笔代价照旧摆在预览文案里。** 用户看完计划想「少几行」的话,单镜删除仍按 id 匹配、
   会把同 id 那几行一并删光(那是删除语义不是去重语义,明令不碰)。预览里把这笔代价说清了,
   但页面上没有「删某一行」的路。
5. **主体侧与镜头侧两条入口的文案各写一份。** 两条入口同形(同读 `Domain.dupIdScan`、同一道确认闸),
   但弹窗里那几句话是各写的;要合并成一份模板得先把单位词(位/行)与引用面交代那一段参数化。

另新记一格,如实登记本槽没做的量:

- **快进形第十五次,树面那两句仍是恒真句,本槽一条判据都没为它补。** `git diff M P2` 空与
  `tree(M) == tree(P2)` 在这十五次里逐次成立,而它们对「我方丢没丢东西」始终一言不发。
  本槽新添的那一格(§2.2 探针在起点 9/13、合完 13/13)是**换判据类别**换来的,不是树面说出话来;
  要让树面自己说得出话仍得做产品面文件集的定向比对,那是另一条线的事。

---

## 七、一句话

快进形第十五条,整树 diff 恒空与 `tree(M) == tree(P2)` 两格都不承载结论;
承载的是两格换来的话——**六手同一概念的变异在起点上锚点全部失配、在合完上逐手红 1/2 条**
(含 CF1:`TOPIC_FLOOR` 退回 23 当场红,即明令那句「禁止仍写 23」有判据接着),
以及**同一份跨分类夹具的 live 探针在起点 9/13、合完 13/13**,四格从红翻绿的全是产品面的话。
数字逐格现跑、**W288 自称的 705 与 W289 自称的 707 都没盲抄**(705 是起点数、707 是 live),
名集多重集 `+2 −0` 自洽;`TOPIC_FLOOR` 与花名册**对齐到 24**、记账件下限 304 → 305 同轮校齐;
`SLACK` 3 一格没用掉、`gaps()` 20 键一个没剥;`dupIdScan` 仍四处同读、`dupIdMarks` 一处消费、
分镜卡片现取仍 0 处标记;W289 五条与 W287 五条残留一条没代修、原话逐条保留。
