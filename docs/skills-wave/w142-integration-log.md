# W142 · 两条槽并入集成线的收敛记录(集成分支)

> 集成分支:`cursor/w142-integration-4266`,基线 `cursor/w139-integration-a7f2 @ 2851d97`。
> 本文只记**收敛过程**:基线怎么定的、合了哪两条、冲突面长什么样、合并后的实测数字、没删测没摘 gaps 的取证。
> 被合入两槽自己的内容分别在 `w137-gate-catch.md` 与 `w138-issues-empty.md`,本文不代述、不复写、不改它们正文里的任何时点读数。
> 两次合并都 `--no-ff`、各一个合并提交、可 revert;全程只解冲突与收敛数字,**不重做已落地的功能**。唯一一处改到别人判据的地方在 3.1,那是两条判据真撞、不改就合不上,处置与理由逐句记在那里。

## 1. 结果一句话

`--no-ff` 合入两条,按序、**各取被合入支的实际 HEAD**:

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w137-gate-catch-9a2f@3a4e458` | `43eec57` | 发布门 G4/G5/G6 的聚合外面兜着一个空 `catch`:三门是同一次遍历的三个投影,遍历失手时拿初值照常报「0 镜 pass」。改成三门共用一个 `aggErr`,缺注入记 fail、`Domain` 自身抛错记 warn,`fails`/`warns` 跟着记 | `js/release-core.js` 21 行 | +2 条(`release` 套件) |
| `w138-issues-empty-ca5b@16eb33d` | `e5c98fb` | 问题中心在无分集时一条问题都收不出来(空项目/只有剧本/只有主体实测 0 条):剧本/主体/分集三步的阻塞码收进 `Domain.gateBlockers` 双端单源,问题中心按一张门槛投影表 `Issues.gates()` 投影,流程条那三步改读同一份 | `js/domain.js` 24 行、`js/issues.js` 42 行 | +4 条(`issues` 2、`release` 1、`contract` 1) |

合并后回归 **`unit 501/501`**、**`integration 130/130`**、**`cli.smoke 100/102`**(2 项失败与 `master` 同名同表现,见 5.2),`contract` 子套件 **109/109**。

治理面零变动:提示词注册表 41 条、能力短名单 30 条、`Skills.CHECKS` 17 条、`preflightStages()` 七面、`gaps()` 20 键(`G-03` 仍是两条值)、`KB.SECTIONS` 18 条、`playbooks()` 5 条——七个数一个没动,且与 W139 一样是**结构性成立**:五个注册表文件相对基线零 diff(见 5.4)。整槽 `js/` 只有三个文件进 diff。

**本槽最值得记下的三件事:**

1. **任务口径预告的那处"真撞",撞的不是代码而是判据,而且代码那半 `git` 一个冲突块都没给。**
   W135(已在基线里)与 W137 改的是 `gates()` 同一个函数,但两处相隔九行未改的 G3 段,
   故 `git` 把两侧的 hunk 各自搬进去、**零冲突块**合上,合并后两侧行为逐字都在(5.1 里那份哈希对照证明它是真融合:四棵树的 blob 互不相等)。
   真红的是 W135 那条用例里的一句 `assertEq(bare.fails, withD.fails)`——它写在"漏注入时只有 G1 会多一门 fail"这个前提上,
   而 W137 让 G4/G5/G6 在同一情形下各多一门,同一夹具当场 `bare 4 / withD 2`。
   **即:代码合得上、判据合不上,而两者不在同一个文件里,`git` 对后者没有任何提示**——冲突块只标到 `tests/unit.js` 里那处两侧同位置插入的用例列表,
   与这句失配毫无关系,是跑了一遍才知道的。处置见 3.1。

2. **`FLOOR` 抄父值这条判据在本槽第一次真的逮住人,而且逮得比前三槽都全。**
   合并后 live:单元 501、记账件 154。逐格实测(4.2):
   单元那格三个父值 497 / 496 / 495 **全红**(差 4 / 5 / 6),记账件那格对侧那个 148 **红**(差 6)、我方那个 152 绿(差 2)。
   W139 量到的是"两侧父值全部放过、其中一侧压在 SLACK 边界(差 3)上",本槽因为一次合进 6 条用例、2 份记账件,
   `live − 父值` 头一回整体越过 SLACK 3。**这条判据仍旧只由 `live − 父值` 与 SLACK 的大小关系决定**,
   但本槽给出了它的另一半读数:**并槽越大它越管用**,小槽(每次只加一两条)正是它必然放过的那一类。

3. **W139 残留里那句"数字的静默窗口今天没有任何判据"本槽实测不成立,该收窄成"prose 没有判据"。**
   本槽第三次踩上同一个窗口,而且这次**踩错了**:契约段条数两侧都写 108(我方从 W136 抬的、对侧也抬到 108),
   `git` 判为"两边做了同一件事"零冲突块放行,而合并后 live 是 109。
   但它并没有静默过去——`contract` 那条精确对账当场红,报「实测 109,文档 108」。
   本槽把三个数字位逐个变异过(4.3):主 README 单元数、契约段条数、`docs` 明写份数,**抄任一父值都各自红且报错不混**。
   真正没有判据的是**散文**:W138 在主 README 的问题中心那一段整段改写,我方一字未动,`git` 直接取对侧、不请你过目,
   而没有任何一条用例读那段话。**"数字有判据、散文没有"才是这个窗口的准确形状**(见 4.4)。

## 2. 基线与这两个槽

### 2.1 基线

任务口径给定 `origin/cursor/w139-integration-a7f2` 的 HEAD,参考值 `2851d97`。现取核实:

```
$ git rev-parse origin/cursor/w139-integration-a7f2
2851d97...
$ git rev-parse origin/cursor/w137-gate-catch-9a2f
3a4e458...
$ git rev-parse origin/cursor/w138-issues-empty-ca5b
16eb33d...
```

三个都与任务给的参考值一致。基线上的四套件数字(开工时现跑,不引用 `w139-integration-log.md` 里的数):

| 套件 | 基线 `2851d97` |
|---|---|
| `node tests/unit.js` | **495 / 495**(`contract` **108 / 108**) |
| `node tests/integration.js` | **130 / 130** |
| `node tests/cli.smoke.js` | **100 / 102** |

与任务给的参考数字逐个相符。

### 2.2 两支的叉点:对基线是真三方,而且落后得多

两支的 `merge-base` 都是 `0dbd69e`(`w133` 的 tip),基线 `2851d97` 在其之上还有四个提交
(`c485c73` 合入 W135、`ccbc12d` 合入 W136、`42b2798`/`2851d97` 两份记账件)。故:

- **两次合并都是真三方合并**,`merge-base != HEAD`,两次都进冲突块;
- 两支彼此的 `merge-base` 也是 `0dbd69e`,即**它们互相看不见对方**,也都看不见基线上已经合掉的 W135/W136;
- 自称数与合并后 live 的关系:W137 自称 unit 494 = 492 + 2、W138 自称 unit 496 = 492 + 4。
  合并后 live 是 **501**(495 + 2 + 4),**两个自称数都不是答案**,而且这次两个自称数**都低于基线的 495**。

### 2.3 不合的两条

远端另有两支在飞,按任务口径**不合**:`w140`(gaps keys)、`w141`(report if)。本槽未取其内容、未做叉点核实,只登记"任务明确要求留着"。

## 3. 冲突面

两次合并共 **6 个文件次、9 个冲突块**,全部落在文档数字、索引表与用例列表上,**`js/` 一个冲突块都没有**。

### 3.1 第一次合并(W137):3 文件 4 块,外加一处 `git` 没标出来的判据失配

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `README.md` | 「单元测试(N 项断言」整句 | 495 那侧带着 W135 插进去的一句 / 494 那侧是叉点原句 | 纯插入,取我方整句、数字换 live **497** |
| `docs/skills-wave/README.md` | 明写份数 | 152 / 148 | 现取 live **153** |
| `docs/skills-wave/README.md` | 索引表尾 | `w134`+`w135`+`w136`+`w139` 四行 / `w137` 一行 | **并集**,五行按波次号排 |
| `tests/unit.js` | 用例列表同位置插入 | W135 两条 / W137 两条 | **并集**,四条都在 |
| `tests/unit.js` | 单元测试下限 | 495 / 494 | 现取 live **497** |
| `tests/unit.js` | 记账件 `FLOOR` | 152 / 148 | 现取 live **153** |

主 `README.md` 那一块按 W133 立的分辨法「差异里有没有删除」判:机器比对下来对侧那句是我方那句**去掉 W135 插入的 76 字**、只差那个数,
即**纯插入不是口径之争**,故取我方整句、W135 那句一个字不删(核实方式:把对侧整句按字符序在我方整句里走一遍,零删除即通过)。

**`js/release-core.js` 零冲突块,但合并结果是真融合。** 两侧改的是同一个 `gates()`:
我方(W135)在 G1 那段开头补必注入判定并把 blocker 的键名从 `action` 改成 `label`,
对侧(W137)把 G4–G6 的空 `catch` 换成共用 `aggErr`。两处相隔九行未改的 G3 段,`git` 各自搬进去即可。
5.1 那份四棵树哈希对照显示合并结果与三棵父/叉点树**互不相等**,即它确实是两侧改动的并,不是取了哪一整份。

**真撞的那一处 `git` 没标。** W135 那条用例里有一句:

```
assertEq(bare.fails, withD.fails, '同一夹具漏注入不许比注入时少一门 fail');
```

它写在"漏注入时只有 G1 会多一门 fail"这个前提上。合入 W137 后同一夹具实测 `withD.fails 2 / bare.fails 4`,当场红。
两条判据都对、都要留:W135 钉的是"G1 这一门不许被 `catch` 降级糊掉",W137 钉的是"那三门判不出来时不许照常报 pass"。
处置是**把 G4–G6 三门从两侧的 `fails` 里各自剔出去再对账**——那三门另有 W137 自己的用例守着,这里不重复算:

```
const aggFails = g => g.gates.filter(x => x.status === 'fail' && ['g4-stale', 'g5-unconfirmed', 'g6-failed'].indexOf(x.code) >= 0).length;
assertEq(bare.fails - aggFails(bare), withD.fails - aggFails(withD), '同一夹具漏注入不许比注入时少一门 fail');
```

改写后 W135 原来的强度一点没丢,这一点是变异实测的而不是眼看的:
把 G1 缺注入那一格改成"记 fail 但不 `fails++`",这一句仍**当场红**且报「期望 1,实际 0」(5.5 的 M6)。
副作用记在 6.1:这句现在与 W137 那半共享同一个计数器,故 W137 那半的计数回归也会顺带把它带红一次(M7)。

### 3.2 第二次合并(W138):3 文件 5 块

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `README.md` | 「单元测试(N 项断言」整句 | 497 那侧 / 496 那侧,而且对侧在同一行里**删了字** | 见下 |
| `docs/skills-wave/README.md` | 明写份数 | 153 / 148 | 现取 live **154** |
| `docs/skills-wave/README.md` | 索引表尾 | 五行 / `w138` 一行 | **并集**,六行按波次号排 |
| `tests/unit.js` | 用例列表同位置插入 | W136 的 SK-25 护栏 / W138 的前置门槛码单源 | **并集**,两条都在 |
| `tests/unit.js` | 单元测试下限 | 497 / 496 | 现取 live **501** |
| `tests/unit.js` | 记账件 `FLOOR` | 153 / 148 | 现取 live **154** |

主 `README.md` 那一块**与 3.1 那处形似而处置相反**:同样是"两侧同一行、长度不同",但机器比对出来对侧**有删除**——
它把问题中心那段里的「,空项目零条」四个字删掉,换成一句「空项目·只有剧本·只有主体三态各收出前置断点(…)」。
按 W133 的分辨法这是**口径之争**:合并后空项目确实不再是零条,旧那句话变成假的。
故处置是**取对侧的正文改动**(删的删、插的插)、同时留住我方那句 W135 的插入,再把数字换成 live 501。
核实方式与 3.1 同一手:改完后把对侧整句按字符序在结果里走一遍,零删除即"对侧那一侧一个字没丢"。

## 4. 数字怎么定的

### 4.1 一律现取合并后 live,不抄任何一个父值

| 数 | 基线 | W137 侧 | W138 侧 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|
| 单元用例数 | 495 | 494 | 496 | **501** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 108 | 108 | 108 | **109** | 主 `README.md` 契约段 |
| 记账件份数 | 152 | 148 | 148 | **154**(含本文后 155) | `docs/skills-wave/README.md` 明写 |
| 记账件 `FLOOR` | 152 | 148 | 148 | **154**(含本文后 155) | `tests/unit.js` |
| `integration` | 130 | 130 | 130 | **130** | 未动 |
| `cli.smoke` | 102 | 102 | 102 | **102**(实跑 100/102) | 未动 |

`contract` 那格是本槽唯一一处**三侧同值**:基线、W137、W138 都写 108(W137 那侧根本没改这个数,W138 把它从叉点的 107 抬到 108,恰好撞上我方从 W136 抬来的 108)。
`git` 由此零冲突块放行,而合并后 live 是 109(W138 加了一条 `contract` 用例)。**这是本线第三次踩这个窗口,也是第一次踩错**(前两次都恰好对)。

### 4.2 `FLOOR` 抄父值:本槽第一次真的逮住

在合并后 HEAD 上把两个 `FLOOR` 逐格改小重跑全套(`SLACK` 现取 3):

| 格 | 值 | 与 live 的差 | 全套结果 |
|---|---|---|---|
| 记账件份数(live 154) | 153 / 152(我方父值) / 151 | 1 / 2 / 3 | 全绿 |
| 记账件份数 | 150 | 4 | **红 1 条**(差额上限) |
| 记账件份数 | **148**(两支各自带来的父值) | **6** | **红 1 条** |
| 单元测试(live 501) | 500 / 499 / 498 | 1 / 2 / 3 | 全绿 |
| 单元测试 | **497**(第一次合并后我方值) | **4** | **红 1 条** |
| 单元测试 | **496**(W138 父值) / **495**(基线父值) | 5 / 6 | **各红 1 条** |

即:**单元那格的三个父值本槽全部被逮住,记账件那格逮住对侧那个、放过我方那个**。
四槽连起来读:W133 差 1(全放过)、W134 差 2 / 差 4(放过一、逮住一)、W139 差 1 / 差 3(全放过,一侧压线)、
**W142 差 2 / 差 4–6(单元三个父值全逮住)**。判据没变,变的是并槽的体量——本槽一次合进 6 条用例、2 份记账件,`live − 父值` 头一回整体越过 SLACK。

### 4.3 README 那三处抄父值必被逮住(逐处实测)

同样在合并后 HEAD 上试抄:

- 主 `README.md` 单元数改成 497 / 496 / 495 → **各红 2 条**(精确对账那条 + 三套件下限那条,报错句不同、含义不混);
- 主 `README.md` 契约段条数改成 108(即本槽真正踩上的那个静默窗口的值)→ **红 1 条**,报「实测 109,文档 108」;
- `docs/skills-wave/README.md` 明写份数改成 153 / 152 / 148 → **各红 1 条**(三方对齐那条,点名"目录实况 154 与明写数不符")。

### 4.4 静默窗口的准确形状:数字有判据,散文没有

W139 残留里写的是「今天没有任何判据在这一路上,合并后按 live 复核是唯一的守卫」。本槽逐处变异过,这句该收窄:

- **数字位有判据**。4.3 三处各自红,契约段那处正是本槽真出错的那一处,它是被判据接住的、不是靠人工眼力。
- **散文没有判据**。W138 在主 `README.md` 把问题中心那一整段改写(加进前置门槛三态、`Issues.gates()` 表、危险级与 G2 的关系,前后差近千字),
  我方那一段一字未动,`git` 直接整段取对侧、零提示,而**没有任何一条用例读那段话**。
  它这次改得对(内容正是 W138 落地的东西),但对不对与判据无关。

故这个窗口的形状是:**它对数字是"`git` 不提醒但判据接得住",对散文是"`git` 不提醒且没人接"**。

## 5. 取证

### 5.1 合并纪律:按四棵树的文件哈希机检

对每次合并,取 `M`(合并结果)、`P1`(我方)、`P2`(对侧)、`B`(叉点)四棵树里同一文件的 blob 哈希做对照:

| 合并 | 文件 | 两侧都改? | 结果 |
|---|---|---|---|
| `43eec57` | `README.md` | 是 | 四者互不相等 → **真融合** |
| `43eec57` | `docs/skills-wave/README.md` | 是 | 四者互不相等 → **真融合** |
| `43eec57` | `tests/unit.js` | 是 | 四者互不相等 → **真融合** |
| `43eec57` | `js/release-core.js` | 是 | 四者互不相等 → **真融合**(零冲突块合上的那份,见 3.1) |
| `e5c98fb` | `README.md` | 是 | 四者互不相等 → **真融合** |
| `e5c98fb` | `docs/skills-wave/README.md` | 是 | 四者互不相等 → **真融合** |
| `e5c98fb` | `tests/unit.js` | 是 | 四者互不相等 → **真融合** |
| `e5c98fb` | `js/release-core.js` | 只我方 | 等于我方 |
| `e5c98fb` | `js/domain.js` / `js/issues.js` | 只对侧 | 等于对侧 |

**两侧都改过的文件**逐个查完:整份取 `--ours` 零命中、整份取 `--theirs` 零命中、回退到叉点内容零命中。
只有一侧改过的那三个文件结果等于那一侧,是单边改动的必然结果、不是取整份。
两次合并全程未用 `checkout --ours/--theirs`,也未用 `git checkout <old> -- .`。

### 5.2 四套件实跑

| 套件 | 基线 `2851d97` | 合入 W137 后 | 合入 W138 后(本槽 tip) |
|---|---|---|---|
| `node tests/unit.js` | 495 / 495 | 497 / 497 | **501 / 501** |
| └ `contract` 子套件 | 108 / 108 | 108 / 108 | **109 / 109** |
| `node tests/integration.js` | 130 / 130 | — | **130 / 130** |
| `node tests/cli.smoke.js` | 100 / 102 | — | **100 / 102** |

`cli.smoke` 那 2 项失败逐条现取,并在 `origin/master` 的独立 worktree 上同跑一遍对照,**同名同表现**:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

单元用例的**不同名字数**现取 501,与用例数 501 相等——**连续第十一槽全部相等**。

### 5.3 没删测:按名成集双向比对

在基线与合并后 HEAD 上各开 worktree 跑一遍全套,把用例名成集比对:

- **只在基线里有的:0 条**(即一条用例都没被删掉、没被改名);
- 只在合并后 HEAD 里有的:**恰好 6 条**,逐条点名:
  - `release · release-core · G4/G5/G6 聚合漏注入 Domain:三门如实未过门,不许照常印「0 镜 pass」`(W137)
  - `release · release-core · G4/G5/G6 聚合半途抛错:三门同记 warn,不拿半截计数当全量报`(W137)
  - `issues · collect:空项目/只有剧本/只有主体 → 前置断点(码与文案取 Domain.gateBlockers,不是零条)`(W138)
  - `issues · collect:前置断点与流程条同一份——三步的码与文案逐夹具双向相等(码名分裂即红)`(W138)
  - `release · G2 不因前置门槛断点改判:无 p.script / 无主体的交付项目照旧 pass(前置断点一律低危)`(W138)
  - `contract · 前置门槛码单源:剧本/主体/分集三步的阻塞码只在 Domain.gateBlockers 一处,流程条与问题中心都不另写`(W138)

495 + 6 = 501,与实跑总数对得上。任务点名要保的两条(**双向码集相等**、**危险级不抬到让 G2 去读 `p.script`**)就在这六条里的第 2、5 条,变异实测见 5.5 的 M15/M16/M18。

### 5.4 没摘 gaps:零 diff

五个注册表/知识层文件相对基线**一个都没进 diff**:

```
$ git diff --name-only 2851d97 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js
(空)
```

故七个数不用现跑也成立;为求稳仍在合并后 HEAD 上现取核对了一遍:提示词注册表 41、能力短名单 30、`CHECKS` 17、面表 7、`gaps()` 20 键(`G-03` 仍是 `review.stage` / `review.reviseLoop` 两条)、`KB.SECTIONS` 18、`playbooks()` 5。
整槽 `js/` 只有三个文件进 diff:`js/release-core.js` 21 行、`js/domain.js` 24 行、`js/issues.js` 42 行。

### 5.5 抽查变异:十八组

全部在**合并后 HEAD** 上跑,一次只改一处,跑完即还原(还原后复跑 501/501 全绿、工作区 clean)。

| # | 变异 | 期望 | 实测 |
|---|---|---|---|
| M1 | `js/release-core.js` 的 G4–G6 整段退回空 `catch` 形态(即 W137 之前) | 红 | 红 2 条,分别报「`g4-stale` 判不出来不得报 pass」与「遍历抛错时按判不出来记 warn」 |
| M2 | 只让 G4 认 `aggErr`,G5/G6 照旧报 0 镜(即"分开补"那条歧路) | 红且点名 G5 | 红 2 条,两条都点名 `g5-unconfirmed` |
| M3 | 缺注入那格由 fail 降成 warn | 红 1 | 红 1,报「期望 fail,实际 warn」 |
| M4 | 摘掉 W135 的 G1 必注入判定,退回让 `catch` 兜成 warn | 红 1 | 红 1,报「缺 Domain 注入不得降成 warn」 |
| M5 | G1 blocker 键名退回 `action`(恒印 `undefined`) | 红 1 | 红 1,报「实际 `第1集(blocked)；第2集(ready)`」 |
| M6 | G1 缺注入记 fail 但不 `fails++`(**直打 3.1 改写过的那一句**) | 红 1 | 红 1,报「同一夹具漏注入不许比注入时少一门 fail:期望 1,实际 0」——改写没削弱它 |
| M7 | G4–G6 失手只改 `status` 不计数 | 红 | 红 3 条:W137 两条各自点名,外加 3.1 那句被顺带带红(计数器共享,记进 6.1) |
| M8 | 记账件 `FLOOR` 逐格 153 / 152 / 151 / 150 / 148 | 前三绿、后二红 | 与期望一致(见 4.2) |
| M9 | 单元下限逐格 500 / 499 / 498 / 497 / 496 / 495 | 前三绿、后三红 | 与期望一致(见 4.2) |
| M10 | 主 `README.md` 单元数抄父值 497 / 496 / 495 | 各红 | 各红 2 条 |
| M11 | 主 `README.md` 契约段抄父值 108(本槽真踩上的那个值) | 红 | 红 1,报「实测 109,文档 108」 |
| M12 | `docs` 明写份数抄父值 153 / 152 / 148 | 各红 | 各红 1 条 |
| M13 | 索引表只摘 `w137` 那一行(份数与文件都不动) | 红 | 红 3 条:双向对齐、**按份点名**、行数三方对齐 |
| M14 | 索引表只摘 `w138` 那一行 | 红 | 红 3 条,同上且点名 `w138-issues-empty.md` |
| M15 | 门槛投影表摘掉 `no-script` 一条 | 红 | 红 3 条,含**双向码集**那条的正向(`Domain` 给得出的码投影表漏了)与 G2 那条 |
| M16 | `no-script` 危险级由 low 抬到 mid(即任务点名不许做的那一手) | 红 | **红 12 条**,含「三条门槛态一律低危:期望 3,实际 2」与三条"提醒不得产出高/中危、门禁状态不变" |
| M17 | 问题中心不再投前置断点(退回只逐集循环) | 红 | 红 6 条,含双端单源那条 |
| M18 | `workflow` 剧本步不读 `gateBlockers`、自己再写一份码 | 红 | 红 2 条,含**双向码集**那条的反向(逐夹具同码同文案对不上)与"码只此一处"那条 |

另加三条对"要保的东西"的反向抽查:

- 把 CLI 闭环那轮 `low = await reviseTargets(args, f)` 退回摘回执里的 `lowShots` → **红 2 条**(重抽面单源那条 + SK-25 护栏那条的反向段);
- 把 `Domain.reviseShotIds` 从走 `reviseTargets` 派生改回调用侧自筛 `score < 7`(W131 之前形态)→ **红 2 条**,报错分别落在"重抽面 ∩ 分镜表实位"与"报告判旧一律回空",含义不混;
- 把 SK-25 的「仍欠(G-03)」三行整段抹掉 → **红 1 条**,报「note 须仍有『仍欠(G-03)』段」。

即 `reviseTargets` 派生、`FLOOR` 的 `SLACK` 字面(现取仍是 3,M8/M9 的报错句里逐条印着"上限 3 格")、SK-25 护栏三样在本槽 tip 上**都还在、都还有判据护着**。

### 5.6 本槽自己改了什么

除两个合并提交解冲突外,本文这一提交动了三处:新增本文、`docs/skills-wave/README.md` 补索引行并把明写份数 154 → 155、`tests/unit.js` 的记账件 `FLOOR` 154 → 155。
单元用例数与 `contract` 条数**本文一条不加**,故 501 / 109 不动。

合并提交里改到别人判据的只有一处(3.1 那句 `assertEq`),已按"两条判据都留、各管各的门"改写并变异复核(M6);
除此之外**没有删过任何一条用例、没有放宽过任何一条断言**。

## 6. 残留与欠账

1. **3.1 那句改写后与 W137 那半共享同一个 `fails` 计数器。** M7 实测:G4–G6 的计数回归会把 W135 那条用例也带红一次。
   这是"一个计数器、两处判据"的必然——只要判 G1 的那一门要经过 `fails`,它就看得见别人往里加减。
   本槽没有把它拆开(拆法要么换成不经计数器的弱判据、要么硬编码 W137 那三门的数,两条都比现在差),故留成已知耦合:
   **失败含义没有混**(两侧报错句各说各的),但**失败条数会串**。
2. **散文没有判据。** 4.4 已实测。本槽 `git` 整段取了对侧改写的问题中心那一段而无人过目,这次内容是对的,但没有任何机制保证下次也对。
   数字那半反而是本线判据覆盖最密的地方(4.3 三处各自红),两者不该再混在一句话里记。
3. **`FLOOR` 抄父值这条判据的可靠性与并槽体量正相关。** 4.2 已量:本槽单元那格三个父值全逮住,而记账件那格我方那个父值(差 2)照旧放过。
   小槽(每次只加一两条用例、一份记账件)仍旧必然落在 SLACK 内,**"禁止抄父分支"在这两个字面上对小槽仍是纯人工纪律**。
4. **W137 收的是 G4/G5/G6 那半,G1 与它们此后是两套写法。** G1 那段是"开头 `if` 判注入 + `else try/catch` 各自 `push`",
   G4–G6 是"共用 `aggErr` + 三门循环 `push`"。两段判的是同一件事(`Domain` 注入齐不齐、遍历抛没抛)却长得不一样,
   本槽按"不重做已落地的功能"没有把它们收成一份。哪天要收,得同时动两槽落地的四条用例。
5. **SK-25 的两个余面仍欠。** W136 补的是护栏不是收编:收敛次数(`maxRetry`)至今没有登记口径、两端闭环形态仍不同构。本槽未动,护栏实测仍红得出来。
6. **索引表行序仍无断言。** 本槽把 `w137`/`w138` 两行按波次号插进 `w136` 与 `w139` 之间,是人工排的第五次。判据只管"每份各有一行",不管行在哪。
7. **远端两支在飞未合**:`w140`(gaps keys)、`w141`(report if),按任务口径不合。故本刻"未合入数"是 **2**,这个 2 是"任务明确要求留着"而不是"还没到"。
