# W247 集成记账:两条槽并进同一条集成线(W245 镜头「多花几行」按点名清单原始条数算 + W246 CLI 主体生图按全表同 id 序数写回)

一次开支、两次 `--no-ff` 真 merge,次序 W245 → W246。两条被合入支都从 W244 集成 tip 平行叉出去,产品面零重叠(`js/domain.js` **只有** W245、`cli.js` **只有** W246,不是"几乎只有"——四树机检里这两个文件各自的另一侧与叉点逐字节相同)。

- 叉点 `B` = `fd5d675b97448056d33420516582a2b56fe75fac`(W244 集成 tip,现取)
- merge 一 = `82e22b1ab93b535bf38cec056afca167303a168a`,parents `fd5d675` + `43d0d566575e4b83710bfe1a8e36fc408fc192fd`
- merge 二 = `1be99079b1d0b44fdcbf38e6ea4192c1cb174360`,parents `82e22b1` + `313c330c215273bd6acb313dae5435eead2c497c`

`master` 没合、没开第三条功能支、没跑 `e2e.js`、没开 PR。

## 〇、交接自称的三个 SHA:一对两错,而错的那个是第十次重演同一个形状

| 支 | 自称 | 现取 | 判 |
|---|---|---|---|
| W244(我方叉点) | `f70ab0b8` | `fd5d675` | **错**,且 `f70ab0b8` 正是 `fd5d675` 的父 |
| W245 | `43d0d56` | `43d0d56` | 对 |
| W246 | `313c330` | `313c330` | 对 |

`f70ab0b8` 是 `merge(w242)` 那一次合并提交,其上还压着一条 `docs(skills-wave): W244 集成记账件 …校到 259` 才是分支 tip。这已是第十次「交接自称的 tip 是合入链中间那个 merge」——**而 W244 自己的记账件里就写着这条规律**(它记 W238 时写下「现取 tip,其后几乎一定有校数字 docs 提交」),下一轮交接照旧自称成了那个 merge。规律被写进口径不等于交接会照口径写,故**判定成本一格没省**:两个对的也是核出来才知道是对的,与错的那个在交接文里长得一模一样。

按 `f70ab0b8` 开支的后果是具体的:那棵树上记账件是 258 份、`FLOOR` 与索引明写都还是 258,W244 那一轮校数字的活会被整个吞掉,而本轮所有棘轮字面都要从一个比 live 低 1 格的水位起抬——落在 `SLACK = 3` 的缓冲里,一条判据都不会红。

## 一、两侧相对叉点各带来什么

| | W245(`43d0d56`) | W246(`313c330`) |
|---|---|---|
| 产品面 | `js/domain.js` +14/−8 | `cli.js` +14/−1 |
| 判据 | `tests/unit.js` +45/−4(净 +1 条,落在 `release` 套件) | `tests/unit.js` +62/−2(净 +1 条,落在 `commands` 套件) |
| 记账件 | `w245-dup-rows-extra-raw.md`(203 行) | `w246-find-subject-first-row.md`(211 行) |
| 共同碰的 | 根 `README.md` L612、`docs/skills-wave/README.md`、`tests/unit.js` | 同左 |

W245 把镜头侧 `dupRowsNote` 的「多花几行」改成 `extra = list.length - picked.length`、补 `extra <= 0` 早退、开头那个数换成 `picked.length`;W246 在 `cli.js` 新增 `nthSubject(proj, sid, nth)` 并在 `subject.generateImage` 的每轮重取上换掉 `findSubject`,序数按快照**全表**同 id 排位数而不在待跑清单上数。

## 二、四棵树机检

### 2.1 merge 一:整棵树 `P1 == B`,零冲突是拓扑退化

`P1` 就是 `B` 本身(叉点即我方 tip,`--no-ff` 把一次本可 fast-forward 的合并按住成了真 merge 提交)。539 个文件全部 `P1 == B`,`P2` 真有改动的 5 个。**这一次的零冲突不是任何判据保住的,是拓扑上没有第二方**;同理,merge 一之后的 live(unit 665 / release 51 / 记账件 260)**恒等于 W245 的自称**,它证明不了 W245 的自称对——第一支合完的数字永远等于第一支自己量的数字。

### 2.2 merge 二:541 个文件,真形态 7 个

| 形态 | 个数 | 文件 |
|---|---|---|
| 四树全同的空壳 | 534 | — |
| 真 `P1 == B`(整份取对侧) | 2 | `cli.js`、`w246-find-subject-first-row.md` |
| 真 `P2 == B`(整份留我方) | 2 | `js/domain.js`、`w245-dup-rows-extra-raw.md` |
| 两侧都动过 | 3 | 根 `README.md`、`docs/skills-wave/README.md`、`tests/unit.js` |
| 其它 | 0 | — |

两个方向的整份取用**一样多**(各 2 个),这是两条槽改动面互补且各自只碰一个产品文件的直接读数。`js/` 下 257 个文件里四树不全同的**只有 `js/domain.js` 一个**;`cli.js` 四树取两个值;`mcp.js`/`server.js`/`billing.js` 四树全同。

反向对账两条都逐字节相同:`P1→tip` == `B→P2`(取对侧那两份)、`P2→tip` == `B→P1`(留我方那两份)——两侧带来的改动一格没丢也一格没串。

### 2.3 `numstat` 三向对账(减数先点名再相减)

| 文件 | `B→P1` | `B→P2` | `B→tip` | 配平 |
|---|---|---|---|---|
| 根 `README.md` | +1/−1 | +1/−1 | +1/−1 | 同一行两侧各改一次,并集仍是一行 |
| `docs/skills-wave/README.md` | +2/−1 | +2/−1 | +3/−1 | 各 1 行索引 + 份数行改写;`1+1+1 = 3` 增、份数行只删一次 |
| `tests/unit.js` | +45/−4 | +62/−2 | +105/−4 | 增 `45+62−2 = 105`、删 `4+2−2 = 4`,**减数是两侧同改的那两个棘轮字面行** |

`tests/unit.js` 那两行共同减数正是下一节的主题。

## 三、名集多重集:`664 + 1 + 1 = 666`

在四棵树各自的全绿快照上跑 `node tests/unit.js`,按 `|` 切出「套件 · 用例名」做多重集:

- `B` 664 条、`P1` 665、`P2` 665、`tip` **666**;tip 名集去重后仍 666 条,**重名 0**
- `P1` 相对 `B`:**+1 −0** — `release · G6 一键处置遇上同 id 多行的分镜表:两行都真跑到,且不许报出"多花了钱"这句假话`
- `P2` 相对 `B`:**+1 −0** — `commands · subject.generateImage 同 id 多位:每一轮的图落到本轮那一位(扣几笔就得有几位到手图,两端同一个结果)`
- 并集期望 666 条,tip 实测 666 条,**缺失 0、多出 0**

即 666 不是算完等于 666,是**哪两条**点得出来。交接文两边都自称 665,合完 live 是 666——两个自称各自在自己那棵树上都是对的,错在它们都看不见对方那一条。

## 四、这一槽最值得抄走的一格:两侧同写同一个数,git 一声不吭,而抄得越齐报警器越少

两个棘轮字面**两侧写出了逐字节相同的值**,git 连冲突面都没把人叫过来:

| 格 | `B` | `P1` | `P2` | git 静默合成 | live |
|---|---|---|---|---|---|
| 单元 `FLOOR` | 664 | **665** | **665** | 665 | **666** |
| 记账件 `FLOOR` | 259 | **260** | **260** | 260 | **261** |

两侧都没做错任何事(各按自己那支的 live 抬了一格),而合完照旧落后一格。八档反事实逐档回读证明变异真落进去了之后跑判据:

| 档 | 改动 | 结果 |
|---|---|---|
| ① | 单元 `FLOOR` 留在 665 | **666/666 全绿**,没有判据接住 |
| ② | 根 README 断言数留在 665(`FLOOR` 已抬到 666) | 红 2:`README 数字对账` + `三套件用例数只增不减` |
| ③ | ①②**同时**留旧 | 红 **1**:只剩 `README 数字对账` |
| ④ | 记账件 `FLOOR` 留在 260 | **666/666 全绿**,没有判据接住 |
| ⑤ | 索引明写份数留在 260 | 红 1:`索引完备性`三方对齐 |
| ⑥ | ④⑤同时留旧 | 红 1,同⑤ |

读出三句:

1. **`FLOOR` 那一层单独失守是静默的**(①④),因为它钉的是「差额 ≤ `SLACK`」而 `SLACK = 3`,落后一格在预算内;真正接住的一律是**明写数字那一层**(②⑤),它钉的是**相等**。
2. **②→③ 少了一条红。** `三套件用例数只增不减` 那条比的是 README 明写的数与 `FLOOR`,只抬 `FLOOR` 而漏改 README 时它会红;**两个数一起留旧,它反而闭嘴了**——把两处旧值抄得越齐整,报警器越少,只剩下那条拿 live 现算的 `README 数字对账`。这是「合入时照抄某一侧」这种改法的固有形状:一致性判据在两侧一致时天然沉默,唯一靠得住的是**现算 live 再比**的那种。
3. 两格棘轮之所以进不了冲突面,靠的是「两侧写了同一个值」;上一槽(W244)靠的是同一行上还有散文把人叫过来。**两个都是偶然**,故本槽五格数字一律按合完 live 现跑重取,不从任何一侧抄。

对照面照旧摆着:⑪⑫ 分别删掉两侧插进根 README 的那段散文、数字仍留 live,**666/666 全绿**——散文一层判据都没有,和 W244 量到的一样。

## 五、冲突三处的解法(禁 `--ours`、禁 `checkout <old> -- .`)

- **根 `README.md` L612**(16939 → 18121 字符的长行,两侧各改一次):先把 `NNN 项断言` 换成占位符抵消两侧共同改动,再把两侧各自还原成「对 `B` 的一处纯插入」——机检确认 W245 插在 offset 8780(长 162)、W246 插在 offset 9149(长 214),**两段不重叠**且 base 侧零字符被改写;两处插入都应用回 `B`,断言数按 live 填 **666**。长度自洽:`17747 + 162 + 214 = 18121`。两处接缝各回读一遍:W245 那段落在 `dupRowsNote` 的镜头段内、W246 那段落在 `dupSubjectRowsNote` 主体段末,各自都在该讲这件事的地方。**这不是取一侧,是量出两侧各自只做了一处插入之后的并集。**
- **`docs/skills-wave/README.md`**:两行索引取并集,按波次号归位成 245、246;份数行两侧同写 260 未进冲突面,按三方(目录实况 / 索引表行数 / 明写份数)现取校到 **261**。
- **`tests/unit.js`**:git 自动合并未报冲突(两侧净增各 1 条、落点相隔很远),两个棘轮字面静默合成后按 live 抬到 **666 / 261**。

## 六、数字全部合完 live

| 项 | `B` | W245 自称 | W246 自称 | **合完 live** |
|---|---|---|---|---|
| `unit` | 664 | 665 | 665 | **666/666 PASS** |
| `unit release` 套件 | 50 | 51 | — | **51/51** |
| `unit contract` 套件 | 145 | 145 | — | **145/145** |
| `unit commands` 套件 | 54 | — | — | **55/55** |
| `integration` | 148 | — | — | **148/148 PASS** |
| `cli.smoke` | 115/117 | — | — | **115/117** |
| 记账件份数 | 259 | 260 | 260 | **262**(含本文) |

**两边七个自称两对五错,而哪两个会对可以事先算出来**:一侧的自称恰等于 live,当且仅当另一侧在那个套件上净增 0——`release` 只有 W245 加了一条故它自称的 51 对,`contract` 两侧净增都是 0 故 145 对;`unit` 与记账件两侧各有净增,四个数**全部低 1 格**。「对」不是核过的结果,是**对侧没动**的结果。

`cli.smoke` 单独整跑 `env -u HUJING_SERVER node tests/cli.smoke.js` 得 115/117,失败两条:

- `未登录 whoami → exit 3`(实际 exit=1)
- `llm --json mock 链路`

同一命令在 `master` `9adcf0f` 上现跑 51/53,失败的是**同样这两条、同名同表现**——本槽没有引入,也没有修好。

## 七、合完的树上两侧各自真跑一遍,并互证不干扰

**W245 侧**(镜头 `dupRowsNote`,合完现取):

- G6 机器派生 `['dup','dup']` 对 2 行 → **一个字不说**(点两条跑两行,一行没多花)
- 用户手打 `['dup']` 对 3 行 → 照旧说「比点名数多花了 **2** 行的钱」并指 `shots-dedupe`
- G4 含定稿的 `['dup','dup','dup']` 真跑 2 行 → `extra = −1` 早退,一句不说

**主体侧同形核对**:`dupSubjectRowsNote` 在同样两档下产出结构逐句对应(机器派生一个字不说、手打报「多花 2 位」),两份派生的 `extra` 算式与早退现在**逐字同形**(`js/domain.js` L324/L325 与 L381/L382),末句仍各说各的修法(镜头指 `shots-dedupe`、主体指回库里删或改 id),没有串用字眼。

**W246 侧**(`cli.js` `nthSubject`):`nthSubject` 定义在 L187、调用在 L1403,序数表在 L1393–L1397 按 `p.subjects` **全表**记;`nthShot` 全树 **0 处**。判据由 `commands` 那条新用例四档钉着(点名 / 整库 / 首位已有图的整库 / 浏览器),合完仍绿。

**互不干扰**:W245 只碰 `js/domain.js`、W246 只碰 `cli.js`,而 `cli.js` 的 `episode.generateVideos` 现取仍在读 `Domain.dupRowsNote`(L1128)——即 W245 改的那份派生真被 W246 那个文件消费着,两条槽在这一点上有真实接触面而两侧用例都绿。

## 八、十档反事实:各红各的

除第四节那六档,产品面四档:

| 档 | 改动 | 结果 |
|---|---|---|
| ⑦ | W245 全退(`extra` 按去重 id 减) | 红 2:`domain · dupRowsNote` + `release · G6 一键处置` |
| ⑧ | W245 **半修**(换了 `extra`、不留早退) | 红 2,同两条,但报文从「点名的 1 镜」变成「点名的 2 镜」——**判据钉的是算术不只是早退** |
| ⑨ | W246 全退(每轮 `findSubject` 取首位) | 红 1:`commands · subject.generateImage 同 id 多位`,报「扣了三笔就得有三位到手图」 |
| ⑩ | W246 序数改在**待跑清单**上数 | 红 1,同一条用例但**红在另一句断言上**:「已有图的首位这一趟一个字不许被改写」 |

⑨⑩ 同红一条用例而报文不同,说明「按序数寻址」与「序数在全表上数」是那条用例里两档独立断言,不是一句话顺带。

## 九、不代修的残留(现跑核过,原样留着)

1. **`cli.js` 的 `findShot` 取首位——与本槽 W246 修掉的那格逐字同形。** 交接明令不代修,现取核对:`findShot`(L174–L178)仍是 `.find(x => x.id === sid)` 首行语义,而 `episode.generateVideos` 的批量循环(L1133–L1137)是 `for (const s of todo)` + 每轮 `withProject` 重取 + `findShot(epLive, s.id)`,**结构与 W246 改前的 `subject.generateImage` 逐句对应**。收法就是 `nthShot` 照抄,两侧唯一差别是镜头侧另有 `shots-dedupe` 这条绕路。全树 `nthShot` 0 处,一个字没碰。
2. **`state-put` 不设闸**:`CMD['state-put']`(L1677)仍只有 `need(f.file, …)` 与 `--force`,整树原样落库是刻意的(L779 注释在册)。没设闸。
3. **选人闸按行/按位筛**:`ids.has(s.id)` 现取 6 处(`cli.js` 4 + `js/commands.js` 2),一个字没动。
4. **`SLACK`**:四棵树全是 3,没碰。

## 十、保留面机检(合完的树上现取)

| 项 | 实况 |
|---|---|
| `Skills.gaps()` | **20** 键,一个没剥 |
| `GUARD_TOPICS` / `GUARD_TOPICS_CLOSED` | **19** / **0**,`TOPIC_FLOOR = 19`,四树相同 |
| `dupSubjectRowsNote` | 22 处,在 |
| `shots-dedupe` | 33 处,在 |
| `floorLagVerdict` | 7 处,在 |
| `blankNonCode`(W243 的修) | 28 处,在 |
| `emptyBatchNote`(四堆按 id) | 38 处,在 |
| `shots-import` 写入闸 | 在,`cli.js` 改动落在 L181 与 L1390 两段,与闸区间不相交 |
| `episode.produce` 提 note / MCP 对账 | 在(`integration` 148/148、`cli.smoke` MCP 段全绿) |
| 本文目录内相对链接 | **0** 处(索引行之外不链别的记账件,不借道散文链接) |

`node --check` 过:`tests/unit.js`、`js/domain.js`、`cli.js`。按约定**未跑** `e2e.js`。
