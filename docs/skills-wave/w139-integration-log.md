# W139 · 两条槽并入集成线的收敛记录(集成分支)

> 集成分支:`cursor/w139-integration-a7f2`,基线 `cursor/w134-integration-135a @ 42b2798`。
> 本文只记**收敛过程**:基线怎么定的、合了哪两条、冲突面长什么样、合并后的实测数字、没删测没摘 gaps 的取证。
> 被合入两槽自己的内容分别在 `w135-g1-receipt-label.md` 与 `w136-sk25-owe-guard.md`,本文不代述、不复写、不改它们正文里的任何时点读数。
> 两次合并都 `--no-ff`、各一个合并提交、可 revert;全程只解冲突与收敛数字,**不重做已落地的功能、不改别人的判据**。

## 1. 结果一句话

`--no-ff` 合入两条,按序、**各取被合入支的实际 HEAD**:

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w135-g1-receipt-label-0fc3@f9ac1bb` | `c485c73` | 发布门 G1 未过门回执的两处失真:推荐动作按 `action` 存却按 `b.label` 读故恒印 `undefined`(改存读同名,两端此后逐字同形);`gates()` 漏注入 `opts.Domain` 时 G1 被自己的 `catch` 降成 warn、`fails` 1→0,改成开头显式判一次注入按 fail 记并点名缺什么 | `js/release-core.js` 10 行 | +2 条(`release` 套件) |
| `w136-sk25-owe-guard-789e@61380b5` | `ccbc12d` | 给 SK-25 的「仍欠(G-03)」段补双向护栏:前半钉段与段里两个余面的锚点、已交账那半不许挤回欠账段、`gaps` 与关联索引不摘键;后半反向逐处对照源码钉住点名的余量此刻确实还在(`maxRetry` 按名字大小写不论与按 1-5 钳位形状两向、两端闭环循环嵌套方向) | **零产品代码**(`js/skills.js` 一个字未动) | +1 条(`contract` 套件) |

合并后回归 **`unit 495/495`**、**`integration 130/130`**、**`cli.smoke 100/102`**(2 项失败与 `master` 同名同表现,见 5.2),`contract` 子套件 **108/108**。

治理面零变动:注册表 41 条、`Skills.CHECKS` 17 条、`preflightStages()` 七面、`gaps()` 20 键(`G-03` 仍是两条值)、`KB.SECTIONS` 18 条、`playbooks()` 5 条、短名单 30——**七个数一个没动**,而且这次是**结构性成立**而不是靠现跑对出来的:五个注册表文件(`js/skills.js`/`js/prompts.js`/`js/knowledge.js`/`js/cmd-registry.js`/`js/experts-data.js`)相对基线**零 diff**(见 5.4)。

**本槽最值得记下的三件事:**

1. **「只有一侧在 diff 里」这一形态第一次被量出它靠的是运气,而不是 `git` 判得对。**
   第一次合并里主 `README.md` 我方一字未动(基线那两个提交只改了一个 `FLOOR` 字面),
   故 `git` 把 W135 那一行整句**原样搬进来、不请你过目**,包括它自己算的「单元测试(494 项断言」。
   这一次它是对的——W135 叉在 `0dbd69e`,而我方走出的两个提交(W130 审计合入 + W134 记账)**一条用例都没加**,
   故 492 + 2 恰好等于合并后 live 494。**只要我方那两个提交里带过一条用例,这一处就会静默错**,
   而它落在"只有一侧改"的那类里,连冲突块都不会有(见 3.1、4.1)。
   前几槽把这一形态归进"三种数字形态"里当良性,本槽把它读准:**它是三种里唯一一种连 `git` 都不提醒的**,
   良性与否只取决于"我方这一侧在同一个数上有没有动静",与形态本身无关。

2. **W132 那条差额上限本槽两侧父值全部放过,而其中一侧恰好卡在 SLACK 的边界上。**
   合并后记账件 live 151,两次合并带来的 `FLOOR` 父值分别是 149(基线)、148(两支各自算的)、150(第一次合并后)。
   照 `live - floor <= SLACK(3)` 逐格实测:150 差 1 全绿、149 差 2 全绿、**148 差 3 全绿**、147 差 4 **当场红**(变异 M4)。
   即**抄任一父值在本槽都不会响,而 148 是差一格就要响的那个值**。
   单元测试那格同理:live 495,两个父值 494 / 493 差 1 / 2 全绿,连基线的 492 差 3 也全绿。
   W134 记的「接住一侧、放过另一侧」本槽退回"两侧都放过"——**判据响不响只由 `live − 父值` 与 SLACK 的大小关系决定,与叉点、与抄没抄都无关**,
   本槽是第一次量到它恰好停在边界值上(差 3 = SLACK 3),故这条判据在 `FLOOR` 上仍旧**只是偶尔响的人工纪律**(见 4.2)。

3. **W136 的护栏在基线上确实是零覆盖,这一点本槽机检过而不是转述。**
   同一手变异(把 SK-25 的「仍欠(G-03)」三行整段从 `js/skills.js` 删掉)在基线 `42b2798` 上跑 **492/492 全绿**,
   在合并后 HEAD 上**当场红 1 条**且报错就是那句「note 须仍有『仍欠(G-03)』段」(变异 M3)。
   W130 审计报的那一处、W136 补的那一条,两端读数在本槽的两棵树上各自复现过一次(见 5.5)。

## 2. 基线与这两个槽

### 2.1 基线

任务口径给定 `origin/cursor/w134-integration-135a` 的 HEAD,参考值 `42b2798`。现取核实:

```
$ git rev-parse origin/cursor/w134-integration-135a
42b2798...
$ git rev-parse origin/cursor/w135-g1-receipt-label-0fc3
f9ac1bb...
$ git rev-parse origin/cursor/w136-sk25-owe-guard-789e
61380b5...
```

三个都与任务给的参考值一致(两条功能支的自称数分别是 unit 494 与 unit 493,见 2.2)。基线上的四套件数字(开工时现跑,不引用 `w134-integration-log.md` 里的数):

| 套件 | 基线 `42b2798` |
|---|---|
| `node tests/unit.js` | **492 / 492**(`contract` **107 / 107**) |
| `node tests/integration.js` | **130 / 130** |
| `node tests/cli.smoke.js` | **100 / 102** |

与任务给的参考数字逐个相符。

### 2.2 两支的叉点:对基线是真三方

两支的 `merge-base` 都是 `0dbd69e`(即 `w133` 的 tip),而基线 `42b2798` 在其之上还有两个提交(`a7033bc` 合入 W130 周期 9 审计、`42b2798` 落 W134 记账件)。故:

- **两次合并都是真三方合并**,`merge-base != HEAD`,两次都进冲突块;
- 两支彼此的 `merge-base` 也是 `0dbd69e`,即**它们互相看不见对方**,各自把明写份数从 147 抬到 148、各自把单元数从 492 抬到自己那一侧的值;
- 自称数与合并后 live 的关系:W135 自称 unit 494 = 492 + 2(它叉点上的 492 恰等于基线的 492,巧合见 1.1);W136 自称 unit 493 = 492 + 1、`contract` 108 = 107 + 1。合并后 live 是 **495 / 108**,**两个自称数都不是答案**。

### 2.3 不合的两条

远端另有两支在飞,按任务口径**不合**,现取核实其叉点与内容:

| 分支 | tip | 叉点 | 内容 |
|---|---|---|---|
| `cursor/w137-gate-catch-9a2f` | `3a4e458` | `0dbd69e` | G4/G5/G6 聚合段的空 `catch` 不再吞掉失手:缺注入记 fail、遍历抛错记 warn |
| `cursor/w138-issues-empty-ca5b` | `bff8f5d` | `0dbd69e` | 无分集时问题中心也收得出前置断点:剧本/主体/分集三步的阻塞码收进 `Domain.gateBlockers` 双端单源 |

两支都叉自更旧的 `w133`,对本槽 tip 同样会是真三方。**W137 正是 W135 在自己记账件里如实登记的"未动的一面"**(G4/G5/G6 与 G1 同类的注入失真),故下一槽合它时那两条会在同一个文件同一段落上撞车,值得先知道。

## 3. 冲突面

两次合并共 **5 个文件次、8 个冲突块**,全部落在文档数字与索引表上,`js/` 一个冲突块都没有。

### 3.1 第一次合并(W135):2 文件 3 块

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `docs/skills-wave/README.md` | 明写份数 | 149(我方,含 W130+W134 两份) / 148(对侧,含 W135 一份) | 现取合并后 live **150** |
| `docs/skills-wave/README.md` | 索引表尾 | `w134` 行 / `w135` 行 | **并集**,按波次号排 |
| `tests/unit.js` | 记账件 `FLOOR` | 149 / 148 | 现取 live **150** |

**没进冲突块但改了的**:主 `README.md` 的「单元测试(N 项断言」整句、`tests/unit.js` 里三套件下限表的单元测试那一格,两处都是"我方零改动"故 `git` 直接取了对侧值 494。这一次它对(见 1.1),但**它对不对与 `git` 无关**——本槽事后按 live 复核过一遍才敢留着。

### 3.2 第二次合并(W136):3 文件 5 块

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `README.md` | 「单元测试(N 项断言」整句 | 495 那侧带着 W135 插进去的一句 / 493 那侧是叉点原句 | 见下 |
| `docs/skills-wave/README.md` | 明写份数 | 150 / 148 | 现取 live **151** |
| `docs/skills-wave/README.md` | 索引表尾 | `w134`+`w135` 两行 / `w136` 一行 | **并集**,三行按波次号排 |
| `tests/unit.js` | 单元测试下限 | 494 / 493 | 现取 live **495** |
| `tests/unit.js` | 记账件 `FLOOR` | 150 / 148 | 现取 live **151** |

主 `README.md` 那一块**不是数字二选一**:两侧长度差 76 字,机器比对(逐 opcode)出来只有两处差异——一处是那个数(494 / 493),另一处是 W135 **零删除地插进去的一句**(「G1 未过门回执逐集点名推荐动作(与浏览器那半逐字同形,不印 `undefined`)且漏注入 Domain 时 G1 如实 fail 不降 warn」)。按 W133 立的分辨法「差异里有没有删除」,这是**纯插入**不是口径之争,故处置是**取我方整句、只把数字换成 live 495**,W135 那句一个字不删。

## 4. 数字怎么定的

### 4.1 一律现取合并后 live,不抄任何一个父值

| 数 | 基线 | W135 侧 | W136 侧 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|
| 单元用例数 | 492 | 494 | 493 | **495** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 107 | 107 | 108 | **108** | 主 `README.md` 契约段(一侧未动故未撞车) |
| 记账件份数 | 149 | 148 | 148 | **151**(含本文后 152) | `docs/skills-wave/README.md` 明写 |
| 记账件 `FLOOR` | 149 | 148 | 148 | **151**(含本文后 152) | `tests/unit.js` |
| `integration` | 130 | 130 | 130 | **130** | 未动 |
| `cli.smoke` | 102 | 102 | 102 | **102**(实跑 100/102) | 未动 |

`contract` 那格值得单记一句:W135 没加契约用例故那一侧是 107、我方也是 107,**只有 W136 一侧在动**,于是走的是 3.1 那条"只有一侧在 diff 里"的路——`git` 直接取 108 且不请你过目,而 108 恰好等于合并后 live。**同一个静默窗口本槽出现了两次(主 README 单元数、契约段条数),两次都恰好对**,这不是判据在护着,是两支各自加测的位置正好不重叠。

### 4.2 `FLOOR` 抄父值:本槽四格实测,两侧父值全部放过

在合并后 HEAD 上把记账件 `FLOOR` 逐格改小重跑全套:

| `FLOOR` | 与 live 151 的差 | 全套结果 |
|---|---|---|
| 150(第一次合并后我方值) | 1 | 495/495 全绿 |
| 149(基线值) | 2 | 495/495 全绿 |
| **148**(两支各自带来的值) | **3** | **495/495 全绿**(恰好等于 SLACK) |
| 147(`w133` 叉点值) | 4 | **红 1 条**(差额上限,报「下限 147 落后实测 151 共 4 格」) |

单元测试那格同理:494 差 1、493 差 2、492 差 3 三个值全绿。

故本槽的读数是:**抄任一父值都不会被逮住,而 148 是差一格就要响的那个值**。W134 记的「接住一侧、放过另一侧」在本槽不成立,原因不是叉点也不是"抄没抄",而是 `live − 父值` 这次最大只到 3,恰好压在 SLACK 上。三槽连起来读:W133 差 1(两侧都放过)、W134 差 2 / 差 4(放过一侧、逮住一侧)、W139 差 1 / 差 3(两侧都放过,其中一侧压线)。**这条判据是一个与叉点距离相关的概率事件,不是纪律**。

### 4.3 README 那三处抄父值必被逮住

同样在合并后 HEAD 上试抄(与 4.2 对照):

- 主 `README.md` 的单元数改成 494 或 493 → **各红 2 条**(精确对账那条 + 三套件下限那条,报错句不同、含义不混);
- `docs/skills-wave/README.md` 明写份数改成 150 或 148 → **各红 1 条**(三方对齐那条,点名"目录实况 151 与明写数不符")。

即 W133 定下的那句分类在本槽复现:**README 那几处抄父分支必被逮住,两个 `FLOOR` 抄了(在 SLACK 内时)全绿**。

## 5. 取证

### 5.1 合并纪律:按四棵树的文件哈希机检

对每次合并,取 `M`(合并结果)、`P1`(我方)、`P2`(对侧)、`B`(叉点)四棵树里同一文件的 blob 哈希做对照:

| 合并 | 文件 | 结果 |
|---|---|---|
| `c485c73` | `docs/skills-wave/README.md` | 四者互不相等 → **真融合** |
| `c485c73` | `tests/unit.js` | 四者互不相等 → **真融合** |
| `ccbc12d` | `README.md` | 四者互不相等 → **真融合** |
| `ccbc12d` | `docs/skills-wave/README.md` | 四者互不相等 → **真融合** |
| `ccbc12d` | `tests/unit.js` | 四者互不相等 → **真融合** |

**两侧都改过的文件**逐个查完:整份取 `--ours` 零命中、整份取 `--theirs` 零命中、回退到叉点内容零命中。
只有一侧改过的文件(`c485c73` 的 `README.md`/`js/release-core.js`、`ccbc12d` 的 `js/release-core.js`)结果等于那一侧,是单边改动的必然结果、不是取整份——这一类正是 1.1 记的那个静默窗口,故本槽对其中带数字的那两处另按 live 复核过(4.1)。

### 5.2 四套件实跑

| 套件 | 基线 `42b2798` | 合入 W135 后 | 合入 W136 后(本槽 tip) |
|---|---|---|---|
| `node tests/unit.js` | 492 / 492 | 494 / 494 | **495 / 495** |
| └ `contract` 子套件 | 107 / 107 | 107 / 107 | **108 / 108** |
| `node tests/integration.js` | 130 / 130 | — | **130 / 130** |
| `node tests/cli.smoke.js` | 100 / 102 | — | **100 / 102** |

`cli.smoke` 那 2 项失败逐条现取,与 `master` 同名同表现:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

单元用例的**不同名字数**现取 495,与用例数 495 相等——**连续第十槽全部相等**。

### 5.3 没删测:按名成集双向比对

在基线与合并后 HEAD 上各跑一遍全套,把用例名成集比对:

- **只在基线里有的:0 条**(即一条用例都没被删掉、没被改名);
- 只在合并后 HEAD 里有的:**恰好 3 条**,逐条点名:
  - `release · release-core · G1 未过门回执:逐集点名推荐动作(不印 undefined),与浏览器那半逐字同形`(W135)
  - `release · release-core · gates 漏注入 Domain:G1 如实 fail 且计进 fails(判不出来不得在回执上记成零未过门)`(W135)
  - `contract · SK-25 仍欠段护栏(G-03 余面):段在不在与段里点名的两处余量对照源码,抹段或写成已清都红`(W136)

492 + 3 = 495,与实跑总数对得上。

### 5.4 没摘 gaps:这次是零 diff 而不是现跑对数

五个注册表/知识层文件相对基线**一个都没进 diff**:

```
$ git diff --name-only 42b2798 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js
(空)
```

故七个数不用现跑也成立;为求稳仍在合并后 HEAD 上现取核对了一遍:注册表 41、短名单 30、`CHECKS` 17、面表 7、`gaps()` 20 键、`KB.SECTIONS` 18、`playbooks()` 5。整槽 `js/` 只有 `js/release-core.js` 进了 diff(10 行,全是 W135 的两处)。

### 5.5 抽查变异:七组

全部在**合并后 HEAD** 上跑,一次只改一处,跑完即还原。

| # | 变异 | 期望 | 实测 |
|---|---|---|---|
| M1 | 把 `js/release-core.js` 里 G1 blocker 的键名改回 `action`(退回 W135 修的第一处失真) | 红 1 | 红 1,报「期望 `第1集(blocked:编写剧本)…`,实际 `第1集(blocked)…`」 |
| M2 | 摘掉 W135 加的 `Domain` 必注入判定,退回让 `catch` 兜成 warn | 红 1 | 红 1,报「缺 Domain 注入不得降成 warn:期望 fail,实际 warn」 |
| M3 | 把 SK-25 的「仍欠(G-03)」三行整段从 `js/skills.js` 删掉 | 红 1 | 红 1,报「note 须仍有『仍欠(G-03)』段」;**同一手在基线 `42b2798` 上 492/492 全绿** |
| M4 | 记账件 `FLOOR` 逐格抄父值 150 / 149 / 148 / 147 | 前三全绿、末一红 | 与期望一致(见 4.2) |
| M5 | 单元测试下限逐格抄父值 494 / 493 / 492 | 全绿 | 全绿(差 1 / 2 / 3 都在 SLACK 内) |
| M6 | 主 `README.md` 单元数抄父值 494 / 493;`docs` 明写份数抄父值 150 / 148 | 各红 | 单元数各红 2 条、份数各红 1 条(见 4.3) |
| M7 | 索引表只摘 `w135` 那一行(份数与文件都不动) | 红 | 红 3 条:双向对齐、**按份点名**(「`w135-g1-receipt-label.md` 在目录里但索引表没有它那一行」)、行数三方对齐 |

另加一条对"要保的东西"的反向抽查:把 `Domain.reviseShotIds` 从走 `reviseTargets` 派生改回在调用侧自筛 `score < 7`(即退回 W131 之前的形态)→ **红 2 条**,报错分别落在"重抽面 ∩ 分镜表实位"与"报告判旧一律回空",两条含义不混。W132 的 SLACK 字面(`const SLACK = 3;`)与 W131 的派生口径在本槽 tip 上**都还在、都还有判据护着**。

M7 那条另外确认了索引三行是**并集**:摘掉任一行都按份点名报出,不靠两个集合相等互相抵消。

### 5.6 本槽自己改了什么

除两个合并提交解冲突外,本文这一提交动了三处:新增本文、`docs/skills-wave/README.md` 补索引行并把明写份数 151 → 152、`tests/unit.js` 的记账件 `FLOOR` 151 → 152。单元用例数与 `contract` 条数**本文一条不加**,故 495 / 108 不动。

## 6. 残留与欠账

1. **`FLOOR` 抄父值在本槽两侧全绿,其中一侧压在 SLACK 边界上。** 4.2 已实测。这条判据能不能逮住"抄父分支"取决于 `live − 父值` 越没越过 3,与叉点、与主观意图都无关。三槽读数连起来看,它响的次数少于不响的次数,**"禁止抄父分支"在这两个字面上仍是纯人工纪律**。
2. **「只有一侧在 diff 里」是三种数字形态里唯一连 `git` 都不提醒的一种。** 本槽两次踩上(主 README 单元数、契约段条数),两次都恰好对,而对的原因是两支加测位置不重叠、我方那两个提交零加测——**换个基线就会静默错**。今天没有任何判据在这一路上,合并后按 live 复核是唯一的守卫。
3. **G4/G5/G6 那半仍欠。** W135 自己如实登记过:聚合那段的 `catch (_) {}` 在漏注入时静静报 0 镜全 pass,是与 G1 同类的失真另一面。本槽未动,而 `cursor/w137-gate-catch-9a2f` 正是在收它——下一槽合它时会与 W135 改过的同一段落撞车,值得先知道(2.3)。
4. **SK-25 的两个余面仍欠。** W136 补的是**护栏**不是收编:收敛次数(`maxRetry`)至今没有登记口径、两端闭环形态仍不同构。护栏的作用是"哪天真收编了、记账不跟着改就红",本槽实测它确实红得出来(M3),但它不催任何人去收编。
5. **索引表行序仍无断言。** 本槽把三行按波次号排回 `w133` 之后,是人工排的第四次。判据只管"每份各有一行",不管行在哪。
6. **远端两支在飞未合**:`w137`(gate catch)、`w138`(issues empty),按任务口径不合。故本刻"未合入数"是 **2**,这个 2 是"任务明确要求留着"而不是"还没到"。
