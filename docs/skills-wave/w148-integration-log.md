# W148 · 两条门槛收口槽并入集成线的收敛记录(集成分支)

> 集成分支:`cursor/w148-integration-9f3a`,基线 `cursor/w146-integration-dbd5 @ 25aa1c0`。
> 本文只记**收敛过程**:基线怎么定的、合了哪两条、冲突面长什么样、合并后的实测数字、没删测没摘 gaps 的取证。
> 被合入两槽自己的内容分别在 `w145-browser-gate-catch.md`、`w147-plans-gates.md`,本文不代述、不复写、不改它们正文里的任何时点读数。
> 两次合并都 `--no-ff`、各一个合并提交、可 revert;全程只解冲突与收敛数字,**不重做已落地的功能**,也**没有改到任何一条别人的判据**。

## 1. 结果一句话

`--no-ff` 合入两条,按序、**各取被合入支的实际 HEAD**:

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w145-browser-gate-catch-5c11@c0ecdfb` | `81c6607` | 浏览器发布门 G4/G5/G6 那段 `catch (_) {}`(`js/release.js`):三门的镜次计数出自同一次遍历,遍历没跑成时 `agg` 停在初值 0,三门照常印「0 镜 → 通过」。改成把"判不出来"从初值里分出来,缺模块与遍历抛错各点名一句、三门写在同一分支里同记 `warn`、判不出来不挂 `fix` | `js/release.js` +21 −11 | +2 条(`release`) |
| `w147-plans-gates-3ae7@dca7227` | `b162dab` | 计划层项目级前置三步的待办判定(`js/plans.js`)是全仓第三份门槛拷贝,剧本那半与 `Domain.gateBlockers` 不同口径(`String(p.script\|\|'').trim()` vs `p.script \|\| p.extractDone`),提取过主体但没存整本原文的老项目整份计划推不出来。改成 `gateMap` 一处收 `gateBlockers`、三个取材器按码取材、码 → 步骤文案的映射显式留在计划层 | `js/plans.js` +22 −8 | +3 条(`plans` 2 + `contract` 1) |

合并后回归 **`unit 509/509`**、**`integration 130/130`**、**`cli.smoke 100/102`**(2 项失败与 `master` 同名同表现,见 5.2),`contract` 子套件 **113/113**。

治理面零变动:提示词注册表 41 条、能力短名单 30 条、`Skills.CHECKS` 17 条、`preflightStages()` 七面、`gaps()` 20 键(`G-03` 仍是两条值)、`KB.SECTIONS` 18 条、`playbooks()` 5 条——七个数一个没动,且**五个注册表文件相对基线零 diff**(5.4)。

**本槽最值得记下的三件事:**

1. **W146 残留 9 对 W145 的两句预告,一句对一句错,错的那句错在"判据在不在同一个文件里"。**
   预告写的是「大概率与 `js/release.js` 撞不上、而与 W137 落地的那两条用例撞得上」。
   前半对:`js/release.js` 与 `js/release-core.js` 是两个文件,W137 那半从头到尾没进本次 diff,**零冲突块**。
   后半**错**:W137 的两条用例整条落在 `release-core` 那半、用的是直调 `ReleaseCore.gates` 的夹具,
   W145 的两条落在浏览器那半、用的是 `loadRelease()` 沙箱夹具,**两两之间没有一个共享的计数器**,合完首跑就是 509/509。
   与 W142 那次"代码合得上、判据合不上"的分界由此量清楚:**撞不撞不看改的是不是同一件事,看有没有一条断言同时读两边的产出**。
   W135 那条 `assertEq(bare.fails, withD.fails)` 之所以红,是因为它把**全部门**的 fail 数聚成一个数;
   W137 与 W145 各自只断言自己那三门的 `status`,故同一件事的两端可以互不知情地并存。

2. **本槽是本线自 W142 以来第一次两条槽都带产品代码,而"两侧同值"这个静默窗口在两次合并里各出现一次、成因相反。**
   第一次合并:契约段条数**对侧一字未改**(叉点 108、我方 112),`git` 连冲突块都不给、直接取我方 112,而 112 恰是对的——
   但这次它对**不是运气**:W145 加的两条用例落在 `release` 套件,`contract` 条数**结构上就不会变**,
   与 W139 那次"两个提交一条用例都没加故 492+2 恰等于 live"是两回事。
   第二次合并:`tests/unit.js` 的单元下限**对侧写的 504 与基线的 504 逐字相同**(W147 自称数恰好撞上基线 live),
   但它进了冲突块——因为我方那一格已被第一次合并抬到 506。故同值窗口这次没能静默。

3. **两条槽收的是同一类病的两个位置,合完之后这类病在主仓里剩几处可以点着数了。**
   W145 收的是"判不出来被当成 0 照常下结论",W147 收的是"同一件事的第 N 份判据口径分岔"。
   合并后现取:门槛判定的消费方是**三个**(流程条 `Domain.workflow`、问题中心 `Issues.gates`、计划层 `Plans.TODO_OF`),
   三份投影表形状各不相同而判据只剩 `Domain.gateBlockers` 一份;
   聚合空 `catch` 两端都已收(headless `release-core` 归 W137、浏览器 `release.js` 归 W145),
   而 W145 自己如实登记的两处仍开着(`buildReleaseZip` 抓分镜文件那处、底部 `Bus.on('*')` 那处),本槽未动(6.3)。

## 2. 基线与这两个槽

### 2.1 基线

任务口径给定 `origin/cursor/w146-integration-dbd5` 的 HEAD,参考值 `25aa1c0`。现取核实:

```
$ git rev-parse origin/cursor/w146-integration-dbd5
25aa1c008d3cddb42b6832c2266649c3aa7b2ba0
$ git rev-parse origin/cursor/w145-browser-gate-catch-5c11
c0ecdfba9a69f21f73288acaa0e39cb208294040
$ git rev-parse origin/cursor/w147-plans-gates-3ae7
dca7227eb529cd76715a77d08daf50a8e998fba4
```

三个都与任务给的参考值一致。基线上的三套件数字(开工时现跑,不引用 `w146-integration-log.md` 里的数):

| 套件 | 基线 `25aa1c0` |
|---|---|
| `node tests/unit.js` | **504 / 504**(`contract` **112 / 112**) |
| `node tests/integration.js` | **130 / 130** |
| `node tests/cli.smoke.js` | **100 / 102** |

与任务给的参考数字逐个相符。

### 2.2 两支的叉点:两条都是真三方,落后程度差一倍

| 支 | 与基线的 `merge-base` | 那是什么 | 基线在其之上还有 |
|---|---|---|---|
| `w145` | `2851d97` | W139 集成记账件 | **14 个提交**(W137/W138 两次合并、W140/W141/W144 三条槽与它们的合并、W142 与 W146 两份记账件) |
| `w147` | `d877051` | W142 集成记账件 | **7 个提交**(W140/W141/W144 三条槽与它们的合并、W146 记账件) |

两次都 `merge-base != HEAD`,**都是真三方合并**,两次都进冲突块。另外:

- 两支彼此的 `merge-base` 也是 `2851d97`,即**它们互相看不见对方**;
- `w145` 看不见 W137/W138(那正是它那半的另一端与它按码取材的上游),`w147` 看得见 W138 故它引用的 `Domain.gateBlockers` 在它自己的叉点上就已经在了。

自称数与合并后 live 的关系:`w145` 自称 unit 497、`w147` 自称 504。合并后 live 是 **509**,**两个自称数都不是答案**;
其中 `w147` 那个 504 **与基线 live 逐字相同**(它叉得晚,叉点之后基线只加了 3 条),这是本线第五次踩上"两侧同值"这个窗口,详见 1.2 与 4.1。

### 2.3 不合的一条

远端另有一支在飞,按任务口径**不合**,现取核实它确实在:

```
8c657bf  refs/heads/cursor/w143-evolve-headless-b087
```

本槽未取其内容、未做叉点核实,只登记"任务明确要求留着(仍在飞)"。故本刻"未合入数"是 **1**,这个 1 是任务口径而不是"还没到"。

## 3. 冲突面

两次合并共 **6 个文件次、11 个冲突块**,全部落在文档数字、索引表与两处 `FLOOR` 字面上。
**两个产品文件(`js/release.js`、`js/plans.js`)零冲突块**,五条新增用例**各自零冲突块插入**。

### 3.1 第一次合并(W145 `c0ecdfb` → `81c6607`)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `README.md` | 「单元测试(N 项断言」整句 | **两侧都带内容**:我方带 W138 对问题中心那段的改写、对侧带 W145 零删除插进去的 G4/G5/G6 那句 | **并集**,数字换 live **506**(见下) |
| `docs/skills-wave/README.md` | 明写份数 | 159 / 153 | 现取 live **160** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方 `w140`/`w141`/`w142`/`w144`/`w146` 五行 / 对侧 `w145` 一行 | **并集**,六行按波次号排 |
| `tests/unit.js` | 单元测试下限 | 504 / 497 | 现取 live **506** |
| `tests/unit.js` | 记账件 `FLOOR` | 159 / 153 | 现取 live **160** |

主 `README.md` 那一块**两侧形态不同**,故按 W133 立的分辨法逐侧判一次(按标点切段后做段级 diff,不靠人眼):

- 我方 vs 叉点:两处差异——数字 495 → 504,以及 W138 把「已审集与未拆镜集都不误报**,空项目零条**)」改写成「…都不误报)/空项目·只有剧本·只有主体三态各收出前置断点(…)」,**含删除**(「,空项目零条」六个字没了),属口径之争;
- 对侧 vs 叉点:两处差异——数字 495 → 497,以及在「脏项目带 blockers 角标和失败标记;」之后**零删除**插入 G4/G5/G6 那一句。

二选一会丢掉一整段,故处置是**并集**:取我方整句(W138 那次口径之争的结果一字不动)、按对侧那段插入在叉点原句里的锚点把它插回去、数字换 live 506。
核实方式:结果对我方整句做段级 diff,只差数字与新插的那一句;结果对对侧整句做段级 diff,只差数字与 W138 那处改写——**W145 侧一个字没丢**。

**契约段条数那一行本次没有进冲突块**:叉点 108、对侧 108(W145 一字未改)、我方 112,`git` 直接取我方。
这次取对了,而且**不是运气**:W145 的两条用例落在 `release` 套件,`contract` 条数结构上就不会因它而变(1.2)。

### 3.2 第二次合并(W147 `dca7227` → `b162dab`)

| 文件 | 块 | 两侧 | 处置 |
|---|---|---|---|
| `README.md` | 契约段整句 | 我方带 W141/W144 两段散文 / **对侧只改了数**(摘掉数字后与叉点逐字相等) | 取我方整句、数字换 live **113** |
| `README.md` | 「单元测试(N 项断言」整句 | **两侧都是纯插入**:我方带 W138/W145 两句、对侧带计划层门槛收口那句 | **并集**,数字换 live **509** |
| `docs/skills-wave/README.md` | 明写份数 | 160 / 156 | 现取 live **161** |
| `docs/skills-wave/README.md` | 索引表尾 | 我方 `w144`/`w145`/`w146` 三行 / 对侧 `w147` 一行 | **并集**,四行按波次号排 |
| `tests/unit.js` | 单元测试下限 | 506 / **504** | 现取 live **509** |
| `tests/unit.js` | 记账件 `FLOOR` | 160 / 156 | 现取 live **161** |

契约段那一块的机检结果:把对侧那句按标点切段与叉点比,**只有 1 段不同**(就是那个数),即它是"叉点原句 + 换个数";
我方那一侧带着 W141 的条件块白名单与 W144 的行序判据两段散文。故取我方整句、只换数字——与 3.1 那处方向相反,因为这次带内容的那一侧是我方。

单元那句是本线第二次**两侧都是纯插入**(上一次是 W146 的契约段)。对侧那段插入的锚点在「项目级前置按投影步序出(提取主体→主体生图→拆集)」之后,
机检取法:把那段从对侧整句里摘掉,剩下的与叉点**除数字外逐字相等**(即它确是零删除的纯插入),再按同一锚点插回我方整句。

**一处要如实登记的操作失误**:第二次合并时先按"取我方整句只换数字"处理了单元那句,随后的机检发现对侧那句里另有一段纯插入被丢掉,当场补插回去并复核双向。
故这一块的正确处置是并集而不是二选一,**判法没变,变的是我先按 3.1 的经验假定了对侧只改数字而没有先机检**——
本槽给下一槽的操作口径是:**每一块都先机检两侧相对叉点的差异形态,再决定处置,不靠上一块的形态外推**。

### 3.3 两次都没有改到任何一条别人的判据

两条槽各改一个此前无人改过的产品文件、各加自己的用例,既有用例一条未动(5.3 的名集比对里"只在基线里有的 0 条"同时也说明没有改名)。
W146 残留 9 预告的那处"与 W137 落地的两条用例撞车"**实测零处**,成因见 1.1。
两次合并全程未用 `checkout --ours/--theirs`,也未用 `git checkout <old> -- .`。

## 4. 数字怎么定的

### 4.1 一律现取合并后 live,不抄任何一个父值

| 数 | 基线 | W145 侧 | W147 侧 | 合并后 live | 落在哪 |
|---|---|---|---|---|---|
| 单元用例数 | 504 | 497 | **504** | **509** | 主 `README.md` 明写 + `tests/unit.js` 下限表 |
| `contract` 条数 | 112 | 108 | 110 | **113** | 主 `README.md` 契约段 |
| 记账件份数 | 159 | 153 | 156 | **161**(含本文后 162) | `docs/skills-wave/README.md` 明写 |
| 记账件 `FLOOR` | 159 | 153 | 156 | **161**(含本文后 162) | `tests/unit.js` |
| `integration` | 130 | 130 | 130 | **130** | 未动 |
| `cli.smoke` | 102 | 102 | 102 | **102**(实跑 100/102) | 未动 |

逐次落定的读数(每次合完当场跑,不等两次合完再算):

| | 合入 W145 后 | 合入 W147 后 |
|---|---|---|
| `unit` | 506 / 506 | **509 / 509** |
| `contract` | 112 / 112 | **113 / 113** |
| 记账件份数 | 160 | **161** |

`w147` 那个 504 与基线的 504 逐字相同,是本线第五次遇上"两侧同值"窗口;这次它没能静默,因为我方那一格已被第一次合并抬到 506(1.2)。

### 4.2 `FLOOR` 抄父值:两格各放过一个、各逮住两个

在**本文落地后的 tip** 上把两个 `FLOOR` 逐格改小重跑全套(`SLACK` 现取仍是 3):

| 格 | 值 | 与 live 的差 | 全套结果 |
|---|---|---|---|
| 单元测试(live 509) | 508 / 507 / 506 | 1 / 2 / 3 | 全绿 |
| 单元测试 | **504**(基线父值,同时也是 W147 父值) | **5** | **红 1 条** |
| 单元测试 | **497**(W145 父值) | **12** | **红 1 条** |
| 记账件份数(live 162) | 161 / 160 / **159**(基线父值) | 1 / 2 / **3**(恰等于 SLACK) | 全绿 |
| 记账件份数 | 158 | 4 | **红 1 条** |
| 记账件份数 | **156**(W147 父值) | **6** | **红 1 条** |
| 记账件份数 | **153**(W145 父值) | **9** | **红 1 条** |

即:**两格的形状与 W146 那槽一致**——被合入支的父值全部逮住;不同的是**单元那格这次连基线父值也逮住了**(差 5 > SLACK 3),
而记账件那格的基线父值仍恰好压在边界上(差 3)被放过。
六槽连起来读:W133 差 1(全放过)、W134 差 2 / 差 4(放过一、逮住一)、W139 差 1 / 差 3(全放过,一侧压线)、
W142 差 2 / 差 4–6(单元三个父值全逮住)、W146 差 3 压线放过 + 差 5–11 全逮住、**W148 单元差 5–12 全逮住 + 记账件差 3 压线放过**。
本槽给出的新读数是:**"抄基线自己那个父值"会不会响,只由本槽加了几条决定**——本槽加 5 条用例故单元那格逮得住,只加 1 份记账件(本文)故记账件那格照旧放过。

### 4.3 README 那三处抄父值必被逮住(逐处实测)

同样在本文落地后的 tip 上试抄:

- 主 `README.md` 单元数改成 504 / 497 → **各红 2 条**(精确对账那条报「实测 509,文档 N」+ 三套件下限那条报「README 写的 N 低于下限」,报错句不同、含义不混);
- 主 `README.md` 契约段条数改成 112(基线父值)/ 110(W147 父值)/ 108(W145 父值)→ **各红 1 条**;
- `docs/skills-wave/README.md` 明写份数改成 159 / 156 / 153 → **各红 1 条**(三方对齐那条)。

### 4.4 索引表:并集与行序两向都实测

| # | 变异 | 期望 | 实测 |
|---|---|---|---|
| M1 | 只摘 `w145` 那行索引(文件与份数都不动) | 红 | **红 4 条**:双向对齐、按份点名(点名 `w145-browser-gate-catch.md`)、行数三方对齐,外加 W144 自带的取数口守卫 |
| M2 | 只摘 `w147` / 本文那行 | 同上 | 各红 4 条,分别点名各自的文件 |
| M3 | 把 `w130` 那行整行搬到表尾 | 红 | 红 1,报「`w148-integration-log.md` 之后排了 `w130-cycle9-audit.md`」;**同一手在基线上 504/504 全绿**(报的是「`w146-integration-log.md` 之后排了…」,即基线上这条判据同样在,本槽只是把表变长了) |
| M4 | 把本槽新插的 `w145` 那行改成追加到表尾(即"并集不归位"这条真实歧路) | 红 | 红 1,报「索引表应按波次号递增排列(新记账件插回它那个波次的位置,别追加到表尾;合并取并集时同样按号归位)」 |

## 5. 取证

### 5.1 合并纪律:按四棵树的文件哈希机检

对每次合并,取 `M`(合并结果)、`P1`(我方)、`P2`(对侧)、`B`(叉点)四棵树里同一文件的 blob 哈希做对照。三个冲突文件在两次合并里**全部四者互不相等**:

| 合并 | 文件 | `M` | `P1` | `P2` | `B` | 结论 |
|---|---|---|---|---|---|---|
| `81c6607` | `README.md` | `db5989c` | `407ff11` | `d0e4cfd` | `71ca689` | 真融合 |
| `81c6607` | `docs/skills-wave/README.md` | `7100e5e` | `763d076` | `c564b46` | `33a3830` | 真融合 |
| `81c6607` | `tests/unit.js` | `e3a7410` | `f76b89f` | `9231979` | `44b0921` | 真融合 |
| `b162dab` | `README.md` | `ca23ce0` | `db5989c` | `5e113bc` | `ca0fc2d` | 真融合 |
| `b162dab` | `docs/skills-wave/README.md` | `5361997` | `7100e5e` | `e45d8f0` | `fc145f3` | 真融合 |
| `b162dab` | `tests/unit.js` | `ba48fab` | `e3a7410` | `c27607b` | `89373ee` | 真融合 |

**整份取 `--ours` 零命中、整份取 `--theirs` 零命中、回退到叉点内容零命中。**
`js/release.js` 与 `js/plans.js` 各只在一次合并里进 diff、结果等于对侧,是单边改动的必然而不是取整份(另一次合并里它们一个字都没动);
两份新记账件是纯新增文件,同理。

### 5.2 三套件实跑

| 套件 | 基线 `25aa1c0` | 合入 W145 后 | 合入 W147 后(本槽 tip) |
|---|---|---|---|
| `node tests/unit.js` | 504 / 504 | 506 / 506 | **509 / 509** |
| └ `contract` 子套件 | 112 / 112 | 112 / 112 | **113 / 113** |
| `node tests/integration.js` | 130 / 130 | — | **130 / 130** |
| `node tests/cli.smoke.js` | 100 / 102 | — | **100 / 102** |

`cli.smoke` 那 2 项失败逐条现取,并在 `origin/master` 的独立 worktree 上同跑一遍对照,**同名同表现**:

```
FAIL | 未登录 whoami → exit 3 | exit=1
FAIL | llm --json mock 链路 | undefined
```

(`master` 那份的总数是 51/53——它还是二十三轮那个版本,套件本身小得多;比的是这两条的名字与表现,不是比总数。)

单元用例的**不同名字数**现取 509,与用例数 509 相等——**连续第十三槽全部相等**。

### 5.3 没删测:按名成集双向比对

在基线与合并后 HEAD 上各开 worktree 跑一遍全套,把用例名(含 `套件 · 用例名` 前缀)成集比对:

- **只在基线里有的:0 条**(即一条用例都没被删掉、没被改名);
- 只在合并后 HEAD 里有的:**恰好 5 条**,逐条点名:
  - `release · 浏览器 collect · G4/G5/G6 聚合缺 Domain:三门如实 warn,不许照常印「0 镜 → 通过」`(W145)
  - `release · 浏览器 collect · G4/G5/G6 聚合半途抛错:三门同记 warn 带错因,不拿半截计数当全量报`(W145)
  - `plans · fromWorkflow:项目级前置三步的待办判定现取 Domain.gateBlockers(计划层不写第三份门槛拷贝)`(W147)
  - `plans · fromWorkflow:剧本这一步与流程条同口径(提取过主体的老项目不再一步都推不出来)`(W147)
  - `contract · 前置门槛第三份拷贝已消:js/plans.js 的项目级待办按 gateBlockers 的码取材,不自己判剧本/主体/分集`(W147)

504 + 5 = 509,与实跑总数对得上。

### 5.4 没摘 gaps、注册表零 diff

```
$ git diff --name-only 25aa1c0 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js
(空)
```

故七个数不用现跑也成立;为求稳仍在合并后 HEAD 上现取核对了一遍:
提示词注册表 **41**、能力短名单 **30**、`CHECKS` **17**、`preflightStages()` **7**、`gaps()` **20 键**(`G-03` 仍是 `review.stage` / `review.reviseLoop` 两条)、`KB.SECTIONS` **18**、`playbooks()` **5**;另有领域命令 **12**、专家 **16**。

与前两槽不同的是,**本槽不是零产品代码槽**:产品面进 diff 的恰好两个文件,且两者此前互不相干:

```
$ git diff --stat 25aa1c0 HEAD -- js/ server.js cli.js mcp.js
 js/plans.js   | 30 ++++++++++++++++++++++--------
 js/release.js | 32 +++++++++++++++++++++-----------
```

整槽相对基线的全部 diff:

```
 README.md                                   |   4 +-
 docs/skills-wave/README.md                  |   4 +-
 docs/skills-wave/w145-browser-gate-catch.md | 171 +++++++++++++++++++++++++
 docs/skills-wave/w147-plans-gates.md        | 163 ++++++++++++++++++++++++
 js/plans.js                                 |  30 +++--
 js/release.js                               |  32 ++++--
 tests/unit.js                               | 107 +++++++++++++++-
```

### 5.5 抽查变异:本槽新合进来的两条,各证是新覆盖

全部在**合并后 HEAD** 上跑,一次只改一处,跑完即还原(还原后复跑 509/509 全绿、工作区 clean)。

| # | 变异 | 合并后 HEAD | 基线 `25aa1c0` |
|---|---|---|---|
| M5 | `js/release.js` 里把缺模块守卫退回 `if (typeof Domain !== 'undefined' …)` 形态(即 W145 改前那一半) | **红 1**,报「`g4-stale` 判不出来不得报 pass:期望 `warn`,实际 `pass`」 | 基线那份 `js/release.js` 还是空 `catch` 形态,把那个 `catch (_) {}` 改一个字仍 **504/504 全绿**——即浏览器那半在基线上零判据 |
| M6 | `js/plans.js` 里把拆集步退回内联 `String(p.script \|\| '').trim() && !(p.episodes \|\| []).length` | **红 3**:桩那条报「门槛派生说没有前置断点,计划层就不该自己再判出一步」、同口径那条报老项目少了拆集步、源级那条报「不得再自己判整本原文在不在」 | — |
| M7 | `js/plans.js` 里把码名分裂成 `subject-no-image` | **红 6**,其中 **4 条是既有用例**(补图步整步静默筛空,与"这步没待办"一模一样),源级那条另点名「不是 `Domain.gateBlockers` 会回的码」 | — |

### 5.6 反向抽查:任务点名要保的五样,逐样变异一次

| 要保的 | 反向变异 | 实测 |
|---|---|---|
| `gaps()` 键集判据(W140) | `js/skills.js` 里把 `gaps: ['S-06']` 改名成 `['S-99']` | **红 1**,报「gaps() 键集应逐字对齐…期望 `…,S-05,S-06,S-07`,实际 `…,S-05,S-07,S-99`」——两串都印出来;**同一手在基线上也红 1**,故判据未被本槽削弱 |
| `report-if` 白名单(W141) | 把 `tests/integration.js` 一条 `report(...)` 原样搬进 `if (process.env.MV_NEVER_SET) { … }` | **红 3**,含 W141 那条报「块链上只许有 main 函数体与裸 `{ }` 分节块…实际 `tests/integration.js:147 外层「if (process.env.MV_NEVER_SET) {」`」 |
| 索引行序(W144) | 把 `w130` 那行整行搬到表尾 | **红 1**,点名「`w148-integration-log.md` 之后排了 `w130-cycle9-audit.md`」;同一手在基线上同样红 1(4.4 的 M3) |
| `Domain.gateBlockers` + `Issues.gates`(W138) | 门槛投影表摘掉 `no-script` 一条 | **红 3**:空项目三条前置断点少一条、两侧码集双向相等那条、G2 不改判那条 |
| `release-core` `aggErr`(W137) | 缺注入守卫退回 | **红 1**,报「`g4-stale` 判不出来不得报 pass:期望 `fail`,实际 `warn`」 |
| 同上 | 遍历 `catch` 退回 `catch (_) {}` | **红 1**,报「`g4-stale` 遍历抛错时按判不出来记 warn(不是拿半截计数下结论):期望 `warn`,实际 `fail`」 |

五样在本槽 tip 上**都还在、都还有判据护着**;`aggErr` 那两格**各守一格、报错句不混**,与 W146 记的"整段退回红 2 条"是同一读数拆开量。

### 5.7 本槽自己改了什么

除两个合并提交解冲突外,本文这一提交动了三处:新增本文、`docs/skills-wave/README.md` 补索引行并把明写份数 161 → 162 与最新收敛记录指针改到本文、`tests/unit.js` 的记账件 `FLOOR` 161 → 162。
单元用例数与 `contract` 条数**本文一条不加**,故 509 / 113 不动。
两个合并提交里**没有删过任何一条用例、没有放宽过任何一条断言、没有改到任何一条别人的判据**(3.3 与 5.3)。

## 6. 残留与欠账

1. **W146 残留 9 那句"下一槽会与 W137 落地的两条用例撞车"实测不成立**,成因已量清楚(1.1):
   两端的用例落在不同文件、用不同夹具、各自只断言自己那三门。
   由此可把 W142 那条经验收窄成:**"代码合得上、判据合不上"只发生在有一条断言同时读两边产出的时候**,与"改的是不是同一件事"无关。
2. **W145 自己登记的两处空 `catch` 仍开着**:`js/release.js` 的 `buildReleaseZip` 抓分镜文件那处(失手少几个文件而 `summary` 不提)、底部 `Bus.on('*')` 那处。本槽未动,理由是不重做已落地之外的功能。
3. **门槛判定现在有三个消费方,三张投影表形状各不相同。** 判据已收成 `Domain.gateBlockers` 一份,但"某个码该不该投、投成什么危险级/什么文案"仍是三处各自决定,
   且**没有任何一条判据管"新加一档阻塞码时三个消费方要不要跟"**——W147 的源级那条只钉住计划层用的码必须是派生会回的码,反向(派生新加一码而计划层漏投)零判据。
4. **W147 如实登记的那条行为余面本槽未收**:`extractDone && !script` 的老项目现在推得出计划,但走到 `project.splitEpisodes` 会回 `blocked / no-script` 并被 `execStep` 记成 `failed`。
   它比基线的"无待推进事项"好(把假话换成指向该做那件事的真话),但那一步仍然跑不成。
5. **W142 残留 1(那句 `assertEq` 与 W137 共享 `fails` 计数器)本槽未动**,耦合照旧:失败含义不混、失败条数会串。
6. **W142 残留 4(G1 与 G4–G6 是两套写法)本槽未动**;现在两端的 G4–G6 各自收敛了,但两端**取值有意不同**(浏览器 `warn` 判"页面模块没加载"、headless `fail` 判"调用方接线错"),这一差异有 W145 与 W137 各自的用例钉着,不是欠账。
7. **散文那半仍没有判据。** 本槽两处散文改动(3.1 与 3.2 的两条单元整句)都进了冲突块、`git` 都请我过目,全槽零处静默取对侧;
   但响不响仍旧只取决于"那段散文与一个每槽必改的数字在不在同一行",**至今没有任何一条用例读那段话**。
   本槽另量出这条的一个新形态:3.2 那处我按上一块的形态外推而漏了对侧的插入,是**机检之外的人为口子**——判据管不到"合的人偷懒",故 3.2 末尾把操作口径写死成"每块都先机检"。
8. **W144 的行序判据仍不管同号内的先后。** 本槽索引表里没有同号多份的情形,故这一面本槽没有实测机会。
9. **W140 留的那个人工出口本槽未动,也未收窄。**
10. **SK-25 的两个余面仍欠**(收敛次数 `maxRetry` 的登记口径、两端闭环形态不同构)。W136 之后无人动。
11. **远端一支在飞未合**:`w143`(evolve headless)`@8c657bf`,按任务口径不合(仍在飞)。
