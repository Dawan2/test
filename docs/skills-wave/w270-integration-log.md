# W270 集成记账:W269 单镜审片四处展示面镜号取实位并进集成线

一条槽并进同一条集成线,一次真 `--no-ff`。本文只记集成面的读数与判断,槽自身的病灶/改法/变异见同目录 `w269-review-shotno-single-shot.md`。

**本槽是集成线上第五条「快进形」**(前四条是 W262、W264、W266、W268),退化程度与 W266/W268 那两次同档:对侧的 `merge-base` 现取**等于我方尖那个提交本身**——`B` 与 `P1` 是同一个 SHA。W262 §三 立下、W264/W266/W268 照办的那条规矩在本槽第五次照办:**树比对这类证据整体归零(它们在快进形下恒真),「有没有丢东西」改由变异 + live 实测承载**。

本槽与 W268 那次的差别不在拓扑而在**对侧改动落点**:W268 那次对侧动的是我方那半的**上游**(`Domain.shotNo` 长在 `Domain.rowIndexOf` 上),本槽对侧动的是**我方刚改过的同一个文件里的另外四处**(`js/review.js`,W267 在这个文件里收过整集那两块面)。同文件不同函数,故本槽要核的是**两层判据在同一文件上是叠着还是互相顶替**——对侧自己已用一手反向变异量过(M7),本槽照跑一遍核报错句;而本槽真正新增的一格是**对侧新立的那句复合判据可不可分**,见 §四 M8。

## 一、拓扑与 SHA(全部现取)

| 位 | 交接自称 | 现取 | 核对 |
|---|---|---|---|
| 我方尖(P1,W268 集成线) | `de04d78` | **`de04d78`** | `git fetch` 后 `rev-parse origin/cursor/w268-integration-2635` |
| 被合入支(P2,W269) | `1c73d0d` | **`1c73d0d`** | `rev-parse origin/cursor/w269-shotno-review-8f21` |
| 叉点(B) | 交接说 W269 从 w268 尖 `de04d78` 起支 | **`de04d78`** | `git merge-base P1 P2` 现取 |

**两个自称全对**,且 `B == P1` 现取成立于**提交级**(`de04d78e82e6ef75d72901774a717dc559dcf4ba` 一个字符不差),`git merge-base --is-ancestor P1 P2` 现取为真——快进形坐实。

`P1..P2` **两条提交**:`4e61497`(产品改动)+ `1c73d0d`(记账),故有「自称尖是不是链尾」这一格可核,现取核过:`1c73d0d` 是 `origin/cursor/w269-shotno-review-8f21` 的链尾,不是链中间某个提交。

新支 `cursor/w270-integration-af81` 从 **`de04d78`** 拉出,`checkout -b` 之后立刻 `rev-parse HEAD` 现取等于 w268 尖。

merge parents 现取:**`de04d78` + `1c73d0d`**,merge commit **`6abf438`**。`--no-ff` 明给,git 报 `Merge made by the 'ort' strategy`、**零冲突**。快进形下 git 本可以直接快进,`--no-ff` 是为了在集成线上留住这个合入点。按明令**没用** `--ours`,**没用** `git checkout <old> -- .`。

## 二、四棵树:两个不同值,`B` 与 `P1` 连提交都是同一个

```
B  de04d78e82e6ef75d72901774a717dc559dcf4ba   ← 提交级 == P1
P1 de04d78e82e6ef75d72901774a717dc559dcf4ba
P2 1c73d0d26f57af790ec8466b1b37a5c734015b8a
M  6abf43897cd689cdf3c4baaf3599dd83b6cf5f88   ← tree(M) == tree(P2) 逐字节(5a15667)
```

逐文件同样只有两档(`P2` 相对 `B` 真有改动的 5 个,其余四树全同,树内文件总数 563 → 564):

| 文件 | `B`/`P1` blob | `P2` blob | `M` blob | 读数 |
|---|---|---|---|---|
| `README.md` | `6d22bbb` | `00dd40f` | `00dd40f` | 整份取对侧 |
| `docs/skills-wave/README.md` | `ed9293a` | `0dc7740` | `0dc7740` | 整份取对侧 |
| `w269-review-shotno-single-shot.md` | 不存在 | `040a8d5` | `040a8d5` | 对侧新增 |
| `js/review.js` | `8cd8be8` | `ff354ad` | `ff354ad` | 整份取对侧 |
| `tests/unit.js` | `3c21407` | `438036d` | `438036d` | 整份取对侧 |

`P1→M` 与 `B→P2` 的 numstat 逐行相同。**以上全部读数在快进形下恒真**:`B == P1` 时 `M` 只能等于 `P2`,零冲突也是必然而非"没冲突"这条信息。故它们一格结论都不承载,只作拓扑登记——本文的结论全部落在 §三 与 §四。

## 三、数字(全部 live 现取,合入前先在我方尖上取基线)

按 W268 §五 立的规矩:基线在 `de04d78` 上**现跑**,合完再跑一遍,`+N` 是跑出来的差,不从对侧自称里读。

| 套件 | 我方尖 `de04d78` 基线(现跑) | 合入后 `M` 现跑 | 差 |
|---|---|---|---|
| `node tests/unit.js` | **686** | **688/688** | +2 |
| `node tests/unit.js contract` | **149** | **150/150** | +1 |
| `node tests/unit.js skills` | **100** | **101/101** | +1 |
| `node tests/integration.js` | **152** | **152/152** | 0(服务端与 CLI 一字未动) |
| `node tests/cli.smoke.js` | — | **115/117** | 单独整跑 |

**W268 自称 unit 686、W269 自称 688,两个数字一个没抄**:686 是我在 `de04d78` 上现跑出来的,688 是合完现跑出来的,两次都在同一台机同一套 env 下取。差额 `+2 / +1 / +1` 与对侧加测表(`skills` 一条行为面 + `contract` 一条源级)逐条对得上——加测两条而 `unit` 总数 +2、其中 `contract` +1、`skills` +1,三个数自洽。

`cli.smoke` 按明令**单独整跑**、`env -u HUJING_SERVER` 并连同 `MV_BASE` / `MV_KEY` / `MV_MODEL` / `MV_APPID` / `MV_TOKEN` 一并 unset 隔离:**115/117**,两条失败为 `未登录 whoami → exit 3`(`exit=1`)与 `llm --json mock 链路`(`undefined`)。同一 env 下现取 `master`(**`9adcf0f`**)跑 **51/53**,失败两条**逐字同名同表现**——按明令属允许的两条 master 同名失败,不是本槽带进来的。

`node --check` 过:`js/review.js`、`tests/unit.js`。按明令**不跑** `tests/e2e.js`。

## 四、变异八手(逐手在已提交的树上做、跑完 `git checkout --` 还原,跑完 `git status` 现取干净)

快进形下 diff 是空集,"没丢东西"这句话只能由变异承载。M1–M7 逐手复现对侧自称,M8 是本槽新增的一手。

| 手 | 改法 | 红几条 | 与对侧自称核对 |
|---|---|---|---|
| M1 | 页头退回 `视频 #${epIdx}-${s.order + 1}` | **2** | 逐字对上(`contract`「报告弹窗页头不许再按 order 字段出镜号(增删镜后与实位漂移):期望 0,实际 1」+ `skills`「报告页头的镜号应报第三镜(读 order 字段时是 #1-1)」) |
| M2 | 只退**导出抬头** | **2** | 逐字对上,`skills` 那句实际值是 `项目:逆袭 · 分集:第一集 · 镜头:1` |
| M3 | 只退**导出文件名** | **2** | 逐字对上,`skills` 报「导出文件名与抬头同源同号,实际:审片报告_逆袭_第一集_镜头1.txt」——与 M2 是同一条用例里的**另一句** |
| M4 | 审片记录标题退回 order | **2** | 逐字对上(`skills`「期望 "审片记录 · 镜头 3(1)",实际 "审片记录 · 镜头 1(1)"」) |
| M5 | 拦截提示退回 order | **2** | 逐字对上(`skills`「期望 "镜头3 视频仍在生成中,…",实际 "镜头1 视频仍在生成中,…"」) |
| M6 | **越界**:一键审片**任务名**也改成 `Domain.shotNo` | **1** | 逐字对上:`contract`「js/review.js 里读 order 出号的应恰剩 4 行(一键审片与一键优化各自的任务名与扣费摘要):期望 4,实际 3」 |
| M7 | **反向**:W267 收过的整集清单行退回 `x.shot.order + 1` | **3** | 逐字对上,且其中两句与 W268 §四 M2 **逐字相同**(`期望 "1,2,3,4",实际 "8,8,1,4"`);第三句是本槽那条全文计数顺带抓到的「期望 4,实际 5」 |
| M8 | **本槽新增·可分性**:把任务名那处的 order 读法**原样提到上一行**(`const tName = …;` 再 `target: tName`) | **1** | 见下 |

### M8:那句复合判据的两半是不是各自都活着

对侧新立的边界判据是**一句复合**:`js/review.js` 全文读 order 出号的①**恰 4 行**,且②**逐行必须落在 `Tasks.start(` 或 `U.charge(` 上**。M6 打的是第①半(4 → 3),第②半此前**没有任何一手单独量过**——而合入时真丢的往往正是复合语义里的某一半:另一半还在、跑起来全绿、读起来像"这条判据在"。

M8 专打第②半:把 `target:` 里那段 `${s.order + 1}` 原样提成上一行的 `const tName`,**全文 `.order + 1` 的行数仍然恰好是 4**(变异跑批里顺带打印了这个数,现取 = 4,第①半恒绿),而那一行不再含 `Tasks.start(` 或 `U.charge(`。现取**红 1**,报的是:

> `剩下的 order 读法只许在任务名与扣费摘要这类落库文本上,展示面一处都不许留:const tName = \`${ep.title}·镜头${s.order + 1}\`;`

两点读数:一是第②半确实活着,不是靠第①半顺带绿的;二是它的报错句**把犯规那一行原样抄了出来**,与 M6 那句纯计数报错读起来完全不同——两半失败各有各的判词,合入后"落库文本这条边界"是两个方向、两句判词都钉住的。这正是明令要的那手:**只破坏复合语义里的一半**。

M1–M5 五手都红 2,单看数字分不出彼此,值钱的是报错句——五手报的是五处不同的面(页头/抬头/文件名/标题/提示),M2 与 M3 落在同一条用例的两句断言上。M7 证明我方那半(W267/W268 落下的整集两块面)在对侧于**同一个文件**上加完东西之后照旧咬得住,报错句与 W268 现取记录逐字相同——两层判据在同一文件上**叠着**,不是互相顶替。

## 五、保留项(三树关键词点数,`M` 与 `P2` 逐格相等、无一格掉到 `P1` 之下)

| 关键词 | `P1` | `P2` | `M` | 关键词 | `P1` | `P2` | `M` |
|---|---|---|---|---|---|---|---|
| `landed` | 150 | 152 | **152** | `stateDigest` | 55 | 55 | **55** |
| `nthShot` | 88 | 88 | **88** | `lastRep` | 33 | 33 | **33** |
| `nthOf` | 24 | 24 | **24** | `mergeReviewPerShot` | 46 | 46 | **46** |
| `pickerNarrowHits` | 41 | 41 | **41** | `reviewRows` | 55 | 55 | **55** |
| `dupRowsNote` | 108 | 108 | **108** | `openReviewSummary` | 23 | 24 | **24** |
| `shots-dedupe` | 173 | 175 | **175** | `shotNo` | 48 | 76 | **76** |
| `floorLagVerdict` | 56 | 56 | **56** | `rowIndexOf` | 62 | 65 | **65** |
| `state-put` | 165 | 167 | **167** | | | | |

变的四格(`landed` +2、`shots-dedupe` +2、`openReviewSummary` +1、`shotNo` +28、`rowIndexOf` +3、`state-put` +2)全是对侧记账件与新判据引入的净增,**没有一格是减**。按 W268 §五 记的那条口径重申:**关键词点数只量得出"引用还在不在",量不出"那些引用的行为判据还红不红"**——后者由 §四 M7 那手反向变异承载,不由本表承载。

本槽点名要保留的几项另有现取核对:`shotNo` 含本槽新并进的四处展示面(`js/review.js:196` 拦截提示、`:241` 页头派生、`:349` 导出派生、`:731` 记录标题),加上 W267 落的整集五处(`:654`/`:660`/`:703`/`:718` 与 `openReviewSummary`),全文现取九处;`stateDigest` 的 `t.order-1`、`lastRep` 按行、`mergeReviewPerShot`、`reviewRows`、`dupRowsNote` extra、`pickerNarrowHits`、`nth*`、`floorLagVerdict`、`shots-dedupe` 一处未触。

## 六、棘轮与账面(现取)

- `gaps()` **20 键**一个没剥:`P1` / `P2` / `M` 三树逐树现算均为 20。
- `GUARD_TOPICS` **19** / `GUARD_TOPICS_CLOSED` **0**(空表)/ `TOPIC_FLOOR` **19** / 花名册 **19**,四者对齐(本槽没登记新主题、没销号)。
- `SLACK` = **3**,一格没用掉;五格差额:`单元测试` 688−688、`集成测试` 152−152、`CLI 冒烟` 117−117、护栏主题 19−19 全为 0,记账件 285−285 为 0。
- 棘轮字面:`['单元测试', 688, …]`、`['集成测试', 152, …]`、`['CLI 冒烟', 117, …]`、`TOPIC_FLOOR = 19` 均由对侧改到位,本槽未动;`const FLOOR`(记账件)**284 → 285**(本文一份),`docs/skills-wave/README.md` 明写份数 **284 → 285**,索引补本文一行(按波次号递增插在 w269 之后)。
- 根 `README.md`:单元用例数 688 与 `contract` 自报 150 由对侧改到位,本槽零加测故一个数字未动。

**三处账面各喂一手反事实,现取核过(跑完 `git checkout --` 还原)**:

| 反事实 | 现取 | 报的是 |
|---|---|---|
| CF-A 明写份数停在 284 而目录实有 285 | **红 1** | 三方对齐那条:「目录里的 wNN-*.md 份数应等于 README 明写的份数……:期望 284,实际 285」 |
| CF-B 记账件下限 `FLOOR` 停在 284 而实有 285 | **绿** | 见下 |
| CF-C 索引行漏补(文件在、行不在) | **红 4** | 逐份点名那条把本文的文件名原样报出:「记账件 w270-integration-log.md 在目录里但索引表没有它那一行(合入时漏登记,或只合到了漏掉索引行的那一版)」,另三条是双向对齐、三方份数与行序取数口各自报 |

**CF-B 绿是判据的设计而不是漏洞,这一格值得记明白**:下限那条是 `live >= FLOOR`,285 ≥ 284 本就该放行——它拦的是"往下走",不是"没跟上"。"下限停在旧水位"这一路由另一层管:`SLACK = 3` 那条钉的是**差额**(`live − floor ≤ 3`),此刻 285 − 284 = 1 仍在额度内,故本槽把 `FLOOR` 抬到 285 是**主动把差额还原成 0**,不是为了让某条判据转绿。这正是 `SLACK` 那条注释写的口径:常规加一槽不必动下限,连着几槽不抬才红。本文如实记下这一格,免得后来人把"抬下限"当成合入必修步骤、或反过来以为不抬会红。

CF-C 那条**红 4** 说明索引这一面是四层叠着的(双向对齐 / 逐份点名 / 三方份数 / 行序取数口),其中只有逐份点名那条报得出**是哪一份**漏了——另三条都只报数字对不上。合入时若只看红几条,分不出"漏了本文"与"漏了别的一份"。

## 七、残留(按明令一条没代修,原话逐条保留)

以下六条**一条没动**,原话照抄对侧 §六,现取核过的行号与点数在括号里注明:

1. **一键审片 / 一键优化的任务名与扣费摘要仍读 `order`**(对侧原话:"本槽有意"):`js/review.js:199`(`Tasks.start` target)、`:201`(`U.charge` 摘要)、`:366`、`:367` 四处。原话——"它们是任务中心与积分流水里的**落库文本**,换取法会让已落库的旧记录与新记录用两套号说同一镜。要收得先定'历史记录里的镜号该不该跟着表变'——那是记账口径不是展示口径。判据已把这 4 行钉成明面(§三 / §四 M6),收编时同轮改那一句。"现取四行行号逐行核过与原话相符,且**逐行确实落在 `Tasks.start(` 或 `U.charge(` 上**(§四 M8 单量过这一半)。

2. **分镜面 / 生成面 / 问题中心的「镜头 N」仍读 `order`**(W267 §六 4 起在册)。现取点数(`.order + 1` 出现行数)与对侧原话逐格相符:`js/sb-gen.js` 48、`js/sb-batch.js` 20、`js/batchops.js` 17、`js/sb-views.js` 13、`js/review.js` 4(全是上一条那四行,展示面 0)、`js/commands.js` 3、`js/sb-io.js` 3、`js/issues.js` 2、`js/wf-core.js` 2、`js/storyboard.js` 2、`js/sb-llm.js` 2、`js/agent-ops.js`/`js/produce.js`/`js/proj-clips.js`/`js/proj-planner.js`/`js/tasks.js`/`js/timeline.js` 各 1,另 `cli.js` 3、`server.js` 4;`js/skills.js` 那处对侧写"十余处 `(+s.order || 0) + 1`",现取**恰 11 处**(属回执口径)。原话——"收法现成(`Domain.shotNo` 就位),但这些面各有各的口径要定:生成面那批混着任务名/退费摘要/下载文件名,问题中心那批进的是 `detail` 文案(与命令回执同源),`js/skills.js` 与 `js/wf-core.js` 那几处是**回执/提示词里的镜号**(LLM 读的那份,与屏幕上给人看的不是同一件事),不宜一把梭。"

3. **浏览器闭环 `common` / `cut` 空壳 vs 服务端沿用**(W261 §六 2 起在册):原话"产品口径,未碰"。

4. **`js/review.js` 的 `openEpisodeReview` 整表覆盖与 generating 过滤**(W261 §六 3 起在册):原话"同上,未碰"。

5. **同 id 多行的源头照旧没追**(W226 起在册):原话——"本槽仍只收消费面;`shots-dedupe` 一个字没碰,它明写的「引用面按 find 首行语义解析、首行留原 id」这条前提在本槽之后仍成立——本槽改的是'给人看的序号从哪儿取',不改任何按 id 的库形态解析。"

6. **库里的 `order` 字段本身仍可能与实位漂移**(明令不在本槽改):原话"本槽只让四处展示串绕开它"。

按明令,本文**不代修**上述任何一条,W269 的产品语义一个字未改。

## 八、交接

集成线现尖:`cursor/w270-integration-af81`,merge commit **`6abf438`**,parents **`de04d78`**(P1)+ **`1c73d0d`**(P2)。

本槽产出:一次 `--no-ff` merge(整份取对侧,5 个文件)+ 本文与索引/下限三处账面。`js/` 与 `tests/` **一字未改**(变异跑批逐手还原,`git status` 现取干净)。

未开 PR、未合 `master`、未开第三条功能支。

下一槽若要收编任务名与扣费摘要那 4 行,**同轮要改的是 `tests/unit.js` 里那句复合判据的两半**:①「恰 4 行」这个数字,②「逐行落在 `Tasks.start(` / `U.charge(` 上」这条落点词表——§四 M6 与 M8 分别量的就是这两半,改一半漏一半会当场红在另一半上。
