# W193 集成收敛记录:两条同叉槽各改 `js/domain.js` 一段而 git 一个冲突块不给、冒烟分母第一次真被抬起来

> 基线 `cursor/w189-integration-5b9b@a895ab8`;本槽分支 `cursor/w193-integration-f315`,tip 是本文这一提交。
> 三次 `--no-ff` 合并,取三支的远端实际 HEAD。

## 1. 结果一句话

三支并进同一条集成线:**W187** 让 `D.workflow` 的推荐动作**只在下游还要读整本原文时才把「下一步」留给剧本步**
(读它的只有提取主体与剧本拆集两件事,两件都办成的项目上剧本步让位给主线真正未完成的那一步),G1 门槛与
`gateBlockers`/`projectScript` 一个字不动;**W188** 新开 `Domain.emptyBatchNote`,把「为什么是 0 镜」那句话
收成双端单源,命令层与 CLI 空跑早退都带 `result.note`、`digest` 照读那一句播报,**空跑仍是 `ok` 不改判 `blocked`**;
**W190** 让 `js/issues.js` 的 `stale-shots` 明细接上 `Domain.staleSplitNote`,`count` 仍是 `c.stale`、照旧不挂 `cmd`。

本槽最要紧的两格:

1. **`js/domain.js` 两侧同文件不同段,`git` 一个冲突块都没给,而"机械取任一侧"两个方向都不是零**——
   W187 改的是第 785 行那一带挑推荐动作的一行判据,W188 加的是第 267 行那一带的新派生,相隔五百余行故自动合上。
   反方向实测:把 W187 那一侧冲掉 **红 3**、把 W188 那一侧冲掉 **红 4**(§5.1 的 M1/M2)。
   "自动合上"不等于"不必核",这是 W185 记过、W189 又记过一次的同一课,本槽第三次撞上且第一次是**两侧各加各的**。
2. **`cli.smoke` 的分母第一次在集成线上被抬起来:107 → 108**。W188 自称 106/108,而分母是不是 108 由合完 live 说了算——
   本槽实跑得 **106/108**,与它自称逐字相同,故 108 成立;若照抄 106/108 而 live 是 109 就成了假账,
   默认留 107 又会把 W188 真加的那条冒烟吃掉。两条与 `master` 同名的失败在 `master` 上现跑复核过(§5.2)。

| tip | 合并提交 | 这一槽做了什么 | 代码面 | 测试面 |
|---|---|---|---|---|
| `w187-recommend-vs-release-379a@322d8ff` | `1eb85f4` | 整本原文的缺口只在下游还要读它时才占「下一步」;判据取本函数已算好的门槛项,不另数一遍主体与分集 | `js/domain.js` +17 −1 | +1 `domain`、+1 `plans`、+1 `release` |
| `w188-digest-empty-ok-395a@52a81cf` | `bbab3fe` | `Domain.emptyBatchNote` 分堆说清一镜也没跑的原因;`digest` 见 `result.note` 即播;CLI 同读一份 | `js/domain.js` +30、`js/commands.js` +16 −3、`cli.js` +4 −1 | +2 `commands`、+1 `domain`、+1 `release`、+1 `contract`、+1 `cli.smoke` |
| `w190-issues-stale-split-4b4d@8fd0e2f` | `c1658fc` | `js/issues.js` 的 `stale-shots` 明细尾巴接 `staleSplitNote`;`count` 与发射条件一个数没动 | `js/issues.js` +10 −2 | +1 `issues`、`contract` 那条取数口改名重算 |

合并后回归 **`unit 586/586`**、**`integration 143/143`**、**`cli.smoke 106/108`**(2 项失败与 `master` 同名同表现,见 §5.2),
`contract` 子套件 **130/130**、`release` **47/47**、`commands` **37/37**、`domain` **34/34**、`issues` **21/21**、
`plans` **14/14**、`pipeline` **12/12**;
`GUARD_TOPICS` **19 条**、`TOPIC_FLOOR` **19**、花名册 **19 行**、`GUARD_TOPICS_CLOSED` **0 条**(三支一条没登记,见 §4);
记账件 **206 份**(含本文)。

治理面:`gaps()` **20 键**、`preflightStages()` **七面**,提示词注册表 / 能力短名单 / `playbooks()` / 领域命令 / 专家数
均未动 —— 本槽**注册表五个文件相对基线零 diff**
(`git diff --stat a895ab8 HEAD -- js/skills.js js/prompts.js js/knowledge.js js/cmd-registry.js js/experts-data.js` 空输出),
故这八个数是结构性成立,与 W189 / W185 / W182 / W179 同形。

**不在本槽射程内**(仍在飞,一行没碰):W191 审片循环、W192 api 回退。`master` 也没合。

## 2. 三次合并各自的形态

三次都是真 `--no-ff`,两个 parent 逐个记在这里:

| # | 合并提交 | parent 1(集成线) | parent 2(被合入支) | 叉点 | 冲突文件 |
|---|---|---|---|---|---|
| 1 | `1eb85f4` | `a895ab8` | `322d8ff` | `5f98d0b`(w182) | `README.md`、`docs/skills-wave/README.md`、`tests/unit.js` |
| 2 | `bbab3fe` | `1eb85f4` | `52a81cf` | `5f98d0b`(w182) | 同上三份 |
| 3 | `c1658fc` | `bbab3fe` | `8fd0e2f` | `fb58034`(w185) | 同上三份 |

W187 与 W188 同叉 `5f98d0b`,W190 叉在 `fb58034`。

**产品文件零冲突块,而三次的成色不同**——机检(比 `M` / `P1` / `P2` / `B` 四棵树的 blob)如下:

| 文件 | 合并 | `B` | `P1`(我方) | `P2`(对侧) | `M` | 判 |
|---|---|---|---|---|---|---|
| `js/domain.js` | 1 | `d4529ae` | `4f0eb02` | `cc3e215` | `38a0741` | **真三方**:四棵树两两不等 |
| `js/domain.js` | 2 | `d4529ae` | `38a0741` | `e539f2c` | `aaf075c` | **真三方**:我方已带 W187 那一段,对侧带 W188 那一段,合完两面都在 |
| `js/commands.js` | 2 | `d391ffe` | `d391ffe` | `9dab0c7` | `9dab0c7` | **不是真三方**:`P1 == B`(我方自 w182 起一字未动),`git` 直接取对侧 |
| `cli.js` | 2 | `082fcc4` | `082fcc4` | `4d4209f` | `4d4209f` | 同上 |
| `tests/cli.smoke.js` | 2 | `4a2734b` | `4a2734b` | `209faf7` | `209faf7` | 同上 |
| `js/issues.js` | 3 | `20ac99a` | `20ac99a` | `175098b` | `175098b` | 同上(我方自 w185 起没碰) |

三个冲突文件在**三次合并里都是真三方**(九个文件次,一个 `M == P1` / `M == P2` 都没有)。
四处 `P1 == B` 如实登记为"我方没碰",不冒充成并集。

### 2.1 `js/domain.js` 两面怎么留的 —— 本槽的主要看点

合完的实况现取:

```
276:  D.emptyBatchNote = function (p, ep, picked, online) {          ← W188
801:    const scriptIdle = !gates.some(g => g.code === GATE.subjects) && !gates.some(g => g.code === GATE.eps);
803:    const cur = steps.find(s => !s.side && !s.done && !(s.key === 'script' && scriptIdle));   ← W187
```

相对基线的整份 diff 只有这两段(`+47 −1`),`gateBlockers`、交付门十项、`projectScript`、`extractSourceText`
一个字未动 —— 即 W187 自称的「G1 门槛不放宽」在集成线上现取成立。

**两侧都要留,不是形式**。反方向各取一侧的变异实测(§5.1):

- 机械取 W188 那一侧(把第 803 行退回 `steps.find(s => !s.side && !s.done)`)→ **红 3**,
  分落 `domain` / `plans` / `release` 三个套件,其中 `plans` 那条报的正是"两面对同一项目说反话"。
- 机械取 W187 那一侧(把 `emptyBatchNote` 掏空)→ **红 4**,分落 `commands` ×2 / `domain` / `release`。

两个方向都不是零,故"自动合上"这件事本槽是**核过的**,不是默认可信。

### 2.2 `tests/unit.js` 的用例块冲突怎么并的

第二次合并给了两处用例块冲突,形状一模一样:**两侧各在同一锚点往下追加用例条目**,
而冲突块之后那一行 `} },` 是两侧共用的收口——机械去掉三行标记会让我方最后一条用例没有收口、
对侧第一条 `{ name:` 直接顶上来,当场语法错。取并集时在两块之间**补回一行 `} },`**:

- 处 1:我方是 W184 的两条 CLI 行为面(`CLI exec generateVideos:…`),对侧是 W188 的两条空跑回执;
- 处 2:我方是 W180 的 `epFixOf` 分档,对侧是 W188 的 `emptyBatchNote` 分堆。

四批用例合完全在,`node --check` 与实跑双向复核(§3)。

### 2.3 `README.md` 那句长行:先证超集,不是超集就做外科

主 `README.md` 那句一万五千字的单行按 `;**` 切段做**多重集**比对(不 unique-sort)。三次的读数不同:

| 合并 | 我方段数 | 对侧段数 | 只在对侧 | 判 |
|---|---|---|---|---|
| 1(W187) | 21 | 19 | 7 段 | **我方不是超集**:其中三段是 W187 真加的,做外科插回 |
| 2(W188) | — | — | 5 段 | 我方是超集(只差断言数与两段已被翻面的旧说法),取我方 |
| 3(W190) | — | — | 5 段 | 同上,取我方 |

第 1 次那三段各按锚点逐段插回(全程没用过 `--ours`、没用过 `checkout <old> -- .`):

1. 计划层那句「与「下一步」不许对同一项目说反话」,插在 `全齐备返回 null` 之后;
2. 领域层那句「整本原文的缺口只在下游还要读它时才占「下一步」」,插在 `workflow 项目级步骤与推荐动作` 之后;
3. 发布门那句「走完全片但没有整本原文」,插在 `overall cond-pass(单 G10 warn)` 之后 —— **这一段差点漏掉**:
   它没有单独成段落进"只在对侧"名单里,而是被并进了一个因两侧插入位不同而**边界位移**的段里,
   逐段做前后缀对齐才露出来。归口一句:**段级多重集只报得出"整段独有",段内的插入要再对一次前后缀**。

三次都有意**不取**对侧那段「Bus 通配订阅:一条主线事件只转一次 `release.dirty`」——
W183 已把那条转发整段摘掉、W189 把留痕面按新实况重算过,取回来就与产品码打架。
同理第 3 次不取对侧那段「底部那处空 `catch` 的覆盖面按源级 + 行为两面钉着」(W189 已改写成「…仍按…钉着,只是随注册一起摘了」)。

### 2.4 目录索引取并集并按波次号归位

`docs/skills-wave/README.md` 三次都是同一形状:份数那一行两侧各写各的、索引行两侧各加各的。
份数按**合完 live** 逐次重测(203 → 204 → 205,含本文 **206**),索引行取并集后**按波次号归位**
——`w187` 插在 `w186` 与 `w189` 之间、`w188` 插在 `w187` 与 `w189` 之间、`w190` 插在 `w189` 之后,
不追加到表尾(W105 那条索引行序判据现管着这件事)。

## 3. 数字全部合完 live 重跑

**三支自称的数一个都没抄**,逐格现取:

| 格 | 基线(W189) | W187 自称 | W188 自称 | W190 自称 | 合完 live | 备注 |
|---|---|---|---|---|---|---|
| `unit` | 577 | 564 | 566 | 570 | **586** | 三支相对各自叉点新增用例名 3 / 6 / 2,其中 W188 有 1 条落在冒烟;单元这一格净 +10 −1(W190 那条改名)= +9,577 + 9 = 586 |
| `contract` | 129 | 128 | 129 | 128 | **130** | 只有 W188 真加了一条(它自称 129 是相对叉点 128);W190 那条是取数口改名重算,不是新增 |
| `integration` | 143 | 143 | 143 | 143 | **143** | 三支都没碰,仍实跑复核 |
| `cli.smoke` | 105/107 | 105/107 | **106/108** | 105/107 | **106/108** | 分母按 live 抬到 108,见 §1 第 2 点 |
| 记账件 | 202 | 196 | 196 | 199 | **206** | 含本文 |
| `GUARD_TOPICS` / `TOPIC_FLOOR` / 花名册 | 19 / 19 / 19 | 同 | 同 | 同 | **19 / 19 / 19** | 三支一条没登记,见 §4 |

五格棘轮下限一并按 live 抬:`单元测试 586`、`集成测试 143`、`CLI 冒烟 108`、`记账件 FLOOR 206`、`TOPIC_FLOOR 19`。

**记账件份数的静默窗口连着第八槽出现**:W187 与 W188 同叉 `5f98d0b`,两支自称的份数**逐字相同的 196**;
W190 叉在 `fb58034` 自称 199。照抄任一个与 live 206 分别差 10 格与 7 格
——即"能被静默删掉的记账件条数"。这一格与 W189 / W185 / W182 记的是同一件事,原样留着。

### 3.1 按名成集(多重集,不 unique-sort)

用例名从三个套件取「套件 · 用例名」,名字按 `|` 切段(`report(...)` 那两套的名字里带 ` | ` 的证据段不算名字)。

基线 `a895ab8` **827 条** → tip **837 条**:

- **只在 tip 有 11 条**,与三支相对各自叉点的新增逐条对得上(W187 三条、W188 六条含一条冒烟、W190 两条),零遗漏;
- **只在基线有 1 条**:`unit · 过期镜分报的取数口:两端 G4 都现取 Domain 的分堆与那句话,谁也不自写第二份终稿判据`
  —— 这是 W190 自己做的**改名**(改成「两端 G4 **与问题中心**都现取…」,同一条判据多接了一个消费方),不是删测。
- **零吃测**(没有任何一条名字在合并中消失而无去向)。

## 4. 花名册与登记表:三支都没登记,故一条都不动

W187 / W188 / W190 三支相对各自叉点都**没有往 `GUARD_TOPICS` 里加登记**(`GUARD_TOPICS` 相对基线逐条相同),
故本槽**不为凑数去登记**:在册仍 19 条、销号台账仍 0 条、花名册仍 19 行、`TOPIC_FLOOR` 仍 19,差额 0。
W185 记的「登记表取并集这件事本身零判据」那一格本槽**没有新读数**(没有并集要取),也没有被收掉。

## 5. 变异抽查

全部在 tip 上现跑,改一处 → 跑 `unit` → 还原。

### 5.1 两侧各取一侧(`js/domain.js` 的并集值多少)

| # | 变异 | 红几条 | 红在哪 |
|---|---|---|---|
| M1 | 机械取 W188 那一侧:第 803 行退回 `steps.find(s => !s.side && !s.done)` | **3** | `domain`「走完全片的项目下一步是交付」、`plans`「期望 合成成片:第一集,实际 …」、`release`「期望 produce,实际 script」 |
| M2 | 机械取 W187 那一侧:`emptyBatchNote` 掏空成返回空串 | **4** | `commands` ×2(回执没点名跑不到的镜数 / 整集那一路的实话)、`domain`(开头报的点名数 `undefined`)、`release`(按钮按下去 0 条回音) |

两个方向都不是零 —— 这正是"两侧都要留"的取证,与 W185 在 `js/domain.js`+`js/release.js` 上量到的同形。

### 5.2 W189 残留③ 的闭环取证(`js/issues.js`)

| # | 变异 | 红几条 | 红在哪 |
|---|---|---|---|
| M3 | `stale-shots` 明细退回老形状(不接 `staleSplitNote`)= W189 残留③ 原样 | **2** | `issues`「明细尾巴应逐字是 `Domain.staleSplitNote` 那一句」、`contract`「`js/issues.js` 的过期镜段须现取…」 |
| M4 | `count` 从 `c.stale` 收窄成 `sp.rerun.length`(等于放宽发布门 G2) | **2** | `issues`「`count` 仍须是过期镜总数(含定稿过期镜)」、`contract`「过期镜总数照旧…」 |

M3 是**闭环那一格的读数**:同一手改法在基线 `a895ab8` 上是全绿(那正是 W189 把它记成残留的原因),
在 tip 上红 2 并点名。M4 证明闭环没有顺手把门槛放宽 —— **`count` 仍是 `c.stale`、发射条件与不挂 `cmd` 一个都没动**。

### 5.3 保留既有(高危项抽查)

| # | 变异 | 红几条 | 备注 |
|---|---|---|---|
| M5 | 把 `final` 从 `counts.stale` 里摘掉(放宽 G4 门槛) | **12** | W185 同一变异红 5、W189 红 7,本槽 **12**——W188 的空跑回执与 W190 的问题中心分报又各接上几格 |
| M6 | `digest` 拿到 `result.note` 也不播 | **2** | `commands`「唯独这句要播」、`release`「按下去 0 条回音」 |

另做源级在场普查(不进变异表,只作对账):W176 两端 `shotIds` 子集位、`staleShotSplit`/`staleSplitNote`、
W186 断点条印 rerun、W178 销号台账、`guardSpread` 四判词、W180 `epFixOf`、W183 全树零 `release.dirty` 发出点、
W181 `release-core.js` 零空 `catch`、`memWrite` 驱逐、`FORGE_SYS` getter(`js/experts.js:199` 仍是 `get FORGE_SYS()`)、
单一 `review.userSystem`、Issues UMD、`project.release`、`reviseRetryLimit`、`reviewGate` 一份、
`projectScript`+`extractSourceText`、`expert.evolve` 不在 `steps` —— **十七项逐条在场**。

### 5.4 `cli.smoke` 那两条失败

tip 上失败两条:`未登录 whoami → exit 3`(`exit=1`)、`llm --json mock 链路`(`undefined`)。
在 `master` 的独立工作树上现跑复核:**同名同表现两条**(`master` 那边是 51/53)。
`cli.smoke` 全程串行跑,没有并行。

## 6. 残留与欠账

1. **W189 残留③ 就此闭环**(§5.2):问题中心那条 `stale-shots` 现在有分报了。
   「回执说的和按钮做的对不对得上」这一面至此收到**四处**:发布门 G1、G4、断点条、问题中心。
   但**闭环的是"有没有分报"这一半,不是"处置该怎么办"那一半** —— `js/issues.js` 这条**本来就不挂 `cmd`**
   (报的是计数、处置是导航回分集页),故它没有"按钮承诺跑几镜"这个问题;真正欠着的是下面第 2 条。
2. **G4 那个按钮在"全是定稿过期镜"时仍原样挂着**(W185 残留 8、W189 残留 10 原样)——摘不摘它是产品口径。
   本槽 W188 把这一格**往前挪了半步但没收掉**:按下去至少现在有回音了(`emptyBatchNote` 那句话),
   而"按下去仍然一镜也跑不到"这件事本身没变,终稿仍不自动解锁。
3. **`Domain.emptyBatchNote` 那几句话仍是拼出来的中文字面**(与 W185 残留 7、W189 残留 12 的 `staleSplitNote` 同一格,
   且本槽让这一格**多了一份**):措辞一改,`commands` / `domain` / `release` / `cli.smoke` 四处期望串会一起红,
   分不出是有意改文案还是改错了。两句话现在共四个消费方,值得一提的是它们**没有**收进同一个注册表。
4. **`js/issues.js` 那条的 README 描述没跟着改**:W190 只改了断言数,`README.md` 里 `issues.js` 那段没有一个字讲分报。
   现取核实那段原本就没写 `stale-shots` 的明细形状,故**不是写错了、是没写**,不构成失同步,如实登记。
5. **`GUARD_TOPICS` / 花名册取并集这件事本身仍然没有任何判据管**(W189 残留 8、W185 残留 1 同一件事)。
   本槽三支都没往登记表里加东西,故这一格**没有新读数**,也没有被收掉。
6. **`TOPIC_FLOOR` 那一格的棘轮上限仍是 `SLACK` 3**(W189 残留 9 原样);本槽 live 19、差额 0。
7. **`js/episodes.js` 那处通配订阅至今没有行为面判据**(W189 残留 6 原样);**重绘面那条数的是"轮次"不是"画对了没有"**
   (W189 残留 7 原样);**`Bus` 的 50 格上限仍是写死的数**(W189 残留 5 原样)。
8. **段级多重集比对的固有面**(本槽新记):§2.3 第 3 段差点漏掉——两侧插入位不同会让段边界位移,
   位移段里的插入在"整段独有"名单上看不见。归口:长行比对要**段级多重集 + 同名段前后缀对齐**两步走,
   只做第一步会静默丢内容。本槽是靠第二步捞回来的,而**这一步今天全靠人跑,没有判据管**。
9. **`cli.smoke` 的分母抬起来这件事没有判据证明"抬对了"**:抬到 108 是因为实跑就是 108,
   而"实跑几条"与"该有几条"之间没有第三份记录 —— 若 W188 那条冒烟本身写错了、跑出来仍是 108。
   契约那条对的是**总数**不是通过数,这一格与 W179 记的是同一件事。
10. **W170 残留 7、W161 残留 4(弱断言普查)仍是零**;**`G-03` 的形态面、`G-11` 的自动进化面均未动**。
