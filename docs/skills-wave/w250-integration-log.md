# W250 集成记账:两条槽并进同一条集成线(W248 CLI 批量生视频按全表同 id 序数写回 + W249 选人闸收窄扫描改按语句 + 浅数据流)

一次开支、两次 `--no-ff` 真 merge,次序 W248 → W249。两条被合入支都从 W247 集成 tip 平行叉出去,产品面**只有一侧有**(`cli.js` 只有 W248;W249 产品码零改动,只动判据)。

- 叉点 `B` = `bfd4cca24e584cfaf7583b2362d1caad46f427ce`(W247 集成 tip,现取)
- merge 一 = `e9e29303cc82178fe0dd09af77e7f676f0ac41e9`,parents `bfd4cca` + `3931e5be0dda276d8dbf981c6b6f2f43d71aea59`
- merge 二 = `dcdfaa49838114b9d9d054d0bd39bbb41aa3325c`,parents `e9e2930` + `73d9578b29e7d4e7facfa8d441d8044bc7f80ec2`

`master` 没合、没开第三条功能支、没跑 `e2e.js`、没开 PR。

## 〇、交接自称的三个 SHA:两对一错,而错的那个是第十一次重演同一个形状

| 支 | 自称 | 现取 | 判 |
|---|---|---|---|
| W247(我方叉点) | `1be9907` | `bfd4cca` | **错**,且 `1be9907` 正是 `bfd4cca` 的父 |
| W248 | `3931e5b` | `3931e5b` | 对 |
| W249 | `73d9578` | `73d9578` | 对 |

`1be9907` 是 `merge(w246)` 那一次合并提交,其上还压着一条 `docs(skills-wave): W247 集成记账件 …校到 262` 才是分支 tip。**这已是第十一次「交接自称的 tip 是合入链中间那个 merge」**,而 W247 自己的记账件第〇节就用整整一节写着这条规律(它记 W244 时写「这已是第十次…」),下一轮交接照旧自称成了那个 merge。规律连着两轮被写进口径、连着两轮不生效,故本槽把它从「值得记一笔」降为**默认前提**:交接给的 tip 一律按「其后至少还有一条校数字 docs 提交」现取,不再逐轮论证。

反过来,交接对 W248/W249 的**叉点**自称(两支都从 `bfd4cca` 起手)现取核对是对的——`git merge-base` 两次都回 `bfd4cca`。即自称错的一直是「某支的 tip」这一类,「某支从哪叉出去」那一类没错过。

按 `1be9907` 开支的后果:那棵树上记账件是 261 份、记账件 `FLOOR` 与索引明写都还是 261,W247 那一轮校数字的活会被整个吞掉,而本轮记账件那格棘轮要从一个比 live 低 1 格的水位起抬——落在 `SLACK = 3` 里,一条判据都不会红(第三节 ① 档实测)。

## 一、两侧相对叉点各带来什么

| | W248(`3931e5b`) | W249(`73d9578`) |
|---|---|---|
| 产品面 | `cli.js` +13/−1 | **零**(判据层单改) |
| 判据 | `tests/unit.js` +86/−2(净 +1 条,落在 `commands` 套件) | `tests/unit.js` +78/−5(**净 +0 条**,两条既有 `contract` 用例就地换扫描) |
| 记账件 | `w248-find-shot-nth-row.md`(224 行) | `w249-picker-set-scan.md`(161 行) |
| 共同碰的 | 根 `README.md` L612、`docs/skills-wave/README.md`、`tests/unit.js` | 同左 |

W248 在 `cli.js` 新增 `nthShot(ep, sid, nth)` 并在 `episode.generateVideos` 的每轮重取上换掉 `findShot`,序数按快照**全表** `ep.shots` 排位数而不在待跑清单上数;W249 新开模块级 `pickerNarrowHits(skel, picked, target)`,把「待跑清单不许被按 id 收窄」那条源级判据从**逐行字面扫**改成**按语句取 + 三层浅数据流回溯**,镜头与主体两侧四处共用一份。

## 二、四棵树机检

### 2.1 merge 一:整棵树 `P1 == B`,零冲突是拓扑退化

`P1` 就是 `B` 本身(叉点即我方 tip,`--no-ff` 把一次本可 fast-forward 的合并按住成了真 merge 提交)。合完的树与 W248 tip 逐字节相同(`tree` 同为 `eb82beb`),故 merge 一之后的 live(unit 667 / 记账件 263)**恒等于 W248 的自称**,它证明不了 W248 的自称对——第一支合完的数字永远等于第一支自己量的数字。

### 2.2 merge 二:544 个文件,真形态 4 个

| 形态 | 个数 | 文件 |
|---|---|---|
| 四树全同的空壳 | 538 | — |
| 真 `P1 == B`(整份取对侧) | 1 | `w249-picker-set-scan.md` |
| 真 `P2 == B`(整份留我方) | 2 | `cli.js`、`w248-find-shot-nth-row.md` |
| 两侧都动过 | 3 | 根 `README.md`、`docs/skills-wave/README.md`、`tests/unit.js` |
| 其它 | 0 | — |

两个方向的整份取用**不一样多**(留我方 2、取对侧 1),这是「一侧带产品文件、一侧不带」的直接读数,与 W247 那一槽(各 2 个,两侧各碰一个产品文件)恰好成对照。`js/` 下 257 个文件里四树不全同的**一个都没有**;`cli.js` 四树取两个值;`mcp.js`/`server.js`/`billing.js` 四树全同。

交接说「`cli.js` 几乎只有 W248」——现取是**不带"几乎"**:W249 那棵树上的 `cli.js` 与叉点逐字节相同(它改的是扫 `cli.js` 的判据,不是 `cli.js`)。

反向对账两条都逐字节相同:`P1→tip` == `B→P2`(取对侧那一份)、`P2→tip` == `B→P1`(留我方那两份)——两侧带来的改动一格没丢也一格没串。

### 2.3 `numstat` 三向对账(减数先点名再相减)

| 文件 | `B→P1` | `B→P2` | `B→tip` | 配平 |
|---|---|---|---|---|
| 根 `README.md` | +1/−1 | +1/−1 | +1/−1 | 同一行两侧各改一次,并集仍是一行 |
| `docs/skills-wave/README.md` | +2/−1 | +2/−1 | +3/−1 | 各 1 行索引 + 份数行改写;`1+1+1 = 3` 增、份数行只删一次 |
| `tests/unit.js` | +86/−2 | +78/−5 | +163/−6 | 增 `86+78−1 = 163`、删 `2+5−1 = 6`,**减数与加数各自的那个 `−1` 都是记账件 `FLOOR` 那一行**(两侧同改的唯一一行) |

`tests/unit.js` 那行共同减数正是第四节的主题。

## 三、名集多重集:`666 + 1 + 0 = 667`,而第二支在名集上一个字都看不见

在四棵树各自的全绿快照上跑 `node tests/unit.js`,按 `|` 切出「套件 · 用例名」做多重集:

- `B` 666 条、`P1` 667、`P2` **666**、`tip` **667**;tip 名集去重后仍 667 条,**重名 0**
- `P1` 相对 `B`:**+1 −0** — `commands · episode.generateVideos 同 id 多行:每一轮的片落到本轮那一行(扣几笔就得有几行出片,两端同一个结果)`
- `P2` 相对 `B`:**+0 −0** — 名集与 `B` **逐条相同**,一条都没变
- 并集期望 667 条,tip 实测 667 条,**缺失 0、多出 0**

**这一槽最该记住的一格在这里**:`666 + 1 + 0 = 667` 这个算式成立,但它的第二项加的是零——W249 整支的产出**在名集这一层完全不可见**。它把两条既有 `contract` 用例的躯干换掉、各加一段判据自证,条数一条没动。故:

1. **名集对账是必要不充分的。** 它能证明 W248 那一条真进来了、没被合掉;它对 W249 是**恒真**——W249 一个字都没进来时名集也是 667、缺失 0、多出 0。同理,合完那句「667/667 全绿」对 W249 同样恒真。
2. 证明 W249 真落进去的只能是**别的东西**:第五节那两手变异(把去重挪到上一行),在合完的树上红 3 条、在 merge 一那棵树上只红 2 条——差的那一条正是 W249 收紧的射程。**换句话说,「就地换判据」这种槽的验收得靠变异演练,靠数数是数不出来的。**
3. 交接四个自称(W248 unit 667 / 记账 263,W249 unit 666 / 记账 263)**一对三错**,而哪个会对照旧可以事先算出来:一侧的自称恰等于 live,当且仅当**对侧在那个数上净增 0**。W249 在 unit 上净增 0,故 W248 自称的 667 对;W248 净增 1,故 W249 自称的 666 低一格;记账件两侧各新增一份,故两个 263 **都**低一格(合完 264,含本文 265)。「对」不是核过的结果,是**对侧没动**的结果。

## 四、棘轮:这一轮只有一格落后,而那一格照旧是静默的

两个棘轮字面的合成路径不同:

| 格 | `B` | `P1` | `P2` | git 静默合成 | live | 判 |
|---|---|---|---|---|---|---|
| 单元 `FLOOR` | 666 | **667** | 666 | 667 | **667** | 只有一侧改,git 取那一侧,**恰好等于 live** |
| 记账件 `FLOOR` | 262 | **263** | **263** | 263 | **264** | 两侧写出逐字节相同的值,git 连冲突面都没把人叫过来,**落后一格** |

单元那格这一轮之所以没落后,靠的是「对侧净增 0 所以对侧没改这个字面」——**是运气不是判据**:W249 只要顺手把它抄成 666(它那棵树的 live),git 同样静默,同样落后一格。

六档反事实逐档回读(变异真落进去之后跑判据):

| 档 | 改动 | 结果 |
|---|---|---|
| ① | 记账件 `FLOOR` 留在 263 | **667/667 全绿**,没有判据接住 |
| ② | 索引明写份数留在 263(`FLOOR` 已抬到 264) | 红 1:`contract · docs/skills-wave 索引完备性` |
| ③ | ①②**同时**留旧 | 红 **1**,与②同一条 |
| ④ | 单元 `FLOOR` 留在 666 | **667/667 全绿**,没有判据接住 |
| ⑤ | 根 README 断言数留在 666(`FLOOR` 已 667) | 红 2:`README 数字对账` + `三套件用例数只增不减` |
| ⑥ | ④⑤同时留旧 | 红 **1**:只剩 `README 数字对账` |

读出两句:

1. **`FLOOR` 那一层单独失守一律是静默的**(①④),因为它钉的是「差额 ≤ `SLACK`」而 `SLACK = 3`;接住的一律是**明写数字那一层**(②⑤),它钉的是**相等**。这与 W247 量到的一样。
2. **「抄得越齐,报警器越少」只在单元那一格重演,记账件那一格没有。** ⑤→⑥ 红 2 变红 1(`三套件用例数只增不减` 比的是 README 明写数与 `FLOOR`,两处一起留旧它就闭嘴);而 ②→③ 红 1 还是红 1——记账件那一侧本来就只有 `索引完备性` 一条现算 live 再比的判据在守,没有第二条一致性判据可供「抄齐后闭嘴」。**同一个形状在两格棘轮上强度不同**,故不能拿一格的反事实结论套另一格。

对照面照旧摆着:删掉两侧插进根 README 的那段散文、数字仍留 live,**667/667 全绿**——散文一层判据都没有,与 W244/W247 量到的一样。

## 五、冲突两处的解法(禁 `--ours`、禁 `checkout <old> -- .`)

- **根 `README.md` L612**(18121 → 18727 字符的长行,两侧各改):先把 `NNN 项断言` 换成占位符抵消两侧共同改动,再把两侧各自还原成「对 `B` 的纯插入」——机检确认 W249 插了**两段**(offset 8999 长 248、offset 9309 长 35)、W248 插了**一段**(offset 9523 长 323),三段**两两不重叠**且 base 侧零字符被改写;三处插入都应用回 `B`,断言数按 live 填 **667**。长度自洽:`18121 + 248 + 35 + 323 = 18727`。三处接缝各回读一遍:W249 第一段落在镜头侧「源级另钉住待跑清单不许被按 id 收窄」之后、第二段落在「主体侧选人闸同样按位筛一字未动」之后、W248 那段落在 `nthSubject` 主体段末尾接上镜头侧同形的一份,各自都在该讲这件事的地方。**这不是取一侧,是量出两侧各做了几处纯插入之后的并集。**
- **`docs/skills-wave/README.md`**:两行索引取并集,按波次号归位成 248、249;份数行两侧同写 263 未进冲突面,按三方(目录实况 / 索引表行数 / 明写份数)现取校到 **264**(本文落地后 **265**)。
- **`tests/unit.js`**:git 自动合并未报冲突(W249 换掉的两处躯干与 W248 新增的那条相隔很远),记账件 `FLOOR` 静默合成 263 后按 live 抬到 **264**;单元 `FLOOR` 只有 W248 改过,取值 667 恰等 live 未动。反向对账两个方向都做过:`P2→tip` 与 `B→P1`、`P1→tip` 与 `B→P2` 的 `tests/unit.js` 差异逐行相同,**唯一的差是那行 `FLOOR` 取的是 264 而不是任何一侧的 263**。

## 六、数字全部合完 live

| 项 | `B` | W248 自称 | W249 自称 | **合完 live** |
|---|---|---|---|---|
| `unit` | 666 | 667 | 666 | **667/667 PASS** |
| `unit commands` 套件 | 55 | — | — | **56/56** |
| `unit contract` 套件 | 145 | — | 145 | **145/145**(W249 就地换,不增条) |
| `unit release` 套件 | 51 | — | — | **51/51** |
| `integration` | 148 | — | — | **148/148 PASS** |
| `cli.smoke` | 115/117 | — | — | **115/117** |
| 记账件份数 | 262 | 263 | 263 | **265**(含本文) |

`cli.smoke` 单独整跑 `env -u HUJING_SERVER node tests/cli.smoke.js` 得 115/117,失败两条:

- `未登录 whoami → exit 3`(实际 exit=1)
- `llm --json mock 链路`

同一命令在 `master` `9adcf0f` 上现跑 51/53,失败的是**同样这两条、同名同表现**——本槽没有引入,也没有修好。

## 七、合完的树上两侧各自真跑一遍,并互证不干扰

**W248 侧**(`cli.js` `nthShot`):`nthShot` 定义在 L194(紧跟 L187 的 `nthSubject`,`findShot`/`findSubject` 那一对没被拆开)、调用在 L1149,序数表在 L1140–L1142 按 `ep.shots` **全表**记;`nthSubject` 一个字未动。判据由 `commands` 那条新用例五档钉着(点名 / 整集 / 首行已出片的整集 / 浏览器 / 并发改表),合完仍绿。

**W249 侧**(`pickerNarrowHits`):全树 9 处(定义 1 + 镜头侧消费与自证 5 + 主体侧消费与自证 3),两条既有 `contract` 用例各只换了取 `narrow` 那一行,选人闸 `ids.has(s.id)` 现取 **6 处**(`cli.js` 4 + `js/commands.js` 2)一个字没动——收紧的是判据的射程,不是产品行为。

**互不干扰,且有真实接触面**:W249 那两条 `contract` 用例扫的正是 `cli.js` 里 `episode.generateVideos` 与 `subject.generateImage` 两段的骨架,而 W248 恰好改的就是前一段。合完之后 W249 的扫描现跑在 W248 改过的那段躯干上——`narrow` 回空(没有误判成收窄)、`ids.has(s.id)` 那句前提断言仍在,两侧用例都绿。**即第二支的判据真的在读第一支的新代码,不是各扫各的。**

## 八、五档产品面反事实:各红各的,而两支的可判性不对称

| 档 | 改动 | 结果 |
|---|---|---|
| ⑦ | W248 全退(每轮 `findShot` 取首行) | 红 1:`commands · episode.generateVideos 同 id 多行` |
| ⑧ | W248 序数改在**待跑清单**上数 | 红 1,同一条用例但**红在另一句断言上**:「已出片的首行这一趟一个字不许被改写」(实报首行被 `v2.mp4` 盖掉、末行仍无片) |
| ⑨ | W249 全退(扫描退回只钉某一行的字面) | 红 2:`contract` 两条(各自那段判据自证) |
| ⑩ | 镜头侧选人闸把去重挪到上一行(`const seen = new Set()` 再 filter) | **合完的树红 3**:`commands` 两条行为面 + `contract · 点名的 id 占多行那句实话双端单源` |
| ⑩′ | 同一手打在 **merge 一那棵树**上(旧逐行字面扫,独立 worktree 现跑) | **红 2**:只有 `commands` 两条行为面,源级那条**静静放行** |

⑦⑧ 同红一条用例而报文不同,说明「按序数寻址」与「序数在全表上数」是那条用例里两档独立断言。

**⑩ 与 ⑩′ 的那一条之差,是本槽唯一能量出 W249 价值的读数**(第三节已说明名集与全绿计数对 W249 恒真):同一手改动,合入 W249 之后源级判据当场点名,合入之前只有行为面接住。而 ⑨ 说明这条收紧自带自证——判据退回钉字面时,`contract` 那两段自证立刻红,不会静静退化。

## 九、不代修的残留(现跑核过,原样留着)

1. **`nthSubject` 的两处兜底仍是判据盲区——与 W248 在镜头侧刚补上的那一档逐字同形。** 交接明令不代收,现取核实:把 `nthSubject` 改成 `return rows[nth];`(序数越界与同 id 一位不剩两处兜底一起摘)全树 **667/667 全绿**,一条判据都不红;同一手打在 `nthShot` 上立刻红 1(`commands · episode.generateVideos 同 id 多行`)。差别只在 W248 为镜头侧补了「并发改表」那一档(`withProject` 桩在末轮开跑前删行),主体侧没有对应档,故 `nthSubject` 的两行兜底至今没有任何一档走得到。收法就是照抄那一档到 `subject.generateImage` 上。本槽一个字没碰。
2. **`state-put` 不设闸**:`CMD['state-put']`(L1689)仍只有 `need(f.file, …)` 与 `--force`,整树原样落库是刻意的。没设闸。
3. **选人闸按行/按位筛**:`ids.has(s.id)` 现取 6 处(`cli.js` 4 + `js/commands.js` 2),一个字没动——W249 收紧的是**扫它的判据**,不是它。
4. **`SLACK`**:四棵树全是 3,没碰。
5. **`pickerNarrowHits` 的射程边界**:它只回溯本段内的声明、只走三层。把去重挪进另一个函数或挪去 `js/domain.js` 的派生里仍接不住(W249 自己的记账件已登记),行为面是最后那层。本槽不再往外扩。

## 十、保留面机检(合完的树上现取)

| 项 | 实况 |
|---|---|
| `Skills.gaps()` | **20** 键,一个没剥 |
| `GUARD_TOPICS` / `GUARD_TOPICS_CLOSED` | **19** / **0**,`TOPIC_FLOOR = 19`,四树相同 |
| `nthSubject` | 3 处,在(行为未动) |
| `nthShot` | 3 处,新入 |
| `dupSubjectRowsNote` | 24 处,在 |
| `shots-dedupe` | 41 处,在 |
| `floorLagVerdict` | 7 处,在 |
| `blankNonCode` | 31 处,在(W249 的新扫描就架在它上面) |
| `emptyBatchNote`(四堆按 id) | 40 处,在 |
| `shots-import` 写入闸 | 在(`cli.js` L735–L763),本槽 `cli.js` 改动落在 L188–L200 与 L1135–L1152 两段,与闸区间不相交 |
| `episode.produce` 提 note / MCP 对账 | 在(`integration` 148/148、`cli.smoke` MCP 段全绿,`mcp.js` 42 个工具四树同) |
| `js/` 全目录 | 257 个文件**四树逐字节全同**(本槽零改动) |
| 本文目录内相对链接 | **0** 处(索引行之外不链别的记账件,不借道散文链接) |

`node --check` 过:`tests/unit.js`、`cli.js`。按约定**未跑** `e2e.js`。
